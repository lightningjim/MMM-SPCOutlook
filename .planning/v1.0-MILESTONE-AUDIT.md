---
milestone: v1.0
audited: 2026-03-10T00:00:00Z
status: gaps_found
scores:
  requirements: 10/15
  phases: 4/5
  integration: 11/15
  flows: 4/5
gaps:
  requirements:
    - id: "SPC-01"
      status: "partial"
      phase: "02-cig-tier-support"
      claimed_by_plans: ["02-01-PLAN.md"]
      completed_by_plans: ["02-01-SUMMARY.md"]
      verification_status: "missing"
      evidence: "Phase 02 has no VERIFICATION.md. SUMMARY frontmatter lists SPC-01 as completed. REQUIREMENTS.md checkbox is [x]. Integration checker confirms the feature is correctly wired (cigToTier, cigComparator, torCig/hailCig/windCig fields confirmed). Gap is documentation only — VERIFICATION.md must be written."
    - id: "SPC-02"
      status: "partial"
      phase: "02-cig-tier-support"
      claimed_by_plans: ["02-02-PLAN.md"]
      completed_by_plans: ["02-02-SUMMARY.md"]
      verification_status: "missing"
      evidence: "Phase 02 has no VERIFICATION.md. SUMMARY frontmatter lists SPC-02 as completed. REQUIREMENTS.md checkbox is [x]. Integration checker confirms cigLabel() defined at MMM-SPCOutlook.js line 40, called at 7 display points (lines 73-91), mapping integers 0-3 to ①②③. Gap is documentation only."
    - id: "PERF-01"
      status: "partial"
      phase: "04-performance"
      claimed_by_plans: ["04-01-PLAN.md"]
      completed_by_plans: ["04-01-SUMMARY.md"]
      verification_status: "human_needed"
      evidence: "fetchGeoJsonCached() with ETag/SHA256-hash cache is implemented and verified statically (5/5 automated truths). Integration checker confirms stale flag (_stale: true) is produced and transmitted via sendSocketNotification but is never consumed by getDom() in MMM-SPCOutlook.js. When the module serves stale cached data during a network outage, the user sees no indication. The caching behavior is complete; the user-facing stale indicator is absent."
    - id: "QUAL-02"
      status: "partial"
      phase: "05-code-quality"
      claimed_by_plans: ["05-02-PLAN.md", "05-03-PLAN.md", "05-04-PLAN.md"]
      completed_by_plans: ["05-02-SUMMARY.md", "05-03-SUMMARY.md", "05-04-SUMMARY.md"]
      verification_status: "human_needed"
      evidence: "Phase 05 VERIFICATION.md confirms zero 'var' declarations. However, integration checker found node_helper.js line 104: 'result = turf.booleanPointInPolygon(loc, poly)' inside evaluatePolygons() assigns to an undeclared variable — an implicit global in live production code. QUAL-02 requirement states 'no implicit globals'. The grep check used 'var' pattern but missed bare assignments."
    - id: "QUAL-03"
      status: "partial"
      phase: "05-code-quality"
      claimed_by_plans: ["05-02-PLAN.md", "05-03-PLAN.md", "05-04-PLAN.md"]
      completed_by_plans: ["05-02-SUMMARY.md", "05-03-SUMMARY.md", "05-04-SUMMARY.md"]
      verification_status: "human_needed"
      evidence: "Phase 05 VERIFICATION.md claims QUAL-03 satisfied based on '//Log.' grep returning zero matches. However, integration checker found three complete commented-out function bodies at node_helper.js lines 896-967: checkDayCat, checkDayPerc, checkDaySign (~72 lines). These are the legacy loop functions replaced by extractPolygons/evaluatePolygons. QUAL-03 states 'Dead/commented-out code blocks removed from node_helper.js'. The grep pattern was too narrow."
  integration:
    - "evaluatePolygonsWeighted() defined at node_helper.js lines 120-147 but never called. Contains bugs: implicit global 'result' at line 126 reads from evaluatePolygons() global; typo 'lamba' at line 130 vs 'lambda' at line 139 (ReferenceError if called). Dead code with latent bugs. Affects QUAL-02, QUAL-03."
    - "evaluatePolygonsContinuous() defined at node_helper.js lines 157-180 but never called. Contains implicit global 'minDistTest' at line 165. Dead code. Affects QUAL-02, QUAL-03."
    - "fetchGeoJson() defined at node_helper.js lines 217-227. No longer called by any live code path — getMesoscaleDiscussion() uses fetchBinBuffer() for KMZ data. Dead code. Affects QUAL-03."
    - "_stale/_staleAsOf fields produced by node_helper.js (lines 641, 796) and transmitted via sendSocketNotification but never read by getDom() in MMM-SPCOutlook.js. No user-visible stale indicator when cache serves outdated data. Affects PERF-01."
  flows:
    - "Stale cache indicator flow: getSpcOutlook() → _stale:true in return object → sendSocketNotification payload[0] → this.spcrisk._stale stored → getDom() never reads it. Break point: getDom() has no branch for _stale or _staleAsOf. User sees normal risk display with no staleness warning during network outages."
tech_debt:
  - phase: "02-cig-tier-support"
    items:
      - "VERIFICATION.md not written — the most critical tech debt in this phase; blocks milestone completion"
  - phase: "04-performance"
    items:
      - "Stale data indicator (_stale field) produced but not displayed. User has no visibility into whether data is fresh or served from cache fallback during network outage. Low priority — does not affect accuracy when network is healthy."
  - phase: "05-code-quality"
    items:
      - "evaluatePolygonsWeighted() (lines 120-147): dead function with two bugs — implicit global 'result' and typo 'lamba'/'lambda' (ReferenceError if called). Should be removed."
      - "evaluatePolygonsContinuous() (lines 157-180): dead function with implicit global 'minDistTest'. Should be removed."
      - "fetchGeoJson() (lines 217-227): no longer called in live paths. Should be removed or confirmed as intentionally kept."
      - "evaluatePolygons() line 104: 'result = turf.booleanPointInPolygon(...)' is an implicit global assignment in live production code. Should be declared with const/let."
      - "checkDayCat/checkDayPerc/checkDaySign: ~72 lines of commented-out function bodies at lines 896-967. Should be removed to satisfy QUAL-03."
nyquist:
  compliant_phases: ["02-cig-tier-support"]
  partial_phases: ["01-bug-fixes", "03-fire-weather", "04-performance", "05-code-quality"]
  missing_phases: []
  overall: partial
---

# Milestone v1.0 Audit Report

**Milestone:** MMM-SPCOutlook v1.0 — Refactor & Repair
**Audited:** 2026-03-10
**Status:** GAPS FOUND
**Scores:** 10/15 requirements satisfied | 4/5 phases verified | 11/15 integration points clean | 4/5 flows complete

---

## Executive Summary

The v1.0 milestone delivers all intended capabilities — bug fixes, CIG tier support, fire weather, performance caching, and code quality — with all 15 requirements implemented in code and wired correctly through the display layer. Five requirements have documentation or code-quality gaps that prevent closure:

1. **Phase 02 has no VERIFICATION.md** (blocker) — SPC-01 and SPC-02 are unverified per GSD process, though the integration checker independently confirms the features are correctly implemented.
2. **QUAL-02 partial** — one implicit global (`result`) remains in live `evaluatePolygons()` code.
3. **QUAL-03 partial** — ~72 lines of commented-out function bodies remain in `node_helper.js` (lines 896-967).
4. **PERF-01 partial** — stale cache flag produced but not displayed to user.
5. **PERF-01/04/05 human_needed** — Phases 04 and 05 have automated checks passing but pending human runtime confirmation.

---

## Phase Verification Summary

| Phase | VERIFICATION.md | Status | Score | Key Issue |
|-------|-----------------|--------|-------|-----------|
| 01 Bug Fixes | ✓ exists | passed | 5/5 | None — all four bugs confirmed fixed |
| 02 CIG Tier Support | **MISSING** | **unverified** | N/A | **Blocker: no VERIFICATION.md written** |
| 03 Fire Weather | ✓ exists | passed | 7/7 | None — all seven truths confirmed |
| 04 Performance | ✓ exists | human_needed | 5/5 automated | Runtime confirmation pending; 3 human-only tests |
| 05 Code Quality | ✓ exists | human_needed | 10/10 automated | Runtime confirmation pending; grep checks missed 2 issues |

---

## Requirements Coverage (3-Source Cross-Reference)

| Req-ID | Description | Phase | VERIFICATION.md | SUMMARY Frontmatter | REQUIREMENTS.md | Final Status |
|--------|-------------|-------|-----------------|---------------------|-----------------|--------------|
| BUG-01 | SIGN detection for Tor/Hail/Wind Days 1-2 | 01 | passed | listed (01-01) | [x] | **satisfied** |
| BUG-02 | Day 8 shows Day 8 risk | 01 | passed | listed (01-01) | [x] | **satisfied** |
| BUG-03 | day48Risk reflects all Days 4-8 | 01 | passed | listed (01-01) | [x] | **satisfied** |
| BUG-04 | All overlapping MDs collected | 01 | passed | listed (01-02) | [x] | **satisfied** |
| SPC-01 | CIG1/CIG2/CIG3 tier parsing | 02 | **missing** | listed (02-01) | [x] | **partial** (verification gap) |
| SPC-02 | CIG tiers visually distinct | 02 | **missing** | listed (02-02) | [x] | **partial** (verification gap) |
| FIRE-01 | Fire weather GeoJSON fetched | 03 | passed | listed (03-01) | [x] | **satisfied** |
| FIRE-02 | Point-in-polygon for fire weather | 03 | passed | listed (03-01) | [x] | **satisfied** |
| FIRE-03 | Fire weather displayed alongside convective | 03 | passed | listed (03-02) | [x] | **satisfied** |
| PERF-01 | Polygon math cached between cycles | 04 | human_needed | listed (04-01) | [x] | **partial** (stale flag not displayed) |
| PERF-02 | No redundant turf calls per cycle | 04 | human_needed | listed (04-01) | [x] | **satisfied** (integration confirmed) |
| QUAL-01 | Day1/Day2 logic in shared function | 05 | human_needed | listed (05-04) | [x] | **satisfied** (7 references confirmed) |
| QUAL-02 | const/let everywhere; no implicit globals | 05 | human_needed | listed (05-02,03,04) | [x] | **partial** (implicit global in live code) |
| QUAL-03 | No dead/commented-out code | 05 | human_needed | listed (05-02,03,04) | [x] | **partial** (~72 lines commented remain) |
| QUAL-04 | No console.log; errors use Log.error | 05 | human_needed | listed (05-02,03,04) | [x] | **satisfied** (integration confirmed) |

**Orphaned requirements:** None. All 15 v1 requirement IDs appear in at least one phase VERIFICATION.md (or SUMMARY for Phase 02).

---

## Integration Checker Findings

### Phase Handoffs

| Handoff | Status | Details |
|---------|--------|---------|
| Phase 01 → Phase 02 | CLEAN | All `torSign`/`hailSign`/`windSign` fields replaced by `torCig`/`hailCig`/`windCig`. No dangling old field names. |
| Phase 02 → Phase 03 | CLEAN | Fire weather fields coexist with CIG fields with no namespace conflicts. |
| Phase 03 → Phase 04 | CLEAN | All 4 fire weather URLs use `fetchGeoJsonCached()`. Stale flag propagated via `anyStale`. |
| Phase 04 → Phase 05 | CLEAN | `fetchAndEvaluateHazard()` calls `fetchGeoJsonCached()` internally. Stale propagation preserved. |

### Cross-Phase Wiring

| Requirement | Wiring | Status |
|-------------|--------|--------|
| BUG-01 | `extractPolygons(label => label)` → `evaluatePolygons(sigComparator)` → return fields | WIRED |
| BUG-02 | `day8*` vars → extended return `day8: {}` → `getDom()` line 100 | WIRED |
| BUG-03 | Days 4-8 OR condition → `day48Risk` → `getDom()` no-risk guard line 56 | WIRED |
| BUG-04 | `getMesoscaleDiscussion()` → `MDArray.push()` → `payload[1]` → `this.mds` → `getDom()` for-of | WIRED |
| SPC-01 | `cigToTier`/`cigComparator` → `fetchAndEvaluateHazard()` → integer fields in return | WIRED |
| SPC-02 | `torCig/hailCig/windCig` → `getDom()` `cigLabel()` → ①②③ at 7 display points | WIRED |
| FIRE-01 | 4 fire URLs → `fetchGeoJsonCached()` lines 587,599,614,626 | WIRED |
| FIRE-02 | `extractPolygons(fireRiskToValue)` → `evaluatePolygons(fireComparator)` → `dayNFireRisk` | WIRED |
| FIRE-03 | `fireWeather:{...}` → `getDom()` lines 102-113 → color-coded rows; no-risk guard extended | WIRED |
| PERF-01 | Cache + stale flag produced → transmitted → **`getDom()` never reads `_stale`** | PARTIAL |
| PERF-02 | Days 4-8 `extractPolygons` called once before any `evaluatePolygons` | WIRED |
| QUAL-01 | `fetchAndEvaluateHazard()` defined once, called 6 times (lines 471,476,481,510,515,520) | WIRED |
| QUAL-02 | Zero `var` declarations — but `result=turf.booleanPointInPolygon(...)` at line 104 (implicit global) | PARTIAL |
| QUAL-03 | Zero `//Log.` lines — but `checkDayCat`/`checkDayPerc`/`checkDaySign` remain commented at lines 896-967 | PARTIAL |
| QUAL-04 | Zero `console.` in both files; `Log.error` at line 873 | WIRED |

### Orphaned Functions (dead code)

| Function | Location | Issue |
|----------|----------|-------|
| `evaluatePolygonsWeighted()` | node_helper.js lines 120-147 | Never called; bugs: implicit global `result` at line 126; typo `lamba` (line 130) vs `lambda` (line 139) = ReferenceError if called |
| `evaluatePolygonsContinuous()` | node_helper.js lines 157-180 | Never called; implicit global `minDistTest` at line 165 |
| `fetchGeoJson()` | node_helper.js lines 217-227 | No longer called by any live path; getMesoscaleDiscussion uses fetchBinBuffer |

---

## Nyquist Compliance

| Phase | VALIDATION.md | nyquist_compliant | Status | Action |
|-------|--------------|-------------------|--------|--------|
| 01 Bug Fixes | exists | false | PARTIAL | `/gsd:validate-phase 1` |
| 02 CIG Tier Support | exists | **true** | COMPLIANT | None |
| 03 Fire Weather | exists | false | PARTIAL | `/gsd:validate-phase 3` |
| 04 Performance | exists | false | PARTIAL | `/gsd:validate-phase 4` |
| 05 Code Quality | exists | false | PARTIAL | `/gsd:validate-phase 5` |

---

## Tech Debt Summary

| Phase | Item | Severity |
|-------|------|----------|
| 02 | VERIFICATION.md missing — highest priority | BLOCKER |
| 05 | `evaluatePolygonsWeighted()` lines 120-147: dead + buggy (implicit global + typo = ReferenceError) | High |
| 05 | `evaluatePolygonsContinuous()` lines 157-180: dead + implicit global | High |
| 05 | `evaluatePolygons()` line 104: `result=` implicit global in live code | High |
| 05 | `checkDayCat`/`checkDayPerc`/`checkDaySign` lines 896-967: ~72 lines commented-out | Medium |
| 05 | `fetchGeoJson()` lines 217-227: dead function (no callers in live paths) | Low |
| 04 | `_stale`/`_staleAsOf` produced but not displayed; user has no staleness indicator | Low |

---

## Unsatisfied / Partial Requirements Requiring Action

### 1. Phase 02 VERIFICATION.md (blocker)

**Action:** Run `/gsd:verify-work` for Phase 02 to produce a VERIFICATION.md.

Alternatively, write the VERIFICATION.md manually — integration checker confirms both SPC-01 and SPC-02 are correctly implemented:
- `cigToTier` + `cigComparator` → `torCig/hailCig/windCig` integer fields (0-3) in all return paths ✓
- `cigLabel()` at MMM-SPCOutlook.js line 40; called at 7 display points (lines 73-91) ✓

### 2. QUAL-02 — Implicit Global in Live Code

**Action:** In `node_helper.js` line 104, add `const` declaration:
```js
// Before (implicit global):
result = turf.booleanPointInPolygon(loc, poly);
// After:
const result = turf.booleanPointInPolygon(loc, poly);
```

### 3. QUAL-03 — Commented-Out Functions Remain

**Action:** Remove `checkDayCat`, `checkDayPerc`, `checkDaySign` commented-out blocks at node_helper.js lines 896-967 (~72 lines).

### 4. Dead Function Cleanup

**Action (recommended):** Remove `evaluatePolygonsWeighted()` (lines 120-147), `evaluatePolygonsContinuous()` (lines 157-180), and `fetchGeoJson()` (lines 217-227) as they are never called and contain bugs.

---

## Scores

| Dimension | Score | Notes |
|-----------|-------|-------|
| Requirements satisfied | 10/15 | 4 BUG + 3 FIRE + PERF-02 + QUAL-01 + QUAL-04 |
| Requirements partial | 5/15 | SPC-01, SPC-02 (unverified), PERF-01 (stale), QUAL-02 (implicit global), QUAL-03 (commented code) |
| Phases with VERIFICATION.md | 4/5 | Phase 02 missing |
| Integration points clean | 11/15 | 4 partial: PERF-01, QUAL-02, QUAL-03 (×2 orphan issues) |
| E2E flows complete | 4/5 | Stale indicator flow incomplete |
| Nyquist compliant phases | 1/5 | Phase 02 only |

---

*Audited: 2026-03-10*
*Auditor: Claude (gsd audit-milestone)*
*Integration Checker: gsd-integration-checker (sonnet)*
