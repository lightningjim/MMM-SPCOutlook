---
milestone: v2.0
milestone_name: "WPC & CPC Integration + Unified Day Report"
audited: 2026-09-11T11:00:00Z
status: tech_debt
scores:
  requirements: 37/37 code-wired; 33/37 with a passing phase verification
  phases: 6/7 verifications passing (Phase 14's report is stale, not failing)
  integration: 0 blockers, 4 warnings, 3 info
  flows: 1/1 E2E flow traced end-to-end, unbroken
gaps: {}
verification_debt:
  - phase: 14-foundation-wpc-excessive-rainfall-outlook
    issue: "VERIFICATION.md predates the phase's own fix passes and was never re-run"
    verification_status: gaps_found
    actual_code_status: "all 3 blocking gaps verified CLOSED at HEAD by independent source read"
    affected_reqs: ["CFG-01", "CFG-02", "ERO-01", "ERO-03"]
    remedy: "re-run phase verification; this is bookkeeping, not re-implementation"
    severity: non-blocking
tech_debt:
  - phase: cross-cutting
    items:
      - "WARNING 1 (new, undocumented): window band and day grid disagree about day numbers for 12 of every 24 hours — the band was never re-anchored to 12Z in Phase 18"
      - "WARNING 4: the rpt01 sole-render-path guard is a literal string scan; bracket/destructure/alias access would evade it (zero violations at HEAD)"
      - "INFO 2: the SPC inline chain is serial and dominates cold-cache latency; PERF-01 scoped only the NEW product fetches"
  - phase: 19-legacy-retirement (deferred by operator 2026-09-07)
    items:
      - "WARNING 2 (CORRECTED 2026-09-11): the todo listed `advisories` as dead — it is the SOLE transport for the MPD/SPC MD band. Executing as written would have deleted MPD from the display."
      - "WARNING 3: retirement is not a pure deletion — the RPT-04 window band is PRODUCED inside the legacy hazardsOutlook block; 3 helpers must be extracted first"
      - "INFO 1: `day48Risk` is a ninth emitted legacy key, was missing from the inventory (now added)"
      - "Legacy `heatRisk.day1..day7` drops day 7 for ~12h/24h — inert on screen, live in the emitted payload"
      - "Legacy `hazardsOutlook.dayN` raw 0-based offset labelling — inert on screen, live in the emitted payload"
  - phase: bookkeeping
    items:
      - "11 REQUIREMENTS.md traceability rows still read `Pending` (CFG-01/02, ERO-01/03, WSSI-01/02/03, MPD-01..04) while their phases are complete and verified"
  - phase: deferred-live-verification
    items:
      - "23 live-observation deferrals accumulated across Phases 14-19.1, each with a documented reason (seasonal WSSI, event-gated MPD/ERO, location-gated products)"
  - phase: 14-foundation-wpc-excessive-rainfall-outlook
    items:
      - "Multi-instance cache/broadcast isolation — unreachable single-instance, becomes live on a second instance or location"
      - "IN-01..IN-08 (8 Info/CONVENTION code-review findings) accepted unfixed"
---

# Milestone v2.0 Audit — WPC & CPC Integration + Unified Day Report

**Status: `tech_debt`** — no blockers, no unsatisfied requirements, no broken flows. Accumulated
deferrals and two stale planning artifacts need review before the milestone closes.

## Scope

7 phases, all complete: 14 (ERO + shared conventions), 15 (WSSI + MPD), 16 (Hazards Outlook),
17 (HeatRisk + parallelization), 18 (merge/precedence + unified schema), 19 (getDom rewrite),
19.1 (width/copy/config-key defect fixes). 37 requirements. Probe suite `191 passed, 0 failed,
0 skipped`, re-run independently by both the verifier and the integration checker.

## Requirements Coverage — 3-source cross-reference

| Source | Result |
|---|---|
| REQUIREMENTS.md traceability | 37 REQ-IDs assigned to milestone phases |
| SUMMARY.md `requirements-completed` frontmatter | **37/37** claimed complete |
| Phase VERIFICATION.md coverage | **37/37** covered |
| **Orphans** (assigned but verified by no phase) | **0** |
| Independently confirmed code-wired at HEAD | **37/37** (integration checker, per-REQ path table) |

**No requirement is unsatisfied.** The FAIL gate is not triggered.

Four requirements (CFG-01, CFG-02, ERO-01, ERO-03) sit under Phase 14's stale `gaps_found`
verification. Under the status matrix's letter that reads `unsatisfied` — but the matrix assumes a
verification reflects current code, and this one does not. See "Phase 14" below. All four are
code-wired and independently confirmed.

## Integration — 0 blockers

The full E2E flow was traced link by link and is unbroken: config → `buildRequestPayload` →
socket → `_inFlight`-guarded `getSpcOutlook` → `Promise.allSettled` over 6 members → 5 assembly
calls → `_resolveGridDayPrecedence` over 14 grid days → unified payload → socket → `getDom`.

All seven products reach the screen, each respecting its toggle end-to-end. `reporting` cannot be
true with a toggle off (`node_helper.js:3872`). Every toggle gate uses strict `=== true`/`!== true`
on both sides — the truthiness split-brain is genuinely closed.

**RPT-01 (sole render path) was verified without relying on its probe.** Every `this.spcrisk.*`
access in the getDom region was enumerated by hand: only `advisories`, `days`, `error`, `_stale`,
`_staleAsOf`, `summary`, `windowBand`. Bracket access, destructuring and aliasing were each checked
separately — zero hits. The `rpt01` probe was then confirmed non-vacuous: it strips comments before
scanning (necessary, since the file's own comments name every legacy accessor) and carries a
positive counter-assertion.

**PERF-01 × `_inFlight` — the specifically-flagged cross-phase hazard HOLDS.** Phase 14 introduced
two helper-globals safe only because `_inFlight` made chain overlap unreachable; Phase 17 then
parallelized fetching. Three independent legs confirm the guard survived: `getSpcOutlook` has
exactly one production caller (inside the try/finally), the batch is `allSettled` with per-member
rejection substitution so one throw cannot skip the `finally`, and
`scripts/check-concurrency-invariant.sh` self-tests its own ability to fail before asserting exact
write-site counts.

## Findings requiring attention

### 1. Phase 14's verification is stale — not failing

`14-VERIFICATION.md` records `has_blocking_gaps: true` (10/13, 2026-08-19). Phase 14 then ran three
code-review rounds and 34 fix commits, and the report was never re-run. All three blocking gaps are
closed at HEAD, verified by independent source read:

| Gap | Status at HEAD | Evidence |
|---|---|---|
| CR-01 residual: a real non-NONE ERO tier silently reported NONE when the winning polygon is not `features[0]` | **CLOSED** | `features[0]` deref replaced by `_validTimeOfWinner(polys, loc, value, …)` (`node_helper.js:393`); probe `ero-leading-bad-feature-preserves-risk` asserts MDT survives — the exact scenario the report demanded |
| CR-02: a malformed body on any SPC layer pins a false all-clear into `_geoJsonCache` behind future 304s | **CLOSED** | `node_helper.js:360-364` names CR-02 directly; rejected bodies are never cached; probe `ero-rejected-body-serves-last-known-good` |
| CR-03: the dominant ERO failure mode produced no `Log.error` and no `_stale` flag | **CLOSED** | `if (fetchResult.stale \|\| fetchResult.failed) anyStale = true` (`:364`), comment citing CR-03; `Log.error` at `:2052` |

This is the one thing that should be reconciled before close — not because the code is wrong, but
because the milestone's own record says it is.

### 2. The legacy-retirement todo was factually dangerous (CORRECTED)

The pending todo listed `advisories` among blocks "that no render path reads". **It is the sole
transport for every MPD and SPC MD band entry** (`MMM-SPCOutlook.js:967`, consumed `:1498-1511`).
Executing it as written would have deleted MPD-01/02/03 and SPC MD from the display. The `rpt01`
probe's own accessor list correctly omitted `advisories` — probe and todo contradicted each other,
and the probe was right.

Corrected in place 2026-09-11, with two further corrections: `day48Risk` added to scope (a ninth
unread legacy key missing from the inventory), and a warning that retirement is **not** a pure
deletion — the RPT-04 window band is produced *inside* the legacy `hazardsOutlook` block, so
`windowEntries`/`windowBand` assembly plus `_hazardDayOffset`, `_todayUtcMs` and
`_isFullNominalWindow` must be extracted first or RPT-04 breaks.

### 3. Window band and day grid disagree about day numbers for half of every day (new, undocumented)

Phase 18 re-anchored the day grid to SPC's 12Z boundary. The window band was not — it still carries
Phase 16's raw UTC-midnight offsets. Both call `_hazardDayOffset`, with different anchors. During
the 12Z–24Z UTC half of each day — US afternoon and evening, the primary viewing window for a wall
mirror — the band's `D<n>` reads one day higher than the grid row covering the same instant.

The in-source comment claims the `+1` conversion makes the band "agree with the grid directly above
it". It does not, for half of every day: the `+1` fixed the 0-vs-1 base, but the anchor offset is a
separate, unaddressed term. MERGE-01's normalization layer is genuinely wired for the grid and
bypassed entirely for the band.

Not a blocker: no data lost, no hazard hidden, no false negative. It is a labelling inconsistency
between two adjacent regions of the same render. **It is, however, the only finding in this audit
that is recorded nowhere else in the project** — searched across `18-deferred-items.md`,
`19-CONTEXT.md`, `19-LEGACY-RETIREMENT.md` and pending todos. Phase 18 deliberately deferred band
*membership*; nothing addresses band *labelling*.

### 4. Bookkeeping staleness — 11 traceability rows

CFG-01/02, ERO-01/03, WSSI-01/02/03 and MPD-01..04 read `Pending` while their phases are complete
and verified. Phase 19's verifier identified this same pattern and deferred it to project-level
reconciliation. Per the status matrix (`passed` + listed + `[ ]` → **satisfied, update checkbox**),
the seven Phase 15 rows are satisfied outright; the four Phase 14 rows follow finding 1.

## Tech debt summary

| Category | Count | Character |
|---|---|---|
| Deferred live verification | 23 | Each with a documented reason — seasonal (WSSI), event-gated (MPD, ERO), location-gated. Not neglect; a standing, recorded practice. |
| Scope deferred to v2.x | 4 | WSSIX-01, MERGEX-01/02, COVX-01 — all deferred at v2.0 scoping, by design |
| Correctness, deferred | 3 | Two (legacy heatRisk day-7 drop, hazardsOutlook raw-offset labelling) live **inside** the deferred legacy-block deletion — one cleanup closes both. The third (multi-instance isolation) is unreachable single-instance. |
| Quality accepted | 4 | Phase 14 IN-01..IN-08, Phase 15 F1-F3 |
| UX | 1 | Proximity badge spacing artifacts, accepted at v1.2 |

**Anti-pattern scan: 0 hits** for TODO/FIXME/XXX/HACK/PLACEHOLDER/TBD across `node_helper.js`,
`MMM-SPCOutlook.js`, `productRegistry.js`, `hazardTaxonomy.js`.

Nyquist validation and the drift gate are both disabled in this project's config; both discovery
steps were skipped per the workflow.

## Recommendation

**The milestone is functionally complete and safe to close.** Zero blockers, zero unsatisfied
requirements, zero broken flows, zero anti-patterns, a green 191-scenario suite, and a fully traced
E2E path.

Two items are worth doing first because both are cheap and both concern the *record* rather than the
code: re-run Phase 14's verification so the milestone's own artifacts stop asserting a blocking gap
that no longer exists, and check off the 11 stale traceability rows. Finding 3 (band/grid day
numbering) is a genuine new defect but a cosmetic one; it belongs in v2.x scoping rather than
blocking this close.
