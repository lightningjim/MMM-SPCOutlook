---
phase: 18-merge-precedence-unified-payload-schema
plan: 12
type: execute
wave: 10
gap_closure: true
depends_on: ["18-10", "18-11"]
files_modified:
  - .planning/phases/18-merge-precedence-unified-payload-schema/18-LIVE-CAPTURE.md
  - .planning/phases/18-merge-precedence-unified-payload-schema/deferred-items.md
  - .planning/phases/18-merge-precedence-unified-payload-schema/18-09-SUMMARY.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [MERGE-01, MERGE-04]

must_haves:
  truths:
    - "ROADMAP Phase 18 success criterion 1 reads PASS, backed by a deterministic replay of the 2026-09-05 captured live feature values against the fixed code, honestly labelled as a replay and not as a fresh live poll"
    - "18-LIVE-CAPTURE.md's Criterion 1 section and its validation table both carry the re-validated verdict, with the original FAIL trace preserved rather than overwritten"
    - "MERGE-04's over-merge half is re-evaluated now that the criterion-1 blocker is gone, and the outcome is recorded either as an observed verdict or as an explicitly reasoned continuing deferral"
    - "Both deferred-items.md entries carry a resolution line naming the plan that closed them"
    - "STATE.md's MERGE-01 FAIL blocker is resolved and the phase's remaining open items are accurate"
    - "The operator has confirmed the live MagicMirror display is unchanged and has re-answered the 18-09 Task 3 checkpoint"
  artifacts:
    - path: ".planning/phases/18-merge-precedence-unified-payload-schema/18-LIVE-CAPTURE.md"
      provides: "the re-validated Criterion 1 verdict plus the preserved original FAIL trace"
      contains: "Re-validation after the 18-10 fix"
    - path: ".planning/phases/18-merge-precedence-unified-payload-schema/18-09-SUMMARY.md"
      provides: "the resolved status and completed Outstanding checklist"
      contains: "Outstanding before Phase 18 can close"
    - path: ".planning/STATE.md"
      provides: "the resolved MERGE-01 blocker and an accurate deferred-items table"
      contains: "RESOLVED (18-10"
  key_links:
    - from: ".planning/ROADMAP.md criterion 1"
      to: "18-LIVE-CAPTURE.md's re-validation section"
      via: "an explicit citation of the replay evidence and the probe scenario name"
      pattern: "Re-validation after the 18-10 fix"
    - from: ".planning/phases/18-merge-precedence-unified-payload-schema/deferred-items.md"
      to: "18-10-SUMMARY.md and 18-11-SUMMARY.md"
      via: "a Resolved line naming the closing plan"
      pattern: "Resolved"
---

<objective>
Re-validate ROADMAP Phase 18 success criterion 1 against the captured live evidence now that the
MERGE-01 defect is fixed, re-evaluate MERGE-04's over-merge half now that its blocker is gone,
propagate the outcome through every tracking document, and take the two operator confirmations
that still gate the phase close.

Purpose: 18-09 paused at a blocking human checkpoint with criterion 1 reading FAIL, so Phase 18
cannot close until that verdict is re-taken honestly and the operator re-answers. This plan is
where the gap-closure run's code work becomes a verdict.

Output: an updated `18-LIVE-CAPTURE.md` with the original FAIL trace preserved and the
re-validation appended, resolved entries in `deferred-items.md` and `STATE.md`, an updated
`ROADMAP.md` and `REQUIREMENTS.md`, and a resolved `18-09-SUMMARY.md`.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/buildomator/bm/4.5.6/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/buildomator/bm/4.5.6/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-merge-precedence-unified-payload-schema/18-CONTEXT.md
@.planning/phases/18-merge-precedence-unified-payload-schema/18-LIVE-CAPTURE.md
@.planning/phases/18-merge-precedence-unified-payload-schema/deferred-items.md
@.planning/phases/18-merge-precedence-unified-payload-schema/18-09-SUMMARY.md
@.planning/phases/18-merge-precedence-unified-payload-schema/18-10-SUMMARY.md
@.planning/phases/18-merge-precedence-unified-payload-schema/18-11-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-validate criterion 1 and re-evaluate MERGE-04's over-merge half</name>

  <read_first>
    - `.planning/phases/18-merge-precedence-unified-payload-schema/18-LIVE-CAPTURE.md` in full —
      in particular Capture 2 (the raw feature and the legacy placement), the validation table at
      line ~169, the `### Criterion 1 (MERGE-01, near-boundary) — FAIL` section at line ~174, and
      the `### Criterion 4 (MERGE-04)` section's over-merge paragraph
    - `.planning/phases/18-merge-precedence-unified-payload-schema/18-10-SUMMARY.md` — the
      verbatim pre-fix RED line for
      `merge-grid-hazards-start-equals-end-live-shape-lands-on-its-own-grid-day`, the post-fix
      GREEN result, the four recorded arithmetic cases, and the two-row mutation table
    - `.planning/phases/18-merge-precedence-unified-payload-schema/18-11-SUMMARY.md` — the
      observed toggle-off/toggle-on `reportedDays` arrays
    - `.planning/ROADMAP.md` lines 247-296 — Phase 18's six success criteria, verbatim
    - `node_helper.js` lines 2835-2925 — the fixed function, so the recorded verdict describes the
      code that actually shipped
    - `productRegistry.js` lines 299-345 — `excessiveRain`'s URL and day span, needed to reason
      about whether ERO can cover the Kotzebue coordinate at all
  </read_first>

  <files>.planning/phases/18-merge-precedence-unified-payload-schema/18-LIVE-CAPTURE.md</files>

  <action>
Produce the criterion-1 re-validation and the MERGE-04 re-check, and record both in
`18-LIVE-CAPTURE.md`. Write to no other file in this task.

**Criterion 1 re-validation.** The evidence is a deterministic replay of the captured live values,
not a fresh NOAA poll. A fresh poll must NOT be a prerequisite: the Kotzebue "Heavy Rain" feature
(objectid 7917) was valid for 2026-09-10 and may no longer exist upstream, and the phase cannot be
gated on a transient upstream condition. The replay is
`merge-grid-hazards-start-equals-end-live-shape-lands-on-its-own-grid-day`, built from the captured
values literally — `start_date === end_date === 1788998400000`, `idp_filedate` 1788639319000,
layer 4, the `"estimated"` SPC anchor branch, `nominalStartMs` `2026-09-05T12:00:00.000Z` — and
which 18-10 recorded RED against the shipped defect and GREEN against the fix.

Run `node scripts/probe-payload-resilience.js` and capture the current result. Then append a new
subsection to `18-LIVE-CAPTURE.md`'s Criterion 1 section titled
`#### Re-validation after the 18-10 fix — PASS`, recording:
  - the verbatim `PASS merge-grid-hazards-start-equals-end-live-shape-lands-on-its-own-grid-day`
    line and the final `PROBE RESULT:` line from the run;
  - the verbatim pre-fix `FAIL ...` line quoted out of `18-10-SUMMARY.md`, labelled as the
    deterministic reproduction of the live defect;
  - the corrected arithmetic against this poll's actual anchor: `gridStart 6`, `gridEnd 6`,
    `lastGridDay = Math.max(6, 5) = 6`, `clampedStart 6`, `clampedEnd 6`, emission on grid day 6
    only — contrasted against the original trace's `lastGridDay 5 -> empty range -> dropped`;
  - the resulting payload facts: `days["6"]` carries the `wpc-hazards` `"Heavy Rain"` entry with
    `dimension: "heavy-precip"`, `date "2026-09-10"`, `windowStart "2026-09-10T12:00:00.000Z"`,
    and `sources["wpc-hazards"].reportedDays`/`activeDays` now include 6 with `reporting: true` —
    each of which the original capture recorded as absent/empty/false;
  - the agreement with the legacy path: `hazardsOutlook.day5` (date `2026-09-10`) carried the same
    label all along, so unified and legacy now agree on the calendar date, which is what D-10's
    forward-align rule requires;
  - an explicit, prominent honesty note stating that this is a replay of captured values through
    the real `getSpcOutlook` code path with a stubbed transport, NOT a new live poll, and naming
    the 18-10 mutation table as the proof the replay is not vacuous. Do not present a fixture as a
    live observation — that is the T-18-26 discipline this document already holds itself to.

Do NOT delete or rewrite the original `### Criterion 1 (MERGE-01, near-boundary) — FAIL` trace.
Retitle that heading to `### Criterion 1 (MERGE-01, near-boundary) — FAIL at capture time,
re-validated PASS after the 18-10 fix` and leave the original body intact beneath it; the FAIL
trace is the record of what live data actually showed on 2026-09-05 and is the reason the fixture
exists.

Update the validation table (currently at line ~169) so criterion 1's Verdict cell reads
`**PASS** (re-validated; **FAIL** at 2026-09-05 capture time)` with an Evidence cell pointing at
the new re-validation subsection.

**MERGE-04 over-merge re-check.** The criterion-4 over-merge half was recorded NOT OBSERVABLE
specifically because the only live `heavy-precip`-eligible feature never reached the unified day
grid. That blocker is now gone, so re-decide the verdict on the evidence:
  - Determine whether the captured payload, replayed through the fixed code, now yields any single
    grid day carrying BOTH a surviving `flash-flood` entry and a surviving `heavy-precip` entry.
    The specific question to answer and record is coverage: Capture 2's coordinate is
    65.936, -163.443 (northwest Alaska) and `wpc-ero`'s MapServer is a CONUS product — state
    explicitly, from the registry row and the captured `sources['wpc-ero']` block, whether ERO
    produced any above-floor day at that coordinate. Capture 1 (Florence, SC) carried the ERO
    entries but had no `wpc-hazards` feature at all, so the two halves sat at different coordinates.
  - If a day now carries both dimensions with `suppressedBy: null` on each, upgrade criterion 4's
    over-merge half from NOT OBSERVABLE to PASS in both the validation table and the Criterion 4
    section, quoting the grid day and both entries verbatim.
  - If it still does not — which the coverage reasoning above makes the likely outcome — say so
    explicitly and in full: name the reason (no ERO coverage at the Alaska coordinate; no
    `wpc-hazards` feature at the CONUS coordinate), state that the criterion-1 fix removed the
    blocker without producing an observation, and note that the existing STATE.md deferral row for
    MERGE-04's over-merge half stays INTACT (Task 2 updates only its rationale text, never deletes
    it). Do not silently flip the verdict either way.
  </action>

  <verify>
    <automated>node scripts/probe-payload-resilience.js 2>&1 | tail -1 && grep -c 'Re-validation after the 18-10 fix' .planning/phases/18-merge-precedence-unified-payload-schema/18-LIVE-CAPTURE.md</automated>
  </verify>

  <acceptance_criteria>
    - `node scripts/probe-payload-resilience.js` prints `PROBE RESULT: 119 passed, 0 failed, 0 skipped` and exits 0.
    - `grep -c 'Re-validation after the 18-10 fix' .planning/phases/18-merge-precedence-unified-payload-schema/18-LIVE-CAPTURE.md` is 1.
    - `grep -c 'clampedEnd (5) < clampedStart (6)' .planning/phases/18-merge-precedence-unified-payload-schema/18-LIVE-CAPTURE.md` is 1 — the original FAIL trace was preserved, not overwritten.
    - `grep -c 'merge-grid-hazards-start-equals-end-live-shape-lands-on-its-own-grid-day' .planning/phases/18-merge-precedence-unified-payload-schema/18-LIVE-CAPTURE.md` is at least 2 — both the verbatim PASS line and the verbatim pre-fix FAIL line are quoted.
    - The re-validation subsection contains a sentence applying the word `replay` and an explicit negation of "a fresh live poll" to this evidence, so its evidence class is unambiguous to a later reader.
    - `grep -n '| 1 — MERGE-01' .planning/phases/18-merge-precedence-unified-payload-schema/18-LIVE-CAPTURE.md` shows a validation-table row containing `PASS`.
    - `grep -c 'over-merge' .planning/phases/18-merge-precedence-unified-payload-schema/18-LIVE-CAPTURE.md` is at least 3, and the Criterion 4 section contains a new paragraph stating either an observed both-dimensions-on-one-day result (quoting the grid day and both entries) or an explicit continuing-deferral statement naming the coverage reason.
    - `git diff --name-only` for this task lists exactly one file: `18-LIVE-CAPTURE.md`.
  </acceptance_criteria>

  <done>Criterion 1 carries an honest re-validated PASS backed by the captured values, the original FAIL trace is preserved, and MERGE-04's over-merge half has an explicit re-decided verdict.</done>
</task>

<task type="auto">
  <name>Task 2: Propagate the outcome to every tracking document</name>

  <read_first>
    - `.planning/STATE.md` in full — the frontmatter, the Current Position block, the Decisions
      list, the Blockers/Concerns section (specifically the
      `**NEW (18-09 live capture, 2026-09-05): MERGE-01 FAIL.**` bullet and the Raspberry Pi
      PERF-03 bullet), and the Deferred Items table including both `Phase 18-09` rows
    - `.planning/ROADMAP.md` lines 247-296 — Phase 18's criteria, the `**Plans**:` count line, and
      the wave-grouped plan checklist with its exact formatting
    - `.planning/REQUIREMENTS.md` line 44 (`MERGE-01` checkbox) and line 134 (the traceability
      table row reading `In Progress`)
    - `.planning/phases/18-merge-precedence-unified-payload-schema/deferred-items.md` — both
      entries in full, so each gets an appended resolution line rather than a rewrite
    - `.planning/phases/18-merge-precedence-unified-payload-schema/18-09-SUMMARY.md` — its
      `status: paused` frontmatter, its task table, its `## Criteria verdicts` table, and its
      `## Outstanding before Phase 18 can close` checklist
    - `.planning/phases/18-merge-precedence-unified-payload-schema/18-LIVE-CAPTURE.md` — Task 1's
      output, so every claim written here is sourced from it rather than restated from memory
    - `.planning/phases/18-merge-precedence-unified-payload-schema/18-10-SUMMARY.md` and
      `18-11-SUMMARY.md` — the values quoted into the resolution lines
  </read_first>

  <files>.planning/STATE.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/18-merge-precedence-unified-payload-schema/deferred-items.md, .planning/phases/18-merge-precedence-unified-payload-schema/18-09-SUMMARY.md</files>

  <action>
Propagate Task 1's verdicts. Every edit below is additive or a status flip — do not delete history.

`deferred-items.md` — append a `**Resolved:**` paragraph to the end of each of the two entries:
  - The `sources[].reportedDays` entry: resolved by plan 18-11. Name the gate (the
    `productToggles[row.configFlag] !== true` early return in `_addRegistryDayGridEntries`), the
    scenario `merge-sources-disabled-registry-source-reports-no-days`, and the observed
    toggle-off/toggle-on arrays quoted from `18-11-SUMMARY.md`.
  - The `start_date === end_date` MERGE-01 entry: resolved by plan 18-10. Name the target
    expression `Math.max(gridStart, gridEnd - 1)`, both scenarios
    (`merge-grid-hazards-start-equals-end-live-shape-lands-on-its-own-grid-day` and
    `merge-grid-hazards-multi-day-exclusive-span-still-ends-on-its-last-covered-day`), and record
    that 18-10 deliberately used the general `Math.max` form rather than this entry's own narrower
    epoch-equality suggestion because the narrower form still drops sub-day spans.

`.planning/STATE.md`:
  - Rewrite the `**NEW (18-09 live capture, 2026-09-05): MERGE-01 FAIL.**` bullet in
    Blockers/Concerns into a `**RESOLVED (18-10, gap closure): MERGE-01 near-boundary drop.**`
    bullet, in the same style the section already uses for the Phase 15 WSSI-03 entry. Keep the
    original description of the defect so the record of what went wrong survives; append the fix,
    the two scenarios, and a pointer to `18-LIVE-CAPTURE.md`'s re-validation subsection.
  - Leave the `Phase 18 PERF-03 requires a real cold-cache latency measurement on target Raspberry
    Pi hardware` blocker EXACTLY as it is, unedited. Per D-19 it is a milestone-close item and does
    not block this phase.
  - Deferred Items table: leave the `Live confirmation of MERGE-02's payload half` row entirely
    unchanged. For the `Live confirmation of MERGE-04's over-merge half` row, update only its
    rationale text to match Task 1's re-decided verdict — if still deferred, replace the
    "blocked by the MERGE-01 FAIL" attribution with the coverage reason Task 1 recorded; if now
    observed, change its Status cell to cite the observation in `18-LIVE-CAPTURE.md`. Do not delete
    either row.
  - Add two Decisions entries in the existing `[Phase NN-NN]:` style: one for 18-10 recording that
    the emission bound became `Math.max(gridStart, gridEnd - 1)` because the upstream feed is not
    internally consistent about the `end_date` endpoint convention (both shapes observed in the
    same 2026-09-05 poll, Precipitation inclusive and Temperature exclusive); one for 18-11
    recording that `_addRegistryDayGridEntries` now takes the request's `productToggles` snapshot
    and skips `noteReported` when the toggle is off, mirroring `_runHeatRiskProduct`'s toggle-off
    path, so `reportedDays` no longer claims an answer that was never asked for.
  - Update the Current Position block to `Plan: 12 of 12`, and update the frontmatter
    `stopped_at`, `last_updated`, `last_activity`, `progress.total_plans` and
    `progress.completed_plans` to reflect the three added plans. Read the current frontmatter
    values first and adjust them; do not assume the numbers.

`.planning/ROADMAP.md`, Phase 18 section — the `**Plans**: 12 plans in 10 waves` count line and
the Wave 8/9/10 checklist blocks for `18-10`/`18-11`/`18-12` were already written at planning time
and are present. Do not re-add them; verify they are there and correct, then:
  - Tick `- [ ] 18-10-PLAN.md` and `- [ ] 18-11-PLAN.md` to `- [x]` (both complete by this wave).
    Leave `18-12-PLAN.md` and `18-09-PLAN.md` as `- [ ]` in this task; Task 3 ticks `18-09` after
    the checkpoint is approved, and the orchestrator ticks `18-12` on plan completion.
  - Append a parenthetical to criterion 1: `(re-validated PASS after the 18-10 fix; see
    18-LIVE-CAPTURE.md "Re-validation after the 18-10 fix")`. Do not reword the criterion itself.
  - Append a parenthetical to criterion 4 carrying Task 1's re-decided over-merge verdict, in the
    same style.

`.planning/REQUIREMENTS.md`:
  - Flip line 44's `- [ ] **MERGE-01**` to `- [x] **MERGE-01**`.
  - Flip the traceability row `| MERGE-01 | Phase 18 | In Progress |` to
    `| MERGE-01 | Phase 18 | Complete |`.
  - Change nothing else; MERGE-04 is already `[x]` / `Complete`.

`18-09-SUMMARY.md`:
  - Tick the first three boxes of `## Outstanding before Phase 18 can close` (the inclusive-endpoint
    fix, the mutation-proven scenario, the criterion-1 re-validation), each with a short
    parenthetical naming the closing plan. Leave the final two boxes (the operator's step-6 display
    confirmation and the Task 3 checkpoint answer) UNTICKED — Task 3 owns those.
  - Update the `## Criteria verdicts` table's criterion-1 row to the re-validated verdict, with a
    parenthetical preserving that it read FAIL at capture time.
  - Leave `status: paused` in the frontmatter for now; Task 3 flips it.
  </action>

  <verify>
    <automated>grep -c 'RESOLVED (18-10' .planning/STATE.md; grep -c '18-10-PLAN.md' .planning/ROADMAP.md; grep -c '\[x\] \*\*MERGE-01\*\*' .planning/REQUIREMENTS.md; grep -c '\*\*Resolved:\*\*' .planning/phases/18-merge-precedence-unified-payload-schema/deferred-items.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'RESOLVED (18-10' .planning/STATE.md` is 1, and `grep -c 'MERGE-01 FAIL' .planning/STATE.md` is 0 — the blocker no longer reads as open.
    - `grep -c 'Raspberry Pi hardware before the milestone can close' .planning/STATE.md` is 1 — the PERF-03 milestone blocker survived untouched, per D-19.
    - `grep -c "Live confirmation of MERGE-02's payload half" .planning/STATE.md` is 1 and `grep -c "Live confirmation of MERGE-04's over-merge half" .planning/STATE.md` is 1 — neither deferral row was deleted.
    - `grep -c '\*\*Resolved:\*\*' .planning/phases/18-merge-precedence-unified-payload-schema/deferred-items.md` is 2 — both entries carry a resolution line.
    - `.planning/ROADMAP.md` contains `**Plans**: 12 plans in 10 waves`, `grep -c '18-1[012]-PLAN.md' .planning/ROADMAP.md` is 3 (no duplicate entries were added), and `grep -c '\[x\] 18-1[01]-PLAN.md' .planning/ROADMAP.md` is 2.
    - `grep -c 'Re-validation after the 18-10 fix' .planning/ROADMAP.md` is 1 — criterion 1 cites its evidence.
    - `grep -c '\[x\] \*\*MERGE-01\*\*' .planning/REQUIREMENTS.md` is 1 and `grep -c '| MERGE-01 | Phase 18 | In Progress |' .planning/REQUIREMENTS.md` is 0.
    - `grep -c '^- \[ \]' .planning/phases/18-merge-precedence-unified-payload-schema/18-09-SUMMARY.md` is 2 — exactly the two operator items remain unticked.
    - `grep -c 'status: paused' .planning/phases/18-merge-precedence-unified-payload-schema/18-09-SUMMARY.md` is 1 — Task 3, not this task, flips it.
    - `git diff --name-only` lists only files inside `.planning/`; no source file was touched by this task.
  </acceptance_criteria>

  <done>Every tracking document reflects the re-validated verdicts, both deferred items are marked resolved, and only the two operator-owned items remain open.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator confirmations — live display unchanged, and the 18-09 checkpoint re-answered</name>

  <read_first>
    - `.planning/phases/18-merge-precedence-unified-payload-schema/18-09-SUMMARY.md` — the
      `## Operator decisions at the checkpoint` section (both decisions the operator took) and the
      `## Outstanding before Phase 18 can close` checklist
    - `.planning/phases/18-merge-precedence-unified-payload-schema/18-09-PLAN.md` — the original
      Task 3 checkpoint wording and its step 6, so this checkpoint asks the same question rather
      than a paraphrase
    - `.planning/phases/18-merge-precedence-unified-payload-schema/18-LIVE-CAPTURE.md` — Task 1's
      re-validation subsection, which is what the operator is being asked to accept
    - `.planning/phases/18-merge-precedence-unified-payload-schema/18-10-SUMMARY.md` and
      `18-11-SUMMARY.md` — the mutation tables, so the checkpoint can present the evidence rather
      than a claim
    - `.planning/phases/18-merge-precedence-unified-payload-schema/18-CONTEXT.md` D-01 — the
      additive-migration guarantee that the legacy display is byte-for-byte unchanged, which is
      exactly what the display check is verifying
  </read_first>

  <files>.planning/phases/18-merge-precedence-unified-payload-schema/18-09-SUMMARY.md, .planning/ROADMAP.md</files>

  <what-built>
Two production fixes and their proofs, plus a re-taken criterion-1 verdict:

  1. `_addHazardsOutlookGridEntries`'s emission bound is now `Math.max(gridStart, gridEnd - 1)`,
     so a `wpc-hazards` Precipitation feature carrying `start_date === end_date` — the exact live
     shape captured near Kotzebue, AK on 2026-09-05 — lands on its own grid day instead of being
     silently dropped, while the exclusive-end convention every other fixture uses is unchanged.
  2. `_addRegistryDayGridEntries` now skips `notes.noteReported` when the product's toggle is off,
     so a disabled `wpc-ero`/`wpc-wssi` reports an empty `sources[].reportedDays` instead of
     claiming every day.
  3. Three new probe scenarios (suite 116 -> 119, all green), each individually mutation-proven
     with verbatim failure messages recorded in the 18-10 and 18-11 summaries.
  4. `18-LIVE-CAPTURE.md` criterion 1 re-validated from FAIL to PASS, with the original FAIL trace
     preserved and the evidence explicitly labelled a deterministic replay of the captured values
     rather than a fresh live poll.

D-01 guarantees the legacy display path is untouched: no legacy block was modified, and every
`assert*BlockIntact` guard in the suite still passes. The display check below is what confirms that
guarantee on real hardware rather than on that claim.
  </what-built>

  <how-to-verify>
Two items. Both must be answered before Phase 18 can close.

**Item 1 — live MagicMirror display unchanged (the outstanding 18-09 step-6 confirmation).**
  1. Run `node scripts/probe-payload-resilience.js` on the deployed machine and confirm it prints
     `PROBE RESULT: 119 passed, 0 failed, 0 skipped` and exits 0.
  2. Restart MagicMirror with the MMM-SPCOutlook module at your normal deployed configuration and
     coordinate.
  3. Confirm the on-screen rendering is visually unchanged versus before this gap-closure run: the
     same product sections, the same risk rows, the same proximity badges, the same no-risk gate
     behaviour. Phase 18 is additive only (D-01) — the display reads the legacy blocks, so any
     visible change is a regression, not an improvement.
  4. Confirm the cold-start timing block still logs its four D-18 lines once at startup.

**Item 2 — re-answer the 18-09 Task 3 blocking checkpoint.**
  Read `18-LIVE-CAPTURE.md`'s `#### Re-validation after the 18-10 fix — PASS` subsection and the
  mutation tables in `18-10-SUMMARY.md` and `18-11-SUMMARY.md`. The question is the same one 18-09
  Task 3 asked and you answered "fix before closing": do you accept the phase's criteria verdicts
  as honestly recorded — criterion 1 PASS (re-validated, replay evidence), criterion 2 NOT
  OBSERVABLE / PASS, criterion 4 as re-decided in Task 1, criterion 5 PASS, criterion 6 deferred
  to the milestone per D-19?

Note explicitly: criterion 1's re-validation is a replay of captured live values, not a new live
observation. The Kotzebue feature was valid for 2026-09-10 and may no longer exist upstream. If you
want a fresh live confirmation instead, say so — that is a legitimate answer and would become a new
plan rather than a blocker on this one.
  </how-to-verify>

  <action>
Present the two items above and stop. Do not proceed autonomously past this point.

After the operator answers, and only then:
  - If approved: set `18-09-SUMMARY.md`'s frontmatter `status: paused` to `status: complete`,
    change its `## Status` heading and body to record that Task 3 was re-presented after the
    18-10/18-11 gap-closure fixes and answered "approved", tick the remaining two boxes in
    `## Outstanding before Phase 18 can close` (recording the date of the display confirmation and
    the approval), update its task table row 3 to Complete, and tick
    `- [ ] 18-09-PLAN.md` to `- [x]` in `.planning/ROADMAP.md`'s Phase 18 Wave 7 block.
  - If the operator reports a display regression: do NOT tick anything. Record the reported symptom
    verbatim in `18-09-SUMMARY.md` and stop, returning the regression to the orchestrator as a new
    blocking finding.
  - If the operator asks for a fresh live confirmation instead of the replay: record that request
    verbatim in `18-09-SUMMARY.md`, leave the two boxes unticked, and return it to the orchestrator
    as a follow-on plan request rather than attempting a live poll inside this checkpoint.
  </action>

  <resume-signal>
Answer both items in one message. For item 1, reply `display unchanged` or describe the visual
difference you saw. For item 2, reply `approved`, or state which criterion verdict you do not
accept and why. Both answers are required — a reply covering only one leaves Phase 18 open.
  </resume-signal>

  <verify>
    <human-check>Operator confirms the deployed MagicMirror render is unchanged and re-answers the 18-09 Task 3 criteria checkpoint.</human-check>
    <automated>grep -c 'status: complete' .planning/phases/18-merge-precedence-unified-payload-schema/18-09-SUMMARY.md</automated>
  </verify>

  <acceptance_criteria>
    - The checkpoint was presented and the operator's reply is recorded verbatim in the plan SUMMARY, covering BOTH items. A SUMMARY that records only one answer is incomplete.
    - On approval: `grep -c 'status: complete' .planning/phases/18-merge-precedence-unified-payload-schema/18-09-SUMMARY.md` is 1 and `grep -c 'status: paused' ...` is 0.
    - On approval: `grep -c '^- \[ \]' .planning/phases/18-merge-precedence-unified-payload-schema/18-09-SUMMARY.md` is 0 — every Outstanding box is ticked.
    - On approval: `grep -c '\[x\] 18-09-PLAN.md' .planning/ROADMAP.md` is 1.
    - On a reported regression or a request for fresh live confirmation: the verbatim operator text appears in `18-09-SUMMARY.md`, `status: paused` is retained, no ROADMAP box is ticked, and the finding is returned to the orchestrator.
    - No production source file (`node_helper.js`, `MMM-SPCOutlook.js`, `hazardTaxonomy.js`, `productRegistry.js`, `scripts/probe-payload-resilience.js`) is modified by this task under any outcome.
  </acceptance_criteria>

  <done>The operator has answered both items, and `18-09-SUMMARY.md` plus the ROADMAP reflect that answer exactly — approved and closed, or open with the operator's own words recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Probe-suite fixture output -> a recorded live-criterion verdict | The re-validation evidence is a stubbed-transport replay, not a network observation. Presenting it as the latter would be a false provenance claim about the phase's central failing criterion. |
| Agent-authored tracking documents -> the phase-close decision | STATE.md, ROADMAP.md and REQUIREMENTS.md are the artifacts a later phase and a later reader trust to know what actually shipped. |
| Operator's spoken approval -> a ticked checkbox | The checkpoint's outcome is the only thing that flips 18-09 from paused to complete. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-36 | Repudiation | a fixture replay presented as a fresh live observation, inflating criterion 1's evidence class | mitigate | Task 1's action requires an explicit honesty note; the acceptance criteria require the word `replay` plus a negation of "fresh live poll" in the re-validation subsection and require the validation table row to carry the `(re-validated; FAIL at 2026-09-05 capture time)` qualifier. This is the same T-18-26 discipline `18-LIVE-CAPTURE.md` already applies to every other verdict. |
| T-18-37 | Repudiation | overwriting the original FAIL trace, erasing the record of what live data actually showed | mitigate | Task 1 retitles rather than replaces the heading, and an acceptance criterion greps for the original `clampedEnd (5) < clampedStart (6)` trace line to prove the body survived. |
| T-18-38 | Repudiation | silently upgrading MERGE-04's over-merge half, or silently deleting its deferral row, to make the phase look cleaner | mitigate | Task 1 requires an explicit verdict either way with the coverage reasoning stated; Task 2 permits editing only the deferral row's rationale text, and acceptance criteria grep for both `Phase 18-09` deferral rows still being present. |
| T-18-39 | Tampering | this plan quietly relaxing the D-19 PERF-03 milestone blocker while editing adjacent STATE.md text | mitigate | Task 2's action names the PERF-03 bullet as explicitly out of scope, and an acceptance criterion greps for its exact surviving text. |
| T-18-40 | Elevation of Privilege | the executor self-approving the operator's checkpoint, or ticking the 18-09 boxes without an answer | mitigate | Task 3 is `checkpoint:human-verify gate="blocking"` with `autonomous: false` on the plan; its acceptance criteria require the operator's verbatim reply in the SUMMARY covering both items, and require `status: paused` to be retained on any non-approval outcome. |
| T-18-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install tasks in this plan; no dependency is added or changed. |
</threat_model>

<verification>
- `node scripts/probe-payload-resilience.js` -> `PROBE RESULT: 119 passed, 0 failed, 0 skipped`, exit 0.
- `18-LIVE-CAPTURE.md` carries both the preserved original FAIL trace and the `Re-validation after the 18-10 fix — PASS` subsection with its replay honesty note.
- `deferred-items.md` carries two `**Resolved:**` lines; STATE.md carries a `RESOLVED (18-10` blocker line, both `Phase 18-09` deferral rows, and the untouched PERF-03 milestone blocker.
- `REQUIREMENTS.md` shows MERGE-01 `[x]` / `Complete`; `ROADMAP.md` shows `12 plans in 10 waves` and three new wave blocks.
- The operator's verbatim two-part answer is recorded, and `18-09-SUMMARY.md`'s status matches it.
- `git diff --name-only` lists only files under `.planning/` — this plan modifies no source.
</verification>

<success_criteria>
- ROADMAP Phase 18 success criterion 1 no longer reads FAIL, and its new verdict is traceable to specific, honestly-classified evidence rather than an assertion.
- MERGE-04's over-merge half has an explicit re-decided verdict, not a silently carried-forward one.
- Every item on 18-09's "Outstanding before Phase 18 can close" checklist is either ticked or carries the operator's own words explaining why it is not.
- No production source file is touched by this plan.
</success_criteria>

<output>
Create `.planning/phases/18-merge-precedence-unified-payload-schema/18-12-SUMMARY.md` when done.
Record the operator's verbatim reply to both checkpoint items, the criterion-1 and criterion-4
verdicts as written into `18-LIVE-CAPTURE.md`, and the resulting status of every remaining Phase 18
open item.
</output>
