---
context: phase
phase: 19-unified-day-report-getdom-rewrite
plan: 19-06
task: 1
total_tasks: 9
status: in_progress
last_updated: 2026-09-07T04:13:20.778Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure
> during waves 1-4 of this phase. Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: **Stale executor worktree bases** — Two separate executor agents (19-01, 19-03) were spawned into worktrees whose HEAD sat on an old commit (`4096c2c`) that did not contain the phase-19 plan set. Both caught it only because the dispatch prompt carried an explicit `EXPECTED_BASE` assertion with a `git reset --hard` correction. Structural mitigation: **every** executor dispatch prompt must embed the current `git rev-parse HEAD` as `EXPECTED_BASE` inside the `<worktree_branch_check>` block. Do not dispatch without it.

- [ ] CONSTRAINT: **Probe suite cannot observe pure-CSS behavior** — Plan 19-05 applied a mutation removing `white-space:pre-wrap` and it produced *zero* RED scenarios. A green probe run is therefore NOT proof of render parity for whitespace, font sizing, or any presentation-only property. Structural mitigation: 19-08's two manual runs are the only gate for these; they cannot be waived or substituted with a probe run.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Stale worktree base | Executor worktree forked from a commit missing prior-wave work; would have silently rebuilt or clobbered merged plans | blocking | Embed `EXPECTED_BASE` (current HEAD) + `git merge-base` assertion + `git reset --hard` correction in every executor dispatch prompt |
| Probe-invisible CSS mutation | `white-space:pre-wrap` removal left the suite fully green — presentation properties are outside probe reach | blocking | Route all presentation-parity claims to 19-08's manual Run A / Run B; never accept a green suite as whitespace/layout proof |
| Mutation with no reachable effect | Two of 19-03's specified mutations had zero effect through the real fixture pipeline, because every non-`SIGNIFICANCE_NEVER` source is always rank-1 in its own dimension | advisory | When a specified mutation cannot go RED through the real pipeline, add a direct-call assertion against the same production method with synthetic inputs, and disclose it in SUMMARY Deviations |
| Parity checklist drift | 19-04 filled only the rows its own tasks named, leaving 14 preserved-behavior rows with empty `New site` cells | advisory | Each remaining plan fills the rows its work lands on; 19-08 treats any unfilled row as a blocking sign-off item |

<current_state>
Paused cleanly at a **wave boundary**. Waves 1-4 (plans 19-01 … 19-05) are merged into `main`,
every executor worktree has been removed, no `worktree-agent-*` branches remain, and the working
tree is clean apart from pre-existing untracked noise (`node_modules/`, `.codegraph/`, `pnpm-lock.yaml`,
a deleted `.idea/` backup file — none of these are phase-19 work).

HEAD is `0caa4b3`. Wave 5 (plan 19-06) has **not** been dispatched.

The post-merge probe gate was run after every single wave and passed each time:
123 (pre-phase baseline) → 126 → 128 → 134 → **143 passed, 0 failed, 0 skipped**.
</current_state>

<completed_work>

Completed Plans (5 of 9):
- **19-01** (wave 1): RPT-06 parity checklist artifact — 35 preserved-behavior rows, 10 proximity-mode
  call sites, 4 intentional-change rows, two manual-run procedures, unchecked sign-off block. The
  executor re-verified every `Old site` line reference against a full read of the pre-rewrite
  `MMM-SPCOutlook.js:184-784` and corrected 4 discrepancies (rows 9, 22, 23, 29).
- **19-02** (wave 1): SPC proximity subtree threaded into the unified `days[]` grid. Three named
  proximity-subtree consts in `node_helper.js`, spread into both `spcLocals.categorical[1..3]` and
  the legacy `day1`/`day2`/`day3` literals; `_addSpcGridEntries` sets `gridDays[d].proximity` above
  the no-risk floor gate.
- **19-03** (wave 2): `SIGNIFICANCE_FLOOR`/`SIGNIFICANCE_NEVER` in `hazardTaxonomy.js` (integrity-guarded
  by `assertTaxonomyIntegrity`), `_resolveGridDayAutoExpand` in `node_helper.js` emitting
  `days[n].autoExpand`. Paused at a blocking decision checkpoint; resolved by the user — see Decisions.
- **19-04** (wave 3): the structural core. `getDom()` rewritten — unified empty-state ladder reading only
  `summary`, relocated guard helpers, `DIMENSION_LABELS`/`SOURCE_SHORT_NAMES`, `daySurvivors`/
  `dayProximityOnly`, and ONE compact day loop over `days[1..14]` replacing seven legacy per-product
  render sections. Net −536 lines. `grep -c renderDayBlock` → 0.
- **19-05** (wave 4): detail mode — detail dispatcher, dimension sub-rows, "also:" lines, whitespace
  preservation, three-shape probabilistic sub-line, ten proximity mode decisions.
</completed_work>

<remaining_work>

- **19-06** (wave 5, autonomous): Move every non-day-scoped item into one band below the day blocks;
  make the unified report canonical. Touches `node_helper.js`, `MMM-SPCOutlook.js`,
  `scripts/probe-payload-resilience.js`, and the parity checklist. **This plan's scope covers most of
  the unfilled checklist rows** (advisory band rows 10-11, window-band rows 26-28) — fill them here.
- **19-07** (wave 6, autonomous): Close the two remaining carried-in backend items.
- **19-08** (wave 7, **autonomous: false**): Close RPT-06 — this phase's actual acceptance gate.
  Will stop for human verification. Requires the two manual runs.
- **19-09** (wave 8, **autonomous: false**): Retire the legacy render path; settle the backend-emission
  deletion with a measured number.

**Unfilled parity checklist rows** (`New site` column empty) — carried forward, must all close before
19-08 sign-off:
`3, 4, 5, 7, 8, 9, 10, 11, 18, 24, 26, 27, 28, 35`
Themes: no-risk gate terms (3, 5, 7), staleness/stale-badge (4, 8), contentMarker fallback (9),
advisory band (10, 11), fire-weather gating (18), hazards label folding (24), window band (26, 27, 28),
and the frontend advisory-registry naming (35).
</remaining_work>

<decisions_made>

- **autoExpand = per-source significance floor, `wpc-hazards` never triggers alone.** User-selected at
  19-03's blocking decision checkpoint. Table: `spc-convective` ≥ 4 (ENH+), `spc-fire` ≥ 2 (CRIT+),
  `wpc-ero` ≥ 3 (MDT+), `wpc-wssi` ≥ 4 (MAJOR+), `heatrisk` ≥ 3 (Major+), `wpc-hazards` never.
  Chosen over "hazards always" (would make auto-expand near-permanent on busy long-range days) and over
  a ≥3-dimension count trigger (a single HIGH-risk convective day — the most serious display the module
  produces — would never auto-expand). **Accepted cost:** `cold`/`wind`/`heavy-precip`, whose only source
  is `wpc-hazards`, can never auto-expand on their own. `19-RESEARCH.md` tagged these cutoffs `[ASSUMED]`;
  they are now locked.
- **Staleness disqualifies the empty-state short-circuit** (19-04, Rule 1 bug fix). A degraded read must
  never render as a confident all-clear, so the ⚠ badge always renders. Recorded as checklist row 4.
- **`showMinorHeat` floor re-homed into `daySurvivors`** (19-04, Rule 2 gap). The legacy floor had no home
  in the new architecture; behavior preserved, location changed.
- **`proximityBadge` output escaped in the probabilistic sub-line** (19-05). A real security gap (T-19-18)
  found by the executor's own threat-model self-review: per-type proximity badges bypassed the shared
  `escapeHtml` guard the label field gets. Fixed and pinned with a mutation-proven scenario.
</decisions_made>

<blockers>
- None technical. The phase is green and unblocked.
- Two human actions are pending but not yet due — they gate 19-08, not 19-06. See Required Reading.
</blockers>

## Required Reading (in order)
1. `.planning/phases/19-unified-day-report-getdom-rewrite/19-06-PLAN.md` — the next plan to execute.
2. `.planning/phases/19-unified-day-report-getdom-rewrite/19-PARITY-CHECKLIST.md` — the RPT-06 acceptance
   gate. Check the unfilled-row list above against it; fill what 19-06 lands on.
3. `.planning/phases/19-unified-day-report-getdom-rewrite/19-04-SUMMARY.md` — read its **Deviations**
   section before touching `getDom()`; four deviations are documented there, two substantive.
4. `.planning/phases/19-unified-day-report-getdom-rewrite/19-05-SUMMARY.md` — the detail-mode contract
   you are extending, plus the `white-space:pre-wrap` non-observability disclosure.
5. `.planning/METHODOLOGY.md` (if it exists) — project analytical lenses.

## Infrastructure State
- **Git**: on `main` at `0caa4b3`. No worktrees beyond the primary. No `worktree-agent-*` branches.
  `branching_strategy` is `none`, so all phase work commits directly to `main`.
- **Test gate**: `node scripts/probe-payload-resilience.js` → 143 passed, 0 failed, 0 skipped.
  This is the post-merge gate; run it after every wave merge and do not let it regress.
- **Untracked noise, pre-existing, not phase-19**: `node_modules/`, `.codegraph/`, `pnpm-lock.yaml`,
  and a deleted `.idea/workspace-dialetics-safeBackup-0001.xml`. Leave them alone.
- **Placeholder scan**: `grep -l "To be filled\|placeholder\|TBD"` flags ~23 files, **all in phases 14-17**.
  No phase-19 artifact is flagged. Pre-existing debt, out of scope here.
- **Target hardware**: the live MagicMirror Pi is reachable as `ssh mm` — needed for 19-08's manual runs.
- **Executor config**: `executor_model: sonnet`, `verifier_model: sonnet`, `parallelization: true`,
  `use_worktrees: true`, `runtime: claude`.

## Known tooling false-negative
`bm-sdk query verify.key-links` reports `"Source file not found"` for entries written as
`"MMM-SPCOutlook.js getDom()"` — it fails to strip the symbol suffix and treats the whole string as a
path. `MMM-SPCOutlook.js` exists. Do not chase this; verify links by hand when the check disagrees.

<context>
This session was orchestrating `/bm:execute-phase 19` — nine plans, eight waves, wave-by-wave with
worktree-isolated `bm:gsd-executor` subagents. I kept the SDK's DAG suggestion of collapsing 19-08 into
wave 6 *rejected* on purpose: the planner declared 19-08 as wave 7, and the parity gate should run after
the backend items in 19-07 that it must verify. Keep that ordering.

Per-wave protocol used throughout, worth repeating: capture `EXPECTED_BASE` → dispatch executor(s) with
that base asserted → on return, check `git diff --diff-filter=D` for deletions → back up STATE/ROADMAP →
merge `--no-ff` → restore STATE/ROADMAP → remove worktree + delete branch → run the probe suite as a
post-merge gate → `roadmap.update-plan-progress` → commit tracking.

The hard part of this phase is behind us. 19-04 was the structural rewrite and it landed green with
seven legacy render sections collapsed into one loop. What is left is additive (19-06, 19-07) and then
two human-gated closure plans.
</context>

<next_action>
Start with: `/bm:execute-phase 19` — plan discovery will skip 19-01…19-05 (they have SUMMARY.md files and
are marked complete) and resume at wave 5, plan 19-06. Capture the current HEAD as `EXPECTED_BASE` before
dispatching, and remind the 19-06 executor to fill the parity checklist rows its work lands on.
</next_action>
