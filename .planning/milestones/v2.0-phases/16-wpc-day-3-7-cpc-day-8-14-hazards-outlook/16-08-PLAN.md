---
phase: 16-wpc-day-3-7-cpc-day-8-14-hazards-outlook
plan: 08
type: execute
wave: 7
depends_on: ["16-07"]
files_modified:
  - .planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-MUTATION-INVENTORY.md
  - .planning/STATE.md
autonomous: false
requirements: [HAZ-01, HAZ-02, HAZ-03, HAZ-04, DATA-02]

must_haves:
  truths:
    - "Every probe scenario added this phase has a recorded mutation, a recorded RED message, and a recorded restore (Phase 15 D-10)"
    - "The five ROADMAP success criteria are each checked off against a named piece of evidence"
    - "Verification that genuinely cannot be performed now is recorded as a deferred item with its reason, not silently claimed"
    - "The day9..day14 audit is discharged with a named result, not an assumption"
  artifacts:
    - path: ".planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-MUTATION-INVENTORY.md"
      provides: "Consolidated mutation inventory for every Phase 16 scenario and frontend check"
      contains: "verbatim RED message"
  key_links:
    - from: "16-MUTATION-INVENTORY.md"
      to: "16-04/16-05/16-06/16-07 SUMMARYs"
      via: "consolidation of the per-plan mutation records"
      pattern: "16-0[4-7]-SUMMARY"
---

<objective>
Consolidate the phase's mutation evidence, discharge the outstanding audits, and run the human UAT
pass against the five ROADMAP success criteria.

Purpose: this project has no automated test framework. A green suite is only evidence if every
scenario was proven to fail under its own mutation (STATE.md records a scenario green under its own
mutation as a fixture defect, not a pass). This plan is where that evidence becomes a single
reviewable artifact and where the human confirms the display actually behaves.

Output: `16-MUTATION-INVENTORY.md`, a discharged `day9..day14` audit, an updated STATE.md deferred
list, and a human sign-off.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/buildomator/bm/4.5.5/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/buildomator/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-CONTEXT.md
@.planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-RESEARCH.md
@.planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-04-SUMMARY.md
@.planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-05-SUMMARY.md
@.planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-06-SUMMARY.md
@.planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate the mutation inventory and discharge the day9..day14 audit</name>
  <files>.planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-MUTATION-INVENTORY.md, .planning/STATE.md</files>
  <read_first>
    - .planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-04-SUMMARY.md,
      16-05-SUMMARY.md, 16-06-SUMMARY.md, 16-07-SUMMARY.md — the per-plan mutation records
      (3 + 6 + 11 + 14 = 34 expected entries; reconcile against what is actually recorded).
    - .planning/phases/15-wpc-winter-storm-severity-mesoscale-precipitation-discussion/15-09-PLAN.md
      — Phase 15's "phase mutation inventory" task, for the format precedent.
    - .planning/STATE.md § Accumulated Context (the Phase 15 D-10 rule) and § Deferred Items
      (the format for a recorded deferral, including the two verification deferrals from Phase 15).
    - MMM-SPCOutlook.js — the whole file, for the `day9..day14` audit below.
    - scripts/probe-payload-resilience.js `:715-760` — `assertPayloadIntact`'s `d = 1; d <= 8` loops.
  </read_first>
  <action>
    **Part A — write `16-MUTATION-INVENTORY.md`.** One table, one row per mutation performed
    anywhere in the phase, with columns:
    `Plan | Scenario or check | File:line mutated | What was broken | Verbatim RED message | Restored`.
    Draw every row from the four SUMMARYs. Reconcile the count: if any scenario added in 16-06 or
    16-07 has no mutation row, that is a blocking gap — list it in a `## Unproven Scenarios` section
    with the reason, and flag it in this plan's SUMMARY rather than quietly omitting it.

    Add a `## Structurally-Proven, Not Mutation-Proven` section for any check where no threshold or
    branch exists to break (Phase 15 F3 set this precedent with
    `wssi-zero-features-out-of-season`). Each entry must state WHY no mutation exists, not merely
    that none was performed.

    Add a `## Vacuity Preventatives` section listing, per scenario, whether it carries a
    **precondition guard**, a **control assertion**, or both — the two mechanisms STATE.md records
    as proven against Phase 15's two vacuity modes. Any scenario with neither is a finding: name it.

    **Part B — discharge the `day9..day14` audit.** CONTEXT.md's constraint states that `day9`
    through `day14` are new to this module and that every frontend loop bounded at 8 must be
    audited. Perform it and record the result explicitly in the inventory under
    `## day9..day14 Audit`:
    - `grep -n "d <= 8\|d<=8\|day8\|<= 8" MMM-SPCOutlook.js scripts/probe-payload-resilience.js node_helper.js`
    - For each hit, state whether it is (a) a genuinely day-1-through-8-scoped product (SPC
      categorical Day 4–8 at `MMM-SPCOutlook.js:393-400`, the fire-weather loop at `:413`,
      `assertPayloadIntact`'s two `1..8` loops over TOP-LEVEL keys), or (b) a bound that would
      silently truncate the new product. RESEARCH.md's Frontend Integration Risk section concludes
      (a) for all of them, because `hazardsOutlook`'s day keys are nested INSIDE its own block and
      never appear at the payload's top level. Confirm or refute that conclusion against the code as
      it now stands, and record the verdict per hit. A refutation is a finding to fix, not a note.
    - Additionally confirm `dayRiskCount`'s `/^day\d+Risk$/` regex does not match any
      `hazardsOutlook` key (it has no `Risk` suffix), so `blockHasRisk` cannot be accidentally
      applied to this block.

    **Part C — record deferrals in STATE.md.** Append to `## Deferred Items` any verification that
    genuinely cannot be performed at execution time, following the existing table format and the
    v1.1 fire-weather / Phase 15 WSSI precedent. Expected candidates, to be confirmed or removed
    based on what UAT in Task 2 actually observes:
    - Live in-season confirmation of HAZ-01's per-day Precipitation bucketing — both Precipitation
      layers returned zero features on 2026-08-26, so there was no live example to validate against;
      the synthetic probe fixtures stand as evidence.
    - Live confirmation of D-04's full-window guard, for the same reason.
    - Live observation of `idp_filedate` across a weekend to settle whether D-14's 84h can be
      tightened (CONTEXT.md Deferred Ideas already names this).
    Do NOT record a deferral for anything Task 2's UAT actually succeeds in observing.
  </action>
  <verify>
    <automated>bash -c 'F=.planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-MUTATION-INVENTORY.md; test -f "$F" || { echo "inventory missing"; exit 1; }; for S in "Unproven Scenarios" "Structurally-Proven" "Vacuity Preventatives" "day9..day14 Audit"; do grep -q "$S" "$F" || { echo "missing section: $S"; exit 1; }; done; ROWS=$(grep -c "^| 16-0" "$F"); echo "mutation rows: $ROWS"; [ "$ROWS" -ge 20 ] || { echo "only $ROWS mutation rows recorded; expected roughly 34 across plans 04-07"; exit 1; }; echo OK'</automated>
    <automated>node scripts/probe-payload-resilience.js</automated>
    <automated>bash -c 'grep -n "d <= 8\|d<=8\|<= 8" MMM-SPCOutlook.js node_helper.js scripts/probe-payload-resilience.js | tee /dev/stderr | wc -l'</automated>
  </verify>
  <acceptance_criteria>
    - `16-MUTATION-INVENTORY.md` exists with all four required sections and at least 20 mutation
      rows (the four SUMMARYs record roughly 34; the exact reconciled count is stated in the file).
    - Every mutation row carries a **verbatim** RED message, not a paraphrase, and a `Restored: yes`
      value. A row whose RED message is blank or whose restore is unconfirmed is a blocking finding.
    - Every scenario added in 16-06 and 16-07 appears in either the mutation table or the
      `## Structurally-Proven, Not Mutation-Proven` section with a stated reason. None is silently
      absent.
    - Every scenario is listed in `## Vacuity Preventatives` with `guard`, `control`, or `both`.
      Any scenario with `neither` is named as a finding in this plan's SUMMARY.
    - The `## day9..day14 Audit` section names every grep hit and gives a per-hit verdict of
      "correctly day-1..8-scoped" or "would truncate the new product". Any hit in the second
      category is fixed before this plan completes.
    - The full probe suite is green.
    - STATE.md's `## Deferred Items` table has a row for each verification genuinely not performable
      now, each with a reason and `Deferred At: Phase 16 close`.
  </acceptance_criteria>
  <done>The inventory exists and reconciles, the day9..day14 audit is discharged with per-hit verdicts, the suite is green, and STATE.md records only the deferrals that Task 2's UAT could not discharge.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human UAT against the five ROADMAP success criteria</name>
  <files>config.js (temporary, restored at the end), .planning/STATE.md</files>
  <read_first>
    - .planning/ROADMAP.md § "Phase 16" — the five success criteria verbatim.
    - .planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-05-SUMMARY.md — the recorded
      rendered markup for one populated day row and one window-band entry, so you know what to look
      for on screen.
    - .planning/STATE.md § Accumulated Context — the live-verification procedure for
      location-gated products: waiting for a product to appear over the deployed coordinate is not
      viable; move `lat`/`lon` into an active polygon, verify with a point-in-polygon test against
      the live payload (a bounding-box check is NOT sufficient), confirm the render, restore.
  </read_first>
  <action>
    Checkpoint task — the automation is already complete; this step is human confirmation that the
    shipped display behaves. Before pausing, prepare the operator by printing the current values of
    `showHazardsOutlook`, `showDrought`, `lat` and `lon` from `config.js` so they can be restored
    verbatim afterwards, and by fetching the current feature counts for all six layers
    (`.../MapServer/{1,3,4,6,7,8}/query?where=1%3D1&outFields=*&f=geojson`) so the operator knows
    which of C1-C5 are observable today before they start. Record those counts in the SUMMARY —
    they are the evidence behind any NOT OBSERVABLE verdict. Then pause for the human.
  </action>
  <what-built>
    The complete Hazards Outlook product: registry row, backend runner with a clock-independent
    cache contract and per-layer freshness, the two no-risk gate terms, the day-grid renderer and
    the window-band renderer, plus 12+ mutation-proven probe scenarios.
  </what-built>
  <how-to-verify>
    Preparation:
    1. In `config.js`, set `showHazardsOutlook: true` and leave `showDrought` unset (the shipped
       default, `false`).
    2. Choose a coordinate that is currently inside a live hazard polygon. Do NOT wait for one to
       appear over the deployed coordinate. Fetch
       `https://mapservices.weather.noaa.gov/vector/rest/services/hazards/cpc_weather_hazards/MapServer/1/query?where=1%3D1&outFields=*&f=geojson`
       (and layers 3, 4, 6, 7, 8), pick a feature, and choose a point genuinely INSIDE its polygon —
       verify with a point-in-polygon test, not a bounding-box check (STATE.md records Phoenix
       falling inside MPD_1122's bbox but outside its polygon). Set `lat`/`lon` to that point.
    3. Restart MagicMirror and let one poll complete.

    Checks — record PASS/FAIL and what you actually saw for each:

    C1 (HAZ-01) — If a Precipitation layer (4 or 6) has a feature covering your point, confirm you
      see per-day rows in the form `Hazards (Fri, Day 5): Heavy Rain`, with the weekday matching the
      feature's own `start_date` in UTC. If both Precipitation layers are empty nationwide (they
      were on 2026-08-26), record C1 as NOT OBSERVABLE and note the layer counts you saw — this is a
      real seasonal condition, not a failure, and it becomes a deferred item.

    C2 (HAZ-02) — Confirm any Temperature or Wildfire hazard covering your point appears in the
      `Extended Hazards:` band BELOW the day rows, exactly once, labeled with its own observed span
      (e.g. `Thu–Mon (D3–7): Hazardous Heat`). Confirm it is NOT repeated as a row on each day of
      that window. Confirm the band is separate from the MD/MPD advisory band and does not say
      "in effect".

    C3 (HAZ-03) — Open the raw payload for a layer covering your point and confirm the feature's
      hazard type is carried in the lowercase `label` field. Confirm that exact string is what
      rendered on screen. This is the deliberate live inspection ROADMAP criterion 3 asks for.

    C4 (HAZ-04) — Confirm no `Flooding Likely` / `Flooding Occurring or Imminent` /
      `Flooding Possible` string appears anywhere on the mirror, and no `Severe Drought` /
      `Rapid Onset Drought Risk` either. **Verify this at the shipped default `showDrought: false`.**
      Note: on 2026-08-26, layer 7 returned 26× `Severe Drought` and zero `Critical Wildfire Risk`,
      so at the default the Wildfire/Drought layer may render nothing at all — that is the correct
      outcome, not a bug. Then set `showDrought: true`, restart, and confirm drought labels DO
      appear while Flooding labels still do NOT. Restore `showDrought` to unset.

    C5 (DATA-02) — Confirm no spurious ⚠ Stale badge attributable to the Hazards Outlook. If you can
      run this on a Saturday or Sunday, that is the direct test. If not, confirm from the logs /
      payload that each layer's `idp_filedate` is within 84 hours and that `_stale` is not set for
      that reason.

    Also confirm, at the end: with `showHazardsOutlook: false` (the shipped default), the display is
    byte-identical to its pre-Phase-16 behaviour for every other product.

    Restore `lat`/`lon` and `config.js` to their original values when finished.
  </how-to-verify>
  <acceptance_criteria>
    - Each of C1–C5 is recorded as PASS, FAIL, or NOT OBSERVABLE, with a note describing what was
      actually seen — not a bare checkbox.
    - Any FAIL blocks phase completion and is written up as a gap for `/bm:plan-phase 16 --gaps`.
    - Any NOT OBSERVABLE is recorded in STATE.md `## Deferred Items` with its reason, following the
      v1.1 fire-weather and Phase 15 WSSI precedent. It is NOT recorded as a PASS.
    - The `showDrought: true` half of C4 is performed and its result recorded — D-10's opt-in is a
      shipped behaviour and must be seen working, not inferred.
    - The default-off confirmation is performed: `showHazardsOutlook: false` produces no change to
      any other product's display.
    - `lat`/`lon` and `showDrought` are restored to their original values, confirmed by a final
      `git diff` showing no unintended config change.
  </acceptance_criteria>
  <resume-signal>Type "approved" with the C1–C5 results, or describe what failed.</resume-signal>
  <verify>
    <automated>bash -c 'git diff --name-only | grep -qx "config.js" && { echo "config.js still modified after UAT - restore lat/lon and showDrought"; exit 1; }; echo OK'</automated>
    <automated>node scripts/probe-payload-resilience.js</automated>
    <human-check>C1-C5 each recorded as PASS, FAIL or NOT OBSERVABLE with a description of what was actually seen on the mirror; the showDrought:true half of C4 performed; the default-off confirmation performed.</human-check>
  </verify>
  <done>All five ROADMAP success criteria have a recorded human verdict, every NOT OBSERVABLE is written into STATE.md's Deferred Items with its reason, config.js is restored, and the probe suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Recorded evidence -> phase completion decision | The inventory and the UAT results are what the verifier and the human rely on to close the phase |
| Live NOAA payload -> human inspection | C3 and C4 read untrusted remote content directly during verification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-32 | Repudiation | A scenario claimed as mutation-proven without evidence | mitigate | Every row requires a verbatim RED message and a confirmed restore; scenarios with no mutation row must appear in the `## Unproven Scenarios` or `## Structurally-Proven` section with a stated reason. Silence is a blocking finding. |
| T-16-33 | Spoofing | A NOT OBSERVABLE check recorded as a PASS | mitigate | The checkpoint's acceptance criteria require three distinct outcomes and require NOT OBSERVABLE to be written into STATE.md's deferred table. This follows the v1.1 fire-weather and Phase 15 WSSI precedent, both of which are already recorded deferrals rather than claimed passes. |
| T-16-34 | Tampering | A temporary UAT `lat`/`lon` change committed by accident | mitigate | The checkpoint requires restoring the coordinate and confirming via `git diff` that no unintended config change remains. |
| T-16-35 | Information Disclosure | The UAT coordinate revealing the operator's location | accept | The coordinate moved during UAT is deliberately NOT the operator's; the original is already committed in this repository's history. No new exposure. |
| T-16-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — zero packages installed in this phase. |
</threat_model>

<verification>
- `16-MUTATION-INVENTORY.md` exists, reconciles against the four SUMMARYs, and has no blank RED
  message.
- The `day9..day14` audit is discharged with a per-hit verdict.
- `node scripts/probe-payload-resilience.js` is green.
- Human UAT recorded C1–C5 with PASS / FAIL / NOT OBSERVABLE and a description of what was seen.
- STATE.md `## Deferred Items` carries a row for each NOT OBSERVABLE result.
- **ROADMAP success criterion 4 reading (binding for the verifier):** criterion 4 ("Flooding and
  Drought sub-labels never appear") is satisfied **at the shipped default config**
  (`showDrought: false`), which CONTEXT.md D-10 establishes. Drought display is an explicit user
  opt-in, not a violation. Flooding, by contrast, is absolute — D-09 provides no override. Do not
  read criterion 4 as an absolute prohibition on Drought and fail a correct implementation.
</verification>

<success_criteria>
- Every Phase 16 probe scenario is either mutation-proven with a recorded RED message or explicitly
  listed as structurally-proven with a reason.
- Every scenario is listed with its vacuity preventative (guard / control / both).
- The `day9..day14` audit is discharged with named results rather than an assumption.
- All five ROADMAP success criteria have a recorded human verdict, with unobservable items deferred
  honestly rather than claimed.
</success_criteria>

<output>
Create `.planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-08-SUMMARY.md` when done,
recording the reconciled mutation count, the day9..day14 audit verdicts, the C1–C5 UAT results,
and every deferral written into STATE.md.
</output>
