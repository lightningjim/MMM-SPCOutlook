---
phase: 16-wpc-day-3-7-cpc-day-8-14-hazards-outlook
plan: 03
type: execute
wave: 2
depends_on: ["16-01", "16-02"]
files_modified:
  - node_helper.js
autonomous: true
requirements: [HAZ-01, HAZ-02, HAZ-03, HAZ-04, DATA-02]

must_haves:
  truths:
    - "No cache entry this product writes contains a day key or a day offset, so a cache hit cannot replay yesterday's day assignment (HIGHEST-RISK ITEM 1)"
    - "Day bucketing is recomputed on every poll — cache hit or miss — against that poll's own todayUtcMs (HIGHEST-RISK ITEM 1)"
    - "Flooding labels are dropped before any evaluation, with no config path that re-enables them (D-09, HAZ-04)"
    - "Drought labels are dropped at the shipped default and appear only when showDrought is explicitly true (D-10, HAZ-04)"
    - "Each of the six layers' freshness is judged against its own idp_filedate, never one shared timestamp (D-13, DATA-02)"
    - "A data-age trip sets the global stale flag but never contributes to the 'N minutes ago' figure (D-15)"
    - "A layer that legitimately returns zero features raises no staleness signal (WSSI-03 precedent, DATA-02)"
    - "Same-day hazards are ordered by the registry's declared order, not ArcGIS response order (D-02)"
    - "The payload always carries day3..day14 and windowBand regardless of the toggle (Phase 14 D-05)"
    - "An unmapped label reaches the payload verbatim with the default color and is logged once (D-11)"
  artifacts:
    - path: "node_helper.js"
      provides: "_hazardMatchesFromHits, _cacheHazardMatches, _runArcGisHazardWindowProduct, hazardsOutlook payload key, showDrought sub-toggle"
      contains: "_runArcGisHazardWindowProduct"
  key_links:
    - from: "_runArcGisHazardWindowProduct"
      to: "PRODUCT_REGISTRY.hazardsOutlook.buildUrl"
      via: "one URL per layer, per poll"
      pattern: "row\\.buildUrl\\(layer\\.id\\)"
    - from: "_runArcGisHazardWindowProduct"
      to: "_bucketHazardMatch"
      via: "recomputed every poll from normalized matches"
      pattern: "_bucketHazardMatch\\("
    - from: "getSpcOutlook"
      to: "hazardsOutlook payload key"
      via: "sibling block beside excessiveRain/winterImpact"
      pattern: "hazardsOutlook: hazardsPayload"
---

<objective>
Add the `arcgis-hazard-window` runner to `node_helper.js`: fetch the six layers, filter labels,
collect all containing features, normalize them into clock-independent matches, cache ONLY those
matches, re-bucket into `day3`..`day14` + `windowBand` on every poll, apply the per-layer data-age
check, and emit the `hazardsOutlook` payload block.

Purpose: this plan carries the phase's #1 risk. `_runArcGisDayProduct` caches a final day-keyed
result — safe for ERO/WSSI because their day key is a property of the URL, catastrophic here
because this product's day key is a property of the **clock**. With this product's confirmed
Mon–Fri-only cadence, every weekend poll is exactly that scenario, and copying the analog produces
a wrong-but-confident render with no error and no ⚠.

Output: three new methods, one dispatch call site, one payload key, one sub-toggle path.
Frontend rendering is 16-04/16-05; probes are 16-06/16-07.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/buildomator/bm/4.5.5/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/buildomator/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-CONTEXT.md
@.planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-RESEARCH.md
@.planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-PATTERNS.md
@.planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-01-SUMMARY.md
@.planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-02-SUMMARY.md

<interfaces>
<!-- Contracts this plan consumes. Extracted from the codebase and from plans 16-01/16-02. -->

From productRegistry.js (16-01):
  PRODUCT_REGISTRY.hazardsOutlook = {
    id, kind: "arcgis-hazard-window", configFlag: "showHazardsOutlook", baseUrl,
    layers: [{ id, group, dayRange }, ...]   // 6 entries, groups: temperature|precipitation|wildfireDrought
    dayRangeTotal: [3, 14],
    excludedLabels: [...3 Flooding labels],  // D-09, hard
    droughtLabels: [...2 Drought labels],    // D-10, gated
    order: [...6 labels],                    // D-02
    displayColor: { label -> "rrggbb" },     // 15 keys
    defaultColor: "aaaaaa",                  // D-11
    buildUrl: (layerId) => string,
    toValue: (label, f) => string,           // reads f.properties.label (HAZ-03)
    maxDataAgeHours: 84,                     // D-14
    paletteSource: string
    // NOTE: no includesFeat, no valueToTier/tierToText/tierToColor. Deliberate.
  }

From node_helper.js (16-02):
  _nowMs() -> number                                   // seam
  _todayUtcMs() -> number                              // UTC midnight, derived from _nowMs()
  _hazardDayOffset(epochMs, todayUtcMs) -> number
  _utcDateString(epochMs) -> "YYYY-MM-DD" | null
  _isFullNominalWindow(offsetStart, offsetEnd, dayRange) -> boolean
  _bucketHazardMatch(match, layer, todayUtcMs, dayBuckets, windowEntries) -> void
     match := { label: string, startDate: number, endDate: number, idpFiledate: number|null }
  evaluatePolygonsCollectAll(items, loc) -> hits[]     // items from extractPolygons
  this._loggedUnmappedHazardLabels : Set              // reset in start()

Pre-existing node_helper.js:
  extractPolygons(data, toValue, includesFeat, url) -> [{ label, value, poly, feature }, ...]
  async fetchGeoJsonCached(url) -> { data, cachedResult, stale, failed, mode, newEtag, newHash }
     On a 304/hash hit: data === null and cachedResult === the previously cached `result`.
  this._geoJsonCache : Map<url, { mode, etag, hash, result, timestamp }>
  _noteStaleEntry(entry)                               // writes _oldestStaleAt — D-15 forbids
                                                       //   calling it for a data-age trip
  _productToggles(products) -> { [configFlag]: boolean }
  getSpcOutlook(...) call sites for ERO/WSSI at node_helper.js:2186-2202; payload keys
  `excessiveRain: eroPayload, winterImpact: wssiPayload, advisories` at :2329-2331.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clock-independent cache contract — _hazardMatchesFromHits and _cacheHazardMatches</name>
  <files>node_helper.js</files>
  <read_first>
    - node_helper.js `:213-307` — `_runArcGisDayProduct` IN FULL, especially the cache-write block
      at `:267-273`. **You are reading this to invert it, not to copy it.** Note exactly what it
      stores in `result` (`{ value, validTime }` — a final, already-resolved per-day answer).
    - node_helper.js `:1440-1530` — `fetchGeoJsonCached`'s return shape and the branches that
      populate `cachedResult` vs `data`, so you know precisely which field is live on a 304.
    - node_helper.js `:990-1060` — `extractPolygons` and (from 16-02) `evaluatePolygonsCollectAll`.
    - .planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-RESEARCH.md
      § "Freshness Integration (DATA-02, D-13/D-14/D-15)" — read the whole section, especially
      "Newly identified pitfall: day-offset drift on a cache hit".
    - .planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-PATTERNS.md
      § "node_helper.js — new _runArcGisHazardWindowProduct sibling runner" — the
      "⚠ CACHE-WRITE BLOCK MUST NOT BE COPIED VERBATIM" callout.
  </read_first>
  <action>
    Add two methods to `node_helper.js`, placed immediately before where the runner will go
    (after `_runArcGisDayProduct`).

    **1. `_hazardMatchesFromHits(hits)`** — pure normalizer. Maps each hit from
    `evaluatePolygonsCollectAll` to exactly the four-field match shape and nothing else:
      `{ label, startDate, endDate, idpFiledate }`
    where `label` is `hit.value` (already the lowercase `label` string, per the registry's
    `toValue`), `startDate` is `props.start_date`, `endDate` is `props.end_date`, and `idpFiledate`
    is `props.idp_filedate` when it is a finite number, otherwise `null`. Drop (do not include) any
    hit whose `label` is not a non-empty string or whose `start_date`/`end_date` is not a finite
    number — per-item containment, no throw.

    **2. `_cacheHazardMatches(url, fetchResult, matches)`** — the ONLY place this product writes to
    `_geoJsonCache`. It must build the cache entry itself, copying each match field-by-field from an
    explicit four-key whitelist (`label`, `startDate`, `endDate`, `idpFiledate`) rather than
    spreading or passing `matches` through by reference. Entry:
      `{ mode: fetchResult.mode, etag: fetchResult.newEtag ?? null, hash: fetchResult.newHash ?? null, result: <whitelisted copy of matches>, timestamp: this._nowMs() }`

    This whitelist copy is the **structural enforcement** the phase's highest-risk item requires.
    The comment above it must say so explicitly, in roughly this substance:

      "`_runArcGisDayProduct` caches a final, already-resolved day-keyed value (`result: { value,
      validTime }`) and that is correct for ERO/WSSI, because their day key is a property of the
      URL — `dayLayers[1]` is always Day 1, and WPC republishes fresh Day-1 content under that same
      URL. This product's day key is a property of the CLOCK: a feature fixed at Aug 29 00:00Z is
      day3 on Aug 26 and day2 on Aug 27, with unchanged bytes and an ETag that never fires. With
      the confirmed Mon–Fri-only cadence, every weekend poll is exactly that scenario. Copying the
      analog would silently shift every hazard one day earlier per elapsed day, with no error, no
      ⚠, and no failing assertion — the existing cache-hit probe scenarios assert byte-identical
      output across a hit, which is the correct invariant for ERO and the wrong one here. This
      function therefore caches only clock-INDEPENDENT data. The field-by-field whitelist (not a
      spread) is deliberate: it makes a day key or an offset physically unable to enter the cache,
      rather than relying on a future maintainer reading this comment."

    Also add a defensive assertion inside `_cacheHazardMatches`: before writing, if any match object
    carries a key matching `/^day\d+$/` or named `offsetStart`/`offsetEnd`/`dayOffset`, throw
    `new Error("MMM-SPCOutlook _cacheHazardMatches: refusing to cache a clock-dependent field: " + key)`.
    The whitelist already prevents this; the assertion converts a future refactor that bypasses the
    whitelist from a silent misdating bug into a loud, immediate failure. Comment it as such.
  </action>
  <verify>
    <automated>node -e "
const h=require('./node_helper.js'); h.start();
const hits=[
 {value:'Heavy Rain',feature:{properties:{start_date:1787990400000,end_date:1788076800000,idp_filedate:1787000000000,label:'Heavy Rain'}}},
 {value:'',feature:{properties:{start_date:1,end_date:2}}},
 {value:'Bad Dates',feature:{properties:{start_date:'x',end_date:2}}},
 {value:'No Filedate',feature:{properties:{start_date:5,end_date:6}}}
];
const m=h._hazardMatchesFromHits(hits);
if(m.length!==2) throw new Error('normalizer kept '+m.length+' of an expected 2');
if(Object.keys(m[0]).sort().join(',')!=='endDate,idpFiledate,label,startDate') throw new Error('match shape: '+Object.keys(m[0]));
if(m[1].idpFiledate!==null) throw new Error('missing idp_filedate should normalize to null');
h._geoJsonCache=new Map();
h._nowMs=()=>1234567890;
h._cacheHazardMatches('u1',{mode:'etag',newEtag:'e1',newHash:null},m);
const e=h._geoJsonCache.get('u1');
if(!e) throw new Error('nothing cached');
if(e.timestamp!==1234567890) throw new Error('timestamp did not come from the _nowMs seam');
if(e.etag!=='e1') throw new Error('etag');
const s=JSON.stringify(e.result);
if(/day\\\\d|offsetStart|offsetEnd|dayOffset/.test(s)) throw new Error('CLOCK-DEPENDENT FIELD IN CACHE: '+s);
for(const r of e.result){ if(Object.keys(r).sort().join(',')!=='endDate,idpFiledate,label,startDate') throw new Error('cached entry leaked extra keys: '+Object.keys(r)); }
if(e.result===m) throw new Error('cache stored the caller array by reference, not a whitelisted copy');
if(e.result[0]===m[0]) throw new Error('cache stored match objects by reference, not a whitelisted copy');
let threw=false;
try{ h._cacheHazardMatches('u2',{mode:'etag'},[{label:'X',startDate:1,endDate:2,idpFiledate:null,day3:['x']}]); }catch(err){ threw=/clock-dependent/.test(err.message); }
if(!threw) throw new Error('the clock-dependent-field assertion did not fire for a day3 key');
console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - The automated check prints OK. It pins seven properties: the four-key match shape; missing
      `idp_filedate` normalizing to `null`; per-item containment of bad labels/dates; the cache
      timestamp coming from the `_nowMs` seam; **no key matching `/day\d/`, `offsetStart`,
      `offsetEnd` or `dayOffset` anywhere in the serialized cache entry**; the whitelist producing
      copies rather than references; and the defensive assertion firing on a smuggled `day3` key.
    - `grep -n "_geoJsonCache.set(" node_helper.js` shows no hazards-related call site other than
      the one inside `_cacheHazardMatches`.
    - `_cacheHazardMatches` uses explicit field assignment, not object spread:
      `grep -n "\.\.\." ` inside its body returns nothing.
    - `_runArcGisDayProduct` is unmodified — `git diff node_helper.js` shows no changed line
      between its opening and closing braces.
    - `node scripts/probe-payload-resilience.js` still reports 32/32 passing.
  </acceptance_criteria>
  <done>Both methods exist, the automated check prints OK, and no other hazards cache-write site exists.</done>
</task>

<task type="auto">
  <name>Task 2: _runArcGisHazardWindowProduct — per-layer fetch, D-09/D-10 filtering, per-layer freshness, per-poll re-bucketing, block assembly</name>
  <files>node_helper.js</files>
  <read_first>
    - node_helper.js `:213-307` — `_runArcGisDayProduct` (structural template: per-item try/catch,
      the `fetchResult.stale || fetchResult.failed` fold, "a contained throw is a degrade, not a
      clean read", convert-once-after-the-branch).
    - node_helper.js `:505-588` — `_runKmlAdvisoryRow` (the `{ list, anyStale }` return shape and
      the toggle-off early return this runner mirrors).
    - node_helper.js `:1300-1340` — `_isWithinStaleWindow` and `_noteStaleEntry`, so you can see
      exactly which function D-15 forbids calling.
    - node_helper.js — your Task 1 additions and 16-02's helpers.
    - .planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-RESEARCH.md
      § "Freshness Integration" (write-site design) and § "Registry Kind & Row Design"
      (the `includesFeat` closure built at call time).
    - .planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-CONTEXT.md — D-02, D-07,
      D-09, D-10, D-11, D-13, D-14, D-15, D-16 verbatim.
  </read_first>
  <action>
    Add `async _runArcGisHazardWindowProduct(row, loc, productToggles)` to `node_helper.js`,
    immediately after `_cacheHazardMatches`. It computes its own `todayUtcMs` internally — the
    signature deliberately does NOT accept one, so no caller can hand it a stale value. Comment
    that this is the structural half of the highest-risk mitigation: Task 1 makes a clock-dependent
    cache entry unwritable; this signature makes a clock-dependent input unpassable.

    Structure:

    1. `const todayUtcMs = this._todayUtcMs();` as the FIRST line, once per poll, before any loop.
    2. Seed the accumulators unconditionally, BEFORE the toggle check (Phase 14 D-05 — a toggle
       being off never changes payload shape):
       - `const dayBuckets = {}` keyed by integer offset
       - `const windowEntries = []`
       - `let anyStale = false`
    3. Build the request-scoped label filter at call time (15 D-02 — this cannot live on the static
       row because `showDrought` is request state):
       `const includesFeat = (label, val) => { if (!val) return false; if (row.excludedLabels.includes(val)) return false; if (row.droughtLabels.includes(val) && productToggles.showDrought !== true) return false; return true; };`
       Note carefully: `extractPolygons` calls `includesFeat(label, value)` where `label` is the
       always-empty uppercase `LABEL` read and `value` is what `toValue` returned — so the checks
       must be against `val`, not `label`. Comment this (HAZ-03/Pitfall 8 again, one layer up).
       Comment D-09 as unconditional with no override, and D-10 as `!== true` (an absent or
       non-boolean `showDrought` must behave exactly like `false`, per CFG-01's default).
    4. If `productToggles[row.configFlag]` is truthy, loop `for (const layer of row.layers)` with a
       per-layer `try/catch`:
       - `const url = row.buildUrl(layer.id);`
       - `const fetchResult = await this.fetchGeoJsonCached(url);`
       - `if (fetchResult.stale || fetchResult.failed) anyStale = true;`
       - Resolve `matches` for this layer:
         * if `fetchResult.data === null && fetchResult.cachedResult !== null` -> `matches = fetchResult.cachedResult`
           (already the clock-independent match array Task 1 wrote; do NOT re-extract, do NOT
           re-evaluate — the polygons are gone, and that is fine: the match array is exactly what
           re-bucketing needs)
         * else if `fetchResult.data !== null` -> `const polys = this.extractPolygons(fetchResult.data, row.toValue, includesFeat, url);`
           `const hits = this.evaluatePolygonsCollectAll(polys, loc);`
           `matches = this._hazardMatchesFromHits(hits);`
           `this._cacheHazardMatches(url, fetchResult, matches);`
         * else -> `matches = []`
       - **Freshness (D-13/D-14/D-15), after the branch closes**, using this layer's own matches:
         `if (Array.isArray(matches) && matches.length > 0) { const filedate = matches[0].idpFiledate; if (typeof filedate === "number" && (this._nowMs() - filedate) > row.maxDataAgeHours * 60 * 60 * 1000) { anyStale = true; /* NO _noteStaleEntry */ } }`
         Required comments at this write site — it is the phase's most comment-worthy line:
         (a) D-15's asymmetry: this sets `anyStale` so the ⚠ badge fires, and deliberately does
             NOT call `this._noteStaleEntry(...)`, because `_staleAsOf` renders as "N minutes ago"
             and describes FETCH/network recency (Phase 14 D-04's global-only rule), not WPC's own
             publish age. Without the omission, a 60-hour-old hazards file would make the badge
             speak for SPC data fetched five minutes ago, training the user to distrust a badge
             that is usually right. An uncommented exclusion reads as a bug — that is why this
             comment exists.
         (b) D-13: `idp_filedate` is PER LAYER, not per service. Live 2026-08-26 recorded three
             distinct filedates across the six layers in one poll, one ~23h staler than its
             siblings. Reading `matches[0]` is safe because the value is uniform WITHIN a layer
             (verified across all 30 non-empty-layer features that poll), but a row-level shared
             timestamp does not exist.
         (c) A zero-feature layer has no `idp_filedate` to read and therefore **skips this check
             entirely** — that is the deliberate verdict, not an oversight: a genuinely empty layer
             is a real answer, not a staleness signal (the WSSI-03 precedent this codebase already
             ships). Both Precipitation layers returned zero features live on 2026-08-26, so this
             is the common case, not an edge case. No service-level per-layer freshness timestamp
             exists to consult in its place within this phase's scope.
         (d) The check runs on a cache HIT too, because `idpFiledate` travels inside the cached
             match — a feed that stalls while still serving 304s must still age out.
       - **Re-bucket, every poll, cache hit or miss:**
         `for (const match of matches) this._bucketHazardMatch(match, layer, todayUtcMs, dayBuckets, windowEntries);`
         Comment: this line is outside and after the cache branch on purpose. It runs identically
         on a hit and a miss, against THIS poll's `todayUtcMs`. Moving it inside the miss branch
         reintroduces the day-offset drift the cache contract exists to prevent.
       - `catch (err)`: `anyStale = true;` then
         `Log.error("MMM-SPCOutlook " + row.id + " layer " + layer.id + ": fetch/parse/evaluate failed, leaving this layer empty", err);`
         Comment: CR-01 — a contained throw is a degrade, not a clean read, and it can happen
         before the `fetchResult.stale || fetchResult.failed` line ever runs.
    5. **Assemble the payload block after the loop, once** (convert-once discipline):
       - Ordering helper (D-02): a comparator that ranks a label by `row.order.indexOf(label)`,
         treating `-1` as `row.order.length`, then falls back to `localeCompare` for determinism.
         Comment: registry-declared order rather than ArcGIS response order, because response order
         can flip between polls on an unchanged forecast, which reads as a change on a glanceable
         mirror and defeats PERF-02's byte-identity intent.
       - For each `d` from `row.dayRangeTotal[0]` to `row.dayRangeTotal[1]` inclusive, build
         `block["day" + d] = { date: this._utcDateString(todayUtcMs + d * MS_PER_DAY), hazards: [...] }`.
         `hazards` is the deduped (by label), ordered list built from `dayBuckets[d] || []`, each
         element resolved to `{ label, color, mapped }` where
         `mapped = Object.prototype.hasOwnProperty.call(row.displayColor, label)` and
         `color = mapped ? row.displayColor[label] : row.defaultColor`.
         Comment: D-01 — every day carries its resolved UTC `date` so the frontend labels weekdays
         from the real date instead of `dowToText(dow + N)`; WPC's "Day N" boundary differs from
         SPC's and offset arithmetic drifts by one (Pitfall 9). Comment: dedupe by label because two
         layers can contribute the same label to the same day.
       - `block.windowBand = [...]` — the `windowEntries`, each resolved to
         `{ label, color, mapped, startDate, endDate, offsetStart, offsetEnd }`, deduped on the
         composite `label|offsetStart|offsetEnd`, sorted by `offsetStart` ascending with the same
         label comparator as the tie-break. Comment D-07 (one band, sorted by span start, each entry
         self-labeling) and D-06 (the entry carries its OWN observed span, never the layer's nominal
         window — live 2026-08-26 a D8–14 feature spanned 2 days inside a nominal 7-day bucket, and
         labeling it `D8–14` would advertise 7 days of heat where the data says 2).
       - D-11 log-once, applied to both the day hazards and the window entries: when `mapped` is
         false and `this._loggedUnmappedHazardLabels` does not already have the label, add it and
         `Log.info("MMM-SPCOutlook hazardsOutlook: unmapped hazard label rendered verbatim in the default style: " + label)`.
         Comment: dropping unknown labels would make a mid-season WPC addition invisible until
         someone read the logs — the Pitfall 8 false-negative shape. The MapServer legend carries
         ~15 labels; visibility beats silence. Log once per process so a 27-feature layer does not
         flood the log every poll.
    6. `return { payload: block, anyStale };`

    Do NOT suppress rows when `anyStale` is true — D-16: a data-age trip is an age signal, not a
    false negative, and `rejectBody`'s serve-last-known-good precedent exists precisely so a WPC
    hiccup during an active hazard does not blank the display. Add a one-line comment saying so.
  </action>
  <verify>
    <automated>node -e "
const stubs=require('./scripts/probe-lib/module-stubs.js');
const {loadNodeHelper,resetHelper,turfStub,resetLogs}=stubs;
const {PRODUCT_REGISTRY:R}=require('./productRegistry.js');
const row=R.hazardsOutlook;
const h=loadNodeHelper(); resetHelper(h);
const D=(y,m,d)=>Date.UTC(y,m,d);
const T=D(2026,7,26);
h._nowMs=()=>T+13*3600*1000;
const feat=(label,s,e,fd)=>({type:'Feature',properties:{label:label,start_date:s,end_date:e,idp_filedate:fd},geometry:{type:'Polygon',coordinates:[[[0,0],[1,0],[1,1],[0,1],[0,0]]]}});
const byLayer={
 1:[feat('Hazardous Heat',D(2026,7,29),D(2026,8,2),h._nowMs()-3600000)],
 4:[feat('Heavy Rain',D(2026,7,29),D(2026,7,30),h._nowMs()-3600000),feat('Flooding Likely',D(2026,7,29),D(2026,7,30),h._nowMs()-3600000),feat('Severe Weather',D(2026,7,29),D(2026,7,29),h._nowMs()-3600000)],
 7:[feat('Severe Drought',D(2026,7,29),D(2026,8,2),h._nowMs()-3600000)],
 3:[],6:[],8:[]
};
const urlOf={}; row.layers.forEach(L=>{urlOf[row.buildUrl(L.id)]=L.id;});
h.fetchGeoJsonCached=async(url)=>({data:{type:'FeatureCollection',features:byLayer[urlOf[url]]},cachedResult:null,stale:false,failed:false,mode:'etag',newEtag:'e',newHash:null});
turfStub.pointInPolygon=()=>true;
(async()=>{
 let r=await h._runArcGisHazardWindowProduct(row,{},{showHazardsOutlook:true});
 const p=r.payload;
 for(let d=3;d<=14;d++){ if(!p['day'+d]) throw new Error('missing day'+d); if(!Array.isArray(p['day'+d].hazards)) throw new Error('day'+d+'.hazards not an array'); if(typeof p['day'+d].date!=='string') throw new Error('D-01: day'+d+' has no date'); }
 if(p.day3.date!=='2026-08-29') throw new Error('D-01 date wrong: '+p.day3.date);
 if(!Array.isArray(p.windowBand)) throw new Error('windowBand missing');
 // HAZ-01 + D-04 spread
 if(p.day3.hazards.map(x=>x.label).join('|')!=='Severe Weather|Heavy Rain') throw new Error('D-02 order wrong: '+JSON.stringify(p.day3.hazards.map(x=>x.label)));
 if(p.day4.hazards.map(x=>x.label).join('|')!=='Heavy Rain') throw new Error('D-04 spread wrong on day4');
 // HAZ-04 D-09: flooding never appears anywhere
 const all=JSON.stringify(p);
 if(/Flooding/.test(all)) throw new Error('HAZ-04/D-09: a Flooding label reached the payload');
 // HAZ-04 D-10: drought absent at the shipped default
 if(/Severe Drought/.test(all)) throw new Error('HAZ-04/D-10: drought rendered with showDrought unset');
 // HAZ-02: temperature to the band, not the days
 if(p.windowBand.length!==1||p.windowBand[0].label!=='Hazardous Heat') throw new Error('HAZ-02 band: '+JSON.stringify(p.windowBand));
 if(p.windowBand[0].offsetStart!==3||p.windowBand[0].offsetEnd!==7) throw new Error('D-06 span: '+JSON.stringify(p.windowBand[0]));
 if(p.windowBand[0].color!=='a80000') throw new Error('color not resolved backend-side');
 if(r.anyStale!==false) throw new Error('a fresh, successful poll set anyStale');
 // D-10 opt-in
 resetHelper(h); h._nowMs=()=>T+13*3600*1000;
 h.fetchGeoJsonCached=async(url)=>({data:{type:'FeatureCollection',features:byLayer[urlOf[url]]},cachedResult:null,stale:false,failed:false,mode:'etag',newEtag:'e',newHash:null});
 turfStub.pointInPolygon=()=>true;
 r=await h._runArcGisHazardWindowProduct(row,{},{showHazardsOutlook:true,showDrought:true});
 if(!/Severe Drought/.test(JSON.stringify(r.payload))) throw new Error('D-10: showDrought true did not surface drought');
 if(/Flooding/.test(JSON.stringify(r.payload))) throw new Error('D-09: showDrought must not un-filter flooding');
 // Phase 14 D-05: toggle off still emits the full block
 resetHelper(h); h._nowMs=()=>T+13*3600*1000;
 let called=0; h.fetchGeoJsonCached=async()=>{called++;return {data:null,cachedResult:null,stale:false,failed:false};};
 r=await h._runArcGisHazardWindowProduct(row,{},{showHazardsOutlook:false});
 if(called!==0) throw new Error('toggle off still fetched');
 for(let d=3;d<=14;d++){ if(!r.payload['day'+d]||r.payload['day'+d].hazards.length!==0) throw new Error('toggle-off block incomplete at day'+d); }
 if(!Array.isArray(r.payload.windowBand)||r.payload.windowBand.length!==0) throw new Error('toggle-off windowBand wrong');
 if(r.anyStale!==false) throw new Error('toggle off set anyStale');
 console.log('OK');
})().catch(e=>{console.error(e.message);process.exit(1);});"</automated>
    <automated>bash -c 'BODY=$(awk "/_runArcGisHazardWindowProduct\(row, loc, productToggles\)/,/^  },?\$/" node_helper.js); echo "$BODY" | grep -v "^\s*//" | grep -v "^\s*\*" | grep -q "_noteStaleEntry" && { echo "D-15 VIOLATION: _noteStaleEntry called inside the hazards runner"; exit 1; }; echo "$BODY" | grep -q "_bucketHazardMatch" || { echo "runner never re-buckets"; exit 1; }; echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - The first automated check prints OK. It independently pins HAZ-01 (day3..day14 present with
      D-01 dates, D-04 multi-day spread), HAZ-02 (temperature routed to `windowBand` with its own
      D-06 span), HAZ-04 both halves (Flooding absent unconditionally; Drought absent at the
      shipped default and present only with `showDrought: true`, while Flooding stays absent even
      then), D-02 ordering (`Severe Weather` before `Heavy Rain` per `row.order`, NOT response
      order — the fixture deliberately supplies `Heavy Rain` first), backend-side color resolution,
      and Phase 14 D-05 (toggle off issues zero fetches yet still emits the complete block).
    - The second automated check passes: after stripping comment lines, the runner body contains no
      `_noteStaleEntry` call (D-15) and does contain a `_bucketHazardMatch` call. Comments are
      stripped first so the mandatory D-15 explanatory comment — which names `_noteStaleEntry` —
      cannot self-invalidate the gate.
    - `_runArcGisHazardWindowProduct`'s signature has exactly three parameters and does NOT accept a
      `todayUtcMs`: `grep -n "_runArcGisHazardWindowProduct(row, loc, productToggles)" node_helper.js`
      returns one line.
    - The `this._bucketHazardMatch(...)` call is lexically OUTSIDE the `if (fetchResult.data !== null)`
      branch — verified by reading the runner: re-bucketing must execute on the cache-hit path too.
      (16-07 mutation-proves this at runtime; this criterion is the source-level check.)
    - `Object.keys(payload)` for a completed run is exactly `day3..day14` plus `windowBand` — 13
      keys, no extras.
    - `node scripts/probe-payload-resilience.js` still reports 32/32 passing.
  </acceptance_criteria>
  <done>The runner exists, both automated checks pass, and the existing probe suite is green.</done>
</task>

<task type="auto">
  <name>Task 3: Dispatch the row, thread showDrought, and emit the hazardsOutlook payload key</name>
  <files>node_helper.js</files>
  <read_first>
    - node_helper.js `:2173-2232` — the ERO and WSSI call sites inside `getSpcOutlook`, the
      `kml-advisory` loop, the `_unusableFeatureCount` WR-08 check, and the
      `...(anyStale ? { _stale: true, _staleAsOf: this._oldestStaleAt } : {})` spread.
    - node_helper.js `:2320-2340` — the `excessiveRain: eroPayload, winterImpact: wssiPayload,
      advisories: advisories` payload keys, so the new key lands beside them in the same style.
    - node_helper.js `:181-195` — `_productToggles`, which keys strictly off each row's
      `configFlag` and therefore does not carry `showDrought`.
    - node_helper.js `:740-760` and `:1600-1615` — the two other `_productToggles(...)` call sites,
      so the sub-toggle reaches every path.
  </read_first>
  <action>
    Three edits:

    **1. Thread `showDrought` through `_productToggles`.** Add a module-level constant beside the
    registry import:
      `const SUB_TOGGLES = ["showDrought"];`
    and in `_productToggles`, after the per-row loop, copy each sub-toggle through with the same
    strict-`=== true` defaulting the row loop uses. Comment (WR-16 / D-10): `showDrought` is NOT a
    product flag in the `configFlag` sense — it does not gate a fetch, it gates which labels within
    an already-fetched product are displayable — so it cannot be derived from `PRODUCT_REGISTRY`
    and needs this explicit, single-source path. Adding it here (rather than reading
    `this._products` inside the runner) keeps the runner pure with respect to helper-global state,
    exactly as `_runArcGisDayProduct`'s `productToggles` parameter already does (WR-13). Note that
    a future sub-toggle appends one string here and nothing else.

    **2. Dispatch.** In `getSpcOutlook`, immediately after the `wssiResult` block and before the
    `kml-advisory` loop:
      `const hazardsResult = await this._runArcGisHazardWindowProduct(PRODUCT_REGISTRY.hazardsOutlook, loc, productToggles);`
      `const hazardsPayload = hazardsResult.payload;`
      `if (hazardsResult.anyStale) anyStale = true;`
    Comment: a direct named call rather than a `kind`-loop because this row is singular (unlike the
    `kml-advisory` loop, which iterates every row of that kind) — matching the codebase's
    straight-line-dispatch style. Note that its `anyStale` folds into the same local every other
    product uses (Phase 14 D-04, global-only staleness), and that per D-15 this product can raise
    `anyStale` from a data-age trip without having contributed to `_oldestStaleAt`, so
    `_staleAsOf` may legitimately be `null` or older-than-this-product while the badge is on.

    **3. Payload key.** Add `hazardsOutlook: hazardsPayload,` to the returned object beside
    `excessiveRain` / `winterImpact` / `advisories`, in the same style. Comment: sibling block per
    Phase 14 D-02 — Phase 18 owns the merged schema; do not pre-adopt it here.

    Also add to the `getSpcOutlook` JSDoc's payload description (node_helper.js `:1590-1600`) a line
    documenting the new key's shape:
      `hazardsOutlook: { day3..day14: { date: "YYYY-MM-DD", hazards: [{ label, color, mapped }] }, windowBand: [{ label, color, mapped, startDate, endDate, offsetStart, offsetEnd }] }`
    with a note that `day9`..`day14` are the first keys in this module past `day8` and that they
    live INSIDE this block, not at the payload's top level, so no existing day-bounded loop or
    assertion is affected.
  </action>
  <verify>
    <automated>node -e "
const h=require('./node_helper.js'); h.start();
const t=h._productToggles({showExcessiveRain:true,showDrought:true});
if(t.showDrought!==true) throw new Error('showDrought not threaded through _productToggles');
if(h._productToggles({}).showDrought!==false) throw new Error('showDrought must default false');
if(h._productToggles({showDrought:'yes'}).showDrought!==false) throw new Error('showDrought must be strict === true');
if(h._productToggles({}).showHazardsOutlook!==false) throw new Error('showHazardsOutlook not derived from the registry');
console.log('OK');"</automated>
    <automated>bash -c 'grep -q "hazardsOutlook: hazardsPayload" node_helper.js || { echo "payload key missing"; exit 1; }; grep -q "_runArcGisHazardWindowProduct(PRODUCT_REGISTRY.hazardsOutlook" node_helper.js || { echo "dispatch missing"; exit 1; }; grep -q "hazardsResult.anyStale) anyStale = true" node_helper.js || { echo "anyStale not folded"; exit 1; }; echo OK'</automated>
    <automated>node scripts/probe-payload-resilience.js</automated>
  </verify>
  <acceptance_criteria>
    - Both `node -e` checks print OK; `showDrought` defaults to `false` and requires a strict `true`.
    - `node scripts/probe-payload-resilience.js` reports **32/32 passing with zero new failures**.
      This is the load-bearing regression gate for this task: `getSpcOutlook` now issues six more
      fetches, and every existing scenario that installs a route table (`eroHttpRoutes`,
      `wssiRoutes`, `installFetch` with an explicit list) will route the six hazards URLs to the
      harness default. If any existing scenario's `_stale` assertion flips because an unrouted
      hazards layer hard-failed, **the fix is to leave `getSpcOutlook` alone and note the affected
      scenario names in the SUMMARY for 16-06 to route** — do NOT make the hazards fetch
      conditional on anything other than its own `configFlag`, and do NOT add a special case to
      suppress its staleness. Record the exact list of affected scenario names in the SUMMARY.
    - The payload from a run with `showHazardsOutlook: false` still contains a complete
      `hazardsOutlook` block (Phase 14 D-05) — verified by the existing `assertPayloadIntact`-style
      shape check in 16-06.
    - `getSpcOutlook`'s JSDoc documents the `hazardsOutlook` key shape including `windowBand`.
    - `grep -c "SUB_TOGGLES" node_helper.js` returns at least 2 (declaration + use).
  </acceptance_criteria>
  <done>Dispatch, sub-toggle, and payload key are in place; `_productToggles` checks pass; the probe suite result (pass count and any newly affected scenario names) is recorded in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NOAA MapServer -> `fetchGeoJsonCached` | Six unauthenticated remote GeoJSON responses per poll; body, ETag and every property value are untrusted |
| `_geoJsonCache` -> next poll's payload | Data written on poll N is replayed on poll N+1 without re-validation |
| runner payload -> frontend `innerHTML` | Remote `label` strings begin their journey to the DOM here (escaped in 16-05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-09 | Spoofing | Six hazards layer fetches | mitigate | Every URL comes from `row.buildUrl(layer.id)` -> `buildArcGisQuery`, which allowlists `https://mapservices.weather.noaa.gov/` and hardcodes `f=geojson` (Phase 14 D-09). `redirect: "error"` and the 15s abort timeout apply via the existing `_fetch`/`withTimeout` path; this plan adds no fetch call site outside `fetchGeoJsonCached`. |
| T-16-10 | Tampering | `includesFeat` label filter | mitigate | D-09's Flooding exclusion is an unconditional `row.excludedLabels.includes(val)` with no config key that reaches it — verified by the "Flooding absent even with `showDrought: true`" assertion. D-10 uses strict `!== true`, so a truthy-but-not-`true` `showDrought` (a string, `1`) does not un-gate drought. |
| T-16-11 | Tampering | Cached day assignment replayed across a date change | mitigate | Task 1's whitelist copy plus the `/^day\d+$/` refusal assertion make a clock-dependent cache entry unwritable; the runner's 3-parameter signature makes a clock-dependent input unpassable; re-bucketing sits outside the cache branch. Runtime-proven by 16-07's `hazards-day-keys-shift-on-a-cache-hit-when-the-day-advances` scenario. |
| T-16-12 | Information Disclosure | `Log.info` of an unmapped label | mitigate | The logged string is a remote hazard label bounded to one line and logged once per process per label (`_loggedUnmappedHazardLabels`), so a hostile 1 MB label cannot flood the log across polls. Logs are local to the MagicMirror process; no remote sink. |
| T-16-13 | Denial of Service | Unbounded remote `label` length | mitigate | RESEARCH.md notes the service imposes no length bound. Feature volume is bounded (`maxRecordCount: 2000`; 1–27 observed). Length is bounded at the render boundary in 16-05 (`HAZARDS_LABEL_MAX_CHARS` truncation) rather than here, so the payload keeps the full remote value for Phase 18's merge while the DOM sees a bounded string. |
| T-16-14 | Repudiation | A data-age trip that raises ⚠ without a timestamp | accept | D-15's deliberate asymmetry: the badge fires with no age suffix contribution. Accepted because the alternative (letting WPC publish-age drive `_staleAsOf`) makes the badge misreport SPC fetch recency, which is the worse failure. Documented at the write site. |
| T-16-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — zero packages installed in this phase (RESEARCH.md § Package Legitimacy Audit). |
</threat_model>

<verification>
- All three tasks' automated checks pass.
- `node scripts/probe-payload-resilience.js` reports 32/32 with no new failures; any scenario whose
  route table now needs a hazards entry is named in the SUMMARY for 16-06.
- Source-level D-15 gate: the runner body (comments stripped) contains no `_noteStaleEntry` call.
- Source-level cache gate: `_geoJsonCache.set(` appears for this product only inside
  `_cacheHazardMatches`.
- `_runArcGisDayProduct` and `evaluatePolygons` are byte-unchanged.
</verification>

<success_criteria>
- HAZ-01: a Precipitation feature produces day entries for every day in its span, each carrying a
  resolved UTC `date`.
- HAZ-02: Temperature and Wildfire features produce exactly one `windowBand` entry each, carrying
  their own observed span.
- HAZ-03: the payload's labels come from the lowercase `label` field (inherited from 16-01's
  `toValue`, exercised end-to-end by Task 2's fixture).
- HAZ-04: Flooding never appears under any configuration; Drought appears only with an explicit
  `showDrought: true`.
- DATA-02: a per-layer `idp_filedate` older than 84h sets `anyStale` without touching
  `_oldestStaleAt`; a zero-feature layer raises nothing.
- The cache contract is structurally clock-independent and the re-bucketing step runs on every poll.
</success_criteria>

<output>
Create `.planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-03-SUMMARY.md` when done.
Record in it: the exact `hazardsOutlook` payload shape as implemented, and the names of any
existing probe scenarios whose route tables now need a hazards entry (input to 16-06).
</output>
