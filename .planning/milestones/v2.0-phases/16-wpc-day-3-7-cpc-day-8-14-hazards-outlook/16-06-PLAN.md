---
phase: 16-wpc-day-3-7-cpc-day-8-14-hazards-outlook
plan: 06
type: execute
wave: 5
depends_on: ["16-05"]
files_modified:
  - scripts/probe-payload-resilience.js
autonomous: true
requirements: [HAZ-01, HAZ-03, HAZ-04, DATA-02]

must_haves:
  truths:
    - "Every existing scenario routes all six hazards layer URLs, so no unrouted layer can set anyStale and make an unrelated scenario's assertion vacuous (WR-02's trap)"
    - "A live-shaped Precipitation fixture keyed on lowercase `label` produces day rows — proving HAZ-03 end to end, not just at the registry"
    - "A Flooding-carrying payload produces no Flooding anywhere, under any toggle combination (HAZ-04, D-09)"
    - "A Drought-carrying payload produces no Drought at the shipped default and Drought only with an explicit opt-in (HAZ-04, D-10)"
    - "A zero-feature layer produces no rows and no staleness signal (WSSI-03 precedent)"
    - "Every new scenario is mutation-proven: breaking the exact line it covers turns it RED with a diagnosable message"
  artifacts:
    - path: "scripts/probe-payload-resilience.js"
      provides: "HAZARDS_URLS, hazardsRoutes, hazardsFeature fixture builder, and the hazards-* core scenario family"
      contains: "hazardsRoutes"
  key_links:
    - from: "hazardsRoutes"
      to: "PRODUCT_REGISTRY.hazardsOutlook.buildUrl"
      via: "URLs derived from the registry, never written out"
      pattern: "PRODUCT_REGISTRY.hazardsOutlook.buildUrl"
---

<objective>
Extend the probe suite with the Hazards Outlook fixture infrastructure and its core correctness
scenarios: HAZ-01's day bucketing, HAZ-03's lowercase-`label` end-to-end read, HAZ-04's Flooding
and Drought handling, D-02's ordering, D-11's unmapped-label passthrough, and the zero-feature
case.

Purpose: this project has no automated test framework — `workflow.nyquist_validation` is disabled
and the probe suite plus manual UAT is the entire verification apparatus. HAZ-01's per-day
bucketing has **no live example** to validate against: both Precipitation layers returned zero
features on 2026-08-26, so synthetic fixtures carry that weight.

Output: shared route/fixture helpers plus the core `hazards-*` scenarios, each mutation-proven.
The two highest-risk regressions (day-offset drift on a cache hit, and the window-band gate) are
16-07's, deliberately separated so they get their own attention.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/buildomator/bm/4.5.5/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/buildomator/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-CONTEXT.md
@.planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-RESEARCH.md
@.planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-PATTERNS.md
@.planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-03-SUMMARY.md
@.planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-05-SUMMARY.md

<interfaces>
<!-- Probe-suite contracts. Extracted from the codebase; use directly. -->

scripts/probe-payload-resilience.js:
  const scenarios = [ { name: "...", run: async (helper) => { ... } }, ... ]   // :876
  installFetch(helper, routes)   :597  — stubs helper.fetchGeoJsonCached-level URL routing
  installHttp(helper, routes)    :663  — stubs helper._fetch(url, options); routes are
                                          [matcherSubstring, handler] pairs; UNROUTED urls get a
                                          503, i.e. a hard failure that sets anyStale
  httpResponse({ status, body, text, buffer, etag })   :630
  freshFetch(body)               :570
  eroHttpRoutes(day1Handler)     :680  — the "every other layer answers 200 with an empty
                                          collection so nothing outside the scenario's subject can
                                          set anyStale" discipline (WR-02's trap). COPY THIS SHAPE.
  wssiRoutes(day1Handler)        :~697
  assertPayloadIntact(out)       :720  — loops top-level day1..day8; hazardsOutlook is a nested
                                          sibling block and is NOT covered by it today
  EMPTY_FEATURE_COLLECTION       :85
  PROBE_LAT / PROBE_LON          :283
  ERO_URLS / WSSI_URLS           :288 / :298

scripts/probe-lib/module-stubs.js exports:
  loadNodeHelper, resetHelper, resetLogs, turfStub, logCalls, loadFrontendModule, renderDom,
  makeKmzBuffer, hasRealKmlDeps, missingKmlDeps
  resetHelper restores every seam (including the new _nowMs) via ORIGINAL_SEAMS.

Payload under test (16-03):
  out.hazardsOutlook = { day3..day14: { date, hazards: [{label,color,mapped}] }, windowBand: [...] }
Toggles reach the helper as helper._products or as getSpcOutlook's `products` argument.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hazards route table, fixture builder, and the existing-suite route repair</name>
  <files>scripts/probe-payload-resilience.js</files>
  <read_first>
    - scripts/probe-payload-resilience.js `:80-100`, `:280-310` — `EMPTY_FEATURE_COLLECTION`,
      `PROBE_LAT`/`PROBE_LON`, `ERO_URLS`, `WSSI_URLS` (the URL-constant shape to mirror).
    - scripts/probe-payload-resilience.js `:565-700` — `freshFetch`, `installFetch`, `httpResponse`,
      `installHttp`, `eroHttpRoutes` and its WR-02 comment, `wssiRoutes`. Read the WR-02 comment in
      full: it is the reason this task exists.
    - scripts/probe-payload-resilience.js `:715-760` — `assertPayloadIntact`.
    - .planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-03-SUMMARY.md — the recorded
      list of existing scenarios whose route tables now need a hazards entry. **Start from that
      list; do not rediscover it.**
    - scripts/probe-lib/module-stubs.js `:203-245` — `loadNodeHelper` / `resetHelper`.
  </read_first>
  <action>
    **Part A — constants and helpers.** Near `ERO_URLS`/`WSSI_URLS`, add:

    - `const HAZARDS_URLS = {};` populated by iterating
      `PRODUCT_REGISTRY.hazardsOutlook.layers` and calling
      `PRODUCT_REGISTRY.hazardsOutlook.buildUrl(layer.id)`, keyed by layer id.
      Comment: derived from the registry, never written out — a layer-id change in
      `productRegistry.js` must not leave a stale literal here (Pitfall 11, cache-key drift).
    - `function hazardsRoutes(overrides)` — returns an `installHttp`-shaped route list covering ALL
      SIX hazards URLs plus a `[".lyr.geojson", okEmpty]` catch-all for the SPC/fire-weather layers,
      exactly mirroring `eroHttpRoutes`. `overrides` is an object keyed by layer id whose values are
      handlers; any layer not overridden answers 200 with `EMPTY_FEATURE_COLLECTION` and a stable
      etag. Also route every `ERO_URLS` and `WSSI_URLS` entry to `okEmpty`.
      Carry `eroHttpRoutes`'s WR-02 comment verbatim in substance: nothing outside the scenario's
      subject may hard-fail, or `anyStale` is set by a layer the scenario is not testing and its
      `_stale` assertion becomes vacuous.
    - `function hazardsFeature({ label, startDate, endDate, filedate })` — builds one GeoJSON
      Feature with `properties: { label, start_date, end_date, idp_filedate }` (lowercase `label`
      **only** — deliberately NO uppercase `LABEL` key, so a fixture cannot accidentally satisfy
      `extractPolygons`'s hardcoded uppercase read and mask a HAZ-03 regression) and a simple
      square Polygon geometry. `startDate`/`endDate` accept epoch ms.
      Add `function hazardsCollection(features)` returning a FeatureCollection.
      Comment: RESEARCH.md live-verified that every `start_date`/`end_date` on this service is exact
      UTC midnight epoch ms; fixtures must match that shape.
    - `function assertHazardsBlockIntact(out)` — asserts `out.hazardsOutlook` exists, has exactly
      the keys `day3`..`day14` plus `windowBand`, every day entry is an object with a string `date`
      and an array `hazards`, and `windowBand` is an array. Comment: Phase 14 D-05 — one payload
      shape always; a toggle being off never changes it. `assertPayloadIntact` deliberately covers
      only the top-level `day1..day8`, so this is the hazards block's own shape gate.

    **Part B — repair the existing suite.** For every scenario named in 16-03's SUMMARY as newly
    affected (and any additional one a full run reveals), add the hazards URLs to its route table:
    scenarios built on `installHttp` get `hazardsRoutes` merged in or their route list extended;
    scenarios built on `installFetch` get the six hazards URLs mapped to an empty collection.
    Do NOT change any existing assertion, and do NOT weaken any `_stale` check — the correct repair
    is to route the new layers so they answer cleanly, exactly as `eroHttpRoutes`/`wssiRoutes`
    already do for ERO and WSSI.
    Record in the SUMMARY every scenario you touched and why.
  </action>
  <verify>
    <automated>node scripts/probe-payload-resilience.js</automated>
    <automated>node -e "
const {PRODUCT_REGISTRY:R}=require('./productRegistry.js');
const src=require('fs').readFileSync('scripts/probe-payload-resilience.js','utf8');
if(!src.includes('PRODUCT_REGISTRY.hazardsOutlook.buildUrl')) throw new Error('HAZARDS_URLS is not derived from the registry');
for(const L of R.hazardsOutlook.layers){ const u=R.hazardsOutlook.buildUrl(L.id); if(src.includes(u)) throw new Error('a hazards URL literal was hardcoded into the probe file: '+u); }
if(/LABEL:/.test(src.split('function hazardsFeature')[1]||'')) throw new Error('hazardsFeature emits an uppercase LABEL key, which would mask a HAZ-03 regression');
console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/probe-payload-resilience.js` reports **all pre-existing scenarios passing** with
      the same pass count as before this phase's code changes (32), plus zero new failures. Record
      the exact reported totals in the SUMMARY.
    - `HAZARDS_URLS` is derived by calling `PRODUCT_REGISTRY.hazardsOutlook.buildUrl` — no hazards
      URL string literal appears anywhere in the probe file (second automated check).
    - `hazardsFeature` emits a lowercase `label` property and **no** uppercase `LABEL` property.
    - `hazardsRoutes()` with no overrides, installed via `installHttp`, produces a run whose
      `out.hazardsOutlook` has 13 keys and whose `_stale` is falsy — i.e. the default route table
      is genuinely quiet and cannot itself set `anyStale`. Assert this inline in a
      `hazards-routes-are-quiet-by-default` scenario so the WR-02 trap is pinned rather than
      assumed.
    - Every touched existing scenario is named in the SUMMARY with the reason it needed routing.
    - No existing assertion was changed or weakened — `git diff scripts/probe-payload-resilience.js`
      shows additions to route tables only, inside the scenarios listed in the SUMMARY.
  </acceptance_criteria>
  <done>Route/fixture helpers exist, the full suite is green at its pre-existing count plus the new quiet-routes scenario, and every touched scenario is documented.</done>
</task>

<task type="auto">
  <name>Task 2: HAZ-01 day-bucketing and HAZ-03 lowercase-label scenarios</name>
  <files>scripts/probe-payload-resilience.js</files>
  <read_first>
    - scripts/probe-payload-resilience.js `:1171-1230` — `ero-wellformed-slgt` and `ero-toggle-off`,
      the headline-well-formed and toggle-off scenario shapes.
    - scripts/probe-payload-resilience.js `:1231-1300` — `wssi-wellformed-minor` and its comment
      about routing the non-subject days to empty collections "rather than left unrouted, so their
      NONE reading proves the empty-routed days are unaffected by day 1's real body, not that the
      harness never reached them at all". That reasoning is the model for every scenario here.
    - Your Task 1 helpers.
    - .planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-RESEARCH.md
      § "1. Live payload pull — all six layers" — the live label/span table these fixtures mirror.
  </read_first>
  <action>
    Add these scenarios. Every one must pin `helper._nowMs` to a fixed instant
    (`Date.UTC(2026, 7, 26, 13, 0)` — Wednesday Aug 26 2026, matching the live research pull) as its
    FIRST act after `resetHelper`, so day offsets are deterministic and the suite cannot go red at
    midnight. Every one must set `turfStub.pointInPolygon = () => true` to place the user inside,
    and rely on `resetHelper` to restore it. Every one must call `assertHazardsBlockIntact(out)`.

    1. `hazards-precip-spread-buckets-every-day-in-span` (HAZ-01, D-04)
       Layer 4 returns two features: `Heavy Rain` Aug29→Aug30 (offsets 3–4) and `Heavy Snow`
       Aug31→Aug31 (offset 5). Assert `day3.hazards` and `day4.hazards` both contain `Heavy Rain`,
       `day5.hazards` contains `Heavy Snow`, `day6`..`day14` are empty, and `windowBand` is empty.
       Also assert `day3.date === "2026-08-29"` (D-01).
       Precondition guard: assert the fixture actually produced two features reaching the payload
       (`day3.hazards.length + day5.hazards.length === 2`) before the emptiness assertions, so a
       fixture that never reached the runner cannot pass by rendering nothing everywhere.

    2. `hazards-lowercase-label-is-read-not-dropped` (HAZ-03, Pitfall 8)
       Layer 4 returns one feature carrying `label: "Heavy Rain"` and **no** `LABEL` key. Assert the
       payload's `day3.hazards[0].label === "Heavy Rain"`.
       **Control assertion:** a second, sibling feature carrying ONLY an uppercase
       `LABEL: "Heavy Snow"` (no lowercase `label`) must produce NO entry — proving the row reads
       the lowercase field specifically rather than falling back to whatever is present.
       Comment: `extractPolygons` hardcodes `f.properties.LABEL` at `node_helper.js:1005`; if the
       row's `toValue` ever reverts to trusting that positional parameter, this scenario's first
       assertion goes red and its control goes red in the opposite direction.

    3. `hazards-day-order-follows-the-registry-not-the-response` (D-02)
       Layer 4 returns three same-day features in the deliberately WRONG response order:
       `Heavy Ice`, `Heavy Rain`, `Severe Weather` (registry order is `Severe Weather`,
       `Heavy Rain`, ..., `Heavy Ice`). Assert `day3.hazards.map(h => h.label)` equals
       `["Severe Weather", "Heavy Rain", "Heavy Ice"]`.
       **Control:** re-run with the features in a DIFFERENT wrong order (`Heavy Rain`,
       `Severe Weather`, `Heavy Ice`) and assert the same output array — proving the ordering comes
       from the registry and not from the response, which a single-order fixture cannot show.
       Read the expected order from `PRODUCT_REGISTRY.hazardsOutlook.order`, not from a literal.

    4. `hazards-unmapped-label-renders-verbatim-and-logs-once` (D-11)
       Layer 4 returns two features with the same never-observed-live label `Frost/Freeze` on
       different days. Assert both reach the payload with `mapped === false` and
       `color === PRODUCT_REGISTRY.hazardsOutlook.defaultColor`, and that `logCalls` contains
       exactly ONE info line naming the label across the run.
       **Control:** a mapped label (`Heavy Rain`) in the same run must have `mapped === true`,
       `color === PRODUCT_REGISTRY.hazardsOutlook.displayColor["Heavy Rain"]`, and produce NO log
       line — proving the log fires on the unmapped condition specifically, not on every feature.

    5. `hazards-zero-feature-layers-render-nothing-and-are-not-stale` (WSSI-03 precedent, DATA-02)
       All six layers return `EMPTY_FEATURE_COLLECTION`. Assert every `day{N}.hazards` is empty,
       `windowBand` is empty, and `out._stale` is falsy.
       Precondition guard: assert the six hazards URLs were actually requested
       (`helper._fetch.calls` contains each) — otherwise "no rows and not stale" is satisfied by the
       runner never having run, which is exactly RESEARCH.md's zero-feature case being masked by a
       harness gap.

    6. `hazards-toggle-off-emits-the-full-block-and-fetches-nothing` (Phase 14 D-05)
       Toggle off with real bodies routed. Assert `assertHazardsBlockIntact(out)` passes, every day
       array is empty, `windowBand` is empty, `out._stale` is falsy, and **no** hazards URL appears
       in `helper._fetch.calls`.
  </action>
  <verify>
    <automated>node scripts/probe-payload-resilience.js</automated>
  </verify>
  <acceptance_criteria>
    - All six scenarios pass; the suite's total pass count increases by exactly the number added
      and no pre-existing scenario regresses.
    - MUTATION PROOF — for each scenario, break the exact line it covers, confirm RED **with a
      diagnosable message**, restore. Record each mutation, the file:line broken, and the verbatim
      RED message in the SUMMARY:
      * #1 — in `_bucketHazardMatch`, change the day loop to `d = offsetStart; d <= offsetStart`
        (single day only). MUST turn `hazards-precip-spread-buckets-every-day-in-span` RED naming
        `day4`.
      * #2 — in `productRegistry.js`, change `hazardsOutlook.toValue` to
        `(label, f) => label` (trusting the broken positional parameter). MUST turn
        `hazards-lowercase-label-is-read-not-dropped` RED naming the missing `Heavy Rain`.
      * #3 — in the runner's assembly step, remove the `row.order` comparator so hazards keep
        response order. MUST turn `hazards-day-order-follows-the-registry-not-the-response` RED on
        at least one of its two orderings.
      * #4 — remove the `_loggedUnmappedHazardLabels` guard so the log fires per feature. MUST turn
        `hazards-unmapped-label-renders-verbatim-and-logs-once` RED naming the log count.
      * #5 — in the runner, make the freshness check treat a zero-feature layer as maximally stale
        (`anyStale = true` when `matches.length === 0`). MUST turn
        `hazards-zero-feature-layers-render-nothing-and-are-not-stale` RED on `_stale`.
      * #6 — remove the `productToggles[row.configFlag]` guard from the runner's fetch loop. MUST
        turn `hazards-toggle-off-emits-the-full-block-and-fetches-nothing` RED naming a fetched URL.
      A mutation that produces GREEN, or RED without naming the specific condition, is a **fixture
      defect** (STATE.md, Phase 15 D-10) — fix the fixture and re-prove before proceeding.
    - Every scenario pins `helper._nowMs` to a fixed instant; `grep -c "_nowMs=" `-style inspection
      shows one pin per new scenario. A scenario relying on the real clock is rejected: it would go
      red at a date boundary and its day offsets would not be reproducible.
    - Scenarios #2, #3 and #4 each contain an explicit **control assertion** as specified; #1 and #5
      each contain an explicit **precondition guard**. These are acceptance criteria, not advice —
      both mechanisms are the proven preventatives for the two vacuity modes recorded in STATE.md.
    - Expected values are read from `PRODUCT_REGISTRY.hazardsOutlook` (`order`, `displayColor`,
      `defaultColor`) rather than duplicated as literals, so a registry change cannot leave a
      scenario asserting a stale expectation.
  </acceptance_criteria>
  <done>All six scenarios pass, all six mutations produced diagnosable RED and were restored, and every mutation is recorded in the SUMMARY with its RED message.</done>
</task>

<task type="auto">
  <name>Task 3: HAZ-04 Flooding and Drought scenarios</name>
  <files>scripts/probe-payload-resilience.js</files>
  <read_first>
    - scripts/probe-payload-resilience.js `:1317-1400` — `wssi-winter-weather-area-renders-nothing`
      and `wssi-zero-features-out-of-season`, the closest precedent for "a filtered label produces
      no row" and the F3 note in STATE.md about a scenario that is structurally proven rather than
      mutation-proven.
    - Your Task 1 and Task 2 additions.
    - .planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-CONTEXT.md — D-09's full
      provenance rationale and D-10's gating rationale.
    - .planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-RESEARCH.md
      § "Colors" table — the exact Flooding and Drought label strings.
  </read_first>
  <action>
    Add three scenarios, following Task 2's conventions (pinned `_nowMs`, `pointInPolygon = true`,
    `assertHazardsBlockIntact`). Read the label strings from
    `PRODUCT_REGISTRY.hazardsOutlook.excludedLabels` and `.droughtLabels`, not from literals.

    1. `hazards-flooding-labels-never-appear-under-any-toggle` (HAZ-04, D-09)
       Layer 4 returns all THREE excluded Flooding labels alongside one `Heavy Rain` feature on the
       same day. Run the payload **three times**: `{showHazardsOutlook: true}`,
       `{showHazardsOutlook: true, showDrought: true}`, and
       `{showHazardsOutlook: true, showDrought: "yes"}` (a truthy non-boolean). In all three,
       assert `JSON.stringify(out.hazardsOutlook)` contains no `"Flooding"` substring.
       **Control (this is what makes the scenario non-vacuous):** assert `Heavy Rain` IS present in
       all three runs. Without it, "no Flooding" is satisfied by the fixture never reaching the
       runner at all.
       Comment the D-09 provenance: these labels come from the National Flood Outlook, a separate
       NOAA product outside v2.0's scope; rendering them would attribute another product's data to
       the Hazards Outlook. There is deliberately no config key that re-enables them, and the third
       run exists to prove a truthy `showDrought` cannot become an accidental un-filter.

    2. `hazards-drought-is-hidden-at-the-default-and-shown-only-on-opt-in` (HAZ-04, D-10)
       Layer 7 returns `Severe Drought` and `Critical Wildfire Risk`; layer 8 returns
       `Rapid Onset Drought Risk`. Two runs:
       * `{showHazardsOutlook: true}` — assert no drought label appears anywhere, AND assert
         `Critical Wildfire Risk` DOES appear in `windowBand` (the control: the Wildfire/Drought
         layer multiplexes both families on the same `label` attribute, so a filter that removed the
         whole layer rather than the drought labels would pass a naive "no drought" check).
       * `{showHazardsOutlook: true, showDrought: true}` — assert both drought labels now appear in
         `windowBand`, and `Critical Wildfire Risk` still appears.
       Comment the live finding: on 2026-08-26 layer 7 returned 26× `Severe Drought` and zero
       `Critical Wildfire Risk`, so at the shipped default this layer currently renders nothing —
       a correct outcome that must not be mistaken for a bug during UAT.
       Add an explicit comment recording the ROADMAP criterion-4 resolution: criterion 4 is verified
       at the shipped default (`showDrought: false`), which D-10 satisfies. Drought display is an
       explicit user opt-in, not a violation. A verifier reading criterion 4 as an absolute
       prohibition would fail a correct implementation.

    3. `hazards-frontend-renders-no-flooding-or-drought-at-the-default` (HAZ-04, end to end)
       Drive the FRONTEND: build the payload through `getSpcOutlook` with the layer-4/7/8 fixtures
       above at the shipped default, then feed the resulting `spcrisk` through
       `renderDom(loadFrontendModule(), { config, spcrisk })` with `showHazardsOutlook: true,
       showDrought: false`. Assert the rendered markup contains neither `Flooding` nor `Drought`,
       and — the control — DOES contain `Heavy Rain` and `Critical Wildfire Risk`.
       Comment: CR-01's lesson — the suite asserted on the payload and stopped there, which is how a
       total outage came to render as a confident all-clear while a payload assertion reported the
       guarantee as met. HAZ-04 is a *display* requirement; it must be proven at the display.
  </action>
  <verify>
    <automated>node scripts/probe-payload-resilience.js</automated>
  </verify>
  <acceptance_criteria>
    - All three scenarios pass and no pre-existing scenario regresses.
    - MUTATION PROOF — record each in the SUMMARY with its verbatim RED message:
      * #1 — in `productRegistry.js`, empty `hazardsExcludedLabels` to `[]`. MUST turn
        `hazards-flooding-labels-never-appear-under-any-toggle` RED naming a Flooding label.
        Restore.
      * #1b — in the runner's `includesFeat`, change the Flooding check to also consult
        `productToggles.showDrought`. MUST turn the SAME scenario RED on its third run
        (`showDrought: "yes"`) or its second — proving the three-run structure is load-bearing and
        not decorative. Restore.
      * #2 — change the D-10 gate from `!== true` to a loose `!productToggles.showDrought`. Re-run.
        If this does NOT turn `hazards-drought-is-hidden-at-the-default-and-shown-only-on-opt-in`
        RED, the scenario is missing the truthy-non-boolean case — add it and re-prove. Restore.
      * #2b — change the gate to drop the entire `wildfireDrought` group rather than the drought
        labels. MUST turn scenario #2 RED on its `Critical Wildfire Risk` control. Restore.
      * #3 — remove the `renderHazardsWindowBand` call from `getDom`. MUST turn scenario #3 RED on
        its `Critical Wildfire Risk` control. Restore.
    - Scenario #1 asserts `Heavy Rain` present in all three runs (control); scenario #2 asserts
      `Critical Wildfire Risk` present in both runs (control); scenario #3 asserts both controls at
      the DOM. Without these, each scenario is satisfiable by the fixture never reaching the code
      under test.
    - Label strings are read from `PRODUCT_REGISTRY.hazardsOutlook.excludedLabels` /
      `.droughtLabels`, not duplicated as literals.
  </acceptance_criteria>
  <done>All three scenarios pass, all five mutations produced diagnosable RED and were restored, and each is recorded in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Probe fixture -> production code path | Fixtures drive the REAL `fetchGeoJsonCached`/`extractPolygons`/runner via the `_fetch` seam; a fixture that bypasses that path proves nothing |
| Probe assertion -> claimed guarantee | A green scenario is read by humans and by the verifier as evidence a requirement holds |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-23 | Spoofing | A scenario that is green while proving nothing | mitigate | Every new scenario is mutation-proven against the exact line it covers, with a diagnosable RED message required (Phase 15 D-10, recorded in STATE.md as blocking). Every scenario additionally carries a **control assertion** or a **precondition guard** — the two preventatives proven against the two vacuity modes Phase 15 hit (an assertion that observes nothing; a fixture that cannot express its own condition). |
| T-16-24 | Tampering | A fixture that satisfies HAZ-03 accidentally | mitigate | `hazardsFeature` emits only a lowercase `label` and never an uppercase `LABEL`, and scenario #2's control feature carries ONLY `LABEL` and must produce no entry. A `toValue` that reverts to the broken positional parameter fails both directions. |
| T-16-25 | Tampering | An unrouted hazards layer setting `anyStale` and making an unrelated scenario's assertion vacuous | mitigate | `hazardsRoutes` covers all six URLs plus a quiet default (WR-02's trap, the same discipline `eroHttpRoutes` documents), and `hazards-routes-are-quiet-by-default` pins that the default table cannot itself set `anyStale`. |
| T-16-26 | Repudiation | Day-offset assertions going red at a date boundary | mitigate | Every new scenario pins `helper._nowMs` to a fixed instant, so offsets are deterministic and reproducible; no scenario reads the real clock. |
| T-16-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — zero packages installed in this phase. |
</threat_model>

<verification>
- `node scripts/probe-payload-resilience.js` reports every pre-existing scenario plus every scenario
  added here passing, with zero skips introduced.
- All eleven mutations across Tasks 2 and 3 produce diagnosable RED and are restored; each is
  recorded in the SUMMARY with the file:line broken and the verbatim RED message.
- No pre-existing assertion was changed or weakened; only route tables were extended, and every
  touched scenario is named in the SUMMARY.
</verification>

<success_criteria>
- HAZ-01: day bucketing and multi-day spread are pinned by a mutation-proven scenario, closing the
  gap left by both Precipitation layers returning zero features live.
- HAZ-03: the lowercase-`label` read is proven end to end, with a control proving the uppercase
  field is NOT the source.
- HAZ-04: Flooding is proven absent under three toggle combinations; Drought is proven hidden at
  the shipped default and shown only on explicit opt-in — both at the payload AND at the DOM.
- The suite remains green in full, with the hazards route table proven quiet by default.
</success_criteria>

<output>
Create `.planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-06-SUMMARY.md` when done.
Record: the suite's before/after scenario counts, every existing scenario whose routes were
extended, and the full mutation inventory (mutation, file:line, verbatim RED message) — input to
16-08's phase mutation inventory.
</output>
