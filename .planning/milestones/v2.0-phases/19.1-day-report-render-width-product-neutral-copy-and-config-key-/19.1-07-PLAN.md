---
phase: 19.1-day-report-render-width-product-neutral-copy-and-config-key
plan: 07
type: execute
wave: 2
depends_on: ["19.1-06"]
gap_closure: true
files_modified:
  - scripts/probe-payload-resilience.js
autonomous: true
requirements: []
traces:
  - "19.1-VERIFICATION.md gaps[0].missing[2] — the three uat05-* scenarios must be RE-EXPRESSED against the replacement mechanism, not deleted"
  - "19.1-PATTERNS.md 'scripts/probe-payload-resilience.js — the three uat05-* scenarios to re-express' — per-scenario re-expression notes"
  - "19.1-CONTEXT.md Claude's Discretion (width regression guard mechanism) and Phase 15 D-10 (mutation proof, blocking)"
  - "19-UAT.md test 5 — the defect class these guards exist to prevent shipping green again"

must_haves:
  truths:
    - "The probe suite is fully green again with zero skipped scenarios"
    - "All three uat05-* scenarios still exist, re-expressed against grid tracks — none was deleted, and each still pins the same regression value it pinned before (no space-run padding, a declared column width, boxes derived from the group's own content)"
    - "A new structural guard pins the property that actually closes UAT-5: every detail row is width-bounded (width:100% + minmax(0,Nfr)) and carries no intrinsic-width unit that could widen the region"
    - "The harness's INERT_MARKUP_ALLOWLIST is proven — by an executed assertion, not by inspection — to accept the new grid row markup without modification"
    - "Every re-expressed and new scenario is individually mutation-proven per Phase 15 D-10: break the exact line it covers, confirm RED with a diagnosable message, restore"
  artifacts:
    - path: "scripts/probe-payload-resilience.js"
      provides: "four uat05-* structural guards re-expressed against the grid mechanism, plus seven repaired collateral scenarios"
      contains: "uat05-detail-row-grid-is-width-bounded-and-cannot-widen-the-region"
  key_links:
    - from: "scripts/probe-payload-resilience.js uat05-* scenarios"
      to: "MMM-SPCOutlook.js detailRowOpen / detailFieldCell output"
      via: "renderDom / renderDomWrapper against a real payload fixture"
      pattern: "display:inline-grid"
    - from: "scripts/probe-payload-resilience.js uat05-wrapper-declares-both-region-containment-contracts"
      to: "MMM-SPCOutlook.js REGION_CAP_REM"
      via: "wrapper.style.maxWidth exact-equality assertion"
      pattern: "22\\.5rem"
---

<objective>
Re-express the three `uat05-*` structural guards against the grid mechanism, add the one guard the
retired mechanism could not express, repair the seven collateral scenarios that asserted the retired
style strings, and mutation-prove every one of them.

Purpose: `19.1-VERIFICATION.md` names these scenarios explicitly — they must be re-expressed, not
deleted, because they encode real regression value that the replacement mechanism needs re-pinned in
its own terms. More sharply: 190 green scenarios and a phase verifier both missed UAT-5 the first
time, because index assertions pass while a layout overflows. A mechanism swap without a guard for
the actual defect class would repeat that exactly.

Output: a fully green, fully mutation-proven suite whose uat05 family pins the property that makes the
defect structurally impossible, not merely the style strings that happen to be in the file today.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/buildomator/bm/4.6.0/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/buildomator/bm/4.6.0/templates/summary.md
</execution_context>

<context>
@.planning/phases/19.1-day-report-render-width-product-neutral-copy-and-config-key-/19.1-PATTERNS.md
@.planning/phases/19.1-day-report-render-width-product-neutral-copy-and-config-key-/19.1-VERIFICATION.md
@.planning/phases/19.1-day-report-render-width-product-neutral-copy-and-config-key-/19.1-06-SUMMARY.md

<interfaces>
<!-- Scenario line ranges at HEAD before plan 19.1-06 (they will have shifted only if 19.1-06
     touched this file — it did not; 19.1-06's files_modified is MMM-SPCOutlook.js only). -->

scripts/probe-payload-resilience.js
  :14286-14339  uat05-wrapper-declares-both-region-containment-contracts
  :14364-14487  uat05-detail-fields-carry-no-space-run-padding-and-declare-a-ch-floor
  :14504-14619  uat05-co-winner-and-also-rows-derive-their-boxes-from-the-groups-own-width

<!-- The seven collateral scenarios that assert the RETIRED mechanism's style strings.
     Counts are occurrences of min-width:/pre-wrap/DejaVu Sans Mono/inline-block/32.4rem/maxWidth. -->

  6 occurrences  wr02-also-rows-column-with-the-winner-row-under-an-unmapped-dimension   (:14157)
  3 occurrences  wr07-assert-inert-markup-accepts-module-tags-and-rejects-everything-else
  3 occurrences  wr04-detail-label-truncation-counts-source-characters-not-padding       (:14050)
  3 occurrences  bl02-two-co-equal-survivors-on-one-dimension-are-peers-not-also-rows    (:13360)
  1 occurrence   wr07-hostile-token-in-every-remote-string-still-renders-inert-markup
  1 occurrence   rpt03-day3-dual-badge-joins-only-when-both-badges-are-non-empty          (:14883)
  1 occurrence   rpt02-proximity-only-day-renders-badge-alone-with-a-two-space-gap

<!-- Harness accessors, unchanged by this phase (scripts/probe-lib/module-stubs.js:375-400) -->

renderDomWrapper(frontend, { config, spcrisk }) -> the stub wrapper OBJECT
    (use for wrapper.style.maxWidth / wrapper.style.textAlign assertions)
renderDom(frontend, { config, spcrisk })        -> wrapper.innerHTML || textContent
assertInertMarkup(html, label)                  -> throws on the first non-allowlisted tag;
                                                    runs automatically inside renderDomWrapper

<!-- The markup shape plan 19.1-06 now emits, per detail row -->

<span style="display:inline-grid;box-sizing:border-box;width:100%;padding-left:0.6rem;
             column-gap:0.4rem;align-items:baseline;
             grid-template-columns:minmax(0,13fr) minmax(0,23fr) minmax(0,13fr)">
  <span style="color:#RRGGBB;display:contents">        <- colour carrier, no box
    <span style="min-width:0;overflow-wrap:anywhere">  <- dimension cell
    <span style="min-width:0;overflow-wrap:anywhere">  <- label cell
  </span>
  <span style="min-width:0;overflow-wrap:anywhere">    <- attribution cell ("— WSSI")
</span><br/>

wrapper.style.maxWidth === "22.5rem"   (REGION_CAP_REM + "rem")
wrapper.style.textAlign === "left"     (unchanged 19-08 anchor)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repair the seven collateral scenarios that assert the retired style strings</name>
  <files>scripts/probe-payload-resilience.js</files>
  <read_first>
    - The seven scenario bodies named in `<interfaces>` above, in full — each one's comment block
      states WHY it asserts what it asserts, and that intent is what must survive the string update.
    - MMM-SPCOutlook.js — the builders plan 19.1-06 landed (`detailTrack`, `detailRowOpen`,
      `detailFieldCell`, the re-targeted `detailColoredWrapper`) so the new expected strings come from
      the code, not from this plan's prose.
    - scripts/probe-lib/module-stubs.js lines 337-400 — the allowlist and both render accessors.
    - .planning/phases/.../19.1-06-SUMMARY.md — the exact failing-scenario list 19.1-06 recorded.
  </read_first>
  <action>
    Update each of the seven scenarios so it asserts the shipped grid markup instead of the retired
    `inline-block;min-width:Nch` / `pre-wrap` / monospace markup. Change the STRINGS, preserve the
    INTENT and the comment rationale in every case. Specifically:

    - `wr02-also-rows-column-with-the-winner-row-under-an-unmapped-dimension`: its subject is that an
      unmapped dimension name whose length overruns `DIMENSION_FIELD_WIDTH` does not detach the `also:`
      row from its winner row. Re-express: assert the winner row and the `also:` row emit the SAME
      `grid-template-columns` string (byte-identical substring), which is a strictly stronger statement
      of the same invariant than matching two `min-width:Nch` numbers was. Its own comment currently
      says "every sub-row is white-space:pre-wrap monospace" — that sentence is now false; rewrite it
      to say the column contract is a property of the row's declared TRACKS, not of the character
      stream.
    - `wr07-assert-inert-markup-accepts-module-tags-and-rejects-everything-else`: add the grid row
      opener shape (a `<span style="...">` whose style contains `display:inline-grid` and
      `grid-template-columns:minmax(0,`) to its accepted-tags fixture, alongside whatever it already
      lists, and keep every rejection case unchanged. This is the executed proof — not an inspection —
      that `INERT_MARKUP_ALLOWLIST` accepts the new mechanism WITHOUT modification. Add a comment
      recording that deliberately: the chosen remedy is CSS Grid expressed through the existing
      `<span style="...">` tag shape, so the allowlist was NOT widened, and any future change that
      introduces `<div>`/`<table>`/`<tr>`/`<td>` WOULD require widening it and must be a deliberate
      decision of its own.
    - `wr07-hostile-token-in-every-remote-string-still-renders-inert-markup`: update only the expected
      style substrings; every hostile-token and escaping assertion stays exactly as it is. This
      scenario is the T-19.1G-01 control — do not weaken it.
    - `wr04-detail-label-truncation-counts-source-characters-not-padding`: its subject is that the
      60-character bound counts the entry's own source text, never the dimension prefix, the padding,
      or the module-authored badge. Padding no longer exists, so re-express the "not padding" half as
      "not the cell markup": assert the truncated label appears inside the label cell with no added
      characters, and that the ellipsis lands at the end of the label rather than mid-column.
    - `bl02-two-co-equal-survivors-on-one-dimension-are-peers-not-also-rows`: re-express its blank-box
      assertion as a blank CELL assertion — the co-winner's dimension cell is `<span
      style="min-width:0;overflow-wrap:anywhere"></span>` with empty content, and still never
      `&nbsp;`, never a `.repeat()` run, never a zero-width space.
    - `rpt03-day3-dual-badge-joins-only-when-both-badges-are-non-empty` and
      `rpt02-proximity-only-day-renders-badge-alone-with-a-two-space-gap`: update the affected style
      substrings only. If `rpt02`'s "two-space gap" refers to a run that 19.1-06 retired, re-express it
      against the surviving separator and rename nothing — record in the comment which literal it used
      to assert and why that literal no longer exists.

    Do not delete any of the seven. Do not weaken any escaping, colour, or truncation assertion in any
    of them. Do not modify `scripts/probe-lib/module-stubs.js`.
  </action>
  <acceptance_criteria>
    - All seven scenario names still exist:
      `grep -c 'wr02-also-rows-column-with-the-winner-row-under-an-unmapped-dimension\|wr07-assert-inert-markup-accepts-module-tags-and-rejects-everything-else\|wr07-hostile-token-in-every-remote-string-still-renders-inert-markup\|wr04-detail-label-truncation-counts-source-characters-not-padding\|bl02-two-co-equal-survivors-on-one-dimension-are-peers-not-also-rows\|rpt03-day3-dual-badge-joins-only-when-both-badges-are-non-empty\|rpt02-proximity-only-day-renders-badge-alone-with-a-two-space-gap' scripts/probe-payload-resilience.js`
      returns at least `7`.
    - `grep -c 'DejaVu Sans Mono' scripts/probe-payload-resilience.js` returns `0`.
    - `grep -c 'inline-block;min-width:' scripts/probe-payload-resilience.js` returns `0`.
    - `wr07-assert-inert-markup-accepts-module-tags-and-rejects-everything-else`'s body contains the
      substring `display:inline-grid` — the allowlist acceptance is executed, not asserted in prose.
    - `git diff --exit-code scripts/probe-lib/module-stubs.js` exits 0.
    - `node scripts/probe-payload-resilience.js` reports failures ONLY from the three `uat05-*`
      scenario names (Task 2's work). No collateral scenario is still red.
  </acceptance_criteria>
  <verify>
    <automated>node --check scripts/probe-payload-resilience.js && test "$(grep -c 'DejaVu Sans Mono' scripts/probe-payload-resilience.js)" = "0" && test "$(grep -c 'inline-block;min-width:' scripts/probe-payload-resilience.js)" = "0" && git diff --exit-code scripts/probe-lib/module-stubs.js && node scripts/probe-payload-resilience.js 2>&1 | grep -E '^\s*(FAIL|✗)' | grep -v 'uat05-' | wc -l | grep -qx 0 && echo "OK collateral scenarios repaired; only uat05-* remain red"</automated>
  </verify>
  <done>All seven collateral scenarios assert the shipped grid markup with their original intent and
  rationale intact; `wr07-assert-inert-markup-...` executes the proof that the allowlist accepts
  `display:inline-grid` unmodified; the only red scenarios left are the three `uat05-*` ones.</done>
</task>

<task type="auto">
  <name>Task 2: Re-express the three uat05-* guards and add the width-bounded guard</name>
  <files>scripts/probe-payload-resilience.js</files>
  <read_first>
    - scripts/probe-payload-resilience.js lines 14286-14339, 14364-14487, 14504-14619 — all three
      `uat05-*` scenarios IN FULL, including every inline comment. Read them before writing a
      replacement; each body states the exact string/regex whose spirit the new version must still
      satisfy.
    - .planning/phases/.../19.1-PATTERNS.md, section "scripts/probe-payload-resilience.js — the three
      uat05-* scenarios to re-express" — the per-scenario re-expression notes, which state which
      assertions are mechanism-agnostic (transfer with the least change) and which are tied to the
      retired `ch`/monospace mechanism (re-express, never delete).
    - MMM-SPCOutlook.js — `REGION_CAP_REM`, `detailRowOpen`, `detailFieldCell`, `detailTrack`,
      `detailColoredWrapper`, and both call sites.
  </read_first>
  <action>
    Re-express all three, and add a fourth. Renaming a scenario is allowed and expected where the old
    name encodes the retired mechanism; deleting one is not.

    **(1) `uat05-wrapper-declares-both-region-containment-contracts` — keep the name.** The name is
    still accurate. Change the `wrapper.style.maxWidth` exact-equality assertion from `"32.4rem"` to
    `"22.5rem"`. Keep the vacuity guard (a detail sub-row actually rendered) and the control block
    (both properties are also set on the loading-branch wrapper) unchanged — both transfer as-is per
    the pattern map. Add to its comment the fact the new value carries: `22.5rem` is
    `(1020 − 540 − 30) / 20`, a budget, and that this scenario proves the property was ASSIGNED, never
    that the region stops short of the centreline — that claim is `MANUAL ONLY` and belongs to plan
    19.1-09's live check.

    **(2) Rename `uat05-detail-fields-carry-no-space-run-padding-and-declare-a-ch-floor` to
    `uat05-detail-rows-carry-no-space-run-padding-and-declare-fixed-grid-tracks`.** Re-express its four
    assertions:
    - (a) **no space-run padding** — mechanism-agnostic, transfers, and gets STRICTLY STRONGER: plan
      19.1-06 retired the attribution gap (`"   — "` → `"— "`) and the sub-line's five-space indent, so
      BOTH of this assertion's current named exemptions are gone. Assert zero runs of two or more
      consecutive spaces anywhere in the rendered detail markup, with no exemption list at all, and
      comment that the exemptions were removed because the literals they covered no longer exist.
    - (b) **the `min-width:13ch`/`min-width:23ch` boxes** — tied to the retired mechanism. Re-express as
      the declared TRACK assertion: the row's style contains `grid-template-columns:` followed by three
      `minmax(0,Nfr)` tracks whose `N` values are `DIMENSION_FIELD_WIDTH` (or the group's own longer
      rendered width), `DETAIL_LABEL_FIELD_WIDTH` (23), and `SOURCE_ATTRIBUTION_FIELD_WIDTH`. The
      intent — "the column floor is declared, not emergent" — is preserved; the unit changes.
    - (c) **`min-width:` is always preceded by `font-family:'DejaVu Sans Mono'` in its own row
      (Pitfall 4)** — this assertion's entire subject is the monospace coupling the swap exists to
      remove. Do NOT delete it and do NOT leave it out: INVERT it. Assert that the rendered detail
      markup contains ZERO occurrences of `font-family` and ZERO occurrences of the `ch` unit, and
      comment that Pitfall 4 no longer applies because no length in this markup resolves against an
      element's own font. That is the honest re-expression: the old assertion pinned that the coupling
      was correctly maintained; the new one pins that the coupling is gone.
    - (d) **overflow floors, not clips, a 200-char label** — mechanism-agnostic, transfers. Update it to
      assert the 200-char label still appears in full (up to `truncateHazardLabel`'s own bound) inside
      its cell and is not clipped, and that the row's `grid-template-columns` string is UNCHANGED by
      the long label — i.e. the overflow is absorbed inside the track rather than widening the row.
      That last clause is new and is the whole point of `minmax(0,Nfr)`.

    **(3) Rename `uat05-co-winner-and-also-rows-derive-their-boxes-from-the-groups-own-width` to
    `uat05-co-winner-and-also-rows-derive-their-grid-tracks-from-the-groups-own-width`.** Its invariant
    is completely mechanism-agnostic and is the single most important assertion to preserve exactly:
    one derived quantity feeds the winner cell, the BL-02 blank co-winner cell, and the `also:` row's
    dimension track, and none may independently drift. Keep the same fixture (a winner, a co-winner and
    a suppressed competitor on one dimension). Re-express:
    - Extract the `grid-template-columns:` substring from all three emitted rows and assert all three
      are byte-identical. Under the retired mechanism this required comparing three separate
      `min-width:Nch` numbers; under grid it is one string equality, which is a stronger and less
      evadable form of the same invariant.
    - Keep the BL-02 assertion that the co-winner's dimension cell is EMPTY — never `&nbsp;`, never a
      `.repeat()` run, never a zero-width space.
    - Keep the assertion that the literal `also:` text stays OUTSIDE any colour span.
    - The `2 + 13 + 2 = 17` spacer arithmetic is retired along with `alsoIndent`; replace it with the
      assertion that the `also:` row's first grid cell is empty and its track is the same one the
      winner row declares. Record in the comment that the arithmetic was replaced by a structural
      guarantee, and cite WR-02 iteration 4 / BL-02 / `19-REVIEW-FIX.md` so the review history that
      this codebase has broken and re-fixed multiple times stays attached to the assertion.

    **(4) Add `uat05-detail-row-grid-is-width-bounded-and-cannot-widen-the-region`.** This is the guard
    the retired mechanism could not express, and it is the one that pins the actual UAT-5 defect class
    rather than a style string. Against a payload that renders detail sub-rows, assert on the rendered
    markup:
    - every row opener carries both `width:100%` and `box-sizing:border-box`;
    - every `grid-template-columns` track is `minmax(0,` — no bare `Nfr`, no `auto`, no `max-content`,
      no `min-content`, no `fit-content`;
    - the detail markup contains zero occurrences of the intrinsic-width units and keywords `ch`,
      `max-content`, `min-content` and `fit-content`;
    - `wrapper.style.maxWidth` is set (the cap exists).
    Comment it with the reasoning: a row that is `width:100%` of a capped container, with every track
    floored at zero, cannot contribute a max-content width larger than the cap, so no detail row can
    widen the region — the property that 190 green scenarios failed to notice was false. State
    explicitly that this is a STRUCTURAL proxy: it proves the markup cannot widen the region by
    construction; it does not and cannot prove the deployed region stops short of the centreline, which
    stays `MANUAL ONLY` (plan 19.1-09).
  </action>
  <acceptance_criteria>
    - `grep -c 'uat05-' scripts/probe-payload-resilience.js` returns at least `4` distinct scenario
      names.
    - All four of these names are present:
      `uat05-wrapper-declares-both-region-containment-contracts`,
      `uat05-detail-rows-carry-no-space-run-padding-and-declare-fixed-grid-tracks`,
      `uat05-co-winner-and-also-rows-derive-their-grid-tracks-from-the-groups-own-width`,
      `uat05-detail-row-grid-is-width-bounded-and-cannot-widen-the-region`.
    - Neither retired name survives:
      `grep -c 'declare-a-ch-floor\|derive-their-boxes-from-the-groups-own-width' scripts/probe-payload-resilience.js`
      returns `0`.
    - `grep -c '22\.5rem' scripts/probe-payload-resilience.js` returns at least `1`;
      `grep -c '32\.4rem' scripts/probe-payload-resilience.js` returns `0`.
    - `node scripts/probe-payload-resilience.js` final line matches `0 failed, 0 skipped` and the
      passed count is at least 191 (190 previous + the one new scenario).
    - `git diff --exit-code scripts/probe-lib/module-stubs.js` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/probe-payload-resilience.js 2>&1 | tail -1 | grep -q '0 failed, 0 skipped' && test "$(grep -c 'declare-a-ch-floor\|derive-their-boxes-from-the-groups-own-width' scripts/probe-payload-resilience.js)" = "0" && grep -q 'uat05-detail-row-grid-is-width-bounded-and-cannot-widen-the-region' scripts/probe-payload-resilience.js && test "$(grep -c '32\.4rem' scripts/probe-payload-resilience.js)" = "0" && git diff --exit-code scripts/probe-lib/module-stubs.js && echo "OK four uat05 guards green, suite fully green"</automated>
  </verify>
  <done>Four `uat05-*` scenarios exist and pass: the wrapper-cap contract, the no-space-run/declared-
  tracks contract with the monospace coupling inverted rather than dropped, the one-derived-quantity
  contract as a single string equality across three row kinds, and the new width-bounded guard. Suite
  is green with zero skips.</done>
</task>

<task type="auto">
  <name>Task 3: Mutation-prove every re-expressed and new scenario individually</name>
  <files>scripts/probe-payload-resilience.js</files>
  <read_first>
    - .planning/phases/.../19.1-PATTERNS.md "Mutation-proof every re-expressed scenario individually
      (Phase 15 D-10, blocking)" — a scenario green under its own mutation is a fixture defect, and
      that is literally the failure class that let UAT-5 ship past 123 (now 190) green scenarios.
    - .planning/phases/.../19.1-04-SUMMARY.md — how plan 19.1-04 recorded its own mutation proofs, so
      this SUMMARY's format matches the phase's established one.
    - MMM-SPCOutlook.js — the exact lines each scenario covers, so each mutation targets the covered
      line rather than something adjacent.
  </read_first>
  <action>
    For EACH of the four `uat05-*` scenarios and for `wr07-assert-inert-markup-accepts-module-tags-
    and-rejects-everything-else`, run one mutation cycle against `MMM-SPCOutlook.js`:

    1. Apply the mutation to the exact line the scenario covers.
    2. Run the full suite. Confirm the target scenario goes RED and that its failure message names
       what actually broke (a message that only says "assertion failed" is not diagnosable — improve
       the scenario's message and repeat).
    3. Restore the line verbatim (`git checkout -- MMM-SPCOutlook.js` or an exact revert) and confirm
       the suite returns to `0 failed, 0 skipped`.

    Use these mutations — each breaks the specific property its scenario claims to pin:
    - `uat05-wrapper-declares-both-region-containment-contracts` → change `REGION_CAP_REM = 22.5` to
      `32.4`.
    - `uat05-detail-rows-carry-no-space-run-padding-and-declare-fixed-grid-tracks` → run TWO mutations,
      because this scenario pins two independent properties: (i) reintroduce a space run by changing
      `detailSourceAttribution`'s `"— "` back to `"   — "`; (ii) reintroduce the retired coupling by
      adding `font-family:'DejaVu Sans Mono','Liberation Mono',monospace` to `detailRowOpen`'s style
      string. Both must go RED.
    - `uat05-co-winner-and-also-rows-derive-their-grid-tracks-from-the-groups-own-width` → in the
      `also:` call site, replace `detailRowOpen(dimensionTrackFr)` with
      `detailRowOpen(DIMENSION_FIELD_WIDTH)` — the exact drift WR-02 iteration 4 exists to prevent.
    - `uat05-detail-row-grid-is-width-bounded-and-cannot-widen-the-region` → in `detailTrack`, change
      `minmax(0,` + N + `fr)` to a bare `N` + `fr` track, removing the zero floor. This is the precise
      mutation that would let a long label widen the row again.
    - `wr07-assert-inert-markup-accepts-module-tags-and-rejects-everything-else` → change
      `detailRowOpen`'s opening `<span` to `<div` and confirm the scenario goes RED with an
      `assertInertMarkup` message. This proves, by execution, that a real new tag shape WOULD have
      required an allowlist change — and therefore that the shipped mechanism genuinely did not.

    Record every cycle in the SUMMARY as a table: scenario, file:line mutated, the mutation, the RED
    message observed (quoted), and confirmation of restore. Confirm `git status --porcelain --
    MMM-SPCOutlook.js` is empty at the end — no mutation may survive into the commit.
  </action>
  <acceptance_criteria>
    - `git status --porcelain -- MMM-SPCOutlook.js` is empty at the end of the task — every mutation
      was reverted.
    - `node scripts/probe-payload-resilience.js` → `0 failed, 0 skipped`.
    - The SUMMARY contains a mutation-proof table with one row per cycle (6 cycles: 4 scenarios, with
      2 cycles for the space-run/tracks scenario, plus the wr07 tag-shape cycle), each quoting the
      observed RED message verbatim.
    - Every RED message quoted in the table names the broken property (e.g. names the scenario's
      subject, the expected string, or the offending tag) — none is a bare "assertion failed".
    - The `<div>` mutation's RED message contains `is not a tag MMM-SPCOutlook.js emits`, proving the
      allowlist is genuinely load-bearing and genuinely untouched.
  </acceptance_criteria>
  <verify>
    <automated>test -z "$(git status --porcelain -- MMM-SPCOutlook.js)" && node scripts/probe-payload-resilience.js 2>&1 | tail -1 | grep -q '0 failed, 0 skipped' && git diff --exit-code scripts/probe-lib/module-stubs.js && echo "OK suite green, no mutation left in the tree"</automated>
  </verify>
  <done>Every re-expressed and new guard has been individually broken and seen to go RED with a
  diagnosable message, then restored; the suite is green; no mutation survives in the working tree; and
  the `<div>` cycle has proven by execution that the allowlist was not silently bypassed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hostile payload fixture → `renderDom`/`renderDomWrapper` → `assertInertMarkup` | The harness is where an escaping defect in the mechanism swap is caught before it reaches a screen. Weakening a scenario here silently removes a live XSS control. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19.1G-06 | Elevation (stored XSS, control removal) | The seven collateral scenarios, especially `wr07-hostile-token-in-every-remote-string-still-renders-inert-markup` | mitigate | Task 1 permits updating expected STYLE substrings only. Every hostile-token, escaping, colour-validation and truncation assertion is explicitly out of scope for modification, and Task 3 re-proves `wr07`'s allowlist enforcement by an executed `<div>` mutation rather than by inspection. |
| T-19.1G-07 | Repudiation (false green) | Any re-expressed scenario that passes without actually pinning anything | mitigate | Phase 15 D-10 mutation proof is mandatory and per-scenario (Task 3), with the RED message required to be diagnosable. A scenario green under its own mutation is a fixture defect, not a pass — this is the exact failure class that let UAT-5 ship past 190 green scenarios. |
| T-19.1G-08 | Tampering | `INERT_MARKUP_ALLOWLIST` | mitigate | `git diff --exit-code scripts/probe-lib/module-stubs.js` is an acceptance criterion on every task in this plan. The allowlist is not widened; the decision not to widen it is pinned by an executed assertion in `wr07`, so a future mechanism change cannot widen it silently. |
| T-19.1G-SC | Tampering | npm/pip/cargo installs | mitigate | Zero dependencies added. `19.1-RESEARCH.md` Pitfall 1 and its Alternatives Considered table already reject `linkedom`/`jsdom` — neither implements CSS layout, so neither would answer the layout question anyway. No package-manager install task exists in this plan; `package.json` and `pnpm-lock.yaml` must remain unmodified. |
</threat_model>

<verification>
- `node scripts/probe-payload-resilience.js` → `0 failed, 0 skipped`, passed count ≥ 191.
- `git diff --exit-code scripts/probe-lib/module-stubs.js package.json` exits 0.
- `grep -c 'DejaVu Sans Mono\|inline-block;min-width:\|32\.4rem' scripts/probe-payload-resilience.js`
  returns `0`.
- All four `uat05-*` names present; neither retired name present.
- The SUMMARY's mutation-proof table has six rows, each with a quoted RED message.
</verification>

<success_criteria>
- The suite is green with zero skips and one net-new scenario.
- All three original `uat05-*` guards survive as re-expressions, with the monospace-coupling assertion
  inverted rather than dropped and the one-derived-quantity invariant strengthened to a single string
  equality across three row kinds.
- A new guard pins the width-bounded property that makes the UAT-5 defect class structurally
  impossible.
- The harness allowlist is byte-identical, and that fact is proven by an executed mutation rather than
  asserted in prose.
- Every guard is individually mutation-proven with a diagnosable RED message.
</success_criteria>

<output>
Create `.planning/phases/19.1-day-report-render-width-product-neutral-copy-and-config-key-/19.1-07-SUMMARY.md`
when done. It MUST include the six-row mutation-proof table with verbatim RED messages, the final
`PROBE RESULT` line, and the four final `uat05-*` scenario names (plan 19.1-09 writes them into
`19-PARITY-CHECKLIST.md`'s Phase 19.1 Addendum).
</output>
