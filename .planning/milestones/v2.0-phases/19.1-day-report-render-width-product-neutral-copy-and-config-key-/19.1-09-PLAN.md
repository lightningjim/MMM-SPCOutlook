---
phase: 19.1-day-report-render-width-product-neutral-copy-and-config-key
plan: 09
type: execute
wave: 3
depends_on: ["19.1-07", "19.1-08"]
gap_closure: true
files_modified:
  - MMM-SPCOutlook.js
  - scripts/probe-payload-resilience.js
  - .planning/phases/19.1-day-report-render-width-product-neutral-copy-and-config-key-/19.1-LIVE-CHECK-2.md
  - .planning/phases/19.1-day-report-render-width-product-neutral-copy-and-config-key-/19.1-UI-SPEC.md
  - .planning/phases/19-unified-day-report-getdom-rewrite/19-PARITY-CHECKLIST.md
autonomous: false
requirements: []
traces:
  - "19.1-VERIFICATION.md gaps[0].missing[3] — a fresh live check on the same deployed hardware (ssh mm) before any new cap/mechanism is trusted"
  - "19.1-VERIFICATION.md gaps[0].truth — the blocking limb-1 failure this plan exists to close"
  - "19-UAT.md test 5 (MAJOR) — the baseline screenshot every verdict is stated against"
  - "19.1-LIVE-CHECK.md 'Validity Preconditions' and 'Condition 2 Resolution' — the protocol this plan reuses rather than reinvents"
  - "19.1-VERIFICATION.md truth 3 — item 6 (also: attachment) was operator-hedged ('I believe so') and is scheduled for re-check on the next live run"

must_haves:
  truths:
    - "The detail-mode day report no longer overlaps the centre column on the deployed MagicMirror, stated by the operator against the 19-UAT test 5 screenshot on real hardware"
    - "Both deploy-validity preconditions are positively confirmed BEFORE any observation is recorded: the deployed file is byte-identical to the local commit, and dayReportDetail:true is genuinely active"
    - "Item 6 (also: rows attached to their winner row) is re-checked and recorded at its true claim strength, replacing the previous 'I believe so' hedge"
    - "The typeface reversal introduced by the mechanism swap — the also: token and the em-dash attributions render proportional again rather than monospace — is observed and either accepted or rejected, never assumed"
    - "The shipped cap is the value the live observation actually supports, and if it changed, every place that restates it changed with it"
    - "Every claim that could not be observed is recorded as NOT OBSERVABLE with its reason, never as PASS"
  artifacts:
    - path: ".planning/phases/19.1-day-report-render-width-product-neutral-copy-and-config-key-/19.1-LIVE-CHECK-2.md"
      provides: "the second live observation record: deploy evidence, validity preconditions, log evidence, per-item verdicts, disposition"
      contains: "## Validity Preconditions"
    - path: ".planning/phases/19-unified-day-report-getdom-rewrite/19-PARITY-CHECKLIST.md"
      provides: "Phase 19.1 Addendum updated to the four re-expressed uat05-* scenario names and the second live verdict"
    - path: ".planning/phases/19.1-day-report-render-width-product-neutral-copy-and-config-key-/19.1-UI-SPEC.md"
      provides: "the Region Containment cap promoted from PENDING LIVE CONFIRMATION to the live outcome"
  key_links:
    - from: "19.1-UI-SPEC.md Region Containment"
      to: "MMM-SPCOutlook.js REGION_CAP_REM"
      via: "the same literal value, in agreement after any tuning"
      pattern: "REGION_CAP_REM"
    - from: "scripts/probe-payload-resilience.js uat05-wrapper-declares-both-region-containment-contracts"
      to: "MMM-SPCOutlook.js REGION_CAP_REM"
      via: "the asserted maxWidth literal must equal the shipped value"
      pattern: "wrapper\\.style\\.maxWidth"
    - from: "19-PARITY-CHECKLIST.md Phase 19.1 Addendum"
      to: "scripts/probe-payload-resilience.js uat05-* scenario names"
      via: "the addendum lists the scenarios that actually exist"
      pattern: "uat05-"
---

<objective>
Prove on the deployed Raspberry Pi that the detail-mode day report no longer overlaps the centre
column, apply whatever tuning the observation requires, and reconcile the two documents whose values
this observation settles.

Purpose: this is the ONLY step that can close the blocking gap. `19.1-VERIFICATION.md` states the rule
plainly — no width claim for this module may be treated as resolved without an on-hardware observation,
and this phase has burned twice on exactly that (the 19-08 false negative against a 21-commits-stale
deploy with a misspelled config key, and the 2026-09-10 disproof of `32.4rem`). Neither `jsdom` nor
`linkedom` implements CSS layout; plan 19.1-07's structural guard proves the markup CANNOT widen the
region by construction, which is a strictly weaker claim than the region actually clearing the
centreline on this specific display.

Output: a second live observation record, a live-supported cap, an updated parity checklist, and a
UI-SPEC whose pending value is resolved.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/buildomator/bm/4.6.0/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/buildomator/bm/4.6.0/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/19.1-day-report-render-width-product-neutral-copy-and-config-key-/19.1-LIVE-CHECK.md
@.planning/phases/19.1-day-report-render-width-product-neutral-copy-and-config-key-/19.1-VERIFICATION.md
@.planning/phases/19.1-day-report-render-width-product-neutral-copy-and-config-key-/19.1-06-SUMMARY.md
@.planning/phases/19.1-day-report-render-width-product-neutral-copy-and-config-key-/19.1-07-SUMMARY.md
@.planning/phases/19-unified-day-report-getdom-rewrite/19-UAT.md
@.planning/phases/19-unified-day-report-getdom-rewrite/19-PARITY-CHECKLIST.md

<interfaces>
<!-- Target hardware, confirmed 2026-09-10 during the first live check. Reuse; do not rediscover. -->

Reachable as             ssh mm
Hardware                 Raspberry Pi 4 Model B, aarch64, Node v24.13.1, MagicMirror 2.37.0
Process manager          pm2, process name `mm` (npm start -> electron js/electron.js). NOT systemd.
PATH GOTCHA              `node` and `pm2` are BOTH absent from the non-interactive SSH PATH, and pm2
                         shells out to node. Every remote command that touches either must first run:
                         export PATH="$HOME/.nvm/versions/node/v24.13.1/bin:$PATH"
                         Without it, `pm2 restart mm` fails with
                         `env: 'node': No such file or directory` and does NOT restart the process.
Module install path      ~/MagicMirror/modules/MMM-SPCOutlook  (a git clone whose own HEAD is stale;
                         the first live check synced FILE CONTENT via rsync and deliberately left the
                         clone's HEAD pointer unmoved. Reuse that mechanism.)
Deploy commands used     rsync -av MMM-SPCOutlook.js README.md mm:~/MagicMirror/modules/MMM-SPCOutlook/
                         rsync -av --delete scripts/ mm:~/MagicMirror/modules/MMM-SPCOutlook/scripts/
Backups already present   MMM-SPCOutlook.js.bak-19.1, README.md.bak-19.1, scripts.bak-19.1/,
                         config/config.js.bak-19.1, config/config.js.bak-19uat
                         -> take NEW backups with a `-19.1gap` suffix; do NOT overwrite the existing
                            ones, they are the pre-19.1 restore point.
Log                      ~/MagicMirror/mm-out.log (record the pre-restart line count as the boundary)
Deployed config state    header: "SPC Forecast" (explicit, so the getHeader fallback is NOT exercised
                         on this mirror), dayReportDetail: true (operator-authorised flip, 2026-09-10)
Deployed coordinate      OKC production, lat 35.4432156, lon -97.595822

<!-- Geometry: measured facts, plus the prediction this run tests -->

Panel                    1080 x 1920 (ROTATED)
Body margins             60px each side -> usable 960px, body right edge x = 1020
Screen centreline        x = 540
Previous (disproven)     cap 32.4rem = 648px -> right region spanned x = 372..1020, 168px past centre
This run's prediction    cap 22.5rem = 450px -> right region should span x = 570..1020,
                         i.e. its left edge should now sit 30px RIGHT of the centreline
Hard ceiling             480px (x = 540) — anything at or left of x=540 is a FAIL

<!-- What changed since the last live check, and therefore what is new to observe -->

1. Column alignment is now CSS grid tracks, not monospace character advance.
2. The detail rows are no longer monospace: the `also:` token and the `— SPC` / `— WSSI`
   attributions render in the host's own proportional typeface again. This REVERSES the visible
   consequence plan 19.1-03 introduced and the operator accepted at item 7 of the first live check.
3. Field overflow now WRAPS inside its own cell instead of pushing the attribution column right.
4. Each row is an `inline-grid` at `width:100%` followed by the existing `<br/>` — vertical rhythm is
   worth confirming explicitly, because a block-level grid would have produced blank lines and this
   plan deliberately chose inline-grid to avoid that.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy the gap-closure code and positively confirm the observation is valid</name>
  <files>.planning/phases/19.1-day-report-render-width-product-neutral-copy-and-config-key-/19.1-LIVE-CHECK-2.md</files>
  <read_first>
    - .planning/phases/.../19.1-LIVE-CHECK.md IN FULL — the `## Deploy`, `## Validity Preconditions`,
      `## Condition 2 Resolution` and `## Log Evidence` sections are the protocol you are reusing.
      Copy its structure and its evidence standard; do not invent a new procedure.
    - .planning/phases/.../19.1-06-SUMMARY.md and 19.1-07-SUMMARY.md — what is expected to be
      observable, and the final probe count that must be green before any deploy.
    - .planning/phases/19-unified-day-report-getdom-rewrite/19-UAT.md test 5 and its `## Gaps` section
      — the baseline screenshot and the recording conventions.
    - MMM-SPCOutlook.js — `REGION_CAP_REM` and `detailRowOpen`, so the code-presence markers below
      match what actually shipped.
  </read_first>
  <action>
    Pre-flight, deploy, then positively confirm validity. Do not skip a confirmation step — they are
    the entire reason the 19-08 observation had to be discarded.

    1. Run `node scripts/probe-payload-resilience.js` locally. It must report `0 failed, 0 skipped`.
       Record the count. A red or skipped suite blocks the deploy.
    2. Confirm `git status --porcelain -- MMM-SPCOutlook.js scripts README.md` is empty and record the
       exact commit SHA to be deployed.
    3. Take NEW backups on the Pi with a `-19.1gap` suffix (`MMM-SPCOutlook.js.bak-19.1gap`,
       `scripts.bak-19.1gap/`). Do NOT overwrite the existing `.bak-19.1` / `.bak-19uat` files — they
       are the pre-19.1 restore point and must survive.
    4. Deploy with the same rsync mechanism the first live check used (the deployed clone's HEAD is
       stale and the local commits are not on `origin`, so `git pull`/`git checkout` is still not a
       viable mechanism). Do not `git push`. Do not modify `~/MagicMirror/config/config.js` in this
       task.
    5. **Confirm precondition 1 — the deployed code is THIS code.** Compare `sha256sum` of the local
       and deployed `MMM-SPCOutlook.js` and record both digests; they must MATCH. Then confirm five
       code-presence markers in the deployed file, recording each count:
       `REGION_CAP_REM`, `display:inline-grid`, `minmax(0,`, `Loading NWS outlooks`,
       `getHeader: function`. Also confirm two ABSENCE markers, each of which must be `0`:
       `32.4rem` and `DejaVu Sans Mono`. The absence markers are what prove the retired mechanism is
       actually gone from the deployed file rather than merely from the local one.
    6. **Confirm precondition 2 — global detail mode is genuinely in effect.** Read the deployed
       `~/MagicMirror/config/config.js` READ-ONLY and confirm the `MMM-SPCOutlook` entry carries
       `dayReportDetail: true`, spelled singular. Quote the entry verbatim in the record. The operator
       authorised this value on 2026-09-10; confirm it is still true rather than assuming it, because
       the first live check found it had been toggled since the note that said it was on. If it reads
       `false`, or if the key is misspelled, STOP and report a named blocker — this plan may not flip
       it without a fresh operator authorisation, exactly as the first live check handled it.
    7. Restart: `ssh mm 'export PATH="$HOME/.nvm/versions/node/v24.13.1/bin:$PATH"; pm2 restart mm'`.
       Record the pre-restart `mm-out.log` line count as the boundary, then capture from the restart
       onward and extract: the module-start line, the first-accepted-payload timing line, and any
       `unrecognized config key` line (expected ABSENT for this correctly-spelled config — its absence
       remains live evidence that plan 19.1-02's validator does not over-fire).
    8. Create `19.1-LIVE-CHECK-2.md` with the sections `## Deploy`, `## Validity Preconditions`,
       `## Log Evidence`, and empty `## Observations` / `## Disposition` sections for Task 2. Do NOT
       modify `19.1-LIVE-CHECK.md` — it is the authoritative record of the first run and must stay
       byte-identical.

    This task modifies no repository source file. Expect a zero-diff working tree apart from the new
    record file.
  </action>
  <acceptance_criteria>
    - `node scripts/probe-payload-resilience.js` → `0 failed, 0 skipped`; the count is quoted in
      `19.1-LIVE-CHECK-2.md`.
    - `git status --porcelain -- MMM-SPCOutlook.js scripts README.md` is empty at deploy time; the SHA
      is recorded.
    - Local and deployed `sha256sum` of `MMM-SPCOutlook.js` are recorded and MATCH.
    - CLI output for each of the five presence markers is recorded and non-zero; both absence markers
      (`32.4rem`, `DejaVu Sans Mono`) are recorded and exactly `0`.
    - The deployed `MMM-SPCOutlook` config entry is quoted verbatim with the `dayReportDetail` spelling
      and value explicitly stated.
    - `ssh mm 'ls ~/MagicMirror/modules/MMM-SPCOutlook/MMM-SPCOutlook.js.bak-19.1'` still succeeds —
      the pre-19.1 backup was not overwritten.
    - MagicMirror restarted successfully (the restart command included the nvm PATH export) and
      `mm-out.log` lines from the restart are quoted.
    - `19.1-LIVE-CHECK-2.md` exists with all four sections and records both preconditions as PASS with
      evidence — or the plan stops here with a FAIL and a named blocker.
    - `git diff --exit-code .planning/phases/19.1-day-report-render-width-product-neutral-copy-and-config-key-/19.1-LIVE-CHECK.md`
      exits 0 — the first record is untouched.
  </acceptance_criteria>
  <verify>
    <automated>D=.planning/phases/19.1-day-report-render-width-product-neutral-copy-and-config-key-; node scripts/probe-payload-resilience.js 2>&1 | tail -1 | grep -q '0 failed, 0 skipped' && test -z "$(git status --porcelain -- MMM-SPCOutlook.js scripts README.md)" && ssh mm 'F=~/MagicMirror/modules/MMM-SPCOutlook/MMM-SPCOutlook.js; for p in "REGION_CAP_REM" "display:inline-grid" "minmax(0," "Loading NWS outlooks" "getHeader: function"; do c=$(grep -c "$p" "$F"); echo "$p -> $c"; [ "$c" -ge 1 ] || exit 1; done; for p in "32.4rem" "DejaVu Sans Mono"; do c=$(grep -c "$p" "$F"); echo "ABSENT? $p -> $c"; [ "$c" -eq 0 ] || exit 1; done' && test -f "$D/19.1-LIVE-CHECK-2.md" && git diff --exit-code "$D/19.1-LIVE-CHECK.md" && echo "OK deployed code confirmed: new mechanism present, retired mechanism absent"</automated>
  </verify>
  <done>The suite is green, the deployed file is byte-identical to the local commit and provably
  contains the grid mechanism and provably does NOT contain the retired one, `dayReportDetail: true` is
  confirmed active, log evidence is captured, and `19.1-LIVE-CHECK-2.md` records both preconditions as
  PASS. Neither condition that invalidated the 19-08 observation can be true.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator observes the live render and decides the cap</name>
  <files>.planning/phases/19.1-day-report-render-width-product-neutral-copy-and-config-key-/19.1-LIVE-CHECK-2.md</files>
  <read_first>
    - .planning/phases/.../19.1-LIVE-CHECK-2.md (created by Task 1) — the deploy and precondition
      evidence.
    - .planning/phases/.../19.1-LIVE-CHECK.md `## Observations` and `## Disposition` — the ten-row
      verdict table format and the recording discipline (no verdict inferred, no PASS the operator did
      not state).
    - .planning/phases/19-unified-day-report-getdom-rewrite/19-UAT.md test 5 and its screenshot — the
      baseline every verdict is stated against.
  </read_first>
  <action>
    Present the script below, collect a verdict for every one of items 1-10, and write them into
    `19.1-LIVE-CHECK-2.md`'s `## Observations` as a ten-row table (number, what was checked,
    PASS/FAIL/NOT OBSERVABLE, reason/evidence). Then record the operator's cap decision verbatim, with
    the date, under `## Disposition`.

    Recording discipline, inherited from the first live check and non-negotiable: do not paraphrase a
    verdict, do not infer one item's verdict from another's, do not record PASS for anything the
    operator did not state, and do not upgrade a hedged answer to a clean PASS — record the hedge.
    Item 6 came back "I believe so" last time and was recorded at that strength; this run is its
    re-check.

    If any of items 4, 5, 6 comes back FAIL, STOP: that is a column-alignment regression and a code
    defect, which must be fixed and re-deployed before this checkpoint is re-run. Do not proceed to
    Task 3's document updates on a column-alignment FAIL.

    Do not modify `~/MagicMirror/config/config.js` at any point without a fresh, explicit operator
    authorisation recorded in the file the same way the first run recorded its own.
  </action>
  <what-built>
    Plans 19.1-06 through 19.1-08 landed and are deployed (Task 1 proved the deployed file contains the
    new mechanism and does NOT contain the retired one):
    - Detail-row columns are now CSS grid tracks (`display:inline-grid` with `minmax(0,Nfr)` columns)
      instead of `min-width:Nch` boxes. Column alignment no longer depends on a monospace character
      stream at all — which is what made the old cap impossible to satisfy, since 450px of DejaVu Sans
      Mono at this host's 20px root fits ~37 characters and the column contract needed 54.
    - The region cap was re-derived from YOUR hardware's measured geometry FIRST:
      body right edge x=1020, screen centreline x=540, module gap 30px → 450px → `22.5rem` at the 20px
      root. The old `32.4rem` (648px) was computed from content needs alone and never checked against
      available space, which is why it overshot the centreline by 168px.
    - Each row is `width:100%` of that capped wrapper with every track floored at zero, so a detail row
      can no longer contribute a width larger than the cap even when its content is too long — long
      content wraps inside its own column instead of pushing the block wider.
    - The detail rows are no longer monospace. This reverses the change you accepted at item 7 last
      time.
    - 4 structural probe scenarios guard the mechanism (one of them new), all mutation-proven. None of
      them can see layout. That is what this checkpoint is for.
  </what-built>
  <how-to-verify>
    Look at the deployed mirror (or a photo/screenshot of it) with `dayReportDetail: true` active.

    **A — The MAJOR defect (the one that matters):**
    1. Does the day-report block still extend into the centre column? Compare directly against the
       19-UAT test 5 screenshot. The prediction being tested: the block's LEFT edge should now sit
       about 30px to the RIGHT of the screen's vertical midline, where before it crossed 168px past it.
    2. Do the compact (unexpanded) day rows still look displaced relative to the module's other
       content?
    3. Is any text now WRAPPING onto a second line? Report WHICH rows wrap and how often. A wrap on a
       rare, unusually long row is the accepted, designed behaviour. A wrap on ordinary everyday rows
       means the cap is too tight and should come UP toward the 480px/24rem hard ceiling.

    **B — Column alignment must not have regressed** (all three were PASS in the 19-UAT screenshot and
    in the first live check, so a regression is visible by direct comparison):
    4. Is the em-dash source-attribution column still straight down the block?
    5. Do co-equal hazards still render as peers (e.g. Flash Flood Marginal / Heat Extreme on one day),
       with the second NOT demoted to an `also:` row?
    6. Do `also:` rows still sit indented under their own winner row rather than detached from it?
       **This is the re-check of the one item you hedged last time ("I believe so") — please look at it
       deliberately and give a definite answer, or say it is still uncertain and it will be recorded as
       uncertain again.**

    **C — Two deliberate visual changes to confirm are acceptable:**
    7. The detail rows are back to the host's normal proportional font: the literal `also:` token and
       the `— SPC` / `— WSSI` attributions are no longer monospace. This is the reverse of the change
       you accepted at item 7 last time, and it is possible because grid tracks do not need a uniform
       glyph width. Does it look acceptable — and specifically, do the columns still read as columns?
    8. Vertical rhythm: are there any unexpected BLANK lines between detail rows, or between a detail
       row and the next day's compact line? There should be none. If there are, the row is rendering as
       a block-level grid instead of an inline one and that is a code defect.

    **D — Copy and config (unchanged limbs, confirmed for completeness):**
    9. Does the header still read exactly `SPC Forecast` (your configured value)? The
       `NWS Hazard Outlooks` fallback is not exercised on this mirror because your config sets an
       explicit header — seeing the fallback would require removing the `header:` line, which is
       optional and entirely your choice.
    10. From `mm-out.log`, which Task 1 captured: your config has no misspelled key, so there should be
        NO `unrecognized config key` line. Confirm which you see.

    **THE DECISION.** Reply with one of:
    - `approved` — the block clears the centreline, nothing in B regressed, C is acceptable. `22.5rem`
      stands.
    - `narrower: <value>` — still overlapping. Say roughly how much too wide it looks and Task 3 will
      re-derive from the measured budget, not nudge.
    - `wider: <value>` — ordinary rows are wrapping and the block is unnecessarily cramped. The hard
      ceiling is `24rem` (480px, the centreline itself); anything above that reintroduces the defect.
    - Or describe what you see and I will propose the value.

    Also give the verdict for each of items 1-10 (PASS / FAIL / NOT OBSERVABLE and why) so the record is
    complete. A NOT OBSERVABLE with a stated reason is a valid outcome.
  </how-to-verify>
  <acceptance_criteria>
    - `19.1-LIVE-CHECK-2.md` `## Observations` records a verdict for every one of items 1-10, each
      PASS / FAIL / NOT OBSERVABLE **with its reason** — no item blank, no item recorded PASS on
      inference rather than observation.
    - Item 1's verdict is stated explicitly against the 19-UAT test 5 screenshot, not in the abstract.
    - Item 6's verdict is recorded at the strength the operator actually gave it; if hedged again, the
      hedge is recorded verbatim and NOT upgraded.
    - `## Disposition` records the operator's decision verbatim (`approved` / `narrower: <value>` /
      `wider: <value>` / free text) and the date.
    - If any of items 4, 5, 6, or 8 is FAIL, the plan does not proceed to Task 3's document updates.
    - `19.1-LIVE-CHECK.md` (the first record) is byte-identical.
  </acceptance_criteria>
  <verify>
    <human-check>Operator confirms items 1-10 on the deployed mirror and states the cap decision.</human-check>
    <automated>D=.planning/phases/19.1-day-report-render-width-product-neutral-copy-and-config-key-; grep -qE '^## Disposition' "$D/19.1-LIVE-CHECK-2.md" && test "$(grep -cE '^\| *([1-9]|10) *\|' "$D/19.1-LIVE-CHECK-2.md")" -ge 10 && git diff --exit-code "$D/19.1-LIVE-CHECK.md" && echo "OK ten verdicts and a disposition recorded; first live-check record untouched"</automated>
  </verify>
  <done>`19.1-LIVE-CHECK-2.md` carries a ten-row `## Observations` table with a verdict and reason for
  every item — including a deliberate re-check of the previously hedged item 6 and a verdict on the
  typeface reversal — and a `## Disposition` recording the operator's cap decision verbatim with its
  date.</done>
  <resume-signal>Reply `approved`, or `narrower: <value>`, or `wider: <value>`, or describe what you see — plus the PASS / FAIL / NOT OBSERVABLE verdict for items 1-10.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Apply the tuning outcome and resolve the two pending document values</name>
  <files>MMM-SPCOutlook.js, scripts/probe-payload-resilience.js, .planning/phases/19.1-day-report-render-width-product-neutral-copy-and-config-key-/19.1-UI-SPEC.md, .planning/phases/19-unified-day-report-getdom-rewrite/19-PARITY-CHECKLIST.md</files>
  <read_first>
    - .planning/phases/.../19.1-LIVE-CHECK-2.md `## Observations` and `## Disposition` — these
      determine whether any code changes at all.
    - MMM-SPCOutlook.js — `REGION_CAP_REM` and its available-space-first derivation comment.
    - scripts/probe-payload-resilience.js — `uat05-wrapper-declares-both-region-containment-contracts`,
      whose asserted `maxWidth` literal must equal whatever ships.
    - .planning/phases/.../19.1-UI-SPEC.md "Region Containment" (the `[PENDING LIVE CONFIRMATION — plan
      19.1-09]` marker and the recompute-don't-nudge rule), the Typography section as plan 19.1-08 left
      it, and "Verification Honesty" rows 5-6.
    - .planning/phases/19-unified-day-report-getdom-rewrite/19-PARITY-CHECKLIST.md — the frontmatter
      probe-result lines, row 1, and the existing `## Phase 19.1 Addendum` (line ~366) which lists the
      three RETIRED uat05-* scenario names.
    - .planning/phases/.../19.1-07-SUMMARY.md — the four final `uat05-*` scenario names and the final
      `PROBE RESULT` line.
  </read_first>
  <action>
    **(a) Code, only if the disposition requires it.** If the operator approved `22.5rem`, change no
    code and confirm a zero diff. Otherwise:
    - Apply the new cap by changing `REGION_CAP_REM` and NOTHING else. Do not touch
      `DETAIL_LABEL_FIELD_WIDTH`, `DIMENSION_FIELD_WIDTH` or any track number: the `fr` tracks are
      proportions of the cap, so changing the cap re-sizes every column together and the field
      constants stay the taxonomy-derived quantities they have always been. Cutting
      `DETAIL_LABEL_FIELD_WIDTH` toward ~15 and shrinking detail-row type to ~0.7rem/14px were both
      considered and EXPLICITLY REJECTED by the operator (truncates information off the screen;
      unreadable at mirror viewing distance) — neither is an option here.
    - Update the derivation comment's arithmetic to the new value, keeping the available-space-first
      ORDER: state the budget the new value corresponds to (`value_rem × 20px`) and how it relates to
      the 480px hard ceiling and the 30px module gap. A value above `24` is out of bounds — it is at or
      past the centreline and reintroduces the defect; if the operator asks for one, stop and report it
      rather than shipping it.
    - Update the asserted literal in `uat05-wrapper-declares-both-region-containment-contracts` to the
      shipped value and re-run the full suite to green.
    - Re-deploy and re-run Task 2's checkpoint. Do NOT record an approval for a value that was never
      observed on hardware — that is the exact failure mode this plan exists to prevent.

    **(b) `19.1-UI-SPEC.md`.** In "Region Containment", replace the
    `[PENDING LIVE CONFIRMATION — plan 19.1-09]` marker with the live outcome: the confirmed value, the
    date, the hardware, the observed left-edge position relative to the centreline, and a one-line
    statement of what the operator actually saw. Keep the available-space-first derivation and the
    preserved SUPERSEDED history exactly as plan 19.1-08 left them. Keep "Verification Honesty" rows 5
    and 6 classified `MANUAL ONLY` and add the `19.1-LIVE-CHECK-2.md` citation to each; do NOT
    reclassify either as machine-checkable, now or ever. If item 7 (the typeface reversal) came back
    rejected, do NOT write an approval into the Typography section — record the rejection and stop, the
    same way (a) stops on a column-alignment FAIL. Leave `## Checker Sign-Off` and the `Approval:` line
    byte-identical.

    **(c) `19-PARITY-CHECKLIST.md`.** Update the existing `## Phase 19.1 Addendum` (do not create a
    second one, and do not overwrite the Phase 19 Run A / Run B history anywhere in the file — append):
    - Replace the three retired `uat05-*` scenario names with the four that now exist, taken verbatim
      from `19.1-07-SUMMARY.md`, and note which two were renamed and why (their old names encoded the
      retired `ch`/box mechanism).
    - Record the mechanism change in one dated line: character-advance/monospace alignment replaced by
      CSS grid tracks; the cap re-derived from measured available space; `INERT_MARKUP_ALLOWLIST`
      unchanged.
    - Restate plainly that the region-overlap claim itself remains `MANUAL ONLY`, now citing
      `19.1-LIVE-CHECK-2.md` item 1 as its evidence and noting that the previous entry recorded it
      confirmed FAILING — the addendum must show both the failure and its closure, not silently replace
      one with the other.
    - Add a `phase_19_1_gap_probe_result` line to the frontmatter carrying the final suite count.

    **(d) STATE.md and Deferred Items are the orchestrator's, not this plan's.** If any of items 2-10
    came back NOT OBSERVABLE, list them in the SUMMARY as candidate Deferred Items rows with their
    reasons and let the phase-close step record them.
  </action>
  <acceptance_criteria>
    - If the disposition was `approved`: `git diff MMM-SPCOutlook.js` is empty for this task and
      `grep -c 'REGION_CAP_REM = 22.5' MMM-SPCOutlook.js` returns `1`.
    - If the cap changed: the same numeric value appears in `MMM-SPCOutlook.js`'s `REGION_CAP_REM`, in
      `uat05-wrapper-declares-both-region-containment-contracts`' asserted literal, and in
      `19.1-UI-SPEC.md`'s Region Containment section — a grep for the new `<value>rem` string returns at
      least `1` in each of the three files, and a grep for `22.5` returns `0` outside passages marked as
      history.
    - `grep -c 'DETAIL_LABEL_FIELD_WIDTH = 23' MMM-SPCOutlook.js` returns `1` in every case — the
      rejected field-width reduction was not applied.
    - `node scripts/probe-payload-resilience.js` → `0 failed, 0 skipped`.
    - `grep -c 'PENDING LIVE CONFIRMATION' 19.1-UI-SPEC.md` returns `0`.
    - `19.1-UI-SPEC.md` Verification Honesty rows 5 and 6 still contain `MANUAL ONLY` and now contain
      `19.1-LIVE-CHECK-2.md`.
    - `19.1-UI-SPEC.md`'s `## Checker Sign-Off` checkboxes and `Approval:` line are byte-identical to
      their pre-task state.
    - `grep -c '## Phase 19.1 Addendum' 19-PARITY-CHECKLIST.md` returns exactly `1`.
    - The addendum names all four current `uat05-*` scenarios, and
      `grep -c 'declare-a-ch-floor\|derive-their-boxes-from-the-groups-own-width' 19-PARITY-CHECKLIST.md`
      returns `0` outside the "renamed from" note.
    - `grep -c 'MANUAL ONLY' 19-PARITY-CHECKLIST.md` is greater than or equal to its pre-task count.
    - `19-PARITY-CHECKLIST.md` frontmatter contains `phase_19_1_gap_probe_result`.
    - `git diff --exit-code scripts/probe-lib/module-stubs.js package.json` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>D=.planning/phases/19.1-day-report-render-width-product-neutral-copy-and-config-key-; P=.planning/phases/19-unified-day-report-getdom-rewrite/19-PARITY-CHECKLIST.md; node scripts/probe-payload-resilience.js 2>&1 | tail -1 | grep -q '0 failed, 0 skipped' && test "$(grep -c 'PENDING LIVE CONFIRMATION' "$D/19.1-UI-SPEC.md")" = "0" && test "$(grep -c 'DETAIL_LABEL_FIELD_WIDTH = 23' MMM-SPCOutlook.js)" = "1" && test "$(grep -c '## Phase 19.1 Addendum' "$P")" = "1" && grep -q 'phase_19_1_gap_probe_result' "$P" && grep -q '19.1-LIVE-CHECK-2.md' "$D/19.1-UI-SPEC.md" && git diff --exit-code scripts/probe-lib/module-stubs.js package.json && echo "OK tuning applied, pending values resolved, suite green"</automated>
  </verify>
  <done>The shipped cap is the value the live observation supports and every place that restates it
  agrees; the UI-SPEC's pending marker is resolved with the live outcome while both honesty rows stay
  `MANUAL ONLY`; the parity checklist's Phase 19.1 Addendum names the four scenarios that actually
  exist and shows both the recorded failure and its closure; the suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local working tree → deployed Pi (`rsync` over SSH) | Code crosses a machine boundary. A partial or stale sync produces exactly the 19-08 false negative this plan exists to avoid. |
| `~/MagicMirror/config/config.js` (operator-owned, outside the module's git checkout) | Not this project's file. Modifying it without explicit, recorded authorisation destroys the operator's own configuration. |
| Operator's spoken verdict → the written record | A paraphrased, inferred or upgraded verdict is a fabricated observation. This is the phase's central integrity risk. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19.1G-12 | Spoofing (stale artifact presented as current) | The deployed `MMM-SPCOutlook.js` | mitigate | `sha256sum` byte-identity between local and deployed, PLUS five presence markers, PLUS two ABSENCE markers (`32.4rem`, `DejaVu Sans Mono`) whose zero counts prove the retired mechanism is gone from the deployed file rather than only from the local one. All recorded in `19.1-LIVE-CHECK-2.md` before any observation. |
| T-19.1G-13 | Repudiation (fabricated or inflated observation) | `19.1-LIVE-CHECK-2.md` `## Observations` | mitigate | Verdicts are recorded only as stated: no paraphrase, no inference from a neighbouring item, no upgrading a hedge. Item 6's previous "I believe so" is explicitly re-asked. NOT OBSERVABLE with a reason is a valid recorded outcome. Enforced by the ten-row completeness check and by a blocking human gate that cannot be auto-approved. |
| T-19.1G-14 | Tampering (destruction of operator state) | `~/MagicMirror/config/config.js` and the existing `.bak-19.1` / `.bak-19uat` backups | mitigate | Config is read-only in Task 1 and may only be changed on a fresh, explicitly recorded operator authorisation, exactly as the first live check handled it. New backups use a distinct `-19.1gap` suffix; the pre-19.1 restore points are asserted still present as an acceptance criterion. |
| T-19.1G-15 | Denial of Service (mirror left broken) | `pm2 restart mm` on the Pi | mitigate | The nvm PATH export is mandatory on every remote command touching `node`/`pm2` — without it the restart silently no-ops and the observation is taken against the OLD process. Restart success is confirmed from the post-restart `mm-out.log` module-start line, not assumed from the command's exit code. Backups taken in step 3 are the rollback path. |
| T-19.1G-16 | Elevation (stored XSS regression) | `MMM-SPCOutlook.js` if Task 3 (a) changes code | mitigate | Task 3 (a) may change exactly one numeric constant, `REGION_CAP_REM`. No markup builder, no escaping site and no call site is in scope, so no path exists by which this task can weaken `escapeHtml`-at-the-leaf. Backstopped by the full mutation-proven suite running green as an acceptance criterion. |
| T-19.1G-SC | Tampering | npm/pip/cargo installs | mitigate | Zero dependencies added or changed on either machine. `git diff --exit-code package.json` is an acceptance criterion, no `npm install` is run locally or on the Pi, and no package-manager install task exists in this plan. |
</threat_model>

<verification>
- Local and deployed `sha256sum` of `MMM-SPCOutlook.js` match; five presence markers non-zero and two
  absence markers zero, all recorded.
- `19.1-LIVE-CHECK-2.md` has `## Deploy`, `## Validity Preconditions`, `## Log Evidence`,
  `## Observations` (ten rows) and `## Disposition`.
- `19.1-LIVE-CHECK.md` is byte-identical.
- `node scripts/probe-payload-resilience.js` → `0 failed, 0 skipped`.
- The cap value agrees across `MMM-SPCOutlook.js`, the wrapper probe scenario, and
  `19.1-UI-SPEC.md`.
- `git diff --exit-code scripts/probe-lib/module-stubs.js package.json` exits 0.
</verification>

<success_criteria>
- The operator has stated, on real hardware and against the 19-UAT test 5 screenshot, that the
  detail-mode day report no longer overlaps the centre column — the blocking gap's own truth.
- Both deploy-validity preconditions were positively confirmed before any verdict was taken.
- Item 6's hedge is resolved or re-recorded as a hedge, and the typeface reversal has an explicit
  verdict.
- The shipped cap is a live-supported value; no field width was reduced and no type size was shrunk.
- The UI-SPEC's pending value is resolved, both honesty rows remain `MANUAL ONLY`, and the parity
  checklist shows the failure and its closure.
</success_criteria>

<output>
Create `.planning/phases/19.1-day-report-render-width-product-neutral-copy-and-config-key-/19.1-09-SUMMARY.md`
when done. It MUST record: the ten verdicts with their stated strengths, the operator's disposition
verbatim, the final shipped cap value, the sha256 pair, and any NOT OBSERVABLE items as candidate
Deferred Items rows for the phase-close step.
</output>
