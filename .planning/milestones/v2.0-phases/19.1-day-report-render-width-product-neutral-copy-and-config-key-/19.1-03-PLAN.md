---
phase: 19.1-day-report-render-width-product-neutral-copy-and-config-key
plan: 03
type: execute
wave: 3
depends_on: ["19.1-02"]
files_modified:
  - MMM-SPCOutlook.js
  - scripts/probe-payload-resilience.js
autonomous: true
# No requirement IDs exist for this phase (ROADMAP: `Requirements: TBD`). See `traces`.
requirements: []
traces:
  - "19-UAT.md test 5 (detail-mode render width overlaps the centre column, MAJOR) and its ## Gaps root-cause section"
  - "19.1-UI-SPEC.md Layout Grammar → Detail line — Column contract (AMENDED mechanism)"
  - "19.1-UI-SPEC.md Layout Grammar → Region Containment (NEW, independent contract)"
  - "19.1-RESEARCH.md Pattern 1 (inline-block + min-width), Pattern 2 (wrapper max-width), Pitfall 2 (mechanism change alone is not enough), Pitfall 3 (allowlist), Pitfall 4 (ch resolves against the element's own font)"
  - "19-REVIEW iteration-4 WR-02 (indents derive from the group's own rendered width), WR-04 (truncate-then-pad order, 60-char source bound), BL-02 (co-winner blank dimension field)"

must_haves:
  truths:
    - "No literal space-run padding (3+ consecutive spaces used as column padding) reaches the emitted detail-row markup"
    - "Both detail field columns are realized by CSS boxes with a min-width floor in ch, on existing <span style=...> tag shapes"
    - "The wrapper div carries an explicit max-width alongside its existing textAlign anchor — both, simultaneously"
    - "A dimension or label longer than its field still pushes the source attribution right, never wraps mid-field and never clips"
    - "A co-winner row still reads as a peer: its dimension field is blank and the same width as the winner's"
    - "An also: row's indent still derives from the group's own rendered dimension width, never the nominal constant"
    - "The 60-character truncation bound still counts the entry's own source text — not the dimension prefix, not the padding, not the proximity badge"
  artifacts:
    - path: "MMM-SPCOutlook.js"
      provides: "inline-block/min-width column mechanism for the detail sub-rows, and the wrapper max-width region cap"
      contains: "display:inline-block;min-width:"
    - path: "scripts/probe-payload-resilience.js"
      provides: "the two mechanism-dependent existing scenarios (wr04-*, wr02-*) re-expressed against the new mechanism"
  key_links:
    - from: "MMM-SPCOutlook.js detail row emitter"
      to: "DIMENSION_FIELD_WIDTH / DETAIL_LABEL_FIELD_WIDTH"
      via: "derived ch values interpolated into an inline style, never hardcoded digits"
      pattern: "min-width:\" \\+|min-width:\" *\\+"
    - from: "MMM-SPCOutlook.js also: row emitter"
      to: "dimensionField.length"
      via: "Math.max(DIMENSION_FIELD_WIDTH, dimensionField.length) — one quantity, shared by all three row kinds"
      pattern: "Math\\.max\\(DIMENSION_FIELD_WIDTH, *dimensionField\\.length\\)"
    - from: "MMM-SPCOutlook.js getDom() wrapper"
      to: "wrapper.style.maxWidth"
      via: "set adjacent to the existing wrapper.style.textAlign assignment"
      pattern: "wrapper\\.style\\.maxWidth"
---

<objective>
Fix 19-UAT.md test 5 — the detail-mode day report widening its MagicMirror region and overlapping the
centre column — with the two independent changes 19.1-UI-SPEC.md requires: (a) replace the literal
space-run `padEnd` column padding with `display:inline-block;min-width:Nch` boxes, and (b) cap the
wrapper's contribution to the region's shrink-to-fit width with an explicit `max-width`.

Purpose: `MMM-SPCOutlook.js:644` bakes `2 + DIMENSION_FIELD_WIDTH + 23` monospace characters of
literal padding into a `white-space:pre-wrap` span, which sets a hard min-content width `pre-wrap`
forbids collapsing. MagicMirror's `.region` declares no `width`/`max-width`/`overflow` (verified in
`~/MagicMirror/css/main.css` on the target Pi), so it resolves via CSS2.1 §10.3.7 shrink-to-fit to
its content's max-content width — the region grows to the widest row and visually overlaps its
neighbour. Compact rows are displaced for the same reason: they sit inside a region sized by the
widest DETAIL row.

**Both halves are required.** Landing (a) alone reproduces UAT test 5's own finding ("alignment and
co-winner fixes hold; the defect is block width") because the field-width NUMBERS do not change, so
the row's preferred width is essentially unchanged. 19.1-UI-SPEC.md: "A plan that lands the column
mechanism above without this section is incomplete."

Output: the new column mechanism, the region cap, and the two existing scenarios that assert the old
markup verbatim re-expressed against the new one. The suite must be green at the end of this plan,
with no new scenario counted as the width guard — plan 19.1-04 owns the guards, and plan 19.1-05
owns the live measurement that is the ONLY thing that can prove the overlap is gone.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/buildomator/bm/4.6.0/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/buildomator/bm/4.6.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19.1-day-report-render-width-product-neutral-copy-and-config-key-/19.1-CONTEXT.md
@.planning/phases/19.1-day-report-render-width-product-neutral-copy-and-config-key-/19.1-RESEARCH.md
@.planning/phases/19.1-day-report-render-width-product-neutral-copy-and-config-key-/19.1-UI-SPEC.md
@.planning/phases/19-unified-day-report-getdom-rewrite/19-UAT.md
@.planning/phases/19-unified-day-report-getdom-rewrite/19-SECURITY.md

<interfaces>
<!-- Current mechanism, verbatim from MMM-SPCOutlook.js. These are the exact lines being replaced. -->

:395-400 (the two span builders the detail rows compose)
    const detailSourceAttribution = (source) =>
      "   — " + escapeHtml(String(lookup(SOURCE_SHORT_NAMES, source, source)));
    const detailColoredSpan = (color, content) =>
      "<span style=\"color:#" + validHazardColor(color) +
      ";white-space:pre-wrap;font-family:'DejaVu Sans Mono','Liberation Mono',monospace\">" +
      escapeHtml(content) + "</span>";
    // NOTE: detailColoredSpan ESCAPES its content. It therefore cannot receive pre-built
    // markup. The nesting this plan introduces needs a sibling builder that takes
    // already-escaped HTML — escaping must stay at the LEAF (T-19-17).

:385-391 (the derived field widths — VALUES UNCHANGED by this plan)
    const DIMENSION_FIELD_WIDTH = Math.max(...Object.values(DIMENSION_LABELS).map((l) => l.length)) + 1;  // 13
    const DETAIL_LABEL_FIELD_WIDTH = 23;

:607  const dimensionField = truncateHazardLabel(String(...)).padEnd(DIMENSION_FIELD_WIDTH);
:618  const blankDimensionField = "".padEnd(dimensionField.length);
:645  const paddedFieldContent = (i === 0 ? dimensionField : blankDimensionField) +
                                 labelContent.padEnd(DETAIL_LABEL_FIELD_WIDTH);
:646  html += "<span style=\"white-space:pre-wrap\">" + "  " +
             detailColoredSpan(entry.color, paddedFieldContent) +
             detailSourceAttribution(entry.source) + "</span><br/>";
:665  const alsoIndent = " ".repeat(2 + dimensionField.length + 2);
:669  const alsoLabelFieldWidth = DETAIL_LABEL_FIELD_WIDTH - 2 - "also: ".length;   // 15
:673  const competitorLabel = truncateHazardLabel(entryText(competitor)).padEnd(alsoLabelFieldWidth);
:675  html += "<span style=\"white-space:pre-wrap\">" + alsoIndent + "also: " +
             detailColoredSpan(competitor.color, competitorLabel) +
             detailSourceAttribution(competitor.source) + "</span><br/>";

:1059 wrapper.style.textAlign = "left";     // shipped 19-08 gap closure — MUST keep working

Harness constraint (scripts/probe-lib/module-stubs.js:337-342) — the COMPLETE allowlist:
    /^<br\/>$/, /^<\/(?:span|i)>$/, /^<span style="[^"<>]*">$/, /^<i class="[^"<>]*">$/
An existing span with a longer `style` value still matches `^<span style="[^"<>]*">$`, so this
mechanism needs NO harness change. A `<div>`, `display:grid` container, or `<table>`/`<tr>`/`<td>`
would throw in all 100+ frontend scenarios. Do not introduce any of them (Pitfall 3).

Column arithmetic the mechanism must reproduce (characters from the row's left edge), verified
against the current code for both the mapped and the 29-char unmapped case:
    winner row:  2 (indent) + max(13, len(dimensionField)) + 23  -> em dash at 38 / 54
    also:  row:  (2 + max + 2) + 6 ("also: ") + 15               -> em dash at 38 / 54
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the literal-space column padding with inline-block min-width boxes</name>
  <files>MMM-SPCOutlook.js</files>
  <read_first>
    - MMM-SPCOutlook.js lines 380-430 — `DIMENSION_FIELD_WIDTH`, `DETAIL_LABEL_FIELD_WIDTH`,
      `detailSourceAttribution`, `detailColoredSpan`. Read the WR-04 comment above
      `detailColoredSpan` in full: it records why escaping-only (never truncating) lives there.
    - MMM-SPCOutlook.js lines 590-685 — the whole detail sub-row and `also:` renderer, including
      every CR-03 / WR-04 / WR-02 / BL-02 comment. These comments are the converged output of four
      review iterations; you are changing the mechanism they describe, so each one needs its
      rationale preserved and its mechanism sentence updated.
    - MMM-SPCOutlook.js lines 285-325 — `escapeHtml`, `validHazardColor`, `truncateHazardLabel`,
      `HAZARDS_LABEL_MAX_CHARS`.
    - MMM-SPCOutlook.js lines 485-512 — `convectiveDetailAugment`'s `subLineHtml` emitter (`:490`,
      a `<span style="white-space:pre-wrap">` with a 5-space literal indent). Read it so you can
      confirm it is OUT of scope for this task.
    - scripts/probe-lib/module-stubs.js lines 330-375 — `INERT_MARKUP_ALLOWLIST` and
      `assertInertMarkup`, so you can see exactly which tag shapes are permitted.
    - 19.1-UI-SPEC.md "Detail line — Column contract (AMENDED mechanism, unchanged field widths)" —
      the six-row mechanism table, the harness-compatibility note, the overflow-behavior rules, and
      "Resolving the `also:` indent".
    - 19.1-RESEARCH.md "Pitfall 4: `ch` unit resolving against the wrong font" and "Anti-Patterns to
      Avoid".
  </read_first>
  <action>
    Change only the MECHANISM. `DIMENSION_FIELD_WIDTH` (derived, 13) and `DETAIL_LABEL_FIELD_WIDTH`
    (23) keep their current derivations and values — do not touch those two declarations.

    **1. Add a field-box builder** next to `detailColoredSpan` (~:400). Name it `detailFieldBox`.
    Signature `(widthCh, innerHtml)`. It returns
    `<span style="display:inline-block;min-width:Nch">` + innerHtml + `</span>`, where `N` is
    produced by a numeric sanitiser — `String(Math.max(0, Math.trunc(Number(widthCh)) || 0))` — so
    only digits can ever reach the `style` attribute even though the width is derived from a
    payload-influenced string length. `innerHtml` is ALREADY-ESCAPED markup or text: the builder must
    NOT call `escapeHtml` on it (escaping stays at the leaf). Write a comment stating both facts and
    naming T-19-17 / T-19-12 as the reason the numeric sanitiser exists.

    **2. Add an unescaped-content sibling of `detailColoredSpan`.** `detailColoredSpan` escapes its
    content, so it cannot receive the nested boxes. Add `detailColoredWrapper(color, innerHtml)`
    emitting the identical opening span (`color:#` + `validHazardColor(color)` +
    `;white-space:pre-wrap;font-family:'DejaVu Sans Mono','Liberation Mono',monospace`) and closing
    `</span>`, but concatenating `innerHtml` verbatim. Express `detailColoredSpan` in terms of it
    (`detailColoredSpan = (color, content) => detailColoredWrapper(color, escapeHtml(content))`) so
    there is ONE definition of the opening span and the two cannot drift — and so every existing
    probe assertion on the exact style string stays green. Keep `detailColoredSpan` in place and in
    use for the other call sites that pass plain text.

    **3. Row-level monospace.** Add `font-family:'DejaVu Sans Mono','Liberation Mono',monospace` to
    the row-level `<span style="white-space:pre-wrap">` of BOTH detail row emitters (`:646` winner,
    `:675` also). This is a DELIBERATE, documented deviation from 19.1-UI-SPEC.md's Typography
    sentence "never on an ancestor": the `also:` indent spacer and the literal `"  "` / `"also: "` /
    `"   — "` runs sit OUTSIDE any colored span, so under the old mechanism they rendered in the
    host's proportional font while the padded fields rendered monospace. `ch` resolves against the
    element's own font (Pitfall 4), so an uncolored spacer with no font-family would compute its
    `ch` against Roboto Condensed and the columns would not align. Declaring the monospace family
    once at the row level makes the whole row one monospace character stream — which is exactly what
    the suite's own `wr02-*` scenario already ASSERTS in its comment ("the column contract is a
    property of the CHARACTER stream (every sub-row is white-space:pre-wrap monospace)"). Record this
    deviation and its justification in a comment AND in the plan SUMMARY, and note the one visible
    consequence for plan 19.1-05's live check: the literal `also:` text and the `— SPC` attribution
    become monospace where they were proportional.

    **4. Winner / co-winner rows** (replacing `:607`, `:618`, `:645`-`:648`):
    - `dimensionField` keeps `truncateHazardLabel(String(...))` but DROPS `.padEnd(...)` — the
      unmapped-dimension own-account truncation (WR-04) stays exactly as it is; only the padding goes.
    - Introduce ONE derived quantity, `const dimensionFieldWidthCh = Math.max(DIMENSION_FIELD_WIDTH,
      dimensionField.length);`, and use it for all three places that need the group's dimension
      width: the winner's dimension box, the co-winner's blank box, and the `also:` spacer. This is
      iteration-4 WR-02's rule restated for the new mechanism — "both row kinds derive the indent
      from one quantity, so they cannot disagree" — and the explicit `Math.max` is now REQUIRED
      because `dimensionField` is no longer pre-padded, so its raw `.length` can be BELOW the nominal
      field width (under the old mechanism `padEnd` guaranteed it never was). Removing the `Math.max`
      would silently narrow the column for every short dimension name.
    - Emit: row span (with the monospace family) + `"  "` +
      `detailColoredWrapper(entry.color, detailFieldBox(dimensionFieldWidthCh, escapeHtml(i === 0 ? dimensionField : "")) + detailFieldBox(DETAIL_LABEL_FIELD_WIDTH, escapeHtml(labelContent)))`
      + `detailSourceAttribution(entry.source)` + `</span><br/>`.
    - BL-02 survives as an EMPTY box of the same width (`escapeHtml("")`), not a run of spaces — an
      empty inline-block of the same floor width reads as blank exactly as the spaces did. Do not
      emit a `&nbsp;`, a `.repeat()` string, or a zero-width space to "fill" it.
    - WR-04's order becomes truncate-then-BOX: `labelContent` is still
      `truncateHazardLabel(entryText(entry)) + augment.labelSuffix`, computed BEFORE it reaches the
      box, so the 60-character bound still counts the entry's own source text and never the dimension
      prefix, the box, or the module-authored proximity badge. Do not move the truncation.
    - The two nested boxes carry NO `color` and NO `font-family` of their own — they inherit both
      from `detailColoredWrapper` (D-02: dimension name and label share one color; Pitfall 4: `ch`
      resolves against the inherited monospace family, which is declared on both the row span and
      the colored wrapper).

    **5. `also:` rows** (replacing `:665`, `:673`, `:675`-`:677`):
    - Replace `alsoIndent = " ".repeat(2 + dimensionField.length + 2)` with an EMPTY, UNCOLORED
      spacer box: `detailFieldBox(2 + dimensionFieldWidthCh + 2, "")`. Same formula, same single
      derived quantity; only the unit changes from "N literal spaces" to "N ch". `ch` is the advance
      width of the `0` glyph in the element's own font and every glyph in a monospace font shares
      that advance, so this reproduces the same visual width (19.1-UI-SPEC.md, "Resolving the `also:`
      indent"). 19.1-RESEARCH.md Open Question 2 proposed accepting a rare-overflow misalignment here
      instead; the approved UI-SPEC supersedes that with the `Math.max` form, which does not drift —
      implement the UI-SPEC form.
    - `alsoLabelFieldWidth` keeps its derivation (`DETAIL_LABEL_FIELD_WIDTH - 2 - "also: ".length`,
      = 15) unchanged; `competitorLabel` keeps `truncateHazardLabel(entryText(competitor))` and DROPS
      `.padEnd(alsoLabelFieldWidth)` — the box supplies the width.
    - Keep the literal `"also: "` text OUTSIDE the colored span (unchanged — 19-SECURITY.md T-19-17
      notes this placement, and `rpt03-also-line-...` asserts it).
    - Emit: row span (with the monospace family) + spacer box + `"also: "` +
      `detailColoredWrapper(competitor.color, detailFieldBox(alsoLabelFieldWidth, escapeHtml(competitorLabel)))`
      + `detailSourceAttribution(competitor.source)` + `</span><br/>`.

    **6. Keep `white-space:pre-wrap`** on the row spans. The `"  "` indent, the `"also: "` literal
    and `detailSourceAttribution`'s three literal spaces still need their spaces preserved, and
    `pre-wrap` (unlike `pre`) still permits the row-level wrap 19.1-UI-SPEC.md's Detail-Mode Density
    amendment accepts for a genuinely-too-wide row.

    **Out of scope for this task — do not touch:** `convectiveDetailAugment`'s `subLineHtml` emitter
    (`:490`, a 5-space literal indent that contributes no meaningful width and is not part of the
    column contract); the compact-line renderer (it is displaced by the REGION's width, not by its
    own padding — Task 2 fixes its displacement); `getStyles()` (`:222`) — no stylesheet, 19.1-UI-SPEC
    closes that question with "no"; `DIMENSION_FIELD_WIDTH`/`DETAIL_LABEL_FIELD_WIDTH` values.

    **Expected interim state:** the suite goes RED on exactly two scenarios
    (`wr04-detail-label-truncation-counts-source-characters-not-padding` and
    `wr02-also-rows-column-with-the-winner-row-under-an-unmapped-dimension`), which assert the old
    padded character stream verbatim. Task 3 repairs them. Any OTHER failing scenario is collateral
    damage from this task and must be fixed here, not accepted.
  </action>
  <acceptance_criteria>
    - Source assertion: `grep -vE '^\s*//' MMM-SPCOutlook.js | grep -c 'padEnd(DETAIL_LABEL_FIELD_WIDTH)'`
      returns `0`; same for `padEnd(DIMENSION_FIELD_WIDTH)`, `padEnd(dimensionField.length)`,
      `padEnd(alsoLabelFieldWidth)`, and `" ".repeat(`.
    - Source assertion: `grep -vE '^\s*//' MMM-SPCOutlook.js | grep -c 'display:inline-block;min-width:'`
      returns exactly `1` (one builder, not restated per call site).
    - Source assertion: `grep -vE '^\s*//' MMM-SPCOutlook.js | grep -c 'Math.max(DIMENSION_FIELD_WIDTH, dimensionField.length)'`
      returns exactly `1` (one derived quantity shared by all three row kinds).
    - Source assertion: `grep -cE '<div|display:grid|<table|<tr|<td' MMM-SPCOutlook.js` returns `0`
      except for the single pre-existing `document.createElement("div")` call — assert with
      `grep -vE '^\s*//' MMM-SPCOutlook.js | grep -cE 'display:grid|<table|<tr>|<td>'` returning `0`.
    - Behavior assertion (no space-run padding survives): for a mapped-dimension detail render, the
      markup contains NO run of three or more consecutive spaces inside any field's text content.
      Specifically, `/High Winds {3,}/` does not match (it DID match before this task — the control
      assertion at the old scripts/probe-payload-resilience.js:13885 asserted exactly
      `"Wind         High Winds             "`).
    - Behavior assertion (boxes present): the markup for a mapped-dimension winner row contains both
      `min-width:13ch` and `min-width:23ch`.
    - Behavior assertion (unmapped overrun widens the box): with a 29-character unmapped dimension,
      the markup contains `min-width:29ch` (the `Math.max` branch) and the full 29-character
      dimension string still renders.
    - Behavior assertion (also: spacer formula): for a mapped dimension the markup contains
      `min-width:17ch` (2 + 13 + 2); for the 29-character unmapped dimension it contains
      `min-width:33ch` (2 + 29 + 2).
    - Behavior assertion (co-winner blank): with two co-winners on one dimension, the markup contains
      exactly two `min-width:13ch` boxes whose content differs — the first carrying the dimension
      label, the second empty — and no `&nbsp;`.
    - Behavior assertion (WR-04 bound unmoved): a 200-character label still renders exactly 60
      source characters plus `…`, and `A`×61 does not appear.
    - Behavior assertion (escaping travels): a hostile label (`<img src=x onerror="alert(1)">`)
      renders only in escaped form inside the box, a hostile color falls back to `aaaaaa`, and
      `renderDom`'s `assertInertMarkup` does not throw.
    - Behavior assertion (monospace colocation): every emitted `min-width:` box is nested inside an
      element whose style declares `font-family:'DejaVu Sans Mono'` — assert by checking that the
      row span's style contains the font-family declaration.
    - Suite state: `node scripts/probe-payload-resilience.js` fails on EXACTLY the two named
      scenarios and nothing else.
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -vE '^\s*//' MMM-SPCOutlook.js | grep -cE 'padEnd\(DETAIL_LABEL_FIELD_WIDTH\)|padEnd\(DIMENSION_FIELD_WIDTH\)|padEnd\(dimensionField\.length\)|padEnd\(alsoLabelFieldWidth\)|" "\.repeat\(')" = "0" && test "$(grep -vE '^\s*//' MMM-SPCOutlook.js | grep -c 'display:inline-block;min-width:')" = "1" && test "$(grep -vE '^\s*//' MMM-SPCOutlook.js | grep -c 'Math.max(DIMENSION_FIELD_WIDTH, dimensionField.length)')" = "1" && test "$(grep -vE '^\s*//' MMM-SPCOutlook.js | grep -cE 'display:grid|<table|<tr>|<td>')" = "0" && node scripts/probe-payload-resilience.js 2>&1 | grep '^FAIL ' | sed 's/:.*//' | sed 's/^FAIL //' | sort | tr '\n' ' ' | grep -qx 'wr02-also-rows-column-with-the-winner-row-under-an-unmapped-dimension wr04-detail-label-truncation-counts-source-characters-not-padding ' && echo "OK mechanism swapped; exactly the two mechanism-dependent scenarios are RED"</automated>
  </verify>
  <done>No `padEnd`/`repeat` column padding remains in the detail renderers; one `detailFieldBox`
  builder emits digit-sanitised `min-width:Nch` boxes on existing span tag shapes; one
  `Math.max(DIMENSION_FIELD_WIDTH, dimensionField.length)` quantity drives the winner box, the
  co-winner blank box and the `also:` spacer; WR-04's truncate-then-box order, BL-02's blank peer
  field and every escaping obligation are intact; and exactly the two mechanism-dependent scenarios
  are RED pending Task 3.</done>
</task>

<task type="auto">
  <name>Task 2: Cap the wrapper's width so the region stops shrink-to-fitting to its widest row</name>
  <files>MMM-SPCOutlook.js</files>
  <read_first>
    - MMM-SPCOutlook.js lines 1046-1070 — the wrapper creation, the full 19-08 comment block above
      `wrapper.style.textAlign = "left"`, and the loading/error branches immediately below it.
    - 19.1-UI-SPEC.md "Region Containment (NEW — a separate, independent contract from column
      alignment)" — the mechanism, the unit rationale (`rem`, not `px` and not `ch`), the full
      derivation of `32.4rem`, and its `[ASSUMED — must be live-tuned]` tag.
    - 19.1-RESEARCH.md Pattern 2 and Pitfall 2 (why the mechanism change alone is not enough).
    - 19.1-RESEARCH.md Open Question 1 — the deployed region's available width was never measured
      and cannot be without the real hardware.
  </read_first>
  <action>
    Immediately after `wrapper.style.textAlign = "left";` (`:1059`), add
    `wrapper.style.maxWidth = "32.4rem";`.

    Do NOT remove, weaken, reorder or merge the `textAlign` anchor. It is a shipped 19-08 gap closure
    for right-hand MagicMirror regions and it solves a different problem (indentation direction);
    `max-width` solves shrink-to-fit sizing. Both are required simultaneously, and the existing 19-08
    comment block above `textAlign` stays as written.

    Write a comment above the new line recording, in this file's voice:
    - the mechanism: MagicMirror's `.region` declares no `width`/`max-width`/`overflow`
      (`~/MagicMirror/css/main.css`, MagicMirror 2.37.0, verified on the target Pi), so a
      `position:absolute` region with `width:auto` resolves via CSS2.1 §10.3.7 shrink-to-fit to its
      content's max-content width. The wrapper is the only element this module controls that sits
      directly inside the region, so capping its contribution is the only lever available.
    - the derivation, written out so the number is traceable rather than magic:
      `2 (indent) + DIMENSION_FIELD_WIDTH (13) + DETAIL_LABEL_FIELD_WIDTH (23) + 3 (attribution gap)
      + 1 (em dash) + 1 (space) + 11 (longest SOURCE_SHORT_NAMES value, "WPC Hazards") ≈ 54
      monospace characters; 54 × ~0.6em advance = 32.4em`, expressed as `rem` against MagicMirror's
      own `--font-size: 20px` root (≈650px today).
    - why `rem` and not `px` (a px literal would silently stop matching the text it is sized to
      contain if the host's `--font-size` changed) and not `ch` (`ch` resolves against the element's
      OWN font, and this wrapper contains both monospace detail rows and proportional compact-line /
      band content, so a `ch` cap would resolve ambiguously; `rem` resolves against the root
      regardless of which child is widest).
    - that the value is `[ASSUMED]` pending the live measurement in plan 19.1-05, and that if the
      field widths are ever changed the cap must be RECOMPUTED from this same formula rather than
      nudged independently, so the two cannot drift.
    - that this cap applies to ALL content this module renders, not only detail rows: the compact
      line's existing "let it wrap naturally" policy is not contradicted, it becomes actually
      enforceable (previously there was no width to wrap within, which is why compact rows were
      displaced too), and the advisory/window band wraps under the same cap.

    Do not add any other style property (no `overflow`, no `width`, no `boxSizing`). Do not touch
    `getStyles()`.
  </action>
  <acceptance_criteria>
    - Source assertion: `grep -vE '^\s*//' MMM-SPCOutlook.js | grep -c 'wrapper.style.maxWidth'`
      returns exactly `1`.
    - Source assertion: `grep -vE '^\s*//' MMM-SPCOutlook.js | grep -c 'wrapper.style.textAlign'`
      returns exactly `1` — the 19-08 anchor is still present and was not replaced.
    - Source assertion: `grep -vE '^\s*//' MMM-SPCOutlook.js | grep -c '32.4rem'` returns exactly `1`
      — one literal, and it is a `rem` value, not `px`, not `ch`, not a bare number.
    - Source assertion: the two assignments are adjacent — `grep -n 'wrapper.style' MMM-SPCOutlook.js`
      shows `textAlign` and `maxWidth` within 20 lines of each other (the comment block sits between).
    - Source assertion: `grep -vE '^\s*//' MMM-SPCOutlook.js | grep -cE 'wrapper.style.(overflow|width|boxSizing)'`
      returns `0`.
    - Behavior assertion: the stub wrapper object's `style` carries both properties after a render —
      `style.textAlign === "left"` and `style.maxWidth === "32.4rem"`. NOTE: `renderDom` currently
      returns only `innerHTML`, so this is asserted here by constructing the render through
      `frontend.getDom.call(ctx)` directly; plan 19.1-04 makes it a first-class, mutation-proven
      probe scenario via a harness accessor.
    - **NOT machine-checkable, and must not be claimed as such:** that `32.4rem` is wide enough for
      the common case and narrow enough to clear the deployed centre column. Neither `jsdom` nor
      `linkedom` implements CSS layout; the harness has no layout engine at all. This is verified
      EXCLUSIVELY by the live render in plan 19.1-05, and the SUMMARY must record the value as
      `[ASSUMED — pending live measurement]`.
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -vE '^\s*//' MMM-SPCOutlook.js | grep -c 'wrapper.style.maxWidth')" = "1" && test "$(grep -vE '^\s*//' MMM-SPCOutlook.js | grep -c 'wrapper.style.textAlign')" = "1" && test "$(grep -vE '^\s*//' MMM-SPCOutlook.js | grep -c '32.4rem')" = "1" && test "$(grep -vE '^\s*//' MMM-SPCOutlook.js | grep -cE 'wrapper\.style\.(overflow|width|boxSizing)')" = "0" && node -e 'const s=require("./scripts/probe-lib/module-stubs.js");const f=s.loadFrontendModule();const ctx=Object.create(f);ctx.config={lat:1,lon:2};ctx.spcrisk=null;ctx.updateDom=()=>{};const w=f.getDom.call(ctx);if(w.style.textAlign!=="left")throw new Error("textAlign anchor lost: "+JSON.stringify(w.style));if(w.style.maxWidth!=="32.4rem")throw new Error("maxWidth not set: "+JSON.stringify(w.style));console.log("OK both wrapper style contracts set simultaneously");'</automated>
  </verify>
  <done>`wrapper.style.maxWidth = "32.4rem"` is set alongside the surviving
  `wrapper.style.textAlign = "left"` anchor, with the derivation and the `rem`-over-`px`/`ch`
  rationale recorded in a comment, and the SUMMARY records the value as `[ASSUMED — pending the live
  measurement in plan 19.1-05]`.</done>
</task>

<task type="auto">
  <name>Task 3: Re-express the two mechanism-dependent scenarios and restore the suite to green</name>
  <files>scripts/probe-payload-resilience.js</files>
  <read_first>
    - scripts/probe-payload-resilience.js lines 13814-13910
      (`wr04-detail-label-truncation-counts-source-characters-not-padding`) — note in particular the
      two assertions that pin the OLD mechanism verbatim: line ~13868
      `rendered.includes("Wind         " + "A".repeat(60) + "…")` and line ~13885
      `shortRendered.includes("Wind         High Winds             ")`.
    - scripts/probe-payload-resilience.js lines 13911-14012
      (`wr02-also-rows-column-with-the-winner-row-under-an-unmapped-dimension`) — its `plainRows` /
      `emDashColumn` helpers, its needles (`unmappedDimension + "Winner Hazard"`, and
      `"Wind         Winner Hazard"` in the control), and its `// Mutation to prove RED:` comment.
    - MMM-SPCOutlook.js lines 590-685 as edited by Task 1 — the emitted markup you are now asserting
      against.
    - .planning/STATE.md "Decisions" → Phase 15 D-10 mutation-proof rule.
  </read_first>
  <action>
    Repair exactly these two scenarios. Do NOT weaken either invariant and do NOT delete either
    scenario — each pins a converged review finding that must survive the mechanism swap.

    **`wr04-detail-label-truncation-counts-source-characters-not-padding`.** Its real invariant is
    WR-04's: the 60-character bound counts the entry's own source text, the same string is cut at the
    same point on the compact header and on both detail row kinds (the `truncatedRows !== 3` count),
    the `…` lands at the END of the label and never mid-field, and the module-authored proximity badge
    survives intact after a truncated label. Every one of those survives the mechanism change
    unaltered — keep them all, including the count of 3.
    Replace only the two padding-literal assertions:
    - the "`…` is the end of the label, never mid-column" assertion (`"Wind         " + A×60 + "…"`)
      becomes: the dimension field's own box renders `Wind` with nothing appended inside the box, and
      the truncated label with its `…` renders inside the FOLLOWING box — assert the ordered
      substring sequence `min-width:13ch">Wind</span>` then `min-width:23ch">` + `A`×60 + `…`.
    - the control "a short label must still be padded to the label field width"
      (`"Wind         High Winds             "`) becomes its NEW-mechanism equivalent plus a
      regression assertion for the defect this phase fixes: assert the markup contains
      `min-width:23ch">High Winds</span>` AND assert that `/High Winds {3,}/` does NOT match the
      markup (no space-run padding reaches the stream). Keep the existing "a short label must not be
      truncated at all" assertion as-is.
    Update the scenario's `// Mutation to prove RED:` comment to name the new line:
    re-adding `.padEnd(DETAIL_LABEL_FIELD_WIDTH)` to `labelContent`.

    **`wr02-also-rows-column-with-the-winner-row-under-an-unmapped-dimension`.** Its real invariant is
    iteration-4 WR-02's: the `also:` row's indent derives from the group's OWN rendered dimension
    width, so an unmapped dimension that overruns the field cannot detach its competitor rows from the
    winner above them. The old scenario measured that as a character-index equality of the two em
    dashes. Under the new mechanism the visual width lives in the `ch` declarations, not in the
    character stream, so re-express the SAME invariant against the declarations:
    - Extract the winner row's dimension-box `min-width` value and the `also:` row's spacer
      `min-width` value from the markup (regex the `min-width:(\d+)ch` occurrences per row, splitting
      rows on `<br/>` as the existing `plainRows` helper already does).
    - Assert `alsoSpacerCh === winnerDimensionCh + 4` (the `2 + … + 2` formula) for BOTH the
      29-character unmapped dimension (expect 33 and 29) and the mapped control (expect 17 and 13).
      Assert the concrete expected numbers, not only the relationship, so a mutation that scales both
      values together still goes RED.
    - Keep the existing vacuity guard (both row kinds rendered, the dimension string survived whole)
      and the mapped-dimension control. Add one new assertion that the winner's dimension box for the
      unmapped case is `min-width:29ch` specifically — i.e. the `Math.max` branch was taken — because
      that is the exact term a regression would delete.
    Update the `// Mutation to prove RED:` comment to name the new line: replacing
    `Math.max(DIMENSION_FIELD_WIDTH, dimensionField.length)` with the bare `DIMENSION_FIELD_WIDTH`.

    Then **mutation-prove both repaired scenarios** (Phase 15 D-10):
    1. Re-add `.padEnd(DETAIL_LABEL_FIELD_WIDTH)` to `labelContent`; run the suite; confirm
       `wr04-…` goes RED with a message that shows the padded string; restore; confirm green.
    2. Replace `Math.max(DIMENSION_FIELD_WIDTH, dimensionField.length)` with `DIMENSION_FIELD_WIDTH`;
       run the suite; confirm `wr02-…` goes RED naming the two `ch` values it found and the two it
       expected; restore; confirm green.
    A repaired scenario that stays green under its own mutation is a fixture defect. If mutation 2
    produces a wider blast radius than its own scenario, record every failing scenario name in the
    SUMMARY (the Phase 18-07 precedent: a wider radius is acceptable and disclosed, a zero radius is
    not).

    Do NOT add new scenarios in this task — plan 19.1-04 owns the width guards. This task's only job
    is that no scenario asserts a mechanism the code no longer uses.
  </action>
  <acceptance_criteria>
    - `node scripts/probe-payload-resilience.js` prints `0 failed, 0 skipped` with the same total as
      at the end of plan 19.1-02 (no scenario added, none removed). Record both numbers in the SUMMARY.
    - `grep -c 'wr04-detail-label-truncation-counts-source-characters-not-padding' scripts/probe-payload-resilience.js`
      and the same for `wr02-also-rows-column-with-the-winner-row-under-an-unmapped-dimension` each
      return at least `1` — neither scenario was deleted or renamed.
    - Source assertion: `grep -c 'Wind         ' scripts/probe-payload-resilience.js` returns `0` —
      no assertion in the suite depends on literal space-run padding any more.
    - `wr04-…` still asserts `truncatedRows === 3`, still asserts `A`×61 is absent, and still asserts
      the proximity badge survives after a truncated label.
    - `wr02-…` asserts the concrete pairs (29, 33) for the unmapped case and (13, 17) for the mapped
      control, and retains its vacuity guard and its control.
    - Mutation proof 1: with `.padEnd(DETAIL_LABEL_FIELD_WIDTH)` restored, the suite reports
      `FAIL wr04-detail-label-truncation-counts-source-characters-not-padding` and the diagnostic
      shows the space-run. Restored → green at the same count.
    - Mutation proof 2: with `Math.max(...)` reduced to `DIMENSION_FIELD_WIDTH`, the suite reports
      `FAIL wr02-also-rows-column-with-the-winner-row-under-an-unmapped-dimension` and the diagnostic
      names the found and expected `ch` values. Restored → green at the same count. Any additional
      scenarios that fail under this mutation are listed in the SUMMARY.
    - Both scenarios' `// Mutation to prove RED:` comments name the NEW lines, not the retired ones.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/probe-payload-resilience.js 2>&1 | tee /tmp/probe-19.1-03.log | tail -1 | grep -q '0 failed, 0 skipped' && grep -q '^PASS wr04-detail-label-truncation-counts-source-characters-not-padding' /tmp/probe-19.1-03.log && grep -q '^PASS wr02-also-rows-column-with-the-winner-row-under-an-unmapped-dimension' /tmp/probe-19.1-03.log && test "$(grep -c 'Wind         ' scripts/probe-payload-resilience.js)" = "0" && echo "OK suite green, both scenarios repaired, no space-run assertions remain"</automated>
  </verify>
  <done>Both mechanism-dependent scenarios assert the new mechanism while preserving their original
  invariants (WR-04's source-character bound and three-row agreement; WR-02's group-own-width indent
  derivation), the suite is green at the plan-19.1-02 count, no assertion anywhere depends on literal
  space-run padding, and both repairs are individually mutation-proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| payload `hazards[].text`/`label`/`color`/`dimension` → nested field boxes → `wrapper.innerHTML` | Remote NWS/MapServer-derived strings cross into markup here; the nesting this plan adds puts a new element BETWEEN the escaping site and the output |
| payload-derived string LENGTH → `min-width:Nch` inside a `style` attribute | A numeric quantity derived from remote content now participates in an attribute value — a surface that did not exist before this plan |
| module wrapper → MagicMirror `.region` shrink-to-fit | The region has no width/overflow rule of its own; the wrapper's preferred width IS the region's width |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19.1-09 | Tampering | remote label/dimension reaching the nested field boxes (the T-19-17 surface, now one element deeper) | mitigate | Escaping stays at the LEAF: `detailFieldBox` receives already-escaped content and must never call `escapeHtml` itself; `detailColoredSpan` is re-expressed as `detailColoredWrapper(color, escapeHtml(content))` so there is one definition of the opening span and no call site loses its escape. Task 1 acceptance criteria assert a hostile label renders only in escaped form and that `renderDom`'s `assertInertMarkup` does not throw; the existing hostile-input scenario (`scripts/probe-payload-resilience.js:~13700`, every branch at once) re-runs over the new nesting as part of the green suite. |
| T-19.1-10 | Tampering | payload-derived length reaching a `style` attribute (`min-width:Nch`) | mitigate | `detailFieldBox` sanitises numerically — `String(Math.max(0, Math.trunc(Number(widthCh)) || 0))` — so only digits can reach the attribute regardless of what produced the number. This is the same posture `validHazardColor` (T-19-12) enforces for the one other value that reaches a `style` attribute. Pinned by the Task 1 criterion that the builder appears exactly once (no unsanitised restatement) and by the `min-width:29ch` / `min-width:33ch` assertions in Task 3. |
| T-19.1-11 | Tampering | a new tag shape defeating `assertInertMarkup` across the whole suite | mitigate | Only existing `<span style="...">` shapes are emitted. No `<div>`, no `display:grid`, no `<table>`/`<tr>`/`<td>` (Pitfall 3 — any of them throws in all 100+ frontend scenarios). Asserted by a source grep returning `0` and by the suite running green. |
| T-19.1-12 | Tampering | `validHazardColor` bypassed by the new wrapper | mitigate | `detailColoredWrapper` is the single site that builds the colored opening span and it calls `validHazardColor(color)` exactly as `detailColoredSpan` did; the nested boxes declare no `color` at all and so have no colour-injection surface. The existing hostile-colour assertion (`color:#aaaaaa` present) is part of the green suite. |
| T-19.1-13 | DoS | an unmapped dimension or label producing an absurd `min-width` and a runaway box | mitigate | Both strings pass `truncateHazardLabel` (60 chars, `HAZARDS_LABEL_MAX_CHARS`, T-19-13) BEFORE their length is measured, so the derived `ch` value is bounded by 60 + 4. `min-width` is a floor, not a hard `width`, so overflow pushes siblings right rather than painting over them (19.1-RESEARCH.md Anti-Patterns: a hard `width` would render overflow behind the next sibling box). |
| T-19.1-14 | Repudiation | claiming the overlap is fixed on evidence that cannot show it | mitigate | Neither `jsdom` nor `linkedom` implements CSS layout and this harness has no layout engine, so NO automated check in this plan proves the region stopped overlapping. Task 2's acceptance criteria say so explicitly, the `32.4rem` value is recorded `[ASSUMED]`, and plan 19.1-05 carries the live measurement as a required, non-autonomous task. Asserting otherwise would repeat the exact failure mode that let this defect ship green past 182 scenarios and a phase verifier. |
| T-19.1-SC | Tampering | npm/pip/cargo installs | mitigate | Zero installs; the mechanism is inline CSS on existing spans precisely to avoid a harness dependency (19.1-RESEARCH.md explicitly rejects adding `linkedom`/`jsdom` — they would still not answer the layout question). Gate: `git diff --name-only` must not list `package.json` or `pnpm-lock.yaml`. |
</threat_model>

<verification>
- `node scripts/probe-payload-resilience.js` → `0 failed, 0 skipped`, total unchanged from plan 19.1-02.
- `grep -vE '^\s*//' MMM-SPCOutlook.js | grep -cE 'padEnd\(DETAIL_LABEL_FIELD_WIDTH\)|padEnd\(DIMENSION_FIELD_WIDTH\)|" "\.repeat\('` → `0`.
- `grep -vE '^\s*//' MMM-SPCOutlook.js | grep -c 'wrapper.style.maxWidth'` → `1` AND
  `grep -vE '^\s*//' MMM-SPCOutlook.js | grep -c 'wrapper.style.textAlign'` → `1`. **Both halves of
  the width fix must be present; neither may be reported done without the other.**
- `grep -c 'Wind         ' scripts/probe-payload-resilience.js` → `0`.
- `git diff --name-only` for the whole plan lists exactly `MMM-SPCOutlook.js` and
  `scripts/probe-payload-resilience.js`. No `node_helper.js` (D-01), no `package.json`, no
  `pnpm-lock.yaml`, no `scripts/probe-lib/module-stubs.js` (the allowlist is NOT edited).
- Four mutation proofs recorded in the SUMMARY (two in Task 3; the Task 1 and Task 2 source-absence
  gates are grep-level, not mutation-level, and plan 19.1-04 converts them into mutation-proven
  scenarios).
- SUMMARY records the `32.4rem` cap as `[ASSUMED — pending live measurement]` and records the
  row-level monospace font-family deviation from 19.1-UI-SPEC's Typography sentence, with its
  Pitfall-4 justification and its one visible consequence.

**Explicitly NOT verified by this plan, and must not be claimed:** that the region no longer overlaps
the centre column, and that `32.4rem` is the right cap. Both are `MANUAL ONLY` per 19.1-UI-SPEC.md's
"Verification Honesty" table and are owned by plan 19.1-05.
</verification>

<success_criteria>
- No literal space-run column padding reaches the emitted markup.
- Exactly one `detailFieldBox` builder emits digit-sanitised `min-width:Nch` on existing span shapes.
- One `Math.max(DIMENSION_FIELD_WIDTH, dimensionField.length)` quantity drives the winner box, the
  co-winner blank box and the `also:` spacer (`+4`).
- `wrapper.style.maxWidth = "32.4rem"` AND `wrapper.style.textAlign = "left"` are both set.
- WR-02, WR-04 and BL-02 all still hold, re-expressed against the new mechanism and mutation-proven.
- Suite green at the plan-19.1-02 count, with no scenario asserting a retired mechanism.
</success_criteria>

<output>
Create `.planning/phases/19.1-day-report-render-width-product-neutral-copy-and-config-key-/19.1-03-SUMMARY.md` when done.
</output>
