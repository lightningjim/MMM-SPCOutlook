---
status: complete
phase: 19.1-day-report-render-width-product-neutral-copy-and-config-key
source: 19.1-01-SUMMARY.md, 19.1-02-SUMMARY.md, 19.1-03-SUMMARY.md, 19.1-04-SUMMARY.md, 19.1-05-SUMMARY.md, 19.1-06-SUMMARY.md, 19.1-07-SUMMARY.md, 19.1-08-SUMMARY.md, 19.1-09-SUMMARY.md
started: 2026-09-12T00:00:00Z
updated: 2026-09-12T12:00:00Z
---

## Evidence Provenance

This UAT does not re-ask the ten hardware questions the operator answered verbatim on
2026-09-11. Those answers are recorded in `19.1-LIVE-CHECK-2.md` under confirmed
preconditions (deploy byte-identity, `dayReportDetail: true` active) and the operator's
disposition was "Approved for all". Re-asking them would be interrogation, not verification.

Three facts establish that those answers still hold at this UAT:

1. **The deployed file is still behaviourally the file that was observed.** Deployed
   `MMM-SPCOutlook.js` SHA256 `e951fb51…c266e28` (re-checked 2026-09-12) vs local HEAD
   `2e5cd944…993e925c`. The only difference is commit `0a0e2ca`, a **comment-only** change
   resolving a stale `[PENDING LIVE CONFIRMATION]` marker (`git diff c2d17f1..HEAD --
   MMM-SPCOutlook.js` = 7 insertions / 3 deletions, all inside one comment block).
2. **The probe suite is green at HEAD:** `191 passed, 0 failed, 0 skipped`.
3. **Nine additional checks were run directly against the shipped module at this UAT**
   (tests 9–12 below), exercising the copy and config-validator behaviour that
   `19.1-LIVE-CHECK-2.md` explicitly could NOT exercise on the mirror.

Tests whose evidence is a live operator observation are marked `source: LIVE-CHECK-2 item N`.
Tests whose evidence is a harness run at this UAT are marked `source: UAT harness`.

## Current Test

[testing complete]

## Tests

### 1. Day report does not enter the centre column
expected: With `dayReportDetail: true` on the deployed mirror, the day-report block stops short of the x=540 centreline (the `22.5rem`/450px cap predicts a left edge at x≈570). This is the direct reversal of the 19-UAT test 5 MAJOR defect, where it crossed 168px past centre.
result: pass
source: LIVE-CHECK-2 item 1 (operator: "No.")

### 2. Compact day rows are not displaced
expected: The unexpanded (compact) day rows sit where they belong — the displacement reported in the first live check is gone.
result: pass
source: LIVE-CHECK-2 item 2 (operator: "no.")

### 3. No text wrapping at the 22.5rem cap
expected: No row wraps onto a second line. Wrapping on ordinary everyday rows would argue the cap is too tight and should rise toward the 24rem ceiling.
result: pass
source: LIVE-CHECK-2 item 3 (operator: "none.")

### 4. Em-dash source-attribution column stays aligned
expected: The `— SPC` / `— WSSI` attribution column reads straight down the block. This is the central risk of the 19.1-06 swap from monospace character-advance to CSS grid tracks.
result: pass
source: LIVE-CHECK-2 item 4 (operator: "yes.")

### 5. Co-equal hazards render as peers
expected: On a day carrying two hazards of co-equal standing, both render as peer rows under the same day — neither demoted to an indented `also:` row.
result: pass
source: UAT 2026-09-12 (operator: "I already was able to view this with Flash Flood and Heat outlooks") — observed on the deployed mirror with a live co-equal pair (WPC Excessive Rainfall / Flash Flood alongside HeatRisk). This is the leg LIVE-CHECK-2 recorded NOT OBSERVABLE for lack of a co-equal pair at the OKC coordinate on 2026-09-11; it is now closed on observation rather than carried forward as a deferred live-observation row.

### 6. `also:` rows sit indented under their own winner row
expected: A second, lower-standing hazard on the same day renders as an `also:` row indented beneath its winner, on the shared grid track.
result: pass
source: LIVE-CHECK-2 item 6 (operator: "yes", confirmed under follow-up that an `also:` row was visible on screen — upgraded from the first check's hedged "I believe so")

### 7. Detail rows use the host proportional font and still read as columns
expected: The row-level `DejaVu Sans Mono` declaration is gone; detail rows render in the host's proportional font, and the columns still read as columns because grid tracks — not glyph width — now hold the alignment.
result: pass
source: LIVE-CHECK-2 item 7 (operator: "yes, actually better.")

### 8. No unexpected blank lines between rows
expected: No blank line appears between detail rows, or between a detail row and the next day's compact line — confirming `inline-grid` rather than block-level grid rendering.
result: pass
source: LIVE-CHECK-2 item 8 (operator: "no.")

### 9. Header three-state lifecycle
expected: Absent `header` key falls back to the literal `"NWS Hazard Outlooks"`; an explicit `header: ""` opt-out is preserved as empty and NOT overridden by the fallback; an operator-configured header passes through verbatim.
result: pass
source: UAT harness (absent -> "NWS Hazard Outlooks"; "" -> ""; "SPC Forecast" -> "SPC Forecast") + LIVE-CHECK-2 item 9 for the configured case on hardware

### 10. Loading string is product-neutral
expected: Before first data arrives the module shows `Loading NWS outlooks...`, not the old `Loading SPC Outlook...`.
result: pass
source: UAT harness (rendered `getDom` with `spcrisk: null`; output contains "Loading NWS outlooks...", zero matches for "Loading SPC Outlook")

### 11. Unrecognised config-key validator
expected: A misspelled key produces one warning naming the key and suggesting the nearest real key; an unknown key warns without a suggestion; legitimate `defaults` keys never warn; the config VALUE is never logged; a bad config never throws out of `start()`.
result: pass
source: UAT harness — `dayReportDetials` warned with `did you mean "dayReportDetail"?`, `zzzTotallyUnknown` warned without suggestion, `apiKey` value `s3cr3t-value` absent from all log output, `start()` did not throw, no warn for `lat`/`lon`/`updateInterval`. Note this reproduces the exact typo that invalidated the 19-08 observation. LIVE-CHECK-2 item 10 covers the negative case (no over-fire on the clean deployed config).

### 12. README copy reconciled to the shipped umbrella
expected: README's opening line and its Multiple-instances quote use the same NWS umbrella as the shipped code, naming the actual seven-product aggregate.
result: pass
source: UAT read — README.md:2 opens "NWS Hazard Outlooks for your location — a point-in-polygon check against SPC convective and fire-weather outlooks, SPC Mesoscale Discussions, WPC Excessive Rainfall, WPC WSSI, WPC Mesoscale Precipitation Discussions, the WPC/CPC US Hazards Outlook, and NWS HeatRisk."; README.md:14 quotes "Loading NWS outlooks..." verbatim as shipped.

## Summary

total: 12
passed: 12
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none yet]
