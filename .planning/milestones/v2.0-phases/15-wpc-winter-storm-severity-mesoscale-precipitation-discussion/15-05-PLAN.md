---
phase: 15-wpc-winter-storm-severity-mesoscale-precipitation-discussion
plan: 05
type: execute
wave: 3
depends_on: ["15-02", "15-03"]
files_modified:
  - scripts/probe-payload-resilience.js
autonomous: true
requirements: [WSSI-01, WSSI-02, WSSI-03]

must_haves:
  truths:
    - "D-10: every new WSSI scenario is mutation-proven load-bearing — break the line it covers, confirm RED, restore"
    - "WSSI-02's case fold is proven load-bearing: replacing it with an exact-match lookup turns a scenario RED"
    - "WSSI-03's zero-feature off-season shape produces no winter row and no error"
    - "D-09 AMENDED: a WINTER WEATHER AREA feature produces no row, exactly as no-polygon does"
    - "A WSSI fetch failure is flagged stale rather than rendered as a confident no-impact reading"
  artifacts:
    - path: "scripts/probe-payload-resilience.js"
      provides: "WSSI fixtures, WSSI_URLS derived from the registry, and six wssi-* scenarios"
      contains: "wssi-case-fold-mismatch-still-resolves"
  key_links:
    - from: "scripts/probe-payload-resilience.js WSSI_URLS"
      to: "PRODUCT_REGISTRY.winterImpact.buildUrl"
      via: "derived, never hardcoded (PERF-02 byte-stability contract)"
      pattern: "PRODUCT_REGISTRY\\.winterImpact\\.buildUrl"
---

<objective>
Extend the probe suite with the WSSI scenarios D-10 names, each mutation-proven load-bearing.

Purpose: D-10 makes probe scenarios the verification standard for WSSI, because live in-season WSSI
confirmation is deferred. These scenarios are therefore the only evidence WSSI-01/02/03 are met at
phase close, which is exactly why each one must be proven capable of failing.
Output: six `wssi-*` scenarios and their fixtures, written against the live-captured value domain.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/buildomator/bm/4.5.5/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/buildomator/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-wpc-winter-storm-severity-mesoscale-precipitation-discussion/15-CONTEXT.md
@.planning/phases/15-wpc-winter-storm-severity-mesoscale-precipitation-discussion/15-RESEARCH.md
@.planning/phases/15-wpc-winter-storm-severity-mesoscale-precipitation-discussion/15-PATTERNS.md
@.planning/phases/14-foundation-wpc-excessive-rainfall-outlook/14-REVIEW-FIX.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add WSSI fixtures and the registry-derived WSSI_URLS map</name>
  <read_first>
    - `scripts/probe-payload-resilience.js` lines 96-130 — `ERO_SLGT_BODY` and neighbouring fixtures,
      the exact shape WSSI fixtures mirror; line 72 defines `EMPTY_FEATURE_COLLECTION` and lines
      28-34 define `SAMPLE_RING`, both reused directly.
    - `scripts/probe-payload-resilience.js` lines 196-204 — `ERO_URLS`, built from
      `PRODUCT_REGISTRY.excessiveRain.buildUrl(day)`, with the comment on why a probe URL is never
      hardcoded (PERF-02 byte-stability).
    - `scripts/probe-payload-resilience.js` lines 296-310 — `eroHttpRoutes` and the WR-02 note on why
      every unrelated layer must answer 200-empty rather than hard-fail.
    - `productRegistry.js` as delivered by plan 15-01 — `wssiRawToValue`, `wssiTierToText` and
      `wssiTierToColor`, the values these assertions are written against.
    - `.planning/phases/15-wpc-winter-storm-severity-mesoscale-precipitation-discussion/15-RESEARCH.md`
      lines 553-588 — the live field schema, the palette, and the verbatim off-season response.
  </read_first>
  <action>
    Add `WSSI_URLS` next to `ERO_URLS`, built as `{ 1: PRODUCT_REGISTRY.winterImpact.buildUrl(1),
    2: PRODUCT_REGISTRY.winterImpact.buildUrl(2), 3: PRODUCT_REGISTRY.winterImpact.buildUrl(3) }`.
    Never write a WSSI query URL literal into this file — the registry is the single source of the
    query string, and deriving it here is what keeps the probe honest about PERF-02.

    Add fixtures beside the ERO fixtures, each a FeatureCollection with one Polygon feature over
    `SAMPLE_RING`, carrying `properties.impact` plus a `valid_time` string:
    - `WSSI_MINOR_BODY` (`impact: "MINOR"`), `WSSI_MODERATE_BODY` (`"MODERATE"`),
      `WSSI_MAJOR_BODY` (`"MAJOR"`), `WSSI_EXTREME_BODY` (`"EXTREME"`) — ALL-CAPS raw values taken
      verbatim from the live renderer's `uniqueValueInfos`.
    - `WSSI_MIXED_CASE_BODY` (`impact: "Minor"`) — the mixed case WPC's prose documentation uses.
    - `WSSI_WWA_BODY` (`impact: "WINTER WEATHER AREA"`) — WPC's own non-impact tier.

    Add a comment above the fixtures recording that WPC's live domain contains no `LIMITED` value, so
    no fixture may assert on that string: a scenario written against it could never go RED and would
    be permanently vacuous.

    Reuse the existing `EMPTY_FEATURE_COLLECTION` for the out-of-season case rather than adding a
    duplicate constant, noting in a comment that it is byte-identical to the live off-season response
    `{"type":"FeatureCollection","features":[]}` captured from the WSSI MapServer.

    Add a `wssiRoutes(day1Handler)` helper mirroring `eroHttpRoutes`: route `WSSI_URLS[1]` to the
    subject handler and route `WSSI_URLS[2]`, `WSSI_URLS[3]`, all five `ERO_URLS`, and `.lyr.geojson`
    to a 200-empty response. This is WR-02's trap — without it, an unrouted layer takes the
    hard-failure default, sets `anyStale`, and any `_stale` assertion passes for a reason that has
    nothing to do with the scenario's subject.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('scripts/probe-payload-resilience.js','utf8');const a=(c,m)=>{if(!c)throw new Error(m)};a(/PRODUCT_REGISTRY\.winterImpact\.buildUrl\(1\)/.test(s),'WSSI_URLS must derive from the registry');a(!/wpc_wssi\/MapServer\/\d+\/query/.test(s),'no hardcoded WSSI query URL may appear');const code=s.split('\n').filter(l=>!/^\s*\/\//.test(l)).join('\n');a(code.indexOf('LIMITED')===-1,'no code line may reference LIMITED');for(const k of ['WSSI_URLS','WSSI_MINOR_BODY','WSSI_MODERATE_BODY','WSSI_MAJOR_BODY','WSSI_EXTREME_BODY','WSSI_MIXED_CASE_BODY','WSSI_WWA_BODY','wssiRoutes'])a(s.includes(k),'missing '+k);console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The `node -e` command above exits 0 and prints `OK`.
    - `WSSI_URLS` values are produced by `PRODUCT_REGISTRY.winterImpact.buildUrl(day)`; no literal
      `wpc_wssi/MapServer/N/query` string exists anywhere in the file.
    - No non-comment line in the file contains the string `LIMITED`.
    - `node scripts/probe-payload-resilience.js` exits 0 with `16 passed, 0 failed, 0 skipped` —
      adding fixtures changes no existing scenario.
  </acceptance_criteria>
  <done>WSSI fixtures cover every live impact value plus the mixed-case and WWA cases, and every WSSI URL is derived from the registry rather than written by hand.</done>
</task>

<task type="auto">
  <name>Task 2: Add the six wssi-* scenarios</name>
  <read_first>
    - `scripts/probe-payload-resilience.js` lines 702-762 — `ero-wellformed-slgt` and `ero-toggle-off`
      in full: the scenario object shape, `resetHelper`/`resetLogs`, the `helper._products`
      assignment, the `turfStub.pointInPolygon` save/restore in a `finally`, and how a toggle-off
      scenario asserts a URL was never requested.
    - `scripts/probe-payload-resilience.js` lines 657-700 — `ero-hard-fail-is-flagged`, the template
      for the stale-on-failure scenario.
    - `scripts/probe-payload-resilience.js` lines 320-400 — `assertPayloadIntact` and `requireLog`,
      the two shared assertions every scenario uses.
    - `.planning/phases/15-wpc-winter-storm-severity-mesoscale-precipitation-discussion/15-RESEARCH.md`
      lines 730-743 — the Validation Architecture section naming these scenarios.
    - `productRegistry.js` as delivered by plan 15-01 — the tier strings and palette values asserted.
  </read_first>
  <action>
    Add six scenarios following the `ero-*` object shape exactly. Every one sets
    `helper._products = { showWinterImpact: ..., showExcessiveRain: false }`, routes through
    `wssiRoutes`, and passes an explicit toggle object as `getSpcOutlook`'s fourth argument (WR-13's
    snapshot discipline). Every one calls `assertPayloadIntact(out)` first, so a scenario can never
    pass against a collapsed `{ error }` payload.

    - `wssi-wellformed-minor`: day 1 serves `WSSI_MINOR_BODY` with `pointInPolygon` forced true.
      Assert `out.winterImpact.day1Risk === "MINOR"`, `day1Text === "Minor"`,
      `day1Color === "faf5a3"`, and that `day1ValidTime` is the fixture's `valid_time` string.
      Assert `day2Risk` and `day3Risk` are `"NONE"`, proving the empty-routed days are unaffected.
    - `wssi-case-fold-mismatch-still-resolves`: day 1 serves `WSSI_MIXED_CASE_BODY` (`"Minor"`).
      Assert `day1Risk === "MINOR"`. This is WSSI-02's direct guard: it must fail if the registry's
      `toValue` stops folding case.
    - `wssi-winter-weather-area-renders-nothing`: day 1 serves `WSSI_WWA_BODY` with `pointInPolygon`
      forced true. Assert `day1Risk === "NONE"` and `out._stale` is not true — D-09 AMENDED's floor
      is a deliberate exclusion, not a degrade. Then render the payload through
      `loadFrontendModule`/`renderDom` with `showWinterImpact: true` and assert the output contains
      no `Winter Impact` substring. Absence must be silence at both layers, not just in the payload.
    - `wssi-zero-features-out-of-season`: all three WSSI days serve `EMPTY_FEATURE_COLLECTION`.
      Assert every `day{N}Risk` is `"NONE"`, `out.error` is undefined, `out._stale` is not true, and
      that the rendered output contains no `Winter Impact` substring. This is WSSI-03's structural
      proof and the substitute for the deferred in-season check.
    - `wssi-toggle-off`: with `showWinterImpact: false`, assert the full 12-key `winterImpact` block
      is still present and entirely `"NONE"`/`"None"` (Phase 14 D-05), and assert no URL containing
      `WSSI_URLS[1]` appears in the recorded fetch calls — the same assertion `ero-toggle-off` makes.
    - `wssi-hard-fail-is-flagged`: day 1 hard-fails while every other route answers 200-empty.
      Assert `out._stale === true` and `day1Risk === "NONE"`, and use `requireLog` to prove a
      diagnostic naming `winterImpact` and `day 1` was emitted. A degraded winter read must never be
      indistinguishable from a genuine no-impact day (CR-01's lesson).
  </action>
  <verify>
    <automated>node scripts/probe-payload-resilience.js</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/probe-payload-resilience.js` exits 0 and prints `PROBE RESULT: 22 passed,
      0 failed, 0 skipped`, with a `PASS` line for each of the six `wssi-*` names.
    - Each scenario calls `assertPayloadIntact` before any product-specific assertion.
    - `wssi-toggle-off` asserts on `helper.fetchGeoJsonCached.calls` (or the `installHttp` call log)
      that the WSSI day-1 URL was never requested.
  </acceptance_criteria>
  <done>Six `wssi-*` scenarios cover a well-formed impact, the case fold, the WWA exclusion at both payload and render layers, the out-of-season empty response, the toggle-off block, and the stale-on-failure signal.</done>
</task>

<task type="auto">
  <name>Task 3: Mutation-prove every new WSSI scenario is load-bearing</name>
  <read_first>
    - `.planning/phases/14-foundation-wpc-excessive-rainfall-outlook/14-REVIEW-FIX.md` — the
      mutation-proof standard D-10 requires: for each scenario, a targeted source mutation must turn
      it RED, and is then reverted.
    - `productRegistry.js` as delivered by plan 15-01 — `toValue`'s fold and `includesFeat`'s
      `val >= 2` gate are the two lines mutated below.
    - `node_helper.js` as delivered by plan 15-03 — `_runArcGisDayProduct`'s `anyStale` accumulation
      and the `winterImpact` call site are the other two mutation targets.
    - `MMM-SPCOutlook.js` as delivered by plan 15-03 — the WSSI render block and the extended
      no-risk gate.
  </read_first>
  <action>
    For each mutation below: apply it, run `node scripts/probe-payload-resilience.js`, confirm the
    named scenario reports FAIL (and that the failure message is specific enough to diagnose), then
    revert with `git checkout --` on the mutated file and confirm the suite returns to all-green.
    Do not commit any mutated state. Record each mutation, the scenario that went RED, and the
    observed failure message in the plan summary — that record is the evidence D-10 asks for.

    1. In `productRegistry.js`, change `winterImpact.toValue`'s fold to an exact-match lookup (drop
       the `.toUpperCase()`). Expect `wssi-case-fold-mismatch-still-resolves` RED and
       `wssi-wellformed-minor` still green — proving the case scenario is what covers WSSI-02 and is
       not merely riding on the ALL-CAPS path.
    2. In `productRegistry.js`, relax `winterImpact.includesFeat` from `val >= 2` to `val > 0`.
       Expect `wssi-winter-weather-area-renders-nothing` RED — proving D-09 AMENDED's floor is
       enforced by that expression and not by an accident elsewhere.
    3. In `node_helper.js`, drop the `fetchResult.stale || fetchResult.failed` accumulation from the
       shared runner. Expect `wssi-hard-fail-is-flagged` RED (and the existing
       `ero-hard-fail-is-flagged` RED too, which confirms the extraction genuinely shares one line
       between both products rather than leaving ERO on a private copy).
    4. In `node_helper.js`, change the `winterImpact` call site to pass
       `PRODUCT_REGISTRY.excessiveRain` instead. Expect `wssi-wellformed-minor` RED — proving the
       scenario is bound to the WSSI row rather than passing on generic no-risk defaults.
    5. In `MMM-SPCOutlook.js`, delete the `winterImpact` term from the no-risk short-circuit gate.
       Expect `wssi-winter-weather-area-renders-nothing` to stay green (WWA correctly renders
       nothing either way) but the render assertion in `wssi-wellformed-minor` to go RED if it
       covers the gate. If no scenario goes RED for this mutation, the gate term is unguarded —
       extend `wssi-wellformed-minor` to render its payload and assert the output is not the literal
       string `No Severe Weather Risk`, then re-run the mutation and confirm RED. A mutation with no
       RED scenario is a coverage gap, not a passing result.

    Mutation 5 is the one most likely to expose a missing assertion, because the gate is in a
    different file from the payload logic. Phase 14 needed a `deep` review round to find exactly this
    class of cross-file defect; treat a green result there as a finding to fix, not as a pass.
  </action>
  <verify>
    <automated>git diff --exit-code productRegistry.js node_helper.js MMM-SPCOutlook.js scripts/probe-payload-resilience.js && node scripts/probe-payload-resilience.js</automated>
  </verify>
  <acceptance_criteria>
    - All five mutations were applied and reverted; for each, the summary records the mutation, the
      scenario(s) that went RED, and the failure message observed.
    - Every mutation produced at least one RED scenario. If mutation 5 initially produced none, the
      summary records the assertion added to close that gap and the RED result after re-running.
    - `git diff --exit-code` over the four source files reports no residual changes — no mutated
      state was left behind.
    - `node scripts/probe-payload-resilience.js` exits 0 with `22 passed, 0 failed, 0 skipped`.
  </acceptance_criteria>
  <done>Every WSSI scenario is demonstrated capable of failing when the line it covers is broken, satisfying D-10's mutation-proof standard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| probe fixtures → asserted behaviour | Fixtures stand in for remote WPC responses; a fixture that does not match the live shape certifies behaviour that does not exist. |
| suite exit code → phase gate | D-10 makes this suite the sole verification for WSSI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-19 | Repudiation | vacuous WSSI scenarios | mitigate | Task 3 mutation-proves each scenario, and a mutation that produces no RED scenario is explicitly treated as a coverage gap to close rather than a pass. The `LIMITED` grep check blocks the specific vacuous assertion RESEARCH.md Pitfall 4 warns about. |
| T-15-20 | Tampering | fixture drift from the live schema | mitigate | Fixtures carry the live-captured `impact` values verbatim and the empty-collection fixture is the live off-season body; `WSSI_URLS` derives from the registry so a URL change cannot silently desynchronise the probe from the product. |
| T-15-21 | Information disclosure | a degraded WSSI read presented as no-impact | mitigate | `wssi-hard-fail-is-flagged` asserts `_stale` and a diagnostic naming the product and day; mutation 3 proves that assertion is load-bearing. |
| T-15-22 | Spoofing | probe reaching the real network | mitigate | Every scenario installs a routed seam and any unrouted URL takes the hard-failure/503 default; the suite makes zero outbound requests, unchanged from Phase 14. |
</threat_model>

<verification>
- `node scripts/probe-payload-resilience.js` → `PROBE RESULT: 22 passed, 0 failed, 0 skipped`, exit 0.
- `git diff --exit-code` over the four touched source files is clean after mutation proofs.
- The summary records five mutations, each with the scenario that went RED.
</verification>

<success_criteria>
Six mutation-proven `wssi-*` scenarios cover WSSI-01, WSSI-02 and WSSI-03 plus D-09 AMENDED's render
floor at both the payload and render layers, using registry-derived URLs and live-captured fixture
values, with no scenario able to pass while the behaviour it names is broken.
</success_criteria>

<output>
Create `.planning/phases/15-wpc-winter-storm-severity-mesoscale-precipitation-discussion/15-05-SUMMARY.md` when done
</output>
