---
phase: 17-nws-wpc-heatrisk-parallelized-fetching
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - productRegistry.js
autonomous: true
requirements: [HEAT-03, DATA-03]

must_haves:
  truths:
    - "PRODUCT_REGISTRY.heatRisk exists as a fourth kind (arcgis-identify-point) with configFlag showHeatRisk, so _productToggles picks it up with no edit to node_helper.js's toggle plumbing (planner decision A)"
    - "buildHeatRiskIdentifyUrl emits a byte-stable ImageServer identify URL declaring wkid 102100 for Mercator-metre coordinates (HEAT-03, D-03 of Phase 14's non-overridable-query-builder lineage; PERF-02)"
    - "The HeatRisk row carries maxDataAgeHours: 12 (D-07) and a cited paletteSource (15 D-08 / 14-REVIEW IN-01)"
    - "assertNoSharedRegistryMaps throws at module load if any two PRODUCT_REGISTRY rows share a value/tier/text/colour map by object identity, naming both row ids (D-11, DATA-03)"
    - "The assertion's own comment states what object identity CANNOT see: a toValue closure reading a foreign constant by name (D-11)"
  artifacts:
    - path: "productRegistry.js"
      provides: "HEATRISK_BASE_URL, buildHeatRiskIdentifyUrl, PRODUCT_REGISTRY.heatRisk, MAP_FIELDS, assertNoSharedRegistryMaps"
      contains: "assertNoSharedRegistryMaps"
  key_links:
    - from: "productRegistry.js PRODUCT_REGISTRY.heatRisk.configFlag"
      to: "node_helper.js _productToggles"
      via: "registry iteration over row.configFlag (no SUB_TOGGLES edit)"
      pattern: "configFlag: \"showHeatRisk\""
    - from: "productRegistry.js module load"
      to: "assertNoSharedRegistryMaps(PRODUCT_REGISTRY)"
      via: "load-time call, same as daySpanOf/dayRangeOf/dayRangeSpanning"
      pattern: "assertNoSharedRegistryMaps\\(PRODUCT_REGISTRY\\)"
---

<objective>
Add the HeatRisk registry row, its dedicated ImageServer `identify` URL builder, and D-11's
load-time cross-row map-identity assertion.

Purpose: `productRegistry.js` is the single place this codebase declares "what a product is."
Every downstream plan in this phase reads from this row: the runner reads `buildUrl`,
`maxDataAgeHours`, `days`, `valueToText`, `valueToColor`; `_productToggles` reads `configFlag`;
the frontend's toggle name must match `configFlag`. Landing it first removes every ambiguity
from the plans that follow. The D-11 assertion ships here because it is a load-time invariant
over this same file.

Output: `productRegistry.js` gains `HEATRISK_BASE_URL`, `buildHeatRiskIdentifyUrl`,
`PRODUCT_REGISTRY.heatRisk`, `MAP_FIELDS`, `assertNoSharedRegistryMaps`, and two new exports.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/buildomator/bm/4.5.5/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/buildomator/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/17-CONTEXT.md
@.planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/17-RESEARCH.md
@.planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/17-PATTERNS.md

<interfaces>
<!-- Contracts this plan DEFINES for every later plan in phase 17. Downstream plans consume
     these names verbatim — do not rename them without updating 17-02..17-09. -->

PRODUCT_REGISTRY.heatRisk fields consumed downstream:
  id            "heatRisk"                       — batch member id (17-05), payload key (17-04)
  kind          "arcgis-identify-point"          — fourth kind; NOT looped like kml-advisory
  configFlag    "showHeatRisk"                   — read by node_helper _productToggles (:244)
                                                    and matched by MMM-SPCOutlook defaults (17-03)
  baseUrl       HEATRISK_BASE_URL
  buildUrl      (mercatorX, mercatorY) => string — NOT buildUrl(day); one URL covers all 7 days
  days          7                                — the SOLE declaration of HeatRisk's day span
  maxDataAgeHours 12                             — D-07, read per surviving item by 17-04
  valueToText   { 0..4 -> string }               — backend-applied tier text (17-04)
  valueToColor  { 0..4 -> 6-hex-digit, no '#' }  — backend-applied colour (17-04)
  paletteSource string                           — provenance citation (15 D-08 / IN-01)

New module exports: buildHeatRiskIdentifyUrl, assertNoSharedRegistryMaps
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HeatRisk registry row and its dedicated identify URL builder</name>

  <files>productRegistry.js</files>

  <read_first>
    - productRegistry.js — read the WHOLE file (456 lines). You need: `buildArcGisQuery` (:24) and
      its `https://mapservices.weather.noaa.gov/` host-prefix allowlist check (:28-30);
      `daySpanOf` (:53) / `dayRangeOf` (:229) / `dayRangeSpanning` (:252) for the load-time-throw
      idiom and comment style; `PRODUCT_REGISTRY.excessiveRain` (:266+) and
      `PRODUCT_REGISTRY.winterImpact` (:~295+) for a row that HAS a severity ladder;
      `PRODUCT_REGISTRY.hazardsOutlook` (:380-449) for a row that has NO `valueToTier`;
      `PRODUCT_REGISTRY.spcMD`/`.mpd` (:337-379) for a row that is not day-layer shaped; the
      closing comment at :450 ("Future row (HeatRisk) lands in Phase 17") which this task
      replaces; and `module.exports` (:453-455).
    - .planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/17-RESEARCH.md §"Endpoint
      Contract — Live-Verified This Session" (the exact working query-parameter set) and
      §"Architecture Patterns → Registry Row & Runner Design" (the illustrative row/builder).
    - .planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/17-PATTERNS.md §9 (the full
      label-to-value table inventory this row must not collide with).
  </read_first>

  <action>
    Replace the placeholder comment at `productRegistry.js:450` with a real HeatRisk row, and add
    the constant and URL builder above `PRODUCT_REGISTRY`.

    (a) Declare `const HEATRISK_BASE_URL = "https://mapservices.weather.noaa.gov/experimental/rest/services/NWS_HeatRisk/ImageServer";`
    alongside the other product base-URL constants.

    (b) Add `function buildHeatRiskIdentifyUrl(mercatorX, mercatorY)`. It is a SEPARATE builder,
    not a bent `buildArcGisQuery` — state that in its comment with the reason: `buildArcGisQuery`
    is hardcoded to a MapServer feature-layer `/{layerId}/query?...&f=geojson` shape, while this
    is an ImageServer `identify` with `geometry`/`geometryType`/`returnGeometry`/`f=json`.
    Requirements for the builder:
      - Apply the SAME host-prefix allowlist check `buildArcGisQuery` enforces at :28-30, against
        the literal prefix `https://mapservices.weather.noaa.gov/`, throwing the same shape of
        error with the offending value serialized in. (ASVS V4 / T-17-02.)
      - Throw if `mercatorX` or `mercatorY` is not `Number.isFinite`, with both values serialized
        into the message.
      - Do NO reprojection itself. It receives coordinates already Web-Mercator-projected by the
        caller (17-04 calls `turf.toMercator`). Comment that constraint: the geometry's `x`/`y`
        and its declared `spatialReference.wkid` MUST originate from the same reprojection so a
        unit/declaration mismatch is unrepresentable — that mismatch, NOT `sr=4326` itself, is the
        live-reproduced root cause of a `"NoData"` response (17-RESEARCH.md Common Pitfalls #2).
        Do not repeat STACK.md's superseded "sr=4326 returns NoData" framing anywhere in the file.
      - Build the geometry as `JSON.stringify({ x, y, spatialReference: { wkid: 102100 } })` with
        that exact key order fixed on every call, `encodeURIComponent`-encoded, and append the
        parameters in this exact fixed order: `geometryType=esriGeometryPoint`, `sr=102100`,
        `returnGeometry=false`, `returnCatalogItems=true`, `f=json`. Comment that the fixed key
        and parameter order is PERF-02's byte-stable-query-string requirement: incidental
        reordering would defeat the ETag/hash cache. `returnCatalogItems=true` is kept explicit
        even though the service appears to default to it — never rely on an unconfirmed default
        (Phase 14 D-09's precedent).

    (c) Add `PRODUCT_REGISTRY.heatRisk` as the final row, before the closing brace, with fields
    exactly as named in this plan's `<interfaces>` block. Notes on the non-obvious fields:
      - `kind: "arcgis-identify-point"` — a fourth kind. Comment that 15 D-01 left the `kind` slot
        open for non-day-layer products and this is the planner's resolution of CONTEXT.md's
        discretion item: the row exists (rather than a standalone function) because `configFlag`
        makes `_productToggles` pick it up with zero edits, `maxDataAgeHours` needs a home
        consistent with 16 D-13/D-14, and D-11's identity assertion iterates `PRODUCT_REGISTRY`
        — a standalone function would leave HeatRisk outside that invariant.
      - `days: 7` — the SOLE place HeatRisk's day span is declared. Comment it as such, citing
        16-REVIEW WR-03 ("derive spans, never restate them"): 17-04's runner reads `row.days`,
        and no literal `7` for HeatRisk may appear anywhere else in `node_helper.js`.
      - `maxDataAgeHours: 12` — comment citing D-07 and the service's live `serviceDescription`
        hourly cadence (12h ≈ 11 missed cycles).
      - `valueToText` — `{ 0: "Little to No Risk", 1: "Minor", 2: "Moderate", 3: "Major",
        4: "Extreme" }`, module-authored, keyed by the raw 0-4 category.
      - `valueToColor` — 6 hex digits, NO leading `#` (matches how `MMM-SPCOutlook.js` renders:
        `"color:#" + value`). Source the hex with provenance, in this order:
          1. `curl -s "https://mapservices.weather.noaa.gov/experimental/rest/services/NWS_HeatRisk/ImageServer/legend?f=json"`
             and `.../ImageServer?f=json` — if either carries machine-readable RGB for the five
             levels, use it.
          2. Otherwise the NWS HeatRisk documentation ladder at
             `https://www.wpc.ncep.noaa.gov/heatrisk/`.
          3. Otherwise fall back to the module-authored ladder
             `{ 0: "00A200", 1: "FFFF00", 2: "FF7F00", 3: "FF0000", 4: "FF00FF" }`, which matches
             this module's existing `fireRiskToColor` house palette (`MMM-SPCOutlook.js:160`).
        Whichever you use, set `paletteSource` to a string naming the exact source URL and the
        retrieval date, or the literal words "module-authored fallback ladder, matches
        fireRiskToColor house palette" if you reached step 3. 15 D-08 / 14-REVIEW IN-01 require
        the citation either way; an uncited palette is the finding.
      - Deliberately NO `toValue`, `includesFeat`, `valueToTier`, `dayLayers`, `layers`,
        `excludedLabels` — comment that HeatRisk has no label vocabulary at all (its value is a
        raw numeric category straight off the service) and no polygon evaluation, so
        `extractPolygons`/`evaluatePolygons`/`checkInPolygon` never apply to it.

    (d) Add `buildHeatRiskIdentifyUrl` to `module.exports` alongside `buildArcGisQuery`.
  </action>

  <verify>
    <automated>node -e "const r=require('./productRegistry'); const h=r.PRODUCT_REGISTRY.heatRisk; if(h.kind!=='arcgis-identify-point'||h.configFlag!=='showHeatRisk'||h.days!==7||h.maxDataAgeHours!==12) throw new Error('row fields wrong: '+JSON.stringify(h)); if(typeof h.paletteSource!=='string'||h.paletteSource.length<10) throw new Error('paletteSource missing'); for(const k of [0,1,2,3,4]){ if(typeof h.valueToText[k]!=='string') throw new Error('valueToText '+k); if(!/^[0-9A-Fa-f]{6}$/.test(h.valueToColor[k])) throw new Error('valueToColor '+k+' not 6 hex digits: '+h.valueToColor[k]); } const u=r.buildHeatRiskIdentifyUrl(-10846971.18,4193818.60); for(const s of ['/identify?geometry=','geometryType=esriGeometryPoint','sr=102100','returnGeometry=false','returnCatalogItems=true','f=json']) if(!u.includes(s)) throw new Error('URL missing '+s); if(!u.includes(encodeURIComponent('\"wkid\":102100'))) throw new Error('URL does not declare wkid 102100'); if(u.includes('f=geojson')) throw new Error('identify URL must not use f=geojson'); if(r.buildHeatRiskIdentifyUrl(-10846971.18,4193818.60)!==u) throw new Error('URL not byte-stable across calls'); let threw=0; try{r.buildHeatRiskIdentifyUrl(NaN,1);}catch(e){threw++;} try{r.buildHeatRiskIdentifyUrl(-97.44,undefined);}catch(e){threw++;} if(threw!==2) throw new Error('non-finite guard did not throw twice'); console.log('OK '+u);"</automated>
  </verify>

  <acceptance_criteria>
    - `node -e "require('./productRegistry')"` exits 0 (no load-time throw regression).
    - The command in `<verify>` prints `OK https://mapservices.weather.noaa.gov/experimental/rest/services/NWS_HeatRisk/ImageServer/identify?geometry=...` and exits 0.
    - `grep -c 'buildArcGisQuery' productRegistry.js` is unchanged from its pre-edit value — the
      HeatRisk builder is additive, `buildArcGisQuery` was not modified. Record both numbers in
      the SUMMARY.
    - `grep -n 'HEATRISK_BASE_URL' productRegistry.js` shows the constant declared exactly once
      and referenced by `buildHeatRiskIdentifyUrl` and the row's `baseUrl`.
    - `grep -vn '^\s*//' productRegistry.js | grep -c 'f=geojson'` is unchanged — no `f=geojson`
      entered the identify path.
    - `grep -n 'sr=4326' productRegistry.js` returns nothing, and no comment in the file claims
      `sr=4326` itself causes `NoData` (the superseded framing; the real cause is a
      coordinate/spatialReference unit mismatch).
    - The row's `days: 7` is the only occurrence of a bare HeatRisk day-span literal in the file:
      `grep -n 'days: 7' productRegistry.js` returns exactly one line, inside the heatRisk row.
    - `paletteSource` string contains either an `https://` URL plus a date, or the exact phrase
      `module-authored fallback ladder`.
  </acceptance_criteria>

  <done>
    `PRODUCT_REGISTRY.heatRisk` and `buildHeatRiskIdentifyUrl` exist and are exported; the verify
    command passes; `buildArcGisQuery` is untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: D-11 load-time cross-row map-identity assertion</name>

  <files>productRegistry.js</files>

  <read_first>
    - productRegistry.js — specifically `daySpanOf` (:53-74), `dayRangeOf` (:229-250) and
      `dayRangeSpanning` (:252-264) for the exact load-time-validator idiom (throw with the
      offending value serialized into the message; called at module scope beneath the registry),
      plus every row's map fields so you can confirm `MAP_FIELDS` covers the real ones.
    - .planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/17-PATTERNS.md §9 — the complete
      inventory of all ten label-to-value tables with file:line, and the statement of what object
      identity cannot see.
    - .planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/17-RESEARCH.md §"D-11 Load-Time
      Identity Assertion" — the proposed implementation and its `MAP_FIELDS` list.
  </read_first>

  <action>
    Add a fourth load-time validator to `productRegistry.js`, beneath the registry and beside the
    existing `daySpanOf`/`dayRangeOf`/`dayRangeSpanning` call sites (D-11, DATA-03).

    Declare `const MAP_FIELDS = [...]` covering every map-shaped field any row actually carries.
    Start from `valueToTier`, `tierToText`, `tierToColor`, `displayColor`, `excludedLabels`,
    `droughtLabels`, `excludedLabelKeys`, `droughtLabelKeys`, then ADD `toValue`, `valueToText`
    and `valueToColor` (Task 1's HeatRisk row introduces the last two, and `toValue` is where
    ERO's `dn` map actually lives). Before finalising the list, grep the real row definitions and
    confirm every map-shaped field name in the file appears in `MAP_FIELDS`; if a row carries a
    map field you did not list, add it and note the addition in the SUMMARY.

    Add `function assertNoSharedRegistryMaps(registry)`: walk `Object.entries(registry)`, and for
    each `MAP_FIELDS` name whose value is neither `undefined` nor `null`, record object identity
    in a `Map` from value -> rowId. On a second row presenting the same object, throw an `Error`
    naming BOTH row ids and the field name, and stating this is DATA-03's exact failure shape (a
    label-to-value mapping reused across products) with the remedy ("give <rowId> its own
    <field>"). Match the existing validators' message style. Note that a field whose value is not
    an object (a number, a string) can be skipped — identity on primitives is meaningless.

    Call `assertNoSharedRegistryMaps(PRODUCT_REGISTRY);` at module scope, in the same region the
    other validators run.

    Write the function's own comment to state, explicitly, the two things D-11 requires:
    (1) what it catches — the shared-object-reference reuse DATA-03's own example names (ERO's
    `dn` map fed through fire weather's `DN` table), caught at load time on first run rather than
    in the field; and (2) what object identity CANNOT see — a `toValue`/`includesFeat` closure
    that reads a FOREIGN constant by name rather than sharing the object by reference would pass
    this check while still being wrong. Point that second half at the recorded spot-check
    inventory in `17-PATTERNS.md` §9, by filename, as the paired artifact. An assertion that
    overstates its own coverage was the explicitly rejected option in D-11.

    Export `assertNoSharedRegistryMaps` from `module.exports` so 17-08's probe scenario can call
    it against a scenario-local fixture registry without mutating the real `PRODUCT_REGISTRY`.
  </action>

  <verify>
    <automated>node -e "const r=require('./productRegistry'); const f=r.assertNoSharedRegistryMaps; if(typeof f!=='function') throw new Error('not exported'); f(r.PRODUCT_REGISTRY); const shared={A:1}; let msg=null; try{ f({ rowA:{ valueToText: shared }, rowB:{ valueToText: shared } }); }catch(e){ msg=e.message; } if(!msg) throw new Error('assertion did not throw on a shared map'); for(const s of ['rowA','rowB','valueToText']) if(!msg.includes(s)) throw new Error('throw message missing '+s+': '+msg); f({ rowA:{ valueToText:{A:1} }, rowB:{ valueToText:{A:1} } }); console.log('OK identity assertion live; message: '+msg);"</automated>
  </verify>

  <acceptance_criteria>
    - `node -e "require('./productRegistry')"` exits 0 — the real registry passes its own new
      assertion (if it throws, a genuine DATA-03 violation already exists and must be fixed in
      this task, not suppressed).
    - The `<verify>` command exits 0 and its printed message names both `rowA` and `rowB` and the
      field `valueToText`.
    - Two structurally identical but distinct objects do NOT throw (proved by the last `f({...})`
      call in `<verify>`) — the check is identity, not deep equality.
    - `grep -n 'assertNoSharedRegistryMaps(PRODUCT_REGISTRY)' productRegistry.js` returns exactly
      one line, at module scope (not inside a function).
    - Every map-shaped field name present on any row appears in `MAP_FIELDS`. Prove it: for each
      row, list its own map-shaped field names and diff against `MAP_FIELDS`; record the diff
      (expected: empty) in the SUMMARY.
    - The assertion's comment block literally contains a sentence describing what it cannot see
      (a closure reading a foreign constant by name) and names `17-PATTERNS.md` as the paired
      spot-check artifact. Verify with
      `grep -n 'foreign constant' productRegistry.js` returning at least one line.
  </acceptance_criteria>

  <done>
    `assertNoSharedRegistryMaps` runs at module load, passes against the real registry, throws
    with both row ids named on a synthetic shared map, and is exported.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| module load -> PRODUCT_REGISTRY | Static config; a mis-declared row silently mis-renders every downstream product |
| buildHeatRiskIdentifyUrl -> outbound HTTPS | The only place this phase constructs a new remote URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-02 | Spoofing | `buildHeatRiskIdentifyUrl` | mitigate | Host-prefix allowlist check against the literal `https://mapservices.weather.noaa.gov/`, mirroring `buildArcGisQuery:28-30`; throws on any other host (ASVS V4) |
| T-17-08 | Tampering | `buildHeatRiskIdentifyUrl` geometry | mitigate | `Number.isFinite` guard on both coordinates plus the "x/y and spatialReference must come from the same reprojection" comment — a unit/declaration mismatch returns a well-formed HTTP-200 `"NoData"` and would otherwise be silent |
| T-17-04 | Tampering (data integrity) | `PRODUCT_REGISTRY` rows | mitigate | `assertNoSharedRegistryMaps` throws at module load naming both offending rows (D-11, DATA-03) |
| T-17-09 | Denial of Service | query-string construction | accept | Fixed key/parameter order, no user-controlled string enters the URL beyond two numeric coordinates already `Number.isFinite`-guarded |
| T-17-SC | Tampering | npm/pip/cargo installs | mitigate | N/A this phase — zero new packages installed (17-RESEARCH.md Package Legitimacy Audit: no install target). No `[ASSUMED]`/`[SUS]` package exists to gate. |
</threat_model>

<verification>
- `node -e "require('./productRegistry')"` exits 0.
- Both task `<verify>` commands exit 0.
- `node scripts/probe-payload-resilience.js` still reports 79/79 scenarios passing — this plan is
  purely additive to the registry and must not disturb any existing scenario. A regression here
  means either the new load-time assertion found a pre-existing DATA-03 violation (fix it) or a
  row was accidentally edited.
</verification>

<success_criteria>
- `PRODUCT_REGISTRY.heatRisk` exists with `kind: "arcgis-identify-point"`, `configFlag:
  "showHeatRisk"`, `days: 7`, `maxDataAgeHours: 12`, a cited `paletteSource`, and 0-4 keyed
  `valueToText`/`valueToColor`.
- `buildHeatRiskIdentifyUrl` produces a byte-stable, host-allowlisted, `wkid:102100`-declaring
  identify URL and throws on non-finite coordinates.
- `assertNoSharedRegistryMaps` runs at module load and is exported.
- The existing probe suite is unchanged and green.
</success_criteria>

<output>
Create `.planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/17-01-SUMMARY.md` when done.
Record: the `paletteSource` you settled on and which of the three sourcing steps you reached; the
final `MAP_FIELDS` list and the (expected-empty) diff against every row's real map fields; and the
before/after `grep -c 'buildArcGisQuery'` counts.
</output>
