---
phase: 17-nws-wpc-heatrisk-parallelized-fetching
plan: 06
type: execute
wave: 5
depends_on: ["17-05"]
files_modified:
  - scripts/probe-payload-resilience.js
autonomous: true
requirements: [HEAT-01, HEAT-02, HEAT-03, HEAT-04]

must_haves:
  truths:
    - "The probe suite can build a live-shaped HeatRisk identify response with configurable catalog order, values, duplicates and gaps, and can route it through the real _fetch transport seam"
    - "Every non-subject URL — including the HeatRisk identify URL in non-HeatRisk scenarios and every ERO/WSSI/hazards/advisory URL in HeatRisk scenarios — answers 200 with a quiet default, so anyStale can only be set by the scenario's own subject (WR-02's trap)"
    - "A HeatRisk identify body survives fetchGeoJsonCached's shape validator and produces real categories, proving the 17-02 generalization is load-bearing (17-RESEARCH.md Pitfall 1)"
    - "Day attribution follows idp_validtime, mutation-proven against the live-observed out-of-order catalog (HEAT-01, HEAT-02)"
    - "The issued URL carries Mercator-magnitude coordinates under a wkid:102100 declaration, mutation-proven (HEAT-03)"
    - "Duplicate idp_validtime resolves to the greatest idp_filedate, mutation-proven (HEAT-04)"
    - "A Values/features length mismatch abandons the poll, mutation-proven (D-06)"
    - "Every scenario added here is individually mutation-proven: break the exact line it covers, confirm RED with a diagnosable message, restore (Phase 15 D-10, blocking)"
  artifacts:
    - path: "scripts/probe-payload-resilience.js"
      provides: "HEATRISK_URL, HEATRISK_NOW_MS, heatRiskIdentifyResponse, heatRiskRoutes, assertHeatRiskBlockIntact, and five heatrisk-* scenarios"
      contains: "heatRiskRoutes"
  key_links:
    - from: "scripts/probe-payload-resilience.js heatRiskRoutes"
      to: "PRODUCT_REGISTRY.heatRisk.buildUrl"
      via: "URL derived from the registry, never a hand-copied literal"
      pattern: "PRODUCT_REGISTRY\\.heatRisk\\.buildUrl"
    - from: "eroHttpRoutes / wssiRoutes / hazardsRoutes / advisoryRoutes"
      to: "the HeatRisk identify URL quiet default"
      via: "added to every pre-existing route builder so a sixth product cannot silently 503"
      pattern: "HEATRISK_URL"
---

<objective>
Build the HeatRisk probe fixtures and route builders, then land the five core scenarios covering
HEAT-01, HEAT-02, HEAT-03, HEAT-04 and D-06 — each individually mutation-proven.

Purpose: this project has no automated test framework. Mutation-proven probe scenarios plus manual
UAT ARE the verification strategy, and Phase 15 D-10 makes individual mutation proof blocking
project policy. Two failure shapes have shipped here before and present identically: an assertion
that observes nothing (the fixture never reaches the code path), and a fixture that cannot express
its own condition. Both are prevented by the same two cheap devices this suite already proves out:
a precondition guard that throws if setup did not produce the state under test, and a control
assertion showing the gate is not simply never firing.

Output: `scripts/probe-payload-resilience.js` gains fixture/route helpers and five `heatrisk-*`
scenarios.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/buildomator/bm/4.5.5/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/buildomator/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/17-CONTEXT.md
@.planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/17-RESEARCH.md
@.planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/17-PATTERNS.md
@.planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/17-04-SUMMARY.md
@.planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/17-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: HeatRisk fixture builder, route builder, and payload-contract assertion</name>

  <files>scripts/probe-payload-resilience.js</files>

  <read_first>
    - scripts/probe-payload-resilience.js `:270-330` — `PROBE_LAT`/`PROBE_LON`, `ERO_URLS`,
      `WSSI_URLS`, `HAZARDS_URLS` (built by calling the registry's own `buildUrl`, `:307-310`), and
      `HAZARDS_NOW_MS` (`:316`) with its comment about pinning `helper._nowMs`.
    - scripts/probe-payload-resilience.js `:606-800` — `installFetch`, `installHttp` (`:699-710`,
      including that an unrouted URL takes a 503 default), `httpResponse` (`:666`),
      `httpBodyReadFailure` (`:684`), `eroHttpRoutes` (`:716`), `wssiRoutes` (`:733`),
      `hazardsRoutes` (`:755-776`) and `hazardsFeature` (`:778-789`). `hazardsRoutes` is the direct
      shape to mirror.
    - scripts/probe-payload-resilience.js `:452-482` — `advisoryRoutes`, the fourth route builder
      that also needs the new quiet default.
    - scripts/probe-payload-resilience.js `:790-830` — the payload contract assertion region
      (`assertPayloadIntact`, `assertHazardsBlockIntact`) and its WR-10 comment about the probe
      stating the contract independently.
    - .planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/17-RESEARCH.md §"Endpoint Contract
      — Live-Verified This Session" — the full live captured response, verbatim, including the
      out-of-order catalog and the exact `idp_validtime` / `idp_filedate` values.
    - .planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/17-04-SUMMARY.md — the shipped
      payload contract as implemented.
  </read_first>

  <action>
    Add HeatRisk harness infrastructure to `scripts/probe-payload-resilience.js`, beside the
    existing hazards helpers.

    (a) `const HEATRISK_URL = PRODUCT_REGISTRY.heatRisk.buildUrl(<mercX>, <mercY>);` — derive it by
    calling the registry's own builder against `turf.toMercator`'s output for
    `PROBE_LAT`/`PROBE_LON`, exactly as `HAZARDS_URLS` calls the registry's `buildUrl` rather than
    hand-copying a literal. If the harness's turf is stubbed, compute the Mercator pair the same
    way the runner does so the URL matches byte-for-byte. Comment that a hand-copied literal would
    make the route silently miss the moment `buildHeatRiskIdentifyUrl` changes, and the scenario
    would then be testing `installHttp`'s 503 default.

    (b) `const HEATRISK_NOW_MS = Date.UTC(2026, 7, 31, 13, 0);` — a pinned clock for HeatRisk
    scenarios, mirroring `HAZARDS_NOW_MS`. Every HeatRisk scenario sets `helper._nowMs = () =>
    HEATRISK_NOW_MS` as its first act after `resetHelper`, so `_todayUtcMs()` derives from it and
    day offsets are deterministic. Pick fixture `idp_validtime` values as
    `Date.UTC(2026, 7, 31, 12, 0) + (d - 1) * 86400000` for day `d`, matching the live-verified
    12:00:00.000Z alignment. Comment the 12:00Z alignment as the reason day 1 lands at
    `Math.round(0.5) === 1`. Note 16-REVIEW WR-05 (a pinned constant drifting behind the real
    clock) as the reason every HeatRisk assertion must route through the pinned seam rather than
    depending on the real date.

    (c) `function heatRiskCatalogItem({ name, validtime, filedate, ingestdate })` — builds one
    `{ attributes: { objectid, name, category, idp_ingestdate, idp_filedate, idp_validtime } }`
    entry with the live-observed field names. `filedate` defaults to `HEATRISK_NOW_MS` (fresh) so
    freshness scenarios must opt in to staleness, mirroring `hazardsFeature`'s defaulting note.

    (d) `function heatRiskIdentifyResponse({ items, values, value, visibilities })` — builds a full
    identify-shaped body: top-level `objectId`, `name: "Pixel"`, `value`, `location`,
    `properties: { Values: values }`, `catalogItems: { objectIdFieldName, features: items }`,
    `catalogItemVisibilities: visibilities`. It must permit `values` to be a DIFFERENT LENGTH from
    `items` (D-06's scenario needs exactly that) and must permit `values` to be omitted entirely.
    Default `value` and `visibilities` to a shape that is deliberately WRONG for "today" — the
    live-observed `[1,0,0,0,0,0,0]` with index 0 being the Day-2 tile — so any implementation that
    ever reads them fails a scenario rather than passing by luck.

    (e) `function heatRiskRoutes(overrides = {})` — mirroring `hazardsRoutes`. Route
    `HEATRISK_URL` to `overrides.heatRisk` when present, else a quiet default 200 carrying a
    complete, healthy 7-item identify body. Then route every ERO URL, every WSSI URL, every hazards
    layer URL and the `".lyr.geojson"` catch-all to a quiet empty-collection 200. Comment WR-02's
    trap by name: without routing every other product, an unrouted URL takes `installHttp`'s 503
    default, sets `anyStale`, and every `_stale` assertion in the scenario passes for a reason that
    has nothing to do with its subject.

    (f) Add `[HEATRISK_URL, okEmptyHeatRisk]` — a quiet, healthy HeatRisk identify 200 — to
    `eroHttpRoutes`, `wssiRoutes`, `hazardsRoutes` AND `advisoryRoutes`. This is the reciprocal of
    (e) and is the single easiest thing to miss in this plan: the moment a scenario enables all
    products, an unrouted HeatRisk URL 503s and corrupts a scenario about something else.

    (g) `function assertHeatRiskBlockIntact(out)` — the probe's own independent statement of the
    payload contract (WR-10). Assert `out.heatRisk` is an object with exactly the keys
    `day1..day7`; each value is an object with exactly the keys `category`, `text`, `color`;
    `category` is either `null` or an integer in `0..4`; `text` and `color` are strings; and when
    `category` is `null`, `text` and `color` are both `""`. Write the day count as a literal `7`
    here deliberately, with a comment: this is the probe stating the contract independently of the
    registry, so a change to `PRODUCT_REGISTRY.heatRisk.days` fails loudly here instead of silently
    agreeing with itself.
  </action>

  <verify>
    <automated>node scripts/probe-payload-resilience.js</automated>
  </verify>

  <acceptance_criteria>
    - `node scripts/probe-payload-resilience.js` pass count unchanged versus the 17-05 baseline (no
      scenarios added yet; this task is pure infrastructure).
    - `grep -c 'HEATRISK_URL' scripts/probe-payload-resilience.js` shows the constant declared once
      and referenced in at least five places: `heatRiskRoutes`, `eroHttpRoutes`, `wssiRoutes`,
      `hazardsRoutes`, `advisoryRoutes`.
    - `grep -n 'PRODUCT_REGISTRY.heatRisk.buildUrl' scripts/probe-payload-resilience.js` returns at
      least one line — the URL is derived, not hand-copied.
    - Infrastructure self-check, pasted into the SUMMARY: build a default `heatRiskRoutes()`,
      install it, run `getSpcOutlook` with all six product toggles ON, and assert the returned
      payload has `_stale` unset. If it is set, some URL is unrouted — find it by inspecting
      `helper._fetch.calls` against the route list and fix the route builder before proceeding.
      This one check is what prevents every scenario in 17-06 through 17-08 from being vacuous.
    - `heatRiskIdentifyResponse({ items: [a,b,c], values: ["1","2"] })` produces a body whose
      `properties.Values.length` is 2 and `catalogItems.features.length` is 3 — the builder can
      express D-06's condition. Assert this directly and record it. (This is the Phase 15 KMZ
      lesson: a fixture that cannot express its own condition makes its scenario vacuous.)
  </acceptance_criteria>

  <done>
    All seven helpers exist, all four pre-existing route builders quietly route the HeatRisk URL,
    the all-toggles-on smoke check produces no stray staleness, and the fixture builder can express
    a length mismatch.
  </done>
</task>

<task type="auto">
  <name>Task 2: HEAT-01/02/03 scenarios plus the shared-validator scenario</name>

  <files>scripts/probe-payload-resilience.js</files>

  <read_first>
    - scripts/probe-payload-resilience.js `:4939-5014` — `hazards-data-age-sets-the-badge-but-not-the-age-figure`
      IN FULL. This is the structural template: precondition guard, primary assertion, control
      assertion, and a `finally` restoring any stubbed global. Every scenario in this plan copies
      its shape.
    - scripts/probe-payload-resilience.js `:4880-4889` — the adjacent scenario's precondition guard
      that throws before its main assertion if setup did not produce the state under test.
    - scripts/probe-payload-resilience.js `:991` (`const scenarios = [`) and `:5075-5083` (`main()`'s
      iteration) — registration is a flat array literal; a new scenario is one more `{ name, run }`
      object appended, with no registration step elsewhere.
    - .planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/17-RESEARCH.md §"Phase Requirements
      → Scenario Map" — the proposed name and mutation for each scenario.
    - .planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/17-04-SUMMARY.md — the exact
      implementation lines each mutation must target.
  </read_first>

  <action>
    Append three scenarios to the `scenarios` array. Each carries a comment naming the decision or
    requirement it pins and the exact line its mutation breaks.

    1. `heatrisk-identify-body-survives-the-shared-fetch-validator`
       Pins 17-RESEARCH.md's headline finding: `fetchGeoJsonCached`'s pre-17-02 hardcoded
       `_isFeatureCollection` check would reject every HeatRisk response permanently, presenting as
       a silent fetch failure rather than an error.
       - Setup: `helper._products = { showHeatRisk: true }`, pinned clock, `heatRiskRoutes()` with a
         healthy 7-item body.
       - Precondition guard: assert `helper._fetch.calls` contains the HeatRisk identify URL after
         the run — if the fetch never happened, everything below observes nothing.
       - Primary assertion: at least one day has a numeric `category`, and `out._stale` is not set.
         A rejected body would leave all seven days `null` AND set `_stale` — note in the comment
         that the failure mode is a well-formed HTTP 200 with `{ data: null, failed: true }`, not a
         throw, which is exactly why asserting "the fetch succeeded" would not catch it.
       - Control assertion: re-run with the identify route returning a body missing
         `catalogItems` entirely, and assert all seven days are `null` and `_stale` is set —
         proving the validator does reject something and the primary assertion is not vacuous.
       - Mutation to prove RED: revert `fetchGeoJsonCached`'s two call sites to
         `this._isFeatureCollection(parsed.value)`.

    2. `heatrisk-day-order-follows-validtime-not-array-order`
       Pins HEAT-01/HEAT-02.
       - Fixture: seven catalog items in the live-observed OUT-OF-ORDER sequence
         `HeatRisk_2, HeatRisk_4, HeatRisk_5, HeatRisk_7, HeatRisk_1, HeatRisk_3, HeatRisk_6`
         (17-RESEARCH.md's live capture), with a DISTINCT category per day so a mis-attribution is
         observable — e.g. day N gets category `(N % 5)`. Give `Values` in the same array order as
         `items` so the zip is correct and only the SORT is under test.
       - Precondition guard: assert the fixture's `catalogItems.features[0].attributes.idp_validtime`
         is NOT the smallest of the seven — if the fixture happened to be pre-sorted, the scenario
         proves nothing. Throw with a diagnosable message if so.
       - Primary assertion: `out.heatRisk.dayN.category` equals the value belonging to the item
         whose `idp_validtime` is the Nth smallest, for all seven days. Assert specifically that
         `day1.category` is NOT equal to `Values[0]` — that inequality is the whole point, and the
         fixture must be constructed so those two differ.
       - Control assertion: `assertHeatRiskBlockIntact(out)` plus `out._stale` unset — proving the
         run was healthy and the day values came from a real evaluation, not from the seeded
         all-null block.
       - Mutation to prove RED: remove the sort in `_runHeatRiskProduct` and bucket by array index.

    3. `heatrisk-geometry-uses-mercator-not-raw-degrees`
       Pins HEAT-03.
       - Assertion operates on the URL actually issued: read `helper._fetch.calls[0].url` (find the
         identify call by substring), decode the `geometry` parameter, and assert
         `spatialReference.wkid === 102100`, `Math.abs(x) > 1e6` and `Math.abs(y) > 1e6` — Mercator
         metres, not degrees.
       - Precondition guard: assert exactly one identify call was issued; zero calls would make the
         assertion vacuous.
       - Control assertion: assert `Math.abs(x) !== Math.abs(PROBE_LON)` and
         `Math.abs(y) !== Math.abs(PROBE_LAT)`, so the test cannot pass on a coincidence of
         magnitudes.
       - Comment the real root cause correctly: a coordinate/spatialReference UNIT MISMATCH returns
         `"NoData"`, not `sr=4326` per se — a well-formed `sr=4326` request works. Do not restate
         STACK.md's superseded framing.
       - Mutation to prove RED: pass raw `lon`/`lat` into `row.buildUrl` instead of the
         `turf.toMercator` output.

    Every scenario must set `helper._nowMs = () => HEATRISK_NOW_MS` immediately after
    `resetHelper(helper)` and `resetLogs()`, and must restore any stubbed global in a `finally`.
  </action>

  <verify>
    <automated>node scripts/probe-payload-resilience.js</automated>
  </verify>

  <acceptance_criteria>
    - Full suite green, pass count = 17-05 baseline + 3.
    - INDIVIDUAL MUTATION PROOF (Phase 15 D-10, blocking) for each of the three scenarios. For each:
      apply the named mutation to the source, run the suite, confirm THAT scenario goes RED and
      the failure message is diagnosable (it names the field/day/value involved, not just
      "assertion failed"), then restore and confirm green. Paste the RED output verbatim for all
      three into the SUMMARY.
      A scenario that stays green under its own mutation is a fixture defect, not a pass — fix the
      fixture and re-prove. Do not proceed to 17-07 with an unproven scenario.
    - `git status` clean of every temporary mutation; `git diff` shows only
      `scripts/probe-payload-resilience.js` modified.
    - Each scenario's precondition guard is proven to be capable of firing: temporarily break the
      SETUP (not the implementation) so the guard's condition is violated, confirm the guard's own
      message appears, restore. Record for all three.
  </acceptance_criteria>

  <done>
    Three scenarios added; each individually mutation-proven RED with a diagnosable message and
    restored green; each precondition guard proven capable of firing.
  </done>
</task>

<task type="auto">
  <name>Task 3: HEAT-04 dedupe and D-06 precondition-guard scenarios</name>

  <files>scripts/probe-payload-resilience.js</files>

  <read_first>
    - scripts/probe-payload-resilience.js — the three scenarios added in Task 2, for the shape to
      follow.
    - .planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/17-RESEARCH.md Common Pitfalls #4
      (the dedupe rule, the carried-forward live duplicate example, and why `catalogItemVisibilities`
      is not the discriminator) and Open Questions #1 (no live duplicate was observable, so a
      synthetic fixture is the required evidence).
    - .planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/17-CONTEXT.md D-06 verbatim,
      including the two rejected alternatives.
    - .planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/17-04-SUMMARY.md — the shipped
      dedupe and guard lines.
  </read_first>

  <action>
    Append two more scenarios.

    4. `heatrisk-duplicate-validtime-keeps-latest-filedate`
       Pins HEAT-04.
       - Fixture: eight catalog items where TWO share one `idp_validtime` (mirroring STACK.md's
         2026-08-15 live capture of `HeatRisk_2`/`HeatRisk_3` both stamped for the same day), with
         DIFFERENT `idp_filedate` values and DIFFERENT categories in `Values`, so which one wins is
         observable. Set both duplicates' `catalogItemVisibilities` entries to `0` — this proves
         the implementation is not using visibility as the discriminator, which live evidence shows
         would be unreliable for a duplicate pair that is not also the globally-visible tile.
       - Precondition guard: assert the fixture actually contains a duplicate — compare
         `new Set(validtimes).size` against `validtimes.length` and throw with both numbers if they
         are equal. This is the exact Phase 15 KMZ lesson: a fixture that cannot express its own
         condition makes the scenario vacuous.
       - Primary assertion: the day the duplicate pair maps to carries the category belonging to the
         GREATER `idp_filedate`, and NOT the other. Assert both halves — equal to the winner AND
         not equal to the loser.
       - Control assertion: a different, non-duplicated day carries its own expected category, and
         the block has seven days — proving the dedupe collapsed only the duplicate and did not
         drop or shift everything.
       - Mutation to prove RED: change the dedupe tiebreak to "keep first" (or to `<` instead of
         `>`), which selects the older `idp_filedate`.

    5. `heatrisk-mismatched-values-length-abandons-poll`
       Pins D-06's precondition guard.
       - Fixture: seven catalog items, six `Values`.
       - Precondition guard: assert `body.properties.Values.length !== body.catalogItems.features.length`
         in the fixture itself before the run, throwing with both lengths.
       - Primary assertion: all seven days have `category === null` AND `out._stale === true` — the
         poll was abandoned, not truncated. Assert additionally that NO day carries a numeric
         category: a front-truncating zip would produce six populated days, which is the confidently
         wrong outcome D-06 rejected as strictly worse than showing nothing.
       - Control assertion: re-run with a matched 7/7 body and assert days ARE populated and
         `_stale` is unset — proving the all-null result above is the guard firing and not the
         harness simply never populating HeatRisk.
       - Comment both rejected alternatives with their reasons: zip-to-shorter silently
         mis-attributes every surviving pair; falling back to the top-level `value` as Day 1 is
         wrong because that field tracks `catalogItemVisibilities`, live-observed pointing at the
         Day-2 tile.
       - Mutation to prove RED: remove the length check from `_zipHeatRiskCatalog` so it zips to the
         shorter length.
  </action>

  <verify>
    <automated>node scripts/probe-payload-resilience.js</automated>
  </verify>

  <acceptance_criteria>
    - Full suite green, pass count = 17-05 baseline + 5.
    - INDIVIDUAL MUTATION PROOF for both new scenarios, RED with a diagnosable message, restored
      green. Paste both RED outputs verbatim.
    - Both precondition guards proven capable of firing (break the setup, see the guard's message,
      restore). Record both.
    - For scenario 4 specifically, prove the visibility-independence claim: temporarily set the
      duplicate pair's `catalogItemVisibilities` entries to `[1, 0]` and confirm the scenario still
      passes with the same winner — the implementation is deciding on `idp_filedate`, not
      visibility. Record the result.
    - `git status` clean of every temporary mutation.
  </acceptance_criteria>

  <done>
    Five `heatrisk-*` scenarios exist, all individually mutation-proven, all with firing-capable
    precondition guards and non-vacuous control assertions.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture -> _fetch seam -> real runner | Scenarios drive the real production path; a mis-routed URL silently substitutes `installHttp`'s 503 for the fixture |
| scenario assertion -> shipped behaviour | An assertion that observes nothing passes forever while proving nothing (Phase 15's 15-05 defect) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-20 | Repudiation (false confidence) | new probe scenarios | mitigate | Every scenario individually mutation-proven RED with a diagnosable message per Phase 15 D-10; every scenario carries a precondition guard proven capable of firing and a control assertion |
| T-17-21 | Tampering (vacuous fixture) | `heatRiskIdentifyResponse` | mitigate | Builder must be able to express a Values/features length mismatch and a duplicate `idp_validtime`; both capabilities asserted directly, not assumed (Phase 15's `adm-zip` alphabetical-sort lesson) |
| T-17-22 | Denial of Service (cross-contamination) | route builders | mitigate | The HeatRisk URL gets a quiet 200 default in all four pre-existing route builders, and `heatRiskRoutes` quiets every other product URL — WR-02's trap in both directions |
| T-17-01 | Tampering | hostile identify body | mitigate | The shared-validator scenario's control arm exercises a body missing `catalogItems` and asserts containment (no throw, all-null block, `_stale` set) |
| T-17-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero new packages; the probe harness runs with no network and no new dependency |
</threat_model>

<verification>
- Full probe suite green at 17-05 baseline + 5.
- Five verbatim RED mutation outputs recorded, one per scenario.
- Five precondition-guard firing proofs recorded.
- The all-toggles-on infrastructure smoke check shows no stray staleness.
</verification>

<success_criteria>
- `HEATRISK_URL`, `HEATRISK_NOW_MS`, `heatRiskCatalogItem`, `heatRiskIdentifyResponse`,
  `heatRiskRoutes` and `assertHeatRiskBlockIntact` exist.
- All four pre-existing route builders quietly route the HeatRisk identify URL.
- Five `heatrisk-*` scenarios exist covering HEAT-01, HEAT-02, HEAT-03, HEAT-04 and D-06.
- Every one is individually mutation-proven.
</success_criteria>

<output>
Create `.planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/17-06-SUMMARY.md` when done.
Record: the scenario names with their exact mutations, the five verbatim RED outputs, the five
guard-firing proofs, the visibility-independence result, and the before/after suite pass counts.
</output>
