---
phase: 11-stale-data-indicator
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - MMM-SPCOutlook.js
autonomous: true
requirements:
  - STALE-02
  - STALE-03
must_haves:
  truths:
    - "Both GET_SPC_DATA payload literals (initial start + setInterval callback) include updateInterval: this.config.updateInterval"
    - "When this.spcrisk._stale === true, a warning indicator is rendered at the top of the data-bearing wrapper"
    - "The indicator shows a relative last-fresh-fetch time (e.g. '12 minutes ago') derived from this.spcrisk._staleAsOf via moment(...).fromNow()"
    - "When _stale is false or omitted, no indicator is rendered"
    - "When _staleAsOf is missing or invalid, the indicator renders without a time suffix (no fabricated freshness)"
    - "When _staleAsOf is in the future (clock skew), the indicator coerces to 'just now' (no 'in X minutes' string)"
  artifacts:
    - path: "MMM-SPCOutlook.js"
      provides: "updateInterval in both socket payloads; stale indicator render in getDom else-branch"
      contains: "updateInterval: this.config.updateInterval"
  key_links:
    - from: "start() socket payload"
      to: "node_helper socketNotificationReceived"
      via: "GET_SPC_DATA payload includes updateInterval"
      pattern: "updateInterval:\\s*this\\.config\\.updateInterval"
    - from: "setInterval callback socket payload"
      to: "node_helper socketNotificationReceived"
      via: "GET_SPC_DATA payload includes updateInterval"
      pattern: "updateInterval:\\s*this\\.config\\.updateInterval"
    - from: "getDom else-branch"
      to: "this.spcrisk._stale / _staleAsOf"
      via: "if (this.spcrisk._stale) { ... wrapper.innerHTML += ... }"
      pattern: "this\\.spcrisk\\._stale"
---

<objective>
Implement STALE-02 (visible warning indicator) and STALE-03 (relative-time string) in the frontend, plus thread `updateInterval` through both `GET_SPC_DATA` socket payloads so Plan 01's backend fix is reachable in production.

Purpose: User can tell at a glance when displayed risk data is stale, with a relative "X minutes ago" string sourced from `_staleAsOf` via the vendored `moment` global. The indicator follows the existing inline-style colored-span pattern (`MD in effect.`) and lives at the top of the data-bearing else-branch so it composes cleanly with existing render branches.

Output: Updated `MMM-SPCOutlook.js` with two socket-payload edits (`start` + `setInterval` callback) and one `getDom` else-branch edit (stale indicator render with D-11/D-12 fallbacks).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-stale-data-indicator/11-CONTEXT.md
@.planning/phases/11-stale-data-indicator/11-PATTERNS.md
@MMM-SPCOutlook.js

<interfaces>
<!-- Current socket payloads (MMM-SPCOutlook.js:13, 15) -->
```js
this.sendSocketNotification("GET_SPC_DATA", { lat: this.config.lat, lon: this.config.lon, extended: this.config.extended });
setInterval(() => {this.sendSocketNotification("GET_SPC_DATA", { lat: this.config.lat, lon: this.config.lon, extended: this.config.extended });}, this.config.updateInterval * 60000);
```

<!-- Current else-branch entrypoint (MMM-SPCOutlook.js:68-75) -->
```js
} else {
  const dow = new Date().getDay();
  wrapper.innerHTML = "";
  if(this.mds) {
    for(const MD of this.mds){
      wrapper.innerHTML += "<span style=\"color: #0059E0\">" + MD + " in effect.</span><br/>"
    }
  }
```

<!-- Backend payload contract (already emitted by node_helper.js:617, 784, untouched by Plan 01) -->
<!-- this.spcrisk._stale: boolean | undefined -->
<!-- this.spcrisk._staleAsOf: number (Date.now() epoch ms) | undefined -->

<!-- moment global: vendored by MagicMirror², available without import. moment(epochMs).fromNow() returns "12 minutes ago" etc. -->

<!-- Defaults already include updateInterval: 60 (line 6) — no defaults change needed. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add updateInterval to both GET_SPC_DATA payloads (start + setInterval)</name>
  <files>MMM-SPCOutlook.js</files>
  <read_first>
    - MMM-SPCOutlook.js (the file being modified — read fully before editing)
    - .planning/phases/11-stale-data-indicator/11-CONTEXT.md (D-01 locks the payload contract)
    - .planning/phases/11-stale-data-indicator/11-PATTERNS.md (Cross-File Invariants section: both sites must change together)
  </read_first>
  <behavior>
    - The initial `start()` `GET_SPC_DATA` socket payload includes `updateInterval: this.config.updateInterval`.
    - The `setInterval` callback `GET_SPC_DATA` socket payload includes `updateInterval: this.config.updateInterval`.
    - The existing `lat`, `lon`, `extended` fields remain on both payloads.
    - The `setInterval` cadence (`this.config.updateInterval * 60000`) is unchanged.
    - `Module.register("MMM-SPCOutlook", { defaults: { ..., updateInterval: 60 } })` block is unchanged (already correct).
  </behavior>
  <action>
1. **Locate line 13** (`start` function — the immediate `sendSocketNotification` call). Replace the payload object literal so it ends with `, updateInterval: this.config.updateInterval }`:
   ```js
   this.sendSocketNotification("GET_SPC_DATA", { lat: this.config.lat, lon: this.config.lon, extended: this.config.extended, updateInterval: this.config.updateInterval });
   ```

2. **Locate line 15** (the `setInterval` callback — same file, same `start` function). Replace the payload object literal identically:
   ```js
   setInterval(() => {this.sendSocketNotification("GET_SPC_DATA", { lat: this.config.lat, lon: this.config.lon, extended: this.config.extended, updateInterval: this.config.updateInterval });}, this.config.updateInterval * 60000);
   ```

3. **Cross-File Invariant check** per PATTERNS.md: both payload sites must include `updateInterval` — if either is missed the bug returns intermittently. Verify both edits before saving.

4. **Do NOT touch** the `defaults` block (line 2–7) — `updateInterval: 60` is already present.

5. **Do NOT touch** anything inside `getDom` in this task — the indicator render is Task 2.
  </action>
  <verify>
    <automated>grep -F 'updateInterval: this.config.updateInterval' MMM-SPCOutlook.js | wc -l | grep -qE '^\s*2\s*$' &amp;&amp; node -e "const fs=require('fs'); const src=fs.readFileSync('MMM-SPCOutlook.js','utf8'); new Function(src.replace(/^\s*Module\.register/m,'(function(){})//Module.register')); console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'updateInterval: this.config.updateInterval' MMM-SPCOutlook.js | wc -l` returns `2` (both payload sites updated).
    - `grep -F 'lat: this.config.lat, lon: this.config.lon, extended: this.config.extended, updateInterval: this.config.updateInterval' MMM-SPCOutlook.js | wc -l` returns `2` (full target shape matches at both sites).
    - `grep -nE 'GET_SPC_DATA' MMM-SPCOutlook.js | wc -l` returns `2` (no new sends introduced; setInterval cadence unchanged).
    - File parses as JavaScript: `node --check MMM-SPCOutlook.js` exits 0.
    - The `defaults` block still contains `updateInterval: 60` (line ~6).
  </acceptance_criteria>
  <done>
    Both `GET_SPC_DATA` payload literals (initial in `start`, recurring in `setInterval`) include `updateInterval: this.config.updateInterval`; existing fields preserved; defaults unchanged; file parses.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Render stale indicator in getDom else-branch with D-11/D-12 fallbacks</name>
  <files>MMM-SPCOutlook.js</files>
  <read_first>
    - MMM-SPCOutlook.js (the file being modified — read fully before editing, especially the else-branch around line 68–75)
    - .planning/phases/11-stale-data-indicator/11-CONTEXT.md (D-05 through D-13 lock the visual style, position, fallbacks)
    - .planning/phases/11-stale-data-indicator/11-PATTERNS.md (Indicator render — analog is the `MD in effect` line; target shape provided verbatim)
  </read_first>
  <behavior>
    - When `this.spcrisk._stale === true` and `this.spcrisk._staleAsOf` is a valid epoch number, the wrapper's first content (in the data-bearing else-branch) is `<span style="color:#FFCC00">⚠ Stale — 12 minutes ago</span><br/>` (the relative time computed by `moment(asOf).fromNow()`).
    - When `this.spcrisk._stale === true` and `this.spcrisk._staleAsOf` is missing or not a finite number, the wrapper renders `<span style="color:#FFCC00">⚠ Stale</span><br/>` (no time suffix, per D-11).
    - When `this.spcrisk._stale === true` and `Date.now() - this.spcrisk._staleAsOf < 0` (clock skew, future timestamp), the wrapper renders `<span style="color:#FFCC00">⚠ Stale — just now</span><br/>` (per D-12 — no future-tense `moment` string).
    - When `this.spcrisk._stale === false` or `this.spcrisk._stale` is omitted, no indicator is rendered (per D-13 / ROADMAP success criterion 4).
    - The indicator appears only inside the data-bearing `else` branch — not during `Loading…`, `Error:`, or `No Severe Weather Risk` branches (per CONTEXT.md `<code_context>`).
    - The indicator appears BEFORE the `MD in effect` loop (top of the data-bearing render).
    - Existing `MD in effect` rendering, day rows, fire weather, and extended days are unchanged.
    - No `getStyles()` extension; no CSS file added (per D-08).
  </behavior>
  <action>
1. **Locate the data-bearing `else` branch** in `getDom` (currently lines 68–75 — confirm with `grep -n 'wrapper.innerHTML = ""' MMM-SPCOutlook.js`). The structure is:
   ```js
   } else {
     const dow = new Date().getDay();
     wrapper.innerHTML = "";
     if(this.mds) {
       for(const MD of this.mds){
         wrapper.innerHTML += "<span style=\"color: #0059E0\">" + MD + " in effect.</span><br/>"
       }
     }
   ```

2. **Insert the stale indicator block immediately after `wrapper.innerHTML = "";` and before the `if(this.mds)` block.** Use `+=` (the reset already happened on the line above). Final shape:
   ```js
   } else {
     const dow = new Date().getDay();
     wrapper.innerHTML = "";
     if (this.spcrisk._stale) {
       let staleSuffix = "";
       const asOf = this.spcrisk._staleAsOf;
       if (typeof asOf === "number" && isFinite(asOf)) {
         const delta = Date.now() - asOf;
         if (delta < 0) {
           staleSuffix = " — just now";
         } else {
           staleSuffix = " — " + moment(asOf).fromNow();
         }
       }
       wrapper.innerHTML += "<span style=\"color:#FFCC00\">⚠ Stale" + staleSuffix + "</span><br/>";
     }
     if(this.mds) {
       for(const MD of this.mds){
         wrapper.innerHTML += "<span style=\"color: #0059E0\">" + MD + " in effect.</span><br/>"
       }
     }
   ```

3. **Visual decisions (locked, do not deviate):**
   - Icon: `⚠` (Unicode U+26A0) per D-06.
   - Color: `#FFCC00` per D-07 (inline `style="color:#FFCC00"`, no space after colon — match the existing `style="color:#" + ...` pattern in this file rather than the `style="color: #0059E0"` MD pattern; either reads identically in browsers but `#FFCC00` is what CONTEXT.md specifies).
   - Text format: `⚠ Stale — 12 minutes ago` (icon + space + "Stale" + em-dash-with-spaces + relative time) per D-05. The em-dash character is `—` (U+2014), already specified in PATTERNS.md target shape.
   - Inline style only — do NOT add a CSS file or extend `getStyles()` (D-08).

4. **Edge-case handling (locked):**
   - Missing/invalid `_staleAsOf`: render `⚠ Stale` (no suffix). Per D-11.
   - Negative delta (future timestamp): render `⚠ Stale — just now`. Per D-12.
   - `_stale === false` or omitted: render nothing. Per D-13. The `if (this.spcrisk._stale)` guard handles this — falsy values short-circuit.
   - Use `isFinite(asOf)` rather than `!isNaN(asOf)` so `Infinity` is also rejected (defensive; matches "valid timestamp" intent of D-11).

5. **Do NOT touch** the early branches (`Loading…`, `Error:`, `No Severe Weather Risk`) — per CONTEXT.md they have no `_stale` to inspect and ROADMAP success criterion 4 is satisfied by placing the indicator only in the data-bearing else.

6. **Do NOT touch** `getStyles()` (lines 28–32) — `weather-icons` registration is unrelated and must stay intact.

7. **Do NOT introduce** template literals; this file uses string concatenation throughout. Match the existing style.
  </action>
  <verify>
    <automated>grep -F '⚠' MMM-SPCOutlook.js &amp;&amp; grep -F 'color:#FFCC00' MMM-SPCOutlook.js &amp;&amp; grep -F 'moment(asOf).fromNow()' MMM-SPCOutlook.js &amp;&amp; grep -F 'this.spcrisk._stale' MMM-SPCOutlook.js &amp;&amp; grep -F 'this.spcrisk._staleAsOf' MMM-SPCOutlook.js &amp;&amp; grep -F ' — just now' MMM-SPCOutlook.js &amp;&amp; node --check MMM-SPCOutlook.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F '⚠' MMM-SPCOutlook.js` exits 0 (Unicode warning icon present).
    - `grep -F 'color:#FFCC00' MMM-SPCOutlook.js` exits 0 (warning yellow inline style present).
    - `grep -F 'moment(asOf).fromNow()' MMM-SPCOutlook.js` exits 0 (relative-time call present).
    - `grep -F 'this.spcrisk._stale' MMM-SPCOutlook.js` exits 0 (guard present).
    - `grep -F 'this.spcrisk._staleAsOf' MMM-SPCOutlook.js` exits 0 (timestamp source consumed).
    - `grep -F ' — just now' MMM-SPCOutlook.js` exits 0 (D-12 clock-skew fallback present).
    - `grep -nE 'isFinite\(asOf\)' MMM-SPCOutlook.js` returns ≥ 1 line (D-11 invalid-timestamp guard present).
    - `grep -nE 'getStyles' MMM-SPCOutlook.js | wc -l` returns `1` (no new getStyles registration; the existing weather-icons one is unchanged).
    - `grep -F 'MMM-SPCOutlook.css' MMM-SPCOutlook.js` exits non-zero (no CSS file introduced — D-08).
    - The indicator block appears BEFORE `if(this.mds)` in source order: `awk '/wrapper.innerHTML = ""/{p=NR} /this.spcrisk._stale/{s=NR} /if\(this.mds\)/{m=NR} END{exit !(p<s && s<m)}' MMM-SPCOutlook.js` exits 0.
    - `node --check MMM-SPCOutlook.js` exits 0 (file parses).
  </acceptance_criteria>
  <done>
    Stale indicator renders inside the data-bearing else-branch only, before the `MD in effect` loop; uses `⚠` icon, `#FFCC00` inline color, and `moment(_staleAsOf).fromNow()` relative time; gracefully degrades to `⚠ Stale` on invalid/missing `_staleAsOf` and to `⚠ Stale — just now` on negative delta; renders nothing when `_stale` is falsy; no CSS file or new `getStyles` entry added; file parses.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| node_helper → frontend module (socket) | `SPC_DATA_RESULT` payload deserialized into `this.spcrisk`. Same-process IPC, no network exposure. |
| frontend → DOM (innerHTML) | New string content composed via concatenation written to `wrapper.innerHTML`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05 | Tampering / Injection | `wrapper.innerHTML +=` for stale indicator | accept | The indicator string is composed of static literals plus `moment(asOf).fromNow()` (a number-derived English string from a vendored library) and `staleSuffix` (only ` — just now` or moment's output). No user-controlled string is interpolated. The existing module already uses `innerHTML +=` extensively with backend-derived values; this plan does not widen that surface. |
| T-11-06 | Tampering | `_staleAsOf` value type | mitigate | `typeof asOf === "number" && isFinite(asOf)` guard rejects strings, NaN, Infinity, undefined before reaching `moment()`. Negative-delta branch handles future timestamps without invoking `moment` (which would otherwise produce a confusing "in X minutes" string). |
| T-11-07 | Denial of Service | Missing `moment` global | accept | `moment` is a vendored MagicMirror² global; absence indicates the host runtime is broken at a level this module cannot mitigate. No try/catch added — fail-loud is correct. |
| T-11-08 | Information Disclosure | Indicator content | accept | No PII; relative-time string is local-only display content. |

ASVS L1 disposition: Plan adds one read of two backend-emitted fields and one `innerHTML +=` write of a literal-mostly string. Existing module patterns dominate the threat surface; this plan does not introduce a new attack class.
</threat_model>

<verification>
- All grep acceptance criteria above pass.
- `node --check MMM-SPCOutlook.js` succeeds.
- Manual smoke (deferred to phase verification): with backend forced to emit `_stale: true, _staleAsOf: Date.now() - 12*60*1000`, the rendered DOM shows `⚠ Stale — 12 minutes ago` in `#FFCC00` at the top of the wrapper. With `_stale: false`, no indicator appears.
</verification>

<success_criteria>
- ROADMAP success criterion 2 satisfied: when backend reports `_stale === true`, a compact warning indicator is visible at the top of the module wrapper.
- ROADMAP success criterion 3 satisfied: the indicator displays a relative last-fresh-fetch time sourced from `_staleAsOf`.
- ROADMAP success criterion 4 satisfied: when `_stale === false` or omitted, no indicator renders.
- STALE-02 acceptance text satisfied: compact warning indicator at top of wrapper when `_stale === true`.
- STALE-03 acceptance text satisfied: relative last-fresh-fetch time via vendored `moment` global from `_staleAsOf`.
- Combined with Plan 01: `updateInterval` is threaded end-to-end so the stale window honors user configuration (success criterion 1).
</success_criteria>

<output>
After completion, create `.planning/phases/11-stale-data-indicator/11-02-SUMMARY.md` per the standard summary template.
</output>
