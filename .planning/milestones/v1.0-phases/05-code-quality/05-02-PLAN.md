---
phase: 05-code-quality
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - MMM-SPCOutlook.js
autonomous: true
requirements:
  - QUAL-02
  - QUAL-03
  - QUAL-04

must_haves:
  truths:
    - "All implicit globals in MMM-SPCOutlook.js are declared with const or let"
    - "console.log calls on lines 12 and 21 are replaced with Log.info"
    - "Commented-out code block on lines 92-94 is removed"
    - "No var declarations and no console. calls remain in MMM-SPCOutlook.js"
  artifacts:
    - path: "MMM-SPCOutlook.js"
      provides: "Clean variable declarations and MagicMirror-standard logging"
      contains: "Log.info"
  key_links:
    - from: "MMM-SPCOutlook.js getDom()"
      to: "Log.info"
      via: "replace console.log"
      pattern: "Log\\.info"
---

<objective>
Clean up MMM-SPCOutlook.js: fix four implicit global declarations (dowToText, dow, two probRiskHTML), replace two console.log calls with Log.info, and remove the commented-out code block on lines 92-94.

Purpose: QUAL-02, QUAL-03, QUAL-04 applied to the frontend file. MMM-SPCOutlook.js is fully independent of node_helper.js changes — this plan runs in Wave 1 parallel to Plan 01.
Output: MMM-SPCOutlook.js with zero implicit globals, zero var, zero console. calls, zero commented-out code.
</objective>

<execution_context>
@/home/kcreasey/.claude/get-shit-done/workflows/execute-plan.md
@/home/kcreasey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-code-quality/05-CONTEXT.md
@.planning/phases/05-code-quality/05-RESEARCH.md

<interfaces>
<!-- Complete inventory of all changes required in MMM-SPCOutlook.js. Sourced from 05-RESEARCH.md audit. -->

QUAL-02 implicit globals to fix (no keyword at all — add const or let):
| Line | Statement | Fix |
|------|-----------|-----|
| 35   | `dowToText = (day) => { ... }` inside getDom() | `const dowToText = (day) => { ... }` |
| 61   | `dow = new Date().getDay();` inside getDom() | `const dow` |
| 72   | `probRiskHTML = ""` (Day 1 block) | `let probRiskHTML = ""` |
| 83   | `probRiskHTML = ""` (Day 2 block) | `let probRiskHTML = ""` |

Note: Lines 72 and 83 are in separate if-blocks. Add `let` to each independently within its own block scope. Do NOT hoist a single `let probRiskHTML` to getDom() scope.

QUAL-03 commented-out code block to remove:
- Lines 92-94: commented-out day3 prob/sign display block

QUAL-04 console.log to replace:
| Line | Statement | Action |
|------|-----------|--------|
| 12   | `console.log("SPC-Outlook: GET_SPC_DATA - " + lat + "," + lon + "," + extended)` | → `Log.info(...)` |
| 21   | `console.log("SPC Outlook: SPC_DATA_RESULT Received - " + JSON.stringify(payload))` | → `Log.info(...)` |

Log is a MagicMirror global in MMM-SPCOutlook.js — no import needed.

MMM-SPCOutlook.js has no var declarations. Only implicit globals and console.log calls.

Pitfall: dowToText at line 35 is an arrow function assignment. Fix it to `const dowToText = (day) => { ... }` — do not change it to a regular function declaration.

Pitfall: Two probRiskHTML assignments in separate if-blocks. Adding `let` to each block-locally is correct. Do not hoist.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fix implicit globals, console calls, and commented code in MMM-SPCOutlook.js</name>
  <files>MMM-SPCOutlook.js</files>
  <action>
Apply three targeted fixes to MMM-SPCOutlook.js:

1. QUAL-02 — Add const/let to four implicit globals inside getDom():
   - Line 35: `dowToText = (day) => { ... }` → `const dowToText = (day) => { ... }` (keep arrow form)
   - Line 61: `dow = new Date().getDay();` → `const dow = new Date().getDay();`
   - Line 72 (Day 1 if-block): `probRiskHTML = ""` → `let probRiskHTML = ""`
   - Line 83 (Day 2 if-block): `probRiskHTML = ""` → `let probRiskHTML = ""`
   Both probRiskHTML fixes are independent — each gets its own `let` within its own block. Do not hoist.

2. QUAL-03 — Remove the commented-out code block at lines 92-94 (day3 prob/sign display). Keep any surrounding prose comments that describe intent.

3. QUAL-04 — Replace two console.log calls:
   - Line 12: `console.log("SPC-Outlook: GET_SPC_DATA - " + ...)` → `Log.info("SPC-Outlook: GET_SPC_DATA - " + ...)`
   - Line 21: `console.log("SPC Outlook: SPC_DATA_RESULT Received - " + ...)` → `Log.info("SPC Outlook: SPC_DATA_RESULT Received - " + ...)`
   Log is already available as a MagicMirror global — no import required.

No other changes. Do not touch any logic, layout, or variable that is already correct.
  </action>
  <verify>
    <automated>grep -n "console\." /home/kcreasey/OneDrive/Projects/weather/MMM-SPCOutlook/MMM-SPCOutlook.js; grep -n "\bvar\b" /home/kcreasey/OneDrive/Projects/weather/MMM-SPCOutlook/MMM-SPCOutlook.js</automated>
  </verify>
  <done>
- grep -n "console\." MMM-SPCOutlook.js returns no output
- grep -n "\bvar\b" MMM-SPCOutlook.js returns no output
- grep -n "Log.info" MMM-SPCOutlook.js shows 2 lines (the two converted calls)
- The day3 commented-out block (lines 92-94 approximately) is gone
- dowToText, dow, probRiskHTML all have const/let keyword
  </done>
</task>

</tasks>

<verification>
After completing this plan:

```bash
# Must return no output
grep -n "console\." /home/kcreasey/OneDrive/Projects/weather/MMM-SPCOutlook/MMM-SPCOutlook.js

# Must return no output
grep -n "\bvar\b" /home/kcreasey/OneDrive/Projects/weather/MMM-SPCOutlook/MMM-SPCOutlook.js

# Should show 2 Log.info lines (converted from console.log)
grep -n "Log\.info" /home/kcreasey/OneDrive/Projects/weather/MMM-SPCOutlook/MMM-SPCOutlook.js
```
</verification>

<success_criteria>
- Zero console. calls in MMM-SPCOutlook.js
- Zero var or implicit globals in MMM-SPCOutlook.js
- Commented-out day3 block removed
- Log.info used for the two converted calls
- No behavior changes
</success_criteria>

<output>
After completion, create `.planning/phases/05-code-quality/05-02-SUMMARY.md`
</output>
