---
phase: 05-code-quality
plan: "03"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - node_helper.js
autonomous: true
requirements:
  - QUAL-02
  - QUAL-03
  - QUAL-04

must_haves:
  truths:
    - "No var declarations remain in node_helper.js"
    - "No implicit globals remain in node_helper.js"
    - "All eight commented-out Log.info lines are removed from node_helper.js"
    - "console.error in the getSpcOutlook catch block is replaced with Log.error"
    - "The console.log MD trace in getMesoscaleDiscussion is removed entirely"
  artifacts:
    - path: "node_helper.js"
      provides: "Clean variable declarations throughout; no debugging artifacts"
      contains: "Log.error"
  key_links:
    - from: "getSpcOutlook() catch block"
      to: "Log.error"
      via: "replace console.error"
      pattern: "Log\\.error"
---

<objective>
Complete the node_helper.js cleanup pass: convert all remaining var and implicit global declarations to const/let, remove all eight commented-out Log.info lines, replace console.error with Log.error, and remove the noisy MD console.log trace.

Purpose: QUAL-02, QUAL-03, QUAL-04 applied to node_helper.js after Plan 01 has already eliminated the six Day1/Day2 var declarations via destructuring. This plan handles all remaining issues.
Output: node_helper.js with zero var, zero implicit globals, zero commented code lines, zero console. calls.
</objective>

<execution_context>
@/home/kcreasey/.claude/get-shit-done/workflows/execute-plan.md
@/home/kcreasey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-code-quality/05-CONTEXT.md
@.planning/phases/05-code-quality/05-RESEARCH.md
@.planning/phases/05-code-quality/05-01-SUMMARY.md

<interfaces>
<!-- Complete inventory of all remaining changes in node_helper.js after Plan 01 completes. Sourced from 05-RESEARCH.md audit. -->

QUAL-02 remaining var declarations (Plan 01 already eliminated the six Day1/Day2 vars):
| Line (approx) | Statement | Fix |
|----------------|-----------|-----|
| 159  | `var ActiveURL = "..."` in getMesoscaleDiscussion | `const ActiveURL` |
| 352  | `var day1RiskResult;` inside block | `let day1RiskResult` (declared before block, used outside) |
| 363  | `var day1Risk = ...` inside block | `const day1Risk` |
| 454  | `var day1ProbRisk = false;` | `const day1ProbRisk` |
| 463  | `var day2RiskResult;` inside block | `let day2RiskResult` |
| 474  | `var day2Risk = ...` inside block | `const day2Risk` |
| 565  | `var day2ProbRisk = false;` | `const day2ProbRisk` |
| 573  | `var day3RiskResult;` inside block | `let day3RiskResult` |
| 584  | `var day3Risk = ...` inside block | `const day3Risk` |
| 588  | `var day3ProbRisk;` | `let day3ProbRisk` |
| 724  | `var day4ProbRisk, day4Sign;` | `let day4ProbRisk, day4Sign` |
| 743  | `var day4Risk = ...` | `const day4Risk` |
| 746  | `var day5ProbRisk, day5Sign;` | `let day5ProbRisk, day5Sign` |
| 765  | `var day5Risk = ...` | `const day5Risk` |
| 768  | `var day6ProbRisk, day6Sign;` | `let day6ProbRisk, day6Sign` |
| 787  | `var day6Risk = ...` | `const day6Risk` |
| 790  | `var day7ProbRisk, day7Sign;` | `let day7ProbRisk, day7Sign` |
| 809  | `var day7Risk = ...` | `const day7Risk` |
| 812  | `var day8ProbRisk, day8Sign;` | `let day8ProbRisk, day8Sign` |
| 831  | `var day8Risk = ...` | `const day8Risk` |

QUAL-02 remaining implicit globals (no keyword at all):
| Line (approx) | Statement | Fix |
|----------------|-----------|-----|
| 167  | `MDArray = [];` in getMesoscaleDiscussion | `const MDArray = []` |
| 314  | `day1CatURL = "..."` | `const day1CatURL` |
| 315  | `day1TorURL = "..."` | `const day1TorURL` |
| 316  | `day1HailURL = "..."` | `const day1HailURL` |
| 317  | `day1WindURL = "..."` | `const day1WindURL` |
| 319  | `day2CatURL = "..."` | `const day2CatURL` |
| 320  | `day2TorURL = "..."` | `const day2TorURL` |
| 321  | `day2HailURL = "..."` | `const day2HailURL` |
| 322  | `day2WindURL = "..."` | `const day2WindURL` |
| 332  | `day3CatURL = "..."` | `const day3CatURL` |
| 333  | `day3ProbURL = "..."` | `const day3ProbURL` |
| 335  | `day4URL = "..."` | `const day4URL` |
| 336  | `day5URL = "..."` | `const day5URL` |
| 337  | `day6URL = "..."` | `const day6URL` |
| 338  | `day7URL = "..."` | `const day7URL` |
| 339  | `day8URL = "..."` | `const day8URL` |
| 342  | `loc = turf.point(...)` | `const loc` |
| 833  | `day48Risk = false;` | `let day48Risk` |

Note: The existing `const day1CigTorURL` through `day3CigUrl` lines (approx lines 324-330) already use const — do not touch those.

QUAL-03 commented-out code lines to remove (all in node_helper.js):
| Line | Content |
|------|---------|
| 31   | `//Log.info("SPC Outlook: GET_SPC_DATA GET")` |
| 33   | `//Log.info("SPC-Outlook - intermediate payload" + lat + " " + lon + " " + extended);` |
| 57   | `//Log.info("SPC-Outlook: " + ZIPper.readFile(entry))` |
| 63   | `//Log.info("SPC-Outlook: Parsed DOM" + doc);` |
| 65   | `//Log.info("SPC‑Outlook: Nodes –", JSON.stringify(MDS));` |
| 79   | `//Log.info(geojson);` |
| 299  | `//Log.info("SPC-Outlook: I'M IN")` |
| 300  | `//Log.info("SPC-Outlook: Day 4-8 extended - " + extended)` |

Prose comments to PRESERVE (not code — do not remove):
- Line 28: `// Called when the front-end (MMM-SPCOutlook.js) sends a socket notification`
- Line 76: `// Polygons`
- Line 182: `//Day3+ % => risk`
- Lines 229, 235, 244: HTTP response handling comments
- Day section headings (// Day 1, // Day 1 Torn, // Fire Weather Day 1, etc.)
- Line 296: `// Part B: sigComparator — fixes latent ReferenceError in extended mode`
- Line 723: `// Day 4 — PERF-02: single-pass extractPolygons...`

QUAL-04 console calls to fix:
| Line | Statement | Action |
|------|-----------|--------|
| 173  | `console.log("SPC-Outlook MD Test:" + ...)` in getMesoscaleDiscussion | Remove entirely |
| 914  | `console.error("Error fetching or parsing SPC data", err)` in getSpcOutlook catch | → `Log.error(...)` |

Log is already imported at the top of node_helper.js: `const Log = require("logger")`.

Pitfall: day1RiskResult, day2RiskResult, day3RiskResult are declared inside a block but used outside it. These must be `let` declared BEFORE the block. Do not use const for these.

Pitfall: day1Risk, day2Risk, day3Risk are assigned inside a block and also used outside. Check whether each is reassigned after the block. If only read after assignment inside the block, `const` inside the block works. If used outside the block, needs `let` before the block. Verify each case in the actual file before changing.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fix var/implicit globals in node_helper.js (QUAL-02)</name>
  <files>node_helper.js</files>
  <action>
Convert all remaining var declarations and implicit globals in node_helper.js to const or let. Plan 01 has already eliminated the six Day1/Day2 Tor/Hail/Wind var declarations (day1TorRisk, day1HailRisk, day1WindRisk, day2TorRisk, day2HailRisk, day2WindRisk) — do not look for those.

Apply every fix in the QUAL-02 inventory above. For each entry:
- Use `const` when the variable is never reassigned after its initial assignment
- Use `let` when the variable is mutated or assigned inside a block but used outside (these must be declared with `let` BEFORE the block)

Key scoping concern: day1RiskResult, day2RiskResult, day3RiskResult are assigned inside a brace block and read outside it. Hoist their `let` declaration to before the block:
```javascript
let day1RiskResult;
{
  // ... block that assigns day1RiskResult
}
// day1RiskResult read here — works with let, would fail with const inside the block
```

Also fix day1Risk, day2Risk, day3Risk — verify in the actual source whether they are referenced outside the block where they are assigned. Use `const` inside the block if they are only read after the block; if they are referenced outside, use `let` before the block.

For the URL constants (day1CatURL, day1TorURL, day1HailURL, day1WindURL, day2CatURL, day2TorURL, day2HailURL, day2WindURL, day3CatURL, day3ProbURL, day4URL–day8URL, loc): these are straightforward `const` additions.

For day48Risk at the bottom of getSpcOutlook: `let day48Risk = false;` — it is set conditionally based on the loop results, so `let` is correct.

Do NOT touch the CIG URL constants (day1CigTorURL, day1CigHailURL, day1CigWindURL, day2CigTorURL, day2CigHailURL, day2CigWindURL, day3CigUrl) — they already use const.
  </action>
  <verify>
    <automated>grep -n "\bvar\b" /home/kcreasey/OneDrive/Projects/weather/MMM-SPCOutlook/node_helper.js</automated>
  </verify>
  <done>
- grep -n "\bvar\b" node_helper.js returns no output
- Every URL variable (day1CatURL, etc.) is preceded by const
- day1RiskResult, day2RiskResult, day3RiskResult are let declarations before their blocks
- day48Risk is let declaration
- MDArray is const declaration
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Remove commented-out code and fix console calls in node_helper.js (QUAL-03, QUAL-04)</name>
  <files>node_helper.js</files>
  <action>
Two cleanups, both low-risk line deletions or one-word replacements:

QUAL-03 — Remove the eight commented-out Log.info lines listed in the inventory above (lines 31, 33, 57, 63, 65, 79, 299, 300 approximately). These are all of the form `//Log.info(...)`. Do NOT remove prose comments (section headings, intent descriptions). Check the preserve list in the interfaces block before removing any line.

QUAL-04 — Fix two console calls:
1. getMesoscaleDiscussion (~line 173): `console.log("SPC-Outlook MD Test:" + ...)` — remove this line entirely (per CONTEXT.md decision: noisy MD trace, not needed in production)
2. getSpcOutlook catch block (~line 914): `console.error("Error fetching or parsing SPC data", err)` → `Log.error("Error fetching or parsing SPC data", err)`

Log is already imported via `const Log = require("logger")` at the top of the file.
  </action>
  <verify>
    <automated>grep -n "console\." /home/kcreasey/OneDrive/Projects/weather/MMM-SPCOutlook/node_helper.js; grep -n "//Log\." /home/kcreasey/OneDrive/Projects/weather/MMM-SPCOutlook/node_helper.js</automated>
  </verify>
  <done>
- grep -n "console\." node_helper.js returns no output
- grep -n "//Log\." node_helper.js returns no output
- Log.error is present in the getSpcOutlook catch block
- The getMesoscaleDiscussion console.log line is gone
  </done>
</task>

</tasks>

<verification>
After completing this plan:

```bash
# Must return no output — all var gone
grep -n "\bvar\b" /home/kcreasey/OneDrive/Projects/weather/MMM-SPCOutlook/node_helper.js

# Must return no output — all console. gone
grep -n "console\." /home/kcreasey/OneDrive/Projects/weather/MMM-SPCOutlook/node_helper.js

# Must return no output — all commented-out Log.info gone
grep -n "//Log\." /home/kcreasey/OneDrive/Projects/weather/MMM-SPCOutlook/node_helper.js

# Should show Log.error in the catch block
grep -n "Log\.error" /home/kcreasey/OneDrive/Projects/weather/MMM-SPCOutlook/node_helper.js
```
</verification>

<success_criteria>
- Zero var declarations in node_helper.js
- Zero implicit globals in node_helper.js
- Eight commented-out Log.info lines removed
- console.error replaced with Log.error in catch block
- getMesoscaleDiscussion console.log trace removed
- Prose/section comments preserved
- No behavior changes
</success_criteria>

<output>
After completion, create `.planning/phases/05-code-quality/05-03-SUMMARY.md`
</output>
