---
phase: 05-code-quality
plan: "04"
type: execute
wave: 3
depends_on:
  - "05-02"
  - "05-03"
files_modified: []
autonomous: false
requirements:
  - QUAL-01
  - QUAL-02
  - QUAL-03
  - QUAL-04

must_haves:
  truths:
    - "All four QUAL requirements pass their grep verification commands"
    - "Human confirms no regressions in module output or behavior"
  artifacts:
    - path: "node_helper.js"
      provides: "Fully cleaned node_helper.js passing all grep checks"
      contains: "fetchAndEvaluateHazard"
    - path: "MMM-SPCOutlook.js"
      provides: "Fully cleaned MMM-SPCOutlook.js passing all grep checks"
      contains: "Log.info"
  key_links:
    - from: "All four QUAL grep checks"
      to: "Zero matches"
      via: "grep commands"
      pattern: "no console\\. or \\bvar\\b or //Log\\. remaining"
---

<objective>
Run all grep verification checks across both files and have the human confirm the module still works correctly after the refactor.

Purpose: Final quality gate confirming all QUAL requirements are met and no regressions were introduced.
Output: Verified, clean codebase ready for Phase 5 completion.
</objective>

<execution_context>
@/home/kcreasey/.claude/get-shit-done/workflows/execute-plan.md
@/home/kcreasey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-code-quality/05-01-SUMMARY.md
@.planning/phases/05-code-quality/05-02-SUMMARY.md
@.planning/phases/05-code-quality/05-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run all grep quality checks across both files</name>
  <files></files>
  <action>
Run the following grep checks. Each must return no output (zero matches) to pass. Report results clearly — list each check, show its output, and state PASS or FAIL.

```bash
# QUAL-02: No var declarations in node_helper.js
grep -n "\bvar\b" /home/kcreasey/OneDrive/Projects/weather/MMM-SPCOutlook/node_helper.js

# QUAL-02: No var declarations in MMM-SPCOutlook.js
grep -n "\bvar\b" /home/kcreasey/OneDrive/Projects/weather/MMM-SPCOutlook/MMM-SPCOutlook.js

# QUAL-04: No console. calls in node_helper.js
grep -n "console\." /home/kcreasey/OneDrive/Projects/weather/MMM-SPCOutlook/node_helper.js

# QUAL-04: No console. calls in MMM-SPCOutlook.js
grep -n "console\." /home/kcreasey/OneDrive/Projects/weather/MMM-SPCOutlook/MMM-SPCOutlook.js

# QUAL-03: No commented-out Log.info lines in node_helper.js
grep -n "//Log\." /home/kcreasey/OneDrive/Projects/weather/MMM-SPCOutlook/node_helper.js

# QUAL-01: fetchAndEvaluateHazard present with 7+ references
grep -c "fetchAndEvaluateHazard" /home/kcreasey/OneDrive/Projects/weather/MMM-SPCOutlook/node_helper.js
```

If any check fails, report the failing lines and note which plan needs a fix before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>grep -n "\bvar\b" /home/kcreasey/OneDrive/Projects/weather/MMM-SPCOutlook/node_helper.js /home/kcreasey/OneDrive/Projects/weather/MMM-SPCOutlook/MMM-SPCOutlook.js; grep -n "console\." /home/kcreasey/OneDrive/Projects/weather/MMM-SPCOutlook/node_helper.js /home/kcreasey/OneDrive/Projects/weather/MMM-SPCOutlook/MMM-SPCOutlook.js; grep -n "//Log\." /home/kcreasey/OneDrive/Projects/weather/MMM-SPCOutlook/node_helper.js</automated>
  </verify>
  <done>All grep checks return empty output. fetchAndEvaluateHazard count is 7 or more.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 5 code quality cleanup:
- Plan 01: fetchAndEvaluateHazard DRY refactor (6 duplicate blocks → 6 calls) + JSDoc on 8 functions in node_helper.js
- Plan 02: MMM-SPCOutlook.js implicit globals fixed + console.log → Log.info + commented block removed
- Plan 03: All var/implicit globals in node_helper.js fixed + 8 commented-out lines removed + console.error → Log.error
  </what-built>
  <how-to-verify>
1. Review node_helper.js: scroll to getSpcOutlook() and confirm the six Day1/Day2 hazard blocks have been replaced with fetchAndEvaluateHazard() calls
2. Review node_helper.js: confirm fetchAndEvaluateHazard is defined as a method (not inside getSpcOutlook) with JSDoc
3. Review MMM-SPCOutlook.js: confirm dowToText, dow, probRiskHTML have const/let keywords
4. Optional: start MagicMirror and confirm the module still displays risk data normally (no JS errors, display unchanged)
5. Confirm the grep checks in Task 1 all passed
  </how-to-verify>
  <resume-signal>Type "approved" if the cleanup looks correct, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
All four QUAL grep checks return zero matches. Human review confirms no regressions.
</verification>

<success_criteria>
- grep -n "\bvar\b" node_helper.js MMM-SPCOutlook.js returns no output (QUAL-02)
- grep -n "console\." node_helper.js MMM-SPCOutlook.js returns no output (QUAL-04)
- grep -n "//Log\." node_helper.js returns no output (QUAL-03)
- grep -c "fetchAndEvaluateHazard" node_helper.js returns 7 or more (QUAL-01)
- Human approves: module behavior unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/05-code-quality/05-04-SUMMARY.md`
</output>
