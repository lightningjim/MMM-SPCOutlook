---
phase: 07-fix-qual-residuals
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - node_helper.js
autonomous: true
requirements:
  - QUAL-02
  - QUAL-03

must_haves:
  truths:
    - "node_helper.js has no implicit globals in the production call path"
    - "The two dead prototype methods (evaluatePolygonsWeighted, evaluatePolygonsContinuous) are absent from node_helper.js"
    - "The three commented-out method bodies (checkDayCat, checkDayPerc, checkDaySign) are absent from node_helper.js"
    - "node_helper.js is syntactically valid JavaScript (module-closing `});` intact)"
  artifacts:
    - path: "node_helper.js"
      provides: "Cleaned production module — no implicit globals, no dead code"
      contains: "const result = turf.booleanPointInPolygon"
  key_links:
    - from: "evaluatePolygons forEach callback"
      to: "const result"
      via: "keyword insertion at line 104"
      pattern: "const result = turf\\.booleanPointInPolygon"
---

<objective>
Eliminate the three specific code quality defects remaining after Phase 5 in node_helper.js:
1. Implicit global `result` at line 104 inside evaluatePolygons (QUAL-02)
2. Dead prototype methods evaluatePolygonsWeighted and evaluatePolygonsContinuous with their JSDoc headers (QUAL-03)
3. Commented-out checkDayCat/checkDayPerc/checkDaySign blocks at the end of the module object (QUAL-03)

Purpose: Close QUAL-02 and QUAL-03, completing the code quality milestone. No logic, behavior, or output changes — pure deletion and one keyword insertion.

Output: node_helper.js with ~141 fewer lines, zero implicit globals in the production call path, and no dead/commented-out code blocks.
</objective>

<execution_context>
@/home/kcreasey/.claude/get-shit-done/workflows/execute-plan.md
@/home/kcreasey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Key code context the executor needs. Sourced from RESEARCH.md direct file read. -->

### evaluatePolygons (KEEP — live production method, only fix line 104)

```javascript
// Lines 95–115 (approximate, before deletion of dead methods)
// JSDoc for evaluatePolygons — KEEP
/**
 * ...
 */
evaluatePolygons(items, loc, comparator){
  let best = comparator.initial;
  items.forEach(({label, value, poly}) => {
    result = turf.booleanPointInPolygon(loc, poly);   // line 104 — FIX: add `const `
    if(result){
      best = comparator.comparator(best, value);
    }
  });
  return best;
},
```

### Dead methods to remove (lines 112–180, including JSDoc)

```
Lines 112–119:  JSDoc block for evaluatePolygonsWeighted  ← DELETE
Lines 120–147:  evaluatePolygonsWeighted method body       ← DELETE
Lines 149–156:  JSDoc block for evaluatePolygonsContinuous ← DELETE
Lines 157–180:  evaluatePolygonsContinuous method body     ← DELETE
```

### Module boundary — DO NOT DELETE line 968

```javascript
// Lines 893–968 (approximate, before deletion of dead methods):
    return false;
  },                              // ← end of checkInPolygon
                                  // ← blank line
//   checkDayCat(geojson, ...) {  // ← line 896: DELETE FROM HERE
//     ...
//   },
//   checkDayPerc(geojson, ...) {
//     ...
//   },
//   checkDaySign(geojson, ...) {
//     ...
//   return false;
// }                              // ← line 967: TO HERE (inclusive)
});                               // ← line 968: KEEP — NodeHelper.register() close
```

After all deletions the file should end:
```javascript
    return false;
  },

});
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Apply all three surgical edits to node_helper.js in one pass</name>
  <files>node_helper.js</files>
  <action>
Read the entire node_helper.js file first (all ~967 lines). Then apply all three edits in a single Write operation to avoid line-number drift between edits:

**Edit 1 — QUAL-02 fix (line 104):**
Find the line inside the `evaluatePolygons` forEach callback:
  `result = turf.booleanPointInPolygon(loc, poly);`
Change to:
  `const result = turf.booleanPointInPolygon(loc, poly);`
This is the ONLY change to evaluatePolygons. Do not touch the method's JSDoc (lines 95–100), its signature, or any other line.

**Edit 2 — QUAL-03: Remove evaluatePolygonsWeighted and evaluatePolygonsContinuous with their JSDoc headers:**
Locate and delete the contiguous block that includes:
- The `/** ... */` JSDoc comment block for `evaluatePolygonsWeighted`
- The `evaluatePolygonsWeighted(items, loc, comparator, transitionDistance = 30){ ... },` method body
- The `/** ... */` JSDoc comment block for `evaluatePolygonsContinuous`
- The `evaluatePolygonsContinuous(items, loc, comparator, transitionDistance = 30){ ... },` method body
Neither method is called anywhere. Delete from the start of the first JSDoc through the closing `,` of `evaluatePolygonsContinuous`. The line immediately following should be the `getSpcOutlook` method or similar live production code.

**Edit 3 — QUAL-03: Remove commented-out checkDayCat/checkDayPerc/checkDaySign blocks:**
These are at the end of the module object, after `checkInPolygon` closes. Locate by searching for `// checkDayCat` or `//   checkDayCat`. Delete from the blank line after `checkInPolygon`'s closing `,` through the last commented-out line (`// }` which is the commented-out closing brace of `checkDaySign`).

CRITICAL BOUNDARY: The very next line after the commented-out block is `});` — the closing of the `NodeHelper.register(module, { ... })` call. Do NOT delete this line. Verify before finalizing: the last line of the file should be `});`.

Apply all three edits by reading the file, constructing the corrected content, and writing it back in one Write operation.
  </action>
  <verify>
    <automated>grep -n 'result =' /home/kcreasey/OneDrive/Projects/weather/MMM-SPCOutlook/node_helper.js</automated>
  </verify>
  <done>
- Line 104 reads `const result = turf.booleanPointInPolygon(loc, poly);` (no bare `result =` without keyword)
- `evaluatePolygonsWeighted` and `evaluatePolygonsContinuous` are absent from the file
- The commented-out `checkDayCat/checkDayPerc/checkDaySign` blocks are absent
- The file ends with `});` (module close intact)
  </done>
</task>

<task type="auto">
  <name>Task 2: Run QUAL-02/QUAL-03 verification grep checks and syntax validation</name>
  <files></files>
  <action>
Run the Phase 5 verification grep commands to confirm all defects are eliminated. All commands must return the expected output (no output means the defect is gone).

Run each command from the project root (`/home/kcreasey/OneDrive/Projects/weather/MMM-SPCOutlook/`):

1. `grep -n '\bvar\b' node_helper.js`
   Expected: no output (no var declarations remain)

2. `grep -n 'result =' node_helper.js`
   Expected: only lines showing `const result =` (the fixed line 104 and the fetch call) — no bare `result =` without a keyword

3. `grep -n 'evaluatePolygonsWeighted\|evaluatePolygonsContinuous' node_helper.js`
   Expected: no output (dead methods removed)

4. `grep -n 'checkDayCat\|checkDayPerc\|checkDaySign' node_helper.js`
   Expected: no output (commented-out blocks removed)

5. `grep -n '//   ' node_helper.js`
   Expected: no output (all `//   ` triple-space commented lines gone with the deleted blocks)

6. `node --check node_helper.js`
   Expected: exits 0, no output (valid JavaScript syntax — confirms `});` module close is intact)

If any check fails, diagnose and fix before proceeding to summary.
  </action>
  <verify>
    <automated>cd /home/kcreasey/OneDrive/Projects/weather/MMM-SPCOutlook && grep -n '\bvar\b' node_helper.js; grep -n 'evaluatePolygonsWeighted\|evaluatePolygonsContinuous' node_helper.js; grep -n 'checkDayCat\|checkDayPerc\|checkDaySign' node_helper.js; node --check node_helper.js && echo "syntax OK"</automated>
  </verify>
  <done>
- `grep -n '\bvar\b'` returns no output
- `grep -n 'evaluatePolygonsWeighted\|evaluatePolygonsContinuous'` returns no output
- `grep -n 'checkDayCat\|checkDayPerc\|checkDaySign'` returns no output
- `node --check node_helper.js` exits 0 and prints "syntax OK"
- QUAL-02 and QUAL-03 are verifiably closed
  </done>
</task>

</tasks>

<verification>
All six grep/syntax checks from the Phase 5 verification pattern pass cleanly:
- No `var` declarations
- No bare `result =` (implicit global) in production call path
- No references to evaluatePolygonsWeighted or evaluatePolygonsContinuous
- No references to checkDayCat, checkDayPerc, checkDaySign
- No `//   ` triple-space commented code lines
- `node --check node_helper.js` exits 0
</verification>

<success_criteria>
1. `node_helper.js` line containing `result = turf.booleanPointInPolygon` now reads `const result = turf.booleanPointInPolygon` (QUAL-02 closed)
2. `evaluatePolygonsWeighted` and `evaluatePolygonsContinuous` methods and their JSDoc are absent (QUAL-03 closed)
3. `checkDayCat`, `checkDayPerc`, `checkDaySign` commented-out blocks are absent (QUAL-03 closed)
4. `node --check node_helper.js` exits 0 (module-closing `});` intact, file is valid JS)
5. File is approximately 826 lines (down from ~967)
</success_criteria>

<output>
After completion, create `.planning/phases/07-fix-qual-residuals/07-01-SUMMARY.md`
</output>
