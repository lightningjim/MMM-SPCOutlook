---
phase: 16-wpc-day-3-7-cpc-day-8-14-hazards-outlook
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - productRegistry.js
autonomous: true
requirements: [HAZ-01, HAZ-02, HAZ-03, HAZ-04, DATA-02]

must_haves:
  truths:
    - "The registry declares one hazardsOutlook row covering all six layers, so no second call site can construct a hazards URL (Pitfall 11, D-09 boundary integrity)"
    - "Flooding labels are excluded in code with no config key that can re-enable them (D-09)"
    - "Drought labels are named separately from excluded labels, so they can be gated rather than filtered (D-10)"
    - "Same-day co-occurring hazards have a deterministic declared order independent of ArcGIS response order (D-02)"
    - "D-12: Severe Weather is deliberately NOT in the excluded set — it renders verbatim this phase, with hazard-dimension suppression left to Phase 18 (D-12)"
    - "Every displayable hazard label has a hex color whose provenance URL is recorded in the row (D-08 precedent, 14-REVIEW IN-01)"
    - "A malformed day range fails at module load, not silently in the field (daySpanOf instinct)"
    - "D-14: The row declares its own 84-hour data-age threshold, sized to the live-confirmed Mon-Fri 17:00Z cadence so a normal weekend never false-alarms (D-13, D-14)"
  artifacts:
    - path: "productRegistry.js"
      provides: "HAZARDS_BASE_URL, hazardsOutlookLayers, hazardsExcludedLabels, hazardsDroughtLabels, hazardsOrder, hazardsDisplayColor, HAZARDS_DEFAULT_COLOR, dayRangeOf, PRODUCT_REGISTRY.hazardsOutlook"
      contains: "PRODUCT_REGISTRY.hazardsOutlook"
  key_links:
    - from: "PRODUCT_REGISTRY.hazardsOutlook.buildUrl"
      to: "buildArcGisQuery"
      via: "direct call, one call site"
      pattern: "buildArcGisQuery\\(HAZARDS_BASE_URL"
    - from: "PRODUCT_REGISTRY.hazardsOutlook.toValue"
      to: "f.properties.label"
      via: "lowercase read directly off the feature (HAZ-03)"
      pattern: "f\\.properties\\.label"
---

<objective>
Add the `hazardsOutlook` registry row and its supporting static constants to `productRegistry.js`,
introducing the third registry `kind` (`"arcgis-hazard-window"`) beside `"arcgis-day-layers"` and
`"kml-advisory"`.

Purpose: this row is the single owner of the product's vocabulary — its six layer ids, its
Flooding exclusion (D-09), its Drought label set (D-10), its same-day ordering (D-02), its
live-sourced palette (D-08 precedent), and its `maxDataAgeHours` (D-13/D-14). Every later plan in
this phase reads from it; nothing else in the phase may restate a layer id, a label, a hex, or a
day count.

Output: new constants + `dayRangeOf` validator + `PRODUCT_REGISTRY.hazardsOutlook` + updated
`module.exports`.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/buildomator/bm/4.5.5/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/buildomator/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-CONTEXT.md
@.planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-RESEARCH.md
@.planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-PATTERNS.md

<interfaces>
<!-- Contracts this plan must honor. Extracted from the codebase; use directly, do not explore. -->

productRegistry.js already exports:
  buildArcGisQuery(baseUrl, layerId) -> string
    Hardcodes f=geojson (non-overridable, Phase 14 D-09 / DATA-01) and restricts baseUrl to
    https://mapservices.weather.noaa.gov/. Requires Number.isInteger(layerId) && layerId >= 0.
  daySpanOf(dayLayers) -> number
    Asserts a contiguous 1..N map. Does NOT fit a [3,14] range; copy the instinct, not the code.
  MPD_FILENAME_PATTERN, PRODUCT_REGISTRY

node_helper.js consumes a row via:
  extractPolygons(data, toValue, includesFeat, url)
    calls toValue(label, f) where `label` is ALWAYS f.properties.LABEL (uppercase, SPC-only,
    hardcoded at node_helper.js:1005) — for this service it is always "".
  this._productToggles(products) keys toggles by each row's `configFlag`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the hazards static constants and the dayRangeOf load-time validator</name>
  <files>productRegistry.js</files>
  <read_first>
    - productRegistry.js (WHOLE FILE, 243 lines) — you need `buildArcGisQuery` (:24-32), the
      `daySpanOf` validator and its long WHY comment (:34-77), the `excessiveRain` row (:126-153),
      the `winterImpact` row and its `paletteSource` citation (:155-191), the pure-static-config
      comment above `spcMD` (:192-195), and the closing comment at :240-241 that reserves this slot.
    - .planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-RESEARCH.md
      § "Registry Kind & Row Design" and § "3. Colors — drawingInfo.renderer, live, all six layers"
      — the Colors table is the authority for every hex below.
    - .planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-PATTERNS.md
      § "productRegistry.js — new hazardsOutlook row" — the exact analog excerpts to copy.
  </read_first>
  <action>
    Insert, above the `PRODUCT_REGISTRY` object literal and after the existing WSSI constants, in
    2-space indent house style:

    1. `const HAZARDS_BASE_URL = "https://mapservices.weather.noaa.gov/vector/rest/services/hazards/cpc_weather_hazards/MapServer";`
       — same allowlisted host `buildArcGisQuery` already permits; no allowlist change needed.

    2. `const hazardsOutlookLayers` — an array of six objects, in this exact order, each with
       `id`, `group`, `dayRange`:
         { id: 1, group: "temperature",     dayRange: [3, 7] }
         { id: 4, group: "precipitation",   dayRange: [3, 7] }
         { id: 7, group: "wildfireDrought", dayRange: [3, 7] }
         { id: 3, group: "temperature",     dayRange: [8, 14] }
         { id: 6, group: "precipitation",   dayRange: [8, 14] }
         { id: 8, group: "wildfireDrought", dayRange: [8, 14] }
       Comment that `group` drives window-band-vs-day-bucket routing (HAZ-01/HAZ-02) and that
       `dayRange` is consumed ONLY by D-04's full-window guard on `precipitation` layers —
       `temperature` and `wildfireDrought` route to the window band unconditionally regardless of
       observed span (HAZ-02, and RESEARCH's live finding that Temperature spans are 1/4/5 days
       within one layer in one poll).

    3. `const hazardsExcludedLabels = ["Flooding Likely", "Flooding Occurring or Imminent", "Flooding Possible"];`
       — with a comment implementing D-09's stated requirement: these three labels originate from
       the **National Flood Outlook**, a separate NOAA product outside v2.0 scope that rides inside
       the Precipitation layers' attributes; rendering them would attribute another product's data
       to the Hazards Outlook. State in the comment that this is a **hard exclusion with no config
       override** and that it is an **interim measure with a known successor** (the National Flood
       Outlook is scoped as its own phase this milestone — CONTEXT.md Deferred Ideas).

    4. `const hazardsDroughtLabels = ["Severe Drought", "Rapid Onset Drought Risk"];`
       — comment: D-10, gated by `showDrought` (default false), NOT excluded. State explicitly that
       `"Critical Wildfire Risk"` is not drought and is never gated, because the Wildfire/Drought
       layers multiplex both families on the same `label` attribute.

    5. `const hazardsOrder` — the explicit same-day ordering list (D-02). Order:
       "Severe Weather", "Heavy Rain", "Heavy Precipitation", "Heavy Snow", "Freezing Rain",
       "Heavy Ice". Comment: registry-declared order exists because ArcGIS response order can flip
       between polls on an unchanged forecast, which reads as a change on a glanceable mirror;
       labels absent from this list sort AFTER every listed label, alphabetically among themselves.

    6. `const HAZARDS_DEFAULT_COLOR = "aaaaaa";` — the D-11 default style for a label that is
       neither excluded, gated, nor present in the display map. Comment that this is the same
       neutral grey the frontend's `fireRiskToColor[0]` already uses, so an unmapped label renders
       visibly in the module's existing no-risk grey rather than being dropped.

    7. `const hazardsDisplayColor` — a label -> 6-char lowercase hex string map (NO leading `#`,
       matching `eroTierToColor`/`wssiTierToColor` convention). Exactly these 15 entries, values
       taken verbatim from RESEARCH.md's Colors table:
         "Frost/Freeze": "c500ff", "Hazardous Heat": "a80000", "Hazardous Cold": "005ce6",
         "High Winds": "cdaa66", "Significant Waves": "ffd37f", "Freezing Rain": "ff00c5",
         "Heavy Precipitation": "00e6a9", "Heavy Rain": "267300", "Heavy Snow": "0084a8",
         "Severe Weather": "e69800", "Heavy Ice": "ff00c5", "Critical Wildfire Risk": "000000",
         "Excessive Heat": "a80000", "Much Above Normal Temperatures": "ff0000",
         "Much Below Normal Temperatures": "005ce6"
       Comment: (a) this map is NOT a filter — an absent label still renders verbatim in
       `HAZARDS_DEFAULT_COLOR` per D-11; (b) `Severe Weather` is present deliberately and renders
       rather than being suppressed (D-12 — the hazard-dimension taxonomy that could justify
       suppression is Phase 18's charter); (c) `Heavy Ice` shares `ff00c5` with `Freezing Rain` in
       WPC's own legend and that is not a transcription error; (d) Drought labels are intentionally
       absent because they are gated elsewhere.

    8. `function dayRangeOf([first, last])` — a sibling to `daySpanOf`, NOT a modification of it.
       Throws `new Error("productRegistry: dayRangeOf requires [first, last] integers with last > first, got " + JSON.stringify([first, last]))`
       unless `Number.isInteger(first) && Number.isInteger(last) && last > first`. Returns
       `[first, last]`. Carry a short comment explaining why `daySpanOf` cannot be reused: it
       asserts a contiguous `1..N` map and this product's grid is a `[3,14]` range — same
       "make the invalid state unrepresentable, throw at load time" instinct, different shape.
  </action>
  <verify>
    <automated>node -e "const r=require('./productRegistry.js'); const {dayRangeOf}=r; if(JSON.stringify(dayRangeOf([3,14]))!=='[3,14]') throw new Error('dayRangeOf([3,14]) did not round-trip'); let threw=false; try{dayRangeOf([14,3]);}catch(e){threw=/last > first/.test(e.message);} if(!threw) throw new Error('dayRangeOf accepted an inverted range'); threw=false; try{dayRangeOf([3.5,14]);}catch(e){threw=true;} if(!threw) throw new Error('dayRangeOf accepted a non-integer'); console.log('OK');"</automated>
    <automated>node -e "const s=require('fs').readFileSync('productRegistry.js','utf8'); const hexes=['c500ff','a80000','005ce6','cdaa66','ffd37f','ff00c5','00e6a9','267300','0084a8','e69800','000000','ff0000','aaaaaa']; for(const h of hexes){ if(!s.includes('\"'+h+'\"')) throw new Error('missing hex '+h); } for(const l of ['Flooding Likely','Flooding Occurring or Imminent','Flooding Possible','Severe Drought','Rapid Onset Drought Risk']){ if(!s.includes(l)) throw new Error('missing label '+l); } console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `require('./productRegistry.js').dayRangeOf` is exported and behaves per the automated check
      above (accepts `[3,14]`, throws on `[14,3]` and on `[3.5,14]`).
    - `daySpanOf` is unchanged — `git diff productRegistry.js` shows no edit inside the
      `function daySpanOf` body.
    - `hazardsOutlookLayers` has exactly 6 entries; the multiset of `id` values is exactly
      `{1,3,4,6,7,8}`; every entry's `group` is one of `temperature`/`precipitation`/`wildfireDrought`;
      every `dayRange` is `[3,7]` or `[8,14]`.
    - `hazardsExcludedLabels` has exactly 3 entries and does NOT contain any string matching
      `/Drought/`.
    - `hazardsDroughtLabels` has exactly 2 entries and does NOT contain `"Critical Wildfire Risk"`.
    - `hazardsDisplayColor` has exactly 15 keys; `"Severe Weather"` IS a key (D-12);
      `"Severe Drought"` and `"Rapid Onset Drought Risk"` are NOT keys; no value starts with `#`;
      every value matches `/^[0-9a-f]{6}$/`.
    - The comment attached to `hazardsExcludedLabels` contains the literal string
      `National Flood Outlook` and the word `interim`.
    - `HAZARDS_BASE_URL` begins with `https://mapservices.weather.noaa.gov/` (the host
      `buildArcGisQuery` already allowlists) — verified by
      `grep -n 'HAZARDS_BASE_URL = "https://mapservices.weather.noaa.gov/' productRegistry.js`
      returning one line.
  </acceptance_criteria>
  <done>All eight constants/functions exist, both automated checks print OK, and `daySpanOf` is byte-unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add PRODUCT_REGISTRY.hazardsOutlook and export dayRangeOf</name>
  <files>productRegistry.js</files>
  <read_first>
    - productRegistry.js (re-read only the region you just edited plus :126-243) — the
      `excessiveRain` row's field ordering, the `winterImpact` row's `paletteSource` comment block
      (:186-190), the "pure static configuration — no `this`, no network, no `require`" comment at
      :192-195, the closing "Future rows (Hazards Outlook, HeatRisk) land in Phases 16-17" comment
      at :240-241, and `module.exports` (:243).
    - .planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-RESEARCH.md
      § "Registry Kind & Row Design" (the proposed row shape) and § "Common Pitfalls" item 1
      (the `LABEL` vs `label` idiom).
  </read_first>
  <action>
    Add `hazardsOutlook` as the last row of the `PRODUCT_REGISTRY` object literal, replacing the
    "Future rows (Hazards Outlook, HeatRisk) land in Phases 16-17" comment with a narrowed version
    that now reserves only the HeatRisk slot for Phase 17.

    Row fields, in this order:
      id: "hazardsOutlook"
      kind: "arcgis-hazard-window"          // third kind, beside arcgis-day-layers and kml-advisory
      configFlag: "showHazardsOutlook"
      baseUrl: HAZARDS_BASE_URL
      layers: hazardsOutlookLayers
      dayRangeTotal: dayRangeOf([3, 14])    // derived through the validator, never restated
      excludedLabels: hazardsExcludedLabels
      droughtLabels: hazardsDroughtLabels
      order: hazardsOrder
      displayColor: hazardsDisplayColor
      defaultColor: HAZARDS_DEFAULT_COLOR
      buildUrl: (layerId) => buildArcGisQuery(HAZARDS_BASE_URL, layerId)
      toValue: (label, f) => (f && f.properties && typeof f.properties.label === "string" ? f.properties.label : "")
      maxDataAgeHours: 84
      paletteSource: "https://mapservices.weather.noaa.gov/vector/rest/services/hazards/cpc_weather_hazards/MapServer/{layerId}?f=json"

    Comments required on the row (this is the row that owns the product's whole vocabulary — an
    uncommented row here is the 14-REVIEW IN-01 failure repeating):

    - Above `kind`: state that this is the third `kind` and that a `node_helper.js` sibling runner
      dispatches on it (15 D-01 left this slot open deliberately).
    - Above `buildUrl`: note that its argument is a LAYER ID, not a day — the inverse of
      `excessiveRain.buildUrl(day)`. This product's day lives inside each feature's
      `start_date`/`end_date`, not in the URL. Every hazards URL must come from here (Pitfall 11,
      cache-key drift) — no inline `buildArcGisQuery` call at a second site.
    - Above `toValue`: the HAZ-03 / Pitfall 8 explanation. `extractPolygons` hardcodes
      `f.properties.LABEL` (uppercase, SPC-only, node_helper.js:1005), so the `label` positional
      parameter is ALWAYS `""` for this service. Read `f.properties.label` (lowercase) directly off
      `f` — the same idiom `eroDnToValue` and `wssiRawToValue` already use to route around this
      trap. State that a non-string `label` yields `""` so a malformed feature is dropped by
      `includesFeat` rather than rendering `undefined`.
    - Above `maxDataAgeHours`: D-13/D-14. 84 hours = Fri 17Z + 84h -> Mon 05Z, clearing a normal
      weekend with ~12h of slack before Monday's 17:00Z issuance and catching a mid-week stall
      within about a day. Live `serviceDescription` re-confirmed 2026-08-26: "Update Frequency:
      Daily Monday-Friday at 17:00Z". Known limitation: a federal-holiday Monday pushes the real
      gap to 96h, so the badge fires on roughly 10 days a year, where it is technically correct
      that nothing new has published. **Applied against each layer's OWN `idp_filedate`** — live
      evidence 2026-08-26 recorded three distinct filedates across the six layers in one poll, one
      ~23h staler than its siblings, so a single shared row-level timestamp does not exist.
    - Above `paletteSource`: D-08 precedent (`winterImpact` :186-190) and 14-REVIEW IN-01. State
      that every hex in `hazardsDisplayColor` was read from `drawingInfo.renderer.uniqueValueGroups[].classes[].symbol.color`
      on all six layers, live 2026-08-26, and that `{layerId}` in the URL is a placeholder for
      `1|3|4|6|7|8`. Note that this supersedes FEATURES.md's "PNG swatches only, LOW confidence"
      claim — no visual verification against wpc.ncep.noaa.gov is needed.
    - Note explicitly on the row that there is deliberately NO `includesFeat` here: D-10's Drought
      gate is request-scoped (`productToggles.showDrought`) and registry rows are pure static
      configuration with no `this` and no closure over request state (15 D-02), so the runner builds
      that closure at call time over `excludedLabels` + `droughtLabels`. Also note there is no
      `valueToTier`/`tierToText`/`tierToColor`: this product has no severity ladder anywhere in its
      schema (Pitfall 1), so a max-comparator is meaningless (D-02).

    Finally, update `module.exports` to
    `module.exports = { buildArcGisQuery, daySpanOf, dayRangeOf, MPD_FILENAME_PATTERN, PRODUCT_REGISTRY };`
  </action>
  <verify>
    <automated>node -e "const {PRODUCT_REGISTRY:R,buildArcGisQuery}=require('./productRegistry.js'); const r=R.hazardsOutlook; if(!r) throw new Error('row missing'); if(r.kind!=='arcgis-hazard-window') throw new Error('kind='+r.kind); if(r.configFlag!=='showHazardsOutlook') throw new Error('configFlag'); if(r.maxDataAgeHours!==84) throw new Error('maxDataAgeHours='+r.maxDataAgeHours); if(JSON.stringify(r.dayRangeTotal)!=='[3,14]') throw new Error('dayRangeTotal'); if(r.layers.length!==6) throw new Error('layers'); for(const L of r.layers){ const u=r.buildUrl(L.id); if(!u.includes('f=geojson')) throw new Error('no f=geojson in '+u); if(u!==buildArcGisQuery(r.baseUrl,L.id)) throw new Error('buildUrl diverged from buildArcGisQuery'); } if(r.includesFeat!==undefined) throw new Error('includesFeat must NOT be on the static row (15 D-02)'); if(typeof r.paletteSource!=='string'||!r.paletteSource.startsWith('https://mapservices.weather.noaa.gov/')) throw new Error('paletteSource'); console.log('OK');"</automated>
    <automated>node -e "const {PRODUCT_REGISTRY:R}=require('./productRegistry.js'); const t=R.hazardsOutlook.toValue; const f={properties:{label:'Heavy Rain',LABEL:'WRONG'}}; if(t('',f)!=='Heavy Rain') throw new Error('HAZ-03: lowercase label not read, got '+t('',f)); if(t('WRONG',f)!=='Heavy Rain') throw new Error('HAZ-03: toValue trusted the positional LABEL param'); if(t('',{properties:{}})!=='') throw new Error('missing label did not yield empty string'); if(t('',{properties:{label:42}})!=='') throw new Error('non-string label did not yield empty string'); console.log('OK');"</automated>
    <automated>node -e "const {PRODUCT_REGISTRY:R}=require('./productRegistry.js'); const ids=Object.values(R).map(r=>r.id); if(new Set(ids).size!==ids.length) throw new Error('duplicate row id'); const kinds=new Set(Object.values(R).map(r=>r.kind)); ['arcgis-day-layers','kml-advisory','arcgis-hazard-window'].forEach(k=>{if(!kinds.has(k)) throw new Error('missing kind '+k);}); console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - All three automated checks print OK.
    - `PRODUCT_REGISTRY.hazardsOutlook.toValue('WRONG', {properties:{label:'Heavy Rain', LABEL:'WRONG'}})`
      returns `"Heavy Rain"` — proving HAZ-03: the lowercase field wins and the uppercase positional
      parameter is ignored.
    - `PRODUCT_REGISTRY.hazardsOutlook.includesFeat` is `undefined` (15 D-02 — the Drought gate is
      request-scoped and cannot live on a static row).
    - The row has no `valueToTier`, `tierToText`, or `tierToColor` key.
    - `grep -n 'buildArcGisQuery(' productRegistry.js` shows the hazards call site exactly once
      (inside `hazardsOutlook.buildUrl`), and `grep -rn 'cpc_weather_hazards' --include=*.js .`
      (excluding `node_modules`) matches only `productRegistry.js` at this point in the phase.
    - The `maxDataAgeHours` comment contains the literal strings `84`, `Monday-Friday at 17:00Z`,
      and `idp_filedate`.
    - `module.exports` line contains `dayRangeOf`.
    - `node -e "require('./node_helper.js')"` still loads without throwing (the registry change did
      not break `_productToggles`, which iterates every row's `configFlag`).
  </acceptance_criteria>
  <done>`PRODUCT_REGISTRY.hazardsOutlook` exists with all fields above, all verify commands pass, and `node_helper.js` still loads.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NOAA MapServer -> node_helper fetch | Unauthenticated remote GeoJSON; every `label`, `start_date`, `end_date`, `idp_filedate` value crosses here untrusted |
| registry row -> URL construction | A row-supplied `baseUrl`/`layerId` becomes an outbound HTTP request |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Spoofing | `hazardsOutlook.buildUrl` outbound host | mitigate | `HAZARDS_BASE_URL` is `https://mapservices.weather.noaa.gov/...`; `buildArcGisQuery` (Phase 14 D-09) rejects any other host and hardcodes `f=geojson`. No new allowlist entry, no second construction site (verified by the `cpc_weather_hazards` grep acceptance criterion). |
| T-16-02 | Tampering | `toValue` reading `f.properties.label` | mitigate | `toValue` returns `""` for any non-string `label`, so a numeric/object/array `label` from a hostile response cannot propagate as a rendered value; downstream `includesFeat` drops `""`. |
| T-16-03 | Information Disclosure | `paletteSource` string | accept | A public NOAA service URL committed as a comment/value; no credentials, no PII. |
| T-16-04 | Denial of Service | `buildArcGisQuery` layer id | mitigate | All six ids are non-negative integer literals in the registry and pass `buildArcGisQuery`'s existing `Number.isInteger(layerId) && layerId >= 0` guard; no user-supplied layer id path exists. |
| T-16-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — this phase installs zero packages (RESEARCH.md § Package Legitimacy Audit). No install task exists in any Phase 16 plan. |
</threat_model>

<verification>
- `node -e "require('./productRegistry.js')"` exits 0 (the `dayRangeOf([3,14])` call runs at module
  load, so a bad range fails here, not in the field).
- `node -e "require('./node_helper.js')"` exits 0.
- `node scripts/probe-payload-resilience.js` still reports its existing 32 scenarios passing —
  this plan adds a registry row and must not perturb any existing product. Note
  `registry-day-span-is-derived-and-cannot-outrun-its-layer-map` and
  `harness-leak-check-resethelper-restores-every-seam` in particular.
</verification>

<success_criteria>
- `PRODUCT_REGISTRY.hazardsOutlook` exists with `kind: "arcgis-hazard-window"` and all fields
  enumerated in Task 2.
- HAZ-03's lowercase-`label` read is provably correct at the registry level (Task 2 verify #2).
- HAZ-04's Flooding exclusion and Drought label sets are declared, disjoint, and commented with
  their provenance.
- DATA-02's `maxDataAgeHours: 84` is declared with its Mon–Fri rationale and its per-layer
  application noted.
- Existing probe suite is green (32/32).
</success_criteria>

<output>
Create `.planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-01-SUMMARY.md` when done.
</output>
