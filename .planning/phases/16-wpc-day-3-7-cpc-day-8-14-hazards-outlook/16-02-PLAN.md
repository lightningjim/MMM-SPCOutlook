---
phase: 16-wpc-day-3-7-cpc-day-8-14-hazards-outlook
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - node_helper.js
autonomous: true
requirements: [HAZ-01, HAZ-02]

must_haves:
  truths:
    - "The current UTC day is obtained through one overridable seam, so a probe can advance the simulated clock without touching the system clock (enables the D-cache-drift probe)"
    - "A hazard feature's day offset is computed from its own UTC calendar date minus today's UTC date, with no local-timezone getter anywhere in the path (D-03)"
    - "A Precipitation feature spanning multiple days yields an entry for every day in its span (D-04)"
    - "A Precipitation feature whose span exactly aligns to its layer's nominal window is routed to the window band, not spread across days (D-04 guard)"
    - "Temperature and Wildfire features are routed to the window band with their own observed span regardless of span length (D-06, HAZ-02)"
    - "A point inside two overlapping hazard polygons yields both hazards, not one winner (D-02)"
    - "One feature whose geometry throws during containment does not discard its siblings (CR-02)"
  artifacts:
    - path: "node_helper.js"
      provides: "_nowMs, _todayUtcMs, _hazardDayOffset, _utcDateString, _isFullNominalWindow, _bucketHazardMatch, evaluatePolygonsCollectAll"
      contains: "evaluatePolygonsCollectAll"
  key_links:
    - from: "_todayUtcMs"
      to: "_nowMs"
      via: "seam indirection so probes can advance the clock"
      pattern: "this\\._nowMs\\(\\)"
    - from: "evaluatePolygonsCollectAll"
      to: "turf.booleanPointInPolygon"
      via: "per-item try/catch"
      pattern: "booleanPointInPolygon"
---

<objective>
Add the two genuinely new pieces of logic this phase needs — the collect-all point-in-polygon
evaluator and the UTC date-bucketing arithmetic — as pure, independently testable helpers on
`node_helper.js`, plus the `_nowMs()` clock seam they depend on.

Purpose: RESEARCH.md's "Don't Hand-Roll" table concludes this phase needs exactly two new
algorithms; everything else is reuse. Landing them alone, before the runner that orchestrates
them, keeps each verifiable in isolation and keeps the runner plan focused on the cache contract
that is the phase's highest risk.

Output: six new methods on the `node_helper` module object plus one new helper-global reset in
`start()`. No fetch, no dispatch, no payload assembly — those are 16-03.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/buildomator/bm/4.5.5/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/buildomator/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-CONTEXT.md
@.planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-RESEARCH.md
@.planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-PATTERNS.md

<interfaces>
<!-- Contracts this plan must honor. Extracted from the codebase; use directly, do not explore. -->

node_helper.js already provides:
  extractPolygons(data, toValue, includesFeat, url) -> [{ label, value, poly, feature }, ...]
    Comparator-agnostic. Screens geometry construction per feature (:1018-1030) and increments
    this._unusableFeatureCount on a construction failure. REUSED UNMODIFIED by this phase.
  evaluatePolygons(items, loc, comparator) -> best value      (node_helper.js:1042-1051)
    Max-reduce. NOT modified by this plan; a sibling is added beside it.
  this._unusableFeatureCount  (init node_helper.js:177, in start())
  Log.error("MMM-SPCOutlook <fn>: <what happened>", err)  — house logging style

scripts/probe-lib/module-stubs.js seam machinery:
  ORIGINAL_SEAMS.set(helper, { ...helper }) is captured BEFORE start(), and resetHelper() does
  Object.assign(helper, originals) then helper.start(). Any method a probe overwrites is therefore
  restored automatically — this is what makes _nowMs a safe seam with no harness change.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the _nowMs clock seam and the UTC date-bucketing helpers</name>
  <files>node_helper.js</files>
  <read_first>
    - node_helper.js `:150-215` — `start()`'s helper-global initialisation block (`_unusableFeatureCount`
      at :174-177, `_oldestStaleAt` at :177, `_products` at :178) and the `_productToggles` JSDoc
      style immediately after. You are adding one field to `start()` and methods in this style.
    - node_helper.js `:1290-1340` — `_isWithinStaleWindow` and `_noteStaleEntry`, so your new
      helpers sit in the same neighbourhood/style as the other small pure utilities.
    - .planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-RESEARCH.md
      § "Date Bucketing Algorithm (D-03/D-04)" — the reference implementation, and § "Standard Stack"'s
      date row (no date library on the backend; `moment` is a frontend-only MagicMirror global).
    - scripts/probe-lib/module-stubs.js `:203-240` — `loadNodeHelper`/`resetHelper`, to confirm your
      new seam is restored automatically and needs no harness edit.
  </read_first>
  <action>
    Add to `node_helper.js`, as sibling methods on the module object (2-space indent, JSDoc in the
    house style used by `_productToggles`):

    1. `_nowMs()` — returns `Date.now()`. One line of body. JSDoc must state: this exists as a
       **seam**, in the same spirit as `_fetch(url, options)`, because this product's day keys are
       derived by comparing a static remote date against a live clock. A probe cannot advance the
       system clock, so without this seam the day-offset-drift regression (the phase's highest-risk
       item) is untestable. `resetHelper` restores it automatically via `ORIGINAL_SEAMS`.

    2. `_todayUtcMs()` — `const d = new Date(this._nowMs()); return Date.UTC(d.getUTCFullYear(), d.getUTCMonth(), d.getUTCDate());`
       JSDoc must state: UTC midnight of the current day. **Only `getUTC*` getters and `Date.UTC`
       are permitted here.** Using `getFullYear()`/`getMonth()`/`getDate()` or `new Date(y, m, d)`
       would silently reintroduce a timezone bug on a Raspberry Pi whose system clock is not UTC —
       and there is no date library on the backend to enforce it, so this is enforced by review and
       by the grep gate in this task's acceptance criteria.

    3. `_hazardDayOffset(epochMs, todayUtcMs)` — returns
       `Math.round((epochMs - todayUtcMs) / MS_PER_DAY)` where `MS_PER_DAY` is a module-level
       `const MS_PER_DAY = 24 * 60 * 60 * 1000;` (add it near the other module-level constants at
       the top of the file if no equivalent already exists — check first, do not duplicate).
       JSDoc: every `start_date`/`end_date` observed live on 2026-08-26 across 32 features was
       exact UTC midnight, so the division is exact; `Math.round` is a defensive guard against a
       future WPC payload that is not midnight-aligned, not a workaround for a known case.

    4. `_utcDateString(epochMs)` — returns the `YYYY-MM-DD` UTC calendar date for an epoch-ms
       value, built from `getUTCFullYear()`/`getUTCMonth()+1`/`getUTCDate()` with zero padding.
       This is the `date` field D-01 requires on every day entry, and the `startDate`/`endDate`
       the window band carries (D-06). Returns `null` for a non-finite input.
       JSDoc: the frontend derives the weekday from this string rather than computing
       `dowToText(dow + N)`, because WPC's "Day N" boundary differs from SPC's and offset
       arithmetic drifts by one (Pitfall 9, D-01).

    5. `_isFullNominalWindow(offsetStart, offsetEnd, dayRange)` — returns
       `offsetStart === dayRange[0] && offsetEnd === dayRange[1]`.
       JSDoc must record the LOCKED reading of D-04: "full nominal window" means **exact offset
       alignment** to the layer's own `[first, last]` (`[3,7]` / `[8,14]`), NOT merely "same
       duration, any position". A 5-day feature positioned off-window is genuinely day-resolved
       information and belongs in the day rows. This resolution was made during planning against
       RESEARCH.md Open Question 1 / Assumption A2 — do not "fix" it into a duration check.

    Add to `start()`, beside `this._unusableFeatureCount` / `this._oldestStaleAt`:
       `this._loggedUnmappedHazardLabels = new Set();`
    with a comment: D-11 requires an unmapped label to render verbatim and be logged **once**; this
    Set is the once-per-process ledger. It is initialised in `start()` (not lazily) so
    `resetHelper` clears it between probe scenarios and a log assertion in one scenario cannot be
    satisfied by a previous scenario's entry.
  </action>
  <verify>
    <automated>node -e "const h=require('./node_helper.js'); h.start(); const T=Date.UTC(2026,7,26); if(h._todayUtcMs()%86400000!==0) throw new Error('_todayUtcMs is not midnight-aligned'); h._nowMs=()=>Date.UTC(2026,7,26,13,45); if(h._todayUtcMs()!==T) throw new Error('_todayUtcMs did not follow the _nowMs seam'); if(h._hazardDayOffset(Date.UTC(2026,7,29),T)!==3) throw new Error('offset for Aug29 vs Aug26 should be 3'); if(h._hazardDayOffset(Date.UTC(2026,8,2),T)!==7) throw new Error('offset for Sep2 should be 7'); if(h._hazardDayOffset(Date.UTC(2026,7,25),T)!==-1) throw new Error('yesterday should be -1'); if(h._utcDateString(Date.UTC(2026,7,29))!=='2026-08-29') throw new Error('_utcDateString: '+h._utcDateString(Date.UTC(2026,7,29))); if(h._utcDateString(Date.UTC(2026,0,5))!=='2026-01-05') throw new Error('_utcDateString zero-padding'); if(h._utcDateString(NaN)!==null) throw new Error('_utcDateString(NaN) should be null'); if(h._isFullNominalWindow(3,7,[3,7])!==true) throw new Error('exact window not detected'); if(h._isFullNominalWindow(4,8,[3,7])!==false) throw new Error('D-04 LOCKED: duration-only match must NOT count as full window'); if(h._isFullNominalWindow(8,14,[8,14])!==true) throw new Error('D8-14 exact window'); if(!(h._loggedUnmappedHazardLabels instanceof Set)) throw new Error('_loggedUnmappedHazardLabels not a Set'); console.log('OK');"</automated>
    <automated>bash -c 'set -e; SNIP=$(awk "/_todayUtcMs|_hazardDayOffset|_utcDateString|_isFullNominalWindow|_nowMs/,/^  },?$/" node_helper.js | grep -v "^\s*//" | grep -v "^\s*\*"); if echo "$SNIP" | grep -nE "getFullYear\(|getMonth\(|getDate\(|getHours\(|new Date\([0-9]" ; then echo "LOCAL-TIME GETTER IN THE UTC HELPERS"; exit 1; fi; echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - Both automated checks print OK.
    - Overriding `helper._nowMs` changes `helper._todayUtcMs()` — proving the seam is real and the
      day-offset drift probe in 16-07 is possible.
    - `_isFullNominalWindow(4, 8, [3,7])` returns `false` (the D-04 exact-alignment lock; a
      duration-only implementation returns `true` here and fails this check).
    - Grep gate: within the bodies of the five new date helpers, after stripping `//` and ` *`
      comment lines, there is no `getFullYear(`, `getMonth(`, `getDate(`, `getHours(`, or
      `new Date(<digit>` occurrence. (Comments are stripped before counting so explanatory prose
      naming the forbidden getters cannot self-invalidate the gate.)
    - `grep -c "require(\"moment\")\|require('moment')" node_helper.js` returns `0`.
    - `helper.start()` sets `_loggedUnmappedHazardLabels` to an empty `Set` — verified by
      `node -e "const h=require('./node_helper.js'); h.start(); h._loggedUnmappedHazardLabels.add('x'); h.start(); if(h._loggedUnmappedHazardLabels.size!==0) throw new Error('not reset by start()'); console.log('OK')"`.
    - `node scripts/probe-payload-resilience.js` still reports 32/32 passing.
  </acceptance_criteria>
  <done>All five helpers plus the `_loggedUnmappedHazardLabels` reset exist, both verify commands and the extra `start()` check print OK, and the existing probe suite is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add evaluatePolygonsCollectAll beside evaluatePolygons</name>
  <files>node_helper.js</files>
  <read_first>
    - node_helper.js `:990-1060` — `extractPolygons` (:997-1034, including its per-feature geometry
      try/catch at :1018-1030 and the `f.properties.LABEL` read at :1005) and `evaluatePolygons`
      (:1042-1051). You must not modify either.
    - node_helper.js `:570-590` — `_runKmlAdvisoryRow`'s per-candidate catch, the CR-02
      per-item-containment precedent this extends one level deeper.
    - .planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-RESEARCH.md
      § "Collect-All Evaluator" — the reference implementation and its rationale.
  </read_first>
  <action>
    Add `evaluatePolygonsCollectAll(items, loc)` immediately AFTER `evaluatePolygons` in
    `node_helper.js`. Do not modify `evaluatePolygons` — every other product's max-comparator
    usage must be bit-for-bit unaffected (D-02's consequence is a new sibling, not a replacement).

    Behaviour: iterate `items` (the `[{label, value, poly, feature}, ...]` shape `extractPolygons`
    returns), call `turf.booleanPointInPolygon(loc, item.poly)` inside a per-item `try/catch`, push
    the whole `item` onto a `hits` array when the call returns truthy, and return `hits`. On a
    caught throw: `Log.error("MMM-SPCOutlook evaluatePolygonsCollectAll: containment check failed for " + (item.label || item.value || "unlabeled feature"), err)`,
    increment `this._unusableFeatureCount`, and `return` from the callback (skip that item only).

    JSDoc must record why the try/catch is new here when `evaluatePolygons` never needed one:
    `extractPolygons` already screens geometry *construction*, but `turf.booleanPointInPolygon` can
    still throw independently (e.g. self-intersecting rings). Under `evaluatePolygons`'s
    single-value reduce an uncaught throw would have discarded the whole product's evaluation
    anyway; under collect-all it must not discard sibling hazards that share the same day (CR-02,
    extended one level — D-02 makes a day a set, so containment now needs per-item isolation).

    Also record that incrementing `_unusableFeatureCount` matters: `getSpcOutlook` samples that
    counter across a run (`node_helper.js` WR-08 site) and sets `anyStale` when it grew, so a
    dropped polygon surfaces as ⚠ rather than as a confident partial answer.
  </action>
  <verify>
    <automated>node -e "
const path=require('path');
const stubs=require('./scripts/probe-lib/module-stubs.js');
const {loadNodeHelper,resetHelper,turfStub,resetLogs,logCalls}=stubs;
const h=loadNodeHelper(); resetHelper(h); resetLogs();
const items=[{label:'a',value:'Heavy Rain',poly:{}},{label:'b',value:'Severe Weather',poly:{}},{label:'c',value:'Heavy Snow',poly:{}}];
turfStub.pointInPolygon=()=>true;
let hits=h.evaluatePolygonsCollectAll(items,{});
if(hits.length!==3) throw new Error('collect-all returned '+hits.length+' of 3 containing items');
if(hits.map(x=>x.value).join('|')!=='Heavy Rain|Severe Weather|Heavy Snow') throw new Error('collect-all did not preserve items');
const before=h._unusableFeatureCount;
let n=0;
turfStub.pointInPolygon=()=>{ n++; if(n===1) throw new Error('self-intersecting ring'); return true; };
hits=h.evaluatePolygonsCollectAll(items,{});
if(hits.length!==2) throw new Error('CR-02: a throwing first item discarded its siblings, got '+hits.length);
if(h._unusableFeatureCount!==before+1) throw new Error('_unusableFeatureCount not incremented on a contained throw');
turfStub.pointInPolygon=()=>false;
if(h.evaluatePolygonsCollectAll(items,{}).length!==0) throw new Error('control: a point outside every polygon must yield zero hits');
if(h.evaluatePolygonsCollectAll([],{}).length!==0) throw new Error('empty items must yield empty hits');
console.log('OK');"</automated>
    <automated>bash -c 'git diff -U0 node_helper.js | grep -E "^[-+]" | grep -v "^[-+][-+]" | grep -c "evaluatePolygons(items, loc, comparator)" | grep -qx 0 && echo OK || { echo "evaluatePolygons signature was modified"; exit 1; }'</automated>
  </verify>
  <acceptance_criteria>
    - The first automated check prints OK. It asserts four distinct properties: all containing
      items are returned (D-02, not a single winner); a throwing item is skipped while its siblings
      survive (CR-02); `_unusableFeatureCount` is incremented on that throw; and the **control** —
      a point outside every polygon returns zero hits, proving the positive assertions are not
      satisfied by the evaluator simply returning everything unconditionally.
    - `evaluatePolygons` is unmodified: `git diff node_helper.js` contains no changed line inside
      its body, and the second automated check passes.
    - `evaluatePolygonsCollectAll` returns the full `item` objects (with `.feature` intact), not
      just labels — 16-03 normalizes each hit into a `match` by reading
      `hit.feature.properties.start_date`, `.end_date` and `.idp_filedate` off them.
    - `node scripts/probe-payload-resilience.js` still reports 32/32 passing — in particular
      `ero-wellformed-slgt`, `wssi-wellformed-minor`, and
      `checkinpolygon-contains-unusable-geometry-per-feature`, which all exercise the untouched
      `evaluatePolygons` path.
  </acceptance_criteria>
  <done>`evaluatePolygonsCollectAll` exists beside an unmodified `evaluatePolygons`, both verify commands pass, and the existing probe suite is green.</done>
</task>

<task type="auto">
  <name>Task 3: Add the per-feature bucketing router</name>
  <files>node_helper.js</files>
  <read_first>
    - node_helper.js — the helpers you added in Task 1 and Task 2 (re-read only your own additions).
    - .planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-RESEARCH.md
      § "Date Bucketing Algorithm (D-03/D-04)" — `bucketPrecipitationFeature` and
      `bucketWindowFeature` reference implementations.
    - .planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-CONTEXT.md — D-04, D-06,
      D-07 verbatim.
  </read_first>
  <action>
    Add one method `_bucketHazardMatch(match, layer, todayUtcMs, dayBuckets, windowEntries)` to
    `node_helper.js`, immediately after the Task 1 date helpers. It routes a single **normalized
    match** and mutates the two accumulators in place. It performs NO fetching and NO ordering —
    ordering is 16-03's job.

    **The `match` shape is the phase's clock-independent unit** (16-03 builds it, caches it, and
    replays it on a cache hit):
      `{ label: string, startDate: number, endDate: number, idpFiledate: number|null }`
    where `startDate`/`endDate` are the feature's raw epoch-ms `start_date`/`end_date`. It carries
    **no day key and no offset** — that is the whole point: the cached unit must not contain a
    value whose correctness depends on an external clock. `_bucketHazardMatch` takes this shape
    rather than a raw `hit` so the fresh-fetch path and the cache-hit path feed the bucketing step
    through one identical input, making a "cache hit skipped re-bucketing" divergence
    unrepresentable rather than merely discouraged.

    Behaviour:
    - If `match` is falsy, or `typeof match.startDate !== "number"` or
      `typeof match.endDate !== "number"`, or either is not finite, `return` without throwing.
      Comment: a malformed match is contained here so its siblings' day and window entries survive
      (CR-02 lesson, extended to the bucketing step). Log nothing — a per-feature date miss is not
      actionable and would be noisy at 27 features/layer.
    - Compute `offsetStart = this._hazardDayOffset(match.startDate, todayUtcMs)` and
      `offsetEnd = this._hazardDayOffset(match.endDate, todayUtcMs)`. If `offsetEnd < offsetStart`,
      `return` (an inverted span from a malformed upstream feature — contained, not thrown).
    - Build the entry payload once:
      `{ label: match.label, startDate: this._utcDateString(match.startDate), endDate: this._utcDateString(match.endDate), offsetStart, offsetEnd }`.
    - Routing:
      * If `layer.group !== "precipitation"` (i.e. `temperature` or `wildfireDrought`): push the
        entry onto `windowEntries` and return. Comment: HAZ-02 — these layers carry no per-day
        resolution and route to the window band **unconditionally, regardless of observed span**.
        Live 2026-08-26 the Temperature layer alone produced 1-day, 4-day and 5-day spans in one
        poll, so any "these always span the whole window" assumption is empirically false (refines
        Pitfall 3). D-06: the entry carries its own observed span, never the layer's nominal window.
      * Else (`precipitation`): if `this._isFullNominalWindow(offsetStart, offsetEnd, layer.dayRange)`
        is true, push the entry onto `windowEntries` and return. Comment: D-04's guard — a
        Precipitation feature spanning its layer's entire nominal window is window-level
        information, and repeating it identically on every day would advertise a daily resolution
        the data does not have (Pitfall 3's spread failure). LOCKED reading: exact offset alignment
        to `[3,7]`/`[8,14]`, not duration-only.
      * Else: loop `for (let d = offsetStart; d <= offsetEnd; d++)`, skip any `d < 3 || d > 14`
        (outside this product's displayed grid), and push `{ label: hit.value }` onto
        `dayBuckets[d]` (creating the array if absent). **Clamp the loop bounds** —
        `for (let d = Math.max(offsetStart, 3); d <= Math.min(offsetEnd, 14); d++)` — rather than
        looping the raw span and skipping inside, so a hostile `[-1e9, 1e9]` span cannot produce an
        unbounded iteration (T-16-05). Comment: D-04 — a 2-day "Heavy Rain"
        renders on both days, because the day grid answers "is there a hazard on this day" and on
        day two the answer is yes.

    Do not dedupe or sort here — 16-03 owns that so the ordering rule (D-02) lives at one site.
  </action>
  <verify>
    <automated>node -e "
const h=require('./node_helper.js'); h.start();
const T=Date.UTC(2026,7,26);
const D=(y,m,d)=>Date.UTC(y,m,d);
const mk=(label,s,e)=>({label:label,startDate:s,endDate:e,idpFiledate:null});
const PRECIP37={id:4,group:'precipitation',dayRange:[3,7]};
const PRECIP814={id:6,group:'precipitation',dayRange:[8,14]};
const TEMP37={id:1,group:'temperature',dayRange:[3,7]};
const WD814={id:8,group:'wildfireDrought',dayRange:[8,14]};
let days={},win=[];
// D-04 spread: Aug29-Aug30 => offsets 3,4
h._bucketHazardMatch(mk('Heavy Rain',D(2026,7,29),D(2026,7,30)),PRECIP37,T,days,win);
if(!days[3]||!days[4]||days[5]) throw new Error('D-04 spread wrong: '+JSON.stringify(Object.keys(days)));
if(win.length!==0) throw new Error('a 2-day precip feature must not reach the window band');
// D-04 guard: exact [3,7] => window band, no day entries
days={};win=[];
h._bucketHazardMatch(mk('Heavy Rain',D(2026,7,29),D(2026,8,2)),PRECIP37,T,days,win);
if(Object.keys(days).length!==0) throw new Error('D-04 guard failed: full-window precip spread across days');
if(win.length!==1||win[0].offsetStart!==3||win[0].offsetEnd!==7) throw new Error('D-04 guard entry wrong: '+JSON.stringify(win));
if(win[0].startDate!=='2026-08-29'||win[0].endDate!=='2026-09-02') throw new Error('D-06 date strings wrong: '+JSON.stringify(win[0]));
// Off-window 5-day precip (offsets 4..8) stays in day rows (LOCKED exact-alignment reading)
days={};win=[];
h._bucketHazardMatch(mk('Heavy Snow',D(2026,7,30),D(2026,8,3)),PRECIP37,T,days,win);
if(win.length!==0) throw new Error('LOCKED D-04: a duration-only match was routed to the window band');
if(!days[4]||!days[8]||days[3]||days[9]) throw new Error('off-window precip day spread wrong: '+JSON.stringify(Object.keys(days)));
// HAZ-02: temperature always to the band, even a 1-day span
days={};win=[];
h._bucketHazardMatch(mk('High Winds',D(2026,7,29),D(2026,7,29)),TEMP37,T,days,win);
if(Object.keys(days).length!==0||win.length!==1) throw new Error('HAZ-02: a 1-day temperature feature did not route to the window band');
if(win[0].offsetStart!==3||win[0].offsetEnd!==3) throw new Error('D-06: observed span not preserved');
// HAZ-02: wildfire/drought always to the band
days={};win=[];
h._bucketHazardMatch(mk('Rapid Onset Drought Risk',D(2026,8,3),D(2026,8,9)),WD814,T,days,win);
if(Object.keys(days).length!==0||win.length!==1) throw new Error('HAZ-02: wildfireDrought did not route to the window band');
// Clamping: a precip span reaching past day 14 keeps only 3..14
days={};win=[];
h._bucketHazardMatch(mk('Heavy Rain',D(2026,8,7),D(2026,8,20)),PRECIP814,T,days,win);
const ks=Object.keys(days).map(Number).sort((a,b)=>a-b);
if(ks[0]!==12||ks[ks.length-1]!==14) throw new Error('grid clamp wrong: '+JSON.stringify(ks));
// Containment: malformed dates and inverted spans are skipped, not thrown
days={};win=[];
h._bucketHazardMatch({label:'X',startDate:'nope',endDate:1},PRECIP37,T,days,win);
h._bucketHazardMatch({label:'X'},PRECIP37,T,days,win);
h._bucketHazardMatch(null,PRECIP37,T,days,win);
h._bucketHazardMatch({label:'X',startDate:NaN,endDate:NaN},PRECIP37,T,days,win);
h._bucketHazardMatch(mk('X',D(2026,7,30),D(2026,7,29)),PRECIP37,T,days,win);
if(Object.keys(days).length!==0||win.length!==0) throw new Error('malformed features produced entries');
console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - The automated check prints OK. It independently pins: D-04 multi-day spread; D-04's
      full-window guard; the **LOCKED exact-alignment reading** (a 5-day off-window Precipitation
      feature stays in the day rows — a duration-only implementation fails this assertion);
      HAZ-02's unconditional window-band routing for both `temperature` and `wildfireDrought`
      including a 1-day span; D-06's observed-span and `YYYY-MM-DD` date strings; the 3..14 grid
      clamp; and four malformed-input containment cases.
    - `_bucketHazardMatch` never throws for any input in the check above.
    - T-16-05: `_bucketHazardMatch({label:'X', startDate: Date.UTC(1971,0,1), endDate: Date.UTC(2200,0,1)}, {id:4, group:'precipitation', dayRange:[3,7]}, Date.UTC(2026,7,26), days, win)`
      returns promptly and produces at most 12 day keys (3..14) — proving the loop header is
      clamped rather than filtered inside the body.
    - `_bucketHazardMatch` performs no sorting and no dedupe — the loop body contains no `sort(`
      and no reference to `row.order`.
    - `_bucketHazardMatch` never reads `.feature` or `.properties` — it consumes only the
      normalized `match` shape, which is what makes the cache-hit and fresh-fetch paths share one
      bucketing input (`grep -n "\.feature\|\.properties" ` inside its body returns nothing).
    - `node scripts/probe-payload-resilience.js` still reports 32/32 passing.
  </acceptance_criteria>
  <done>`_bucketHazardMatch` exists, the automated check prints OK, and the existing probe suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Remote GeoJSON feature -> bucketing helpers | `start_date`, `end_date`, `label` arrive untrusted and unvalidated from the MapServer |
| Remote geometry -> turf | Ring coordinates arrive untrusted; turf can throw on pathological input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-05 | Denial of Service | `_bucketHazardMatch` day loop | mitigate | The loop bounds are clamped to `Math.max(offsetStart, 3)` .. `Math.min(offsetEnd, 14)` at the loop header (mandated in the action), not filtered inside the body, so a hostile `[-1e9, 1e9]` span iterates at most 12 times. The inverted-span early return covers the reversed case. Verified by the T-16-05 assertion in this task's acceptance criteria. |
| T-16-06 | Denial of Service | `evaluatePolygonsCollectAll` | mitigate | Per-item `try/catch` around `turf.booleanPointInPolygon`; one pathological ring cannot abort the evaluation or the poll. Feature volume is bounded upstream by ArcGIS `maxRecordCount: 2000` and observed at 1–27 per layer. |
| T-16-07 | Tampering | `_utcDateString` / `_hazardDayOffset` | mitigate | Both return `null`/`NaN`-safe results for non-finite input; `_bucketHazardMatch` type-guards `start_date`/`end_date` as `number` before either is called, so a string or object date cannot reach the arithmetic. |
| T-16-08 | Repudiation | `_nowMs` seam | accept | A seam is overridable at runtime by anything already executing in-process; the module has no in-process adversary boundary (single trusted MagicMirror process). Same disposition the existing `_fetch` seam already carries. |
| T-16-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — zero packages installed in this phase (RESEARCH.md § Package Legitimacy Audit). |
</threat_model>

<verification>
- All three tasks' automated checks print OK.
- `node -e "require('./node_helper.js')"` exits 0.
- `node scripts/probe-payload-resilience.js` reports the pre-existing 32 scenarios passing with no
  new failures and no new skips. This plan adds only new methods; any existing-scenario regression
  means `evaluatePolygons` or `start()` was disturbed.
- Grep gate for the UTC discipline (comments stripped first, per Task 1 acceptance criteria).
</verification>

<success_criteria>
- `_nowMs`, `_todayUtcMs`, `_hazardDayOffset`, `_utcDateString`, `_isFullNominalWindow`,
  `_bucketHazardMatch`, `evaluatePolygonsCollectAll` all exist and behave per their checks.
- The D-04 exact-alignment lock is provable by a single assertion (`_isFullNominalWindow(4,8,[3,7]) === false`).
- The clock seam is provable by a single assertion (overriding `_nowMs` moves `_todayUtcMs`).
- No local-timezone `Date` getter exists in any new date helper.
- Existing probe suite green (32/32).
</success_criteria>

<output>
Create `.planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-02-SUMMARY.md` when done.
</output>
