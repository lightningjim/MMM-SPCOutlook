---
phase: 16-wpc-day-3-7-cpc-day-8-14-hazards-outlook
plan: 05
type: execute
wave: 4
depends_on: ["16-04"]
files_modified:
  - MMM-SPCOutlook.js
autonomous: true
requirements: [HAZ-01, HAZ-02, HAZ-03, HAZ-04]

must_haves:
  truths:
    - "A day with hazards renders one row naming that day's weekday, its offset, and every hazard on it (HAZ-01, D-01, D-02)"
    - "A day with no hazards renders nothing at all — no blank row, no 'None' row (ERO-03, 15 D-09)"
    - "Window-spanning hazards render once, in their own labeled region below the day rows, each carrying its own observed span (HAZ-02, D-05, D-06, D-07, D-08)"
    - "Every rendered hazard label passes through escapeHtml before reaching innerHTML (D-11, ASVS output encoding)"
    - "An unmapped label renders verbatim in the default style rather than being dropped (D-11)"
    - "A stale payload still renders hazards content (D-16)"
  artifacts:
    - path: "MMM-SPCOutlook.js"
      provides: "renderHazardsDays, renderHazardsWindowBand, both invoked from getDom's content branch"
      contains: "renderHazardsWindowBand"
  key_links:
    - from: "renderHazardsDays / renderHazardsWindowBand"
      to: "escapeHtml"
      via: "every label escaped before innerHTML concatenation"
      pattern: "escapeHtml\\("
    - from: "renderHazardsWindowBand"
      to: "day rows"
      via: "rendered strictly after them (D-05)"
      pattern: "renderHazardsWindowBand"
---

<objective>
Render the Hazards Outlook: a `day3`..`day14` grid of per-day rows, and — strictly below it — a
separately labeled window band for the hazards that carry no per-day resolution.

Purpose: this is where D-11's design decision (unmapped remote labels render verbatim) meets
`innerHTML`. `escapeHtml` is load-bearing here, not incidental — it is the phase's primary
injection surface and the reason the security enforcement gate applies to this plan.

Output: two renderer functions and their two call sites inside `getDom`'s content branch.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/buildomator/bm/4.5.5/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/buildomator/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-CONTEXT.md
@.planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-PATTERNS.md
@.planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-03-SUMMARY.md
@.planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-04-SUMMARY.md

<interfaces>
<!-- Payload contract (from 16-03) and the frontend helpers you must use. -->

this.spcrisk.hazardsOutlook = {
  day3..day14: { date: "YYYY-MM-DD", hazards: [ { label, color, mapped } ] },
  windowBand:  [ { label, color, mapped, startDate, endDate, offsetStart, offsetEnd } ]
}
// Colors are resolved BACKEND-side (D-01 precedent: carry raw + resolved data in the payload).
// `color` is a bare 6-char hex with no leading '#', matching block["day"+d+"Color"] convention.

MMM-SPCOutlook.js helpers (all in getDom's local scope — use, do not reimplement):
  escapeHtml(value)   :205-207  — String(value).replace(/[&<>"']/g, ...)
  dowToText(day)      :145-149  — index 0..13 into ["Sun".."Sat"], subtracts 7 when day >= 7
  wrapper.innerHTML   string concatenation only; the probe harness's `document` stub is a plain
                      object, so nothing parses what is assigned — assertions are on the markup
                      string.
  contentMarker       :335 — snapshot of wrapper.innerHTML taken after the ⚠ badge; the
                      "No Severe Weather Risk (unconfirmed)" tail at :456-458 compares against it.
                      Your renderers MUST run before that comparison so a stale hazards-only
                      payload does not render as a bare badge (D-16).

The probe harness's `moment` stub is `(_ts) => ({ fromNow: () => "PROBE_AGE" })` — it supports
ONLY `.fromNow()`. Do not use moment for weekday formatting; derive it from the payload's date
string with `new Date(dateStr + "T00:00:00Z").getUTCDay()` and feed `dowToText`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: renderHazardsDays — the day3..day14 grid</name>
  <files>MMM-SPCOutlook.js</files>
  <read_first>
    - MMM-SPCOutlook.js `:140-160` — `dowToText` (note its `if (day >= 7) day -= 7` behaviour) and
      `cigLabel`, for house style.
    - MMM-SPCOutlook.js `:200-210` — `escapeHtml` and its WR-12 comment explaining why remote text
      concatenated into `innerHTML` is escaped first.
    - MMM-SPCOutlook.js `:424-455` — `renderDayBlock` and its WR-08 comment, the closest same-role
      precedent, plus the `showExcessiveRain` / `showWinterImpact` call sites and the D-09 AMENDED
      comment on the WSSI one. Note that `renderDayBlock` does NOT escape — it renders
      registry-controlled tier text, not remote strings. Yours renders remote strings and must.
    - MMM-SPCOutlook.js `:330-340` and `:452-458` — where `contentMarker` is taken and where the
      "(unconfirmed)" tail compares against it, so you place your call site between them.
    - .planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-CONTEXT.md — D-01, D-02,
      D-11, D-16.
  </read_first>
  <action>
    Add `renderHazardsDays(block)` as a `const` arrow function in `getDom`'s local scope,
    immediately after the existing `renderDayBlock` definition.

    Add a module-scope-local constant beside it:
      `const HAZARDS_LABEL_MAX_CHARS = 60;`
    with a comment: the service imposes no length bound on `label`, and D-11 renders unmapped
    labels verbatim, so a malformed or hostile 1 MB string would otherwise become a 1 MB DOM node
    on a Raspberry Pi. Truncation is applied at the render boundary only — the payload keeps the
    full value for Phase 18's merge. Truncate with a trailing `…` when the label exceeds the bound,
    and truncate BEFORE escaping so the bound counts source characters, not entity expansions.

    Behaviour:
    - Guard: if `block` is not an object, return without rendering. Same version-skew tolerance as
      the 16-04 predicates (WR-07 — a throw here takes the whole render down).
    - Derive the day list from the block's OWN keys via a `/^day\d+$/` filter, numerically sorted
      ascending. Do NOT write a `3`..`14` literal loop: WR-08's rule is that no literal day count
      survives outside the registry, and `dayRangeTotal` is the registry knob.
    - For each day whose entry is an object with a non-empty `hazards` array:
      * Derive the weekday: `const dt = new Date(String(entry.date) + "T00:00:00Z");` and use
        `dowToText(dt.getUTCDay())` when `isFinite(dt.getTime())`, otherwise omit the weekday
        segment entirely rather than rendering `undefined` or `NaN`.
        Comment: D-01 — the weekday comes from the payload's resolved UTC date, NOT from
        `dowToText(dow + N)`. WPC's "Day N" boundary differs from SPC's, so offset arithmetic
        drifts by one (Pitfall 9); the backend already resolved the real date, so use it.
      * Build the row: `"Hazards (" + weekdaySegment + "Day " + d + "): "` followed by the day's
        hazards joined with `", "`, each rendered as
        `"<span style=\"color:#" + entry.hazards[i].color + "\">" + escapeHtml(truncated label) + "</span>"`,
        then `"<br/>"`.
        With a weekday the prefix reads `Hazards (Fri, Day 3): `; without one, `Hazards (Day 3): `.
      * If a hazard's `color` is not a string matching `/^[0-9a-fA-F]{6}$/`, substitute `aaaaaa`
        rather than interpolating it — an unvalidated value reaching `color:#...` is an attribute
        injection vector and also the `color:#undefined` class IN-08 already flagged. Comment it.
    - A day whose `hazards` array is empty renders NOTHING — no row, no "None". Comment: "absence
      is silence" (ERO-03, 15 D-09), applied per day.
    - Ordering within a row is the payload's array order, untouched: the backend already applied
      D-02's registry-declared order, and re-sorting here would put the ordering rule at two sites.
      Comment this.

    Call site: inside `getDom`'s content branch, after the `showWinterImpact` /
    `renderDayBlock("Winter Impact", ...)` call and BEFORE the `wrapper.innerHTML === contentMarker`
    "(unconfirmed)" check:
      `if (this.config.showHazardsOutlook) { renderHazardsDays(this.spcrisk.hazardsOutlook); }`
    Comment: gated on the same flag the no-risk gate terms use (WR-09 — gate and render must agree
    about what is displayable). Placed before the `contentMarker` comparison so a stale payload
    carrying real hazards renders its content and not a bare ⚠ badge (D-16, CR-01).
  </action>
  <verify>
    <automated>node -e "
const {loadFrontendModule,renderDom}=require('./scripts/probe-lib/module-stubs.js');
const f=loadFrontendModule();
function base(){const b={};for(let d=3;d<=14;d++)b['day'+d]={date:new Date(Date.UTC(2026,7,26+d)).toISOString().slice(0,10),hazards:[]};b.windowBand=[];return b;}
function payload(haz,stale){return{_stale:!!stale,_staleAsOf:stale?1:undefined,day48Risk:false,
 day1:{risk:'NONE',text:'',color:'',probRisk:0},day2:{risk:'NONE',text:'',color:'',probRisk:0},day3:{risk:'NONE',text:'',color:'',cig:0},
 day4:{},day5:{},day6:{},day7:{},day8:{},
 fireWeather:{day1Risk:0,day2Risk:0,day3Risk:0,day4Risk:0,day5Risk:0,day6Risk:0,day7Risk:0,day8Risk:0},
 excessiveRain:{},winterImpact:{},advisories:{spcMD:[],mpd:[]},hazardsOutlook:haz};}
const cfg=(on)=>({lat:1,lon:2,extended:false,updateInterval:60,proximityWeighting:false,showExcessiveRain:false,showWinterImpact:false,showMPD:false,showSPCMD:false,showHazardsOutlook:on,showDrought:false});
// HAZ-01 + D-01 weekday-from-date + D-02 order preserved
let b=base(); b.day3.date='2026-08-29'; b.day3.hazards=[{label:'Severe Weather',color:'e69800',mapped:true},{label:'Heavy Rain',color:'267300',mapped:true}];
let out=renderDom(f,{config:cfg(true),spcrisk:payload(b)});
if(!/Hazards \(Sat, Day 3\)/.test(out)) throw new Error('D-01: weekday not derived from the payload date. got: '+out);
if(out.indexOf('Severe Weather')>out.indexOf('Heavy Rain')) throw new Error('D-02: renderer re-ordered the payload array');
if(!out.includes('color:#e69800')) throw new Error('color not interpolated from the payload');
// ERO-03: empty days render nothing
if(/Day 4\)/.test(out)) throw new Error('absence-is-silence violated: an empty day rendered a row');
if((out.match(/Hazards \(/g)||[]).length!==1) throw new Error('more than one hazards day row for one populated day');
// D-11: unmapped label renders verbatim in the default style
b=base(); b.day5.hazards=[{label:'Frost/Freeze',color:'aaaaaa',mapped:false}];
out=renderDom(f,{config:cfg(true),spcrisk:payload(b)});
if(!out.includes('Frost/Freeze')) throw new Error('D-11: an unmapped label was dropped instead of rendered verbatim');
// ASVS output encoding: a hostile label must be escaped
b=base(); b.day6.hazards=[{label:'<img src=x onerror=alert(1)>',color:'267300',mapped:false}];
out=renderDom(f,{config:cfg(true),spcrisk:payload(b)});
if(/<img/.test(out)) throw new Error('XSS: an unescaped tag reached innerHTML');
if(!out.includes('&lt;img')) throw new Error('the hostile label was neither escaped nor present — it was silently dropped, which is a different bug');
// color validation
b=base(); b.day7.hazards=[{label:'X',color:'\" onload=\"alert(1)',mapped:false}];
out=renderDom(f,{config:cfg(true),spcrisk:payload(b)});
if(/onload/.test(out)) throw new Error('attribute injection through the color field');
if(!out.includes('color:#aaaaaa')) throw new Error('an invalid color did not fall back to aaaaaa');
b=base(); b.day7.hazards=[{label:'X',mapped:false}];
out=renderDom(f,{config:cfg(true),spcrisk:payload(b)});
if(/color:#undefined/.test(out)) throw new Error('IN-08 class: color:#undefined rendered');
// length bound
b=base(); b.day8.hazards=[{label:'A'.repeat(5000),color:'267300',mapped:false}];
out=renderDom(f,{config:cfg(true),spcrisk:payload(b)});
if(out.length>3000) throw new Error('label length bound not applied at the render boundary, output was '+out.length+' chars');
// D-16: a stale payload still renders hazards content
b=base(); b.day9.hazards=[{label:'Heavy Snow',color:'0084a8',mapped:true}];
out=renderDom(f,{config:cfg(true),spcrisk:payload(b,true)});
if(!out.includes('Heavy Snow')) throw new Error('D-16: a stale payload suppressed hazards content');
if(/No Severe Weather Risk \(unconfirmed\)/.test(out)) throw new Error('hazards rendered after the contentMarker comparison, so a stale hazards-only payload still showed (unconfirmed)');
// CONTROL: toggle off renders no hazards row at all
out=renderDom(f,{config:cfg(false),spcrisk:payload(b,true)});
if(/Heavy Snow/.test(out)) throw new Error('CONTROL FAILED: hazards rendered while showHazardsOutlook was false');
// version skew
renderDom(f,{config:cfg(true),spcrisk:payload(undefined)});
renderDom(f,{config:cfg(true),spcrisk:payload({day3:'junk',windowBand:5})});
b=base(); b.day10.date='not-a-date'; b.day10.hazards=[{label:'Heavy Rain',color:'267300',mapped:true}];
out=renderDom(f,{config:cfg(true),spcrisk:payload(b)});
if(/NaN|undefined/.test(out)) throw new Error('a bad date string leaked NaN/undefined into the row: '+out);
console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - The automated check prints OK. It pins, each independently: HAZ-01's row; D-01's
      weekday-derived-from-the-payload-date (`2026-08-29` is a Saturday — a `dowToText(dow + N)`
      implementation produces a different weekday and fails here); D-02's preserved order;
      ERO-03's absence-is-silence for empty days; D-11's verbatim unmapped label; **escaping of a
      hostile label**, with a companion assertion that the label was escaped rather than silently
      dropped; color-field attribute-injection rejection; the `color:#undefined` IN-08 class; the
      render-boundary length bound; D-16's stale-still-renders; a **control** proving the toggle-off
      path renders nothing; and three version-skew inputs that must not throw.
    - MUTATION PROOF (perform, record in SUMMARY, restore):
      * Mutation 1 — remove the `escapeHtml(` wrapper from the day-row label. Re-run. MUST fail
        with `XSS: an unescaped tag reached innerHTML`. Restore.
      * Mutation 2 — replace the weekday derivation with `dowToText(new Date().getDay() + d)`.
        Re-run. MUST fail on the `D-01: weekday not derived from the payload date` assertion.
        Restore.
      * Mutation 3 — render empty-`hazards` days as a row with an empty span. Re-run. MUST fail on
        `absence-is-silence violated`. Restore.
      Any mutation producing GREEN is a fixture defect — fix the fixture before proceeding.
    - `grep -c "for (let d = 3; d <= 14" MMM-SPCOutlook.js` returns `0` — the day span is derived
      from the block's own keys (WR-08), not written out.
    - `renderDayBlock`, `dayRiskCount`, `blockHasRisk` and `escapeHtml` are byte-unchanged.
    - `node scripts/probe-payload-resilience.js` still reports 32/32 passing.
  </acceptance_criteria>
  <done>`renderHazardsDays` exists and is called from the correct place, the automated check prints OK, all three mutations produced diagnosable RED and were restored, and the probe suite is green.</done>
</task>

<task type="auto">
  <name>Task 2: renderHazardsWindowBand — the labeled region below the day rows</name>
  <files>MMM-SPCOutlook.js</files>
  <read_first>
    - MMM-SPCOutlook.js `:336-363` — the advisory band loop, its D-05/D-07 comment block, the
      `escapeHtml(entry.label)` / `escapeHtml(entry.hazardType)` pattern, and the guard
      `if (!entry || typeof entry !== "object") continue;`. This is the shape to copy; the
      placement and the "in effect" wording are NOT.
    - MMM-SPCOutlook.js — your Task 1 `renderHazardsDays` and its call site.
    - .planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-CONTEXT.md — D-05, D-06,
      D-07, D-08 verbatim.
    - .planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-PATTERNS.md
      § "MMM-SPCOutlook.js — window band renderer".
  </read_first>
  <action>
    Add `renderHazardsWindowBand(block)` immediately after `renderHazardsDays`, and call it from
    `getDom` immediately AFTER the `renderHazardsDays` call site (still before the `contentMarker`
    comparison), inside the same `if (this.config.showHazardsOutlook)` block.

    Behaviour:
    - Guard: `block` not an object, or `block.windowBand` not an array, or the array is empty ->
      render nothing at all, not even the heading. Comment: absence is silence applies to the band
      as a whole (ERO-03, 15 D-09).
    - Skip any entry that is not an object, and any entry whose `offsetEnd` is a number less than
      `0` — a window that has entirely elapsed is not a forecast and rendering it would be worse
      than silence. Comment this as a deliberate rule, not an edge case.
    - Render a heading line ONCE, before the first surviving entry:
      `"Extended Hazards:<br/>"`. Comment D-05: this is its **own labeled region, below the day
      rows** — deliberately NOT folded into the advisory band 15 D-05 built. That band holds things
      in effect NOW (MDs/MPDs are 1–6h nowcasts) and its "in effect" wording does not apply to a
      5-to-7-day forecast window. Accepted cost: Phase 19 relocates two blocks rather than one.
      Emit the heading lazily (only when at least one entry survives the skips) so a band of
      entirely-elapsed entries renders nothing at all.
    - Per entry, one line, in this exact shape (D-08):
      `<StartWeekday>–<EndWeekday> (D<offsetStart>–<offsetEnd>): <colored, escaped label><br/>`
      e.g. `Thu–Mon (D3–7): Hazardous Heat`.
      * Weekdays come from `startDate`/`endDate` via
        `dowToText(new Date(String(v) + "T00:00:00Z").getUTCDay())`, same rule as Task 1. If either
        date is unparseable, omit the weekday pair and render only the `(D3–7)` segment rather than
        `NaN`.
      * Use the en dash `–` (U+2013) in both the weekday pair and the offset pair, matching the
        `D3–7` form used throughout ROADMAP/REQUIREMENTS.
      * If `offsetStart === offsetEnd`, render the single-day form: `Sat (D3): High Winds` — a
        1-day span was observed live on 2026-08-26 (`High Winds`, Aug29–Aug29), so this is a real
        case, not a hypothetical.
      * The label goes through `escapeHtml` and the same `HAZARDS_LABEL_MAX_CHARS` truncation and
        `/^[0-9a-fA-F]{6}$/` color validation Task 1 established. Factor those two into small
        shared helpers used by both renderers rather than duplicating them — WR-06's lesson is that
        a fix applied to one of a pair and not the other is this codebase's recurring defect shape.
    - Ordering is the payload array's order, untouched — the backend already applied D-07 (sorted
      by span start, ties broken by the registry order). Comment that re-sorting here would put the
      ordering rule at two sites.
    - Comment D-06 on the entry construction: the span rendered is the feature's **own observed
      span**, never the layer's nominal window. Live 2026-08-26, a D8–14 `Extreme Heat` feature
      spanned 2 days inside a nominal 7-day bucket; labeling it `D8–14` would advertise 7 days of
      heat where the data says 2.
  </action>
  <verify>
    <automated>node -e "
const {loadFrontendModule,renderDom}=require('./scripts/probe-lib/module-stubs.js');
const f=loadFrontendModule();
function base(){const b={};for(let d=3;d<=14;d++)b['day'+d]={date:new Date(Date.UTC(2026,7,26+d)).toISOString().slice(0,10),hazards:[]};b.windowBand=[];return b;}
function payload(haz){return{_stale:false,day48Risk:false,
 day1:{risk:'NONE',text:'',color:'',probRisk:0},day2:{risk:'NONE',text:'',color:'',probRisk:0},day3:{risk:'NONE',text:'',color:'',cig:0},
 day4:{},day5:{},day6:{},day7:{},day8:{},
 fireWeather:{day1Risk:0,day2Risk:0,day3Risk:0,day4Risk:0,day5Risk:0,day6Risk:0,day7Risk:0,day8Risk:0},
 excessiveRain:{},winterImpact:{},advisories:{spcMD:[],mpd:[]},hazardsOutlook:haz};}
const cfg=(on)=>({lat:1,lon:2,extended:false,updateInterval:60,proximityWeighting:false,showExcessiveRain:false,showWinterImpact:false,showMPD:false,showSPCMD:false,showHazardsOutlook:on,showDrought:false});
// HAZ-02 / D-08 / D-06: one entry, own observed span, weekday + offset
let b=base(); b.windowBand=[{label:'Hazardous Heat',color:'a80000',mapped:true,startDate:'2026-08-27',endDate:'2026-08-31',offsetStart:3,offsetEnd:7}];
let out=renderDom(f,{config:cfg(true),spcrisk:payload(b)});
if(!out.includes('Extended Hazards:')) throw new Error('D-05: the window band has no labeled heading');
if(!/Thu–Mon \(D3–7\): /.test(out)) throw new Error('D-08 wording wrong. got: '+out);
if(!out.includes('Hazardous Heat')) throw new Error('band label missing');
if(/in effect/.test(out)) throw new Error('D-05: the band reused the advisory band\\'s \"in effect\" wording');
if((out.match(/Hazardous Heat/g)||[]).length!==1) throw new Error('HAZ-02: the window hazard was repeated, not rendered once');
if(/Day 3\)|Day 4\)|Day 7\)/.test(out)) throw new Error('HAZ-02: a window hazard leaked into the day grid');
// D-05 ordering: band renders BELOW the day rows
b=base(); b.day3.date='2026-08-29'; b.day3.hazards=[{label:'Heavy Rain',color:'267300',mapped:true}];
b.windowBand=[{label:'High Winds',color:'cdaa66',mapped:true,startDate:'2026-08-29',endDate:'2026-08-29',offsetStart:3,offsetEnd:3}];
out=renderDom(f,{config:cfg(true),spcrisk:payload(b)});
if(out.indexOf('Heavy Rain')>out.indexOf('High Winds')) throw new Error('D-05: the window band rendered ABOVE the day rows');
if(!/Sat \(D3\): /.test(out)) throw new Error('single-day span form wrong. got: '+out);
// ERO-03: empty band renders nothing, not even the heading
b=base(); out=renderDom(f,{config:cfg(true),spcrisk:payload(b)});
if(/Extended Hazards/.test(out)) throw new Error('an empty windowBand rendered a heading');
// entirely-elapsed entries render nothing at all
b=base(); b.windowBand=[{label:'Old Heat',color:'a80000',mapped:true,startDate:'2026-08-20',endDate:'2026-08-24',offsetStart:-6,offsetEnd:-2}];
out=renderDom(f,{config:cfg(true),spcrisk:payload(b)});
if(/Extended Hazards|Old Heat/.test(out)) throw new Error('an entirely-elapsed window entry rendered');
// ASVS: escaping and color validation, same guarantees as the day rows (WR-06 pairing)
b=base(); b.windowBand=[{label:'<script>alert(1)</script>',color:'\" onload=\"x',mapped:false,startDate:'2026-08-29',endDate:'2026-08-30',offsetStart:3,offsetEnd:4}];
out=renderDom(f,{config:cfg(true),spcrisk:payload(b)});
if(/<script|onload/.test(out)) throw new Error('XSS/attribute injection in the window band');
if(!out.includes('&lt;script')) throw new Error('the hostile band label was dropped rather than escaped');
b=base(); b.windowBand=[{label:'B'.repeat(5000),color:'a80000',mapped:false,startDate:'2026-08-29',endDate:'2026-08-30',offsetStart:3,offsetEnd:4}];
out=renderDom(f,{config:cfg(true),spcrisk:payload(b)});
if(out.length>3000) throw new Error('band label length bound not applied, output was '+out.length+' chars');
// CONTROL: toggle off renders no band
b=base(); b.windowBand=[{label:'Hazardous Heat',color:'a80000',mapped:true,startDate:'2026-08-29',endDate:'2026-09-02',offsetStart:3,offsetEnd:7}];
out=renderDom(f,{config:cfg(false),spcrisk:payload(b)});
if(/Hazardous Heat|Extended Hazards/.test(out)) throw new Error('CONTROL FAILED: the band rendered while showHazardsOutlook was false');
// version skew
renderDom(f,{config:cfg(true),spcrisk:payload({windowBand:'junk'})});
renderDom(f,{config:cfg(true),spcrisk:payload({windowBand:[null,7,{}]})});
b=base(); b.windowBand=[{label:'Heat',color:'a80000',mapped:true,startDate:'nope',endDate:'nope',offsetStart:8,offsetEnd:14}];
out=renderDom(f,{config:cfg(true),spcrisk:payload(b)});
if(/NaN|undefined/.test(out)) throw new Error('unparseable band dates leaked NaN/undefined: '+out);
if(!/\(D8–14\)/.test(out)) throw new Error('the offset segment should survive an unparseable date');
console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - The automated check prints OK. It pins: HAZ-02 (one entry, rendered once, never repeated into
      the day grid); D-05 (own labeled heading, rendered strictly BELOW the day rows, no "in
      effect" wording); D-06/D-08 (the observed span in both weekday and offset form, plus the
      single-day form); ERO-03 (empty band renders nothing, not even the heading); the
      entirely-elapsed skip; escaping and color validation matching the day rows (WR-06's
      fix-both-twins rule); the length bound; a **control** for the toggle-off path; and four
      version-skew inputs.
    - MUTATION PROOF (perform, record in SUMMARY, restore):
      * Mutation 1 — move the `renderHazardsWindowBand` call ABOVE the `renderHazardsDays` call.
        Re-run. MUST fail with `D-05: the window band rendered ABOVE the day rows`. Restore.
      * Mutation 2 — remove the `escapeHtml(` wrapper from the band label. Re-run. MUST fail with
        `XSS/attribute injection in the window band`. Restore.
      * Mutation 3 — render the heading unconditionally rather than lazily. Re-run. MUST fail with
        `an empty windowBand rendered a heading`. Restore.
      Any mutation producing GREEN is a fixture defect — fix the fixture before proceeding.
    - The truncation helper and the color-validation helper are each defined ONCE and used by BOTH
      renderers — `grep -c` for each helper's definition returns `1`, and each appears in both
      renderer bodies (WR-06: a fix applied to one twin and not the other is this codebase's
      recurring defect shape).
    - The advisory band loop at `:350-363` is byte-unchanged.
    - `node scripts/probe-payload-resilience.js` still reports 32/32 passing.
  </acceptance_criteria>
  <done>`renderHazardsWindowBand` exists and renders below the day rows, the automated check prints OK, all three mutations produced diagnosable RED and were restored, and the probe suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Remote hazard `label` -> `wrapper.innerHTML` | **The phase's primary injection surface.** D-11 renders unmapped remote labels verbatim by design, so an unescaped label is markup |
| Payload `color` -> `style="color:#..."` | A payload-supplied string is interpolated into an HTML attribute |
| Payload `label` length -> DOM node size | The MapServer imposes no length bound on `label` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-18 | Tampering | Unescaped remote `label` reaching `innerHTML` in either renderer | mitigate | Every rendered label passes through `escapeHtml` (`MMM-SPCOutlook.js:205-207`) before concatenation, in BOTH renderers. Mutation-proven in both tasks (Mutation 2 in each), with a companion assertion that the escaped form is actually present so "dropped entirely" cannot masquerade as "escaped". This is the same control 15's WR-08 established as load-bearing for this codebase. |
| T-16-19 | Tampering | Attribute injection via the payload `color` field | mitigate | Both renderers validate `color` against `/^[0-9a-fA-F]{6}$/` and substitute `aaaaaa` on failure, so a `" onload="` payload cannot escape the `style` attribute. Pinned by an explicit assertion in each task. |
| T-16-20 | Denial of Service | Unbounded remote label length rendered on a Raspberry Pi | mitigate | `HAZARDS_LABEL_MAX_CHARS = 60` truncation at the render boundary, applied before escaping, in one shared helper used by both renderers. Pinned by a 5000-char assertion in each task. |
| T-16-21 | Denial of Service | A throw out of `getDom` taking the entire module render down | mitigate | Both renderers guard a missing/non-object block, a non-array `windowBand`, non-object entries, and unparseable date strings, returning quietly rather than throwing — the WR-07 failure mode ("advisories.mpd is not iterable" took the whole render with it). Pinned by the version-skew assertions in both tasks. |
| T-16-22 | Information Disclosure | Truncated label hides the tail of a real hazard name | accept | 60 characters exceeds every label in the MapServer's ~15-label legend (longest: `Much Above Normal Temperatures`, 30 chars), so truncation can only ever fire on a malformed or hostile value. Documented at the constant. |
| T-16-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — zero packages installed in this phase. |
</threat_model>

<verification>
- Both tasks' automated checks print OK.
- All six mutations (three per task) produce diagnosable RED and are restored; every RED message is
  recorded in the SUMMARY.
- `node scripts/probe-payload-resilience.js` reports 32/32 passing.
- `git diff MMM-SPCOutlook.js` shows no edit inside `escapeHtml`, `dowToText`, `renderDayBlock`,
  `dayRiskCount`, `blockHasRisk`, or the advisory band loop.
- **ROADMAP success criterion 4 note, carry forward to the verifier:** criterion 4 is verified at
  the shipped default (`showDrought: false`). Drought display is an explicit opt-in per D-10, not a
  violation. Flooding exclusion IS absolute (D-09).
</verification>

<success_criteria>
- HAZ-01: per-day rows render for Days 3–14 with weekday, offset and every hazard on that day.
- HAZ-02: window hazards render exactly once, in a labeled region below the day rows, each with its
  own observed span — never repeated across the days in that window.
- HAZ-03: labels reaching the DOM are the lowercase `label` values the backend resolved (verified
  end-to-end by 16-06's probe family).
- HAZ-04: no Flooding or Drought label can reach these renderers at the shipped default — they were
  filtered backend-side in 16-03; the renderers add no path that reintroduces them.
- D-11's verbatim unmapped label is visible AND escaped.
- Existing probe suite green (32/32).
</success_criteria>

<output>
Create `.planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-05-SUMMARY.md` when done.
Record the exact RED message produced by each of the six mutations, and the final rendered markup
for one populated day row and one window-band entry (input to 16-08's UAT script).
</output>
