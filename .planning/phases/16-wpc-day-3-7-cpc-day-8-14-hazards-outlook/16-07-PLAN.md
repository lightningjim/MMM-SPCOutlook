---
phase: 16-wpc-day-3-7-cpc-day-8-14-hazards-outlook
plan: 07
type: execute
wave: 6
depends_on: ["16-06"]
files_modified:
  - scripts/probe-payload-resilience.js
autonomous: true
requirements: [HAZ-02, DATA-02]

must_haves:
  truths:
    - "A cache hit on a poll one day later re-derives day keys against the new date instead of replaying the old assignment (HIGHEST-RISK ITEM 1)"
    - "No cache entry this product writes survives a date change carrying a day key (HIGHEST-RISK ITEM 1)"
    - "A window-band-only payload with an empty day grid never renders 'No Severe Weather Risk' (HIGHEST-RISK ITEM 2, HAZ-02)"
    - "A window hazard appears exactly once in a labeled band and never repeated across the days of its window (HAZ-02)"
    - "Each layer's freshness is judged against its own idp_filedate, so one stale layer among five fresh ones is caught (D-13, DATA-02)"
    - "A weekend poll serving Friday's file raises no false stale warning (DATA-02)"
    - "A data-age trip sets the stale badge without contributing to the 'N minutes ago' figure (D-15)"
  artifacts:
    - path: "scripts/probe-payload-resilience.js"
      provides: "hazards-day-keys-shift-on-a-cache-hit-when-the-day-advances, frontend-hazards-window-band-only-is-not-an-all-clear, and the hazards freshness scenario family"
      contains: "hazards-day-keys-shift-on-a-cache-hit-when-the-day-advances"
  key_links:
    - from: "the cache-drift scenario"
      to: "helper._nowMs"
      via: "advancing the simulated clock 24h between two polls with no upstream change"
      pattern: "_nowMs"
    - from: "the window-band gate scenario"
      to: "renderDom"
      via: "asserting at the DOM, not at the payload (CR-01's lesson)"
      pattern: "renderDom"
---

<objective>
Add the two scenarios that close this phase's two highest-risk regressions, plus the DATA-02
freshness family.

Purpose: both risks are documented in RESEARCH.md and flagged in PATTERNS.md as "an analog exists
but MUST NOT be copied verbatim", and both produce silent, confident, wrong output rather than an
error. Risk 1 (day-offset drift on a cache hit) is invisible to every existing cache-hit scenario
because those assert byte-identical output across a hit — the correct invariant for ERO, the exact
wrong one here. Risk 2 (the `getDom` no-risk short-circuit) already shipped as a live production
defect once, in Phase 15.

Output: two headline scenarios plus four freshness scenarios, all mutation-proven.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/buildomator/bm/4.5.5/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/buildomator/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-CONTEXT.md
@.planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-RESEARCH.md
@.planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-PATTERNS.md
@.planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-03-SUMMARY.md
@.planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-04-SUMMARY.md
@.planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-06-SUMMARY.md

<interfaces>
<!-- Contracts. From 16-06's helpers and the existing suite. -->

From 16-06 (Task 1):
  HAZARDS_URLS                      // { layerId: url }, derived from the registry
  hazardsRoutes(overrides)          // installHttp route list; unspecified layers answer 200-empty
  hazardsFeature({label, startDate, endDate, filedate})   // lowercase `label` only, no LABEL
  hazardsCollection(features)
  assertHazardsBlockIntact(out)

Existing suite:
  installHttp(helper, routes)                :663   // stubs helper._fetch, records calls+headers
  httpResponse({ status, body, text, etag }) :630
  loadFrontendModule / renderDom             module-stubs
  an-hour-old-reading-still-survives-a-hiccup-but-a-day-old-one-does-not  :3171
     — the precedent for advancing a clock by mutating cache state, and for a negative control
       keeping the fix from degrading into "serve any cached reading forever"
  ero-rejected-body-serves-last-known-good   :1457
     — the two-phase warm/degrade structure to COPY, whose ASSERTION must be INVERTED here
  frontend-advisory-only-is-not-an-all-clear :1999
     — the exact model for scenario 2, including its control assertion

From 16-02/16-03:
  helper._nowMs()  — the seam; restored by resetHelper via ORIGINAL_SEAMS
  helper._geoJsonCache — Map<url, { mode, etag, hash, result, timestamp }>; `result` for this
    product is the clock-independent match array
  PRODUCT_REGISTRY.hazardsOutlook.maxDataAgeHours === 84
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HIGHEST-RISK ITEM 1 — the day-offset-drift-on-a-cache-hit scenario</name>
  <files>scripts/probe-payload-resilience.js</files>
  <read_first>
    - scripts/probe-payload-resilience.js `:1450-1512` — `ero-rejected-body-serves-last-known-good`
      IN FULL. **Copy its warm/degrade two-phase structure; INVERT its assertion.** Its
      `out.excessiveRain.day1Risk !== "SLGT"` check asserts byte-identical output across a cache
      hit, which is correct for ERO and is precisely the wrong invariant for this product.
    - scripts/probe-payload-resilience.js `:3160-3280` — `an-hour-old-reading-still-survives-a-hiccup-but-a-day-old-one-does-not`
      IN FULL, especially its comment explaining why a real-elapsed-time test cannot observe the
      bug and its negative control at the end.
    - .planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-RESEARCH.md
      § "Newly identified pitfall: day-offset drift on a cache hit" — read it in full.
    - .planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-03-SUMMARY.md — how
      `_cacheHazardMatches` and the re-bucketing step were actually implemented.
  </read_first>
  <action>
    Add `hazards-day-keys-shift-on-a-cache-hit-when-the-day-advances`.

    Structure:
    1. `resetHelper(helper); resetLogs();` then pin
       `helper._nowMs = () => Date.UTC(2026, 7, 26, 13, 0);` — Wednesday Aug 26 2026, 13:00Z.
    2. Route layer 4 (D3–7 Precipitation) via `installHttp(helper, hazardsRoutes({...}))` to return
       a single `Heavy Rain` feature with `start_date`/`end_date` BOTH fixed at
       `Date.UTC(2026, 7, 29)` (Aug 29) — a genuinely static remote date — and a fresh
       `idp_filedate`. Serve it with a stable etag (`"hazards-v1"`).
    3. **Poll 1.** `const wed = await helper.getSpcOutlook(PROBE_LAT, PROBE_LON, false, { showHazardsOutlook: true });`
       with `turfStub.pointInPolygon = () => true`.
       Assert `wed.hazardsOutlook.day3.hazards.length === 1` and its label is `Heavy Rain`, and that
       `day2` does not exist as a key and `day4.hazards` is empty.
    4. **Precondition guard — the cache must actually be warm and clock-independent.**
       `const entry = helper._geoJsonCache.get(HAZARDS_URLS[4]);`
       Throw a named error if `entry` is missing. Then assert
       `!/day\d|offsetStart|offsetEnd|dayOffset/.test(JSON.stringify(entry.result))` with the
       message "the cached entry carries a clock-dependent field, so the drift assertion below
       cannot distinguish a correct re-bucketing from a lucky replay". Without this guard, the
       scenario passes whenever the cache silently failed to warm, which is exactly the vacuity mode
       that made Phase 15's 15-05 mutation yield zero RED scenarios.
    5. **Advance the clock 24h with NO upstream change.**
       `helper._nowMs = () => Date.UTC(2026, 7, 27, 13, 0);` — Thursday. Re-install the SAME route
       returning the SAME body and the SAME `"hazards-v1"` etag, so the fetch legitimately 304s /
       hash-hits and the runner takes the cache path. Do not change a single byte of the fixture.
       Comment: with this product's confirmed Mon–Fri-only cadence, this is not an exotic case —
       every weekend poll and every between-issuance poll is exactly this state.
    6. **Poll 2 and the inverted assertion.**
       `const thu = await helper.getSpcOutlook(...)` with the same toggles.
       Assert `thu.hazardsOutlook.day2 === undefined` (the grid still starts at day3),
       `thu.hazardsOutlook.day3.hazards.length === 0`, and
       `thu.hazardsOutlook.day2Equivalent` is not the mechanism — specifically assert the hazard
       now lands where Aug 29 falls relative to Aug 27, i.e. **offset 2, which is outside the 3..14
       grid, so it renders on NO day**. Assert every `day{N}.hazards` is empty on `thu`.
       Also assert `thu.hazardsOutlook.day3.date === "2026-08-30"` — the date labels advanced too
       (D-01). Failure message must name the bug directly, e.g.
       "day-offset drift: a cache hit replayed the previous day's bucketing — Heavy Rain fixed at
       Aug 29 is day3 on Aug 26 and day2 on Aug 27, and the ETag never fires because the bytes did
       not change".
    7. **Second advance, to a date where the hazard is still inside the grid** — this is the
       positive half, and it is what proves the re-bucketing actually recomputes rather than merely
       emptying. Reset, warm at `Date.UTC(2026, 7, 24)` (Monday; Aug 29 is offset 5), assert
       `day5.hazards` has the hazard and `day3` is empty. Advance to `Date.UTC(2026, 7, 26)`
       (Wednesday; offset 3) with the same bytes/etag, and assert the hazard has MOVED to `day3`
       and `day5` is now empty. A message naming both the expected and actual day key.
    8. **Control assertion.** With the clock held FIXED across two polls (no advance) and the same
       bytes, assert the two payloads' `hazardsOutlook` blocks are deep-equal. Without this, the
       drift assertions above are satisfied by an implementation that simply discards its cache and
       produces arbitrary output on every poll — the fix degrading into "never cache anything", the
       same degradation the `an-hour-old-reading...` scenario's negative control guards against.
    9. Assert `helper._fetch.calls` shows the layer-4 URL was requested on BOTH polls in step 7
       (the cache path is a 304, not a skipped fetch) — so "the day keys changed" cannot be
       explained by the second poll never having run.

    Comment the scenario header with the full rationale: what the analog asserts, why it is right
    for ERO and wrong here, and that this is the single highest-value scenario the phase adds.
  </action>
  <verify>
    <automated>node scripts/probe-payload-resilience.js</automated>
  </verify>
  <acceptance_criteria>
    - The scenario passes as part of the full suite, and no pre-existing scenario regresses.
    - MUTATION PROOF — perform each, confirm RED **with a diagnosable message**, restore, and record
      the file:line and verbatim RED message in the SUMMARY:
      * M1 — in `_runArcGisHazardWindowProduct`, move the `_bucketHazardMatch` loop INSIDE the
        `fetchResult.data !== null` branch (so a cache hit skips re-bucketing) and cache the
        bucketed result instead. MUST turn this scenario RED naming the drift. **This is the exact
        defect the scenario exists to catch — if it goes green, the scenario is a fixture defect,
        not a pass.**
      * M2 — in `_cacheHazardMatches`, replace the field-by-field whitelist with a pass-through of
        the caller's array and add a `day3` key to each match. MUST turn the step-4 precondition
        guard RED naming the clock-dependent field.
      * M3 — make `_todayUtcMs()` return a value memoized on first call for the process lifetime.
        MUST turn the step-7 positive half RED naming the expected vs actual day key.
      * M4 — make the runner recompute from scratch on every poll by never writing the cache at all.
        MUST turn the step-8 **control** RED (or the step-9 fetch-count assertion). This proves the
        scenario does not merely reward "cache nothing".
    - The step-4 **precondition guard** and the step-8 **control assertion** are both present and
      both named in the SUMMARY. They are acceptance criteria, not advice — they are the two proven
      preventatives for the two vacuity modes recorded in STATE.md.
    - The scenario never reads the real clock: `helper._nowMs` is pinned before every poll.
    - The scenario drives the real `fetchGeoJsonCached` via `installHttp`'s `_fetch` seam, not a
      wholesale stub of `fetchGeoJsonCached` — verified by the step-9 `helper._fetch.calls`
      assertion. A wholesale stub would bypass the cache logic the scenario exists to test.
  </acceptance_criteria>
  <done>The scenario passes, all four mutations produced diagnosable RED and were restored, and the precondition guard and control assertion are both present.</done>
</task>

<task type="auto">
  <name>Task 2: HIGHEST-RISK ITEM 2 — the window-band-only no-risk-gate scenario</name>
  <files>scripts/probe-payload-resilience.js</files>
  <read_first>
    - scripts/probe-payload-resilience.js `:1999-2032` — `frontend-advisory-only-is-not-an-all-clear`
      IN FULL. It is the exact model, including its closing control:
      "the same shape with empty advisory arrays must still short-circuit, or the positive assertion
      above is satisfied by the gate simply never firing."
    - scripts/probe-payload-resilience.js `:2880-3010` —
      `frontend-tolerates-a-half-populated-advisories-object` and
      `frontend-advisory-band-respects-its-config-toggles`, for the frontend-scenario shape and the
      `noRiskPayloadWith*` fixture-builder idiom.
    - .planning/STATE.md § Performance Metrics — "the `getDom` no-risk gate that made MPD
      invisible" is one of two live production defects Phase 15 found and fixed.
    - .planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-04-SUMMARY.md — the gate terms
      as actually written, and the three mutation results recorded there.
  </read_first>
  <action>
    Add a `noRiskPayloadWithHazards(hazardsBlock)` fixture builder beside the existing
    `noRiskPayloadWithAdvisory` helper: every SPC day `NONE`, every fire-weather day `0`, ERO and
    WSSI blocks empty, `advisories: { spcMD: [], mpd: [] }`, `_stale` absent, and the supplied
    `hazardsOutlook` block. Add a companion `emptyHazardsBlock()` returning the full
    `day3`..`day14` + `windowBand` shape with everything empty.

    Add `frontend-hazards-window-band-only-is-not-an-all-clear`:
    1. Config: `showHazardsOutlook: true`, every other product flag false.
    2. Build a payload whose `windowBand` carries one `Hazardous Heat` entry
       (`offsetStart: 3, offsetEnd: 7`, valid `startDate`/`endDate`, a valid color) and whose
       **every** `day3`..`day14` `hazards` array is empty.
    3. **Precondition guard:** loop `day3`..`day14` and throw a named error if any array is
       non-empty — "setup did not produce an empty day grid, so this scenario would pass through
       the day-grid gate term and prove nothing about the window-band term". Also assert
       `windowBand.length === 1`.
    4. `const rendered = renderDom(frontend, { config, spcrisk });`
       Assert `rendered !== "No Severe Weather Risk"` with a message naming the failure directly:
       "HAZ-02: a window-band-only payload short-circuited to a confident all-clear — a location
       inside a Hazardous Heat polygon with no day-resolved hazards. This is the Phase 15 getDom
       regression class, which shipped live once."
       Assert `rendered.includes("Hazardous Heat")`.
    5. **Control:** the same payload with `windowBand: []` MUST render exactly
       `"No Severe Weather Risk"` — otherwise the positive assertion is satisfied by the gate simply
       never firing.
    6. **Second control:** the same populated payload with `showHazardsOutlook: false` MUST render
       exactly `"No Severe Weather Risk"` (WR-09 — content the config disabled must not disqualify
       the short-circuit for a band that will not render).

    Also add `frontend-hazards-window-hazard-appears-once-not-per-day` (HAZ-02's second half):
    a payload with one `windowBand` entry spanning `offsetStart: 3, offsetEnd: 7` and an empty day
    grid. Assert the label appears **exactly once** in the rendered markup
    (`(rendered.match(/Hazardous Heat/g) || []).length === 1`), and that no `Day 3)`..`Day 7)` row
    was emitted for it. Control: a payload with the same label appearing on three separate days in
    the day grid renders it three times — proving the count assertion is measuring something real
    and not a renderer that emits nothing.
  </action>
  <verify>
    <automated>node scripts/probe-payload-resilience.js</automated>
  </verify>
  <acceptance_criteria>
    - Both scenarios pass as part of the full suite; no pre-existing scenario regresses.
    - MUTATION PROOF — record each with its verbatim RED message:
      * M1 — delete the `hazardsOutlookHasWindowEntries` gate term from `MMM-SPCOutlook.js`. MUST
        turn `frontend-hazards-window-band-only-is-not-an-all-clear` RED naming the Phase 15
        regression class. **This is the defect the scenario exists to catch.**
      * M2 — replace the two gate terms with a single OR'd term that checks only
        `hazardsOutlookHasAnyDay`. MUST turn the same scenario RED — proving the scenario catches
        the "one predicate that only checks one of them" shape, not just a wholly missing term.
      * M3 — make `hazardsOutlookHasWindowEntries` return `true` unconditionally. MUST turn the
        step-5 **control** RED.
      * M4 — remove the `this.config.showHazardsOutlook &&` guard from the window-band gate term.
        MUST turn the step-6 second control RED.
      * M5 — change the window-band renderer to also emit a row per day in the entry's span. MUST
        turn `frontend-hazards-window-hazard-appears-once-not-per-day` RED naming the count.
    - The precondition guard (step 3) and BOTH controls (steps 5 and 6) are present and named in
      the SUMMARY.
    - The scenarios assert at the **DOM** via `renderDom`, not at the payload — CR-01's lesson: the
      suite asserted on the payload and stopped there, which is how a total outage came to render
      as a confident "No Severe Weather Risk" while a payload scenario reported the guarantee met.
  </acceptance_criteria>
  <done>Both scenarios pass, all five mutations produced diagnosable RED and were restored, and the guard plus both controls are present.</done>
</task>

<task type="auto">
  <name>Task 3: DATA-02 freshness family — per-layer age, the weekend case, and D-15's asymmetry</name>
  <files>scripts/probe-payload-resilience.js</files>
  <read_first>
    - node_helper.js `:1300-1340` — `_isWithinStaleWindow` and `_noteStaleEntry`, and
      `:2220-2230` — the `...(anyStale ? { _stale: true, _staleAsOf: this._oldestStaleAt } : {})`
      spread, so the scenarios assert on the two fields D-15 deliberately treats differently.
    - scripts/probe-payload-resilience.js `:1126-1170` — `ero-hard-fail-is-flagged`, and
      `:3160-3280` — `an-hour-old-reading-still-survives-a-hiccup-but-a-day-old-one-does-not`, for
      the staleness-assertion idiom and its negative control.
    - .planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-RESEARCH.md
      § "Freshness Integration" and § "1. Live payload pull" (the three-distinct-filedates finding).
    - .planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-CONTEXT.md — D-13, D-14,
      D-15, D-16.
  </read_first>
  <action>
    Add four scenarios. All pin `helper._nowMs` and use `hazardsRoutes`.

    1. `hazards-weekend-poll-of-fridays-file-is-not-stale` (DATA-02, the requirement's headline)
       Pin the clock to Sunday `Date.UTC(2026, 7, 30, 12, 0)`. Every layer returns features whose
       `idp_filedate` is Friday `Date.UTC(2026, 7, 28, 17, 0)` — a 67-hour-old file, inside the 84h
       budget. Assert `out._stale` is falsy.
       **Control:** re-run with the filedate pushed to the previous Wednesday
       `Date.UTC(2026, 7, 26, 17, 0)` (91 hours old, outside the budget) and assert `out._stale` is
       `true`. Without the control, "not stale on a weekend" is satisfied by an implementation that
       never checks age at all — which is precisely the "no age check" option D-13 rejected.
       Comment: today's `_isWithinStaleWindow` measures time since the last *successful fetch*, so
       no weekend false alarm arises from the pre-existing code; the false alarm DATA-02 anticipates
       only appears once a data-age check exists. This scenario is what proves the newly added check
       does not create it.
       Read `84` from `PRODUCT_REGISTRY.hazardsOutlook.maxDataAgeHours`, not as a literal, and
       compute both filedates relative to it so a future D-14 retune cannot silently invalidate the
       scenario.

    2. `hazards-idp-filedate-is-evaluated-per-layer-not-shared` (D-13)
       Five layers return features with a fresh `idp_filedate`; ONE layer (layer 3, D8–14
       Temperature — the layer that was ~23h stale in the live 2026-08-26 pull) returns a feature
       whose `idp_filedate` is beyond the budget. Assert `out._stale === true`.
       **Control:** the identical fixture with layer 3's filedate fresh must produce a falsy
       `_stale` — proving the trip came from layer 3's own timestamp and not from anything else in
       the run.
       **Precondition guard:** assert layer 3's feature actually reached the payload (its label
       appears in `windowBand`), so "stale" cannot be explained by layer 3 having failed to parse.
       Comment the live finding: three distinct filedates were observed across six layers in one
       poll on 2026-08-26, one ~23h staler than its siblings. A row-level shared timestamp does not
       exist; "any layer aged out" is the rule.

    3. `hazards-data-age-sets-the-badge-but-not-the-age-figure` (D-15)
       One layer aged out; ALL other products quiet (ERO/WSSI/SPC routed to empty collections, no
       advisory candidates) so nothing else can write `_oldestStaleAt`. Assert `out._stale === true`
       AND `out._staleAsOf` is `null` or `undefined` — the data-age trip reached the badge without
       dragging the "N minutes ago" figure.
       **Control:** a run in which a *fetch* genuinely fails (route one hazards layer to a 503) must
       produce `out._staleAsOf` as a number or the normal hard-failure value — proving the
       assertion above is measuring D-15's deliberate omission and not a `_staleAsOf` that is simply
       never populated in this harness under any condition.
       Comment D-15 in full at the scenario header: without the omission, a 60-hour-old hazards file
       would make the badge speak for SPC data fetched five minutes ago, training the user to
       distrust a badge that is usually right.

    4. `hazards-stale-data-still-renders-its-rows` (D-16, end to end)
       Build a payload with an aged-out layer AND real hazard content, then render it through
       `renderDom`. Assert the ⚠ badge is present AND the hazard label is present AND the markup is
       not `"No Severe Weather Risk"` and does not end at `"No Severe Weather Risk (unconfirmed)"`.
       Comment: D-16 — suppressing rows would convert an age signal into a false negative;
       `rejectBody` serves last-known-good specifically so a WPC hiccup during an active hazard does
       not blank the display, and CR-01 already made staleness disable the confident all-clear
       rather than suppress data.
  </action>
  <verify>
    <automated>node scripts/probe-payload-resilience.js</automated>
  </verify>
  <acceptance_criteria>
    - All four scenarios pass; no pre-existing scenario regresses.
    - MUTATION PROOF — record each with its verbatim RED message:
      * M1 — remove the data-age check entirely from the runner. MUST turn scenario 1's **control**
        RED (the 91-hour file no longer reports stale). If it does not, scenario 1 is vacuous.
      * M2 — change the age check to read `matches[0].idpFiledate` once outside the layer loop and
        reuse it for every layer. MUST turn `hazards-idp-filedate-is-evaluated-per-layer-not-shared`
        RED.
      * M3 — add a `this._noteStaleEntry(...)` call at the data-age trip site. MUST turn
        `hazards-data-age-sets-the-badge-but-not-the-age-figure` RED naming `_staleAsOf`.
      * M4 — change `maxDataAgeHours` to `48` in the registry. MUST turn scenario 1 RED on its
        primary (67-hour, Friday-file) assertion — proving the weekend case is genuinely load-bearing
        and that reading the constant from the registry keeps the scenario honest.
      * M5 — suppress hazards rendering when `_stale` is true. MUST turn
        `hazards-stale-data-still-renders-its-rows` RED.
    - Every scenario reads `maxDataAgeHours` from `PRODUCT_REGISTRY.hazardsOutlook` and derives its
      filedates from it, rather than hardcoding 84, 67 or 91.
    - Scenarios 1, 2 and 3 each carry an explicit **control**; scenario 2 additionally carries a
      **precondition guard**. All are named in the SUMMARY.
    - The zero-feature freshness verdict is already pinned by 16-06's
      `hazards-zero-feature-layers-render-nothing-and-are-not-stale` — do not duplicate it here;
      cross-reference it in the SUMMARY instead.
  </acceptance_criteria>
  <done>All four scenarios pass, all five mutations produced diagnosable RED and were restored, and every control and guard is present and documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cached data -> next poll's rendered day assignment | Data written on poll N is replayed on poll N+1 across a possible date change |
| Payload -> `getDom` short-circuit | A payload's shape decides whether the module claims "No Severe Weather Risk" |
| Probe assertion -> claimed guarantee | These six scenarios are the phase's primary evidence for HAZ-02 and DATA-02 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-27 | Tampering | A cache hit replaying a stale day assignment | mitigate | Task 1's scenario advances the simulated clock 24h with byte-identical upstream content and asserts the day keys moved, plus a positive half proving the hazard relocates to the correct new day rather than merely vanishing. Mutation M1 reproduces the exact defect. |
| T-16-28 | Spoofing | The module claiming "No Severe Weather Risk" while a hazard is live | mitigate | Task 2's scenario asserts at the DOM that a window-band-only payload never renders that string, with two controls and a precondition guard. Mutations M1/M2 reproduce both the "term missing" and the "terms merged into one OR" shapes. |
| T-16-29 | Repudiation | A stale feed reading as fresh indefinitely | mitigate | Task 3 scenario 1's control (a 91-hour file MUST report stale) plus mutation M1 (removing the check turns that control RED) prove the age check exists and fires. D-13's rejected "no age check at all" option is thereby excluded by test, not by convention. |
| T-16-30 | Spoofing | A stale badge misreporting SPC fetch recency as WPC publish age | mitigate | Task 3 scenario 3 asserts `_staleAsOf` stays unpopulated on a data-age trip, with a control proving `_staleAsOf` IS populated on a genuine fetch failure — so the assertion measures D-15's deliberate omission, not a harness that never populates the field. |
| T-16-31 | Spoofing | A scenario green while proving nothing | mitigate | Every scenario in this plan carries a precondition guard, a control assertion, or both, and every one is mutation-proven against the exact line it covers with a diagnosable RED message required (Phase 15 D-10, STATE.md blocking rule). |
| T-16-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — zero packages installed in this phase. |
</threat_model>

<verification>
- `node scripts/probe-payload-resilience.js` reports every scenario passing, with the count grown by
  exactly the six added here and zero new skips.
- All fourteen mutations across the three tasks produce diagnosable RED and are restored; each is
  recorded in the SUMMARY with file:line and the verbatim RED message.
- Every scenario in this plan drives the real code path via the `_fetch` / `_nowMs` seams, never a
  wholesale stub of `fetchGeoJsonCached` or of a renderer.
</verification>

<success_criteria>
- HIGHEST-RISK ITEM 1 is closed by a scenario that goes RED under the exact defect (M1) and does
  not reward "cache nothing" (step-8 control).
- HIGHEST-RISK ITEM 2 is closed by a DOM-level scenario that goes RED under both the "missing term"
  and the "merged OR term" shapes.
- HAZ-02's "never repeated identically across every day in that window" clause is pinned by an
  explicit occurrence count with a control.
- DATA-02 is closed at both ends: no weekend false alarm (primary), and a genuinely stalled feed IS
  caught (control), with D-15's asymmetry proven in both directions.
</success_criteria>

<output>
Create `.planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/16-07-SUMMARY.md` when done.
Record the full mutation inventory (mutation, file:line, verbatim RED message) and the suite's
final scenario count — input to 16-08.
</output>
