---
phase: 15-wpc-winter-storm-severity-mesoscale-precipitation-discussion
plan: 08
type: execute
wave: 6
depends_on: ["15-05", "15-07"]
files_modified:
  - scripts/probe-payload-resilience.js
autonomous: true
requirements: [MPD-01]

must_haves:
  truths:
    - "Pitfall 1 is pinned: a fixture serving an http:// href through the real allowlist yields a fetched MD, and would have gone RED against the pre-fix code"
    - "The allowlist is still a security control: an off-host href is refused and the refusal is logged"
    - "CR-03's out-of-order discard is proven still working at the new socket index"
    - "MPD-01: an advisory-only payload does not render as a confident all-clear"
    - "D-10: each new scenario is mutation-proven load-bearing"
  artifacts:
    - path: "scripts/probe-payload-resilience.js"
      provides: "KMZ fixture builders and the spc-md-* plus advisory frontend scenarios"
      contains: "spc-md-http-href-not-rejected-by-allowlist"
  key_links:
    - from: "scripts/probe-payload-resilience.js"
      to: "helper._fetch"
      via: "installHttp serving Buffer bodies for KMZ fetches, driving the real fetchBinBuffer"
      pattern: "installHttp"
---

<objective>
Add the KMZ fixture builders and the SPC MD and frontend-contract scenarios: the Pitfall 1
regression guard, the allowlist-still-refuses control, the socket-index discard guard, and the
advisory-only no-risk gate guard.

Purpose: two of the defects this phase fixes were live and silent — SPC MD returning zero on every
poll because of the scheme mismatch, and an advisory-only payload rendering as "No Severe Weather
Risk". Neither had a scenario, which is why both survived. These scenarios are what stop them
returning.
Output: KMZ/KML fixture builders plus four mutation-proven scenarios.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/buildomator/bm/4.5.5/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/buildomator/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-wpc-winter-storm-severity-mesoscale-precipitation-discussion/15-CONTEXT.md
@.planning/phases/15-wpc-winter-storm-severity-mesoscale-precipitation-discussion/15-RESEARCH.md
@.planning/phases/15-wpc-winter-storm-severity-mesoscale-precipitation-discussion/15-PATTERNS.md
@.planning/phases/14-foundation-wpc-excessive-rainfall-outlook/14-REVIEW-FIX.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add KML/KMZ fixture builders for advisory scenarios</name>
  <read_first>
    - `scripts/probe-lib/module-stubs.js` as delivered by plan 15-02 — `makeKmzBuffer(entries)`,
      `hasRealKmlDeps`, and the `renderDom` signature after the `mds` parameter was dropped.
    - `scripts/probe-payload-resilience.js` lines 266-294 — `httpResponse` (extended in plan 15-02 to
      accept a `buffer`) and `installHttp`, the `_fetch` seam these scenarios route through.
    - `scripts/probe-payload-resilience.js` lines 28-34 — `SAMPLE_RING`, and lines 193-194 —
      `PROBE_LAT`/`PROBE_LON`, so fixture polygons and the probe coordinate agree.
    - `.planning/phases/15-wpc-winter-storm-severity-mesoscale-precipitation-discussion/15-RESEARCH.md`
      lines 602-638 — the verbatim live MPD description table and the live SPC MD KML structure.
  </read_first>
  <action>
    Add three fixture builders near the existing fixtures, each returning source text or a Buffer.
    They are shared by this plan and plan 15-09, so give them explicit parameters rather than
    hardcoding one scenario's values.

    `mdKml(name)` returns a minimal KML Document with one Placemark whose `<name>` is the argument and
    whose Polygon uses the same ring coordinates as `SAMPLE_RING`. This is the SPC MD shape: its
    Placemark name is already a clean `MD 2108`-style label.

    `mpdKml({ number, issueTime, validEndTi, hazardType })` returns a KML Document with one Polygon
    Placemark whose `<description>` is a CDATA block containing an HTML table with the live row set —
    `ValidStart`, `ValidEndTi`, `IssueTime`, `MPDNumber`, `Forecaster`, `MPDType`, `WFO` — using the
    argument values. When `hazardType` is null, omit the `MPDType` row entirely rather than emitting
    an empty cell, because a genuinely absent row is what D-06's branch must be exercised against.
    Set the Placemark `<name>` to a raw DTG string like the live samples do, so any code that
    mistakenly labels an MPD from the Placemark name instead of `MPDNumber` produces a visibly wrong
    label rather than a plausible one.

    `activeIndexKml(hrefs)` returns a KML Document with one `NetworkLink/Link/href` per supplied
    string, verbatim and unmodified — the scenarios pass `http://` hrefs deliberately.

    Add `kmzOf(entries)` as a thin wrapper over `makeKmzBuffer` that places a non-`.kml` entry first
    by default, so every advisory fixture exercises `extractSoleKmlEntry`'s scan rather than letting a
    first-entry implementation pass by luck.

    Add an `advisoryRoutes({ index, members, listing })` helper that builds an `installHttp` route
    list: the SPC index URL to a Buffer response, each member KMZ URL to its Buffer, the MPD listing
    URL to a text response, and `.lyr.geojson` plus both ERO and WSSI query URLs to 200-empty JSON.
    Routing the unrelated layers to 200-empty is WR-02's trap again — otherwise a hard-failure default
    sets `anyStale` and every `_stale` assertion in these scenarios passes vacuously.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('scripts/probe-payload-resilience.js','utf8');const a=(c,m)=>{if(!c)throw new Error(m)};for(const k of ['mdKml','mpdKml','activeIndexKml','kmzOf','advisoryRoutes'])a(s.includes(k),'missing builder '+k);console.log('OK')" && node scripts/probe-payload-resilience.js</automated>
  </verify>
  <acceptance_criteria>
    - The `node -e` check exits 0 and `node scripts/probe-payload-resilience.js` exits 0 with
      `22 passed, 0 failed, 0 skipped` — adding builders changes no existing scenario.
    - `mpdKml` with `hazardType: null` produces markup containing no `MPDType` substring.
    - `kmzOf` places a non-`.kml` entry before the `.kml` entry by default.
    - `activeIndexKml` reproduces supplied hrefs verbatim, including an `http://` scheme.
  </acceptance_criteria>
  <done>Shared builders produce SPC MD KML, MPD KML with a controllable description table, an ActiveMD-style index, and real KMZ buffers, plus a route helper that isolates the subject layer.</done>
</task>

<task type="auto">
  <name>Task 2: Add the SPC MD allowlist scenarios and the frontend contract scenarios</name>
  <read_first>
    - `scripts/probe-payload-resilience.js` lines 391-430 — `requireLog`, used to prove a refusal was
      actually logged rather than silently dropped.
    - `scripts/probe-payload-resilience.js` lines 962-1003 —
      `frontend-total-outage-still-shows-the-outage`, the template for a scenario that drives the real
      `getDom` and includes a control assertion.
    - `node_helper.js` as delivered by plans 15-04 and 15-07 — `normalizeAdvisoryUrl`,
      `_runKmlAdvisoryRow`, the advisories block in `getSpcOutlook`, and the two-element socket emit.
    - `MMM-SPCOutlook.js` as delivered by plan 15-07 — the `payload[1]` sequence read and the advisory
      term in the no-risk gate.
    - `.planning/phases/15-wpc-winter-storm-severity-mesoscale-precipitation-discussion/15-RESEARCH.md`
      lines 450-466 (Pitfall 1) and 517-533 (Pitfall 5).
  </read_first>
  <action>
    Add four scenarios, all marked `requires: "kml-deps"` since they drive the real ZIP/KML chain.
    Each resets the helper and logs, sets an explicit toggle object, and routes through
    `advisoryRoutes`.

    - `spc-md-http-href-not-rejected-by-allowlist`: the index fixture serves
      `http://www.spc.noaa.gov/products/md/MD2108.kmz` — the scheme SPC actually publishes — and the
      member KMZ contains `mdKml("MD 2108")`. With `pointInPolygon` forced true, assert
      `out.advisories.spcMD` has length 1 with label `SPC MD 2108`, and assert no log line matching
      `refusing off-host` was emitted. This is Pitfall 1's regression guard. Write into the comment
      that against the pre-fix `startsWith(MD_HOST_PREFIX)` code this scenario would report zero
      entries, which was the live production behaviour on every poll.
    - `spc-md-off-host-href-still-refused`: the index serves one legitimate `http://www.spc.noaa.gov/…`
      href and one `http://evil.test/x.kmz`. Assert exactly one entry results, that no fetch was made
      to `evil.test` (check the `installHttp` call log), and use `requireLog` to prove the
      `refusing off-host NetworkLink href` diagnostic naming the rejected value was emitted. This is
      the control for the scenario above: it proves the Pitfall 1 fix loosened the scheme without
      loosening the host check.
    - `frontend-seq-discard-survives-socket-index-migration`: drive the real frontend
      `socketNotificationReceived` with `[outlookA, 5]`, then `[outlookB, 3]`, then `[outlookC, 6]`.
      Assert the payload after the second call is still A — a lower sequence was discarded — and after
      the third is C. Also assert `updateDom` was not called for the discarded payload. Comment that
      this exists because the guard fails open: with the old `payload[2]` read against the new
      two-element payload, `seq` is `undefined`, `typeof seq === "number"` is false, and the discard
      silently becomes a no-op with no error, no log and no other failing test.
    - `frontend-advisory-only-is-not-an-all-clear`: build a payload where every day, fire weather, ERO
      and winter value is no-risk and `_stale` is absent, but `advisories.mpd` has one entry. Render
      with `showMPD: true` and assert the output is not the literal `No Severe Weather Risk` and does
      contain the MPD label. Then assert the control: the same payload with empty advisory arrays
      renders exactly `No Severe Weather Risk`. Comment that before Phase 15 the no-risk gate had no
      advisory term at all, so this rendered as a confident all-clear while the user was inside an
      active discussion — the MPD-01 false negative.
  </action>
  <verify>
    <automated>node scripts/probe-payload-resilience.js</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/probe-payload-resilience.js` exits 0 with `PROBE RESULT: 26 passed, 0 failed,
      0 skipped` and a `PASS` line for each of the four new scenario names.
    - `spc-md-http-href-not-rejected-by-allowlist` asserts on `out.advisories.spcMD` length 1 and on
      the absence of a `refusing off-host` log line.
    - `spc-md-off-host-href-still-refused` asserts the `evil.test` URL never appears in the recorded
      `_fetch` call log and that the refusal was logged.
    - `frontend-advisory-only-is-not-an-all-clear` contains both the positive assertion and the
      empty-advisories control, so the scenario cannot pass by the gate simply never firing.
  </acceptance_criteria>
  <done>Four scenarios pin the Pitfall 1 fix, the surviving host control, the socket-index discard guarantee, and the advisory-only render gate.</done>
</task>

<task type="auto">
  <name>Task 3: Mutation-prove the four new scenarios</name>
  <read_first>
    - `.planning/phases/14-foundation-wpc-excessive-rainfall-outlook/14-REVIEW-FIX.md` — the
      mutation-proof standard: break the line the scenario covers, confirm RED, revert.
    - `node_helper.js` as delivered by plans 15-04 and 15-07 — `normalizeAdvisoryUrl`'s scheme and
      hostname checks and the socket emit are the mutation targets.
    - `MMM-SPCOutlook.js` as delivered by plan 15-07 — the `payload[1]` read and the advisory gate term.
  </read_first>
  <action>
    Apply each mutation, run the suite, confirm the named scenario reports FAIL with a diagnostic
    message, then revert with `git checkout --` and confirm all-green. Record each mutation, the RED
    scenario and the observed failure message in the plan summary.

    1. In `normalizeAdvisoryUrl`, restrict the accepted protocol to `https:` only. Expect
       `spc-md-http-href-not-rejected-by-allowlist` RED. This mutation reproduces the exact live
       production defect, so its RED result is the proof the guard is real.
    2. In `normalizeAdvisoryUrl`, drop the hostname equality check while keeping the scheme check.
       Expect `spc-md-off-host-href-still-refused` RED — proving the fix did not trade the host
       control away for the scheme fix.
    3. In `MMM-SPCOutlook.js`, change the sequence read back to `payload[2]`. Expect
       `frontend-seq-discard-survives-socket-index-migration` RED. Confirm no *other* scenario goes
       RED for this mutation, and record that observation: it is the direct evidence for RESEARCH.md
       Pitfall 5's claim that nothing else in the suite can detect this regression.
    4. In `MMM-SPCOutlook.js`, delete the advisory term from the no-risk short-circuit gate. Expect
       `frontend-advisory-only-is-not-an-all-clear` RED.

    If any mutation produces no RED scenario, that is a coverage gap and not a pass: strengthen the
    scenario's assertions until the mutation is detected, then re-run and record the RED result.
  </action>
  <verify>
    <automated>git diff --exit-code node_helper.js MMM-SPCOutlook.js scripts/probe-payload-resilience.js && node scripts/probe-payload-resilience.js</automated>
  </verify>
  <acceptance_criteria>
    - All four mutations were applied and reverted, each producing at least one RED scenario, with the
      mutation, the RED scenario and the failure message recorded in the summary.
    - Mutation 3's blast radius is recorded explicitly: which scenarios went RED, confirming whether
      any scenario other than the dedicated one can detect the socket-index regression.
    - `git diff --exit-code` over the three files reports no residual changes.
    - `node scripts/probe-payload-resilience.js` exits 0 with `26 passed, 0 failed, 0 skipped`.
  </acceptance_criteria>
  <done>Each of the four scenarios is demonstrated capable of failing when the line it covers is broken.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| probe fixtures → asserted allowlist behaviour | These fixtures are the only place the allowlist's accept and refuse paths are both exercised. |
| suite exit code → phase gate | These scenarios are the standing regression guard for two defects that shipped silently. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-34 | Repudiation | an allowlist scenario that cannot fail | mitigate | Mutations 1 and 2 prove the accept path and the refuse path are independently load-bearing, so a future loosening of the host check cannot hide behind the scheme fix. |
| T-15-35 | Spoofing | fixture hrefs reaching the real network | mitigate | Every scenario installs `installHttp`, and any unrouted URL returns a 503 from the router rather than being fetched; `evil.test` is asserted absent from the call log rather than merely assumed unreachable. |
| T-15-36 | Tampering | a silent socket-contract regression | mitigate | A dedicated scenario drives the real frontend handler through an out-of-order sequence, and mutation 3 confirms it is the only detector — recorded as evidence rather than assumed. |
| T-15-37 | Information disclosure | an advisory-only outage rendering as an all-clear | mitigate | `frontend-advisory-only-is-not-an-all-clear` asserts both the positive case and the empty-advisory control, so the gate must actually fire for the scenario to pass. |
</threat_model>

<verification>
- `node scripts/probe-payload-resilience.js` → `PROBE RESULT: 26 passed, 0 failed, 0 skipped`, exit 0.
- `git diff --exit-code` clean after mutation proofs.
- The summary records four mutations with their RED scenarios and mutation 3's blast radius.
</verification>

<success_criteria>
Shared KMZ/KML fixture builders exist, and four mutation-proven scenarios pin the `http://` allowlist
fix, the surviving off-host refusal, CR-03's discard at the new socket index, and the advisory-only
render gate.
</success_criteria>

<output>
Create `.planning/phases/15-wpc-winter-storm-severity-mesoscale-precipitation-discussion/15-08-SUMMARY.md` when done
</output>
