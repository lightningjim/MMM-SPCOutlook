---
phase: 15-wpc-winter-storm-severity-mesoscale-precipitation-discussion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - productRegistry.js
autonomous: true
requirements: [WSSI-01, WSSI-02, MPD-01, MPD-02]

must_haves:
  truths:
    - "D-01: every PRODUCT_REGISTRY row declares a `kind`, and two kinds exist: arcgis-day-layers and kml-advisory"
    - "D-08: the WSSI colour palette is WPC's own published renderer palette, with the source URL cited in the row"
    - "D-09 AMENDED: a WSSI feature whose impact is MINOR/MODERATE/MAJOR/EXTREME is included; WINTER WEATHER AREA is excluded exactly as no-polygon is"
    - "WSSI-02: an impact value in any letter case resolves to the same tier as the ALL-CAPS live value"
    - "D-02: SPC Mesoscale Discussions are declared as a registry row, not a hardcoded free function"
  artifacts:
    - path: "productRegistry.js"
      provides: "winterImpact (arcgis-day-layers), spcMD and mpd (kml-advisory) registry rows plus a kind discriminator on excessiveRain"
      contains: "kind: \"kml-advisory\""
  key_links:
    - from: "productRegistry.js winterImpact.buildUrl"
      to: "buildArcGisQuery"
      via: "arrow closure over WSSI_BASE_URL and wssiDayLayers"
      pattern: "buildArcGisQuery\\(WSSI_BASE_URL"
---

<objective>
Widen `PRODUCT_REGISTRY` with a `kind` discriminator (D-01) and add the three rows this phase
needs: `winterImpact` (WSSI, `arcgis-day-layers`) and `spcMD` + `mpd` (`kml-advisory`).

Purpose: every later plan in this phase reads its configuration from these rows. The registry is
the single place a product's day span, value domain, palette and host are declared, so the WSSI
loop, the advisory pipeline and the probe URL maps all derive from here rather than from literals.
Output: `productRegistry.js` carrying four rows across two kinds, with no behaviour change to the
shipped `excessiveRain` row.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/buildomator/bm/4.5.5/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/buildomator/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-wpc-winter-storm-severity-mesoscale-precipitation-discussion/15-CONTEXT.md
@.planning/phases/15-wpc-winter-storm-severity-mesoscale-precipitation-discussion/15-RESEARCH.md
@.planning/phases/15-wpc-winter-storm-severity-mesoscale-precipitation-discussion/15-PATTERNS.md
</context>

<interfaces>
<!-- Contracts this plan must satisfy for downstream plans. Do not explore; use these directly. -->

Consumed by `node_helper.js` `_productToggles()` (node_helper.js:74-80) — iterates
`Object.values(PRODUCT_REGISTRY)` and reads `row.configFlag`. Adding a row is the only wiring
needed for its toggle to exist.

Consumed by the ERO day-loop contract (node_helper.js:1353-1428), which reads exactly these row
members on an `arcgis-day-layers` row: `id`, `configFlag`, `days`, `buildUrl(day)`,
`toValue(label, feature)`, `includesFeat(label, value)`, `valueToTier`, `tierToText`,
`tierToColor`, `validTimeField`.

Produced for plan 15-04's advisory runner — a `kml-advisory` row exposes: `id`, `kind`,
`configFlag`, `allowedHost`, `discovery` (strategy name string), `discoveryUrl`, and
`toEntry(feature, ctx)` returning `{ label, hazardType }`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add the kind discriminator and the winterImpact (WSSI) row</name>
  <read_first>
    - `productRegistry.js` in full (83 lines) — the file being modified; `excessiveRain`
      (lines 52-81) is the verbatim template and `buildArcGisQuery` (lines 24-32) is the guard
      the new baseUrl must pass unmodified.
    - `.planning/phases/15-wpc-winter-storm-severity-mesoscale-precipitation-discussion/15-PATTERNS.md`
      lines 43-112 — the exact WSSI row to write, derived from the live-verified values.
    - `.planning/phases/15-wpc-winter-storm-severity-mesoscale-precipitation-discussion/15-RESEARCH.md`
      lines 537-588 — the live MapServer captures: layer inventory, field schema, the palette, and
      the off-season zero-feature response.
    - `.planning/phases/15-wpc-winter-storm-severity-mesoscale-precipitation-discussion/15-CONTEXT.md`
      lines 99-116 — D-09 including the AMENDED block that moves the render floor to MINOR.
  </read_first>
  <action>
    Add `kind: "arcgis-day-layers"` as the first member of the existing `excessiveRain` row. This is
    additive only — do not touch any other `excessiveRain` member, and do not reorder them; the
    probe's golden snapshots compare serialised payloads and the shipped ERO behaviour must not move.

    Add module-scope constants above `PRODUCT_REGISTRY`, following the existing `ero*` naming and
    placement convention:
    - `WSSI_BASE_URL` = `https://mapservices.weather.noaa.gov/vector/rest/services/outlooks/wpc_wssi/MapServer`
    - `wssiDayLayers` = `{ 1: 1, 2: 2, 3: 3 }`. Note these are 1-based, unlike ERO's 0-based
      `eroDayLayers`; layer 0 is a Group Layer and layer 4 is the Days 1-3 composite, neither of
      which is a per-day feature layer. Carry that as a comment.
    - `wssiRawToValue` = `{ "WINTER WEATHER AREA": 1, MINOR: 2, MODERATE: 3, MAJOR: 4, EXTREME: 5 }`.
      These are the live renderer's raw values, verbatim and ALL CAPS.
    - `wssiValueToTier` = `{ 0: "NONE", 1: "WWA", 2: "MINOR", 3: "MODERATE", 4: "MAJOR", 5: "EXTREME" }`
    - `wssiTierToText` = `{ NONE: "None", WWA: "Winter Weather Area", MINOR: "Minor",
      MODERATE: "Moderate", MAJOR: "Major", EXTREME: "Extreme" }`
    - `wssiTierToColor` = `{ NONE: "afddf6", WWA: "d2dfe7", MINOR: "faf5a3", MODERATE: "f7962f",
      MAJOR: "e61f26", EXTREME: "7853a1" }` — hex with no leading `#`, matching `eroTierToColor`.

    D-08 requires the palette's provenance to be recorded in the registry row rather than left
    uncited (this is the complaint 14-REVIEW.md IN-01 raised about ERO's palette). Add a
    `paletteSource` member on the `winterImpact` row whose value is the literal URL
    `https://mapservices.weather.noaa.gov/vector/rest/services/outlooks/wpc_wssi/MapServer/1?f=json`
    and a comment naming `drawingInfo.renderer.uniqueValueInfos` as the field the RGB triples came
    from. Do not backfill ERO's citation — that is an explicitly deferred idea for this phase.

    Add the `winterImpact` row with `id: "winterImpact"`, `kind: "arcgis-day-layers"`,
    `configFlag: "showWinterImpact"`, `baseUrl: WSSI_BASE_URL`, `dayLayers: wssiDayLayers`,
    `days: 3`, `buildUrl: (day) => buildArcGisQuery(WSSI_BASE_URL, wssiDayLayers[day])`,
    `valueToTier: wssiValueToTier`, `tierToText: wssiTierToText`, `tierToColor: wssiTierToColor`,
    and `validTimeField: "valid_time"` (the live field list carries both `valid_time` and
    `issue_time`; `valid_time` is the window, matching ERO's choice).

    `toValue` implements WSSI-02: read `f.properties.impact` (the field name is lowercase `impact`),
    and when it is a string, `.trim().toUpperCase()` it before the `wssiRawToValue` lookup, returning
    `0` for anything unrecognised or non-string. The fold must happen here, before the lookup — this
    is the single place the case-insensitivity is implemented, and the probe will mutate exactly this
    line. `label` is unused, matching ERO's signature contract.

    `includesFeat` implements D-09 AMENDED: `(label, val) => val >= 2`. MINOR and above are included;
    `WINTER WEATHER AREA` (value 1) is filtered out at exactly the same point a no-risk feature is,
    so it can never reach `evaluatePolygons` and can never produce a row. Comment this with WPC's own
    justification — the service describes that tier as not anticipated to impact daily life — and
    state that value 1 is retained in `wssiValueToTier` to document the real domain, not because a
    WWA tier can ever surface in the payload. Do NOT reference the string `LIMITED` anywhere: WPC's
    live domain contains no such value and any code or comment asserting it is wrong by construction.
  </action>
  <verify>
    <automated>node -e "const {PRODUCT_REGISTRY:R}=require('./productRegistry.js');const w=R.winterImpact;const a=(c,m)=>{if(!c)throw new Error(m)};a(R.excessiveRain.kind==='arcgis-day-layers','ERO kind');a(w.kind==='arcgis-day-layers','WSSI kind');a(w.days===3,'days');a(w.buildUrl(1).includes('/1/query'),'day1 layer 1');a(w.buildUrl(3).includes('/3/query'),'day3 layer 3');a(w.buildUrl(1).includes('f=geojson'),'geojson');a(w.toValue(null,{properties:{impact:'MINOR'}})===2,'MINOR');a(w.toValue(null,{properties:{impact:'minor'}})===2,'case fold');a(w.toValue(null,{properties:{impact:' Major '}})===4,'trim+fold');a(w.toValue(null,{properties:{impact:'WINTER WEATHER AREA'}})===1,'WWA value');a(w.toValue(null,{properties:{impact:null}})===0,'non-string');a(w.includesFeat(null,1)===false,'WWA excluded');a(w.includesFeat(null,2)===true,'MINOR included');a(w.includesFeat(null,5)===true,'EXTREME included');a(w.tierToColor.MINOR==='faf5a3','palette');a(typeof w.paletteSource==='string'&&w.paletteSource.includes('wpc_wssi'),'palette cited');console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e` verification command above exits 0 and prints `OK`.
    - `grep -c 'LIMITED' productRegistry.js` returns 0.
    - `PRODUCT_REGISTRY.excessiveRain.kind === "arcgis-day-layers"` and every other
      `excessiveRain` member is byte-identical to its pre-task value (`git diff productRegistry.js`
      shows only an added `kind` line inside that row).
    - `node scripts/probe-payload-resilience.js` exits 0 with `0 failed` — the shipped ERO
      behaviour is unchanged by the registry widening.
  </acceptance_criteria>
  <done>`winterImpact` resolves ALL-CAPS and mixed-case impact values to identical tiers, excludes WINTER WEATHER AREA at `includesFeat`, cites its palette source, and the 15 existing probe scenarios still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add the spcMD and mpd kml-advisory rows</name>
  <read_first>
    - `productRegistry.js` as modified by Task 1 — the file being modified.
    - `node_helper.js` lines 433-481 (`getMesoscaleDiscussion`) — the shipped SPC MD logic being
      declared as a row here; its `ActiveMD.kmz` URL and its `hit.properties.name` label source are
      the values this row must carry.
    - `node_helper.js` line 40 (`MD_HOST_PREFIX`) — the host constant this row's `allowedHost`
      supersedes. Leave `MD_HOST_PREFIX` in place for now; plan 15-04 removes it.
    - `.planning/phases/15-wpc-winter-storm-severity-mesoscale-precipitation-discussion/15-RESEARCH.md`
      lines 590-638 — the live MPD directory listing and the MPD/SPC MD KML structure, including
      why MPD's Placemark `<name>` is unusable as a label and `MPDNumber` must be used instead.
    - `.planning/phases/15-wpc-winter-storm-severity-mesoscale-precipitation-discussion/15-CONTEXT.md`
      lines 34-45 (D-02) and lines 73-90 (D-05, D-06, D-07).
  </read_first>
  <action>
    Add two `kml-advisory` rows. These rows are pure static configuration — they must contain no
    `this`, no network call and no `require`, matching the file's existing character. Candidate
    discovery needs the `_fetch` transport seam and therefore lives in `node_helper.js` (plan 15-04);
    the row only names the strategy it wants by string.

    Row `spcMD`: `id: "spcMD"`, `kind: "kml-advisory"`, `configFlag: "showSPCMD"`,
    `allowedHost: "www.spc.noaa.gov"`, `discovery: "spc-active-index"`,
    `discoveryUrl: "https://www.spc.noaa.gov/products/md/ActiveMD.kmz"`.

    Record in a comment that `showSPCMD` is the one product flag in this registry that defaults to
    **true** rather than false. Phase 14's CFG-01 rule ("every new product flag defaults to false")
    governs new products; SPC MD is a shipping, always-on feature being migrated under D-02, and
    defaulting it false would silently delete a live capability. The default itself is set in
    `MMM-SPCOutlook.js` (plan 15-03), not here.

    Row `mpd`: `id: "mpd"`, `kind: "kml-advisory"`, `configFlag: "showMPD"`,
    `allowedHost: "www.wpc.ncep.noaa.gov"`, `discovery: "wpc-mpd-listing"`,
    `discoveryUrl: "https://www.wpc.ncep.noaa.gov/kml/mpd/"`. `showMPD` is a genuinely new product
    and defaults to false per CFG-01.

    Give each row a `toEntry(feature, ctx)` pure function returning the display record the frontend
    renders, so that all product-specific label knowledge stays in the row (the D-07 precedent that
    each row owns its own label maps). Both return the same shape: `{ label, hazardType }`.
    - `spcMD.toEntry` returns `{ label: "SPC " + name, hazardType: null }` where `name` is
      `feature.properties.name`, which SPC publishes already formatted as `MD 2108`; the composed
      label is therefore `SPC MD 2108`, matching D-05's `SPC MD 0123 in effect.` example. Return
      `null` from `toEntry` when `feature.properties.name` is missing or not a non-empty string, so
      the caller can log it rather than pushing `undefined` into the band.
    - `mpd.toEntry` returns `{ label: "WPC MPD " + number, hazardType }` where `number` and
      `hazardType` are supplied by the caller on `ctx` (`ctx.number`, `ctx.hazardType`) — they come
      from the description-CDATA table (`MPDNumber`, `MPDType`) which only `node_helper.js` can
      parse. Return `null` when `ctx.number` is absent. Per D-06 a `null` `hazardType` is normal and
      must never cause the entry to be dropped.

    Add `MPD_FILENAME_PATTERN` as an exported module constant with value `/^MPD_(\d+)_final\.kmz$/`.
    Plan 15-06's directory-listing discovery accepts only filenames matching this pattern and joins
    them to `discoveryUrl`; declaring it here keeps the one expression that bounds what remote markup
    can be turned into a fetch target next to the host it is bounded against.

    Extend `module.exports` to include `MPD_FILENAME_PATTERN` alongside the existing
    `buildArcGisQuery` and `PRODUCT_REGISTRY`.
  </action>
  <verify>
    <automated>node -e "const {PRODUCT_REGISTRY:R,MPD_FILENAME_PATTERN:P}=require('./productRegistry.js');const a=(c,m)=>{if(!c)throw new Error(m)};for(const id of ['spcMD','mpd']){const r=R[id];a(r.kind==='kml-advisory',id+' kind');a(typeof r.allowedHost==='string'&&!r.allowedHost.includes('/'),id+' allowedHost is a bare hostname');a(typeof r.discovery==='string',id+' discovery');a(new URL(r.discoveryUrl).hostname===r.allowedHost,id+' discoveryUrl on allowedHost');a(typeof r.toEntry==='function',id+' toEntry')}a(R.spcMD.configFlag==='showSPCMD','spcMD flag');a(R.mpd.configFlag==='showMPD','mpd flag');a(R.spcMD.toEntry({properties:{name:'MD 2108'}},{}).label==='SPC MD 2108','spcMD label');a(R.spcMD.toEntry({properties:{}},{})===null,'spcMD nameless');a(R.mpd.toEntry({},{number:'1118',hazardType:'Heavy rainfall'}).label==='WPC MPD 1118','mpd label');a(R.mpd.toEntry({},{number:'1118',hazardType:null}).hazardType===null,'mpd D-06 null hazard kept');a(R.mpd.toEntry({},{})===null,'mpd numberless');a(P.test('MPD_1118_final.kmz'),'pattern accepts real name');a(!P.test('../../etc/passwd'),'pattern rejects traversal');a(!P.test('MPD_1118_final.kmz.exe'),'pattern anchored');a(!P.test('http://evil/MPD_1_final.kmz'),'pattern rejects absolute url');console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e` verification command above exits 0 and prints `OK`.
    - `grep -n 'this\.' productRegistry.js` returns no match inside either new row (rows are pure config).
    - `MPD_FILENAME_PATTERN` is anchored at both ends and rejects `../`, absolute URLs, and any
      trailing suffix, as asserted above.
    - `node scripts/probe-payload-resilience.js` exits 0 with `0 failed` — adding rows with
      toggles defaulting off through `_productToggles` changes no existing scenario.
  </acceptance_criteria>
  <done>`PRODUCT_REGISTRY` declares four rows across two kinds; both advisory rows carry a bare hostname allowlist, a named discovery strategy, and a pure `toEntry` that returns `{ label, hazardType }` or `null`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registry config → outbound fetch URL | Every URL this module fetches is ultimately built from constants declared in this file; a wrong constant here is a fetch to the wrong host on every poll. |
| remote KML/directory markup → fetch target | `MPD_FILENAME_PATTERN` declared here is the expression that decides which remote-supplied strings may become a fetch target in plan 15-06. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Spoofing | `winterImpact.buildUrl` | mitigate | `buildArcGisQuery` is untouched and already refuses any baseUrl not prefixed `https://mapservices.weather.noaa.gov/`; `WSSI_BASE_URL` passes that guard unmodified, so no new host is reachable through the ArcGIS path. |
| T-15-02 | Tampering | `allowedHost` on both advisory rows | mitigate | Stored as a bare hostname (`www.spc.noaa.gov`), never a scheme+prefix string, so plan 15-04 can compare it against a parsed `URL.hostname` rather than doing a prefix match that a lookalike host or a userinfo-embedded URL can defeat. Asserted by the `!allowedHost.includes('/')` check. |
| T-15-03 | Tampering | `MPD_FILENAME_PATTERN` | mitigate | Anchored `^...$` pattern permitting only `MPD_<digits>_final.kmz`, so a hostile directory listing cannot inject a traversal segment, an absolute URL, or an alternate extension into the candidate set. Asserted by four negative cases in Task 2's verification. |
| T-15-04 | Information disclosure | `paletteSource` URL in the row | accept | A public, unauthenticated NOAA metadata URL recorded as a citation; it is never fetched at runtime. |
</threat_model>

<verification>
- `node -e` assertions in both tasks exit 0.
- `node scripts/probe-payload-resilience.js` → `0 failed`, exit 0 (this plan runs in wave 1 alongside 15-02, which changes both the scenario total and the summary format, so assert the failure count rather than the total).
- `git diff productRegistry.js` shows the `excessiveRain` row gained exactly one line (`kind`).
</verification>

<success_criteria>
`PRODUCT_REGISTRY` carries `excessiveRain` and `winterImpact` as `arcgis-day-layers` rows and `spcMD`
and `mpd` as `kml-advisory` rows. WSSI impact values resolve case-insensitively, WINTER WEATHER AREA
is excluded at `includesFeat`, the palette cites WPC's renderer, and the 15 shipped probe scenarios
still pass unchanged.
</success_criteria>

<output>
Create `.planning/phases/15-wpc-winter-storm-severity-mesoscale-precipitation-discussion/15-01-SUMMARY.md` when done
</output>
