---
phase: 19.1-day-report-render-width-product-neutral-copy-and-config-key
plan: 08
type: execute
wave: 2
depends_on: ["19.1-06"]
gap_closure: true
files_modified:
  - .planning/phases/19.1-day-report-render-width-product-neutral-copy-and-config-key-/19.1-UI-SPEC.md
  - .planning/phases/19.1-day-report-render-width-product-neutral-copy-and-config-key-/19.1-RESEARCH.md
autonomous: true
requirements: []
traces:
  - "19.1-VERIFICATION.md Anti-Patterns Found row 2 — the UI-SPEC Typography section still asserts font-family is 'never on an ancestor', which the shipped mechanism contradicts; plan 19.1-05 Task 3 part (e) was scoped to reconcile it and explicitly deferred it"
  - "19.1-CONTEXT.md Claude's Discretion — 'A CSS-column approach changes that contract and needs a UI-SPEC amendment of the same kind BL-03 made. Make the amendment explicitly; do not leave the spec contradicting the code.'"
  - "19.1-PATTERNS.md 'THE CENTRAL TENSION the planner must resolve' — RESEARCH.md Pitfall 3 and Anti-Patterns warn against the very remedy the operator chose"
  - "19.1-VERIFICATION.md gaps[0].missing[0] — the derivation ORDER, not just the value, is what was disproven"

must_haves:
  truths:
    - "No signed design document still instructs a future implementer to build the mechanism this gap closure retired"
    - "The Typography section's 'never on an ancestor' sentence no longer contradicts shipped code — the deferred 19.1-05 Task 3 part (e) is closed"
    - "RESEARCH.md's warning against CSS Grid is reconciled rather than deleted: the reason it was written is preserved, and the reason it does not apply to the shipped mechanism is stated"
    - "The Region Containment section states the correct derivation ORDER (available space first, content allocated inside it) and marks the 22.5rem value as pending live confirmation rather than confirmed"
    - "Every superseded passage is preserved as history, never silently overwritten — the same discipline the existing SUPERSEDED note already uses"
  artifacts:
    - path: ".planning/phases/19.1-day-report-render-width-product-neutral-copy-and-config-key-/19.1-UI-SPEC.md"
      provides: "grid-track column contract, available-space-first Region Containment derivation, reconciled Typography section"
      contains: "display:inline-grid"
    - path: ".planning/phases/19.1-day-report-render-width-product-neutral-copy-and-config-key-/19.1-RESEARCH.md"
      provides: "supersession notes on Pattern 1, Anti-Patterns, Pitfall 3 and Pitfall 4"
      contains: "SUPERSEDED"
  key_links:
    - from: "19.1-UI-SPEC.md Region Containment"
      to: "MMM-SPCOutlook.js REGION_CAP_REM"
      via: "the same literal value, derived by the same available-space-first arithmetic"
      pattern: "22\\.5rem"
    - from: "19.1-UI-SPEC.md Detail line — Column contract"
      to: "MMM-SPCOutlook.js detailRowOpen"
      via: "the same grid-template-columns shape"
      pattern: "grid-template-columns"
---

<objective>
Reconcile the two design documents that now contradict shipped code: `19.1-UI-SPEC.md` (which still
specifies the retired `ch`/monospace column contract, still asserts `font-family` is never on an
ancestor, and still carries a content-needs-first Region Containment derivation) and
`19.1-RESEARCH.md` (whose Anti-Patterns and Pitfall 3 warn against the exact remedy the operator
chose).

Purpose: `19.1-CONTEXT.md` states the rule directly — "Make the amendment explicitly; do not leave the
spec contradicting the code." One half of this work was already scoped by plan 19.1-05 Task 3 part (e)
and explicitly deferred; `19.1-VERIFICATION.md` records that deferral as an open anti-pattern and says
it should fold into the gap-closure phase, since that phase is replacing the mechanism in question
anyway.

Output: two documents that describe the shipped mechanism, preserve the retired one as recorded
history, and state the correct derivation order for any future cap.

Scope boundary: this plan reconciles the MECHANISM and the derivation METHOD. It does NOT record a
live-confirmed cap value — the `22.5rem` figure stays marked pending until plan `19.1-09`'s
on-hardware observation, and `19-PARITY-CHECKLIST.md` is plan `19.1-09`'s file, not this one's.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/buildomator/bm/4.6.0/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/buildomator/bm/4.6.0/templates/summary.md
</execution_context>

<context>
@.planning/phases/19.1-day-report-render-width-product-neutral-copy-and-config-key-/19.1-VERIFICATION.md
@.planning/phases/19.1-day-report-render-width-product-neutral-copy-and-config-key-/19.1-PATTERNS.md
@.planning/phases/19.1-day-report-render-width-product-neutral-copy-and-config-key-/19.1-06-SUMMARY.md
@.planning/phases/19.1-day-report-render-width-product-neutral-copy-and-config-key-/19.1-03-SUMMARY.md

<interfaces>
<!-- Section anchors in 19.1-UI-SPEC.md (line numbers at HEAD; re-locate by heading, not by number) -->

:22    ## Amendment Index
:50    ## Layout Grammar
:52    ### Detail line — Column contract (AMENDED mechanism, unchanged field widths)
:114   ### Region Containment (NEW — a separate, independent contract from column alignment)
:178   **SUPERSEDED (live check, 2026-09-10 ...)**   <- the existing supersession note; imitate its form
:228   ## Typography           <- "never on an ancestor or a sibling" is at :232-233
:239   ## Color
:336   ## Detail-Mode Density (one-sentence amendment)
:354   ## Component / Render Inventory
:381   ## Verification Honesty
:394   ## Checker Sign-Off     <- DO NOT TOUCH

<!-- Section anchors in 19.1-RESEARCH.md -->

:198   ### Pattern 1: `min-width` + `display:inline-block` on the existing colored span (width fix)
:222   ### Pattern 2: Explicit `max-width` on the wrapper div (region containment)
:396   ### Anti-Patterns to Avoid
:469   ### Pitfall 2: Mechanism change alone does not guarantee the region shrinks
:486   ### Pitfall 3: Breaking `assertInertMarkup`'s allowlist with a new tag
:499   ### Pitfall 4: `ch` unit resolving against the wrong font
:581   ## Assumptions Log
:593   ## Open Questions (ALL RESOLVED)

<!-- The shipped mechanism these documents must now describe (plan 19.1-06) -->

Row opener   <span style="display:inline-grid;box-sizing:border-box;width:100%;
                          padding-left:0.6rem;column-gap:0.4rem;align-items:baseline;
                          grid-template-columns:minmax(0,{dimensionTrackFr}fr)
                                                minmax(0,{DETAIL_LABEL_FIELD_WIDTH}fr)
                                                minmax(0,{SOURCE_ATTRIBUTION_FIELD_WIDTH}fr)">
Colour       <span style="color:#RRGGBB;display:contents">   (box removed, colour inherited)
Cell         <span style="min-width:0;overflow-wrap:anywhere">
Cap          REGION_CAP_REM = 22.5   ->   wrapper.style.maxWidth = "22.5rem"
Budget       (1020 body right edge − 540 centreline − 30 module gap) / 20px root = 22.5rem
Tag shapes   unchanged; INERT_MARKUP_ALLOWLIST byte-identical
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the UI-SPEC's column contract, Region Containment and Typography sections</name>
  <files>.planning/phases/19.1-day-report-render-width-product-neutral-copy-and-config-key-/19.1-UI-SPEC.md</files>
  <read_first>
    - 19.1-UI-SPEC.md IN FULL for the sections listed in `<interfaces>`: the Amendment Index (:22),
      "Detail line — Column contract" (:52-113), "Region Containment" (:114-200, including the existing
      SUPERSEDED note at :178 whose FORM you will imitate), Typography (:228-238), Color (:239-246),
      Detail-Mode Density (:336-345), Component / Render Inventory (:354-380) and Verification Honesty
      (:381-393).
    - MMM-SPCOutlook.js — `REGION_CAP_REM` and its derivation comment, `detailRowOpen`, `detailTrack`,
      `detailFieldCell`, `detailColoredWrapper`, and both call sites. The spec must describe the code,
      not this plan's summary of it.
    - 19.1-03-SUMMARY.md "Assumption Drift" — the record of WHY plan 19.1-03 declared `font-family` at
      row level against the spec's instruction. That reasoning is the input to the Typography fix.
    - 19.1-VERIFICATION.md Anti-Patterns Found row 2 — the precise statement of the contradiction.
  </read_first>
  <action>
    Amend `19.1-UI-SPEC.md`. Preserve every superseded passage as history — mark it, do not delete it —
    using the same form as the existing SUPERSEDED note at :178.

    (a) **"Detail line — Column contract"** — mark the entire existing "New column contract" table and
    its "Harness compatibility (Pitfall 3)" and "Resolving the `also:` indent" paragraphs
    `SUPERSEDED (gap closure, plan 19.1-06)`, keeping them in place, then write the replacement
    contract below them:
    - Row container: `display:inline-grid` on the existing row `<span>`, `width:100%`,
      `box-sizing:border-box`, `padding-left` for the indent (replacing the literal `"  "`),
      `column-gap` (replacing the `"   "` attribution gap), `align-items:baseline`.
    - Three tracks, declared identically on every row kind, which is what aligns columns ACROSS rows:
      `minmax(0,{dimension}fr) minmax(0,{DETAIL_LABEL_FIELD_WIDTH}fr) minmax(0,{SOURCE_ATTRIBUTION_FIELD_WIDTH}fr)`.
    - `SOURCE_ATTRIBUTION_FIELD_WIDTH` is NEW and is derived from `SOURCE_SHORT_NAMES`' own longest
      value + 2, following the same derive-never-hardcode rule `DIMENSION_FIELD_WIDTH` follows.
    - Colour carrier: `detailColoredWrapper` now declares `display:contents`, so its box is removed and
      its two child cells become grid items of the row while still inheriting its `color`. State
      explicitly that Phase 19 D-02 ("the dimension name and the label share one colour") is preserved:
      one `validHazardColor` call, one colour decision, two cells.
    - BL-02: the co-winner's blank dimension field is an EMPTY CELL in the same track. WR-02 iteration
      4: all three row kinds derive their first track from the single quantity
      `Math.max(DIMENSION_FIELD_WIDTH, dimensionField.length)`.
    - Retired and why: `alsoIndent` and `alsoLabelFieldWidth`. Both were arithmetic that kept the em
      dash on the winner's column; the third track now guarantees that structurally, so arithmetic that
      could drift was replaced by a contract that cannot.
    - **Harness compatibility, restated as the resolution of the central tension:** the retired
      paragraph instructed "Do not introduce `<div>`, CSS Grid, or `<table>`/`<tr>`/`<td>` anywhere in
      this fix." Keep it visible, marked superseded, and immediately state the resolution: CSS Grid is
      a property VALUE, not a tag, so `display:inline-grid` on the existing `<span style="...">`
      delivers layout-based alignment while `INERT_MARKUP_ALLOWLIST` (`module-stubs.js:337-342`) stays
      byte-identical. The warning was correct about `<div>`/`<table>` markup and remains binding for
      those; it was over-broad about Grid. Cite plan 19.1-07's
      `wr07-assert-inert-markup-accepts-module-tags-and-rejects-everything-else`, which proves this by
      execution (including a `<div>` mutation that goes RED).
    - **Overflow behaviour** — replace the existing two bullets. Field-level overflow no longer pushes
      the attribution right: `minmax(0,Nfr)` floors every track at zero, so an over-long dimension name
      or label wraps INSIDE its own cell and the row's declared tracks are unchanged. This is a real
      behaviour change from the retired push-right rule and must be stated as such, not glossed. Row-
      level overflow can no longer inflate the region at all, because the row is `width:100%` of a
      capped container.

    (b) **"Region Containment"** — leave the existing derivation paragraph and its SUPERSEDED note
    (:154-178) exactly where they are; they are the preserved negative example of content-needs-first
    ordering. Below them, add a new subsection stating the replacement derivation in the correct order:
    measured geometry (1080×1920 rotated, 60px body margins, 960px usable, 20px root, body right edge
    x=1020, centreline x=540, `--gap-modules` 30px) → budget `1020 − 540 − 30 = 450px` → cap
    `450 / 20 = 22.5rem` → content allocated INSIDE that budget by `fr` tracks. State the hard ceiling
    (480px / 24rem) and why the 30px module gap backs off from it. State the rule for any future cap in
    one sentence: available space first, content checked against it, never the reverse.
    Mark the value `[PENDING LIVE CONFIRMATION — plan 19.1-09]`. Do NOT write it as confirmed; no width
    claim for this module may be treated as resolved without an on-hardware observation, and this phase
    has burned twice on exactly that.
    Add one paragraph on why the new mechanism is a stronger containment guarantee than a cap alone: a
    `width:100%` row with zero-floored tracks cannot contribute a max-content width larger than its
    container, so the cap is now a bound the content cannot exceed rather than a permission the content
    may or may not use. Note plainly that this is what the retired mechanism got wrong — the region was
    648px wide because the cap PERMITTED it, not because content demanded it.

    (c) **Typography (:228-238)** — this closes plan 19.1-05 Task 3 part (e), which was scoped and
    explicitly deferred. Two changes:
    - Delete the reinforcement paragraph's instruction that the amended spans must keep
      `font-family:'DejaVu Sans Mono','Liberation Mono',monospace` colocated with a `ch` width. It
      describes a mechanism that no longer exists.
    - Replace the "never on an ancestor or a sibling" sentence. Scope it to `color` only (which does
      still live on a single carrier), and record the two-step history in one short paragraph: plan
      19.1-03 deliberately declared `font-family` at ROW level, contradicting this sentence, because the
      uncoloured `also:` spacer sat outside every coloured span and needed an ancestor's font to resolve
      `ch` (RESEARCH.md Pitfall 4); the gap closure then removed `font-family` from the detail markup
      entirely, because grid tracks do not resolve against a font at all. State that the detail rows
      now inherit the host's own typeface, and that this reverses plan 19.1-03's accepted visible
      consequence — the literal `also:` token and the `— SPC` / `— WSSI` attributions render in the
      host's proportional font again rather than monospace. Flag that as an item for plan 19.1-09's live
      observation; do not record it as approved here.

    (d) **Detail-Mode Density (:336-345)** — the accepted-degradation sentence currently grants a
    second physical line to a row that overflows the cap. Amend it: overflow is now absorbed by wrapping
    INSIDE a cell rather than by the whole row wrapping, so the "each sub-row stays exactly one physical
    line" rule holds for more cases than before, with a wrap inside a cell as the remaining exception.

    (e) **Amendment Index (:22)** and **Component / Render Inventory (:354)** — add entries for
    everything above so the index does not silently omit the largest amendment in the document.

    (f) **Verification Honesty (:381-393)** — leave rows 5 and 6 classified `MANUAL ONLY`. Do NOT
    reclassify either as machine-checkable, now or ever. Append to each the note that plan 19.1-07's
    new `uat05-detail-row-grid-is-width-bounded-and-cannot-widen-the-region` scenario is a STRUCTURAL
    proxy — it proves the markup cannot widen the region by construction, and it still cannot prove the
    deployed region stops short of the centreline.

    (g) **Do not touch** the `## Checker Sign-Off` checkboxes or the `Approval:` line. Those belong to
    the UI checker.
  </action>
  <acceptance_criteria>
    - `grep -c 'display:inline-grid' 19.1-UI-SPEC.md` returns at least `1`.
    - `grep -c 'grid-template-columns' 19.1-UI-SPEC.md` returns at least `1`.
    - `grep -c '22.5rem' 19.1-UI-SPEC.md` returns at least `1`, and the surrounding text contains
      `1020`, `540`, `450` and `PENDING LIVE CONFIRMATION`.
    - `grep -n 'never on an ancestor' 19.1-UI-SPEC.md` either returns nothing, or the matched sentence
      is scoped to `color` — read the surrounding sentence to confirm; it must no longer make a claim
      about `font-family`.
    - `grep -c "DejaVu Sans Mono" 19.1-UI-SPEC.md` — every remaining occurrence sits inside a passage
      explicitly marked SUPERSEDED or inside the two-step history paragraph; none appears as a live
      instruction.
    - `grep -c 'SUPERSEDED' 19.1-UI-SPEC.md` is strictly greater than its pre-task count — passages were
      marked, not deleted.
    - `grep -c 'Do not introduce `<div>`, CSS Grid' 19.1-UI-SPEC.md` still returns `1` — the retired
      warning is preserved as history rather than removed.
    - `grep -c 'MANUAL ONLY' 19.1-UI-SPEC.md` is greater than or equal to its pre-task count — no honesty
      row was reclassified.
    - The `## Checker Sign-Off` block is byte-identical to its pre-task state
      (`git diff 19.1-UI-SPEC.md` shows no hunk touching those lines).
    - `grep -c 'SOURCE_ATTRIBUTION_FIELD_WIDTH' 19.1-UI-SPEC.md` returns at least `1`.
  </acceptance_criteria>
  <verify>
    <automated>D=.planning/phases/19.1-day-report-render-width-product-neutral-copy-and-config-key-; grep -q 'display:inline-grid' "$D/19.1-UI-SPEC.md" && grep -q '22.5rem' "$D/19.1-UI-SPEC.md" && grep -q 'PENDING LIVE CONFIRMATION' "$D/19.1-UI-SPEC.md" && grep -q 'SOURCE_ATTRIBUTION_FIELD_WIDTH' "$D/19.1-UI-SPEC.md" && test "$(grep -c 'MANUAL ONLY' "$D/19.1-UI-SPEC.md")" -ge 2 && ! git diff "$D/19.1-UI-SPEC.md" | grep -q 'Checker Sign-Off' && echo "OK UI-SPEC reconciled, honesty rows and sign-off intact"</automated>
  </verify>
  <done>The UI-SPEC describes the shipped grid mechanism, states the available-space-first derivation
  with the cap marked pending live confirmation, no longer asserts anything false about `font-family`,
  and preserves every retired passage as marked history. The Checker Sign-Off block is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add supersession notes to RESEARCH.md's pattern, anti-pattern and pitfall sections</name>
  <files>.planning/phases/19.1-day-report-render-width-product-neutral-copy-and-config-key-/19.1-RESEARCH.md</files>
  <read_first>
    - 19.1-RESEARCH.md sections at :198 (Pattern 1), :222 (Pattern 2), :396 (Anti-Patterns to Avoid),
      :469 (Pitfall 2), :486 (Pitfall 3), :499 (Pitfall 4), :581 (Assumptions Log), :593 (Open
      Questions). Read each in full before amending — several state citations that stay valid even
      where the conclusion does not.
    - 19.1-PATTERNS.md, "THE CENTRAL TENSION the planner must resolve" — the precise statement of the
      conflict between this document and the operator's chosen remedy.
    - The amended 19.1-UI-SPEC.md from Task 1, so the two documents agree.
  </read_first>
  <action>
    Amend `19.1-RESEARCH.md` with supersession notes. This is a research record: never rewrite a finding
    to look as though it had said something else. Append dated notes; keep the original text.

    - **Pattern 1** (`min-width` + `display:inline-block`): append
      `SUPERSEDED (gap closure, plan 19.1-06, 2026-09-10)`. Record that the pattern was correctly
      implemented and did not regress any column-alignment invariant, but that its character-advance
      alignment REQUIRED a monospace stream, and 450px of DejaVu Sans Mono at a 20px root fits ~37
      characters while the column contract needed 54 — so no cap value could satisfy both constraints
      while this pattern stood. Point to the replacement in the amended UI-SPEC.
    - **Pattern 2** (explicit `max-width` on the wrapper): the pattern SURVIVES; only its derivation
      order was wrong. Append a note stating the cap is still the right lever and is still on the same
      wrapper, but the value must be derived from measured available space first. Cite
      `19.1-LIVE-CHECK.md`.
    - **Anti-Patterns to Avoid** and **Pitfall 3** (breaking the allowlist with a new tag): append a
      note to each. Both warned against `<div>`, CSS Grid and `<table>`/`<tr>`/`<td>` as one group. The
      warning is UPHELD for `<div>`/`<table>`/`<tr>`/`<td>` — those are new tag shapes and would force
      an `INERT_MARKUP_ALLOWLIST` entry that all ~190 frontend scenarios then re-exercise. It was
      OVER-BROAD for CSS Grid, which is a property value, not a tag: the shipped mechanism uses
      `display:inline-grid` on the existing `<span style="...">` and the allowlist is byte-identical.
      State that this is proven by execution in plan 19.1-07's
      `wr07-assert-inert-markup-accepts-module-tags-and-rejects-everything-else`, whose `<div>` mutation
      goes RED — so the finding's underlying claim (a new TAG breaks the allowlist) is confirmed, not
      contradicted.
    - **Pitfall 4** (`ch` resolving against the wrong font): append a note that the pitfall is now
      MOOT for the detail rows because no length in that markup resolves against a font. Keep the
      citation and the explanation intact — it remains the correct reason the retired mechanism needed
      its row-level `font-family` declaration, and it remains binding for any future use of `ch` or `ex`
      anywhere in this module.
    - **Pitfall 2** (mechanism change alone does not guarantee the region shrinks): append a note that
      this finding was CORRECT and was the one the shipped fix under-weighted. Under the new mechanism
      it is satisfied structurally rather than by a separate cap: the row is `width:100%` of a capped
      container with zero-floored tracks, so mechanism and containment are no longer independent.
    - **Assumptions Log**: mark assumption A1 (the ~0.6em monospace advance ratio) as
      `RETIRED — no longer load-bearing`, with the reason: the cap no longer multiplies a character
      count by an assumed advance ratio, so no estimate of glyph width appears anywhere in the
      derivation. This is the single assumption whose failure mode produced the defect; say so.
    - **Open Questions**: Open Question 1 (the deployed region's available width was never measured)
      is now ANSWERED by `19.1-LIVE-CHECK.md`; append the measured numbers and the citation. Open
      Question 2 (the `also:` indent drifting under a CSS mechanism) is now MOOT; append the reason —
      the indent arithmetic was removed entirely and replaced by a shared track declaration.

    Do not delete any citation, any source-confidence rating, or the Package Legitimacy Audit section.
    No package is added, removed or re-rated by this gap closure.
  </action>
  <acceptance_criteria>
    - `grep -c 'SUPERSEDED' 19.1-RESEARCH.md` is strictly greater than its pre-task count.
    - `grep -c '19.1-LIVE-CHECK.md' 19.1-RESEARCH.md` returns at least `2`.
    - The Anti-Patterns section and Pitfall 3 each contain the string `display:inline-grid` and the
      word `over-broad` (or `over-broad`'s explicit equivalent) — the reconciliation is stated, not
      implied.
    - Pitfall 3's original warning text against `<div>`/`<table>`/`<tr>`/`<td>` is still present
      verbatim.
    - The Assumptions Log entry for the ~0.6em advance ratio contains `RETIRED`.
    - Open Question 1's section contains the measured numbers `1020`, `540` and `450`.
    - `git diff 19.1-RESEARCH.md` contains no deletion of a line beginning with `[CITED:` — no citation
      was removed.
    - `git diff --stat 19.1-RESEARCH.md` shows additions substantially exceeding deletions (this is an
      append-notes task, not a rewrite).
  </acceptance_criteria>
  <verify>
    <automated>D=.planning/phases/19.1-day-report-render-width-product-neutral-copy-and-config-key-; grep -q 'display:inline-grid' "$D/19.1-RESEARCH.md" && test "$(grep -c '19.1-LIVE-CHECK.md' "$D/19.1-RESEARCH.md")" -ge 2 && grep -q 'RETIRED' "$D/19.1-RESEARCH.md" && ! git diff "$D/19.1-RESEARCH.md" | grep -q '^-.*\[CITED:' && echo "OK RESEARCH.md reconciled by appended notes, all citations intact"</automated>
  </verify>
  <done>Pattern 1, Pattern 2, Anti-Patterns, Pitfalls 2/3/4, the Assumptions Log and both Open Questions
  each carry a dated note reconciling them with the shipped mechanism. Every original finding, warning
  and citation is preserved; nothing was rewritten to look as though it had said something else.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Design documents → future implementers | These documents are the instruction set a later phase reads before touching this render path. A spec that still describes a retired mechanism, or that reclassifies a `MANUAL ONLY` claim as machine-checkable, causes the next implementer to reintroduce the defect or to trust a probe that cannot see layout. This is the only threat surface a docs-only plan has, and it is a real one — this phase's own history is the evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19.1G-09 | Repudiation (false claim strength) | `19.1-UI-SPEC.md` Verification Honesty rows 5 and 6 | mitigate | Both rows stay `MANUAL ONLY`. The new structural probe is recorded as a proxy that cannot prove the layout claim. Pinned by a `grep -c 'MANUAL ONLY'` acceptance criterion that must not decrease, and by marking the `22.5rem` value `[PENDING LIVE CONFIRMATION — plan 19.1-09]` rather than confirmed. |
| T-19.1G-10 | Tampering (history loss) | Superseded passages in both documents | mitigate | Every retired passage is MARKED, never deleted — including RESEARCH.md's `<div>`/`<table>` warning, whose underlying claim remains correct and binding. Pinned by acceptance criteria asserting the retired warning text still greps, that `SUPERSEDED` counts increase, and that no `[CITED:` line was deleted. |
| T-19.1G-11 | Elevation (indirect) | Escaping contract documentation | accept | Neither document is the escaping control; `escapeHtml` at the leaf is enforced in code (plan 19.1-06 T-19.1G-01) and in the probe suite (plan 19.1-07 T-19.1G-06). This plan changes no executable file — `MMM-SPCOutlook.js` and `scripts/**` are not in `files_modified`. |
| T-19.1G-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable: this plan modifies two Markdown files and nothing else. No package-manager install task exists; no dependency is added, removed or re-rated, and RESEARCH.md's Package Legitimacy Audit section is explicitly out of scope for modification. |
</threat_model>

<verification>
- `git diff --name-only` for this plan lists exactly the two Markdown files in `files_modified` —
  `MMM-SPCOutlook.js`, `scripts/**`, `package.json` and `pnpm-lock.yaml` are untouched.
- `node scripts/probe-payload-resilience.js` still reports the same result as at plan start (this plan
  changes no executable file, so the count must not move).
- No `[CITED:` line and no `MANUAL ONLY` row was removed from either document.
- Both documents name `display:inline-grid`, `22.5rem` and `19.1-LIVE-CHECK.md`.
</verification>

<success_criteria>
- No signed design document still instructs a future implementer to build the retired
  `ch`/monospace/`inline-block` mechanism.
- The deferred 19.1-05 Task 3 part (e) is closed: Typography no longer makes a false claim about
  `font-family`.
- RESEARCH.md's CSS-Grid warning is reconciled with a stated reason, its `<div>`/`<table>` half upheld,
  and its proof pointed at an executed mutation rather than at prose.
- The correct derivation ORDER is stated as a rule, not just applied once.
- The `22.5rem` cap is marked pending live confirmation, not confirmed.
</success_criteria>

<output>
Create `.planning/phases/19.1-day-report-render-width-product-neutral-copy-and-config-key-/19.1-08-SUMMARY.md`
when done. It MUST list every section amended in each document with its heading, and quote the final
text of the Typography sentence that replaced "never on an ancestor or a sibling" so the reviewer can
check the deferred item is genuinely closed.
</output>
