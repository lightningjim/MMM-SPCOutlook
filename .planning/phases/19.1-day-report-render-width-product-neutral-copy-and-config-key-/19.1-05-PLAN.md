---
phase: 19.1-day-report-render-width-product-neutral-copy-and-config-key
plan: 05
type: execute
wave: 5
depends_on: ["19.1-04"]
files_modified:
  - MMM-SPCOutlook.js
  - scripts/probe-payload-resilience.js
  - .planning/phases/19-unified-day-report-getdom-rewrite/19-PARITY-CHECKLIST.md
  - .planning/phases/19.1-day-report-render-width-product-neutral-copy-and-config-key-/19.1-UI-SPEC.md
autonomous: false
# No requirement IDs exist for this phase (ROADMAP: `Requirements: TBD`). See `traces`.
requirements: []
traces:
  - "19-UAT.md test 5 — the MAJOR defect, observable ONLY on the deployed hardware"
  - "19.1-UI-SPEC.md Verification Honesty rows 5 and 6 (both MANUAL ONLY) and Region Containment's '[ASSUMED — must be live-tuned]' tag"
  - "19.1-RESEARCH.md Open Question 1 (the deployed region's available width was never measured and cannot be without the real hardware)"
  - "19-PARITY-CHECKLIST.md row 1 (the loading string, changed by D-03)"
  - "19.1-CONTEXT.md D-03, D-06 (Copywriting Contract amendment)"

must_haves:
  truths:
    - "The detail-mode day report no longer overlaps the centre column on the deployed MagicMirror, observed by the operator on real hardware"
    - "The max-width cap in the code is the value the live observation actually supports, not the assumed starting candidate"
    - "If the field widths were reduced during tuning, the cap was recomputed from the same formula rather than nudged independently"
    - "The new loading string and the new header fallback were observed on the real display"
    - "19-PARITY-CHECKLIST.md row 1 reflects the changed loading string rather than the retired one"
    - "Every claim that cannot be observed is recorded as NOT OBSERVABLE with its reason, never as PASS"
  artifacts:
    - path: ".planning/phases/19.1-day-report-render-width-product-neutral-copy-and-config-key-/19.1-LIVE-CHECK.md"
      provides: "the live observation record: what was measured, what was observed, what was not observable and why"
    - path: ".planning/phases/19-unified-day-report-getdom-rewrite/19-PARITY-CHECKLIST.md"
      provides: "row 1 updated to the new loading string, plus a Probe Coverage note for the new width guards"
    - path: ".planning/phases/19.1-day-report-render-width-product-neutral-copy-and-config-key-/19.1-UI-SPEC.md"
      provides: "the Region Containment value promoted from [ASSUMED] to live-confirmed (or corrected)"
  key_links:
    - from: "19.1-UI-SPEC.md Region Containment"
      to: "MMM-SPCOutlook.js wrapper.style.maxWidth"
      via: "the same literal value, recomputed from the field-width formula if either changed"
      pattern: "wrapper\\.style\\.maxWidth"
    - from: "19-PARITY-CHECKLIST.md row 1"
      to: "MMM-SPCOutlook.js loading branch"
      via: "the quoted string must match the code verbatim"
      pattern: "Loading NWS outlooks"
---

<objective>
Verify on the deployed Raspberry Pi that the detail-mode day report no longer overlaps the centre
column, tune the `max-width` cap to what the real region can actually accommodate, and reconcile the
two documents whose content this phase invalidated.

Purpose: this is the ONLY step that can prove the MAJOR defect is fixed. No probe scenario can — the
harness has no layout engine, and `jsdom`/`linkedom` both document that they do not implement CSS
layout. 19.1-RESEARCH.md Open Question 1 is explicit that the deployed region's available width was
never measured and cannot be without the hardware. It is also the discipline that caught the original
defect AND caught the 19-08 invalidated observation (a measurement taken against a 21-commits-stale
deploy with the misspelled config key, so detail mode was never active).

Output: a live observation record, a tuned-and-traceable cap, an updated parity checklist, and a
UI-SPEC whose `[ASSUMED]` tag is resolved.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/buildomator/bm/4.6.0/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/buildomator/bm/4.6.0/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/19.1-day-report-render-width-product-neutral-copy-and-config-key-/19.1-CONTEXT.md
@.planning/phases/19.1-day-report-render-width-product-neutral-copy-and-config-key-/19.1-UI-SPEC.md
@.planning/phases/19-unified-day-report-getdom-rewrite/19-UAT.md
@.planning/phases/19-unified-day-report-getdom-rewrite/19-PARITY-CHECKLIST.md

<interfaces>
<!-- Target hardware facts, verified during the 19.1 research session. -->

Reachable as:            ssh mm
Hardware:                Raspberry Pi 4 Model B, aarch64, Node v24.13.1, MagicMirror 2.37.0
Deployed coordinate:     OKC production — lat 35.4432156, lon -97.595822
Config state:            `dayReportDetail: true` is DELIBERATELY left on by the operator (not
                         leftover state) — do not turn it off, and do not "fix" it
Config backup:           ~/MagicMirror/config/config.js.bak-19uat
Module install path:     the deployed MagicMirror's modules directory (confirm with
                         `ssh mm 'ls ~/MagicMirror/modules'` — do not assume a symlink vs a clone)
Log:                     mm-out.log (the channel the config-key warning from plan 19.1-02 writes to)
Root font size:          MagicMirror's `--font-size: 20px` (~/MagicMirror/css/main.css)
Region CSS:              `.region { position:absolute; <directional insets only> }` — no width,
                         no max-width, no overflow. Shrink-to-fit per CSS2.1 §10.3.7.

The 19-08 false-negative precedent (from 19.1-CONTEXT.md `<specifics>`): the earlier width
observation was invalid on TWO counts — it ran against deployed `ca2a3f3` (21 commits behind main)
AND the config carried the misspelled `dayReportDetails`, so global detail mode was never active and
every expanded day came from `autoExpand` alone. Both conditions must be positively confirmed before
any observation in this plan is recorded.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy the phase's changes to the Pi and positively confirm the observation is valid</name>
  <files>scripts/probe-payload-resilience.js</files>
  <read_first>
    - .planning/phases/19.1-day-report-render-width-product-neutral-copy-and-config-key-/19.1-CONTEXT.md
      `<specifics>` → "Evidence status of the width defect" and "Target hardware" — the two conditions
      that invalidated the previous observation.
    - .planning/phases/19-unified-day-report-getdom-rewrite/19-UAT.md — the procedure and recording
      conventions the previous UAT used, and its `## Gaps` section for what test 5 actually reported.
    - The SUMMARYs of plans 19.1-01 through 19.1-04 — what is expected to be observable.
  </read_first>
  <action>
    Pre-flight, then deploy, then positively confirm validity. Do NOT skip the confirmation steps —
    they are the whole reason the previous observation had to be thrown away.

    1. Run the full probe suite locally and confirm `0 failed, 0 skipped`. Record the count. A red
       suite must not be deployed.
    2. Confirm the local working tree is clean and record the exact commit SHA that will be deployed.
    3. Deploy to the Pi. Inspect first (`ssh mm 'ls -la ~/MagicMirror/modules/MMM-SPCOutlook'`) to
       determine whether the deployed module is a git clone, a symlink to a working copy, or a plain
       copy, and use the matching mechanism (`git pull`/`git fetch && git checkout <sha>`, or `rsync`
       of `MMM-SPCOutlook.js` plus `README.md` plus `scripts/`). Do not guess; do not force-push
       anything; do not modify `~/MagicMirror/config/config.js`.
    4. **Confirm condition 1 — the deployed code is THIS code.** `ssh mm` and verify the deployed
       `MMM-SPCOutlook.js` contains `wrapper.style.maxWidth`, contains
       `display:inline-block;min-width:`, contains `Loading NWS outlooks`, and contains
       `getHeader: function`. If the deploy is a git checkout, also confirm the deployed SHA equals
       the SHA recorded in step 2.
    5. **Confirm condition 2 — global detail mode is genuinely in effect.** Read the deployed
       `~/MagicMirror/config/config.js` (read-only) and confirm the `MMM-SPCOutlook` entry carries
       `dayReportDetail: true` spelled singular, inside the nested `config: {}` block. If it carries
       the misspelled plural, STOP and report it: plan 19.1-02's validator will now warn about it in
       `mm-out.log`, which is itself a live confirmation of that fix, but the width observation cannot
       proceed until the operator corrects the key. Record which spelling was found either way — it is
       evidence for the 19.1-02 fix.
    6. Restart MagicMirror and capture `mm-out.log` from the restart onward. Extract and record: any
       `unrecognized config key` line (plan 19.1-02's warning — this is the live confirmation of UAT
       test 13, and its presence or absence depends on step 5's finding), the module-start line, and
       the first-accepted-payload timing line.
    7. Create
       `.planning/phases/19.1-day-report-render-width-product-neutral-copy-and-config-key-/19.1-LIVE-CHECK.md`
       with sections: `## Deploy` (SHA, mechanism, the four code-presence confirmations),
       `## Validity Preconditions` (conditions 1 and 2, each PASS/FAIL with evidence),
       `## Log Evidence` (the extracted lines), and empty `## Observations` / `## Disposition`
       sections for Task 2 to fill.

    Note: the only repo file this task may modify is `scripts/probe-payload-resilience.js`, and only
    if step 1 exposes a genuine failure that must be fixed before deploying. If step 1 is green,
    expect a zero-diff task.
  </action>
  <acceptance_criteria>
    - `node scripts/probe-payload-resilience.js` → `0 failed, 0 skipped`; the count is recorded in
      `19.1-LIVE-CHECK.md`.
    - `git status --porcelain` is empty at deploy time and the deployed SHA is recorded.
    - CLI output: `ssh mm 'grep -c "wrapper.style.maxWidth" ~/MagicMirror/modules/MMM-SPCOutlook/MMM-SPCOutlook.js'`
      returns a non-zero count; the same for `display:inline-block;min-width:`,
      `Loading NWS outlooks`, and `getHeader: function`. All four counts are recorded.
    - CLI output: the deployed `config.js`'s `MMM-SPCOutlook` entry is quoted verbatim in
      `19.1-LIVE-CHECK.md`, with the `dayReportDetail` spelling explicitly stated.
    - MagicMirror restarted; `mm-out.log` lines from the restart are quoted in `19.1-LIVE-CHECK.md`.
    - `19.1-LIVE-CHECK.md` exists with all four sections, and `## Validity Preconditions` records both
      conditions as PASS with evidence (or the plan stops here with a FAIL and a named blocker).
  </acceptance_criteria>
  <verify>
    <automated>node scripts/probe-payload-resilience.js 2>&1 | tail -1 | grep -q '0 failed, 0 skipped' && test -z "$(git status --porcelain -- MMM-SPCOutlook.js scripts README.md)" && ssh mm 'F=~/MagicMirror/modules/MMM-SPCOutlook/MMM-SPCOutlook.js; for p in "wrapper.style.maxWidth" "display:inline-block;min-width:" "Loading NWS outlooks" "getHeader: function"; do c=$(grep -c "$p" "$F"); echo "$p -> $c"; [ "$c" -ge 1 ] || exit 1; done' && test -f .planning/phases/19.1-day-report-render-width-product-neutral-copy-and-config-key-/19.1-LIVE-CHECK.md && echo "OK deployed code confirmed to contain all four of this phase's changes"</automated>
  </verify>
  <done>The suite is green, the deployed module provably contains all four of this phase's code
  changes, the deployed `dayReportDetail` spelling is recorded, `mm-out.log` evidence is captured, and
  `19.1-LIVE-CHECK.md` records both validity preconditions as PASS. Neither of the two conditions that
  invalidated the 19-08 observation can be true.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator observes the live render and decides the cap</name>
  <files>.planning/phases/19.1-day-report-render-width-product-neutral-copy-and-config-key-/19.1-LIVE-CHECK.md</files>
  <action>
    Present the verification script below to the operator, collect a verdict for every one of items
    1-10, and write them into `19.1-LIVE-CHECK.md`'s `## Observations` section as a ten-row table
    (one row per item: number, what was checked, PASS/FAIL/NOT OBSERVABLE, reason/evidence). Then
    record the operator's cap decision verbatim, with the date, under `## Disposition`. Do not
    paraphrase a verdict, do not infer one from another item, and do not record PASS for anything the
    operator did not state. If any of items 4, 5, 6 comes back FAIL, stop: that is a column-alignment
    regression and a code defect, which must be fixed and re-deployed before this checkpoint is
    re-run. Do not modify the deployed `~/MagicMirror/config/config.js` at any point.
  </action>
  <what-built>
    Plans 19.1-01 through 19.1-04 landed and are deployed to the Pi (Task 1 proved the deployed file
    contains all four changes):
    - The detail rows' column padding is now `display:inline-block;min-width:Nch` CSS boxes instead of
      literal trailing spaces, so the rows no longer force a hard min-content width that `pre-wrap`
      forbids collapsing.
    - The wrapper carries `max-width: 32.4rem` (≈650px at the host's 20px root) alongside its existing
      left-alignment anchor, bounding the region's shrink-to-fit calculation.
    - The loading string now reads `Loading NWS outlooks...`, and a `getHeader()` fallback supplies
      `NWS Hazard Outlooks` when no `header:` key is configured.
    - An unrecognised config key now produces a `Log.warn` in `mm-out.log` naming the key and its
      nearest `defaults` match.
    - 5 new probe scenarios guard the mechanism; none of them can see layout. That is what this
      checkpoint is for.
  </what-built>
  <how-to-verify>
    Look at the deployed mirror (or a photo/screenshot of it) with `dayReportDetail: true` active.

    **A — The MAJOR defect (the one that matters most):**
    1. Does the day-report block still extend into the centre column? Compare against the screenshot
       in 19-UAT.md test 5.
    2. Do the compact (unexpanded) day rows still look displaced relative to the module's other
       content?
    3. If the block is contained but text now WRAPS onto a second line on some rows: that is an
       accepted, documented degradation for genuinely-too-wide rows (19.1-UI-SPEC.md's Detail-Mode
       Density amendment) — report WHICH rows wrap and how often, because a wrap on an ordinary row
       means the cap is too tight and the field widths should come down instead.

    **B — The column alignment must not have regressed** (all three were confirmed PASS in the same
    19-UAT screenshot, so a regression here is visible by direct comparison):
    4. Is the em-dash source-attribution column still straight down the block?
    5. Do co-equal hazards still render as peers (e.g. Flash Flood Marginal / Heat Extreme on one day),
       with the second one NOT demoted to an `also:` row?
    6. Do `also:` rows still sit indented under their own winner row rather than detached from it?

    **C — One deliberate visual change to confirm is acceptable:**
    7. The literal `also:` text and the `— SPC` / `— WSSI` attributions now render in the monospace
       font (DejaVu Sans Mono) rather than the host's proportional font. This was necessary: the `ch`
       unit resolves against the element's own font, so the uncoloured indent spacer had to inherit
       monospace or the columns would not align. Does it look acceptable at viewing distance?

    **D — Copy:**
    8. If the module has no `header:` key in config, does the header read `NWS Hazard Outlooks`? If it
       has one, does it still read exactly what you set? (If you want to see the fallback, the only
       safe way is to remove the `header:` line — your choice, and it is optional.)
    9. The loading string is transient and was NOT OBSERVABLE in both Phase 19 UAT runs. If you happen
       to catch it during the restart, confirm it reads `Loading NWS outlooks...`; if not, it is
       recorded NOT OBSERVABLE again, which is honest and expected.

    **E — Config key (from the log, not the screen):**
    10. Task 1 captured `mm-out.log`. If the deployed config still carries a misspelled key, there
        should be a line naming it and suggesting the correct spelling. If the config is clean, there
        should be NO such line — confirm which you see.

    **THE DECISION.** Reply with one of:
    - `approved` — the block is contained, nothing in B regressed, C is acceptable. `32.4rem` stands.
    - `narrower: <value>` — still overlapping. The cap comes down. Note that 19.1-UI-SPEC.md permits
      reducing `DIMENSION_FIELD_WIDTH`/`DETAIL_LABEL_FIELD_WIDTH` instead of (or as well as) the cap,
      and REQUIRES that the cap be recomputed from the field-width formula rather than nudged
      independently, so the two cannot drift. Say roughly how much too wide it looks (e.g. "about a
      quarter of the block is over the line") and Task 3 will recompute both.
    - `wider: <value>` — ordinary rows are wrapping, the cap is too tight.
    - Or describe what you see and I will propose the recomputation.

    Also tell me the verdict for each of items 1-10 (PASS / FAIL / NOT OBSERVABLE and why) so the
    record is complete — a NOT OBSERVABLE with its reason is a valid and expected outcome for items 9
    and possibly 5, 6 and 8.
  </how-to-verify>
  <read_first>
    - .planning/phases/19.1-day-report-render-width-product-neutral-copy-and-config-key-/19.1-LIVE-CHECK.md
      (created by Task 1) — the deploy and precondition evidence.
    - .planning/phases/19-unified-day-report-getdom-rewrite/19-UAT.md tests 5, 11, 13 and `## Gaps` —
      the baseline observation this is compared against.
    - 19.1-UI-SPEC.md "Region Containment" (the derivation and what tuning is permitted) and
      "Verification Honesty" rows 5-6.
  </read_first>
  <acceptance_criteria>
    - `19.1-LIVE-CHECK.md` `## Observations` records a verdict for every one of items 1-10, each
      PASS / FAIL / NOT OBSERVABLE **with its reason** — no item left blank, and no item recorded as
      PASS on inference rather than observation.
    - `## Disposition` records the operator's decision verbatim (`approved` / `narrower: <value>` /
      `wider: <value>` / free text) and the date.
    - If any item in B (4, 5, 6) is FAIL, the plan does not proceed to Task 3's doc updates: a
      column-alignment regression is a code defect and must be fixed first, with the fix re-deployed
      and this checkpoint re-run.
    - Item 1's verdict is stated against the 19-UAT test 5 screenshot explicitly, not in the abstract.
  </acceptance_criteria>
  <verify>
    <human-check>Operator confirms items 1-10 on the deployed mirror and states the cap decision.</human-check>
    <automated>grep -qE '^## Disposition' .planning/phases/19.1-day-report-render-width-product-neutral-copy-and-config-key-/19.1-LIVE-CHECK.md && test "$(grep -cE '^\| *([1-9]|10) *\|' .planning/phases/19.1-day-report-render-width-product-neutral-copy-and-config-key-/19.1-LIVE-CHECK.md)" -ge 10 && echo "OK all ten observation verdicts recorded with a disposition"</automated>
  </verify>
  <done>`19.1-LIVE-CHECK.md` carries a ten-row `## Observations` table with a verdict and a reason
  for every item, and a `## Disposition` section recording the operator's cap decision verbatim with
  its date.</done>
  <resume-signal>Reply `approved`, or `narrower: <value>`, or `wider: <value>`, or describe what you see — plus the PASS/FAIL/NOT OBSERVABLE verdict for items 1-10.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Apply the tuning outcome and reconcile the two invalidated documents</name>
  <files>MMM-SPCOutlook.js, scripts/probe-payload-resilience.js, .planning/phases/19-unified-day-report-getdom-rewrite/19-PARITY-CHECKLIST.md, .planning/phases/19.1-day-report-render-width-product-neutral-copy-and-config-key-/19.1-UI-SPEC.md</files>
  <read_first>
    - .planning/phases/19.1-day-report-render-width-product-neutral-copy-and-config-key-/19.1-LIVE-CHECK.md
      — Task 2's `## Disposition`, which determines whether any code changes at all.
    - .planning/phases/19-unified-day-report-getdom-rewrite/19-PARITY-CHECKLIST.md — the frontmatter
      (`baseline_probe_result`, `final_probe_result`, `review_fix_probe_result`), the line-citation
      note, and row 1 (the loading string) in full, including its `Probe Coverage` / Run A / Run B
      cells.
    - 19.1-UI-SPEC.md "Region Containment" (the `[ASSUMED — must be live-tuned]` tag and the
      recompute-don't-nudge rule) and the "Checker Sign-Off" block.
    - MMM-SPCOutlook.js lines 380-395 and the wrapper block — the two field-width constants and the
      cap, if the disposition requires changing them.
    - scripts/probe-payload-resilience.js — the `uat05-wrapper-declares-both-region-containment-
      contracts` scenario from plan 19.1-04, whose asserted literal must match whatever value ships.
  </read_first>
  <action>
    **(a) Code, only if the disposition requires it.** If the operator approved `32.4rem`, change no
    code. Otherwise:
    - Apply the new cap, and — if the field widths come down too — change
      `DETAIL_LABEL_FIELD_WIDTH` (and/or the `+ 1` in `DIMENSION_FIELD_WIDTH`'s derivation) and then
      RECOMPUTE the cap from the same formula recorded in the wrapper comment
      (`2 + DIMENSION_FIELD_WIDTH + DETAIL_LABEL_FIELD_WIDTH + 3 + 1 + 1 + 11` characters × 0.6em).
      Do not nudge the rem value independently of the field widths — 19.1-UI-SPEC.md requires the two
      stay derived from one another so they cannot drift. Update the comment's arithmetic to the new
      numbers.
    - Update the `uat05-wrapper-declares-both-region-containment-contracts` scenario's asserted literal
      to the shipped value, and update the `min-width:Nch` numbers asserted by the plan-19.1-04
      scenarios and the repaired `wr04-*`/`wr02-*` scenarios if a field width changed. Re-run the full
      suite to green.
    - Re-deploy and re-run Task 2's checkpoint. Do not record an approval for a value that was never
      observed.

    **(b) `19-PARITY-CHECKLIST.md` row 1.** The row currently reads `!this.spcrisk` → literal
    `"Loading SPC Outlook..."` with `NOT OBSERVABLE` in both run columns. Update it to quote
    `"Loading NWS outlooks..."`, and append a dated note in the row's `Notes` cell recording: that
    Phase 19.1 D-03 changed the string to the NWS umbrella; that the behavior being preserved
    (hardcoded literal, unconditional `innerHTML`, no payload participation) is unchanged; the new
    scenario name that now gives this row probe coverage
    (`uat11-loading-string-names-the-nws-umbrella-not-spc-alone`, so the `Probe Coverage` cell moves
    off `MANUAL ONLY` for the string's identity while the *transient visibility* of the branch stays
    unobservable); and the Phase 19.1 live verdict for item 9 from `19.1-LIVE-CHECK.md`. Do not rewrite
    the Run A / Run B cells' Phase 19 history — append, do not overwrite.
    Also append a short `## Phase 19.1 Addendum` section at the end of the checklist listing the new
    Probe Coverage for the width mechanism (the three `uat05-*` scenario names) and stating plainly
    that the region-overlap claim itself remains `MANUAL ONLY`, citing `19.1-LIVE-CHECK.md` item 1 as
    its only evidence. Update the frontmatter with a `phase_19_1_probe_result` line carrying the final
    suite count.

    **(c) `19.1-UI-SPEC.md`.** In "Region Containment", replace the
    `[ASSUMED — must be live-tuned, see below]` tag on the cap with the live outcome: the confirmed
    value, the date, the hardware, and a one-line statement of what was observed. Keep the derivation
    paragraph — it is why the number is traceable. Keep "Verification Honesty" rows 5 and 6 as
    `MANUAL ONLY` and add the `19.1-LIVE-CHECK.md` citation to each; do NOT reclassify them as
    machine-checkable, now or ever. Leave the `## Checker Sign-Off` checkboxes and the `Approval:`
    line alone — that is the UI checker's to complete, not this plan's.

    **(e) `19.1-UI-SPEC.md` — record the row-level `font-family` mechanism** (plan-checker warning 1).
    Plan `19.1-03` Task 1 deliberately deviated from this spec: the spec's "Detail line — Column
    contract" table places the `font-family`/`color` carrier on the inner `detailColoredSpan` only, and
    its Typography section says the declaration goes there and "never on an ancestor or a sibling". The
    implementation instead declares `font-family:'DejaVu Sans Mono','Liberation Mono',monospace` on the
    **row-level** span, because the spec's own `also:` indent spacer
    (`<span style="display:inline-block;min-width:Nch"></span>`, uncoloured, no font-family) sits
    outside every coloured span and would therefore resolve its `ch` against the host's proportional
    Roboto Condensed — RESEARCH.md Pitfall 4 applied to the one element the spec left bare. The
    deviation is correct on the merits and is corroborated by `wr02-*`'s own comment at
    `scripts/probe-payload-resilience.js:13930-13932` ("every sub-row is white-space:pre-wrap
    monospace"), but nothing has yet written it back into the contract.

    Amend two places so the signed spec stops contradicting shipped code — the same rule CONTEXT.md
    states for the primary mechanism swap ("make the amendment explicitly; do not leave the spec
    contradicting the code"):
    - the "Detail line — Column contract" table's font-family-carrier row: the monospace family is
      declared on the row-level span; the nested width-bearing spans and the `also:` spacer inherit it;
      `color` still lives on `detailColoredSpan` alone, so the D-02 "dimension name and label share one
      colour" unit is unchanged.
    - the Typography section's "never on an ancestor" sentence: narrow it to `color`, and state the
      Pitfall-4 reason `font-family` must sit at row level instead.

    Record the accepted visible consequence in the same edit: the literal `also:` token and the
    `— SPC` / `— WSSI` source attributions render monospace where they were previously proportional,
    observed and signed off at `19.1-LIVE-CHECK.md` item 7. If item 7 came back rejected, do NOT write
    an approval here — record the rejection and stop, the same way (a) stops on a FAIL.

    **(d) STATE.md / Deferred Items are the orchestrator's, not this plan's.** If any of items 2-10
    came back NOT OBSERVABLE, note them in the SUMMARY as candidate Deferred Items rows with their
    reasons, and let the phase-close step record them.
  </action>
  <acceptance_criteria>
    - If the disposition was `approved`: `git diff MMM-SPCOutlook.js` is empty for this task, and
      `grep -c '32.4rem' MMM-SPCOutlook.js` is still `1`.
    - If the disposition changed the cap: the cap literal in `MMM-SPCOutlook.js`, the asserted literal
      in `uat05-wrapper-declares-both-region-containment-contracts`, and the value in 19.1-UI-SPEC.md's
      Region Containment section are all the SAME string — asserted by a grep of all three files for
      the new literal returning `1` each.
    - If a field width changed: the wrapper comment's arithmetic reproduces the new cap from the new
      field widths, and every `min-width:Nch` number asserted anywhere in the probe suite matches the
      new derivation.
    - `node scripts/probe-payload-resilience.js` → `0 failed, 0 skipped`.
    - `grep -c 'Loading SPC Outlook' .planning/phases/19-unified-day-report-getdom-rewrite/19-PARITY-CHECKLIST.md`
      returns `0` in row 1's behavior cell — the quoted string matches the code. (A historical mention
      inside an appended dated note is acceptable and should be phrased as history, e.g. "was
      `Loading SPC Outlook...` before 19.1 D-03".)
    - `grep -c 'Loading NWS outlooks' .planning/phases/19-unified-day-report-getdom-rewrite/19-PARITY-CHECKLIST.md`
      returns at least `1`.
    - `19-PARITY-CHECKLIST.md` contains a `## Phase 19.1 Addendum` section naming all three `uat05-*`
      scenarios and stating the region-overlap claim is `MANUAL ONLY`.
    - `19.1-UI-SPEC.md` no longer contains the string `[ASSUMED — must be live-tuned` for the cap, and
      its Verification Honesty rows 5 and 6 still say `MANUAL ONLY` and now cite `19.1-LIVE-CHECK.md`.
    - `19.1-UI-SPEC.md`'s `## Checker Sign-Off` checkboxes and `Approval:` line are byte-identical to
      their pre-task state.
    - `19.1-UI-SPEC.md` no longer asserts that `font-family` is never declared on an ancestor: the
      string `never on an ancestor` either no longer appears, or appears scoped to `color` only —
      asserted by `grep -n 'never on an ancestor' 19.1-UI-SPEC.md` and reading the surrounding
      sentence.
    - `19.1-UI-SPEC.md` contains the literal string `row-level` within its Detail-line Column-contract
      table or Typography section, describing where the monospace `font-family` is declared, AND cites
      Pitfall 4 as the reason — so the spec's mechanism matches
      `grep -n "font-family:'DejaVu Sans Mono'" MMM-SPCOutlook.js`'s actual call site.
    - `19.1-UI-SPEC.md` records the monospace `also:`/attribution consequence together with its
      `19.1-LIVE-CHECK.md` item 7 disposition.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/probe-payload-resilience.js 2>&1 | tail -1 | grep -q '0 failed, 0 skipped' && P=.planning/phases/19-unified-day-report-getdom-rewrite/19-PARITY-CHECKLIST.md && U=.planning/phases/19.1-day-report-render-width-product-neutral-copy-and-config-key-/19.1-UI-SPEC.md && grep -q 'Loading NWS outlooks' "$P" && grep -q '## Phase 19.1 Addendum' "$P" && grep -q 'uat05-wrapper-declares-both-region-containment-contracts' "$P" && test "$(grep -c 'ASSUMED — must be live-tuned' "$U")" = "0" && test "$(grep -c 'MANUAL ONLY' "$U")" -ge 2 && grep -q 'Checker Sign-Off' "$U" && grep -q 'row-level' "$U" && grep -q 'Pitfall 4' "$U" && echo "OK docs reconciled, suite green, MANUAL ONLY rows preserved, font-family mechanism recorded"</automated>
  </verify>
  <done>The shipped cap, the probe assertion and the UI-SPEC all carry the same live-confirmed value
  (recomputed from the field-width formula if anything changed), the parity checklist's row 1 quotes
  the string the code actually emits and its addendum records the new probe coverage plus the surviving
  `MANUAL ONLY` claim, and the UI-SPEC's `[ASSUMED]` tag is resolved without reclassifying anything
  that still cannot be machine-checked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local working tree → deployed Pi module directory | Code crosses onto live hardware; a stale or partial deploy is what invalidated the 19-08 observation |
| deployed `config.js` (operator-owned secret-adjacent file) → this plan's evidence record | The config contains the deployed coordinate; it is READ, never modified, and what gets quoted into the record matters |
| live observation → verification claim | The boundary between "observed" and "inferred"; the defect this phase fixes shipped because this boundary was crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19.1-19 | Repudiation | an observation recorded against code that is not the code under test | mitigate | Task 1 positively confirms FOUR distinct markers in the deployed file (`wrapper.style.maxWidth`, `display:inline-block;min-width:`, `Loading NWS outlooks`, `getHeader: function`) plus the deployed SHA, and separately confirms `dayReportDetail` is spelled singular in the live config. These are exactly the two conditions that invalidated the 19-08 observation, checked explicitly rather than assumed. A FAIL on either stops the plan. |
| T-19.1-20 | Spoofing | a `NOT OBSERVABLE` item recorded as PASS | mitigate | Every one of items 1-10 must carry an explicit PASS / FAIL / NOT OBSERVABLE verdict **with a reason**; the acceptance criteria forbid a blank and forbid a PASS on inference. The 19-08 Deferred Items rows are the precedent: NOT OBSERVABLE with a reason is a valid, expected outcome and is recorded as such. |
| T-19.1-21 | Info disclosure | the operator's deployed coordinate / config contents entering the planning record or a log | mitigate | The coordinate (35.4432156, -97.595822) is already recorded in `19.1-CONTEXT.md` and this repo's planning docs, so quoting the `MMM-SPCOutlook` config entry discloses nothing new. Quote ONLY that module's entry — not the whole `config.js`, which may carry other modules' API keys. Plan 19.1-02's validator never logs a config VALUE (T-19.1-05), so the `mm-out.log` excerpts captured here cannot leak one either. |
| T-19.1-22 | Tampering | modifying the deployed config or the operator's backup during verification | mitigate | `~/MagicMirror/config/config.js` is READ-ONLY to this plan; `config.js.bak-19uat` is not touched. `dayReportDetail: true` is deliberate operator state and must be left in place. If the config carries the misspelled key, the plan REPORTS it and stops rather than correcting it — that is the operator's file. |
| T-19.1-23 | Tampering | the cap and the field widths drifting apart during tuning | mitigate | Task 3 requires the cap to be RECOMPUTED from the field-width formula whenever either changes, with the wrapper comment's arithmetic updated to match, and requires the shipped literal, the probe assertion and the UI-SPEC value to be the same string (asserted by three greps). |
| T-19.1-24 | Repudiation | reclassifying a `MANUAL ONLY` row as machine-checkable to close the phase | mitigate | Task 3 explicitly forbids it and asserts that 19.1-UI-SPEC.md's Verification Honesty rows 5-6 still read `MANUAL ONLY` after the edit, now citing `19.1-LIVE-CHECK.md` as their only evidence. |
| T-19.1-SC | Tampering | npm/pip/cargo installs | mitigate | Zero installs, locally and on the Pi. No `npm ci`/`npm install` is run as part of deployment (no dependency changed this phase). Gate: `git diff --name-only` must not list `package.json` or `pnpm-lock.yaml`. |
</threat_model>

<verification>
- `19.1-LIVE-CHECK.md` exists with `## Deploy`, `## Validity Preconditions`, `## Log Evidence`,
  `## Observations` (items 1-10, each with a verdict and a reason) and `## Disposition`.
- The deployed module provably contains all four of this phase's code markers.
- `node scripts/probe-payload-resilience.js` → `0 failed, 0 skipped`.
- The cap literal is identical in `MMM-SPCOutlook.js`, the `uat05-wrapper-…` scenario, and
  19.1-UI-SPEC.md's Region Containment section.
- `19-PARITY-CHECKLIST.md` row 1 quotes `Loading NWS outlooks...` and the `## Phase 19.1 Addendum`
  names the three `uat05-*` scenarios and preserves the `MANUAL ONLY` region-overlap claim.
- 19.1-UI-SPEC.md's `[ASSUMED]` cap tag is resolved; Verification Honesty rows 5-6 still
  `MANUAL ONLY`; the Checker Sign-Off block is untouched.
- `git diff --name-only` lists no `node_helper.js`, no `package.json`, no `pnpm-lock.yaml`.
</verification>

<success_criteria>
- The operator has observed the deployed detail-mode render and stated whether the block still
  overlaps the centre column, against the 19-UAT test 5 screenshot.
- The shipped `max-width` is the value that observation supports, derived from the field widths rather
  than nudged.
- No column-alignment regression (em-dash column, co-winner peers, `also:` attachment).
- Both invalidated documents are reconciled, and nothing that still cannot be machine-checked has been
  reclassified as if it could.
</success_criteria>

<output>
Create `.planning/phases/19.1-day-report-render-width-product-neutral-copy-and-config-key-/19.1-05-SUMMARY.md` when done.
</output>
