---
phase: 19.1-day-report-render-width-product-neutral-copy-and-config-key
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - MMM-SPCOutlook.js
  - README.md
  - scripts/probe-payload-resilience.js
autonomous: true
# This phase has NO requirement IDs. ROADMAP.md carries `Requirements: TBD` and this is a
# defect-fix phase traced to 19-UAT.md finding numbers and 19.1-CONTEXT.md decision IDs.
# Inventing REQ-IDs here would be fabrication — see `traces` instead.
requirements: []
traces:
  - "19-UAT.md test 11 (product-neutral copy, minor)"
  - "19.1-CONTEXT.md D-02 (getHeader as fallback only)"
  - "19.1-CONTEXT.md D-03 (umbrella name is NWS; both strings use the same umbrella)"
  - "19.1-CONTEXT.md D-04 (header: \"\" is an explicit opt-out)"
  - "19.1-CONTEXT.md D-05 (sweep covers user-facing strings plus README, and stops there)"
  - "19.1-CONTEXT.md D-06 (empty-state copy already neutral — NOT touched)"
  - "19.1-UI-SPEC.md Copywriting Contract (AMENDED), New Component: getHeader()"

must_haves:
  truths:
    - "A new operator with no `header:` key in config.js sees the header `NWS Hazard Outlooks`, not `SPC FORECAST` and not an SPC-named string"
    - "An operator with `header: \"SPC FORECAST\"` still sees `SPC FORECAST` verbatim — the module never overrides a configured header"
    - "An operator with `header: \"\"` still sees no header row at all"
    - "Before any payload arrives the module shows `Loading NWS outlooks...`, never `Loading SPC Outlook...`"
    - "The README's opening line and its `Multiple instances` section quote the same umbrella noun the runtime strings use"
  artifacts:
    - path: "MMM-SPCOutlook.js"
      provides: "getHeader() three-state override and the product-neutral loading string"
      contains: "getHeader: function"
    - path: "README.md"
      provides: "product-neutral module description and a loading-string quote that matches the code"
    - path: "scripts/probe-payload-resilience.js"
      provides: "mutation-proven scenarios pinning the three getHeader states and the loading string"
  key_links:
    - from: "MMM-SPCOutlook.js getHeader()"
      to: "this.data.header"
      via: "strict undefined check, never a truthiness check"
      pattern: "this\\.data\\.header === undefined|this\\.data === undefined"
    - from: "scripts/probe-payload-resilience.js"
      to: "MMM-SPCOutlook.js getHeader()"
      via: "loadFrontendModule() + direct call with a synthesized this.data"
      pattern: "getHeader\\.call"
---

<objective>
Retire the last two SPC-named user-facing strings (19-UAT.md test 11) by shipping a `getHeader()`
fallback and rewording the loading string to the same NWS umbrella, then pin all four states with
mutation-proven probe scenarios.

Purpose: the module aggregates SPC convective/fire, SPC MD, WPC ERO, WPC WSSI, WPC MPD, WPC/CPC
Hazards Outlook and NWS HeatRisk, but its loading text and the header an operator is steered toward
name SPC alone. A new user lands on inaccurate copy by default.

Output: `getHeader()` override, reworded loading string, README reconciliation, and four new probe
scenarios.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/buildomator/bm/4.6.0/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/buildomator/bm/4.6.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19.1-day-report-render-width-product-neutral-copy-and-config-key-/19.1-CONTEXT.md
@.planning/phases/19.1-day-report-render-width-product-neutral-copy-and-config-key-/19.1-UI-SPEC.md

<interfaces>
<!-- Extracted from the codebase and from the MagicMirror 2.37.0 source on the target Pi.
     Use directly — no exploration needed. -->

MagicMirror 2.37.0 base `Module` class (~/MagicMirror/js/module.js:119-120) ALREADY implements:
    getHeader() { return this.data.header; }
…which is why an operator's `header:` line renders today with zero code in this repo. Header
visibility is a plain truthiness check in `main.js`'s `updateModuleContent()`, so `undefined` and
`""` are ALREADY both falsy and both already suppress the header region. D-04 therefore needs no
new machinery — only that the override return the fallback when `this.data.header` is `undefined`
and return `this.data.header` verbatim in every other case.

Probe harness entry points (scripts/probe-lib/module-stubs.js, all exported):
    loadFrontendModule()            -> the module definition object captured from Module.register
    renderDom(frontend, {config, spcrisk}) -> wrapper.innerHTML (or textContent), after assertInertMarkup
    resetLogs(), logCalls           -> log assertion surface
Verified standalone (no installStubs() call needed):
    node -e "const s=require('./scripts/probe-lib/module-stubs.js');
             const f=s.loadFrontendModule();
             console.log(s.renderDom(f,{config:{lat:1,lon:2},spcrisk:null}));"
    -> "Loading SPC Outlook..."

Current strings:
    MMM-SPCOutlook.js:1069   wrapper.innerHTML = "Loading SPC Outlook...";
    README.md:2              SPC Point-in-Polygon check for location in Outlooks
    README.md:12             ...and stays on "Loading SPC Outlook...".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the getHeader() three-state override</name>
  <files>MMM-SPCOutlook.js</files>
  <read_first>
    - MMM-SPCOutlook.js lines 1-130 (the `defaults` block, `resolveUpdateInterval`, `start`) — you
      are adding a sibling lifecycle method and must match the file's comment density and its
      "warn and degrade" idiom.
    - MMM-SPCOutlook.js line 222 (`getStyles`) — the adjacent lifecycle method; place `getHeader`
      near it so the lifecycle methods stay grouped.
    - .planning/phases/19.1-day-report-render-width-product-neutral-copy-and-config-key-/19.1-UI-SPEC.md
      section "New Component: `getHeader()` Lifecycle Method (D-02/D-03/D-04)" — the three-state
      contract, verbatim.
  </read_first>
  <action>
    Add a `getHeader: function()` member to the `Module.register("MMM-SPCOutlook", {...})` object
    literal, immediately before `getStyles` (MMM-SPCOutlook.js:222) so the lifecycle methods stay
    adjacent.

    Contract, exactly as 19.1-UI-SPEC.md locks it (D-02/D-03/D-04):
    - Read `this.data && this.data.header`. Guard `this.data` itself for `undefined`/non-object and
      treat that as the absent case — MagicMirror always supplies `this.data`, but the module's own
      established posture is that a read which can throw out of a lifecycle method is guarded.
    - If the header value is strictly `=== undefined`, return the hardcoded literal
      `"NWS Hazard Outlooks"`. This is the ONLY case the fallback fires.
    - In every other case return `this.data.header` verbatim — including `""`. Do NOT special-case
      the empty string, do NOT add any suppression logic, and do NOT use a truthiness check in place
      of the `undefined` comparison: a truthiness check would convert D-04's deliberate `header: ""`
      opt-out into a silently-reinstated header row.

    The fallback string MUST be a hardcoded literal (never interpolated, never payload-derived) —
    it is returned to MagicMirror, which paints it without this module's `escapeHtml`. Use the exact
    casing `NWS Hazard Outlooks` so it matches 19.1-UI-SPEC.md's Copywriting Contract table and the
    umbrella noun D-03 locks for the loading string in Task 2.

    Write a comment block above the method, in this file's established voice, recording: (a) that
    the base `Module` class already returned `this.data.header` verbatim, so this override's only
    net effect is the absent-key fallback; (b) that `undefined` and `""` are both already falsy in
    MagicMirror's own `updateModuleContent()` truthiness check, which is why D-04 needs no
    suppression logic here; (c) the D-02 rule that a configured header is never overridden.

    Do NOT add a `showHeader` config key (explicitly rejected in 19.1-CONTEXT.md `<deferred>`), and
    do NOT touch the `defaults` block in this task.
  </action>
  <acceptance_criteria>
    - Source assertion: `grep -vE '^\s*//' MMM-SPCOutlook.js | grep -c 'getHeader: function'`
      returns exactly `1`.
    - Source assertion: `grep -vE '^\s*//' MMM-SPCOutlook.js | grep -c 'NWS Hazard Outlooks'`
      returns exactly `1` (one hardcoded literal, not restated).
    - Behavior assertion (absent key): `getHeader()` called with `this.data = {}` returns exactly
      the string `NWS Hazard Outlooks`.
    - Behavior assertion (explicit opt-out): `getHeader()` called with `this.data = {header: ""}`
      returns exactly `""` — NOT `NWS Hazard Outlooks`.
    - Behavior assertion (operator-configured): `getHeader()` called with
      `this.data = {header: "SPC FORECAST"}` returns exactly `SPC FORECAST`.
    - Behavior assertion (defensive): `getHeader()` called with `this.data = undefined` returns
      `NWS Hazard Outlooks` and does not throw.
    - Source assertion: no new key appears in `defaults` — `node -e` reports
      `Object.keys(loadFrontendModule().defaults).length === 14`.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const s=require('./scripts/probe-lib/module-stubs.js');const f=s.loadFrontendModule();const call=(data)=>f.getHeader.call({data});const cases=[[{},'NWS Hazard Outlooks'],[{header:''},''],[{header:'SPC FORECAST'},'SPC FORECAST'],[undefined,'NWS Hazard Outlooks']];for(const [data,want] of cases){const got=call(data);if(got!==want)throw new Error('getHeader('+JSON.stringify(data)+') returned '+JSON.stringify(got)+', want '+JSON.stringify(want));}if(Object.keys(f.defaults).length!==14)throw new Error('defaults gained or lost a key: '+Object.keys(f.defaults).length);console.log('OK all four getHeader states + defaults key count');"</automated>
  </verify>
  <done>`getHeader()` exists, returns `NWS Hazard Outlooks` only when `this.data.header` is strictly
  `undefined`, returns the configured value verbatim (including `""`) otherwise, and the
  `defaults` key count is unchanged at 14.</done>
</task>

<task type="auto">
  <name>Task 2: Reword the loading string and reconcile the README</name>
  <files>MMM-SPCOutlook.js, README.md</files>
  <read_first>
    - MMM-SPCOutlook.js lines 1046-1075 (the wrapper creation, the `textAlign` anchor, the
      `!this.spcrisk` loading branch and the error branch) — confirm the loading assignment is the
      single `innerHTML` site for this string.
    - MMM-SPCOutlook.js lines 1000-1020 — `NO_HAZARD_TEXT`, `(unconfirmed)`, `(filtered by
      settings)`. These are RPT-06 parity targets and are ALREADY product-neutral. Read them so you
      can confirm by inspection that this task touches none of them (D-06).
    - README.md lines 1-20 — the opening description and the `Multiple instances` section that
      quotes the loading string.
    - 19.1-CONTEXT.md D-03, D-05, D-06; 19.1-RESEARCH.md Open Question 3 (README wording
      recommendation: reuse the umbrella, do not invent a third phrasing).
  </read_first>
  <action>
    (a) `MMM-SPCOutlook.js:1069` — change `wrapper.innerHTML = "Loading SPC Outlook...";` to
    `wrapper.innerHTML = "Loading NWS outlooks...";`. Exact replacement string:
    `Loading NWS outlooks...` (three ASCII full stops, not an ellipsis character — the existing
    string uses three full stops and 19-PARITY-CHECKLIST row 1 records it that way). Same umbrella
    noun as Task 1's `NWS Hazard Outlooks` per D-03. It stays a hardcoded literal assigned via
    `innerHTML`; do not route it through `escapeHtml` and do not convert it to `textContent` — both
    would be unrelated changes to a shipped, parity-checklisted branch.

    (b) `README.md:2` — replace `SPC Point-in-Polygon check for location in Outlooks` with a line
    that opens with the same umbrella noun and names what the module actually aggregates. Use:
    `NWS Hazard Outlooks for your location — a point-in-polygon check against SPC convective and
    fire-weather outlooks, SPC Mesoscale Discussions, WPC Excessive Rainfall, WPC WSSI, WPC
    Mesoscale Precipitation Discussions, the WPC/CPC US Hazards Outlook, and NWS HeatRisk.`
    Do not invent a separate umbrella phrasing (Open Question 3).

    (c) `README.md:12` — the `Multiple instances` section quotes the old loading string verbatim
    (`...and stays on "Loading SPC Outlook...".`). Update the quote to `"Loading NWS outlooks..."`
    so the documentation does not drift from the code it quotes.

    Do NOT change: `NO_HAZARD_TEXT` (`:1007`), `(unconfirmed)` (`:1008`), `(filtered by settings)`
    (`:1017`), `Extended Hazards:` (`:811`), the advisory band's `in effect.` line (`:1271`), or
    the `Error: {error}` passthrough. D-06 is explicit: they are already product-neutral and are
    RPT-06 parity targets — changing them is a regression, not a fix. Do NOT touch any
    `Log.*("SPC Outlook: ...")` prefix, the `GET_SPC_DATA`/`SPC_DATA_RESULT` notification names, or
    the `spcrisk` field (D-05 deferred items).
  </action>
  <acceptance_criteria>
    - Behavior assertion: `renderDom(frontend, {config, spcrisk: null})` returns exactly
      `Loading NWS outlooks...`.
    - Source assertion: `grep -rc 'Loading SPC Outlook' MMM-SPCOutlook.js README.md` returns `0`
      for both files.
    - Source assertion: `grep -c 'Loading NWS outlooks' README.md` returns exactly `1`.
    - Source assertion: the three D-06 strings survive byte-identical —
      `grep -c 'No Hazards Forecast' MMM-SPCOutlook.js` is unchanged from its pre-task value, and
      `grep -c '(unconfirmed)' MMM-SPCOutlook.js` and
      `grep -c '(filtered by settings)' MMM-SPCOutlook.js` are both unchanged.
    - Source assertion: `git diff --stat` lists exactly two files (`MMM-SPCOutlook.js`,
      `README.md`) for this task, and `git diff MMM-SPCOutlook.js` shows exactly one changed line.
    - `README.md:2` names SPC, WPC, CPC and HeatRisk products and opens with `NWS Hazard Outlooks`.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const s=require('./scripts/probe-lib/module-stubs.js');const f=s.loadFrontendModule();const out=s.renderDom(f,{config:{lat:1,lon:2},spcrisk:null});if(out!=='Loading NWS outlooks...')throw new Error('loading string is '+JSON.stringify(out));console.log('OK loading string');" && test "$(grep -c 'Loading SPC Outlook' MMM-SPCOutlook.js README.md | grep -v ':0' | wc -l)" = "0" && grep -q 'Loading NWS outlooks' README.md && grep -q 'No Hazards Forecast' MMM-SPCOutlook.js && echo "OK README + D-06 strings intact"</automated>
  </verify>
  <done>The loading branch renders `Loading NWS outlooks...`, no `Loading SPC Outlook` string
  remains in either file, README:2 carries the umbrella plus the product list, and every D-06
  empty-state string is byte-identical.</done>
</task>

<task type="auto">
  <name>Task 3: Pin all four copy states with mutation-proven probe scenarios</name>
  <files>scripts/probe-payload-resilience.js</files>
  <read_first>
    - scripts/probe-payload-resilience.js lines 12004-12050 (`rpt03-detail-mode-renders-source-
      labeled-sub-rows-under-the-compact-header`) — the scenario object shape, the `config` literal
      convention, and the precondition-guard idiom this suite uses.
    - scripts/probe-payload-resilience.js lines 13820-13890 (`wr04-...`) — the vacuity-guard and
      control-assertion idioms, and the `Mutation to prove RED:` comment convention every scenario
      in this suite carries.
    - scripts/probe-payload-resilience.js lines 14835-14900 (`main()`) — the runner resets
      `resetHelper`/`resetLogs` for you; scenarios take `(_helper)` and throw on failure.
    - scripts/probe-lib/module-stubs.js lines 268-330 (`loadFrontendModule`) and 376-387
      (`renderDom`) — what the harness can and cannot observe.
    - .planning/STATE.md "Decisions" → the Phase 15 D-10 mutation-proof rule (blocking project
      convention) and its two named failure modes.
  </read_first>
  <action>
    Add two scenarios to the `scenarios` array, placed immediately after
    `rpt05-no-products-enabled-branch-is-retired-and-its-count-has-a-floor-of-two` so the
    copy/empty-state scenarios sit together.

    Scenario A — `name: "uat11-getheader-falls-back-only-when-the-header-key-is-absent"`.
    Call `loadFrontendModule()`, then invoke `frontend.getHeader.call({ data })` directly (no
    render is involved — it is a plain string return). Assert all four states from Task 1:
    `{}` -> `NWS Hazard Outlooks`; `{header:""}` -> `""`; `{header:"SPC FORECAST"}` ->
    `SPC FORECAST`; `undefined` -> `NWS Hazard Outlooks` without throwing.
    Add a **control assertion** proving the fallback is not simply always returned: the
    `"SPC FORECAST"` case must NOT equal the fallback string, asserted as its own named check with
    its own message (this is the assertion that goes RED if someone converts the `=== undefined`
    test into a truthiness test, which is the single most likely regression).
    Add the comment `// Mutation to prove RED: change `=== undefined` to a truthiness check — the
    `{header:""}` case must then fail with "returned 'NWS Hazard Outlooks', want ''".`

    Scenario B — `name: "uat11-loading-string-names-the-nws-umbrella-not-spc-alone"`.
    `renderDom(frontend, { config, spcrisk: null })` must equal exactly
    `Loading NWS outlooks...`. Add a **precondition guard** that throws if the fixture did not
    actually reach the loading branch (assert the returned markup contains no `<br/>` and no
    `Day ` token, i.e. the day loop never ran) — without it, a future refactor that made
    `!this.spcrisk` unreachable would leave this scenario asserting against whatever else rendered.
    Add a negative assertion that the render does not contain the substring `SPC Outlook`.
    Add a **control**: a minimal populated payload (reuse `unifiedPayload({})` with
    `summary.anyHazard = false`) must NOT produce the loading string, proving the branch is
    selected by `!this.spcrisk` rather than always taken.
    Add the comment `// Mutation to prove RED: restore "Loading SPC Outlook..." at
    MMM-SPCOutlook.js's !this.spcrisk branch.`

    Then **mutation-prove each scenario individually** (Phase 15 D-10, blocking):
    1. For Scenario A: edit `getHeader` to use a truthiness check instead of `=== undefined`, run
       the suite, confirm Scenario A fails with a message that names the state and both strings,
       restore.
    2. For Scenario B: restore the literal `Loading SPC Outlook...`, run the suite, confirm
       Scenario B fails with a message quoting the wrong string, restore.
    Record both mutation runs (the mutated line, the failing scenario name, the diagnostic message,
    and the restored-green count) in the plan SUMMARY. A scenario that stays green under its own
    mutation is a fixture defect, not a pass — fix the fixture, do not accept the green.
  </action>
  <acceptance_criteria>
    - `node scripts/probe-payload-resilience.js` prints `PROBE RESULT: 184 passed, 0 failed, 0
      skipped` (baseline at plan start is 182 — verified by running the suite before any edit; if
      the observed baseline differs, the expected total is baseline + 2 and the SUMMARY must record
      both numbers).
    - Both new scenario names appear on `PASS` lines.
    - Mutation proof 1: with `getHeader`'s `=== undefined` replaced by a truthiness check, the suite
      reports `FAIL uat11-getheader-falls-back-only-when-the-header-key-is-absent` and the message
      names the `{header:""}` state and both the returned and wanted strings. After restore, the
      suite is green again at the same count.
    - Mutation proof 2: with the loading literal reverted to `Loading SPC Outlook...`, the suite
      reports `FAIL uat11-loading-string-names-the-nws-umbrella-not-spc-alone` and the message
      quotes the actual rendered string. After restore, green at the same count.
    - Each new scenario carries a `// Mutation to prove RED:` comment naming the exact line to break.
    - Scenario B carries both a precondition guard and a control assertion; Scenario A carries a
      control assertion. Each failure message names what was expected and what was observed.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/probe-payload-resilience.js 2>&1 | tee /tmp/probe-19.1-01.log | tail -1 | grep -q '0 failed, 0 skipped' && grep -q '^PASS uat11-getheader-falls-back-only-when-the-header-key-is-absent' /tmp/probe-19.1-01.log && grep -q '^PASS uat11-loading-string-names-the-nws-umbrella-not-spc-alone' /tmp/probe-19.1-01.log && echo "OK both scenarios present and suite green"</automated>
  </verify>
  <done>Two new scenarios pass, the suite is green with zero failures and zero skips, and both
  scenarios have been individually proven RED under a mutation of the exact line they cover with a
  diagnosable message, with the evidence recorded in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator's `config.js` → `this.data.header` → MagicMirror's header region | Operator-authored string painted by MagicMirror core, not by this module's `innerHTML`; this module's `escapeHtml` never sees it |
| module source → `wrapper.innerHTML` | The loading string is module-authored and hardcoded; nothing remote reaches this assignment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19.1-01 | Tampering | `getHeader()` fallback return value | mitigate | The fallback is a hardcoded string literal `"NWS Hazard Outlooks"` — no interpolation, no concatenation, no read of `this.config`/`this.spcrisk`. Pinned by the Task 1 acceptance criterion that `grep -c 'NWS Hazard Outlooks'` is exactly 1 and by Scenario A's exact-equality assertions. Matches 19.1-RESEARCH.md's Security Domain "getHeader() fallback default string" row. |
| T-19.1-02 | Tampering | `this.data.header` returned verbatim to MagicMirror | accept | The operator's own config value, returned unmodified — identical to the base `Module.getHeader()` behavior that ships today (`~/MagicMirror/js/module.js:119-120`). This override adds no new path for it and no new escaping obligation; MagicMirror core owns how it is painted. No change to the pre-existing risk surface. |
| T-19.1-03 | Tampering | loading string → `innerHTML` (`:1069`) | mitigate | Remains a hardcoded literal, assigned unconditionally with no payload participation. 19-SECURITY.md row 1 (`T-19-01` class) and 19-PARITY-CHECKLIST row 1 already record this branch as "hardcoded string, safe"; the reword preserves that property. Pinned by Scenario B's exact-equality assertion and by `renderDom`'s `assertInertMarkup` over the whole output. |
| T-19.1-04 | Info disclosure | README quoting a runtime string | accept | Documentation text only; no runtime surface, no operator data. |
| T-19.1-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed. 19.1-RESEARCH.md's Package Legitimacy Audit records "Not applicable — this phase installs no new npm packages, dev or runtime. `package.json`/`pnpm-lock.yaml` are not touched." Gate: `git diff --name-only` for this plan must not list `package.json` or `pnpm-lock.yaml`. |
</threat_model>

<verification>
- `node scripts/probe-payload-resilience.js` → `0 failed, 0 skipped`, total = pre-plan baseline + 2.
- `grep -r 'Loading SPC Outlook' MMM-SPCOutlook.js README.md` → no matches.
- `git diff --name-only` for the whole plan lists exactly: `MMM-SPCOutlook.js`, `README.md`,
  `scripts/probe-payload-resilience.js`. No `package.json`, no `pnpm-lock.yaml`, no
  `node_helper.js` (D-01: this phase does not touch the backend).
- Both mutation proofs recorded in the SUMMARY with the mutated line, the failing scenario name and
  the diagnostic message.
</verification>

<success_criteria>
- `getHeader()` returns `NWS Hazard Outlooks` only for a strictly-`undefined` header, and returns a
  configured header — including `""` — verbatim in every other case.
- The pre-payload render is exactly `Loading NWS outlooks...`.
- README opening line and its loading-string quote both use the same NWS umbrella.
- Every D-06 empty-state string is byte-identical to its pre-plan value.
- Suite green, two new scenarios, both individually mutation-proven.
</success_criteria>

<output>
Create `.planning/phases/19.1-day-report-render-width-product-neutral-copy-and-config-key-/19.1-01-SUMMARY.md` when done.
</output>
