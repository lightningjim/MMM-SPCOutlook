# Phase 19.1 Plan 09 — Second Live Check Record

**Purpose:** the second live observation record for the day-report render-width gap closure. Plans
19.1-06/07/08 replaced the disproven `32.4rem` content-needs-only cap and its `ch`/monospace column
mechanism with `REGION_CAP_REM = 22.5` (CSS grid tracks, available-space-first derivation). Neither
`jsdom` nor `linkedom` implements CSS layout, so only a real render on the deployed Raspberry Pi can
confirm or refute whether the region actually stops short of the centreline. This document reuses
the `19.1-LIVE-CHECK.md` protocol exactly, per this plan's own instruction not to reinvent it.

**Precedent this record exists to avoid repeating:** the 19-08 width observation was discarded
because it was taken against a 21-commits-stale deploy AND a misspelled `dayReportDetails` config
key. Task 1 below positively confirms deploy validity rather than assuming it.

---

## Deploy

- **Local commit deployed:** `2b54dc39734070adffda19f9551f770b2175923b` (`docs(phase-19.1): update
  tracking after wave 2`) — the tip of this execution's worktree branch at deploy time, which itself
  contains plans 19.1-06 (grid-track mechanism, `REGION_CAP_REM = 22.5`) and 19.1-07 (probe suite
  re-expression, 191/0/0) as merged commits.
- **Local working tree state at deploy time:** `git status --porcelain -- MMM-SPCOutlook.js scripts
  README.md` returned empty (clean) immediately before deploy.
- **Local probe suite immediately before deploy:** `node scripts/probe-payload-resilience.js` →
  `PROBE RESULT: 191 passed, 0 failed, 0 skipped`. A red or skipped suite would have blocked the
  deploy; it was green.
- **Deployed module install path:** `~/MagicMirror/modules/MMM-SPCOutlook` on the Pi (`ssh mm`), a
  git clone whose own HEAD remains stale relative to `main` (per `19.1-LIVE-CHECK.md`'s established
  mechanism — file content is synced via `rsync`, the clone's HEAD pointer is deliberately left
  unmoved; no `git push`/`pull`/`checkout` was performed).
- **Deploy mechanism used: `rsync`, not `git pull`/`git checkout <sha>`** — identical to the first
  live check, and for the same reason (the local worktree's commits are not on `origin`):
  - `rsync -av MMM-SPCOutlook.js README.md mm:~/MagicMirror/modules/MMM-SPCOutlook/`
  - `rsync -av --delete scripts/ mm:~/MagicMirror/modules/MMM-SPCOutlook/scripts/`
- **New backups taken before overwrite, with the `-19.1gap` suffix** (the existing `.bak-19.1` /
  `.bak-19uat` files were NOT touched or overwritten):
  - `~/MagicMirror/modules/MMM-SPCOutlook/MMM-SPCOutlook.js.bak-19.1gap`
  - `~/MagicMirror/modules/MMM-SPCOutlook/scripts.bak-19.1gap/` (directory copy via `cp -a`, includes
    `probe-payload-resilience.js`, `probe-lib/module-stubs.js`, `check-concurrency-invariant.sh`,
    `hazards-at.js`)
- **Pre-19.1 backup confirmed still present after this deploy:**
  `ssh mm 'ls ~/MagicMirror/modules/MMM-SPCOutlook/MMM-SPCOutlook.js.bak-19.1'` → succeeded
  (`PRE-19.1 BACKUP INTACT`).
- **Post-deploy byte-identity check:** `sha256sum` of the local and deployed `MMM-SPCOutlook.js`:
  - local:  `e951fb51edabacc08b82da23b99f78d97db15b2925d4b40a68b1b76f2c266e28`
  - remote: `e951fb51edabacc08b82da23b99f78d97db15b2925d4b40a68b1b76f2c266e28`
  - **MATCH** — the deployed file is byte-identical to the local `2b54dc3` worktree copy.
- **Five code-presence confirmations** (`ssh mm`, against the deployed
  `~/MagicMirror/modules/MMM-SPCOutlook/MMM-SPCOutlook.js`):

  | Marker | grep -c count |
  |---|---|
  | `REGION_CAP_REM` | 2 |
  | `display:inline-grid` | 1 |
  | `minmax(0,` | 3 |
  | `Loading NWS outlooks` | 1 |
  | `getHeader: function` | 1 |

  All five non-zero.
- **Two absence confirmations** (proving the retired mechanism is gone from the deployed file, not
  merely from the local one):

  | Marker | grep -c count |
  |---|---|
  | `32.4rem` | 0 |
  | `DejaVu Sans Mono` | 0 |

  Both exactly `0`.
- **`~/MagicMirror/config/config.js` was read-only accessed only** — no write, no edit, confirmed by
  no `git status`/`diff` applicability (it is not part of the module's own git checkout) and by the
  fact this session issued only `grep`/`sed -n` read commands against it.

---

## Validity Preconditions

| Condition | Result | Evidence |
|---|---|---|
| **1 — the deployed code is THIS code** (not stale, per the 19-08 precedent) | **PASS** | All five code-presence markers above returned non-zero counts, both absence markers returned exactly `0`, AND the deployed file's SHA256 (`e951fb51edabacc08b82da23b99f78d97db15b2925d4b40a68b1b76f2c266e28`) is byte-identical to the local `2b54dc3` worktree commit's `MMM-SPCOutlook.js`. The deployed git clone's own HEAD is stale relative to this worktree's history (no push/pull performed), but the file **content** deployed via `rsync` is exactly this worktree's content. |
| **2 — global detail mode is genuinely in effect** (the 19-08 precedent's second invalidating count, the misspelled-key case) | **FAIL — named blocker, see below** | The key is spelled **correctly** (singular `dayReportDetail`, not the misspelled `dayReportDetails` plural that broke the 19-08 observation) — quoted verbatim from the deployed `~/MagicMirror/config/config.js`, read-only, below. **However, the live value is `dayReportDetail: false`**, not `true`. The first live check's operator-authorised flip to `true` (2026-09-10, recorded in `19.1-LIVE-CHECK.md`'s "Condition 2 Resolution") is no longer the deployed state — the value has evidently been toggled back off since that session. Per this plan's own Task 1 action step 6: "If it reads `false` ... STOP and report a named blocker — this plan may not flip it without a fresh operator authorisation, exactly as the first live check handled it." This executor did **not** flip the value; a fresh, explicit operator authorisation is required before Task 2's full, every-day-detail-row observation can be presented as valid. |

Quoted verbatim, the deployed `~/MagicMirror/config/config.js`'s `MMM-SPCOutlook` entry (read-only,
line numbers from `sed -n`):

```javascript
{
    module: "MMM-SPCOutlook",
    position: "top_right",
    header: "SPC Forecast",
    config: {
        lat: 35.4432156,
        lon: -97.595822,
        extended: true,
        proximityWeighting: true,
        updateInterval: 60,
        showExcessiveRain: true,
        showMPD: true,
        showHazardsOutlook: true,
        showDrought: false,
        showHeatRisk: true,
        showMinorHeat: false,
        dayReportDetail: false,

        // Testing
        //lat: 42.85,
        //lon: -106.31
    }
},
```

**This is NOT the 19-08 misspelling failure mode** — the key is spelled correctly, so the 19.1-02
config-key validator has nothing to warn about here (confirmed by its absence in Log Evidence
below), which is itself live evidence the validator does not over-fire on a well-formed key. The
failure mode here is purely that the value is `false` rather than `true`.

**Named blocker:** `dayReportDetail` is currently `false` on the deployed mirror. Per this plan's
own `<deploy_constraints>`-equivalent instruction, this executor may not flip that value without a
fresh, explicit operator authorisation recorded the same way the first live check recorded its own
(see `19.1-LIVE-CHECK.md`'s "Condition 2 Resolution"). **Without `dayReportDetail: true`, only days
whose own hazard significance triggers `autoExpand` will render detail sub-rows** — the remaining
days render compact-only, which would make Task 2's full-width observation partial and weaker than
the plan requires. Per this plan's own acceptance criteria ("both conditions as PASS ... or the plan
stops here with a FAIL and a named blocker"), Task 1 stops here with condition 2 recorded FAIL.

Restart and log capture (harmless, non-destructive, and still evidentiary for the config-key-
validator claim regardless of this value) were performed anyway, following the exact precedent
`19.1-LIVE-CHECK.md` set for this situation — see Log Evidence below.

---

## Log Evidence

- **Restart performed:** `ssh mm 'export PATH="$HOME/.nvm/versions/node/v24.13.1/bin:$PATH"; pm2
  restart mm'` (mandatory nvm PATH export — without it, `pm2 restart mm` fails with `env: 'node': No
  such file or directory` and does not restart the process). Restart succeeded:
  `[PM2] [mm](0) ✓`, process `online`, pid `228329`.
- **Pre-restart `mm-out.log` line count (boundary):** `545623`. All lines below are from the restart
  onward (line 545624+). Post-capture total: `545703`.
- **Module-start line:**
  ```
  [2026-09-10 19:47:26.338] [LOG]   [app] Starting MagicMirror: v2.37.0
  [2026-09-10 19:47:29.214] [LOG]   [node_helper] Initializing new module helper ...
  [2026-09-10 19:47:29.214] [LOG]   [node_helper] Module helper loaded: MMM-SPCOutlook
  [2026-09-10 19:47:29.369] [INFO]  [MMM-SPCOutlook] Starting node_helper for MMM-SPCOutlook...
  ```
- **First-accepted-payload timing line:**
  ```
  [2026-09-10 19:47:38.326] [INFO]  [MMM-SPCOutlook] MMM-SPCOutlook: new-product batch settled in 1040ms {"spc-inline":4777,"winterImpact":8,"spcMD":79,"heatRisk":418,"excessiveRain":647,"hazardsOutlook":984,"mpd":1032}
  [2026-09-10 19:47:38.343] [INFO]  [MMM-SPCOutlook] MMM-SPCOutlook: cold-start timing -- backend interval 5841ms (GET_SPC_DATA received -> SPC_DATA_RESULT emitted); 8973ms since process start (module-load to first result sent)
  ```
- **`unrecognized`/`unrecognised config key` line (plan 19.1-02's warning):** **ABSENT.**
  `ssh mm 'tail -n +545624 ~/.pm2/logs/mm-out.log | grep -ic "unrecognized\|unrecognised"'` returned
  `0` (grep exit code 1, no matches). Expected given condition 2's finding: the deployed config's
  `MMM-SPCOutlook` entry carries no misspelled key, so the validator correctly has nothing to warn
  about. This absence is itself live evidence the 19.1-02 validator is not over-firing.
- Cold start completed cleanly with no errors; the module produced a real payload (`new-product
  batch settled in 1040ms`) rather than staying stuck loading. No syntax errors reported by MM's own
  config check (`Your configuration file doesn't contain syntax errors :)`).

---

## Observations

*Not yet filled in — this section is Task 2's, per this plan's own recording discipline (no verdict
may be recorded until the operator has stated it). Filling it in now would also be premature for a
second reason: Precondition 2 above is FAIL. `dayReportDetail` is `false` on the deployed mirror, so
only autoExpand-triggered days would render detail sub-rows — Task 2's full-width, every-day
observation cannot be presented as ready on this basis. A fresh, explicit operator authorisation to
flip `dayReportDetail: false → true` is required first, exactly as the first live check required and
received one (see `19.1-LIVE-CHECK.md`'s "Condition 2 Resolution"). This blocker is returned to the
orchestrator rather than resolved by this executor.*

---

## Disposition

*Not yet recorded — awaiting resolution of the Precondition 2 blocker (a fresh operator
authorisation to flip `dayReportDetail` to `true`), and then the operator's live observation and cap
decision at Task 2's checkpoint.*
