# Phase 19.1 Plan 09 — Second Live Check Record

**Purpose:** the second live observation record for the day-report render-width gap closure. Plans
19.1-06/07/08 replaced the disproven `32.4rem` content-needs-only cap and its `ch`/monospace column
mechanism with `REGION_CAP_REM = 22.5` (CSS grid tracks, available-space-first derivation). Neither
`jsdom` nor `linkedom` implements CSS layout, so only a real render on the deployed Raspberry Pi can
confirm or refute whether the region actually stops short of the centreline. This document reuses
the `19.1-LIVE-CHECK.md` protocol exactly, per this plan's own instruction not to reinvent it.

**Precedent this record exists to avoid repeating:** the 19-08 width observation was discarded
because it was taken against a 21-commits-stale deploy AND a misspelled `dayReportDetails` config
key. Task 1 below positively confirms deploy validity rather than assuming it.

---

## Deploy

- **Local commit deployed:** `2b54dc39734070adffda19f9551f770b2175923b` (`docs(phase-19.1): update
  tracking after wave 2`) — the tip of this execution's worktree branch at deploy time, which itself
  contains plans 19.1-06 (grid-track mechanism, `REGION_CAP_REM = 22.5`) and 19.1-07 (probe suite
  re-expression, 191/0/0) as merged commits.
- **Local working tree state at deploy time:** `git status --porcelain -- MMM-SPCOutlook.js scripts
  README.md` returned empty (clean) immediately before deploy.
- **Local probe suite immediately before deploy:** `node scripts/probe-payload-resilience.js` →
  `PROBE RESULT: 191 passed, 0 failed, 0 skipped`. A red or skipped suite would have blocked the
  deploy; it was green.
- **Deployed module install path:** `~/MagicMirror/modules/MMM-SPCOutlook` on the Pi (`ssh mm`), a
  git clone whose own HEAD remains stale relative to `main` (per `19.1-LIVE-CHECK.md`'s established
  mechanism — file content is synced via `rsync`, the clone's HEAD pointer is deliberately left
  unmoved; no `git push`/`pull`/`checkout` was performed).
- **Deploy mechanism used: `rsync`, not `git pull`/`git checkout <sha>`** — identical to the first
  live check, and for the same reason (the local worktree's commits are not on `origin`):
  - `rsync -av MMM-SPCOutlook.js README.md mm:~/MagicMirror/modules/MMM-SPCOutlook/`
  - `rsync -av --delete scripts/ mm:~/MagicMirror/modules/MMM-SPCOutlook/scripts/`
- **New backups taken before overwrite, with the `-19.1gap` suffix** (the existing `.bak-19.1` /
  `.bak-19uat` files were NOT touched or overwritten):
  - `~/MagicMirror/modules/MMM-SPCOutlook/MMM-SPCOutlook.js.bak-19.1gap`
  - `~/MagicMirror/modules/MMM-SPCOutlook/scripts.bak-19.1gap/` (directory copy via `cp -a`, includes
    `probe-payload-resilience.js`, `probe-lib/module-stubs.js`, `check-concurrency-invariant.sh`,
    `hazards-at.js`)
- **Pre-19.1 backup confirmed still present after this deploy:**
  `ssh mm 'ls ~/MagicMirror/modules/MMM-SPCOutlook/MMM-SPCOutlook.js.bak-19.1'` → succeeded
  (`PRE-19.1 BACKUP INTACT`).
- **Post-deploy byte-identity check:** `sha256sum` of the local and deployed `MMM-SPCOutlook.js`:
  - local:  `e951fb51edabacc08b82da23b99f78d97db15b2925d4b40a68b1b76f2c266e28`
  - remote: `e951fb51edabacc08b82da23b99f78d97db15b2925d4b40a68b1b76f2c266e28`
  - **MATCH** — the deployed file is byte-identical to the local `2b54dc3` worktree copy.
- **Five code-presence confirmations** (`ssh mm`, against the deployed
  `~/MagicMirror/modules/MMM-SPCOutlook/MMM-SPCOutlook.js`):

  | Marker | grep -c count |
  |---|---|
  | `REGION_CAP_REM` | 2 |
  | `display:inline-grid` | 1 |
  | `minmax(0,` | 3 |
  | `Loading NWS outlooks` | 1 |
  | `getHeader: function` | 1 |

  All five non-zero.
- **Two absence confirmations** (proving the retired mechanism is gone from the deployed file, not
  merely from the local one):

  | Marker | grep -c count |
  |---|---|
  | `32.4rem` | 0 |
  | `DejaVu Sans Mono` | 0 |

  Both exactly `0`.
- **`~/MagicMirror/config/config.js` was read-only accessed only** — no write, no edit, confirmed by
  no `git status`/`diff` applicability (it is not part of the module's own git checkout) and by the
  fact this session issued only `grep`/`sed -n` read commands against it.


> **Session 2 addendum (2026-09-11).** The first pass of this task (2026-09-10, commit `f73cf73`)
> stopped at Precondition 2 with a named blocker. This session resumed the same plan with a fresh
> operator authorisation. The `## Deploy` evidence above is UNCHANGED and was re-confirmed, not
> re-taken: the local `MMM-SPCOutlook.js` SHA256 is still
> `e951fb51edabacc08b82da23b99f78d97db15b2925d4b40a68b1b76f2c266e28` at local HEAD `c2d17f1`, which
> is two **docs-only** commits ahead of the `2b54dc3` recorded above. The matching digest is what
> proves those two commits touched no source. No re-deploy was necessary or performed.

---

## Validity Preconditions

| Condition | Result | Evidence |
|---|---|---|
| **1 — the deployed code is THIS code** (not stale, per the 19-08 precedent) | **PASS** | Re-confirmed 2026-09-11. All five code-presence markers returned non-zero against the deployed file (`REGION_CAP_REM` → 2, `display:inline-grid` → 1, `minmax(0,` → 3, `Loading NWS outlooks` → 1, `getHeader: function` → 1); both absence markers returned exactly `0` (`32.4rem` → 0, `DejaVu Sans Mono` → 0), proving the retired `ch`/monospace mechanism is gone from the **deployed** file and not merely the local one. Deployed SHA256 `e951fb51edabacc08b82da23b99f78d97db15b2925d4b40a68b1b76f2c266e28` is byte-identical to local. `MMM-SPCOutlook.js.bak-19.1` (the pre-19.1 restore point) confirmed still present and unoverwritten. |
| **2 — global detail mode is genuinely in effect** (the 19-08 precedent's second invalidating count, the misspelled-key case) | **PASS** — after the authorised flip recorded in "Condition 2 Resolution" below | The key is spelled **correctly** (singular `dayReportDetail`, not the misspelled `dayReportDetails` plural that invalidated the 19-08 observation). It was found at `false` and flipped to `true` under fresh operator authorisation; the value was then **positively re-read from the deployed file and parsed by node**, not assumed. Post-flip verification: `node -e "require(config.js)"` → `parsed OK; dayReportDetail = true | header = "SPC Forecast" | lat/lon = 35.4432156 -97.595822`. MagicMirror's own startup config check then independently reported `Your configuration file doesn't contain syntax errors :)`. |

### Condition 2 Resolution

**Blocker as found (2026-09-11, unchanged from the 2026-09-10 pass):** the deployed
`~/MagicMirror/config/config.js` carried `dayReportDetail: false` at line 123. Quoted verbatim from a
read-only read, before any edit:

```javascript
{
    module: "MMM-SPCOutlook",
    position: "top_right",
    header: "SPC Forecast",
    config: {
        lat: 35.4432156,
        lon: -97.595822,
        extended: true,
        proximityWeighting: true,
        updateInterval: 60,
        showExcessiveRain: true,
        showMPD: true,
        showHazardsOutlook: true,
        showDrought: false,
        showHeatRisk: true,
        showMinorHeat: false,
        dayReportDetail: false,

        // Testing
        //lat: 42.85,
        //lon: -106.31
    }
},
```

**This was NOT the 19-08 misspelling failure mode.** The key is spelled correctly, so the 19.1-02
config-key validator has nothing to warn about — confirmed by the warning's absence in Log Evidence
below, which is itself live evidence the validator does not over-fire on a well-formed key. The
failure mode was purely that the value read `false`.

**Fresh operator authorisation (2026-09-11).** The operator was presented with the blocker and the
three available dispositions (flip-and-check, operator-flips-manually, or defer the gap to backlog)
and selected **"Flip it and run the check"** — authorising this session to set
`dayReportDetail: true` on the deployed mirror, take the observation, and **restore the value to
`false`** when the check is complete. This is the same authorisation shape the first live check
obtained and recorded in `19.1-LIVE-CHECK.md`'s own "Condition 2 Resolution"; it is a per-session
authorisation and does not carry forward.

**Change applied, and its blast radius bounded:**

- New backup taken first, with the `-19.1gap` suffix: `~/MagicMirror/config/config.js.bak-19.1gap`.
  The two pre-existing restore points (`config.js.bak-19.1`, `config.js.bak-19uat`) were **not**
  touched or overwritten — re-confirmed by `ls` after the edit.
- Edit was line-scoped (`sed -i '123s/dayReportDetail: false,/dayReportDetail: true,/'`). `diff`
  against the fresh backup returned **exactly one changed line** and nothing else:

  ```
  123c123
  <                 dayReportDetail: false,
  ---
  >                 dayReportDetail: true,
  ```

- No other config value was altered. `lat`/`lon` remain the **OKC production coordinate**
  (35.4432156, -97.595822) — unlike the Phase 19-08 session, no substitute coordinate was used, so
  no coordinate restoration is owed at the end of this run. `header: "SPC Forecast"` is unchanged and
  still explicit, so the `getHeader` fallback string is deliberately **not** exercised on this mirror
  (item 9 below).

**Restoration owed at the end of this run:** `dayReportDetail` must be returned to `false`. Restore
command of record: `ssh mm "sed -i '123s/dayReportDetail: true,/dayReportDetail: false,/' ~/MagicMirror/config/config.js"`
followed by a `pm2 restart mm` with the nvm PATH export. The `-19.1gap` backup is the fallback.

---

## Log Evidence

**Path correction (deviation from this plan's `<interfaces>` block).** The plan's interfaces block
records the log as `~/MagicMirror/mm-out.log`. That path **does not exist** on the deployed Pi. The
real location, from `pm2 describe mm`, is `~/.pm2/logs/mm-out.log` (with `~/.pm2/logs/mm-error.log`
alongside). The first live check's record inherited the same wrong path in prose while evidently
reading the right one. Corrected here for future runs; the interfaces block in the plan file itself
is left as written, since plans are historical records.

- **Restart performed:** `ssh mm 'export PATH="$HOME/.nvm/versions/node/v24.13.1/bin:$PATH"; pm2 restart mm'`
  — the nvm PATH export is mandatory; without it `pm2 restart mm` fails with
  `env: 'node': No such file or directory` and silently does not restart. Restart succeeded: process
  `mm` `online`, pid `233672`, restart count `76`.
- **Pre-restart `~/.pm2/logs/mm-out.log` line count (boundary):** `547555`. All evidence below is
  from line `547556` onward.
- **Config accepted by MagicMirror's own checker:**
  ```
  [2026-09-11 09:37:09.207] [INFO]  [utils] Checking config file /home/kcreasey/MagicMirror/config/config.js ...
  [2026-09-11 09:37:09.383] [INFO]  [utils] Your configuration file doesn't contain syntax errors :)
  ```
- **Module-start lines:**
  ```
  [2026-09-11 09:37:09.175] [LOG]   [app] Starting MagicMirror: v2.37.0
  [2026-09-11 09:37:11.578] [LOG]   [node_helper] Module helper loaded: MMM-SPCOutlook
  [2026-09-11 09:37:11.713] [INFO]  [MMM-SPCOutlook] Starting node_helper for MMM-SPCOutlook...
  [2026-09-11 09:37:14.204] [INFO]  [MMM-SPCOutlook] MMM-SPCOutlook: location changed — cache results invalidated
  ```
- **First-accepted-payload timing line** (a real payload was produced; the module did not stay stuck
  loading):
  ```
  [2026-09-11 09:37:21.900] [INFO]  [MMM-SPCOutlook] MMM-SPCOutlook: new-product batch settled in 3795ms {"spc-inline":3899,"winterImpact":14,"spcMD":83,"heatRisk":358,"excessiveRain":683,"hazardsOutlook":943,"mpd":3781}
  [2026-09-11 09:37:21.911] [INFO]  [MMM-SPCOutlook] MMM-SPCOutlook: cold-start timing -- backend interval 7709ms (GET_SPC_DATA received -> SPC_DATA_RESULT emitted); 10196ms since process start (module-load to first result sent)
  [2026-09-11 09:37:21.913] [INFO]  [MMM-SPCOutlook] MMM-SPCOutlook: cold-start slowest source spc-inline (3899ms)
  ```
- **`unrecognized config key` line (plan 19.1-02's validator warning):** **ABSENT** — grep over the
  post-restart slice returned no matches. Expected and correct: the deployed config carries no
  misspelled key, so the validator has nothing to warn about. This absence is live evidence the
  19.1-02 validator does not over-fire on a well-formed config, and it is the direct answer to
  item 10 below.
- **Environment confirmed from the startup banner:** Raspberry Pi 4 Model B Rev 1.5, Debian 13,
  arm64, kernel 6.18.34, Electron 41.3.0, node 24.15.0 in use, MagicMirror v2.37.0, TZ
  America/Chicago.
- **One pre-existing INFO line, noted and explicitly NOT treated as a defect of this phase:**
  ```
  [2026-09-11 09:37:15.084] [INFO]  [MMM-SPCOutlook] MMM-SPCOutlook: SPC grid anchor unavailable
  (VALID_ISO/EXPIRE_ISO ... absent/unparseable, or EXPIRE_ISO had already elapsed); falling back to
  the clock-derived estimate.
  ```
  This is Phase 18's documented `_spcGridAnchor` degrade-to-estimated path behaving as designed, not
  a width or render defect. Recorded for completeness; it is out of scope for this plan's limb.

**Both preconditions are now PASS.** Neither condition that invalidated the 19-08 observation can be
true for the observation taken at Task 2: the deployed code is provably this code, and detail mode is
provably active.

---

## Observations

*Task 2's section — to be filled in from the operator's stated verdicts only. Per this plan's
recording discipline: no verdict inferred from another, nothing recorded PASS that the operator did
not state, and no hedged answer upgraded to a clean PASS.*

---

## Disposition

*Task 2's section — awaiting the operator's live observation and cap decision.*
