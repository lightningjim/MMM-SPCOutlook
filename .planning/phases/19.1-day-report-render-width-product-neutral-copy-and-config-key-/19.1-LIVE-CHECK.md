# Phase 19.1 Plan 05 — Live Check Record

**Purpose:** the live observation record for the day-report render-width fix. Only a real render
on the deployed Raspberry Pi can confirm or refute the MAJOR defect (UAT-5); no probe scenario can
(neither `jsdom` nor `linkedom` implements CSS layout). This document exists so that claim is never
inferred, only recorded as observed.

**Precedent this record exists to avoid repeating:** the 19-08 width observation was thrown away
because it was taken against a 21-commits-stale deploy AND a misspelled `dayReportDetails` config
key, so global detail mode was never active. Task 1 below positively confirms both of those
conditions rather than assuming either.

---

## Deploy

- **Local commit deployed:** `d29aff6a1531fa4b4fdfa6aa79a6d83978122b63` (`docs(phase-19.1): update
  tracking after wave 4`) — the tip of `main` at the start of this plan's execution.
- **Local working tree state at deploy time:** `git status --porcelain -- MMM-SPCOutlook.js scripts
  README.md` returned empty (clean) immediately before deploy. (Unrelated untracked/deleted paths
  existed in the tree — `.codegraph/`, `node_modules/`, `pnpm-lock.yaml`, a deleted `.idea/`
  workspace file — none of which are part of this deploy.)
- **Local probe suite immediately before deploy:** `node scripts/probe-payload-resilience.js` →
  `PROBE RESULT: 190 passed, 0 failed, 0 skipped`. A red or skipped suite would have blocked the
  deploy; it was green.
- **Deployed module install path:** `~/MagicMirror/modules/MMM-SPCOutlook` on the Pi (`ssh mm`),
  confirmed to be a **git clone** (`.git` present, `origin` = `ssh://git@coldpool.myqnapcloud.com:2222/lightningjim/MMM-SPCOutlook.git`,
  branch `main`, clean working tree relative to its own HEAD `a89e981` before this deploy).
- **Deploy mechanism used: `rsync`, not `git pull`/`git checkout <sha>`.** The deployed clone's
  `main` was 21 commits behind local `main` (`a89e981` vs `d29aff6`), and per this plan's
  `<deploy_constraints>` no `git push` to the remote was performed this session — the four new
  local commits (19.1-01 through 19.1-04, plus this plan's own tracking commit) exist only in the
  local working tree, not on `origin`. A `git fetch` on the Pi therefore could not have retrieved
  them; `git pull`/`git checkout <sha>` was not a viable mechanism here, so `rsync` of the three
  files the plan names was used instead:
  - `rsync -av MMM-SPCOutlook.js README.md mm:~/MagicMirror/modules/MMM-SPCOutlook/`
  - `rsync -av --delete scripts/ mm:~/MagicMirror/modules/MMM-SPCOutlook/scripts/`
- **Backups taken before overwrite** (reversibility, per `<deploy_constraints>`):
  - `~/MagicMirror/modules/MMM-SPCOutlook/MMM-SPCOutlook.js.bak-19.1`
  - `~/MagicMirror/modules/MMM-SPCOutlook/README.md.bak-19.1`
  - `~/MagicMirror/modules/MMM-SPCOutlook/scripts.bak-19.1/` (directory copy, includes
    `probe-payload-resilience.js`, `probe-lib/module-stubs.js`, `check-concurrency-invariant.sh`,
    `hazards-at.js`)
  - All three backups are the exact pre-deploy content from the Pi's own `a89e981` checkout — they
    were **not** touched again after being made.
- **Post-deploy byte-identity check:** `sha256sum` of the local `MMM-SPCOutlook.js` and the
  deployed `MMM-SPCOutlook.js` after rsync:
  - local:  `ae56d8d7a26ea16889093f7db22f34f25002dae4cdc8c2695def9f205370d014`
  - remote: `ae56d8d7a26ea16889093f7db22f34f25002dae4cdc8c2695def9f205370d014`
  - **MATCH** — the deployed file is byte-identical to the local `d29aff6` copy.
- **Four code-presence confirmations** (`ssh mm`, against the deployed
  `~/MagicMirror/modules/MMM-SPCOutlook/MMM-SPCOutlook.js`):

  | Marker | grep -c count |
  |---|---|
  | `wrapper.style.maxWidth` | 1 |
  | `display:inline-block;min-width:` | 1 |
  | `Loading NWS outlooks` | 1 |
  | `getHeader: function` | 1 |

  All four non-zero — all four of this phase's code changes are present in the deployed file.
- **`~/MagicMirror/config/config.js` was not modified at any point** — read-only access only,
  confirmed by `git status`/`diff` not being applicable (it is not part of the module's own git
  checkout) and by direct visual comparison of the quoted block below against the config's own
  timestamp (unchanged file mtime before/after this session's `ssh` reads).

---

## Validity Preconditions

| Condition | Result | Evidence |
|---|---|---|
| **1 — the deployed code is THIS code** (not stale, per the 19-08 precedent's first invalidating count) | **PASS** | All four code-presence markers above returned non-zero counts, AND the deployed file's SHA256 is byte-identical to the local `d29aff6` commit's `MMM-SPCOutlook.js`. The deployed git clone's own HEAD (`a89e981`) is stale relative to `main`, but the file **content** deployed via `rsync` is exactly `d29aff6`'s content — the git clone's HEAD pointer was deliberately left unmoved (no `git push`/`pull` performed) while the file itself was synced directly. |
| **2 — global detail mode is genuinely in effect** (the 19-08 precedent's second invalidating count, the misspelled-key case) | **FAIL — but not for the 19-08 reason.** The key is spelled **correctly** (singular `dayReportDetail`, not the misspelled `dayReportDetails` plural that broke the 19-08 observation), so the 19.1-02 config-key validator has nothing to warn about here and none fired (see Log Evidence below) — that absence is itself evidence the 19.1-02 fix behaves correctly for a well-formed key. **However, the live value is currently `dayReportDetail: false`**, not `true`. The hardware-access notes carried into this plan state "`dayReportDetail: true` is DELIBERATELY on... do not turn it off" — that was true in a prior session but is **no longer the deployed state**; the operator has evidently toggled it off since then. Quoted verbatim from the deployed `~/MagicMirror/config/config.js`, read-only: |

  ```javascript
  {
      module: "MMM-SPCOutlook",
      position: "top_right",
      header: "SPC Forecast",
      config: {
          lat: 35.4432156,
          lon: -97.595822,
          extended: true,
          proximityWeighting: true,
          updateInterval: 60,
          showExcessiveRain: true,
          showMPD: true,
          showHazardsOutlook: true,
          showDrought: false,
          showHeatRisk: true,
          showMinorHeat: false,
          dayReportDetail: false,

          // Testing
          //lat: 42.85,
          //lon: -106.31
      }
  }
  ```

**Consequence:** with `dayReportDetail: false`, only days whose own hazard significance triggers
`autoExpand` will render detail sub-rows this session (e.g. Days 1/4 in the 19-UAT run); the
remaining days will render compact-only. Task 2's checkpoint script assumes `dayReportDetail: true`
is active for a *full*, every-day observation of the width defect. Per this plan's own acceptance
criteria ("both conditions as PASS ... or the plan stops here with a FAIL and a named blocker"),
**Task 1 stops here with condition 2 recorded FAIL.** This is NOT the 19-08 misspelling failure
mode (the key is correct), and this plan's `<deploy_constraints>` forbid modifying
`~/MagicMirror/config/config.js` under any circumstance — only the operator can flip this value.
Task 1 proceeded with the restart and log capture below anyway (harmless, non-destructive, and
still evidentiary for the config-key-validator claim regardless of this value), but the full,
every-day width observation Task 2 is designed around should not be presented as ready until the
operator confirms `dayReportDetail: true` is the state they want active for the check — see the
blocker note returned with this checkpoint.

---

## Log Evidence

- **Restart performed:** `pm2 restart mm` (the Pi runs MagicMirror under pm2 as process `mm`,
  `npm start` → `electron js/electron.js`; not systemd, not a bare foreground process). Pre-restart
  `mm-out.log` line count recorded (543518) as the boundary; all lines below are from the restart
  onward (line 543519+).
- **Module-start line:**
  ```
  [2026-09-10 08:45:17.641] [LOG]   [node_helper] Initializing new module helper ...
  [2026-09-10 08:45:17.642] [LOG]   [node_helper] Module helper loaded: MMM-SPCOutlook
  [2026-09-10 08:45:17.793] [INFO]  [MMM-SPCOutlook] Starting node_helper for MMM-SPCOutlook...
  ```
- **First-accepted-payload timing line:**
  ```
  [2026-09-10 08:45:26.573] [INFO]  [MMM-SPCOutlook] MMM-SPCOutlook: new-product batch settled in 2043ms {"spc-inline":4175,"winterImpact":6,"spcMD":108,"heatRisk":349,"excessiveRain":634,"hazardsOutlook":848,"mpd":2036}
  [2026-09-10 08:45:26.585] [INFO]  [MMM-SPCOutlook] MMM-SPCOutlook: cold-start timing -- backend interval 6236ms (GET_SPC_DATA received -> SPC_DATA_RESULT emitted); 8791ms since process start (module-load to first result sent)
  ```
- **`unrecognized config key` line (plan 19.1-02's warning):** **ABSENT.** `grep -i unrecognized`
  against the full restart-onward log window returned no matches. This is the **expected** result
  given condition 2's finding above: the deployed config's `MMM-SPCOutlook` entry carries no
  misspelled key (`dayReportDetail` is spelled correctly), so the validator correctly has nothing
  to warn about. The absence of a false-positive warning on an otherwise-valid config is itself
  live evidence the 19.1-02 validator is not over-firing.
- Cold start completed cleanly with no errors; the module produced a real payload
  (`new-product batch settled in 2043ms`) rather than staying stuck loading.

---

## Observations

*Not yet filled in — this section is Task 2's, to be completed after the operator has observed the
deployed mirror. Per this plan's own rule, no verdict may be recorded here until the operator has
stated it; a blank row here is the honest and expected state at the end of Task 1.*

| # | What was checked | Verdict | Reason / evidence |
|---|---|---|---|
| 1 | Day-report block no longer extends into the centre column (vs 19-UAT test 5 screenshot) | | |
| 2 | Compact (unexpanded) day rows no longer look displaced | | |
| 3 | Any row wraps onto a second line (and which/how often) | | |
| 4 | Em-dash source-attribution column still straight | | |
| 5 | Co-equal hazards still render as peers (not demoted to `also:`) | | |
| 6 | `also:` rows still sit indented under their own winner row | | |
| 7 | `also:`/attribution monospace font-family change acceptable at viewing distance | | |
| 8 | Header reads `NWS Hazard Outlooks` (absent-key case) or the configured value verbatim | | |
| 9 | Loading string reads `Loading NWS outlooks...` (transient; NOT OBSERVABLE is expected) | | |
| 10 | Config-key log line present/absent matching the config's actual spelling | | |

## Disposition

*(empty — pending the operator's cap decision; not to be filled in by the executor)*
