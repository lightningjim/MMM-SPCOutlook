---
phase: 19-unified-day-report-getdom-rewrite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/19-unified-day-report-getdom-rewrite/19-PARITY-CHECKLIST.md
autonomous: true
requirements: [RPT-06]

must_haves:
  truths:
    - "A single artifact enumerates every display behavior the old getDom() shipped, each with its defect/requirement ID, so a rewrite cannot silently drop one."
    - "The three roadmap-mandated intentional relocations are recorded as expected changes, not as regressions to investigate."
    - "The two mandatory manual runs have written, reproducible procedures (exact config.js blocks and expected observations) that an operator can execute without re-deriving them."
  artifacts:
    - path: ".planning/phases/19-unified-day-report-getdom-rewrite/19-PARITY-CHECKLIST.md"
      provides: "RPT-06 acceptance gate: 35 preserved-behavior rows + 3 intentional-change rows + 2 manual-run procedures"
      contains: "PROXUI-04"
      min_lines: 90
  key_links:
    - from: ".planning/phases/19-unified-day-report-getdom-rewrite/19-PARITY-CHECKLIST.md"
      to: ".planning/phases/19-unified-day-report-getdom-rewrite/19-RESEARCH.md"
      via: "each checklist row cites the RESEARCH.md checklist item number and the MMM-SPCOutlook.js source line it was enumerated from"
      pattern: "MMM-SPCOutlook.js:"
---

<objective>
Build the RPT-06 behavior-parity checklist as a literal tracking artifact **before any render
code is written**. Pitfall 9 (`.planning/research/PITFALLS.md`) instructs this explicitly: the
dominant failure mode of a display rewrite is silently dropping one of the ~35 behaviors the old
renderer accumulated across four milestones, and a checklist assembled after the rewrite is
written is a checklist written from the new code rather than the old.

Purpose: RPT-06 is this phase's acceptance gate. This artifact IS the gate. Plans 19-04 through
19-06 tick rows as they carry each behavior forward; plan 19-08 signs it off.
Output: `19-PARITY-CHECKLIST.md` — 35 preserved-behavior rows, 3 intentional-change rows, and
two written manual-run procedures.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/buildomator/bm/4.6.0/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/buildomator/bm/4.6.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-unified-day-report-getdom-rewrite/19-RESEARCH.md
@.planning/phases/19-unified-day-report-getdom-rewrite/19-UI-SPEC.md
@.planning/phases/19-unified-day-report-getdom-rewrite/19-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the 35-row preserved-behavior checklist</name>
  <read_first>
    - `.planning/phases/19-unified-day-report-getdom-rewrite/19-RESEARCH.md` — section "The RPT-06 Behavior-Parity Checklist" is the source table (35 numbered rows, each with a Req/Defect ID, an observable behavior, and a source line range). Also read its closing paragraph on BUG-01/02/04, FWXT-05, PROX-01/02/05 being backend-only.
    - `MMM-SPCOutlook.js` — read `getDom()` in full (lines 184–784). Every inline comment in that range is a prior defect's tombstone; the checklist rows must describe the behavior the comment defends, not merely the line's syntax.
    - `.planning/phases/19-unified-day-report-getdom-rewrite/19-PATTERNS.md` — the "Phase shape note" and "helpers to carry forward verbatim" sections. PATTERNS.md line numbers are freshly verified against the working tree and are authoritative where they disagree with RESEARCH.md prose.
  </read_first>
  <files>.planning/phases/19-unified-day-report-getdom-rewrite/19-PARITY-CHECKLIST.md</files>
  <action>
    Create the file with YAML frontmatter (`phase: 19`, `requirement: RPT-06`, `status: open`,
    `baseline_probe_result: "123 passed, 0 failed, 0 skipped"`) and a markdown table titled
    `## Preserved Behaviors (35)`.

    Columns, in this exact order: `#`, `ID`, `Behavior (observable)`, `Old site`, `New site`,
    `Run A (no risk anywhere)`, `Run B (everything active)`, `Notes`.

    Transcribe all 35 rows from RESEARCH.md's checklist verbatim in its numbering, correcting
    each `Old site` line reference against the current working tree using PATTERNS.md's verified
    numbers. Specifically use these verified anchors, confirmed against the working tree at
    planning time: `dowToText` :185–189, `cigLabel` :190–195, `PROX_MIN_WEIGHT` :197,
    `cigLabelFromTierString` :198–203, `hasRenderableProximity` :209–220,
    `hasAnyRenderableProximity` :222–229, `proximityBadge` :230–238, `escapeHtml` :245–247,
    `ADVISORY_SOURCES` :254, `dayRiskCount` :265, `heatRiskDaysToRender` :381–398,
    `enabledAdvisories` :409, loading string :420, no-risk gate :427–488, confident all-clear
    string :490, stale badge + `_staleAsOf` :494–505, `contentMarker` :512, advisory band
    :513–540, day 1/2 rows :541–561, day 3 dual badge :562–569, days 4–8 :570–577, fire weather
    :578–598, `renderDayBlock` :604–614, `HAZARDS_LABEL_MAX_CHARS` :623, `truncateHazardLabel`
    :627–632, `validHazardColor` :636–638, `hazardsWeekdayFromDate` :643–646,
    `renderHazardsDays` :649–676, `renderHazardsWindowBand` :682–730, HeatRisk loop :751–763,
    unconfirmed all-clear string :778–779.

    Leave `New site`, `Run A`, and `Run B` empty (they are filled by 19-04..19-06 and 19-08).

    Add a `## Backend-Only Provenance (footnote)` section listing BUG-01 (SIGN detection),
    BUG-02 (day 8 shape), BUG-04 (`checkInPolygon` full iteration), FWXT-05 (URL/parsing), and
    PROX-01/02/05 (proximity computation) as behaviors whose fixes live entirely in
    `node_helper.js` — no `getDom()` behavior exists to preserve for them beyond rows 3, 12–15
    and 18 already covering the fields they produce.

    Add a `## Ten Proximity Mode Call Sites` section enumerating all ten inside/outside-mode
    decisions with their exact legacy source condition, because RESEARCH.md Pitfall 4 names
    centralizing these into one shared predicate as the specific way a rewrite silently flips
    badge wording: Day 1 categorical (`day1.risk == "NONE"`), Day 1 torCig (`day1.torCig === 0`),
    Day 1 hailCig (`day1.hailCig === 0`), Day 1 windCig (`day1.windCig === 0`), Day 2 categorical
    (`day2.risk == "NONE"`), Day 2 torCig, Day 2 hailCig, Day 2 windCig (same shape as Day 1),
    Day 3 categorical (`day3.risk == "NONE"`), Day 3 cig (`day3.cig === 0`). Record that each
    per-hazard-type mode is independent of the day's categorical mode (PROXUI-03/04).
  </action>
  <verify>
    <automated>test $(grep -c '^| [0-9]' .planning/phases/19-unified-day-report-getdom-rewrite/19-PARITY-CHECKLIST.md) -ge 35 &amp;&amp; grep -q 'PROXUI-04' .planning/phases/19-unified-day-report-getdom-rewrite/19-PARITY-CHECKLIST.md &amp;&amp; grep -q 'day3.cig === 0' .planning/phases/19-unified-day-report-getdom-rewrite/19-PARITY-CHECKLIST.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/19-unified-day-report-getdom-rewrite/19-PARITY-CHECKLIST.md` exists with YAML frontmatter containing `requirement: RPT-06`.
    - The `## Preserved Behaviors (35)` table has at least 35 numbered data rows.
    - Every one of these ID strings appears at least once in the table: `BUG-03`, `CR-01`, `FWXT-03`, `WR-08`, `WR-09`, `WR-04`, `WR-12`, `WR-02`, `WR-06`, `WR-01`, `PROXUI-01`, `PROXUI-02`, `PROXUI-03`, `PROXUI-04`, `PROXUI-05`, `T-16-19`, `T-16-20`, `T-16-22`, `IN-08`, `ADVISORY_SOURCES`.
    - Every row's `Old site` cell contains a `MMM-SPCOutlook.js:`-prefixed line reference or a bare `:NNN` line reference that exists in the current file (no reference above line 784).
    - The `## Ten Proximity Mode Call Sites` section lists exactly 10 entries, and each names its legacy source condition verbatim (e.g. `day1.torCig === 0`).
    - The `## Backend-Only Provenance (footnote)` section names BUG-01, BUG-02, BUG-04, FWXT-05, PROX-01, PROX-02 and PROX-05.
    - `New site`, `Run A` and `Run B` columns are present and empty for every row.
  </acceptance_criteria>
  <done>The checklist artifact exists, has ≥35 ID-tagged rows with verified old-site line references, the 10 proximity mode call sites, and empty tracking columns ready for 19-04..19-08 to fill.</done>
</task>

<task type="auto">
  <name>Task 2: Record intentional changes and write the two manual-run procedures</name>
  <read_first>
    - `.planning/phases/19-unified-day-report-getdom-rewrite/19-PARITY-CHECKLIST.md` — the file just created in Task 1.
    - `.planning/phases/19-unified-day-report-getdom-rewrite/19-UI-SPEC.md` — sections "The Band (RPT-04 — below all day blocks)", "Proximity-only compact exception (D-08)", "Copywriting Contract", "Global Stale Badge (D-09 — position now decided)". These define the target end state each manual run observes against.
    - `.planning/phases/19-unified-day-report-getdom-rewrite/19-CONTEXT.md` — D-07's "Accepted cost, must be recorded on the RPT-06 checklist" paragraph, and D-08.
    - `MMM-SPCOutlook.js` lines 2–32 — the `defaults` block, so the manual-run `config.js` snippets name real flags with their real default values.
  </read_first>
  <files>.planning/phases/19-unified-day-report-getdom-rewrite/19-PARITY-CHECKLIST.md</files>
  <action>
    Append two sections to the checklist.

    First, `## Intentional Changes (verified-expected, NOT regressions)` — a table with columns
    `ID`, `Change`, `Authority`, `Verified expected by`. Three rows, minimum:
    (1) `RPT-04-RELOC` — SPC MD and WPC MPD advisories move from ABOVE the day rows (legacy
    `MMM-SPCOutlook.js:513-540`) to a band BELOW all day blocks; authority ROADMAP Phase 19
    success criterion 4 / RPT-04 / UI-SPEC "The Band". Note that RESEARCH.md Pitfall 3 predicts a
    parity reviewer will otherwise file this as a regression.
    (2) `D-07-RELOC` — the categorical proximity badge and the SPC tornado/hail/wind probabilistic
    breakdown are no longer on the default (compact) line; both move to detail mode only. At
    default config a user sees neither. Authority CONTEXT.md D-07. Record this as a *relocation*,
    not a removal — both are fully present in detail mode and D-04's auto-expand surfaces them
    without reconfiguration.
    (3) `D-03-SKIP` — days with no surviving hazard render no row and no marker, so the rendered
    day list is non-contiguous (e.g. Day 3, Day 6, Day 9). Authority CONTEXT.md D-03.
    Add a fourth row `D-08-EXC` recording the deliberate exception: with `proximityWeighting:
    true`, a day with no surviving hazard still renders, badge alone, so the shipped v1.2 PROXUI
    outside-mode behavior is not lost at default config.

    Second, `## Mandatory Manual Runs`. Two numbered procedures. Each must state: the exact
    `config.js` module block to paste (naming every flag with an explicit value, not "defaults"),
    how to reach the state, and a numbered list of expected observations cross-referenced to
    checklist row numbers.

    Run A — "no risk anywhere": all seven product toggles enabled (`showExcessiveRain`,
    `showWinterImpact`, `showMPD`, `showHazardsOutlook`, `showHeatRisk` all `true`, `showSPCMD`
    `true`, `extended: true`), `proximityWeighting: false`, `dayReportDetail: false`, at a
    coordinate with no active hazard from any source. Expected observations: exactly one line of
    content; the literal string `No Severe Weather Risk` when the payload is not stale, or
    `No Severe Weather Risk (unconfirmed)` when it is; zero day rows; zero band lines; no bare
    `⚠` badge with nothing beneath it (CR-01). Record that the operator must note which of the two
    strings appeared and whether `_stale` was set.

    Run B — "everything active at once": same toggles, plus `proximityWeighting: true` and
    `dayReportDetail: true`, at a coordinate inside an active SPC convective risk area during a
    period with a live SPC MD and/or WPC MPD. Expected observations, each cross-referenced to a
    checklist row: compact `Day N (Weekday)` header lines with two literal spaces and ` · `
    separators; dimension sub-rows with `— {Source}` attribution; the tornado/hail/wind icon line
    on days 1–2 only; a single combined CIG glyph inline in the day-3 label field; no probabilistic
    sub-line on days 4–8; the band below all day blocks containing MD/MPD lines then
    `Extended Hazards:`; the stale badge at the very top if `_stale` is set.

    Add a closing `## Sign-Off` section with unchecked boxes for: all 35 preserved rows ticked in
    both Run A and Run B columns; all 4 intentional-change rows confirmed observed-and-expected;
    `node scripts/probe-payload-resilience.js` reporting `0 failed, 0 skipped`; operator name and
    date.

    Note explicitly in `## Mandatory Manual Runs` that per 15 D-10 the probe suite is proof of
    mechanism and these two runs are proof of live behavior — neither substitutes for the other.
  </action>
  <verify>
    <automated>grep -q 'RPT-04-RELOC' .planning/phases/19-unified-day-report-getdom-rewrite/19-PARITY-CHECKLIST.md &amp;&amp; grep -q 'D-07-RELOC' .planning/phases/19-unified-day-report-getdom-rewrite/19-PARITY-CHECKLIST.md &amp;&amp; grep -q 'D-08-EXC' .planning/phases/19-unified-day-report-getdom-rewrite/19-PARITY-CHECKLIST.md &amp;&amp; grep -q 'Mandatory Manual Runs' .planning/phases/19-unified-day-report-getdom-rewrite/19-PARITY-CHECKLIST.md &amp;&amp; grep -q 'No Severe Weather Risk (unconfirmed)' .planning/phases/19-unified-day-report-getdom-rewrite/19-PARITY-CHECKLIST.md</automated>
  </verify>
  <acceptance_criteria>
    - `## Intentional Changes (verified-expected, NOT regressions)` exists with at least 4 rows carrying the IDs `RPT-04-RELOC`, `D-07-RELOC`, `D-03-SKIP`, `D-08-EXC`, each with an `Authority` cell naming a requirement ID or CONTEXT.md decision ID.
    - `## Mandatory Manual Runs` contains exactly two procedures labelled Run A and Run B.
    - Each procedure contains a fenced `config.js` snippet naming `showExcessiveRain`, `showWinterImpact`, `showMPD`, `showHazardsOutlook`, `showHeatRisk`, `showSPCMD`, `extended`, `proximityWeighting`, and `dayReportDetail` with explicit boolean values.
    - Run A's expected observations include both literal strings `No Severe Weather Risk` and `No Severe Weather Risk (unconfirmed)` and the CR-01 no-bare-badge condition.
    - Run B's expected observations reference at least 6 distinct checklist row numbers.
    - `## Sign-Off` exists with an unchecked box whose text includes `0 failed, 0 skipped`.
  </acceptance_criteria>
  <done>The checklist carries its intentional-change register and both manual-run procedures with paste-ready config blocks and row-cross-referenced expected observations, plus an unchecked sign-off block.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none crossed by this plan) | This plan writes a planning artifact only; it modifies no executable code and processes no remote input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Repudiation | `19-PARITY-CHECKLIST.md` sign-off | mitigate | Sign-off section records operator name and date, and requires the probe result line be transcribed, so a "verified" claim is attributable and re-checkable rather than asserted |
| T-19-02 | Tampering | npm/pip/cargo installs | mitigate | Not applicable this plan — no package install occurs. RESEARCH.md's Package Legitimacy Audit records that this phase introduces zero new packages; any install appearing in execution is out of scope and must halt the plan |
</threat_model>

<verification>
- `node scripts/probe-payload-resilience.js` still reports `123 passed, 0 failed, 0 skipped` (this plan changes no code; a change here means something else was touched).
- The checklist file parses as valid markdown with YAML frontmatter.
</verification>

<success_criteria>
- `19-PARITY-CHECKLIST.md` exists with ≥35 ID-tagged preserved-behavior rows, ≥4 intentional-change rows, the 10 proximity mode call sites, both manual-run procedures, and an unchecked sign-off block.
- No source file was modified by this plan.
</success_criteria>

<output>
Create `.planning/phases/19-unified-day-report-getdom-rewrite/19-01-SUMMARY.md` when done.
</output>
