---
phase: 19-unified-day-report-getdom-rewrite
plan: 05
type: execute
wave: 4
depends_on: ["19-04"]
files_modified:
  - MMM-SPCOutlook.js
  - scripts/probe-payload-resilience.js
  - .planning/phases/19-unified-day-report-getdom-rewrite/19-PARITY-CHECKLIST.md
autonomous: true
requirements: [RPT-03, RPT-06]

must_haves:
  truths:
    - "With dayReportDetail on, each day's compact line is kept as a header and every hazard on that day expands into a source-labeled sub-row."
    - "A day whose winning entry clears its own source's significance floor expands on its own, with no visual marker distinguishing it from a globally-expanded day."
    - "A suppressed competing entry renders beneath its dimension's winner as an 'also:' line, in its own payload color, so suppression reads as competing claims resolved rather than one source silencing another."
    - "The SPC probabilistic sub-line renders only where the payload can support it: full breakdown on grid days 1-2, a single inline CIG glyph on day 3, nothing on days 4-14."
    - "Every one of the ten legacy inside/outside proximity mode decisions is reproduced with its own source condition, not collapsed into one shared check."
    - "Space padding survives to the screen instead of collapsing, so the column grid actually aligns."
  artifacts:
    - path: "MMM-SPCOutlook.js"
      provides: "detail-mode dispatcher, dimension sub-rows, also: competitor lines, three-shape probabilistic sub-line, inside-mode and per-type proximity badges"
      contains: "autoExpand"
  key_links:
    - from: "MMM-SPCOutlook.js detail dispatcher"
      to: "this.config.dayReportDetail and days[n].autoExpand"
      via: "OR — either expands the day, and both render identically"
      pattern: "dayReportDetail"
    - from: "MMM-SPCOutlook.js convective sub-row"
      to: "days[n].proximity and hazard.detail"
      via: "inside/outside mode derived per call site from that site's own condition"
      pattern: "proximityBadge"
---

<objective>
Add detail mode on top of the compact renderer built in plan 19-04: dimension sub-rows with
source attribution, `also:` competitor lines, the SPC probabilistic sub-line with its three-shape
branching, and every proximity badge.

Two facts that a plan written from CONTEXT.md alone would get wrong, both verified:

1. **The convective `detail` sub-object has three shapes, not one** (`node_helper.js:3159-3167`).
   Grid days 1–2 carry `{probRisk, torRisk, torCig, hailRisk, hailCig, windRisk, windCig}`; day 3
   carries only `{probRisk, cig}`; days 4–8 carry only `{probRisk, sign}`; days 9–14 have no
   `detail` at all (the convective winner there is `wpc-hazards`). CONTEXT.md's D-07 mockup shows
   only the days-1–2 shape. A renderer that reads `detail.torRisk` unconditionally emits
   `undefined%` on day 3 and on the extended days.

2. **`detail.sign` has never been rendered by any shipped `getDom()`, in any milestone.** RESEARCH
   Open Question 3 is unanswered. Rendering it would be NEW UI in a phase whose framing is
   display-only with no new anything, and there is no parity behavior to preserve because nothing
   was ever shown. This plan does not render it. That is a deliberate no-op, recorded on the parity
   checklist, not an oversight.

Purpose: RPT-03 (source-labeled sub-rows), RPT-06 (PROXUI-01..05 and the day-1/2/3 probabilistic
behaviors).
Output: a `getDom()` that renders both detail levels from the same data model.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/buildomator/bm/4.6.0/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/buildomator/bm/4.6.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19-unified-day-report-getdom-rewrite/19-CONTEXT.md
@.planning/phases/19-unified-day-report-getdom-rewrite/19-UI-SPEC.md
@.planning/phases/19-unified-day-report-getdom-rewrite/19-RESEARCH.md
@.planning/phases/19-unified-day-report-getdom-rewrite/19-PARITY-CHECKLIST.md

<interfaces>
<!-- The three detail shapes. Verified at node_helper.js:3159-3167. -->
grid days 1-2 : detail = { probRisk, torRisk, torCig, hailRisk, hailCig, windRisk, windCig }
grid day 3    : detail = { probRisk, cig }
grid days 4-8 : detail = { probRisk, sign }          // sign: never rendered, out of scope
grid days 9-14: no detail object at all              // convective winner is wpc-hazards

<!-- Proximity subtree, added to the unified grid by plan 19-02. Grid days 1-3 only. -->
days["1"].proximity = { categorical?, torCig?, hailCig?, windCig? }
days["2"].proximity = { categorical?, torCig?, hailCig?, windCig? }
days["3"].proximity = { categorical?, cig? }

<!-- Existing helpers, all in scope after plan 19-04's hoist. -->
cigLabel(numericCig)            -> "① " | "② " | "③ " | ""   // NOTE the trailing space
cigLabelFromTierString("CIG2")  -> "②"                        // no trailing space
hasRenderableProximity(prox)    -> boolean                     // PROX_MIN_WEIGHT = 0.1 floor
proximityBadge(prox, "outside") -> " 0.3 (near Marginal)"      // LEADING space embedded
proximityBadge(prox, "inside")  -> " → Moderate 0.4"           // LEADING space embedded
DIMENSION_LABELS[dimensionId]   -> "Convective" .. "Heavy Precip"   (added by 19-04)
SOURCE_SHORT_NAMES[sourceId]    -> "SPC" | "SPC Fire" | "WPC ERO" | "WSSI" | "WPC Hazards" | "HeatRisk"

<!-- Legacy icon markup to reuse verbatim (verified MMM-SPCOutlook.js:546-548). -->
tornado: <i class="wi wi-tornado"></i>
hail:    <i class="wi wi-meteor"></i>
wind:    <i class="wi wi-strong-wind"></i>
percentage: 100 * risk + "% "

<!-- The ten legacy inside/outside mode conditions (parity checklist section). -->
day1 categorical: day1.risk == "NONE" ? "outside" : "inside"
day1 torCig:      day1.torCig === 0   ? "outside" : "inside"
day1 hailCig:     day1.hailCig === 0  ? "outside" : "inside"
day1 windCig:     day1.windCig === 0  ? "outside" : "inside"
day2 (same four, against day2's own fields)
day3 categorical: day3.risk == "NONE" ? "outside" : "inside"
day3 cig:         day3.cig === 0      ? "outside" : "inside"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Detail dispatcher, dimension sub-rows, also: lines, and whitespace preservation</name>
  <read_first>
    - `MMM-SPCOutlook.js` — the compact day loop written by plan 19-04, plus the hoisted helpers and `DIMENSION_LABELS`/`SOURCE_SHORT_NAMES` maps.
    - `.planning/phases/19-unified-day-report-getdom-rewrite/19-UI-SPEC.md` — "Detail line (per-day, `dayReportDetail: true` or auto-expand)" in full, including the column-offset table, the "Label field width, derived exactly from the CONTEXT.md mockup" paragraph (23 characters, em dash at column 42), "Detail-Mode Column Alignment", "Do not use font-size to establish hierarchy", "Vertical rhythm in detail mode", and "No Chrome for Auto-Expand".
    - `.planning/phases/19-unified-day-report-getdom-rewrite/19-CONTEXT.md` — D-04, D-05, D-06 and the `<specifics>` mockups.
    - `MMM-SPCOutlook.js:669-673` — `renderHazardsDays`'s per-hazard span builder, the guarded pattern every colored label must copy.
  </read_first>
  <files>MMM-SPCOutlook.js</files>
  <action>
    **Whitespace preservation — a required correction to UI-SPEC's character grid.** UI-SPEC
    specifies column offsets achieved by literal space padding, but HTML collapses runs of
    consecutive whitespace, so literal padding inside a plain `innerHTML` string renders as a
    single space and the grid silently fails. Every element that carries specified padding must
    therefore also carry `white-space:pre-wrap` in its inline `style`. Use `pre-wrap`, not `pre`:
    UI-SPEC explicitly wants a too-wide compact line to wrap naturally rather than be clipped.
    Apply this to (a) the compact day line built in plan 19-04, wrapping the whole line in one
    `<span style="white-space:pre-wrap">`, and (b) every detail sub-row, `also:` line and
    probabilistic sub-line. Note in a code comment that the probe suite asserts on the concatenated
    markup string and therefore cannot observe collapsing — only the plan 19-08 manual runs can, so
    this style must not be dropped as "cosmetic".

    Add the detail dispatcher inside the existing day loop: a day renders its sub-rows when
    `this.config.dayReportDetail === true || day.autoExpand === true`. The two paths must produce
    **identical** output — no marker, icon, or color hint distinguishing an auto-expanded day from
    a globally-expanded one (UI-SPEC "No Chrome for Auto-Expand"). Keep the compact line as the
    day's header, restated verbatim rather than rebuilt as a second, different header string (D-06).

    Sub-row construction, per dimension present on the day, in `DIMENSION_ORDER` — which the
    payload already provides as array order (18 D-15), so iterate the day's hazards in payload
    order and group by dimension without re-sorting:
    - Indent: 2 literal spaces.
    - Dimension name field: `DIMENSION_LABELS[dimension]`, left-aligned, space-padded to exactly
      13 characters. Derive the pad width from the longest value in `DIMENSION_LABELS` plus one
      rather than hardcoding 13 in a second place, so a future taxonomy addition widens the field
      instead of breaking it (UI-SPEC states this explicitly). For a `dimension === null` entry,
      leave the dimension field blank (13 spaces) and render the label alone — never drop it.
    - Label field: the winning entry's `text` (falling back to `label`), left-aligned and
      space-padded to exactly 23 characters, wrapped in one
      `<span style="color:#" + validHazardColor(entry.color) + ";white-space:pre-wrap;font-family:'DejaVu Sans Mono','Liberation Mono',monospace">`
      containing `escapeHtml(truncateHazardLabel(...))`. The dimension name and label share one
      color and one span (D-02, carried into detail). A label longer than 23 characters breaks
      alignment for that one row only — every row computes its own padding independently, which
      UI-SPEC accepts as rare cosmetic degradation and explicitly forbids engineering around with
      more truncation.
    - Source attribution: three literal spaces, an em dash (U+2014), one space, then
      `SOURCE_SHORT_NAMES[entry.source]` — uncolored, in no color span. This lands the em dash at
      column 42 for a full-width label. If `entry.source` has no `SOURCE_SHORT_NAMES` entry, fall
      back to `escapeHtml(entry.source)` rather than rendering `undefined`.
    - `also:` competitor lines: for every entry on that dimension whose `suppressedBy` is not null,
      in payload order (never re-sorted, 18 D-03), emit a line indented 17 literal spaces reading
      `also: ` then the competitor's label — **colored in its own payload color**, since all
      entries render and a competitor is not visually punished by being uncolored (18 D-13) — then
      the same three-space/em-dash/space attribution format. The word `also:` itself stays
      uncolored. Word it as a competing claim; never as one source silencing another.
    - Vertical rhythm: one extra `<br/>` after each day's block, applied uniformly to compact-only
      days adjacent to expanded days as well, so the rhythm never announces which day auto-expanded.

    Do not introduce a CSS file and do not modify `getStyles()` — every style added here is inline,
    matching the module's existing convention. Do not set `font-size`, `font-weight` or
    `line-height` anywhere: viewing distance is meters, and UI-SPEC forbids using size for
    hierarchy.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('MMM-SPCOutlook.js','utf8'); const t=s.split('\n').filter(l=>!l.trim().startsWith('//')).join('\n'); if(!/white-space:pre-wrap/.test(t)) throw new Error('whitespace preservation style missing'); if(!/autoExpand/.test(t)) throw new Error('autoExpand not consumed'); if(!/monospace/.test(t)) throw new Error('detail monospace override missing'); if(/font-size/.test(t)) throw new Error('font-size introduced, forbidden by UI-SPEC'); console.log('OK')" &amp;&amp; node scripts/probe-payload-resilience.js 2>&amp;1 | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - The detail branch condition is an OR of `this.config.dayReportDetail` and `day.autoExpand`, and both produce byte-identical markup for the same day (demonstrable by rendering the same payload twice, once with each trigger, and comparing strings).
    - `white-space:pre-wrap` appears in the inline style of the compact day line and of every detail sub-row, `also:` line and probabilistic sub-line.
    - The detail sub-row style contains `font-family` with `monospace` as its final fallback; the compact line does not switch font-family.
    - `grep -c "font-size" MMM-SPCOutlook.js` returns 0 and `getStyles()` is unchanged from HEAD.
    - The dimension field pad width is computed from `DIMENSION_LABELS`' longest value plus one, not written as a bare `13` literal at the padding site.
    - The source attribution segment is emitted outside any `style="color:` span.
    - An `also:` competitor label is wrapped in a color span using `validHazardColor(entry.color)`, while the literal `also:` text is not.
    - A `dimension === null` entry renders its label with a blank dimension field and is never dropped.
    - `node scripts/probe-payload-resilience.js` reports `0 failed, 0 skipped`.
  </acceptance_criteria>
  <done>Detail mode renders dimension sub-rows with attribution and `also:` competitor lines, with padding that survives to the screen, and an auto-expanded day is visually indistinguishable from a globally-expanded one.</done>
</task>

<task type="auto">
  <name>Task 2: Three-shape probabilistic sub-line and all ten proximity mode decisions</name>
  <read_first>
    - `MMM-SPCOutlook.js` lines 541–577 as they existed at HEAD (use `git show HEAD:MMM-SPCOutlook.js | sed -n '541,577p'` if plan 19-04 has already removed them) — the legacy day 1/2 probabilistic lines, the day-3 dual badge with its `day3DualSep` join, and the days 4–8 asymmetry. These are the exact behaviors this task must reproduce in the new location.
    - `.planning/phases/19-unified-day-report-getdom-rewrite/19-UI-SPEC.md` — "Detail-Mode Density (Priority 4 — the `detail` sub-object's three shapes)" table, and the "Inside-mode proximity badge (D-07, detail only)" paragraph.
    - `.planning/phases/19-unified-day-report-getdom-rewrite/19-RESEARCH.md` — "The `detail` Sub-Object Is Not Uniform", Pitfall 1, and Pitfall 4 (the ten proximity mode call sites).
    - `.planning/phases/19-unified-day-report-getdom-rewrite/19-PARITY-CHECKLIST.md` — the `## Ten Proximity Mode Call Sites` section and rows 12–17, 33, 34.
    - `MMM-SPCOutlook.js` lines 190–238 — `cigLabel` (numeric, trailing space) versus `cigLabelFromTierString` (string, no trailing space); `hasRenderableProximity`; `proximityBadge`. Do not merge the two CIG functions: their input domains never overlap and merging risks silently mis-branching on `typeof`.
  </read_first>
  <files>MMM-SPCOutlook.js, .planning/phases/19-unified-day-report-getdom-rewrite/19-PARITY-CHECKLIST.md</files>
  <action>
    Attach the inside-mode categorical proximity badge to the convective sub-row's label field:
    `proximityBadge(day.proximity && day.proximity.categorical, mode)` appended directly to the
    label text **inside the same color span** (UI-SPEC: inside mode is colored because it describes
    where the day's own active hazard is trending; outside mode is not, because it describes a tier
    the location is not in). Append the function's return with no added literal space — its return
    already carries a leading space. The badge text counts toward the 23-character label field's
    content, exactly as `Enhanced → Moderate 0.4` (23 characters) does in the approved mockup.

    Derive the mode **per call site from that site's own condition**, reproducing all ten legacy
    decisions. Do not centralize them into one shared "is this hazard active" check: RESEARCH
    Pitfall 4 names that as the specific way a rewrite silently flips badge wording. In the unified
    payload the legacy `dayN.risk == "NONE"` condition becomes "this day has no surviving
    `convective` entry" (a NONE-risk day produces no entry at all), and each `dayN.<type>Cig === 0`
    condition becomes the same test against `detail.torCig`/`hailCig`/`windCig`/`cig`, which are
    carried on the surviving convective entry's `detail`. Write each of the ten as its own explicit
    expression with a comment naming its checklist row, so a reviewer can diff them one-to-one.

    Branch the probabilistic sub-line on which `detail` fields are present — never on the day
    number, and never assuming uniformity:
    - `detail.torRisk` family present (grid days 1–2): emit a sub-line indented 5 literal spaces
      containing, in `torRisk, hailRisk, windRisk` order, one segment per type **only when that
      type's own risk is greater than 0** (the legacy per-type gate). Each segment is the legacy
      icon markup verbatim (`<i class="wi wi-tornado"></i>`, `wi-meteor`, `wi-strong-wind`), then
      `cigLabel(detail.<type>Cig)`, then
      `proximityBadge(day.proximity && day.proximity.<type>Cig, <that type's own mode>)`, then
      `100 * detail.<type>Risk + "% "`. Gate the whole sub-line on `detail.probRisk` being truthy,
      matching the legacy gate.
    - Only `detail.cig` present (grid day 3): emit **no** sub-line. Instead append
      `cigLabel(detail.cig)` directly into the label field, matching today's day-3 inline
      treatment. Additionally reproduce the day-3 dual badge (checklist row 15): compute the
      categorical badge and the cig badge separately and join them with `";"` **only when both are
      non-empty strings** — the legacy `day3DualSep` behavior. A single non-empty badge must not
      acquire a stray semicolon.
    - Only `detail.sign` present (grid days 4–8): emit no sub-line and no glyph. Plain label only.
      `sign` is not rendered. Add a comment recording that this is deliberate per RESEARCH Open
      Question 3, that no shipped `getDom()` has ever rendered it, and that surfacing it would be
      new UI requiring an explicit user decision outside this phase.
    - No `detail` object (grid days 9–14, or any non-convective dimension): plain label only.

    Guard every numeric read: a missing or non-numeric `torRisk`/`cig` must produce no segment
    rather than `undefined%` or `NaN%`.

    Then fill the parity checklist's `New site` column for rows 12, 13, 14, 15, 16, 17, 33 and 34,
    and add a checklist note under `## Intentional Changes` recording `SIGN-NOOP` — `detail.sign`
    remains unrendered by deliberate decision, with RESEARCH Open Question 3 as its authority.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('MMM-SPCOutlook.js','utf8'); const t=s.split('\n').filter(l=>!l.trim().startsWith('//')).join('\n'); for(const g of ['wi-tornado','wi-meteor','wi-strong-wind','cigLabel(','cigLabelFromTierString(']) if(!t.includes(g)) throw new Error('missing '+g); if(/detail\.sign/.test(t)) throw new Error('detail.sign is being read; it must remain unrendered'); console.log('OK')" &amp;&amp; node scripts/probe-payload-resilience.js 2>&amp;1 | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - The probabilistic sub-line branch tests for the presence of `detail.torRisk` (or the tor/hail/wind family) and separately for `detail.cig`; no branch keys off a hardcoded day number.
    - No code path reads `detail.sign` (grep, excluding comments) — the field stays unrendered.
    - Both `cigLabel` and `cigLabelFromTierString` are still present and still called; neither is deleted or merged into the other.
    - Ten distinct mode expressions exist, each with a comment naming its parity-checklist row; none of the ten reads another site's condition.
    - The day-3 dual badge joins with `";"` only when both badge strings are non-empty — demonstrable by rendering a day-3 fixture with exactly one renderable badge and confirming the output contains no `;`.
    - Each per-type segment is emitted only when that type's own risk is `> 0`; a fixture with `torRisk: 0` produces no tornado icon in the output.
    - A fixture with a non-numeric `hailRisk` produces neither `undefined` nor `NaN` in the output.
    - The parity checklist's `New site` column is filled for rows 12–17, 33 and 34, and a `SIGN-NOOP` row exists under `## Intentional Changes`.
  </acceptance_criteria>
  <done>The probabilistic sub-line branches on payload shape rather than day number, `sign` stays unrendered by recorded decision, and all ten inside/outside mode decisions are reproduced individually.</done>
</task>

<task type="auto">
  <name>Task 3: Mutation-prove detail mode, the three shapes, and the ten mode decisions</name>
  <read_first>
    - `MMM-SPCOutlook.js` — the code written in Tasks 1 and 2.
    - `scripts/probe-payload-resilience.js` — the `unifiedPayload` fixture builder added by plan 19-04, and the frontend scenario templates at `:4154-4189` and `:4928-4986`.
    - `.planning/STATE.md` — the 15 D-10 mutation-proof standard.
  </read_first>
  <files>scripts/probe-payload-resilience.js</files>
  <action>
    Extend `unifiedPayload` with helpers for building a convective entry with each of the three
    `detail` shapes and for attaching a `proximity` subtree, so every scenario below builds its
    fixture from one place.

    Add these scenarios:
    - `rpt03-detail-mode-renders-source-labeled-sub-rows-under-the-compact-header` — two dimensions
      surviving, `dayReportDetail: true`. Assert the compact header line still renders verbatim
      (D-06), that a sub-row containing `— SPC` and one containing `— HeatRisk` both render, and
      that both use the short names rather than the payload's long `displayName`. Precondition
      guard: throw unless the fixture's `sources` entries carry the long `displayName` values, so
      the assertion genuinely discriminates.
    - `rpt03-autoexpand-day-renders-identically-to-a-globally-expanded-day` — render the same day
      twice: once with `dayReportDetail: true` and `autoExpand: false`, once with
      `dayReportDetail: false` and `autoExpand: true`. Assert the two output strings are equal.
      Control: with both false, assert the sub-rows do not render, proving the equality is not
      trivially satisfied by neither path expanding.
    - `rpt03-also-line-renders-every-suppressed-competitor-in-its-own-color` — one dimension with a
      winner and two suppressed competitors. Assert both competitors render, that each appears in
      its own payload color (two distinct `color:#` values beyond the winner's), that the literal
      `also:` is not inside a color span, and that the competitors appear in payload order.
    - `rpt03-detail-shape-days-1-2-renders-the-full-icon-line` — assert all three icon classes
      render with their percentages, and that a type with `risk: 0` contributes no icon.
    - `rpt03-detail-shape-day-3-renders-one-inline-cig-and-no-icon-line` — assert the output
      contains a CIG glyph, contains none of the three `wi-` icon classes, and contains no `%`.
    - `rpt03-detail-shape-extended-days-render-plain-label-only` — a day-5 convective entry with
      `detail = { probRisk: true, sign: true }`. Assert the label renders, and that the output
      contains no `wi-` icon, no CIG glyph, no `%`, and no rendering of the `sign` value.
    - `rpt03-day3-dual-badge-joins-only-when-both-badges-are-non-empty` — two runs: both badges
      renderable (assert `;` present) and only one renderable (assert `;` absent).
    - `proxui-per-type-badge-modes-are-independent-of-the-day-categorical-mode` — a day with an
      active categorical risk (so the categorical badge is inside mode) but `torCig === 0` (so the
      tornado badge is outside mode). Assert the output contains both the inside-mode arrow form
      and the outside-mode `(near ` form in the same day's render — the single assertion that
      would catch the Pitfall 4 collapse.

    Mutation-prove each scenario individually per 15 D-10, recording the mutated line, the verbatim
    RED message and the restore:
    - change the detail dispatcher's `||` to `&&` → expect the autoExpand-equality scenario RED;
    - remove `white-space:pre-wrap` from the sub-row style → note whether ANY scenario goes RED; if
      none does, record explicitly in the SUMMARY that this style is **not** probe-observable (the
      DOM stub cannot see collapsing) and that plan 19-08's manual runs are its only proof, per
      15 D-10's rule that a structurally-unprovable guard must be disclosed rather than counted;
    - change the day-3 branch to emit the full icon line → expect the day-3 shape scenario RED;
    - remove the `risk > 0` per-type gate → expect the days-1-2 shape scenario RED;
    - change the day-3 dual-badge join to unconditional → expect the dual-badge scenario RED;
    - replace the tornado badge's own mode condition with the day's categorical mode → expect
      `proxui-per-type-badge-modes-are-independent-of-the-day-categorical-mode` RED;
    - change `SOURCE_SHORT_NAMES` lookup to `sources[id].displayName` → expect the sub-row
      scenario RED.
    A mutation yielding zero RED scenarios is either a fixture defect (rebuild the scenario) or a
    structurally-unprovable guard (disclose it in the SUMMARY, as `wssi-zero-features-out-of-season`
    was disclosed at Phase 15 close).
  </action>
  <verify>
    <automated>node scripts/probe-payload-resilience.js 2>&amp;1 | tail -1 | grep -q '0 failed, 0 skipped'</automated>
  </verify>
  <acceptance_criteria>
    - All eight new scenario names are present in `scripts/probe-payload-resilience.js`.
    - `node scripts/probe-payload-resilience.js` reports `0 failed, 0 skipped` with a passed count at least 8 higher than plan 19-04's final count.
    - `rpt03-autoexpand-day-renders-identically-to-a-globally-expanded-day` asserts string equality of two renders AND contains a control proving both-false does not expand.
    - `rpt03-detail-shape-extended-days-render-plain-label-only` asserts the absence of `wi-`, of a CIG glyph, and of `%`.
    - `proxui-per-type-badge-modes-are-independent-of-the-day-categorical-mode` asserts both the inside-mode and outside-mode badge forms appear in one day's render.
    - The SUMMARY records, per mutation, the exact line, the verbatim RED message, the scenario(s) that went RED, and the restore — and explicitly discloses any mutation that produced zero RED scenarios along with why.
  </acceptance_criteria>
  <done>Eight scenarios pin detail mode, the three detail shapes, the dual badge and per-type mode independence, each individually mutation-proven or explicitly disclosed as structurally unprovable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| remote `hazard.text`/`label`/`color` → detail sub-row and `also:` line `innerHTML` | Detail mode adds new call sites for the same remote-controlled strings the compact line renders; `wpc-hazards` labels pass through verbatim (18 D-07) |
| `days[n].proximity.nextTier` → `proximityBadge` → `innerHTML` | A remote-derived tier token reaches markup through a second render path added by this plan |
| `detail.torRisk`/`cig`/`sign` numerics → arithmetic and markup | Non-numeric values would surface as `NaN%`/`undefined%` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-17 | Tampering | remote label reaching the `also:` line and the detail label field | mitigate | Both call sites use the same `escapeHtml(truncateHazardLabel(...))` + `validHazardColor(...)` expression as the compact segment; Task 1's acceptance criteria require the `also:` label be inside a `validHazardColor` span and the literal `also:` text outside it |
| T-19-18 | Tampering | `prox.nextTier` concatenated into markup by `proximityBadge` | mitigate | `hasRenderableProximity` (`:209-220`) rejects a non-string or empty `nextTier` and the CIG branch maps only the three literal `CIG1/2/3` tokens; the plain-tier branch passes a source-token string through — apply `escapeHtml` to `proximityBadge`'s return at every new call site, and assert it in the WR-02 scenario family |
| T-19-19 | Denial of service | non-numeric `detail` fields producing `NaN%` or throwing inside the day loop | mitigate | Every numeric read is guarded before use; a malformed field yields no segment rather than a broken one, pinned by the non-numeric `hailRisk` acceptance criterion |
| T-19-20 | Information disclosure | detail mode surfacing per-source staleness or attribution that PROJECT.md scopes out of v2.0 | mitigate | Only `SOURCE_SHORT_NAMES` attribution renders; 18 D-04's per-source staleness stays unread. `grep -c "sources\[.*\]\.stale" MMM-SPCOutlook.js` must remain 0 |
| T-19-21 | Repudiation | a proximity badge silently changing inside/outside wording, misstating whether the location is in the risk area | mitigate | Ten independently-written mode expressions, each commented with its parity-checklist row, plus a scenario asserting both modes coexist in one day's render |
| T-19-SC | Tampering | npm/pip/cargo installs | mitigate | No package install in this plan; RESEARCH.md's Package Legitimacy Audit records zero new packages for this phase. An install appearing during execution halts the plan for a blocking human check |
</threat_model>

<verification>
- `node scripts/probe-payload-resilience.js` → `0 failed, 0 skipped`.
- `grep -c "detail\.sign" MMM-SPCOutlook.js` → 0 outside comments.
- `grep -c "font-size" MMM-SPCOutlook.js` → 0; `getStyles()` unchanged from HEAD.
- `grep -c "sources\[.*\]\.stale" MMM-SPCOutlook.js` → 0.
- Parity checklist `New site` column filled for rows 12–17, 33, 34.

</verification>

<success_criteria>
- Detail mode renders header + dimension sub-rows + `also:` lines, with padding that survives to screen.
- Auto-expanded and globally-expanded days are byte-identical.
- The probabilistic sub-line branches on payload shape; `sign` is unrendered by recorded decision.
- All ten proximity mode decisions reproduced individually.
- Eight new scenarios, each mutation-proven or explicitly disclosed as unprovable.
</success_criteria>

<output>
Create `.planning/phases/19-unified-day-report-getdom-rewrite/19-05-SUMMARY.md` when done.
</output>
