---
phase: 19-unified-day-report-getdom-rewrite
plan: 08
type: execute
wave: 7
# Depends on 19-06 (the last plan carrying an artifact this sign-off actually needs), NOT on
# 19-07. 19-07 is `droppable: true` per ROADMAP's contingency ("cut these back to the backlog
# rather than letting them compete with behavior parity"), and RPT-06 sign-off is not droppable
# — wiring this gate behind 19-07 would make the non-droppable gate unreachable in exactly the
# scenario the droppability clause exists to handle. 19-07's SUMMARY is read informationally
# only (probe-unobservable disclosures) and its absence is expected when 19-07 was cut.
depends_on: ["19-06"]
files_modified:
  - .planning/phases/19-unified-day-report-getdom-rewrite/19-PARITY-CHECKLIST.md
  - .planning/phases/19-unified-day-report-getdom-rewrite/19-HUMAN-UAT.md
autonomous: false
requirements: [RPT-06]

must_haves:
  truths:
    - "Every one of the 35 preserved behaviors has a new site in the rewritten renderer, or an explicit, authority-cited reason it does not."
    - "The two mandatory manual runs were executed on the deployed hardware and their observations recorded against the checklist row by row."
    - "The three roadmap-mandated relocations were confirmed observed-and-expected rather than filed as regressions."
    - "No behavior was recorded as verified on the strength of the probe suite alone where the probe suite structurally cannot observe it."
  artifacts:
    - path: ".planning/phases/19-unified-day-report-getdom-rewrite/19-PARITY-CHECKLIST.md"
      provides: "fully reconciled RPT-06 gate with Run A / Run B columns filled and a dated sign-off"
      contains: "Sign-Off"
    - path: ".planning/phases/19-unified-day-report-getdom-rewrite/19-HUMAN-UAT.md"
      provides: "verbatim operator observations from both mandatory runs, including any NOT OBSERVABLE rows and why"
  key_links:
    - from: ".planning/phases/19-unified-day-report-getdom-rewrite/19-PARITY-CHECKLIST.md"
      to: ".planning/phases/19-unified-day-report-getdom-rewrite/19-HUMAN-UAT.md"
      via: "each Run A / Run B cell cites the UAT observation that supports it"
      pattern: "19-HUMAN-UAT"
---

<objective>
Close RPT-06 — this phase's actual acceptance gate.

The rewrite is finished by plan 19-06; this plan proves it did not silently drop anything. Two
things must both hold, and neither substitutes for the other: the probe suite proves mechanism
(15 D-10, mutation-proven), and the two mandatory manual runs prove live behavior on the deployed
mirror. STATE.md records that Phase 19 carries the milestone's highest regression risk precisely
because there is no automated test framework standing between a rewrite and the wall.

Purpose: RPT-06.
Output: a fully reconciled parity checklist with a dated operator sign-off, and a UAT record.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/buildomator/bm/4.6.0/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/buildomator/bm/4.6.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-unified-day-report-getdom-rewrite/19-PARITY-CHECKLIST.md
@.planning/phases/19-unified-day-report-getdom-rewrite/19-CONTEXT.md
@.planning/phases/19-unified-day-report-getdom-rewrite/19-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile the checklist against the finished renderer</name>
  <read_first>
    - `.planning/phases/19-unified-day-report-getdom-rewrite/19-PARITY-CHECKLIST.md` — all 35 preserved-behavior rows, the intentional-change register, and the ten proximity mode call sites.
    - `MMM-SPCOutlook.js` — the finished `getDom()` in full, so each row's `New site` is verified against real code rather than against the plan that intended it.
    - The plan SUMMARYs `19-02` through `19-07`, specifically every disclosure of a mutation that produced zero RED scenarios (a structurally-unprovable guard) — each must be carried into the checklist as probe-unobservable rather than silently counted as verified. `19-07-SUMMARY.md` may legitimately not exist: 19-07 is `droppable: true` and may have been cut back to the backlog per ROADMAP's contingency. Its absence is expected in that case and is NOT itself a gap to report — read the SUMMARYs that exist and note the drop in the checklist header.
  </read_first>
  <files>.planning/phases/19-unified-day-report-getdom-rewrite/19-PARITY-CHECKLIST.md</files>
  <action>
    Run `node scripts/probe-payload-resilience.js` and transcribe the exact result line into the
    checklist's frontmatter as `final_probe_result`. A non-zero `failed` or `skipped` count blocks
    this plan — a skip is a missing proof, not a pass (15 D-10 / T-15-05).

    Walk all 35 rows against the finished `MMM-SPCOutlook.js`. For each, confirm the `New site`
    cell names a real line range in the current file and that the behavior described is actually
    present there. Any row whose `New site` is still empty must be resolved one of three ways, and
    the resolution written into the `Notes` cell with its authority:
    (a) the behavior was carried forward — fill `New site`;
    (b) the behavior was relocated by an approved decision — cross-reference the matching row in
        `## Intentional Changes` (`RPT-04-RELOC`, `D-07-RELOC`, `D-03-SKIP`, `D-08-EXC`,
        `SIGN-NOOP`);
    (c) the behavior retired with the code it defended — state which construct is gone and why the
        defect it guarded is now unreachable (row 20 / WR-02 is the model: `renderDayBlock` no
        longer exists and every replacement segment applies all three guards).
    A row with an empty `New site` and no `Notes` resolution is an unclosed parity gap and blocks
    the checkpoint in Task 2 — do not paper over it.

    Add a `## Probe Coverage` section mapping each of the 35 rows to the probe scenario name that
    observes it, or to the literal string `MANUAL ONLY` where no scenario can. Mark as
    `MANUAL ONLY` at minimum: the whitespace-preservation styles (the DOM stub is a flat object and
    cannot observe collapsing — disclosed in plan 19-05), the detail-mode column alignment, the
    monospace font override, and anything a `19-0X-SUMMARY.md` recorded as producing zero RED
    scenarios under mutation. Disclosure here is what keeps the sign-off honest; Phase 15 closed
    `wssi-zero-features-out-of-season` the same way.

    Add a `## Ten Proximity Mode Call Sites` verification column: for each of the ten, name the
    line in the finished renderer that implements it and confirm its condition matches the legacy
    condition recorded in plan 19-01.
  </action>
  <verify>
    <automated>node scripts/probe-payload-resilience.js 2>&amp;1 | tail -1 | grep -q '0 failed, 0 skipped' &amp;&amp; grep -q 'Probe Coverage' .planning/phases/19-unified-day-report-getdom-rewrite/19-PARITY-CHECKLIST.md &amp;&amp; grep -q 'MANUAL ONLY' .planning/phases/19-unified-day-report-getdom-rewrite/19-PARITY-CHECKLIST.md &amp;&amp; grep -q 'final_probe_result' .planning/phases/19-unified-day-report-getdom-rewrite/19-PARITY-CHECKLIST.md</automated>
  </verify>
  <acceptance_criteria>
    - `final_probe_result` in the checklist frontmatter matches the actual runner output line and contains `0 failed, 0 skipped`.
    - Every one of the 35 rows has a non-empty `New site` **or** a non-empty `Notes` cell citing an intentional-change ID or a retired-construct explanation. Zero rows are blank in both.
    - `## Probe Coverage` maps all 35 rows to either a scenario name that exists in `scripts/probe-payload-resilience.js` or the literal `MANUAL ONLY`.
    - Every scenario name cited in `## Probe Coverage` is found by `grep` in `scripts/probe-payload-resilience.js`.
    - `## Ten Proximity Mode Call Sites` has a verified line reference for all ten, each pointing into the current `MMM-SPCOutlook.js`.
    - Every zero-RED-mutation disclosure from the 19-02..19-07 SUMMARYs appears in `## Probe Coverage` as `MANUAL ONLY`.
  </acceptance_criteria>
  <done>All 35 rows are resolved against real code, probe coverage is mapped with honest MANUAL ONLY disclosures, and the suite is green with zero skips.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Execute the two mandatory manual runs on the deployed mirror</name>
  <read_first>
    - `.planning/phases/19-unified-day-report-getdom-rewrite/19-PARITY-CHECKLIST.md` — `## Mandatory Manual Runs` (both procedures with their paste-ready config blocks) and `## Intentional Changes`.
    - `.planning/STATE.md` — the decision that live verification of location-gated products requires temporarily moving `lat`/`lon`, verified with a point-in-polygon test against the live KMZ rather than a bounding-box check.
  </read_first>
  <what-built>
    `getDom()` has been fully rewritten: one merged block per day from the unified payload, compact
    by default with per-day auto-expand, source-labeled sub-rows in detail mode, MDs/MPDs and
    window-spanning Hazards Outlook entries in one band below the day blocks, three empty states,
    and the stale badge at the top. The probe suite is green with zero skips and every new scenario
    is individually mutation-proven. What the suite structurally cannot observe is on screen:
    whitespace collapsing, column alignment, the monospace override, and legibility at viewing
    distance.
  </what-built>
  <how-to-verify>
    1. Deploy the current working tree to the target Raspberry Pi (`ssh mm`) without disturbing the
       live MagicMirror configuration beyond the module block under test.
    2. Run **Run A ("no risk anywhere")** exactly as written in `## Mandatory Manual Runs`. Paste
       its config block verbatim. If the deployed coordinate carries live risk, temporarily move
       `lat`/`lon` to a quiet coordinate and record the coordinate used and the time.
       Confirm each expected observation, and record verbatim which of `No Severe Weather Risk` or
       `No Severe Weather Risk (unconfirmed)` appeared and whether `_stale` was set. Confirm there
       is no bare `⚠` badge with nothing beneath it (CR-01).
    3. Run **Run B ("everything active at once")** exactly as written. If no live convective risk
       covers the deployed coordinate, move `lat`/`lon` into an active polygon and confirm with a
       point-in-polygon test against the live KMZ — a bounding-box check is not sufficient
       (Phoenix fell inside MPD_1122's bbox but outside its polygon). Record the coordinate and time.
    4. For Run B specifically, confirm on screen: the two-space gap after `Day N (Weekday)` renders
       as two visible spaces and not one; the ` · ` separators; the detail sub-rows' columns
       actually line up (the monospace override is working); the probabilistic icon line appears on
       days 1–2 only; day 3 shows a single inline CIG glyph and no icon line; days 4–8 show a plain
       label; the band renders **below** every day block; and the whole render is legible from
       normal viewing distance with no font-size change.
    5. Confirm each `## Intentional Changes` row is observed and reads as expected, not as a
       regression: `RPT-04-RELOC` (MD/MPD moved from above the day rows to below them),
       `D-07-RELOC` (no proximity badge and no probabilistic breakdown at default config),
       `D-03-SKIP` (a non-contiguous day list with no marker for skipped days),
       `D-08-EXC` (with `proximityWeighting: true`, a would-be-empty day renders badge alone),
       `SIGN-NOOP` (nothing new rendered for days 4–8's trend indicator).
    6. For any row that cannot be observed in this session (seasonal product absent, no live MPD,
       no window-band entry), record it as `NOT OBSERVABLE` with the reason — do **not** record it
       as PASS. Phase 15/16/18 closed several rows this way and the precedent is that a disclosed
       non-observation is acceptable while a fabricated pass is not.
    7. Restore `lat`/`lon` and the original config when finished.
  </how-to-verify>
  <acceptance_criteria>
    - Both runs were executed and the operator states, for each of the 35 rows, one of `PASS`, `NOT OBSERVABLE (reason)`, or `FAIL (observation)`.
    - Any `FAIL` blocks phase completion and must be routed to a gap-closure plan rather than annotated away.
    - The coordinates and timestamps used for each run are recorded, including any temporary `lat`/`lon` move and the point-in-polygon confirmation for Run B.
    - The operator explicitly confirms or rejects each of the five intentional-change rows.
    - The operator explicitly confirms the whitespace and column alignment items from step 4, since these are the `MANUAL ONLY` rows the probe suite cannot observe.
  </acceptance_criteria>
  <resume-signal>Type "approved" with the per-row verdicts, or describe the failures observed.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the UAT results and sign off the checklist</name>
  <read_first>
    - The operator's verdicts from Task 2.
    - `.planning/phases/19-unified-day-report-getdom-rewrite/19-PARITY-CHECKLIST.md` — the `Run A`, `Run B` and `## Sign-Off` sections.
    - `.planning/phases/18-merge-precedence-unified-payload-schema/18-HUMAN-UAT.md` — the format precedent for recording a UAT session, including how NOT OBSERVABLE rows and their reasons were written.
  </read_first>
  <files>.planning/phases/19-unified-day-report-getdom-rewrite/19-HUMAN-UAT.md, .planning/phases/19-unified-day-report-getdom-rewrite/19-PARITY-CHECKLIST.md</files>
  <action>
    Create `19-HUMAN-UAT.md` following `18-HUMAN-UAT.md`'s format: session metadata (date, host,
    Node version, coordinates used per run, any temporary `lat`/`lon` move with its restore), the
    verbatim config blocks used, the operator's observations per run in their own words, and a
    table of the five intentional-change rows with their confirmed/rejected verdicts.

    Fill the checklist's `Run A` and `Run B` columns from those verdicts, one cell per row, using
    exactly `PASS`, `NOT OBSERVABLE`, or `FAIL`. For every `NOT OBSERVABLE`, put the reason in the
    `Notes` cell and cross-reference the supporting mutation-proven scenario if one exists —
    that is the standing precedent from Phase 15/16/18 closes (fixture evidence stands where live
    observation is not available on demand).

    Complete `## Sign-Off`: tick the boxes for all 35 rows resolved, all intentional-change rows
    confirmed, and the probe result line, and record the operator name and date.

    If any row came back `FAIL`, do **not** sign off. Instead write a `## Open Gaps` section naming
    each failure with its row number and the observation, and state in the plan SUMMARY that RPT-06
    is not met and a gap-closure pass is required before plan 19-09 may run.

    Append a `## Deferred Items` row to `.planning/STATE.md`'s deferred table for each
    `NOT OBSERVABLE` row, matching how Phase 15/16/18 closes recorded theirs, so the
    non-observation survives past this phase rather than being lost in a plan artifact.
  </action>
  <verify>
    <automated>test -f .planning/phases/19-unified-day-report-getdom-rewrite/19-HUMAN-UAT.md &amp;&amp; node -e "const fs=require('fs'); const c=fs.readFileSync('.planning/phases/19-unified-day-report-getdom-rewrite/19-PARITY-CHECKLIST.md','utf8'); const rows=c.split('\n').filter(l=>/^\| [0-9]+ \|/.test(l)); if(rows.length<35) throw new Error('fewer than 35 behavior rows'); const unfilled=rows.filter(r=>{const c2=r.split('|'); return !/PASS|NOT OBSERVABLE|FAIL/.test(r);}); if(unfilled.length) throw new Error(unfilled.length+' rows have no Run verdict'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `19-HUMAN-UAT.md` exists with session metadata, both verbatim config blocks, per-run observations, and the intentional-change verdict table.
    - Every one of the 35 checklist rows carries a `PASS`, `NOT OBSERVABLE` or `FAIL` verdict in both the `Run A` and `Run B` columns.
    - Every `NOT OBSERVABLE` has a reason in `Notes`.
    - `## Sign-Off` is complete with operator name and date, **or** an `## Open Gaps` section exists naming every `FAIL` and the SUMMARY states RPT-06 is not met.
    - Each `NOT OBSERVABLE` row also appears as a new row in `.planning/STATE.md`'s deferred-items table.
    - The recorded probe result line contains `0 failed, 0 skipped`.
  </acceptance_criteria>
  <done>The UAT session is recorded, every checklist row carries a verdict from both runs, non-observations are disclosed and carried into STATE.md, and RPT-06 is either signed off or explicitly declared unmet with named gaps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live NOAA data → deployed Raspberry Pi render | The manual runs observe real remote content reaching the real renderer — the only place the guards are exercised end to end |
| operator verdict → project record | A verdict recorded without observation would make the phase's acceptance gate fictional |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-33 | Repudiation | a parity row marked verified on the strength of a probe scenario that structurally cannot observe it | mitigate | `## Probe Coverage` forces every row to name a real scenario or the literal `MANUAL ONLY`, and every zero-RED-mutation disclosure from the earlier SUMMARYs must appear as `MANUAL ONLY` |
| T-19-34 | Spoofing | a `NOT OBSERVABLE` row later read as a PASS | mitigate | Distinct verdict vocabulary, a required reason in `Notes`, and a carried row in `.planning/STATE.md`'s deferred table so the non-observation outlives this phase's artifacts |
| T-19-35 | Tampering | a green probe run cited as proof while scenarios were skipped | mitigate | The skip count is transcribed into `final_probe_result` and the acceptance criteria require `0 failed, 0 skipped`; T-15-05 already made a skip a non-zero exit |
| T-19-36 | Information disclosure | a temporary `lat`/`lon` move left in place after the session | mitigate | Step 7 of the verification procedure requires restoring coordinates and config, and the UAT record must state the restore |
| T-19-SC | Tampering | npm/pip/cargo installs | mitigate | No package install in this plan; RESEARCH.md's Package Legitimacy Audit records zero new packages for this phase. An install appearing during execution halts the plan for a blocking human check |
</threat_model>

<verification>
- `node scripts/probe-payload-resilience.js` → `0 failed, 0 skipped`, transcribed into the checklist.
- All 35 rows carry a `New site` or an authority-cited `Notes` resolution, and a verdict in both run columns.
- `19-HUMAN-UAT.md` exists and records coordinates, timestamps and the coordinate restore.
</verification>

<success_criteria>
- RPT-06 is signed off with a dated operator sign-off, or explicitly declared unmet with named gaps routed to gap closure.
- Probe coverage is mapped honestly, with `MANUAL ONLY` on every row the harness cannot observe.
- All five intentional changes were confirmed observed-and-expected.
</success_criteria>

<output>
Create `.planning/phases/19-unified-day-report-getdom-rewrite/19-08-SUMMARY.md` when done.
</output>
