---
phase: 19-unified-day-report-getdom-rewrite
plan: 07
type: execute
wave: 6
depends_on: ["19-06"]
files_modified:
  - node_helper.js
  - scripts/probe-payload-resilience.js
autonomous: true
requirements: [RPT-04, RPT-05]
droppable: true

must_haves:
  truths:
    - "A source whose only live content is a window-band span reports itself as reporting, not silent."
    - "An unmapped hazard label that routes to the window band is recorded in that source's unmappedLabels ledger, matching the log line the same run already emits."
    - "A window-band label containing a pipe character can no longer collide with a different real span and silently drop an entry from the band."
    - "reportedDays and activeDays keep their day-grid-keyed meaning: the new reporting signal is additive, never a fabricated day key."
  artifacts:
    - path: "node_helper.js"
      provides: "noteWindowBandReported accumulator, window-routed noteUnmapped call, collision-free window-band dedupe key"
      contains: "noteWindowBandReported"
    - path: "scripts/probe-payload-resilience.js"
      provides: "the window-band-only fixture shape no scenario has ever had, plus an IN-01 collision scenario"
  key_links:
    - from: "node_helper.js:_addHazardsOutlookGridEntries window-routing branch"
      to: "notes.noteUnmapped / notes.noteWindowBandReported"
      via: "called before the routing continue, for window-routed matches too"
      pattern: "noteWindowBandReported"
    - from: "node_helper.js:_buildSourceHealth"
      to: "windowBandReported accumulator"
      via: "OR-ed into the reporting derivation without touching reportedDays"
      pattern: "reporting"
---

<objective>
Close the two remaining carried-in backend items.

**This plan is explicitly severable and droppable.** The ROADMAP marks all three carried-in items
SECONDARY to the rewrite: if RPT-01..06 is at risk, these go back to the backlog rather than
competing with behavior parity. This plan is sequenced last among the code plans and touches only
`node_helper.js` and the probe suite — nothing in plans 19-04 through 19-06 depends on it, and
dropping it unpicks nothing. (The third carried-in item, WR-02, is **not** in this plan and is
**not** droppable: the ROADMAP requires it close before any new row routes through the renderer,
which happens inside this phase, so it was discharged structurally in plan 19-04 — `renderDayBlock`
is deleted and every replacement segment applies `validHazardColor` + `escapeHtml` +
`truncateHazardLabel`, pinned by `wr02-unified-compact-segment-escapes-hostile-label-and-rejects-hostile-color`.)

Item 2 — `sources['wpc-hazards']` under-reports. Observed live on the target Pi 2026-09-06: the
fetch succeeded, `idpFiledate` was set, the window band carried two real spans, and the run logged
`unmapped hazard label rendered verbatim: Severe Drought` — yet the payload reported
`reporting: false`, `reportedDays: []` and `unmappedLabels: []`. The empty `days[]` is **correct by
design** (RPT-04 routes window spans to the band); only those two metadata fields are wrong.

Item 3 — IN-01. The window-band dedupe key is a `|`-joined string over a remote-controlled label,
so a label containing `|` can collide with a different real triple and silently drop a band entry.
Bounded by `HAZARDS_MAX_WINDOW_ENTRIES = 40`, so this is key hygiene, not a resource risk.

Purpose: RPT-05 (nothing keys display off a wrong reporting signal) and RPT-04 (the band is where a
dropped entry would actually be seen).
Output: correct `sources['wpc-hazards']` metadata, a collision-free band key, and the
window-band-only fixture shape the suite has never had.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/buildomator/bm/4.6.0/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/buildomator/bm/4.6.0/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19-unified-day-report-getdom-rewrite/19-RESEARCH.md
@.planning/phases/19-unified-day-report-getdom-rewrite/19-PATTERNS.md
@.planning/phases/18-merge-precedence-unified-payload-schema/18-LIVE-CAPTURE.md

<interfaces>
<!-- Verified against the working tree at planning time. -->

node_helper.js:4964-4993  the accumulators and the gridNotes object:
  const reportedDays = {}; const activeDays = {}; const unmappedLabels = {};
  noteReported(sourceId, gridDay)   // lazily creates a Set per source
  noteActive(sourceId, gridDay)
  noteUnmapped(sourceId, label)     // dedupes, truncates to HAZARDS_LOG_LABEL_MAX_CHARS,
                                    // caps at UNMAPPED_LABELS_MAX_PER_SOURCE
  const gridNotes = { noteReported, noteActive, noteUnmapped };

node_helper.js:2865-2873  the routing `continue` — root cause. A match with
  `match.group !== "precipitation"` (Temperature / Wildfire / Drought) OR a full-nominal-window
  Precipitation feature exits the loop iteration HERE, before any accumulator call.

node_helper.js:2908-2925  the day-grid branch's own `dimension === null` check and its
  `notes.noteUnmapped("wpc-hazards", match.label)` call at :2916 — the twin to mirror.

node_helper.js:2927-2935  `notes.noteReported("wpc-hazards", d)` / `noteActive` inside the
  per-day loop — day-grid-keyed by construction.

node_helper.js:3663-3665  the site to EXTEND, not replace:
  const reporting = !enabled ? false : (isAdvisory
    ? staleBySource[sourceId] !== true
    : (reportedDays[sourceId] ? reportedDays[sourceId].size : 0) > 0);

node_helper.js:3681        unmappedLabels: unmappedLabels[sourceId] || []

node_helper.js:936-941     IN-01's site:
  const seenWindowKeys = new Set();
  const windowBand = [];
  for (const entry of windowEntries) {
    const key = entry.label + "|" + entry.offsetStart + "|" + entry.offsetEnd;
    if (seenWindowKeys.has(key)) continue;
    seenWindowKeys.add(key);

node_helper.js:963-968     HAZARDS_MAX_WINDOW_ENTRIES = 40 cap
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record window-routed unmapped labels and give reporting a day-independent signal</name>
  <read_first>
    - `node_helper.js` lines 2830–2965 — `_addHazardsOutlookGridEntries` in full: its JSDoc naming the `notes` contract (`:2838`), the routing `continue` (`:2865-2873`) with its HAZ-02/D-04 comment, the `dimension === null` branch and its `noteUnmapped` call (`:2908-2925`), and the per-day `noteReported`/`noteActive` loop (`:2927-2935`).
    - `node_helper.js` lines 4960–4995 — the accumulator declarations and the `gridNotes` object literal that must gain the new function.
    - `node_helper.js` lines 3590–3690 — `_buildSourceHealth`: its signature, its JSDoc, the `reporting` derivation at `:3663-3665` with its 18-05 comment on gating `reporting` on `enabled`, and `unmappedLabels[sourceId] || []` at `:3681`.
    - `.planning/phases/19-unified-day-report-getdom-rewrite/19-RESEARCH.md` — "Carried-In Item 2" (traced root cause and fix shape) and Pitfall 5, which warns that fabricating a day key for a window-routed match corrupts `reportedDays`' D-14 absent-vs-below-floor distinction.
    - `.planning/phases/18-merge-precedence-unified-payload-schema/18-LIVE-CAPTURE.md` — the original live observation, so the fix is validated against the real shape rather than an invented one.
  </read_first>
  <files>node_helper.js</files>
  <action>
    Add a fourth accumulator beside `reportedDays`/`activeDays`/`unmappedLabels` at
    `node_helper.js:4964-4966`: `const windowBandReported = {};` plus
    `const noteWindowBandReported = (sourceId) => { windowBandReported[sourceId] = true; };`,
    following the same lazy-keying convention (no seeded roster — that would restate the source-id
    list `hazardTaxonomy.js` owns). Add it to the `gridNotes` object literal at `:4993`.

    In `_addHazardsOutlookGridEntries`, inside the window-routing branch at `:2865-2873` and
    **before** the `continue`:
    - call `notes.noteWindowBandReported("wpc-hazards")` unconditionally for the routed match — the
      source genuinely answered, in the only form this group can answer in;
    - compute `dimensionOf("wpc-hazards", match.label)` and, when it is `null`, call
      `notes.noteUnmapped("wpc-hazards", match.label)`, mirroring the day-grid branch's own call at
      `:2916`. Do not duplicate the `Log.info` block — the legacy path at `:894-970` already logs
      this label once per process; duplicating it would double-log the live-observed
      `"unmapped hazard label rendered verbatim: Severe Drought"` line.
    - Do **not** call `noteReported` or `noteActive` here, and do not fabricate a day number. Write
      a comment stating why: `reportedDays`/`activeDays` are day-grid-keyed `Set<number>` structures
      that `_resolveGridDayPrecedence` consumes for D-14's absent-vs-below-floor distinction, and a
      window-band entry has no day-grid association by design (RPT-04 requires exactly that).
    Extend the function's JSDoc `notes` contract at `:2838` to include `noteWindowBandReported`.

    In `_buildSourceHealth`, add `windowBandReported` as a parameter (append it to the signature and
    update the single call site at `node_helper.js:5066`), and extend — never replace — the
    `reporting` derivation at `:3663-3665` so a non-advisory source reports `true` when it has
    day-grid reported days **or** `windowBandReported[sourceId] === true`. Keep the outer
    `!enabled ? false` gate exactly as it is: 18-05's comment records that gating `reporting` on
    `enabled` is an invariant a reader must be able to trust even if a future source's own gate
    regresses. Add a comment recording that this OR is what makes `reporting` mean "this source
    answered in *any* form" rather than "this source produced a day-grid reading", and that
    `reportedDays` deliberately stays empty for a window-band-only poll. Update the
    `_buildSourceHealth` JSDoc accordingly.

    Keep the blast radius to these two functions plus the accumulator declaration. Do not touch
    `_resolveGridDayPrecedence`, `_buildGridSummary`, `_addHeatRiskGridEntries`,
    `_addSpcGridEntries`, `_addRegistryDayGridEntries`, or any other source's health derivation.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('node_helper.js','utf8'); const t=s.split('\n').filter(l=>!l.trim().startsWith('//')&&!l.trim().startsWith('*')).join('\n'); for(const g of ['noteWindowBandReported','windowBandReported']) if(!t.includes(g)) throw new Error('missing '+g); console.log('OK')" &amp;&amp; node scripts/probe-payload-resilience.js 2>&amp;1 | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - `windowBandReported` and `noteWindowBandReported` are declared beside the other three accumulators, and `noteWindowBandReported` is a member of the `gridNotes` object literal.
    - `_addHazardsOutlookGridEntries` calls `notes.noteWindowBandReported("wpc-hazards")` at a line number **lower** than the routing `continue` in the same branch.
    - The window-routing branch calls `notes.noteUnmapped("wpc-hazards", match.label)` only when `dimensionOf("wpc-hazards", match.label)` is `null`.
    - The window-routing branch contains **no** call to `noteReported` or `noteActive` and no day-number literal.
    - `node_helper.js` contains exactly one `Log.info` emission of the unmapped-hazard-label message (no duplicate logging introduced).
    - `_buildSourceHealth`'s signature gained a `windowBandReported` parameter, its single call site passes it, and the `reporting` expression still begins with the `!enabled ? false :` gate.
    - The JSDoc for both `_addHazardsOutlookGridEntries`'s `notes` contract and `_buildSourceHealth` names the new accumulator.
    - `node scripts/probe-payload-resilience.js` reports `0 failed, 0 skipped`.
  </acceptance_criteria>
  <done>A window-routed match records its unmapped label and marks its source as having answered, without touching `reportedDays`/`activeDays` or their D-14 semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Build the window-band-only fixture the suite has never had, and mutation-prove the metadata fix</name>
  <read_first>
    - `node_helper.js` — the changes made in Task 1.
    - `scripts/probe-payload-resilience.js` — the `hazards-precip-spread-buckets-every-day-in-span` scenario (search by name) for Hazards Outlook fixture-construction style, and `ero-wellformed-slgt` (`:1618-1652`) for the backend scenario skeleton.
    - `scripts/probe-payload-resilience.js` — search for existing scenario names `merge-sources-spc-fire-reporting-tracks-answering-not-finding` and `merge-unmapped-label-passes-through-and-is-recorded`; these are the two invariants the live defect contradicted, and the new scenarios must be consistent with them.
    - `.planning/ROADMAP.md` §Phase 19 carried-in item 2 — the sentence stating that **no probe fixture currently has a windowBand-only span with empty day arrays**, which is why 123 green scenarios and the phase verifier both missed this defect.
    - `.planning/STATE.md` — the 15 D-10 mutation-proof standard.
  </read_first>
  <files>scripts/probe-payload-resilience.js</files>
  <action>
    Add a fixture builder producing the shape the suite has never had: a Hazards Outlook response
    whose only matching features are in a non-Precipitation group (Temperature, Wildfire or
    Drought), so every match routes to the window band, every `days[n].hazards` array is empty for
    `wpc-hazards`, and at least one of the labels has no taxonomy entry (use the live-observed
    `"Severe Drought"` so the fixture reproduces the actual 2026-09-06 Pi observation rather than an
    invented one).

    Add these scenarios:
    - `merge-sources-window-band-only-poll-still-reports-the-source-as-reporting` — assert
      `out.sources["wpc-hazards"].reporting === true`, and — the precondition that makes this
      non-vacuous — assert `out.sources["wpc-hazards"].reportedDays` is an empty array and every
      `out.days[n].hazards` contains no `wpc-hazards` entry, so the scenario proves the *day-
      independent* signal rather than accidentally exercising the day-grid path. Assert
      `out.windowBand.length >= 1` as a second precondition. Control: the same fixture with the
      Hazards Outlook toggle off must report `reporting === false` (the `!enabled` gate must still
      dominate).
    - `merge-unmapped-window-routed-label-is-recorded-in-the-ledger` — assert
      `out.sources["wpc-hazards"].unmappedLabels` includes `"Severe Drought"`, and that the log
      captured exactly one emission of the unmapped-label message (not two), pinning the
      no-double-logging requirement.
    - `merge-window-band-reporting-does-not-fabricate-a-grid-day` — assert
      `out.sources["wpc-hazards"].reportedDays` and `.activeDays` are both empty arrays on the
      window-band-only fixture, so Pitfall 5's corruption of D-14's absent-vs-below-floor
      distinction cannot creep in later.

    Mutation-prove each individually per 15 D-10, recording the mutated line, the verbatim RED
    message and the restore:
    - move `noteWindowBandReported` below the routing `continue` → expect
      `merge-sources-window-band-only-poll-still-reports-the-source-as-reporting` RED;
    - remove the `dimensionOf(...) === null` condition so `noteUnmapped` fires for every routed
      match → expect `merge-unmapped-window-routed-label-is-recorded-in-the-ledger` RED (a mapped
      label would appear in the ledger);
    - change the `reporting` OR back to the day-grid-only derivation → expect the reporting
      scenario RED;
    - add a `noteReported("wpc-hazards", 1)` call in the window-routing branch → expect
      `merge-window-band-reporting-does-not-fabricate-a-grid-day` RED.
    Any mutation yielding zero RED scenarios is a fixture defect; rebuild the scenario.
  </action>
  <verify>
    <automated>node scripts/probe-payload-resilience.js 2>&amp;1 | tail -1 | grep -q '0 failed, 0 skipped' &amp;&amp; for s in merge-sources-window-band-only-poll-still-reports-the-source-as-reporting merge-unmapped-window-routed-label-is-recorded-in-the-ledger merge-window-band-reporting-does-not-fabricate-a-grid-day; do grep -q "$s" scripts/probe-payload-resilience.js || { echo "missing $s"; exit 1; }; done; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All three scenario names are present, and each calls `assertPayloadIntact(out)` first.
    - The window-band-only fixture uses a non-Precipitation group and the literal label `Severe Drought`.
    - `merge-sources-window-band-only-poll-still-reports-the-source-as-reporting` carries both preconditions (empty `reportedDays`, non-empty `windowBand`) and the toggle-off control.
    - `merge-window-band-reporting-does-not-fabricate-a-grid-day` asserts both `reportedDays` and `activeDays` are empty.
    - `node scripts/probe-payload-resilience.js` reports `0 failed, 0 skipped`.
    - The SUMMARY records, per mutation, the exact line, the verbatim RED message, the scenario(s) that went RED, and the restore.
  </acceptance_criteria>
  <done>The window-band-only fixture shape exists in the suite for the first time, and the live-observed metadata defect is pinned by three individually mutation-proven scenarios.</done>
</task>

<task type="auto">
  <name>Task 3: Replace the window-band dedupe key (IN-01) and pin the collision</name>
  <read_first>
    - `node_helper.js` lines 930–975 — the window-band dedupe loop, the sort at `:953`, and the `HAZARDS_MAX_WINDOW_ENTRIES` cap at `:963-968`.
    - `.planning/phases/19-unified-day-report-getdom-rewrite/19-PATTERNS.md` — the "IN-01" section, which records that **no existing dedupe site in this file uses a multi-field composite key over remote-controlled input**: `seenLabels` at `:920` keys on a raw single label, the MPD listing's `seen` at `:1396` is single-field, and `_inFlightCacheKeys`/`_geoJsonCache` key on a single URL string. There is nothing to copy; this is a new pattern for this codebase, and the plan says so rather than implying an analog exists.
    - `.planning/phases/16-*/16-REVIEW.md` — IN-01's original finding.
  </read_first>
  <files>node_helper.js, scripts/probe-payload-resilience.js</files>
  <action>
    Replace the key construction at `node_helper.js:939` with a form that cannot collide on a
    remote-controlled label. Use `JSON.stringify([entry.label, entry.offsetStart, entry.offsetEnd])`
    — a length-and-quote-delimited encoding in which no label content can be mistaken for a field
    boundary. Do not use a different separator character (any single delimiter has the same defect
    against a label containing it), and do not hash. Keep the `Set`-based loop, the sort, the cap,
    and the entry shape exactly as they are; the blast radius is this one expression.

    Add a comment recording: that the key is remote-controlled, that a bare `|` join let a label
    containing `|` collide with a different real triple and silently drop a band entry, that this
    is key hygiene rather than a resource risk because `HAZARDS_MAX_WINDOW_ENTRIES = 40` already
    bounds growth, and that no prior composite-key analog existed in this file so this is a new
    pattern rather than a relocation.

    Add one scenario `in01-window-band-labels-containing-a-pipe-do-not-collide`: a fixture with two
    genuinely different window entries whose old `label + "|" + offsetStart + "|" + offsetEnd`
    strings are identical — for example label `"A|1"` with offsets `2,3` versus label `"A"` with
    offsets `1|2` reconstructed so the joined strings match; construct the pair by computing the old
    key for both and asserting in a precondition guard that the two old keys are equal, so the
    scenario provably exercises a real collision rather than an imagined one. Then assert
    `out.windowBand.length === 2` and that both labels appear. Control: two genuinely identical
    entries still dedupe to `length === 1`, proving the fix did not simply disable deduping.

    Mutation-prove per 15 D-10: restore the `|`-joined key, confirm
    `in01-window-band-labels-containing-a-pipe-do-not-collide` goes RED with a diagnosable message
    (and the control stays green), then restore the fix. Record all of it in the SUMMARY.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('node_helper.js','utf8'); const t=s.split('\n').filter(l=>!l.trim().startsWith('//')).join('\n'); if(/entry\.label \+ \"\|\"/.test(t)) throw new Error('IN-01 pipe-joined key still present'); if(!/JSON\.stringify\(\[entry\.label/.test(t)) throw new Error('composite key not applied'); console.log('OK')" &amp;&amp; node scripts/probe-payload-resilience.js 2>&amp;1 | tail -1 | grep -q '0 failed, 0 skipped'</automated>
  </verify>
  <acceptance_criteria>
    - The `|`-joined key expression no longer exists in `node_helper.js` (grep, excluding comments).
    - The replacement is `JSON.stringify([entry.label, entry.offsetStart, entry.offsetEnd])` at the single call site; no other dedupe site in the file was changed.
    - `HAZARDS_MAX_WINDOW_ENTRIES` cap, the span-start sort, and the pushed entry shape are unchanged from HEAD.
    - Scenario `in01-window-band-labels-containing-a-pipe-do-not-collide` exists, carries a precondition guard asserting the two constructed entries' OLD keys are equal, and carries the identical-entries dedupe control.
    - `node scripts/probe-payload-resilience.js` reports `0 failed, 0 skipped`.
    - The SUMMARY records the mutation (restoring the `|` key), the verbatim RED message, that the control stayed green, and the restore.
  </acceptance_criteria>
  <done>A `|`-containing remote label can no longer collide with a different real span, pinned by a scenario whose fixture provably reproduces the old collision.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hazards Outlook feature attributes → `entry.label` → dedupe key | The label is remote-controlled and is used as key material |
| Hazards Outlook matches → `sources[].reporting` / `.unmappedLabels` | Metadata a display may key off; a wrong value produces a self-contradicting UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-28 | Tampering | remote label crafted with `\|` colliding in the window-band dedupe key and silently dropping a real hazard span | mitigate | Key replaced with `JSON.stringify([label, offsetStart, offsetEnd])`, whose quoting and escaping make label content unable to impersonate a field boundary; pinned by a scenario whose precondition guard proves the two old keys were genuinely equal |
| T-19-29 | Spoofing | `sources['wpc-hazards'].reporting === false` while the band actively renders that source's entries — a live, self-contradicting UI | mitigate | `reporting` OR-ed with a day-independent window-band signal; pinned by a scenario that also asserts `reportedDays` stays empty, so the fix cannot pass by corrupting the day-grid semantics |
| T-19-30 | Repudiation | an unmapped remote label rendered verbatim but absent from the diagnostic ledger, so a captured payload cannot explain what was shown | mitigate | `noteUnmapped` called for window-routed matches too, mirroring the day-grid twin; the ledger keeps its existing dedupe, truncation and `UNMAPPED_LABELS_MAX_PER_SOURCE` cap |
| T-19-31 | Denial of service | unbounded ledger or band growth from adversarial label variety | mitigate | `UNMAPPED_LABELS_MAX_PER_SOURCE` and `HAZARDS_MAX_WINDOW_ENTRIES = 40` are both untouched; Task 3 forbids changing the cap or the sort |
| T-19-32 | Information disclosure | duplicate `Log.info` emissions of remote label text inflating logs | mitigate | Task 1 forbids duplicating the log block; Task 2's ledger scenario asserts exactly one emission |
| T-19-SC | Tampering | npm/pip/cargo installs | mitigate | No package install in this plan; RESEARCH.md's Package Legitimacy Audit records zero new packages for this phase. An install appearing during execution halts the plan for a blocking human check |
</threat_model>

<verification>
- `node scripts/probe-payload-resilience.js` → `0 failed, 0 skipped`.
- `grep -c 'entry.label + "|"' node_helper.js` → 0.
- `_resolveGridDayPrecedence`, `_buildGridSummary` and the other four grid-entry adders are unchanged from HEAD (`git diff --stat` shows changes confined to the named functions).
</verification>

<success_criteria>
- A window-band-only poll reports `reporting: true`, records its unmapped label, and still reports empty `reportedDays`/`activeDays`.
- No duplicate unmapped-label logging.
- The window-band dedupe key cannot collide on a `|`-containing label.
- Seven new scenarios total across the three tasks, each individually mutation-proven.
</success_criteria>

<output>
Create `.planning/phases/19-unified-day-report-getdom-rewrite/19-07-SUMMARY.md` when done.
</output>
