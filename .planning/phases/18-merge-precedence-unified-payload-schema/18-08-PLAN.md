---
phase: 18-merge-precedence-unified-payload-schema
plan: 08
type: execute
wave: 6
depends_on: ["18-06", "18-07"]
files_modified:
  - scripts/probe-payload-resilience.js
autonomous: true
requirements: [MERGE-02, MERGE-03, MERGE-04, RPT-07]
must_haves:
  truths:
    - "SPC's granular tier suppressing WPC's Severe Weather is pinned by an executable scenario that keys off the dimension, not a label string"
    - "The rank-1-absent path and the rank-1-below-floor path are pinned by two separate scenarios, so a change that conflates them turns the suite red"
    - "HeatRisk 0 and null both failing the floor is pinned, distinctly from a genuine suppression at category 1 or above"
    - "flash-flood and heavy-precip never cross-suppressing is pinned"
    - "An unmapped label passing through with dimension null and being recorded in sources is pinned"
    - "sources[].reporting is asserted true for heatrisk and wpc-hazards on a day each actively reports, closing the wave-2 accumulator wiring"
    - "The unified days block and the legacy blocks are cross-checked against each other while both still exist"
    - "Every new scenario is individually mutation-proven with a diagnosable failure message"
  artifacts:
    - path: "scripts/probe-payload-resilience.js"
      provides: "the merge-precedence-*, merge-unmapped-*, merge-summary-* and merge-parity-* scenario families"
      contains: "merge-precedence-"
  key_links:
    - from: "new scenarios"
      to: "sources[].reportedDays / activeDays"
      via: "the absent-versus-below-floor distinction asserted from the payload"
      pattern: "reportedDays"
---

<objective>
Encode MERGE-02, MERGE-03, MERGE-04 and RPT-07 as permanent, mutation-proven probe scenarios,
including the old-versus-new parity cross-check that is only constructible while both representations
coexist.

Purpose: the ROADMAP words criteria 2 and 4 as "confirmed by inspecting the suppression code path
against a captured live payload" and "validated against captured live payloads". Plan 18-09 does the
live half. This plan does the executable half, so a future regression is caught by a command rather
than by someone remembering to look. RESEARCH.md's Pitfall 9 note specifically asks for the
legacy-versus-unified cross-check to be built now, because Phase 19 removes the legacy blocks and
makes it impossible to construct.

Output: four scenario families appended to the existing `scenarios` array, plus a recorded mutation
inventory.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/buildomator/bm/4.5.6/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/buildomator/bm/4.5.6/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-merge-precedence-unified-payload-schema/18-CONTEXT.md
@.planning/phases/18-merge-precedence-unified-payload-schema/18-RESEARCH.md
@.planning/phases/18-merge-precedence-unified-payload-schema/18-PATTERNS.md
@.planning/phases/18-merge-precedence-unified-payload-schema/18-05-SUMMARY.md
@.planning/phases/18-merge-precedence-unified-payload-schema/18-07-SUMMARY.md
@hazardTaxonomy.js
</context>

<interfaces>
<!-- Same probe harness contract as plan 18-07. Baseline entering this plan: 98 passed, 0 failed, 0 skipped. -->

Payload surface these scenarios assert against:
  days[N].hazards[]        { dimension, source, label, text, value, color, suppressedBy, detail? }
  summary                  { anyHazard, dimensions, activeDays, windowStart, windowEnd,
                             enabledSourceCount, reportingSourceCount,
                             bandDiagnostics: { windowBandCount, advisoryCount } }   // D-20
  sources[id]              { id, displayName, enabled, reporting, stale, idpFiledate,
                             reportedDays, activeDays, unmappedLabels, gridAnchor? }
  legacy, unchanged        day1..day8, day48Risk, fireWeather, excessiveRain, winterImpact,
                           hazardsOutlook, heatRisk, advisories, _stale, _staleAsOf
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add the precedence, floor and over/under-merge scenarios</name>

  <read_first>
    - `scripts/probe-payload-resilience.js` lines 5155-5228 and 4900-4920 — the five-part scenario
      shape and the isolation-focused precondition guard
    - `scripts/probe-payload-resilience.js` — the `merge-grid-*` family added by plan 18-07, for the
      fixture-building idioms this plan reuses
    - `hazardTaxonomy.js` — `PRECEDENCE`, `NO_RISK_FLOOR`
    - `.planning/phases/18-merge-precedence-unified-payload-schema/18-05-SUMMARY.md` — every
      scratch-harness output this task converts into permanent scenarios
    - `.planning/phases/18-merge-precedence-unified-payload-schema/18-RESEARCH.md`
      §"Precedence Table" (its D-14 nuance paragraph) and §"Why `flash-flood` and `heavy-precip` are
      kept separate"
    - `.planning/phases/18-merge-precedence-unified-payload-schema/18-CONTEXT.md` D-13, D-14, D-15
  </read_first>

  <files>scripts/probe-payload-resilience.js</files>

  <action>
Append these scenarios to the existing `scenarios` array. Every one uses a pinned `helper._nowMs`,
`resetHelper`/`resetLogs` at the top of each re-run closure, a precondition guard, a primary
assertion, a control assertion, and `assertPayloadIntact` plus the relevant legacy-block assertion.

`merge-precedence-spc-suppresses-wpc-severe-weather`
  SPC day-1 categorical at `SLGT` plus a `wpc-hazards` `Severe Weather` feature landing on grid day 1.
  Precondition: assert BOTH entries are present on `days["1"]` before asserting on `suppressedBy` —
  a suppression cannot be claimed proven if the WPC entry never reached the day at all.
  Primary: the `spc-convective` entry has `suppressedBy: null`; the `wpc-hazards` entry has
  `suppressedBy: "spc-convective"`; both are still present (D-03 keeps suppressed entries so this is
  inspectable). Control: with the WPC feature removed, the SPC entry is still `suppressedBy: null`,
  proving the field is not merely always-null-for-SPC.

`merge-precedence-spc-below-floor-does-not-suppress-wpc`
  SPC day-1 categorical at `TSTM` — a real reading at the floor — plus the same `Severe Weather`
  feature. Precondition: assert `sources['spc-convective'].reportedDays` contains 1, proving SPC
  really did report and this is the below-floor path rather than the absent path.
  Primary: the `wpc-hazards` entry has `suppressedBy: null`, and there is NO `spc-convective` entry
  on that day. This is D-13: a quiet source never erases another source's warning.
  Control: `sources['spc-convective'].activeDays` does NOT contain 1, distinguishing "reported below
  floor" from "reported and won".

`merge-precedence-spc-absent-day-is-not-the-floor-path`
  The same `Severe Weather` feature on a grid day SPC never covers (day 9 or later).
  Precondition: assert `sources['spc-convective'].reportedDays` does NOT contain that day, proving
  this is the absent path and not a below-floor reading.
  Primary: the `wpc-hazards` entry has `suppressedBy: null`. Control: assert the day-1 below-floor
  scenario's distinguishing fact holds here too but for the opposite reason — quote both
  `reportedDays` arrays in the failure message. RESEARCH.md requires these two states stay
  structurally distinct; this pair of scenarios is what makes a future conflation visible.

`merge-precedence-heatrisk-suppresses-wpc-hazardous-heat`
  HeatRisk category 2 on grid day 3 plus a `wpc-hazards` `Hazardous Heat` feature on the same day.
  Precondition: both entries present. Primary: `heatrisk` entry `suppressedBy: null`, `wpc-hazards`
  entry `suppressedBy: "heatrisk"`. Control: the same fixture with the WPC feature removed still
  yields `suppressedBy: null` on the HeatRisk entry.

`merge-precedence-heatrisk-zero-does-not-suppress-wpc-heat`
  HeatRisk category `0` plus the same `Hazardous Heat` feature.
  Precondition: assert the LEGACY `heatRisk.dayN.category` is exactly `0` and not `null`, proving the
  fixture expressed the distinction under test — 17 D-02 preserved the raw category precisely so this
  case is representable, and a fixture that cannot express its own condition is the second Phase 15
  vacuity mechanism.
  Primary: the `wpc-hazards` entry has `suppressedBy: null` and there is no `heatrisk` entry on that
  day. Control: with the category raised to 1, the same fixture yields `suppressedBy: "heatrisk"` —
  proving the gate fires at the documented floor and is not simply never firing.

`merge-precedence-heatrisk-null-does-not-suppress-wpc-heat`
  The same shape with `category: null` (no reading). Precondition: assert the legacy block's category
  is `null` and not `0`, the mirror of the previous scenario's guard. Primary: WPC entry survives.
  Control: the category-1 case suppresses. Keep this SEPARATE from the zero scenario: D-13 makes them
  behave identically, and a single scenario covering both could pass while one of the two paths is
  broken.

`merge-flash-flood-and-heavy-precip-never-cross-suppress`
  An ERO Slight tier on grid day 3 and a `wpc-hazards` `Heavy Rain` feature on the same day.
  Precondition: assert both entries are present and that their `dimension` values are
  `"flash-flood"` and `"heavy-precip"` respectively — if the taxonomy ever merged them, this guard
  fails first and names the merge, rather than the scenario silently proving nothing.
  Primary: both entries carry `suppressedBy: null`.
  Control: on the same day, add a `Severe Weather` feature and an above-floor SPC tier, and assert
  that pair DOES suppress — proving the suppression machinery is live in this fixture and that the
  flash-flood/heavy-precip survival is a real dimension separation rather than suppression being
  globally inert. This is MERGE-04's over-merge half and Pitfall 10's exact trap.

`merge-distinct-hazards-on-one-day-both-survive`
  MERGE-04's under-merge half: on one grid day, an above-floor SPC convective tier, an ERO
  flash-flood tier, and a `wpc-hazards` `High Winds` feature. Precondition: all three present.
  Primary: all three carry `suppressedBy: null`, `summary.dimensions` contains all three dimension
  strings, and `summary.activeDays` contains that day. Control: the `days[N].hazards` array is in
  `DIMENSION_ORDER` sequence, asserted against `hazardTaxonomy.DIMENSION_ORDER` rather than a
  hardcoded list — D-15's ordering is a taxonomy concern with one declaration.
  </action>

  <verify>
    <automated>node scripts/probe-payload-resilience.js | tail -3</automated>
  </verify>

  <acceptance_criteria>
    - `node scripts/probe-payload-resilience.js` -> `PROBE RESULT: 106 passed, 0 failed, 0 skipped`,
      exit 0 (98 entering + 8 new). A different count must be explained in the SUMMARY with the
      scenario list.
    - `node scripts/probe-payload-resilience.js | grep -cE '^PASS merge-(precedence|flash-flood|distinct)-'` is 8.
    - No new scenario asserts on a label string to establish suppression:
      `awk '/name: "merge-precedence-/,/^  },/' scripts/probe-payload-resilience.js | grep -nE 'suppressedBy.*(includes|indexOf)'`
      produces no output.
    - The absent-versus-below-floor pair both assert on `reportedDays`:
      `awk '/name: "merge-precedence-spc-(below-floor|absent)/,/^  },/' scripts/probe-payload-resilience.js | grep -c 'reportedDays'`
      is at least 2.
    - Every new scenario contains `precondition failed:` and `control:` strings.
  </acceptance_criteria>

  <done>Eight scenarios pin MERGE-02, MERGE-03 and MERGE-04, with the two conflation-prone paths kept apart.</done>
</task>

<task type="auto">
  <name>Task 2: Add the D-07 pass-through, summary-verdict and legacy-parity scenarios</name>

  <read_first>
    - `scripts/probe-payload-resilience.js` — `frontend-advisory-only-is-not-an-all-clear` and
      `frontend-heatrisk-only-is-not-an-all-clear`, the existing no-risk-gate scenarios whose
      backend counterpart `summary.anyHazard` now becomes
    - `scripts/probe-payload-resilience.js` — `assertHazardsBlockIntact`, `assertHeatRiskBlockIntact`
      and `assertPayloadIntact` in full, to see what the parity scenario may safely assume
    - `.planning/phases/18-merge-precedence-unified-payload-schema/18-RESEARCH.md` §"Common Pitfalls"
      -> Pitfall 9, the paragraph asking for the legacy-versus-unified cross-check
    - `.planning/STATE.md` Deferred Items — the Phase 15 MPD-01 and Phase 16 no-risk-gate rows
    - `.planning/phases/18-merge-precedence-unified-payload-schema/18-CONTEXT.md` D-01, D-07, D-16
  </read_first>

  <files>scripts/probe-payload-resilience.js</files>

  <action>
Append these scenarios to the same array.

`merge-unmapped-label-passes-through-and-is-recorded`
  A `wpc-hazards` Precipitation feature carrying a label absent from the taxonomy (for example
  `Volcanic Ash`). Precondition: assert the label reached the legacy `hazardsOutlook` day block,
  proving it was not filtered upstream. Primary: a `days[N].hazards` entry with `dimension: null`,
  `label` and `text` both the verbatim string, `suppressedBy: null`; and the label present in
  `sources['wpc-hazards'].unmappedLabels`. Control: on the same day add an above-floor SPC convective
  tier and assert the unmapped entry is STILL `suppressedBy: null` — D-07's "never suppresses and is
  never suppressed" is a two-sided claim and a one-sided assertion would half-prove it.

`merge-unmapped-label-counts-toward-anyhazard`
  The same fixture with every other product quiet. Precondition: assert `summary.dimensions` is empty
  — an unmapped label contributes no dimension — so the verdict cannot be explained by a mapped
  hazard sneaking in. Primary: `summary.anyHazard` is `true`. This is D-07's "counts toward
  summary.anyHazard", and it is the shape that would otherwise render a real hazard as an all-clear.

`merge-summary-band-only-is-not-an-all-clear`
  Every day-scoped product quiet, one `hazardsOutlook.windowBand` entry present, no advisories.
  Precondition: assert every `days[N].hazards` array is empty, so the verdict is attributable to the
  band term and nothing else — the isolation guard idiom from line 4903. Primary:
  `summary.anyHazard` is `true` and `summary.bandDiagnostics.windowBandCount` is 1 (D-20 nests the
  two counters; nothing may assert on a flat `summary.windowBandCount`). Control: with the band entry
  removed, `summary.anyHazard` is `false`, proving the gate is not stuck true.
  This closes, at the payload level, the Phase 16 deferred item "live Hazards-Outlook no-risk-gate
  behaviour" that has never been exercised.

`merge-summary-advisory-only-is-not-an-all-clear`
  Every day-scoped product quiet, one `advisories.spcMD` entry present, empty window band.
  Precondition and control mirror the band scenario. Primary: `summary.anyHazard` is `true` and
  `summary.bandDiagnostics.advisoryCount` is 1. This is the backend counterpart of the live Phase 15
  defect where the `getDom` no-risk gate made MPD invisible.

`merge-sources-reporting-true-for-wave2-sources`
  The positive-case assertion whose absence let a real wiring gap survive plan review: `heatrisk` and
  `wpc-hazards` populate `reportedDays`/`activeDays` in wave 2 (plan 18-03) while the other four
  sources populate them in wave 3 (plan 18-04), so nothing else in this suite proves the wave-2 half
  is wired at all.
  Fixture: every toggle on, a HeatRisk category 2 landing on grid day 3, and a `wpc-hazards`
  `High Winds` feature landing on the same grid day.
  Precondition: assert both entries are present in `days["3"].hazards`, so a false `reporting` cannot
  be explained by the fixture producing no data.
  Primary: `sources['heatrisk'].reporting` and `sources['wpc-hazards'].reporting` are both `true`;
  `sources['heatrisk'].reportedDays` and `sources['wpc-hazards'].reportedDays` both contain 3; and
  `summary.reportingSourceCount` equals the count of `sources[id].reporting === true`, computed
  independently in the scenario rather than read from the payload.
  Control: with both toggles off, the same two `reporting` values are `false` and
  `reportingSourceCount` drops by exactly two — proving the field tracks real reporting rather than
  being hardcoded either way.

`merge-sources-heatrisk-absent-day-is-not-the-floor-path`
  The `heatrisk` counterpart to `merge-precedence-spc-absent-day-is-not-the-floor-path`, closing the
  asymmetry that hid the gap: `heatrisk` is rank 1 for the `heat` dimension and stops at day 7, so
  days 8-14 are the one non-SPC place where "rank 1 never covered this day" decides an outcome.
  Fixture: a `wpc-hazards` `Excessive Heat` feature on a grid day of 8 or later, HeatRisk reporting
  normally on days 1-7.
  Precondition: assert `sources['heatrisk'].reportedDays` is non-empty but does NOT contain that grid
  day — proving this is the absent path and not a below-floor reading, and proving the accumulator is
  populated at all rather than empty for an unrelated reason.
  Primary: the `wpc-hazards` heat entry on that day carries `suppressedBy: null`.
  Control: on a day 1-7 where HeatRisk reports category 2, the same label IS suppressed with
  `suppressedBy: "heatrisk"` — the two branches, side by side, in one scenario.

`merge-summary-all-quiet-is-an-all-clear`
  Every product enabled and reporting, nothing active anywhere, no band, no advisories.
  Precondition: assert `summary.reportingSourceCount` is greater than 0, proving this is genuinely
  all-quiet rather than all-failed — the exact three-way distinction D-16 says Phase 19 needs
  different copy for. Primary: `summary.anyHazard` is `false`, `summary.dimensions` and
  `summary.activeDays` are both empty. Control: `summary.enabledSourceCount` is greater than 0,
  proving this is not all-disabled either.

`merge-parity-unified-days-agree-with-legacy-blocks`
  RESEARCH.md's Pitfall 9 cross-check, constructible only while both representations coexist.
  Build one poll with several products active across several days. For each grid day N, derive the
  expected legacy presence and compare:
    - `days[N]` contains an above-floor `spc-convective` entry if and only if the legacy `dayN.risk`
      is above the categorical floor (days 1-3) or the probabilistic floor (days 4-8);
    - `days[N]` contains a `wpc-ero` entry if and only if `excessiveRain.dayNRisk` is not `"NONE"`,
      for N in 1..5;
    - `days[N]` contains a `wpc-wssi` entry if and only if `winterImpact.dayNRisk` is not `"NONE"`,
      for N in 1..3;
    - `days[N]` contains a `spc-fire` entry if and only if `fireWeather.dayNRisk` is greater than 0.
  Do NOT extend this comparison to `wpc-hazards` or `heatrisk`: D-09 deliberately re-anchors those
  two onto the SPC grid, so their day keys are expected to disagree with the legacy block near a
  boundary, and asserting agreement would encode the bug this phase exists to fix. Comment that
  exclusion and its reason directly in the scenario.
  Precondition: assert at least three of the four compared sources produced at least one above-floor
  reading in this fixture — a parity check over an all-quiet poll is vacuous, which is Phase 15's
  first vacuity mechanism exactly.
  Primary: every comparison agrees. Control: the failure message must name the grid day, the source,
  the legacy value and the unified presence, so a mismatch is diagnosable without a debugger.
  </action>

  <verify>
    <automated>node scripts/probe-payload-resilience.js | tail -3</automated>
  </verify>

  <acceptance_criteria>
    - `node scripts/probe-payload-resilience.js` -> `PROBE RESULT: 114 passed, 0 failed, 0 skipped`,
      exit 0 (106 entering + 8 new).
    - `node scripts/probe-payload-resilience.js | grep -cE '^PASS merge-(unmapped|summary|parity|sources)-'` is 8.
    - No scenario asserts on a flat counter path:
      `grep -nE "summary\.(windowBandCount|advisoryCount)" scripts/probe-payload-resilience.js`
      produces no output — D-20 nests both under `summary.bandDiagnostics`.
    - The parity scenario explicitly excludes `wpc-hazards` and `heatrisk` with a comment naming D-09:
      `awk '/name: "merge-parity-/,/^  },/' scripts/probe-payload-resilience.js | grep -c 'D-09'`
      is at least 1.
    - The parity scenario's precondition guard asserts a non-vacuous fixture:
      `awk '/name: "merge-parity-/,/^  },/' scripts/probe-payload-resilience.js | grep -c 'precondition failed:'`
      is at least 1.
    - Every new scenario contains `precondition failed:` and `control:` strings.
  </acceptance_criteria>

  <done>D-07 pass-through, the three empty-state causes, the wave-2 sources' reporting state, the heatrisk absent-day path, and the legacy-versus-unified parity check are all pinned by executable scenarios.</done>
</task>

<task type="auto">
  <name>Task 3: Mutation-prove every scenario added in Tasks 1 and 2</name>

  <read_first>
    - `.planning/phases/18-merge-precedence-unified-payload-schema/18-07-SUMMARY.md` — the mutation
      table format established by plan 18-07
    - `.planning/STATE.md` — the Phase 15 D-10 decision text and both vacuity mechanisms
    - `node_helper.js` — `_resolveGridDayPrecedence`, `_buildGridSummary`, `_buildSourceHealth`, and
      the entry-creation floor tests in `_addSpcGridEntries` / `_addHeatRiskGridEntries` /
      `_addRegistryDayGridEntries`
    - `hazardTaxonomy.js` — `PRECEDENCE`, `NO_RISK_FLOOR`, `DIMENSION_ORDER`
  </read_first>

  <files>scripts/probe-payload-resilience.js</files>

  <action>
Break the exact line each of the sixteen new scenarios covers, one at a time, confirm THAT scenario
goes RED with a diagnosable message, and restore. Record every result.

Suggested mutations, one per scenario:
  - spc-suppresses-wpc: reverse `PRECEDENCE.convective`.
  - spc-below-floor-does-not-suppress: change `NO_RISK_FLOOR['spc-convective'].categorical` to
    `value > 0`, so `TSTM` becomes active.
  - spc-absent-is-not-the-floor-path: make `_addSpcGridEntries` record `reportedDays` for all
    fourteen grid days regardless of coverage.
  - heatrisk-suppresses-wpc-heat: reverse `PRECEDENCE.heat`.
  - heatrisk-zero-does-not-suppress: change `NO_RISK_FLOOR.heatrisk` to `category !== null`.
  - heatrisk-null-does-not-suppress: change `NO_RISK_FLOOR.heatrisk` to `category !== undefined`.
  - flash-flood-and-heavy-precip-never-cross-suppress: map `Heavy Rain` to `flash-flood` in
    `HAZARD_TAXONOMY`. This is the single highest-value mutation for MERGE-04 — it is Pitfall 10's
    over-merge, applied deliberately.
  - distinct-hazards-both-survive: make `_resolveGridDayPrecedence` suppress every entry after the
    first, ignoring dimension.
  - unmapped-passes-through-and-is-recorded: make `_addHazardsOutlookGridEntries` drop entries whose
    dimension is `null`.
  - unmapped-counts-toward-anyhazard: make `_buildGridSummary` require a non-null dimension for
    `anyHazard`.
  - summary-band-only: drop the window-band term from `anyHazard`'s union.
  - summary-advisory-only: drop the advisories term from `anyHazard`'s union.
  - summary-all-quiet: make `anyHazard` unconditionally `true`.
  - parity-unified-days-agree-with-legacy: make `_addRegistryDayGridEntries` skip ERO day 1.
  - sources-reporting-true-for-wave2-sources: delete the `notes.noteReported("heatrisk", gridDay)`
    call in `_addHeatRiskGridEntries`. This is the mutation that reproduces the exact wiring gap the
    plan-checker found — a green result here would mean the fix is unverified.
  - sources-heatrisk-absent-day-is-not-the-floor-path: make `_addHeatRiskGridEntries` call
    `notes.noteReported("heatrisk", d)` for all fourteen grid days regardless of coverage, so the
    absent path becomes indistinguishable from the below-floor path.

For each, record in the SUMMARY: the file and line mutated, a one-line description, the verbatim
`FAIL <name>: <message>` the runner printed, and confirmation the suite returned to
`114 passed, 0 failed, 0 skipped` after restoring. Note any mutation that reddened MORE scenarios
than its target — that is useful signal, not a defect. A mutation that reddened NONE is a blocking
fixture defect: strengthen the fixture, then re-prove.

Confirm at task end that `git diff node_helper.js hazardTaxonomy.js` is empty.
  </action>

  <verify>
    <automated>node scripts/probe-payload-resilience.js | tail -2 && git diff --stat node_helper.js hazardTaxonomy.js | tail -1</automated>
  </verify>

  <acceptance_criteria>
    - The SUMMARY contains a sixteen-row mutation table with columns: scenario, file:line mutated,
      mutation description, verbatim `FAIL` message, restored (yes/no).
    - Every row has a non-empty verbatim `FAIL` message naming the day, source, dimension or field
      involved. A row reporting "no scenario failed" is blocking and must be resolved before the plan
      is complete.
    - `git diff node_helper.js hazardTaxonomy.js` is empty at task end.
    - `node scripts/probe-payload-resilience.js` -> `PROBE RESULT: 114 passed, 0 failed, 0 skipped`,
      exit 0.
  </acceptance_criteria>

  <done>All sixteen scenarios are individually mutation-proven, with verbatim failure messages recorded and every mutation reverted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Probe fixtures -> the merge and precedence code paths | The suite feeds unmapped, hostile and boundary-case labels through the real resolver. |
| Probe suite result -> the phase's MERGE-02/03/04 verification claim | A green run is cited as evidence that suppression is dimension-scoped and that no hazard is lost or duplicated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-21 | Repudiation | a suppression scenario that passes while proving nothing | mitigate | Every scenario carries a precondition guard proving BOTH competing entries reached the day, plus a control assertion proving the suppression gate is not globally inert; each is individually mutation-proven with a verbatim `FAIL` message recorded. Both Phase 15 vacuity mechanisms are addressed: an assertion that observes nothing, and a fixture that cannot express its own condition. |
| T-18-13 | Spoofing | suppression drifting to a label-string match | mitigate | `merge-precedence-spc-suppresses-wpc-severe-weather` is mutation-proven against a reversed `PRECEDENCE.convective`, and an acceptance grep forbids `suppressedBy` assertions built on `includes`/`indexOf`. MERGE-02's requirement text names label-string matching as the failure mode. |
| T-18-23 | Tampering | an over-merge collapsing two distinct hazards | mitigate | `merge-flash-flood-and-heavy-precip-never-cross-suppress` is mutation-proven by deliberately mapping `Heavy Rain` into `flash-flood` — Pitfall 10's exact trap, executed on purpose so the scenario is known to catch it. |
| T-18-24 | Repudiation | a false all-clear rendering over a real hazard | mitigate | Three `merge-summary-*` scenarios pin `anyHazard`'s union over days, band and advisories, each with an isolation precondition proving which term produced the verdict. These are the backend counterparts of two live defects already found in this project. |
| T-18-22 | Tampering | a mutation left in the working tree | mitigate | `git diff node_helper.js hazardTaxonomy.js` must be empty at task end, asserted in the acceptance criteria and re-checked by the verify command. |
| T-18-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install tasks in this phase; RESEARCH.md confirms zero new dependencies. |
</threat_model>

<verification>
- `node scripts/probe-payload-resilience.js` -> `PROBE RESULT: 114 passed, 0 failed, 0 skipped`, exit 0.
- The sixteen-row mutation table in the SUMMARY, each row carrying a verbatim `FAIL` message.
- `git diff node_helper.js hazardTaxonomy.js` empty; `git diff --stat` shows only
  `scripts/probe-payload-resilience.js`.
</verification>

<success_criteria>
- MERGE-02 is pinned including its two structurally distinct rank-1-absent and rank-1-below-floor
  paths, with suppression keyed off the dimension and never off a label string.
- MERGE-03 is pinned with HeatRisk `0` and `null` as two separate scenarios.
- MERGE-04 is pinned on both halves: no over-merge (flash-flood versus heavy-precip) and no
  under-merge (three distinct hazards on one day all survive).
- RPT-07's empty-state verdict is pinned for all three causes, including band-only and advisory-only.
- The legacy-versus-unified parity cross-check exists while both representations still do.
- `sources[].reporting` is asserted TRUE for the two wave-2 sources on a day each actively reports,
  and the `heatrisk` absent-day path has the same matched-pair coverage `spc-convective` already has.
- Nothing asserts on a flat `summary.windowBandCount`/`advisoryCount` — D-20 nests both.
- All sixteen scenarios are individually mutation-proven.
</success_criteria>

<output>
Create `.planning/phases/18-merge-precedence-unified-payload-schema/18-08-SUMMARY.md` when done.
The sixteen-row mutation table is the deliverable — record it in full.
</output>
