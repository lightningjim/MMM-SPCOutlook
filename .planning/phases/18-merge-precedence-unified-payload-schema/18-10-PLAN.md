---
phase: 18-merge-precedence-unified-payload-schema
plan: 10
type: execute
wave: 8
gap_closure: true
depends_on: ["18-08"]
files_modified:
  - node_helper.js
  - scripts/probe-payload-resilience.js
autonomous: true
requirements: [MERGE-01]

must_haves:
  truths:
    - "A live-shaped wpc-hazards Precipitation feature carrying start_date === end_date lands on the unified days[] grid day whose window starts at 12Z on its own calendar date, instead of being silently dropped"
    - "A wpc-hazards Precipitation feature carrying the exclusive-end convention (end_date = start_date + 86400000) still lands on exactly the same single grid day it landed on before this plan — no regression on the convention the existing fixtures use"
    - "A multi-day exclusive-end span still ends on its last covered grid day and does not gain a spurious trailing day"
    - "The emission loop remains bounded by its header clamp, so a hostile or absurd end_date cannot produce unbounded iteration"
    - "Each new probe scenario is individually mutation-proven: the exact expression it covers is broken, the run goes RED with a diagnosable message, and the expression is restored"
  artifacts:
    - path: "node_helper.js"
      provides: "_addHazardsOutlookGridEntries emission bound that tolerates both the inclusive/zero-duration and exclusive end_date conventions"
      contains: "Math.max(gridStart, gridEnd - 1)"
    - path: "scripts/probe-payload-resilience.js"
      provides: "two new merge-grid-hazards-* scenarios built on the live-observed start_date === end_date shape and its exclusive-convention counterpart"
      contains: "merge-grid-hazards-start-equals-end"
  key_links:
    - from: "scripts/probe-payload-resilience.js new scenarios"
      to: "node_helper.js _addHazardsOutlookGridEntries lastGridDay expression"
      via: "getSpcOutlook driven through installHttp with a hazards layer-4 fixture"
      pattern: "merge-grid-hazards-start-equals-end"
    - from: "node_helper.js lastGridDay"
      to: "the loop header clamp"
      via: "Math.min(lastGridDay, GRID_DAY_COUNT)"
      pattern: "Math\\.min\\(lastGridDay, GRID_DAY_COUNT\\)"
---

<objective>
Close the blocking MERGE-01 correctness defect the 18-09 live capture found: a real
`wpc-hazards` Precipitation feature whose `start_date` and `end_date` are the identical epoch
instant is silently dropped from every unified `days[]` grid day, while the legacy
`hazardsOutlook` block places it correctly.

Purpose: ROADMAP Phase 18 success criterion 1 currently reads FAIL. The upstream feed does not
honour a single endpoint convention — the same 2026-09-05 poll returned both an inclusive
(zero-duration) Precipitation feature and an exclusive-end Temperature feature — so the fix must
carry both conventions, not switch wholesale to inclusive. The existing probe suite cannot catch
this: every hazards fixture in it, including `merge-parity-unified-days-agree-with-legacy-blocks`,
was built on the exclusive convention, so the suite is green today despite the defect.

Output: a corrected emission bound in `_addHazardsOutlookGridEntries`, two new probe scenarios
built from the live-captured feature values, and a recorded mutation table proving each scenario
is not vacuous.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/buildomator/bm/4.5.6/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/buildomator/bm/4.5.6/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-merge-precedence-unified-payload-schema/18-CONTEXT.md
@.planning/phases/18-merge-precedence-unified-payload-schema/18-LIVE-CAPTURE.md
@.planning/phases/18-merge-precedence-unified-payload-schema/deferred-items.md
@.planning/phases/18-merge-precedence-unified-payload-schema/18-09-SUMMARY.md
@.planning/phases/18-merge-precedence-unified-payload-schema/18-04-SUMMARY.md
@.planning/phases/18-merge-precedence-unified-payload-schema/18-07-SUMMARY.md
</context>

<interfaces>
<!-- Contracts the executor needs. Use directly; do not go exploring for them. -->

node_helper.js:
  MS_PER_DAY                                   line 146, `24 * 60 * 60 * 1000`
  GRID_DAY_COUNT                               line 152, `14`
  _hazardDayOffset(epochMs, todayUtcMs)        line 2505, `Math.round((epochMs - todayUtcMs) / MS_PER_DAY)`
  _gridDayOf(epochMs, nominalStartMs)          line 2571, `this._hazardDayOffset(epochMs, nominalStartMs) + 1`, 1-based, unclamped
  _spcGridAnchor(validIso, expireIso)          line 2532, returns { nominalStartMs, day1StartMs, day1EndMs, anchor }
  _isFullNominalWindow(oS, oE, dayRange)       line 2726, `oS === dayRange[0] && oE === dayRange[1]`
  _bucketHazardMatch(...)                      line 2754, legacy path; its day loop is
                                               `for (let d = Math.max(offsetStart, firstDay); d <= Math.min(offsetEnd, lastDay); d++)`
                                               — INCLUSIVE on both endpoints
  _addHazardsOutlookGridEntries(gridDays, gridMatches, anchorInfo, notes)
                                               line 2835; the defect is at line 2880

scripts/probe-payload-resilience.js:
  const scenarios = [                          line 1283; the SINGLE registration array — APPEND, never create a second
  scenario shape                               { name: string, run: async (helper) => void }
  resetHelper(helper) / resetLogs()            first two calls of every run/re-run closure
  installHttp(helper, routes)                  drives the real fetchGeoJsonCached through the _fetch seam
  hazardsRoutes(overrides)                     line 970; overrides keyed by layer id (1,3,4,6,7,8)
  hazardsFeature({label,startDate,endDate,filedate})  line 996; filedate defaults to startDate
  hazardsCollection(features)                  line 1009
  httpResponse({ body, etag })                 the route return shape
  assertPayloadIntact(out)                     legacy payload-contract guard
  assertHazardsBlockIntact(out)                legacy hazardsOutlook guard
  turfStub.pointInPolygon                      hand-mutable; restore in a `finally`
  helper._nowMs = () => nowMs                  the pinned-clock idiom
  helper._products = { showHazardsOutlook: true }
  PROBE_LAT / PROBE_LON                        38.9 / -77.0
  SAMPLE_RING                                  the polygon ring every fixture reuses
  runner                                       `node scripts/probe-payload-resilience.js`; prints
                                               `PASS <name>` / `FAIL <name>: <msg>` and a final
                                               `PROBE RESULT: N passed, M failed, K skipped`; exits
                                               non-zero on any failure OR any skip

Baseline entering this plan (verified 2026-09-05): `PROBE RESULT: 116 passed, 0 failed, 0 skipped`.

Live-captured values this plan is built from (18-LIVE-CAPTURE.md, Capture 2, 2026-09-05T21:41Z):
  coordinates                65.936, -163.443 (Kotzebue, AK polygon centroid)
  raw feature                objectid 7917, label "Heavy Rain", idp_source "Prcp_D3_7_Clip",
                             idp_subset "PRCP_D3_7", idp_filedate 1788639319000
  start_date === end_date    1788998400000  === 2026-09-10T00:00:00.000Z
  SPC anchor                 "estimated" (coordinate outside every SPC day-1 polygon)
  nominalStartMs             2026-09-05T12:00:00.000Z
  expected grid day          6  (date 2026-09-10, windowStart 2026-09-10T12:00:00.000Z)
  legacy placement           hazardsOutlook.day5, date 2026-09-10, label "Heavy Rain"
  taxonomy                   dimension "heavy-precip", color "267300", mapped true
  observed exclusive control same poll's Temperature-group "High Winds",
                             start 2026-09-08T00:00:00Z / end 2026-09-09T00:00:00Z
                             (Temperature routes to the window band per D-04, so the exclusive
                             control in the probe MUST be a Precipitation-group feature)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add the two live-shaped scenarios and record them RED against the unfixed code</name>

  <read_first>
    - `scripts/probe-payload-resilience.js` lines 7960-8090 verbatim — the analog
      `merge-grid-hazards-00z-feature-forward-aligns-to-next-grid-day`, which supplies the exact
      idiom to reproduce: a `day1Fixture` closure, a pinned `NOW_MS`, a `runWithFeature` re-run
      closure that calls `resetHelper`/`resetLogs` and re-installs routes per run, a precondition
      guard that throws with a `precondition failed:` prefix, the primary assertion, a `control:`
      assertion, and a `finally` restoring `turfStub.pointInPolygon`
    - `scripts/probe-payload-resilience.js` lines 960-1012 — `hazardsRoutes`, `hazardsFeature`,
      `hazardsCollection` and their header comments
    - `scripts/probe-payload-resilience.js` line 1283 (`const scenarios = [`) and lines 9371-9490
      (`merge-grid-parity...` through the array close) — the append point
    - `node_helper.js` lines 2835-2919 — the function under test, in full
    - `node_helper.js` lines 2495-2580 — `_hazardDayOffset`, `_spcGridAnchor` (both branches),
      `_gridDayOf`
    - `.planning/phases/18-merge-precedence-unified-payload-schema/18-LIVE-CAPTURE.md` lines
      100-215 — Capture 2's raw feature, the legacy-block placement, and the traced arithmetic
    - `.planning/STATE.md` — the D-10 mutation-proof decision text and both mechanisms by which a
      scenario can pass while proving nothing
  </read_first>

  <files>scripts/probe-payload-resilience.js</files>

  <action>
Append two scenarios to the existing `scenarios` array in `scripts/probe-payload-resilience.js`,
immediately after `merge-parity-unified-days-agree-with-legacy-blocks`. Do NOT touch
`node_helper.js` in this task — the point of this task is to observe the live defect reproduce
against the currently-committed code, which is the strongest possible proof that the scenarios are
not vacuous.

Both scenarios pin `helper._nowMs` to `Date.UTC(2026, 8, 5, 21, 41)` so `_spcGridAnchor`'s clock
fallback (`getUTCHours() >= 12`) resolves `nominalStartMs` to `2026-09-05T12:00:00.000Z` — the
exact anchor the live capture recorded. Do not route a real SPC day-1 `VALID_ISO`/`EXPIRE_ISO`
fixture: the live capture's anchor was `"estimated"`, and reproducing that branch is part of
replaying the captured conditions faithfully. Set `helper._products = { showHazardsOutlook: true }`
and drive `helper.getSpcOutlook(PROBE_LAT, PROBE_LON, false, { showHazardsOutlook: true })` with
`installHttp(helper, hazardsRoutes({ 4: layer4Fixture }))` and
`turfStub.pointInPolygon = () => true`, restored in a `finally`. Every run asserts
`assertPayloadIntact(out)` and `assertHazardsBlockIntact(out)` so a D-01 regression turns the same
scenario red.

Scenario A — `merge-grid-hazards-start-equals-end-live-shape-lands-on-its-own-grid-day`

  Header comment must record, verbatim enough to be traceable: this fixture is the live feature
  captured 2026-09-05 at 65.936,-163.443 — objectid 7917, label "Heavy Rain",
  `start_date === end_date === 1788998400000` (`2026-09-10T00:00:00.000Z`), `idp_filedate`
  1788639319000, layer 4 (`Prcp_D3_7_Clip` / `PRCP_D3_7`) — and that this zero-duration/inclusive
  shape is the one every pre-existing hazards fixture in this file failed to model. Also record the
  mutation that must prove it RED (Task 3's mutation M1).

  Primary run: one `hazardsFeature({ label: "Heavy Rain", startDate: 1788998400000,
  endDate: 1788998400000, filedate: 1788639319000 })` on layer 4.

  Precondition guard: scan `out.hazardsOutlook.day3` through `day14` and throw with a
  `precondition failed:` prefixed message naming the searched range if `"Heavy Rain"` reached none
  of them — so a missing unified grid entry can never be blamed on the feature being filtered out
  upstream of the grid-entry builder. Additionally assert `out.hazardsOutlook.day5.hazards` carries
  the `"Heavy Rain"` label and that `out.hazardsOutlook.day5.date` is `"2026-09-10"`, matching the
  live capture's recorded legacy placement exactly.

  Primary assertions:
    - `out.days["6"].hazards` contains exactly one entry with `source === "wpc-hazards"` and
      `label === "Heavy Rain"`, and that entry's `dimension` is `"heavy-precip"` and `color` is
      `"267300"`.
    - `out.days["6"].date` is `"2026-09-10"` and `out.days["6"].windowStart` is
      `"2026-09-10T12:00:00.000Z"` — proving the entry landed on the grid day D-10's forward-align
      rule names, not merely on some non-empty day.
    - `out.days["5"].hazards` and `out.days["7"].hazards` carry no `wpc-hazards` `"Heavy Rain"`
      entry, so the placement is a point, not a smear.
    - `out.sources["wpc-hazards"].reportedDays` and `.activeDays` both include 6, and
      `out.sources["wpc-hazards"].reporting` is `true` — the live capture recorded all three as
      empty/false, so these assertions pin the source-health half of the same defect.
  Every failure message must name the grid day, the source and the label involved, and must
  interpolate `JSON.stringify` of the day's `hazards` array so a wrong answer is diagnosable
  without a debugger.

  Control run (a second invocation of the re-run closure, same shape, fresh `resetHelper`): the
  same calendar day expressed under the EXCLUSIVE convention —
  `hazardsFeature({ label: "Heavy Rain", startDate: Date.UTC(2026, 8, 10),
  endDate: Date.UTC(2026, 8, 11) })`. Assert with a `control:` prefixed message that this feature
  ALSO lands on `days["6"]` and only on `days["6"]`. This is the constraint the gap report locks:
  the fix must carry both conventions, and a control that only exercises the new inclusive shape
  could not catch a regression that breaks the exclusive path. Run the two features in SEPARATE
  invocations, never in one poll — `_addHazardsOutlookGridEntries` dedupes by
  `(source, label)` within a grid day, so a single combined poll would collapse them and make the
  control vacuous.

Scenario B — `merge-grid-hazards-multi-day-exclusive-span-still-ends-on-its-last-covered-day`

  Header comment must state that this scenario is the regression guard for the OTHER direction:
  it exists so that a fix which switches wholesale to inclusive-end semantics cannot pass, and it
  records Task 3's mutation M2 as the mutation that must prove it RED.

  Single run: one `hazardsFeature({ label: "Heavy Rain", startDate: Date.UTC(2026, 8, 10),
  endDate: Date.UTC(2026, 8, 12) })` on layer 4 — a two-day span under the exclusive convention,
  covering calendar days Sep 10 and Sep 11.

  Precondition guard: same legacy-reach guard as Scenario A, `precondition failed:` prefixed.

  Primary assertions: `days["6"]` and `days["7"]` each carry exactly one `wpc-hazards`
  `"Heavy Rain"` entry; `days["5"]` and `days["8"]` carry none. The `days["8"]` assertion is the
  load-bearing one — under an always-inclusive bound it becomes populated and the scenario goes
  RED. Message must name the specific offending grid day.

  Control assertion (`control:` prefixed): assert `days["6"].date` is `"2026-09-10"` and
  `days["7"].date` is `"2026-09-11"`, proving the two populated days are the two calendar days the
  span actually covers rather than an arbitrary adjacent pair.

Do not modify any existing scenario. Do not create a second `scenarios` array.

Expected outcome of this task, and the thing to record: Scenario A goes RED against the
currently-committed `node_helper.js` (this reproduces the live defect deterministically);
Scenario B goes GREEN against the currently-committed code (the exclusive path already works).
Capture both verbatim `PASS`/`FAIL` lines. If Scenario A comes out GREEN, the fixture does not
reproduce the live defect and is therefore wrong — fix the fixture before proceeding; do not move
on to Task 2 with a green Scenario A.
  </action>

  <verify>
    <automated>node scripts/probe-payload-resilience.js 2>&1 | grep -E '^(PASS|FAIL) merge-grid-hazards-|^PROBE RESULT'</automated>
  </verify>

  <acceptance_criteria>
    - The runner prints a line beginning `FAIL merge-grid-hazards-start-equals-end-live-shape-lands-on-its-own-grid-day:` and the message names grid day 6, the source `wpc-hazards` and the label `Heavy Rain`. The full line is recorded verbatim in the SUMMARY.
    - The runner prints `PASS merge-grid-hazards-multi-day-exclusive-span-still-ends-on-its-last-covered-day`.
    - The runner prints `PROBE RESULT: 117 passed, 1 failed, 0 skipped` and exits non-zero. This RED state is the deliverable of this task, not a defect in it.
    - `git diff --stat node_helper.js` is empty — this task changed no production code.
    - `grep -c 'const scenarios = \[' scripts/probe-payload-resilience.js` is 1.
    - `grep -c '1788998400000' scripts/probe-payload-resilience.js` is at least 2 — the live epoch value is used literally for both `start_date` and `end_date`, not recomputed from a `Date.UTC` expression that could drift from the captured value.
    - Both new scenarios contain a `precondition failed:` line and a `control:` line: `awk '/name: "merge-grid-hazards-start-equals-end/,/^  },/' scripts/probe-payload-resilience.js | grep -c 'precondition failed:'` is at least 1, and the same for `control:`; repeat for `merge-grid-hazards-multi-day-exclusive`.
    - Both new scenarios call `assertPayloadIntact` and `assertHazardsBlockIntact` at least once.
  </acceptance_criteria>

  <done>Two new scenarios exist; Scenario A reproduces the live MERGE-01 defect as a RED with a diagnosable message; Scenario B is green; `node_helper.js` is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the emission bound in _addHazardsOutlookGridEntries</name>

  <read_first>
    - `node_helper.js` lines 2835-2919 — the full function, including the containment guard at
      2843-2847, the legacy-offset routing decision at 2855-2866, the `_gridDayOf` calls at
      2871-2872, the comment block at 2874-2879, the `lastGridDay`/clamp block at 2880-2883, and
      the emission loop at 2909 onward
    - `node_helper.js` lines 2754-2818 — `_bucketHazardMatch`, whose day loop
      `for (let d = Math.max(offsetStart, firstDay); d <= Math.min(offsetEnd, lastDay); d++)` is
      inclusive on both endpoints and is the semantics the fix mirrors; also its T-16-05 comment
      explaining why the clamp lives at the loop header and not in the body
    - `node_helper.js` lines 2495-2580 — `_hazardDayOffset` (`Math.round` division) and
      `_gridDayOf`, so the rounding behaviour of a sub-day span is understood before changing the
      bound
    - `.planning/phases/18-merge-precedence-unified-payload-schema/deferred-items.md` — the
      recorded suggested fix, which this task deliberately supersedes (see the action)
    - `.planning/phases/18-merge-precedence-unified-payload-schema/18-LIVE-CAPTURE.md` lines
      174-215 — the traced arithmetic and the substituted-value control
  </read_first>

  <files>node_helper.js</files>

  <action>
Change exactly one expression in `_addHazardsOutlookGridEntries`, and rewrite the comment block
above it.

Current (node_helper.js:2880):  `const lastGridDay = gridEnd - 1;`
Target:                         `const lastGridDay = Math.max(gridStart, gridEnd - 1);`

This is the concrete target state. Nothing else in the function changes: `gridStart` and `gridEnd`
keep their existing `_gridDayOf(..., anchorInfo.nominalStartMs)` derivations (D-10 — never
`day1StartMs`); the `Math.max(gridStart, 1)` / `Math.min(lastGridDay, GRID_DAY_COUNT)` clamp pair
and the `if (clampedEnd < clampedStart) continue;` line stay exactly where they are, at the loop
header, unmoved and unweakened.

Why `Math.max(gridStart, gridEnd - 1)` and not `deferred-items.md`'s narrower suggestion
(`when match.endDate === match.startDate, add 1 to gridEnd before subtracting 1`): the narrower
form keys on exact epoch equality and therefore still drops any sub-day span — e.g. a feature
running `2026-09-10T00:00:00Z` to `2026-09-10T18:00:00Z`, for which `_gridDayOf` rounds both
endpoints to the same grid day and `gridEnd - 1` again produces an empty range. `Math.max` is the
general statement of the same intent — "a span that begins and ends inside one grid day emits on
that one grid day" — and it collapses to identical behaviour on every case the existing exclusive
fixtures cover. Verify this equivalence explicitly rather than assuming it, against this poll's
live anchor `nominalStartMs = 2026-09-05T12:00:00.000Z`:
  - live inclusive shape, start = end = 1788998400000: gridStart 6, gridEnd 6 -> max(6, 5) = 6 ->
    emits on grid day 6 only. Was: lastGridDay 5, clamped range empty, entry dropped.
  - exclusive single day, start Sep 10 / end Sep 11: gridStart 6, gridEnd 7 -> max(6, 6) = 6 ->
    grid day 6 only. Unchanged from today.
  - exclusive two-day, start Sep 10 / end Sep 12: gridStart 6, gridEnd 8 -> max(6, 7) = 7 ->
    grid days 6 and 7. Unchanged from today.
  - the existing `merge-grid-hazards-00z-feature-forward-aligns-to-next-grid-day` fixture,
    start Sep 6 / end Sep 7 against nominal `2026-09-05T12:00:00Z`: gridStart 2, gridEnd 3 ->
    max(2, 2) = 2 -> grid day 2 only. Unchanged; that scenario must stay green.

Replace the comment block currently at lines 2874-2879 (the one asserting "`endDate` is the
exclusive end of the source's own 00Z-00Z span") with a block that states the corrected fact: the
upstream feed is NOT internally consistent about this endpoint convention, evidenced by the
2026-09-05 poll returning both a Precipitation-group feature (objectid 7917, "Heavy Rain",
`start_date === end_date === 1788998400000`) with the inclusive/zero-duration shape AND a
Temperature-group feature ("High Winds") with `end_date = start_date + 86400000`. State that
`Math.max(gridStart, gridEnd - 1)` reads correctly under both, that this mirrors
`_bucketHazardMatch`'s own inclusive-endpoint loop, that the clamp stays at the loop header for
T-16-05/T-18-03 reasons so an absurd `end_date` cannot iterate unbounded, and cite
`18-LIVE-CAPTURE.md`'s Criterion 1 section as the trace. Do not delete the D-10 nominal-anchor
comment at 2868-2870.

Then run the full suite. Scenario A from Task 1 must flip RED to GREEN. Every one of the 116
pre-existing scenarios must stay green — in particular
`merge-grid-hazards-00z-feature-forward-aligns-to-next-grid-day`,
`merge-parity-unified-days-agree-with-legacy-blocks`, and every other `hazards-*` scenario built on
the exclusive convention. If any pre-existing scenario turns red, stop and report it rather than
adjusting the scenario to accommodate the change.
  </action>

  <verify>
    <automated>node scripts/probe-payload-resilience.js 2>&1 | tail -1 && git diff --stat node_helper.js</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'Math.max(gridStart, gridEnd - 1)' node_helper.js` is 1.
    - `grep -c 'const lastGridDay = gridEnd - 1;' node_helper.js` is 0.
    - `sed -n '2835,2925p' node_helper.js | grep -c 'Math.min(lastGridDay, GRID_DAY_COUNT)'` is 1 — the header clamp survived the edit.
    - `sed -n '2835,2925p' node_helper.js | grep -c 'anchorInfo.day1StartMs'` is 0 — D-10's nominal anchor was not disturbed.
    - `sed -n '2835,2925p' node_helper.js | grep -v '^\s*//' | grep -c 'exclusive end'` is 0 — the superseded assertion no longer appears in executable code or as a load-bearing claim; the new comment may reference the exclusive convention by name but must not restate it as the sole convention.
    - `node scripts/probe-payload-resilience.js` prints `PROBE RESULT: 118 passed, 0 failed, 0 skipped` and exits 0.
    - `node scripts/probe-payload-resilience.js | grep -c '^PASS merge-grid-hazards-'` is 3.
    - `git diff --stat node_helper.js` shows exactly one file changed and no more than 20 changed lines; `git diff node_helper.js` touches only the `_addHazardsOutlookGridEntries` comment block and its `lastGridDay` line.
    - The SUMMARY records the four arithmetic cases above with their computed `gridStart`/`gridEnd`/`lastGridDay` values, confirmed against the actual run rather than asserted.
  </acceptance_criteria>

  <done>`_addHazardsOutlookGridEntries` places both endpoint conventions correctly, the live-shaped scenario is green, and all 116 pre-existing scenarios still pass.</done>
</task>

<task type="auto">
  <name>Task 3: Mutation-prove both new scenarios</name>

  <read_first>
    - `.planning/phases/18-merge-precedence-unified-payload-schema/18-07-SUMMARY.md` and
      `18-08-SUMMARY.md` — the recorded mutation-table format this phase uses (scenario,
      file:line mutated, mutation description, verbatim FAIL message, restored yes/no)
    - `.planning/STATE.md` — the Phase 15 D-10 decision text, including both mechanisms by which
      a scenario can pass while proving nothing
    - `node_helper.js` lines 2870-2885 — the exact expression both mutations target
    - `scripts/probe-payload-resilience.js` — the two scenarios added in Task 1, so each mutation
      is aimed at the assertion that is supposed to catch it
  </read_first>

  <files>scripts/probe-payload-resilience.js</files>

  <action>
D-10 is blocking and is this phase's verification standard: a scenario green under its own
mutation is a fixture defect, not a pass. Apply each mutation below to `node_helper.js` one at a
time — never two at once — run the full suite, record the verbatim `FAIL <name>: <message>` line,
then restore the line and re-run to confirm the suite returns to
`118 passed, 0 failed, 0 skipped` before applying the next.

M1 — targets `merge-grid-hazards-start-equals-end-live-shape-lands-on-its-own-grid-day`
  File/line: `node_helper.js`, the `lastGridDay` line inside `_addHazardsOutlookGridEntries`
  (line 2880 before the Task 2 edit).
  Mutation: replace `Math.max(gridStart, gridEnd - 1)` with `gridEnd - 1` — i.e. restore the exact
  pre-fix expression. This is the highest-value mutation of this gap-closure run: it reproduces the
  live production defect verbatim, so a green result here would mean the live MERGE-01 failure is
  still unpinned and criterion 1 cannot be re-validated.
  Required additional observation: record whether
  `merge-grid-hazards-multi-day-exclusive-span-still-ends-on-its-last-covered-day` stays GREEN
  under M1. It must — if it also goes red, the two scenarios are not covering distinct halves and
  Scenario B needs strengthening.

M2 — targets `merge-grid-hazards-multi-day-exclusive-span-still-ends-on-its-last-covered-day`
  File/line: the same `lastGridDay` line.
  Mutation: replace `Math.max(gridStart, gridEnd - 1)` with `Math.max(gridStart, gridEnd)` — the
  "switch wholesale to inclusive" error the gap report explicitly forbids. Scenario B must go RED
  on the `days["8"]` assertion. A wider blast radius is acceptable and worth recording (Scenario A's
  exclusive-convention control run is expected to go red too, since its feature would gain grid day
  7); a mutation that makes NOTHING fail is a blocking fixture defect that must be resolved by
  strengthening the scenario and re-proving, not by narrowing the mutation.

Record in the SUMMARY a two-row mutation table with columns: scenario, file:line mutated, mutation
description, verbatim `FAIL` message, restored (yes/no) — plus a note row recording M1's effect on
Scenario B. Every `FAIL` message must be quoted in full and must be diagnosable: it must name the
grid day, the source or the field involved, never merely "assertion failed".

Also record, as standing evidence for the criterion-1 re-validation that plan 18-12 performs,
Task 1's pre-fix RED line for Scenario A verbatim — that observation is a mutation proof in its
own right (the "mutation" was the shipped defect) and is the deterministic replay of the live
capture that criterion 1 needs.

Confirm the working tree is clean of every mutation before finishing: `git diff node_helper.js`
must show only the Task 2 change relative to the pre-plan state, with no mutation residue.
  </action>

  <verify>
    <automated>node scripts/probe-payload-resilience.js 2>&1 | tail -1 && git diff node_helper.js | grep -cE '^\+.*Math\.max\(gridStart, gridEnd\)|^\+.*const lastGridDay = gridEnd - 1;'</automated>
  </verify>

  <acceptance_criteria>
    - The SUMMARY contains a two-row mutation table with the columns named above, plus the M1/Scenario-B cross-check note.
    - Both rows carry a non-empty, fully quoted, verbatim `FAIL <name>: <message>` line. A row reporting "no scenario failed" is a blocking defect and must be resolved before the plan completes.
    - Each `FAIL` message names a grid day number, the source id, or the payload field involved.
    - The SUMMARY records M1's effect on `merge-grid-hazards-multi-day-exclusive-span-still-ends-on-its-last-covered-day` explicitly as GREEN or RED, with the consequence stated if RED.
    - The SUMMARY quotes Task 1's pre-fix `FAIL merge-grid-hazards-start-equals-end-...` line verbatim, labelled as the deterministic replay of the 2026-09-05 live capture.
    - The verify command's second half prints `0` — no mutation expression remains in the diff.
    - `node scripts/probe-payload-resilience.js` prints `PROBE RESULT: 118 passed, 0 failed, 0 skipped` and exits 0.
    - `git diff --stat` lists exactly `node_helper.js` and `scripts/probe-payload-resilience.js`.
  </acceptance_criteria>

  <done>Both new scenarios are individually mutation-proven with verbatim diagnosable failure messages recorded, and every mutation is reverted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NOAA ArcGIS `cpc_weather_hazards/MapServer` JSON -> `_addHazardsOutlookGridEntries` date arithmetic | `start_date`/`end_date` are attacker-shaped from this module's perspective: unvalidated remote numbers feeding a loop bound. The 2026-09-05 poll already proved the feed's endpoint convention is not what the code assumed, so "upstream is well-formed" is a disproven assumption, not a safe one. |
| Probe suite result -> phase verification claim | A green run is cited as the evidence that ROADMAP criterion 1 now holds. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-27 | Tampering | non-numeric, `NaN`, `null` or absent `start_date`/`end_date` reaching `_gridDayOf` | mitigate | The existing `typeof === "number" && Number.isFinite` containment at node_helper.js:2843-2847 runs before the changed expression and is not modified by this plan; the fix adds no new parse, no `Number()` coercion and no `new Date()` call. Acceptance criteria pin the diff to the comment block and the `lastGridDay` line only. |
| T-18-28 | Denial of Service | an absurdly large `end_date` (e.g. `1e15`) producing a huge `gridEnd` and an unbounded emission loop | mitigate | `Math.max(gridStart, gridEnd - 1)` is only the *upper candidate*; the loop header still applies `Math.min(lastGridDay, GRID_DAY_COUNT)` and `Math.max(gridStart, 1)`, so iteration is bounded to at most 14 regardless of input magnitude. The acceptance criteria assert the `Math.min(lastGridDay, GRID_DAY_COUNT)` clamp survives the edit and that the clamp is not moved into the loop body (the T-16-05 discipline). |
| T-18-29 | Denial of Service | a negative or inverted span (`end_date` far below `start_date`) | mitigate | The `legacyOffsetEnd < legacyOffsetStart` guard at node_helper.js:2857 rejects inverted spans before the changed expression is reached, and is untouched. `Math.max(gridStart, ...)` can only ever raise the upper bound to `gridStart` itself, never below it, so the widest possible range this change can produce for a non-inverted span is a single grid day — it cannot invert the loop or widen it beyond the clamp. |
| T-18-30 | Elevation of Privilege | a crafted `end_date` widening a feature's day coverage to advertise risk on days the source never claimed | mitigate | Scenario B (`...multi-day-exclusive-span-still-ends-on-its-last-covered-day`) asserts `days["8"]` stays empty for a Sep 10 -> Sep 12 span, and mutation M2 proves that assertion is live. A change that widens coverage by one day cannot pass the suite. |
| T-18-31 | Repudiation | a probe scenario that passes forever while proving nothing, making a false phase-close claim | mitigate | Per D-10: Task 1 records Scenario A RED against the shipped defect before any fix exists, and Task 3 individually mutation-proves both scenarios with verbatim `FAIL` text. Both scenarios carry a `precondition failed:` guard (the label actually reached the legacy block) and a `control:` assertion. |
| T-18-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install tasks in this plan; zero new dependencies. The probe runner's own `requires: "kml-deps"` skip path already forces a non-zero exit rather than a green run when a dependency is missing. |
</threat_model>

<verification>
- `node scripts/probe-payload-resilience.js` -> `PROBE RESULT: 118 passed, 0 failed, 0 skipped`, exit 0.
- `node scripts/probe-payload-resilience.js | grep -c '^PASS merge-grid-hazards-'` -> 3.
- `grep -c 'Math.max(gridStart, gridEnd - 1)' node_helper.js` -> 1.
- The two-row mutation table in the SUMMARY, each row carrying a verbatim diagnosable `FAIL` message, plus Task 1's pre-fix RED line quoted verbatim.
- `git diff --stat` lists only `node_helper.js` and `scripts/probe-payload-resilience.js`.
</verification>

<success_criteria>
- The live-observed `start_date === end_date` `wpc-hazards` shape reaches its correct unified grid day (day 6, `2026-09-10`, `windowStart 2026-09-10T12:00:00.000Z`) instead of being silently dropped, and shows up in `sources['wpc-hazards'].reportedDays`/`activeDays`.
- The exclusive-end convention every pre-existing fixture uses is unchanged on every single-day and multi-day case.
- The fixture blind spot the 18-09 capture identified is closed: the suite now contains a hazards scenario built on the live inclusive shape, using the captured epoch value literally.
- Both new scenarios are individually mutation-proven, and Task 1's pre-fix RED stands as the deterministic replay of the live capture that plan 18-12 cites for criterion 1.
</success_criteria>

<output>
Create `.planning/phases/18-merge-precedence-unified-payload-schema/18-10-SUMMARY.md` when done.
The mutation table and Task 1's verbatim pre-fix RED line are the deliverables — record them in
full; plan 18-12 cites them directly.
</output>
