---
phase: 18-merge-precedence-unified-payload-schema
plan: 09
type: execute
wave: 7
depends_on: ["18-08"]
files_modified:
  - .planning/phases/18-merge-precedence-unified-payload-schema/18-LIVE-CAPTURE.md
  - .planning/STATE.md
autonomous: false
requirements: [MERGE-01, MERGE-02, MERGE-04, RPT-07, PERF-03]
must_haves:
  truths:
    - "A real payload captured from live NOAA endpoints exists and is recorded, not a synthetic fixture"
    - "MERGE-02's suppression is confirmed by inspecting the suppression code path against the captured payload, exactly as ROADMAP criterion 2 words it"
    - "MERGE-04's distinct-and-duplicate behaviour is confirmed against the captured payload, not against assumed synthetic cases"
    - "The near-boundary MERGE-01 case is either observed live or explicitly recorded as NOT OBSERVABLE with the mutation-proven scenario that stands in its place"
    - "A local cold-start PERF-03 figure is recorded, and the Raspberry Pi figure is left on the existing STATE.md milestone blocker"
  artifacts:
    - path: ".planning/phases/18-merge-precedence-unified-payload-schema/18-LIVE-CAPTURE.md"
      provides: "the captured payload excerpt, the per-criterion validation table, and the PERF-03 local figures"
      min_lines: 60
  key_links:
    - from: "18-LIVE-CAPTURE.md"
      to: "ROADMAP Phase 18 success criteria 1, 2, 4, 5, 6"
      via: "one row per criterion with a verdict and its evidence"
      pattern: "MERGE-0"
---

<objective>
Capture a real payload from the live NOAA endpoints, validate ROADMAP success criteria 1, 2, 4 and 5
against it, record the local cold-cache PERF-03 figures, and get a human verdict.

Purpose: the ROADMAP wording for criteria 1, 2 and 4 is explicit that validation is against captured
live payloads, not synthetic cases. Plans 18-07 and 18-08 built the executable evidence; this plan
supplies the live evidence those criteria actually ask for, and honestly records anything not
observable on demand rather than substituting a fixture and calling it observed.

Output: `18-LIVE-CAPTURE.md`, an updated STATE.md, and a human checkpoint.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/buildomator/bm/4.5.6/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/buildomator/bm/4.5.6/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-merge-precedence-unified-payload-schema/18-CONTEXT.md
@.planning/phases/18-merge-precedence-unified-payload-schema/18-RESEARCH.md
@.planning/phases/18-merge-precedence-unified-payload-schema/18-05-SUMMARY.md
@.planning/phases/18-merge-precedence-unified-payload-schema/18-06-SUMMARY.md
@.planning/phases/18-merge-precedence-unified-payload-schema/18-07-SUMMARY.md
@.planning/phases/18-merge-precedence-unified-payload-schema/18-08-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture a live payload and validate criteria 1, 2, 4 and 5 against it</name>

  <read_first>
    - `.planning/ROADMAP.md` section "Phase 18" — the six success criteria verbatim, especially
      criterion 2's "confirmed by inspecting the suppression code path against a captured live payload
      to verify it keys off the hazard dimension, not a label string match" and criterion 4's
      "validated against captured live payloads, not assumed synthetic cases"
    - `node_helper.js` — `getSpcOutlook`'s signature, the `_products` toggle snapshot, and
      `_resolveGridDayPrecedence` in full, since criterion 2 requires inspecting that code path
    - `.planning/STATE.md` — the recorded procedure for live verification of location-gated products:
      temporarily move `lat`/`lon` into an active polygon, confirm with a real point-in-polygon test
      against live geometry, never a bounding-box check
    - `.planning/phases/18-merge-precedence-unified-payload-schema/18-RESEARCH.md`
      section "Live Field Formats" — the near-boundary case description
  </read_first>

  <files>.planning/phases/18-merge-precedence-unified-payload-schema/18-LIVE-CAPTURE.md</files>

  <action>
Drive one real poll against the live NOAA endpoints with every product toggle enabled
(`showExcessiveRain`, `showWinterImpact`, `showHazardsOutlook`, `showHeatRisk`, `showMPD`,
`showSPCMD`, `extended: true`; state whether you enabled `showDrought` and why). Use a throwaway
capture script, not the probe harness — the probe stubs the transport seam, and this capture must
exercise the real network path. Write the raw payload to the scratch directory, never into the repo.

Choose the coordinate deliberately: a capture over a quiet location proves nothing about suppression.
Follow the procedure STATE.md already records — if the deployed coordinate is quiet, move `lat`/`lon`
into an area with active SPC convective coverage, confirm containment with a real point-in-polygon
test against the live geometry rather than a bounding-box check (Phoenix fell inside MPD_1122's bbox
but outside its polygon, a recorded false positive), capture, and record the coordinate and the reason.

Create `18-LIVE-CAPTURE.md` containing:

1. Capture metadata: timestamp, coordinates, toggle set, the SPC `VALID_ISO`/`EXPIRE_ISO` the anchor
   resolved from, and `sources['spc-convective'].gridAnchor`.

2. A trimmed payload excerpt: the full `summary` block, the full `sources` block, and every `days[N]`
   entry with a non-empty `hazards` array. State how many grid days were empty rather than pasting them.

3. A validation table, one row per ROADMAP criterion, each with a verdict of PASS, NOT OBSERVABLE or
   FAIL and its specific evidence:

   - Criterion 1 (MERGE-01, near-boundary): look for a `wpc-hazards` entry whose upstream
     `start_date` falls between the grid day's 00Z and 12Z. If present, record the entry, its grid
     day, and that grid day's `windowStart`/`windowEnd`, and confirm the placement matches D-10's
     forward-align rule. If absent, record NOT OBSERVABLE, state that a near-boundary case is not
     available on demand, and cite `merge-grid-hazards-00z-feature-forward-aligns-to-next-grid-day`
     plus its recorded mutation proof as the standing evidence — the precedent this project already
     set for HEAT-04 and for the Phase 16 Precipitation-layer cases.

   - Criterion 2 (MERGE-02): find a grid day where both `spc-convective` and `wpc-hazards` contributed
     a `convective` entry, and record both verbatim with their `suppressedBy` values. Then do what the
     criterion literally asks: inspect `_resolveGridDayPrecedence` and record the specific lines
     showing the decision is made on `entry.dimension` and `PRECEDENCE[dimension]`, with no
     label-string comparison anywhere in the method. If the capture has no such day, record
     NOT OBSERVABLE for the payload half but still complete the code-path half — that half is always
     available and is half of what the criterion asks.

   - Criterion 4 (MERGE-04): list every grid day carrying two or more surviving entries and their
     dimensions, and confirm no dimension appears twice with two different sources both surviving
     (the under-merge check). Confirm separately that no `flash-flood` and `heavy-precip` pair
     suppressed each other (the over-merge check). Record NOT OBSERVABLE with the standing scenario
     citations if the capture has no multi-hazard day.

   - Criterion 5 (RPT-07): for one populated grid day, write out the compact line a renderer would
     produce (the `suppressedBy === null` entries in array order) and the detailed sub-rows (all
     entries, labelled with `sources[entry.source].displayName`). Confirm nothing required reading
     `PRECEDENCE`, `NO_RISK_FLOOR`, or a raw source value. Name any field a renderer would still have
     to derive — if there is one, that is a Phase 19 finding, not a silent omission.

4. A "Days 9-14 shape check" section closing the CONTEXT Deferred item: record what `days["9"]`
   through `days["14"]` contained, and confirm they are well-shaped whether or not `wpc-hazards`
   reached them.

Never present a fixture result as a live observation. NOT OBSERVABLE with a named, mutation-proven
standing scenario is an honest and accepted outcome here; a fixture relabelled as live evidence is not.
  </action>

  <verify>
    <automated>test -f .planning/phases/18-merge-precedence-unified-payload-schema/18-LIVE-CAPTURE.md &amp;&amp; grep -cE "MERGE-01|MERGE-02|MERGE-04|RPT-07" .planning/phases/18-merge-precedence-unified-payload-schema/18-LIVE-CAPTURE.md</automated>
  </verify>

  <acceptance_criteria>
    - `18-LIVE-CAPTURE.md` exists and contains a validation table with a row for each of MERGE-01,
      MERGE-02, MERGE-04 and RPT-07, each carrying exactly one verdict of PASS, NOT OBSERVABLE or FAIL.
    - The capture metadata section records a real timestamp, real coordinates, and a real
      `gridAnchor` value read from the captured payload.
    - The criterion 2 row includes quoted lines from `_resolveGridDayPrecedence`, and a
      `grep -nE "label ?===|\.includes\(|indexOf\(" ` over that method's line range produces no output,
      with that command and its empty result recorded in the document.
    - Every NOT OBSERVABLE verdict names the specific mutation-proven probe scenario standing in for
      it, by scenario name.
    - No raw captured payload file is committed to the repository:
      `git status --porcelain` shows no new file outside `.planning/`.
    - `node scripts/probe-payload-resilience.js` still reports
      `PROBE RESULT: 114 passed, 0 failed, 0 skipped` and exits 0.
  </acceptance_criteria>

  <done>A real live payload has been captured and each of ROADMAP criteria 1, 2, 4 and 5 has an honest, evidenced verdict.</done>
</task>

<task type="auto">
  <name>Task 2: Record the local cold-cache PERF-03 figures and update project state</name>

  <read_first>
    - `.planning/phases/18-merge-precedence-unified-payload-schema/18-06-SUMMARY.md` — the cold-start
      log block and the frontend wall-clock line as captured during instrumentation
    - `node_helper.js` — the cold-start summary log block and `_processStartMs`, to confirm what a
      real fresh-process run emits
    - `.planning/STATE.md` — the existing blocker line "Phase 18 PERF-03 requires a real cold-cache
      latency measurement on target Raspberry Pi hardware before the milestone can close", which stays
    - `.planning/phases/18-merge-precedence-unified-payload-schema/18-CONTEXT.md` D-17, D-18, D-19 and
      the Deferred item stating no pass/fail latency target exists
  </read_first>

  <files>.planning/phases/18-merge-precedence-unified-payload-schema/18-LIVE-CAPTURE.md, .planning/STATE.md</files>

  <action>
Run a genuine cold-cache measurement on the local development machine. Cold cache requires no setup
beyond a fresh process: `node_helper.js` holds the only cache, an in-memory `Map` keyed by URL, with
no disk cache and no cache directory to clear (D-17's established fact). Start a fresh process, drive
one `GET_SPC_DATA`, and capture the emitted cold-start log block verbatim.

Add a "PERF-03 local cold-cache measurement" section to `18-LIVE-CAPTURE.md` recording:
  - the verbatim cold-start log block (all four lines);
  - the backend interval in ms;
  - the full per-product breakdown and the named slowest source;
  - the wall-clock figure from the frontend line, or an explicit note that the frontend half was
    measured through the module rather than a browser and how;
  - the machine it was measured on, so the figure is attributable and the Pi figure is comparable;
  - an explicit statement that no pass/fail latency target exists (research derived none for this
    hardware), so this figure is a baseline, not a gate.

Update `.planning/STATE.md`:
  - Under Accumulated Context, add a decision line recording that Phase 18 ships a permanent,
    always-on cold-start timing instrument logged once per process with no config flag (D-18), and
    that the local baseline figure lives in `18-LIVE-CAPTURE.md`.
  - Leave the existing Raspberry Pi blocker line in place, unchanged. Per D-19 the phase completes
    when the instrument is built and locally verified; the Pi figure is a milestone-close item, and
    reclassifying it here would either falsely close a blocker or wrongly gate Phase 19 on a
    measurement no agent can take.
  - Add any NOT OBSERVABLE verdicts from Task 1 to the Deferred Items table, each with its standing
    scenario name and the reason it was not observable, matching the format the Phase 15 and Phase 16
    rows already use.
  </action>

  <verify>
    <automated>grep -c "PERF-03 local cold-cache measurement" .planning/phases/18-merge-precedence-unified-payload-schema/18-LIVE-CAPTURE.md &amp;&amp; grep -c "Raspberry Pi" .planning/STATE.md</automated>
  </verify>

  <acceptance_criteria>
    - `18-LIVE-CAPTURE.md` contains a "PERF-03 local cold-cache measurement" section with the verbatim
      four-line log block, a numeric backend interval, a per-product breakdown, and a named slowest
      source.
    - The section states the measuring machine and states explicitly that no pass/fail target exists.
    - `.planning/STATE.md` still contains the existing Raspberry Pi cold-cache blocker line, unmodified:
      `grep -c "PERF-03 requires a real cold-cache latency measurement on target Raspberry Pi" .planning/STATE.md`
      is 1.
    - `.planning/STATE.md` gained a decision line naming D-18 and pointing at `18-LIVE-CAPTURE.md`.
    - Every NOT OBSERVABLE verdict from Task 1 has a matching row in STATE.md's Deferred Items table.
  </acceptance_criteria>

  <done>The local PERF-03 baseline is recorded and attributable, and the Pi measurement remains exactly where D-19 puts it — on the milestone, not on this phase.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human review of the live capture and the phase's honest verdict</name>

  <what-built>
    Phase 18 emits a unified `days`/`summary`/`sources` block beside the eight untouched legacy blocks.
    Suppression is resolved per grid day from a standalone `hazardTaxonomy.js`, keyed on hazard
    dimension rather than label text. The probe suite grew from 91 to 114 scenarios, every new one
    individually mutation-proven with its failure message recorded. A permanent cold-start timing
    instrument is in place and locally measured. A live payload has been captured and each ROADMAP
    criterion given a PASS, NOT OBSERVABLE or FAIL verdict.
  </what-built>

  <how-to-verify>
    1. Read `.planning/phases/18-merge-precedence-unified-payload-schema/18-LIVE-CAPTURE.md` end to end.
       Focus on the validation table: every NOT OBSERVABLE verdict is a claim that something could not
       be seen live, backed by a named mutation-proven scenario. Judge whether each is honest or
       whether it should have been chased harder — this project has a standing rule that a fixture is
       never presented as a live observation.
    2. Confirm the criterion 2 row satisfies the ROADMAP's literal wording: it must show BOTH the
       captured payload's `suppressedBy` values AND the inspected code path proving the decision keys
       off `entry.dimension` and `PRECEDENCE[dimension]`, not a label-string match.
    3. Run `node scripts/probe-payload-resilience.js` and confirm
       `PROBE RESULT: 114 passed, 0 failed, 0 skipped`.
    4. Read the mutation tables in `18-07-SUMMARY.md` and `18-08-SUMMARY.md`. Twenty-three rows total,
       each with a verbatim `FAIL` message. Spot-check two or three and judge whether the message
       would actually let you diagnose the break, per the Phase 15 D-10 standard.
    5. Review the PERF-03 local figures. There is no pass/fail target — the question is only whether
       the numbers look plausible and whether the breakdown attributes time usefully. The Raspberry Pi
       figure is deliberately NOT taken here; it stays on the existing milestone blocker per D-19.
    6. Confirm the display still works: with the module running against the legacy render path, nothing
       should have changed on screen. D-01's entire purpose is that Phase 18 is invisible to the user.
       A visible change is a D-01 violation and a blocking finding.
  </how-to-verify>

  <resume-signal>
    Type "approved" to close the phase, or describe what needs to change. If step 6 shows any visible
    display change, say so — that is a D-01 violation and takes priority over everything else here.
  </resume-signal>

  <files>.planning/phases/18-merge-precedence-unified-payload-schema/18-09-SUMMARY.md</files>

  <action>
Pause and present the six verification steps above to the operator. Do not proceed past this task
without an answer. Record the operator's verdict verbatim in the SUMMARY, including their explicit
answer to step 6 (whether the rendered display changed). Treat any reported display change as a
blocking D-01 violation and stop rather than closing the phase.
  </action>

  <verify>
    <automated>node scripts/probe-payload-resilience.js | tail -1</automated>
  </verify>

  <done>The operator has answered, the verdict is recorded verbatim in the SUMMARY, and step 6's display-unchanged answer is captured explicitly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live NOAA endpoints -> capture script -> recorded artifact | Real upstream data, including free-text labels, is captured and pasted into a planning document. |
| Captured payload -> scratch directory | The capture may contain the operator's real coordinates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-25 | Information Disclosure | operator coordinates in a committed artifact | mitigate | The raw payload is written to the scratch directory and never committed; only a trimmed excerpt enters `18-LIVE-CAPTURE.md`, and the coordinate recorded there is the one deliberately chosen for the capture, which the plan already requires be stated openly. An acceptance criterion asserts `git status --porcelain` shows no new file outside `.planning/`. |
| T-18-26 | Repudiation | a fixture result recorded as a live observation | mitigate | Every verdict must be PASS, NOT OBSERVABLE or FAIL, and every NOT OBSERVABLE must name the specific mutation-proven scenario standing in for it. The human checkpoint's step 1 exists to judge exactly this. |
| T-18-27 | Tampering | pasted upstream label text in a markdown artifact | accept | Markdown planning documents are not rendered as HTML by any part of this project, and the excerpt is trimmed and human-reviewed. No executable surface. |
| T-18-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install tasks in this phase; RESEARCH.md confirms zero new dependencies. |
</threat_model>

<verification>
- `18-LIVE-CAPTURE.md` exists with a complete validation table and the PERF-03 section.
- `node scripts/probe-payload-resilience.js` -> `PROBE RESULT: 114 passed, 0 failed, 0 skipped`, exit 0.
- `.planning/STATE.md` retains the Raspberry Pi blocker unchanged and gains the D-18 instrument
  decision line plus any NOT OBSERVABLE deferred rows.
- The human checkpoint is answered, with step 6's display-unchanged confirmation explicitly recorded.
</verification>

<success_criteria>
- ROADMAP criteria 1, 2, 4 and 5 each have an evidenced verdict against a real captured payload, with
  every NOT OBSERVABLE backed by a named mutation-proven scenario.
- Criterion 2's code-path inspection is recorded, proving suppression keys off the hazard dimension.
- Criterion 6's local half is recorded with attributable figures; the Pi half remains the milestone
  blocker it already was (D-19).
- The human operator confirms the rendered display is unchanged, which is D-01's whole point.
</success_criteria>

<output>
Create `.planning/phases/18-merge-precedence-unified-payload-schema/18-09-SUMMARY.md` when done.
Record the human verdict verbatim, including the answer to step 6.
</output>
