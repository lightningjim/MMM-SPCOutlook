---
phase: 17-nws-wpc-heatrisk-parallelized-fetching
plan: 09
type: execute
wave: 8
depends_on: ["17-08"]
files_modified:
  - .planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/17-MUTATION-INVENTORY.md
  - .planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/17-DATA03-SPOTCHECK.md
autonomous: false
requirements: [HEAT-01, HEAT-02, HEAT-03, HEAT-04, PERF-01, DATA-03]

must_haves:
  truths:
    - "Every scenario added in phase 17 has a recorded mutation, a recorded RED message, and a recorded restore — the phase's blocking Phase 15 D-10 obligation is auditable in one place"
    - "DATA-03's recorded spot-check table exists as its own artifact, covering what assertNoSharedRegistryMaps structurally cannot see (D-11's paired requirement; the assertion alone was explicitly rejected as overstating its coverage)"
    - "The user has observed the live HeatRisk product on the deployed MagicMirror against the five ROADMAP success criteria, or each unobservable criterion is recorded with its reason"
  artifacts:
    - path: ".planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/17-MUTATION-INVENTORY.md"
      provides: "one row per phase-17 scenario: name, requirement/decision pinned, exact mutation, RED message, restore confirmation"
    - path: ".planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/17-DATA03-SPOTCHECK.md"
      provides: "the enumerated label-to-value table inventory and the identity-blind-spot analysis D-11 requires alongside the load-time assertion"
  key_links:
    - from: "17-MUTATION-INVENTORY.md"
      to: "scripts/probe-payload-resilience.js scenario names"
      via: "one row per scenario, names matching exactly"
      pattern: "heatrisk-"
---

<objective>
Consolidate the phase's mutation evidence into one auditable inventory, produce DATA-03's recorded
spot-check artifact, and run the human UAT against the ROADMAP's five success criteria.

Purpose: Phase 15 D-10 is blocking project policy, and its evidence is currently spread across four
plan SUMMARYs. Phase 16 closed with the same consolidation step and it is what made the phase's
verification auditable. D-11 explicitly requires the spot check as a paired artifact, having
rejected "assertion alone" as overstating its own coverage — so a table living only inside
`17-PATTERNS.md` (a planning input, not a phase deliverable) does not discharge it.

This plan writes documentation and runs a human check. It touches no source file.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/buildomator/bm/4.5.5/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/buildomator/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/17-CONTEXT.md
@.planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/17-RESEARCH.md
@.planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/17-PATTERNS.md
@.planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/17-06-SUMMARY.md
@.planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/17-07-SUMMARY.md
@.planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/17-08-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Phase mutation inventory and the DATA-03 spot-check artifact</name>

  <files>.planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/17-MUTATION-INVENTORY.md, .planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/17-DATA03-SPOTCHECK.md</files>

  <read_first>
    - .planning/phases/16-wpc-day-3-7-cpc-day-8-14-hazards-outlook/ — locate Phase 16's own mutation
      inventory artifact (16-08 produced one) and match its format so the two are comparable.
    - The 17-05, 17-06, 17-07 and 17-08 SUMMARYs — every recorded mutation, RED output and control
      result.
    - scripts/probe-payload-resilience.js — the authoritative list of scenario names, so the
      inventory's names match the source exactly.
    - .planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/17-PATTERNS.md §9 — the enumerated
      label-to-value table inventory with file:line, and the identity blind-spot analysis.
    - productRegistry.js and node_helper.js — re-derive the map inventory from the CURRENT source
      rather than copying 17-PATTERNS.md, which was written before this phase's edits landed.
    - .planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/17-CONTEXT.md D-11 verbatim.
  </read_first>

  <action>
    (a) Write `17-MUTATION-INVENTORY.md`. One table, one row per scenario added in this phase (the
    15 `heatrisk-*` / `frontend-heatrisk-*` / `registry-*` / `new-product-batch-*` scenarios plus
    `scripts/check-concurrency-invariant.sh`'s own mutation proof from 17-05). Columns:
      - Scenario name (exact, as it appears in the source)
      - Requirement / decision pinned (HEAT-01..04, PERF-01, DATA-03, D-03..D-11)
      - Exact mutation applied (file, function, and what was changed)
      - RED message observed, quoted
      - Restored green (yes/no)
      - Precondition guard proven capable of firing (yes/no)
      - Control assertion proven non-vacuous (yes/no)
    Add a short section recording the two cross-branch independence matrices from 17-07 (D-04's
    pair and D-05's pair, plus the D-04-vs-D-05 non-collapse check), since those are the artifacts
    proving the compound conditions shipped as distinguished branches.
    Add a section listing anything NOT mutation-proven, with the reason, following the disclosure
    convention Phase 15 F3 and Phase 16 established. If everything is proven, say so explicitly
    rather than omitting the section.

    (b) Write `17-DATA03-SPOTCHECK.md`. This is the recorded spot check D-11 requires ALONGSIDE the
    load-time assertion. It must contain:
      - A table of every label-to-value / tier / text / colour map in the codebase, re-derived from
        the CURRENT source: product, table name, file:line, and shape. Cover at minimum ERO, fire
        weather, WSSI, SPC categorical, Hazards Outlook, the `kml-advisory` rows (which legitimately
        have none), and HeatRisk's new `valueToText`/`valueToColor`.
      - An explicit statement, per product, that no two rows share a map object by reference —
        with `assertNoSharedRegistryMaps`'s load-time pass cited as the mechanical proof.
      - The blind-spot analysis: identity CANNOT see a `toValue`/`includesFeat` closure that reads a
        FOREIGN constant by name rather than sharing the object by reference. Walk each product's
        `toValue`/`includesFeat` closure and confirm by reading it that each references only its own
        table. Give special attention to the ERO `dn` / fire weather `DN` pair DATA-03's own wording
        names — their keys numerically overlap while being semantically distinct, which is exactly
        the confusion that would survive an identity check.
      - A note that HeatRisk introduces no label vocabulary (its value is a raw numeric category
        straight off the service), so it cannot itself be a violation source — but its row's
        text/colour maps are covered by the assertion like every other row's.
      - A line recording which `MAP_FIELDS` names exist and confirming every map-shaped field on
        every row is covered by one of them, with the diff (expected empty).

    Both documents must be self-contained: a reader must not need to open a plan SUMMARY to audit
    the phase's verification claims.
  </action>

  <verify>
    <automated>node scripts/probe-payload-resilience.js && bash scripts/check-concurrency-invariant.sh && node -e "require('./productRegistry')" && node -e "const fs=require('fs'),p='.planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/'; for(const f of ['17-MUTATION-INVENTORY.md','17-DATA03-SPOTCHECK.md']){ const s=fs.readFileSync(p+f,'utf8'); if(s.length<800) throw new Error(f+' too short to be a real artifact'); } const inv=fs.readFileSync(p+'17-MUTATION-INVENTORY.md','utf8'); const src=fs.readFileSync('scripts/probe-payload-resilience.js','utf8'); const names=[...src.matchAll(/name: \"((?:heatrisk|frontend-heatrisk|registry-rejects|new-product-batch)[^\"]*)\"/g)].map(m=>m[1]); if(names.length===0) throw new Error('no phase-17 scenarios found in source'); const missing=names.filter(n=>!inv.includes(n)); if(missing.length) throw new Error('inventory missing scenarios: '+missing.join(', ')); console.log('OK '+names.length+' phase-17 scenarios all present in the inventory');"</automated>
  </verify>

  <acceptance_criteria>
    - The `<verify>` command exits 0 and prints the scenario count, confirming every phase-17
      scenario name found in the source appears in the inventory. A scenario in the source but not
      the inventory is an unaudited mutation claim.
    - Every inventory row has a non-empty "RED message observed" cell quoting real output. A row
      with a placeholder, an ellipsis, or "see SUMMARY" fails this criterion — the document must be
      self-contained.
    - Every row's "Restored green" is `yes`, and `git status` is clean of any lingering mutation.
    - `17-DATA03-SPOTCHECK.md` enumerates at least the ten map tables from 17-PATTERNS.md §9 plus
      HeatRisk's two, each with a file:line re-derived from the current source. Spot-verify three of
      the cited line numbers by hand and record which three you checked.
    - The spot-check document contains an explicit per-closure finding for the ERO `dn` / fire
      weather `DN` pair.
    - Full probe suite green; `scripts/check-concurrency-invariant.sh` exits 0;
      `node -e "require('./productRegistry')"` exits 0.
  </acceptance_criteria>

  <done>
    Both artifacts exist, are self-contained, and every phase-17 scenario is accounted for in the
    inventory with a quoted RED message.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human UAT against the ROADMAP's five success criteria</name>

  <files>none (operator-run verification; no file is edited by this task)</files>

  <read_first>
    - .planning/ROADMAP.md § "Phase 17" — the five success criteria verbatim, since the operator's
      verdict is recorded against them one by one.
    - .planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/17-09-SUMMARY.md's sibling
      artifacts written in Task 1 — `17-MUTATION-INVENTORY.md` and `17-DATA03-SPOTCHECK.md` — so you
      can point the operator at the right file for criterion 5.
    - .planning/STATE.md § Deferred Items — the row format used for every NOT OBSERVABLE outcome in
      Phases 15 and 16, so a NOT OBSERVABLE verdict is recorded in the established shape.
    - .planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/17-RESEARCH.md §"Live UAT Check" —
      which criteria live data can and cannot prove this session (HEAT-04, D-05's branches and
      PERF-01 are fixture-only by necessity).
  </read_first>

  <action>
    Present the `<what-built>` and `<how-to-verify>` content below to the operator, wait for their
    verdict, and record it. Do not run this check yourself and do not infer a verdict from the probe
    suite — the whole point is observation against live upstream data on the deployed hardware.

    Before presenting, confirm the three preconditions so the operator is not sent to check a build
    that is already known broken: `node scripts/probe-payload-resilience.js` green,
    `bash scripts/check-concurrency-invariant.sh` exit 0, and `node -e "require('./productRegistry')"`
    exit 0. If any fails, fix it first rather than presenting the checkpoint.

    After the operator responds, transcribe their verdict per criterion into the SUMMARY verbatim,
    and format each NOT OBSERVABLE as a STATE.md Deferred Items row (Category | Item | Status |
    Deferred At) rather than as prose.
  </action>

  <what-built>
    Phase 17 is complete in code and fixture-verified:
    - HeatRisk ships end to end — a registry row with its own ImageServer `identify` URL builder, a
      Web-Mercator-reprojected point query, and a zip/sort/dedupe/bucket pipeline that attributes
      each of Days 1-7 from `idp_validtime` rather than array order.
    - `showHeatRisk` (default off) enables it; `showMinorHeat` (default off) drops the display floor
      from Moderate to Minor and is frontend-only.
    - The six new-product fetches now run as one `Promise.allSettled` batch, with a per-run log line
      carrying each member's elapsed ms and the batch's wall clock.
    - `productRegistry.js` throws at module load if any two product rows share a label/value map.
    - 15 new mutation-proven probe scenarios; full suite green.

    Note before you start: this session's research made a live, successful `identify` call against
    the deployment coordinate (Norman, OK) on 2026-08-31 and got real categories back, so HeatRisk
    is in season and in coverage. Unlike Phase 16's Precipitation layers, live confirmation of
    criteria 1-2 should be genuinely observable today.
  </what-built>

  <how-to-verify>
    On the deployed MagicMirror, set `showHeatRisk: true` (leave `showMinorHeat: false` initially)
    alongside your existing config, restart, and check each ROADMAP success criterion in turn.

    1. HEAT-01 / HEAT-02 — Do you see HeatRisk rows for the days that clear the Moderate floor, and
       do the categories look plausible against
       `https://www.wpc.ncep.noaa.gov/heatrisk/` for your location and the next several days? In
       particular, does Day 1 correspond to TODAY rather than tomorrow? (The catalog arrives in
       arbitrary order and the service's own "visible tile" pointed at Day 2 during research, so
       an off-by-one here is the specific failure this criterion exists to catch.)

    2. HEAT-03 — Do you get real categories rather than nothing at all? If every day is blank AND
       the warning badge is showing, that is D-04's all-NoData signal, which for this deployment
       means a broken identify call rather than an out-of-coverage location. Report it.

    3. HEAT-04 — Not directly observable: no duplicate `idp_validtime` was present in any live poll
       during research, and there is no way to manufacture one on demand. It is covered by
       `heatrisk-duplicate-validtime-keeps-latest-filedate`, mutation-proven against a synthetic
       fixture. Nothing to check; confirm you are content to accept the fixture evidence.

    4. PERF-01 — In your MagicMirror logs, find the line beginning
       `MMM-SPCOutlook: new-product batch settled in`. Does it appear once per poll, and does its
       JSON carry a timing entry for every enabled product? Optionally: with several products
       enabled, is the batch's total wall clock closer to the SLOWEST member's time than to the SUM
       of all members' times? That is the concurrency win, and it is the figure Phase 18's PERF-03
       Pi measurement will build on.

    5. DATA-03 — Not a visual check. Confirm the module started at all: `productRegistry.js` now
       throws at load time if any two products share a label map, so a clean start IS the check.
       The recorded spot-check table is at
       `.planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/17-DATA03-SPOTCHECK.md`.

    Then, if HeatRisk showed nothing at the default floor, set `showMinorHeat: true`, restart, and
    confirm any Minor days now appear. Importantly, in EITHER state the module must never be blank
    with no message — if you ever see an empty module rather than either rows or "No Severe Weather
    Risk", stop and report it. That is the Phase 15 defect class D-03 exists to prevent and it is
    the single most important thing to watch for.

    Report per criterion: PASS, FAIL, or NOT OBSERVABLE with the reason. A NOT OBSERVABLE is a
    valid outcome and gets recorded as a deferred verification row, exactly as Phases 15 and 16 did.
  </how-to-verify>

  <resume-signal>
    Reply with a verdict per criterion (1-5), or describe what you saw. Type "approved" if all
    observable criteria passed.
  </resume-signal>

  <verify>
    <human-check>Operator returns PASS / FAIL / NOT OBSERVABLE for each of the five ROADMAP success criteria, with a reason for any non-PASS.</human-check>
    <automated>node scripts/probe-payload-resilience.js && bash scripts/check-concurrency-invariant.sh && node -e "require('./productRegistry')"</automated>
  </verify>

  <acceptance_criteria>
    - All three precondition commands exit 0 BEFORE the checkpoint is presented.
    - The operator has returned a verdict for all five criteria; none is left unaddressed.
    - Every NOT OBSERVABLE verdict is recorded with its reason, formatted as a STATE.md Deferred
      Items row.
    - If the operator reports a blank module (no rows AND no "No Severe Weather Risk" text), this
      task FAILS regardless of the other four criteria — that is the D-03 defect class and it is
      blocking, not a deferred observation.
    - Any FAIL is written into the SUMMARY with the observed behaviour quoted, so gap closure has a
      reproduction to work from.
  </acceptance_criteria>

  <done>
    A recorded operator verdict exists for each of the five criteria, and no blank-module report was
    returned.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recorded evidence -> phase close | An inventory that omits a scenario lets an unproven mutation claim ship as verified |
| live deployment -> operator observation | The only boundary in this phase where real upstream data reaches a human |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-20 | Repudiation (false confidence) | phase verification evidence | mitigate | The inventory is machine-cross-checked against the scenario names actually present in the source; a missing row fails the verify command |
| T-17-04 | Tampering (data integrity) | DATA-03 coverage claim | mitigate | The spot-check artifact records the identity check's blind spot explicitly, per D-11's rejection of "assertion alone" |
| T-17-25 | Information disclosure | UAT config change | accept | The operator temporarily edits local MagicMirror config only; no credentials involved and the deployment coordinate is already in the repo's defaults |
| T-17-12 | Spoofing (false all-clear) | live render | mitigate | The UAT instructions call out the blank-module failure mode explicitly as the single most important thing to watch for |
| T-17-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero new packages this phase |
</threat_model>

<verification>
- `node scripts/probe-payload-resilience.js` green.
- `bash scripts/check-concurrency-invariant.sh` exits 0.
- `node -e "require('./productRegistry')"` exits 0.
- The inventory cross-check command passes with every phase-17 scenario present.
- The human checkpoint returns a verdict for all five criteria.
</verification>

<success_criteria>
- `17-MUTATION-INVENTORY.md` accounts for every phase-17 scenario with a quoted RED message,
  a guard-firing verdict and a control-assertion verdict.
- `17-DATA03-SPOTCHECK.md` enumerates every label-to-value map with a current file:line and records
  the identity check's blind spot.
- The operator has returned PASS / FAIL / NOT OBSERVABLE for each of the five ROADMAP criteria.
</success_criteria>

<output>
Create `.planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/17-09-SUMMARY.md` when done.
Record: the operator's verdict per criterion verbatim, every NOT OBSERVABLE with its reason
formatted as a STATE.md Deferred Items row, and any defect the live check surfaced.
</output>
