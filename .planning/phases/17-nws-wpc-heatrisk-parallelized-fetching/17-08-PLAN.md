---
phase: 17-nws-wpc-heatrisk-parallelized-fetching
plan: 08
type: execute
wave: 7
depends_on: ["17-07"]
files_modified:
  - scripts/probe-payload-resilience.js
  - scripts/probe-lib/module-stubs.js
autonomous: true
requirements: [PERF-01, DATA-03]

must_haves:
  truths:
    - "A Minor-only HeatRisk day with showMinorHeat off renders no row AND still renders the all-clear — the module is never simultaneously silent and uninformative (D-03)"
    - "A HeatRisk-only day above the floor with every other product NONE does not render 'No Severe Weather Risk' (D-03, the Phase 15 MPD-invisible defect class)"
    - "A later batch member's first request is issued before an earlier member's response resolves, proven without any dependence on wall-clock timing (PERF-01, D-10)"
    - "assertNoSharedRegistryMaps throws with both offending row ids named when two rows share a map object by reference (DATA-03, D-11)"
    - "Every scenario added here is individually mutation-proven per Phase 15 D-10"
  artifacts:
    - path: "scripts/probe-payload-resilience.js"
      provides: "two frontend-heatrisk-* scenarios, the PERF-01 overlap scenario, and the DATA-03 load-time assertion scenario"
      contains: "new-product-batch-fetches-issue-before-siblings-resolve"
    - path: "scripts/probe-lib/module-stubs.js"
      provides: "deferred-resolution fetch stub supporting manually-triggered promise resolution"
  key_links:
    - from: "the PERF-01 overlap scenario"
      to: "helper._fetch transport seam"
      via: "a deferred stub that holds a request open until the scenario releases it"
      pattern: "deferred"
    - from: "the DATA-03 scenario"
      to: "productRegistry.assertNoSharedRegistryMaps"
      via: "a scenario-local fixture registry, never the real PRODUCT_REGISTRY"
      pattern: "assertNoSharedRegistryMaps"
---

<objective>
Land the four remaining scenarios: D-03's two frontend gate/render scenarios, PERF-01's
concurrency-overlap scenario (with the new deferred-fetch harness infrastructure it requires), and
DATA-03's load-time assertion scenario.

Purpose: three of these four cover things no existing scenario in this suite can reach. The
frontend pair covers the exact defect Phase 15 shipped — a gate term and a render loop disagreeing
about what is displayable, producing a blank module with no message. The PERF-01 scenario has NO
analog anywhere in this codebase: there is no prior `Promise.allSettled` fan-out and no existing
harness helper can hold a request open, so it needs new infrastructure and full specification. The
DATA-03 scenario is a unit-style test of a validator rather than a poll-driven scenario.

Output: `scripts/probe-lib/module-stubs.js` gains a deferred-resolution fetch stub;
`scripts/probe-payload-resilience.js` gains four scenarios.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/buildomator/bm/4.5.5/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/buildomator/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/17-CONTEXT.md
@.planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/17-RESEARCH.md
@.planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/17-PATTERNS.md
@.planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/17-03-SUMMARY.md
@.planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/17-05-SUMMARY.md
@.planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/17-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-03 frontend gate and render scenarios, and the DATA-03 load-time scenario</name>

  <files>scripts/probe-payload-resilience.js</files>

  <read_first>
    - scripts/probe-payload-resilience.js `:2114-2150` — `frontend-advisory-only-is-not-an-all-clear`
      IN FULL. This is the direct structural analog for scenario 12: it renders a payload with one
      product active and everything else quiet, asserts the all-clear is NOT shown, and carries a
      control render proving the all-clear IS shown when the product is absent.
    - scripts/probe-payload-resilience.js `:538-605` — `noRiskPayloadWithAdvisory` and
      `noRiskPayloadWithHazards`, the builders that produce an otherwise-all-quiet payload. You need
      an equivalent for HeatRisk; extend one of these or add a sibling.
    - scripts/probe-payload-resilience.js `:1849-1880` — `frontend-total-outage-still-shows-the-outage`
      for the `loadFrontendModule()` / `renderDom(frontend, { config, spcrisk })` idiom and its
      `cleanRender` control.
    - MMM-SPCOutlook.js — the shipped `heatRiskDaysToRender`, gate term and render block from 17-03.
    - productRegistry.js — the shipped `assertNoSharedRegistryMaps` and `MAP_FIELDS` from 17-01.
    - .planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/17-CONTEXT.md D-01, D-02, D-03,
      D-11 verbatim.
  </read_first>

  <action>
    Append three scenarios.

    12. `frontend-heatrisk-only-is-not-an-all-clear`
        Pins D-03's gate term. Direct sibling of `frontend-advisory-only-is-not-an-all-clear`.
        - Build an otherwise-all-quiet payload (every SPC day NONE, no proximity, no fire weather,
          no ERO/WSSI/hazards risk, empty advisories) carrying `heatRisk.day3.category = 3`.
        - Config: `{ showHeatRisk: true, showMinorHeat: false }`.
        - Precondition guard: render the SAME payload with `heatRisk.day3.category = null` and
          assert that control render DOES contain `No Severe Weather Risk`. If it does not, some
          other term in the payload is already disqualifying the gate and the scenario proves
          nothing about HeatRisk. Throw with the control HTML if so.
        - Primary assertion: the real render does NOT contain `No Severe Weather Risk` AND DOES
          contain `Heat Risk (Day 3)`. Both halves — a gate term without a render is the other half
          of the same defect.
        - Mutation to prove RED: delete the HeatRisk gate term from `getDom`'s short-circuit.

    13. `frontend-heatrisk-minor-floor-is-not-a-blank-module`
        Pins D-03's shared-predicate discipline and D-01's floor. This is the scenario that would
        have caught Phase 15's defect class.
        - Same otherwise-all-quiet payload, `heatRisk.day3.category = 1`.
        - Arm A, `{ showHeatRisk: true, showMinorHeat: false }`: assert the render does NOT contain
          `Heat Risk (Day 3)` AND DOES contain `No Severe Weather Risk`. A blank module — neither
          the row nor the all-clear — is the failure this pins, and it is what a gate reading the
          raw category while the loop reads the floored one produces.
        - Arm B, `{ showHeatRisk: true, showMinorHeat: true }`: assert the render DOES contain
          `Heat Risk (Day 3)` AND does NOT contain `No Severe Weather Risk`. The same payload, the
          opposite outcome, driven only by the frontend flag — which also demonstrates the flag
          never needed to cross the wire (D-02).
        - Control assertion: with `category = 2` and `showMinorHeat: false`, the row renders — so
          Arm A's non-render is the floor at work, not a renderer that never renders.
        - Mutation to prove RED: make the gate term read the raw category (ignore the floor) while
          the render loop keeps the floor — the exact disagreement D-03 makes unrepresentable. If
          this mutation is impossible to apply because the two share one expression, that is the
          structural proof D-03 asked for: record that, then instead mutate the floor constant from
          2 to 1 inside `heatRiskDaysToRender` and confirm Arm A goes RED.

    14. `registry-rejects-shared-label-maps-at-load-time`
        Pins DATA-03 / D-11. This is a unit-style test of the validator, run once, not a poll-driven
        scenario — say so in its comment.
        - Import `assertNoSharedRegistryMaps` from `productRegistry`.
        - Assert it does NOT throw against the real `PRODUCT_REGISTRY` (the shipped state is clean).
        - Build a SCENARIO-LOCAL fixture registry — never mutate the real `PRODUCT_REGISTRY` —
          with two rows whose `valueToTier` (or another `MAP_FIELDS` name) is literally the SAME
          object reference. Assert the call throws, and that the thrown message contains BOTH row
          ids and the field name.
        - Control assertion: two rows with structurally identical but DISTINCT map objects do NOT
          throw — proving the check is object identity, not deep equality, and is not simply
          throwing on everything.
        - Second control: a row whose map field is `undefined` or `null` is skipped without throwing.
        - Record the coverage limit in the scenario's comment: identity cannot see a `toValue`
          closure that reads a foreign constant BY NAME rather than sharing the object by reference.
          Name `17-PATTERNS.md` §9's enumerated table as the paired recorded spot-check artifact
          D-11 requires alongside the assertion, and note that D-11 explicitly rejected the
          assertion alone as overstating its own coverage.
        - Mutation to prove RED: remove one field name from `MAP_FIELDS` (the one the fixture uses)
          and confirm the scenario reds because the shared map is no longer detected.
  </action>

  <verify>
    <automated>node scripts/probe-payload-resilience.js</automated>
  </verify>

  <acceptance_criteria>
    - Full suite green, pass count = 17-07 baseline + 3.
    - INDIVIDUAL MUTATION PROOF for all three scenarios, RED with a diagnosable message, restored
      green. Paste all three RED outputs verbatim.
    - For scenario 13, record which of the two mutation routes you used and why. If the
      gate/loop-disagreement mutation was structurally impossible to express, that is a positive
      finding about D-03 — record it explicitly, because it is the evidence that the decision was
      implemented structurally rather than by convention.
    - Scenario 12's precondition guard proven capable of firing.
    - Scenario 14's two control assertions both proven: distinct-but-equal objects do not throw, and
      `undefined`/`null` fields are skipped.
    - Scenario 14 does not mutate the real `PRODUCT_REGISTRY`: after the suite runs, a fresh
      `node -e "require('./productRegistry')"` still exits 0 and
      `assertNoSharedRegistryMaps(PRODUCT_REGISTRY)` still does not throw.
    - `git status` clean of every temporary mutation.
  </acceptance_criteria>

  <done>
    Three scenarios added and individually mutation-proven; the real registry is provably unmutated
    by scenario 14.
  </done>
</task>

<task type="auto">
  <name>Task 2: Deferred-resolution fetch stub and the PERF-01 overlap scenario</name>

  <files>scripts/probe-lib/module-stubs.js, scripts/probe-payload-resilience.js</files>

  <read_first>
    - scripts/probe-payload-resilience.js `:606-715` — `freshFetch` (`:606`), `throwingFetch`
      (`:619`), `installFetch` (`:633`), `httpResponse` (`:666`), `httpBodyReadFailure` (`:684`) and
      `installHttp` (`:699-710`). Note that `installHttp` records `{ url, headers }` into
      `helper._fetch.calls` at CALL time, before the handler runs — that recording order is exactly
      what makes issue-order observable and must be preserved by the deferred stub.
    - scripts/probe-lib/module-stubs.js — `loadNodeHelper`, `resetHelper`, `resetLogs`, `turfStub`,
      `logCalls` and the existing seam-reset machinery, so the new stub fits the file's conventions.
    - scripts/probe-payload-resilience.js — `heatRiskRoutes`, `ERO_URLS`, `WSSI_URLS`,
      `HAZARDS_URLS`, `HEATRISK_URL` and the advisory route URLs, so the scenario can enumerate
      every member's first request.
    - node_helper.js — the shipped `Promise.allSettled` batch from 17-05, including the member order
      and the `memberTimings` capture.
    - .planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/17-CONTEXT.md D-10 verbatim, and
      17-PATTERNS.md "No Analog Found" — this stub is confirmed new harness infrastructure with no
      existing helper to reuse.
  </read_first>

  <action>
    (a) Add `installDeferredHttp(helper, routes)` to `scripts/probe-lib/module-stubs.js` (or to the
    probe script's helper region if that fits the file's layout better — pick one and say which in
    the SUMMARY). Contract:
      - Behaves like `installHttp`: matches a URL by substring against `routes`, records
        `{ url, headers, at }` into `helper._fetch.calls` SYNCHRONOUSLY at call time, before any
        awaiting, so issue order is observable independently of resolve order.
      - Instead of returning the handler's response immediately, returns a promise that stays
        PENDING until the scenario explicitly releases it.
      - Exposes a control surface on the installed function:
          `helper._fetch.pending` — an array of `{ url, release(), reject(err) }` in issue order
          `helper._fetch.releaseAll()` — resolves every currently pending request
          `helper._fetch.releaseMatching(substring)` — resolves only requests whose URL matches
      - Any URL matching no route resolves immediately with the same 503 default `installHttp`
        uses, so an unrouted URL fails loudly rather than hanging the scenario forever.
      - Guard against the scenario deadlocking: if a scenario awaits a batch while nothing is ever
        released, the suite would hang with no diagnostic. Add a bounded safety timer that rejects
        with a clear message naming the still-pending URLs. Prefer a small, explicit timeout the
        scenario itself can set.
      - Comment that this exists solely because no existing helper in this suite can hold a request
        open, and that holding requests open is the ONLY way to observe concurrent issuance without
        depending on wall-clock timing — a timing-based assertion would be flaky on a Raspberry Pi
        and would not be a proof of structure.

    (b) Append scenario 15, `new-product-batch-fetches-issue-before-siblings-resolve`, pinning
    PERF-01 / D-10.
      - Setup: enable ALL SIX product toggles. Install `installDeferredHttp` with routes covering
        every product URL (ERO days, WSSI days, all six hazards layers, the HeatRisk identify URL,
        and both advisory discovery URLs), each returning a healthy quiet body once released.
      - Kick off `getSpcOutlook(...)` WITHOUT awaiting it — capture the promise.
      - Yield the event loop enough turns for every member's first request to be issued (a small
        number of `await Promise.resolve()` / `setImmediate` ticks; do not use a wall-clock sleep).
      - PRECONDITION GUARD: assert `helper._fetch.pending.length >= 2` and that the pending URLs
        belong to at least two DIFFERENT products. If only one request is pending, either the batch
        is sequential (the thing under test) or the fixture failed to enable multiple products —
        distinguish these in the message by listing the pending URLs and the enabled toggles, so a
        RED here is diagnosable rather than ambiguous.
      - PRIMARY ASSERTION: a LATER member's first request appears in `helper._fetch.calls` while an
        EARLIER member's request is still pending — i.e. it was issued before the earlier one
        resolved. State it concretely: the HeatRisk identify URL (member 4) is present in `calls`
        while the ERO day-1 URL (member 1) is still in `pending`. Under sequential awaits, member 4
        cannot have been issued at all until member 1 resolved, so this assertion is exactly the
        structural difference.
      - Release everything, await the `getSpcOutlook` promise, and assert the payload is intact:
        every product block present, `_stale` unset. Concurrency must not have cost correctness.
      - CONTROL ASSERTION: assert that with only ONE product toggle enabled, exactly one member's
        request is pending at the same yield point — proving the multi-pending observation above is
        caused by the batch and not by the harness issuing spurious requests.
      - SECOND ASSERTION (D-10's log): after settlement, assert the `Log.info` output matches
        `new-product batch settled in <n>ms` and that its JSON payload carries a timing key for
        every one of the six member ids. Success criterion 4 names timing AND logs.
      - Mutation to prove RED: revert the batch to sequential `await`s (three named awaits plus the
        for-loop). The scenario must go RED at the primary assertion with a message naming which
        member was expected in `calls` and was not.
  </action>

  <verify>
    <automated>node scripts/probe-payload-resilience.js</automated>
  </verify>

  <acceptance_criteria>
    - Full suite green, pass count = 17-07 baseline + 4. The suite must still complete in well under
      a second with no network I/O — record the runtime, and if the deferred stub introduced a real
      wall-clock delay, remove it (the safety timer must not fire on the happy path).
    - INDIVIDUAL MUTATION PROOF: revert the batch to sequential awaits, confirm scenario 15 goes RED
      with a diagnosable message naming the missing member, restore, confirm green. Paste the RED
      output verbatim. This is the single most important mutation proof in the phase — PERF-01 has
      no other evidence.
    - The scenario contains no wall-clock assertion: `grep -n 'Date.now\|setTimeout' ` within
      scenario 15's body returns nothing except the deferred stub's own safety timer, if you placed
      it there. Record the grep result. A timing-based proof would be flaky and would not be a proof
      of structure.
    - The precondition guard is proven capable of firing: temporarily disable five of the six
      toggles, confirm the guard fires with a message listing the pending URLs and the enabled
      toggles, restore.
    - The control assertion (single-toggle case) passes and is recorded.
    - The deferred stub cannot hang the suite: temporarily route one URL and never release it,
      confirm the safety timer rejects with a message naming the pending URL, restore. Record.
    - `git status` clean of every temporary mutation.
  </acceptance_criteria>

  <done>
    `installDeferredHttp` exists with a documented control surface and a non-hanging safety timer;
    scenario 15 proves concurrent issuance structurally and is mutation-proven against a reversion
    to sequential awaits.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| payload -> getDom -> innerHTML | The frontend scenarios render real payloads through the real render path |
| deferred stub -> real batch | The overlap scenario drives production code, so a stub defect presents as a product defect |
| scenario fixture registry -> real PRODUCT_REGISTRY | A scenario that mutated the shared registry would corrupt every later scenario in the same process |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-12 | Spoofing (false all-clear) | `getDom` no-risk gate | mitigate | Scenarios 12 and 13 pin both halves — a HeatRisk-only day must not show the all-clear, and a below-floor day must show it rather than rendering blank |
| T-17-04 | Tampering (data integrity) | shared registry maps | mitigate | Scenario 14 exercises `assertNoSharedRegistryMaps` against a scenario-local fixture and asserts the real registry is untouched afterwards |
| T-17-07 | Spoofing (false all-clear) | batch settlement | mitigate | Scenario 15 asserts payload integrity after concurrent settlement, complementing 17-05's synthetic-rejection proof |
| T-17-24 | Denial of Service | deferred stub | mitigate | Unrouted URLs resolve immediately with 503; a bounded safety timer rejects with the pending URLs named, so a mis-written scenario fails loudly instead of hanging the suite |
| T-17-20 | Repudiation (false confidence) | PERF-01 evidence | mitigate | Structural issue-order assertion with no wall-clock dependence, mutation-proven against a reversion to sequential awaits |
| T-17-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero new packages; the deferred stub is hand-written, not a library |
</threat_model>

<verification>
- Full probe suite green at 17-07 baseline + 4, still completing in well under a second.
- Four verbatim RED mutation outputs recorded, including the sequential-await reversion.
- The no-wall-clock grep result, the single-toggle control, and the safety-timer proof recorded.
- The real `PRODUCT_REGISTRY` provably unmutated after the suite.
</verification>

<success_criteria>
- `frontend-heatrisk-only-is-not-an-all-clear`, `frontend-heatrisk-minor-floor-is-not-a-blank-module`,
  `registry-rejects-shared-label-maps-at-load-time` and
  `new-product-batch-fetches-issue-before-siblings-resolve` all exist and pass.
- `installDeferredHttp` supports manually-triggered resolution and cannot hang the suite.
- Every scenario is individually mutation-proven.
</success_criteria>

<output>
Create `.planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/17-08-SUMMARY.md` when done.
Record: where you placed `installDeferredHttp` and why, the four verbatim RED outputs, scenario 13's
mutation route with its finding about D-03's structural enforceability, the suite runtime, and
before/after pass counts.
</output>
