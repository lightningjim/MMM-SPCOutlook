---
phase: 17-nws-wpc-heatrisk-parallelized-fetching
plan: 07
type: execute
wave: 6
depends_on: ["17-06"]
files_modified:
  - scripts/probe-payload-resilience.js
autonomous: true
requirements: [HEAT-01, HEAT-04]

must_haves:
  truths:
    - "Partial NoData renders no row and raises no badge, while all-days-NoData raises the badge — the two branches proven by separate scenarios, separately mutation-proven (D-04)"
    - "A tail gap is silence with no badge, while a Day-1 or interior gap raises the badge — the two branches proven by separate scenarios, separately mutation-proven (D-05)"
    - "A per-item data-age trip sets the warning badge but is excluded from _staleAsOf, so the badge never speaks for SPC data fetched moments ago (D-07, 16 D-15)"
    - "A cache hit recomputes the day offset against the current clock, so a HeatRisk category cannot drift one day per elapsed day — Phase 16's day-offset-cache-staleness class, proven not to have recurred"
  artifacts:
    - path: "scripts/probe-payload-resilience.js"
      provides: "six further heatrisk-* scenarios covering D-04's two branches, D-05's two branches, D-07 and the cache-hit day-offset contract"
      contains: "heatrisk-day1-gap-sets-stale"
  key_links:
    - from: "each new scenario"
      to: "helper._nowMs / helper._todayUtcMs"
      via: "pinned clock seams so day offsets and data ages are deterministic"
      pattern: "HEATRISK_NOW_MS"
---

<objective>
Land the six scenarios covering HeatRisk's compound staleness conditions: D-04's two branches,
D-05's two branches, D-07's per-item freshness asymmetry, and the day-offset-on-cache-hit contract.

Purpose: CONTEXT.md's D-04 and D-05 are the phase's two deliberately compound conditions — "failure
shapes get distinguished, not collapsed" — and CONTEXT.md states explicitly that both branches of
each must be mutation-proven SEPARATELY. A single scenario covering "gaps set stale" would pass with
a blanket rule implemented and would never catch a tail gap wrongly badging, or a Day-1 gap silently
not badging. The cache-hit scenario covers the one defect class this project has already shipped
once in Phase 16: a day key derived from a static remote timestamp against a live clock, cached as a
resolved value, drifting one day per elapsed day with no error and no badge.

Output: `scripts/probe-payload-resilience.js` gains six more `heatrisk-*` scenarios.
</objective>

<execution_context>
@/home/kcreasey/.claude/plugins/cache/buildomator/bm/4.5.5/workflows/execute-plan.md
@/home/kcreasey/.claude/plugins/cache/buildomator/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/17-CONTEXT.md
@.planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/17-RESEARCH.md
@.planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/17-PATTERNS.md
@.planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/17-04-SUMMARY.md
@.planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/17-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-04's two NoData branches</name>

  <files>scripts/probe-payload-resilience.js</files>

  <read_first>
    - scripts/probe-payload-resilience.js — the five `heatrisk-*` scenarios from 17-06 and the
      helpers `heatRiskRoutes`, `heatRiskIdentifyResponse`, `heatRiskCatalogItem`,
      `assertHeatRiskBlockIntact`, `HEATRISK_NOW_MS`.
    - scripts/probe-payload-resilience.js `:4939-5014` — the mutation-proven template with its
      precondition guard and control assertion.
    - .planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/17-CONTEXT.md D-04 verbatim,
      including its rationale and its accepted cost.
    - .planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/17-04-SUMMARY.md — the shipped D-04
      branch and its `_loggedHeatRiskAllNoData` flag.
  </read_first>

  <action>
    Append two scenarios. They are separate on purpose: D-04 is a compound condition and a single
    combined scenario would pass with a blanket rule.

    6. `heatrisk-partial-nodata-is-silent`
       - Fixture: seven catalog items; `Values` has real categories for some days and the literal
         string `"NoData"` for others — include at least one interior `"NoData"` so the scenario is
         not accidentally also a tail case.
       - Precondition guard: assert the fixture contains BOTH at least one `"NoData"` and at least
         one parseable 0-4 value, throwing with the actual `Values` array if not. Without both, the
         scenario is testing the all-NoData branch by accident.
       - Primary assertion: `out._stale` is NOT set, AND the `"NoData"` days have `category: null`
         while the real days carry their numeric categories. Both halves — silence AND no badge.
       - Control assertion: run the identical fixture with ONE `Values` entry changed so that every
         value is `"NoData"`, and confirm `_stale` IS set. This proves the primary assertion's
         "no badge" is a real branch and not a harness that never badges HeatRisk at all.
       - Comment D-04's rationale as fact: if any day resolves to a real 0-4, the identify call and
         the Mercator reprojection are demonstrably working, so a `NoData` elsewhere is genuine data
         absence — 15 D-04's "a clean zero-result is not stale".
       - Mutation to prove RED: make any `NoData` day set `anyStale` (the blanket rule).

    7. `heatrisk-all-nodata-sets-stale`
       - Fixture: seven catalog items, every `Values` entry the literal `"NoData"`.
       - Precondition guard: assert every entry in the fixture's `Values` is exactly `"NoData"`,
         throwing with the array if not.
       - Primary assertion: `out._stale === true` AND all seven days have `category: null`.
       - Second assertion, D-04's own reason for existing: assert the log was emitted exactly ONCE
         across two consecutive `getSpcOutlook` runs with the same fixture — the one-shot guard
         works. Use `logCalls`/`resetLogs` as the existing scenarios do.
       - Control assertion: a healthy all-real-values run leaves `_stale` unset — proving the badge
         above is this branch firing.
       - Comment why this branch exists: with every day `NoData` the module cannot distinguish an
         out-of-coverage location from a systemic break, and both warrant a signal. This is exactly
         HEAT-03's failure mode reading as "no heat risk anywhere, forever" — the false-negative
         shape this project exists to prevent. Note the accepted cost: a permanent badge for a
         genuinely out-of-coverage deployment, the same trade 16 D-14 took. Note also that the
         deployment coordinate (Norman, OK) sits solidly inside HeatRisk's CONUS coverage, so for
         this deployment the badge is a break signal, not a coverage signal.
       - Mutation to prove RED: remove the all-NoData check so the branch never fires.
  </action>

  <verify>
    <automated>node scripts/probe-payload-resilience.js</automated>
  </verify>

  <acceptance_criteria>
    - Full suite green, pass count = 17-06 baseline + 2.
    - INDIVIDUAL MUTATION PROOF for each scenario, RED with a diagnosable message, restored green.
      Paste both RED outputs verbatim.
    - Cross-branch independence proof, and this is the point of splitting them: apply scenario 6's
      mutation (blanket "any NoData sets stale") and confirm scenario 6 goes RED while scenario 7
      stays GREEN. Then apply scenario 7's mutation and confirm scenario 7 goes RED while scenario 6
      stays GREEN. If one mutation reds both, or if either mutation reds neither, the two scenarios
      are not covering distinct branches — restructure them. Record both matrices.
    - Both precondition guards proven capable of firing.
    - `git status` clean of every temporary mutation.
  </acceptance_criteria>

  <done>
    Two D-04 scenarios exist, each mutation-proven, and the cross-branch independence matrix shows
    each mutation reds exactly its own scenario.
  </done>
</task>

<task type="auto">
  <name>Task 2: D-05's two gap branches</name>

  <files>scripts/probe-payload-resilience.js</files>

  <read_first>
    - The two D-04 scenarios from Task 1, for shape.
    - .planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/17-CONTEXT.md D-05 verbatim,
      including its "consequence for planning" sentence requiring separate mutation proof.
    - .planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/17-04-SUMMARY.md — the shipped D-05
      branch, its `maxPresent` comparison and its `_loggedHeatRiskSequenceGap` flag.
  </read_first>

  <action>
    Append two scenarios. A gap here means NO CATALOG ITEM buckets to that day — structurally
    different from D-04's "a tile exists and its pixel is NoData". Build the fixtures so the two
    causes cannot be confused: every item present in these fixtures carries a real 0-4 value.

    8. `heatrisk-tail-gap-is-silent`
       - Fixture: five catalog items bucketing to days 1-5, none for days 6 and 7, every value real.
       - Precondition guard: assert exactly five items are present, that their computed day offsets
         against `HEATRISK_NOW_MS` are `1,2,3,4,5`, and that no item buckets to 6 or 7. Throw with
         the computed offsets if not — a fixture whose items land on unexpected days would test
         something else entirely.
       - Primary assertion: `out._stale` is NOT set; days 1-5 carry categories; days 6-7 are
         `category: null`.
       - Control assertion: the same fixture with the day-3 item REMOVED (making it an interior gap)
         DOES set `_stale` — proving the "no badge" above is the tail branch and not a gap check
         that never fires.
       - Comment the rationale: the live-observed response (7 catalog items, 6 distinct
         `idp_validtime` values) would fire a badge under any blanket gap-is-stale rule, which is
         evidence that tail gaps are routine mosaic rotation — the newest tile has not landed yet.
       - Mutation to prove RED: make any gap set `anyStale` (the blanket rule).

    9. `heatrisk-day1-gap-sets-stale`
       - Fixture: six catalog items bucketing to days 2-7, NONE for day 1, every value real.
       - Precondition guard: assert no item buckets to day 1 and that days 2-7 are all present,
         throwing with the computed offsets if not.
       - Primary assertion: `out._stale === true`, day 1 is `category: null`, days 2-7 carry
         categories. Assert days 2-7 are populated explicitly — a badge with an empty block would
         mean the whole poll was abandoned, which is a different branch.
       - Second assertion: also cover the INTERIOR half of D-05's compound condition in this same
         scenario by adding a second arm — a fixture with days 1,2,4,5,6,7 present and day 3 missing
         — asserting `_stale === true` and day 3 `null` while days 1-2 and 4-7 carry categories. Both
         arms exercise the `d < maxPresent` branch; asserting both is what proves the branch is
         position-aware rather than a "day 1 specifically" special case.
       - Control assertion: the tail-gap fixture from scenario 8 leaves `_stale` unset — proving
         this scenario's badge is the interior branch firing.
       - Comment the rationale: a missing Day 1 during a heat wave rendering identically to a Day 1
         with no heat risk violates this project's core value statement ("no false negatives")
         directly, and the sequence itself being broken means a day is being attributed by a rule
         that has just demonstrated it is unreliable.
       - Mutation to prove RED: invert the `d < maxPresent` comparison, or drop the interior branch
         so all gaps are treated as tail gaps.
  </action>

  <verify>
    <automated>node scripts/probe-payload-resilience.js</automated>
  </verify>

  <acceptance_criteria>
    - Full suite green, pass count = 17-06 baseline + 4.
    - INDIVIDUAL MUTATION PROOF for each scenario, RED with a diagnosable message, restored green.
      Paste both RED outputs verbatim.
    - Cross-branch independence matrix, exactly as in Task 1: scenario 8's mutation reds 8 and not
      9; scenario 9's mutation reds 9 and not 8. Record it. If a single mutation reds both, the
      branches are collapsed in the implementation and D-05 is not actually shipped as specified —
      stop and fix `_runHeatRiskProduct`, not the scenarios.
    - Additionally prove D-04 and D-05 do not collapse into each other: apply scenario 9's mutation
      and confirm scenarios 6 and 7 both stay GREEN. Record.
    - Both precondition guards proven capable of firing.
  </acceptance_criteria>

  <done>
    Two D-05 scenarios exist covering the tail and the Day-1/interior branches, each mutation-proven
    and each proven independent of the other and of D-04's pair.
  </done>
</task>

<task type="auto">
  <name>Task 3: D-07 freshness asymmetry and the cache-hit day-offset contract</name>

  <files>scripts/probe-payload-resilience.js</files>

  <read_first>
    - scripts/probe-payload-resilience.js `:4939-5014` — `hazards-data-age-sets-the-badge-but-not-the-age-figure`
      IN FULL. Scenario 10 is its direct HeatRisk sibling, including the warm-then-fail control arm
      that proves `_staleAsOf` can be populated at all.
    - scripts/probe-payload-resilience.js — the existing hazards day-offset-drift-on-cache-hit
      scenario added in Phase 16 (search the scenario names for `day-offset` / `cache`), which is
      scenario 11's direct analog.
    - .planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/17-CONTEXT.md D-07 verbatim,
      including 16 D-15's carried-forward asymmetry.
    - .planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/17-04-SUMMARY.md — the shipped D-07
      check, the deliberate absence of `_noteStaleEntry`, and the `_cacheHeatRiskTuples` whitelist.
    - node_helper.js `_isWithinStaleWindow` (`:1818`) and `_noteStaleEntry` (`:1853`) — for the
      control arm's warm-then-fail mechanics.
  </read_first>

  <action>
    Append two scenarios.

    10. `heatrisk-stale-item-sets-badge-not-staleAsOf`
        Pins D-07 and 16 D-15's asymmetry. Mirror
        `hazards-data-age-sets-the-badge-but-not-the-age-figure` closely — it is the same shape.
        - Read `maxAgeHours` from `PRODUCT_REGISTRY.heatRisk.maxDataAgeHours` rather than hardcoding
          12, so the scenario tracks the row.
        - Fixture: seven items, all real values, but ONE surviving item's `idp_filedate` set to
          `HEATRISK_NOW_MS - (maxAgeHours + 5) hours`. Every other item fresh. This is the
          per-item application D-07 requires — live capture shows `idp_filedate` varying by ~15
          minutes across the seven items, unlike the Hazards Outlook's per-layer uniformity, so an
          implementation reading only the first item's filedate must fail here.
        - Precondition guard: assert exactly one item is older than the threshold and the rest are
          not, throwing with the computed ages in hours.
        - Primary assertion: `out._stale === true` AND `out._staleAsOf` is `null`/`undefined` — the
          badge fires, the age figure does not. Give the failure message D-15's own reasoning: a
          data-age trip dragging `_staleAsOf` along would make the badge speak for SPC data fetched
          moments ago while describing a WPC file up to `maxAgeHours` old.
        - Control assertion: a genuine FETCH failure (warm the cache, then fail within the
          stale-fallback window, exactly as the hazards scenario does at its control arm) leaves a
          NUMERIC `_staleAsOf` — proving the primary assertion measures a deliberate omission and
          not a `_staleAsOf` this harness never populates.
        - Second control: place the aged `idp_filedate` on an item that is DEDUPED AWAY (a duplicate
          loser) and assert `_stale` is NOT set — proving the check runs on surviving items only,
          per D-07's "applied per item surviving HEAT-04's dedupe".
        - Mutation to prove RED: remove the per-item age check.

    11. `heatrisk-day-offset-recomputed-on-cache-hit`
        Pins the day-offset-cache-staleness class Phase 16 discovered and this phase inherits.
        - Arrange a warm cache: run once with a healthy fixture at `HEATRISK_NOW_MS`, confirming
          `helper._geoJsonCache` has an entry for `HEATRISK_URL`.
        - Precondition guard: assert the cache entry exists AND that its stored
          `result.tuples` contain NO key matching `/^day\d+$/` and no `dayOffset` key — the
          clock-independent whitelist actually held. Throw listing the stored keys if not. This
          guard is what makes the scenario non-vacuous: if a day key were cached, the assertion
          below would be testing the wrong mechanism.
        - Advance the pinned clock by exactly one day (`helper._nowMs = () => HEATRISK_NOW_MS +
          86400000`) and re-run so the fetch takes the CACHE-HIT path (serve a 304, or the same ETag,
          as the existing cache scenarios do).
        - Primary assertion: every category has shifted DOWN one day — what was `day2` on the first
          run is `day1` on the second, and `day7` is now `null` (its tile is now yesterday's and
          falls outside 1..7). A cached bucketed result would return the identical day mapping and
          fail this. Assert at least two specific day-to-category correspondences so a partial shift
          is caught.
        - Control assertion: assert the second run actually took the cache path — check
          `helper._fetch.calls` shows a conditional request (If-None-Match) or that no new body was
          parsed, whichever the harness makes observable. Without this the scenario could pass by
          simply refetching, which proves nothing about the cache contract.
        - Mutation to prove RED: change `_cacheHeatRiskTuples` to store the bucketed
          `{ day1: ..., day2: ... }` result and the runner to return it directly on a hit — i.e.
          adopt `_runArcGisDayProduct`'s cache shape. The defensive throw inside
          `_cacheHeatRiskTuples` should ALSO fire on this mutation; if it does, note that in the
          SUMMARY as a second, independent line of defence, and additionally prove the scenario
          itself reds by bypassing the throw (mutate the guard's regex too).
  </action>

  <verify>
    <automated>node scripts/probe-payload-resilience.js</automated>
  </verify>

  <acceptance_criteria>
    - Full suite green, pass count = 17-06 baseline + 6.
    - INDIVIDUAL MUTATION PROOF for both scenarios, RED with a diagnosable message, restored green.
      Paste both RED outputs verbatim.
    - Scenario 10's two control arms both proven: the fetch-failure arm leaves a numeric
      `_staleAsOf`, and the deduped-loser arm leaves `_stale` unset. Record both.
    - Scenario 11's cache-path control proven: record the evidence you used (conditional request
      headers or equivalent) showing the second run genuinely hit the cache.
    - Scenario 11's precondition guard proven capable of firing: temporarily add a `day1` key to the
      object passed to `_cacheHeatRiskTuples` and confirm either the guard or
      `_cacheHeatRiskTuples`'s own throw fires with a diagnosable message. Restore. Record.
    - `git status` clean of every temporary mutation.
    - Full independence sweep across the six scenarios added in this plan: each of the six
      mutations reds exactly its own scenario and no other. Record the 6x6 result as a compact
      matrix in the SUMMARY. This is the artifact that proves the compound conditions were shipped
      as distinguished branches rather than collapsed rules.
  </acceptance_criteria>

  <done>
    Six scenarios added across this plan, all individually mutation-proven, with the 6x6
    independence matrix recorded and every control assertion demonstrated non-vacuous.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pinned clock seam -> day-offset arithmetic | A scenario that reads the real clock drifts out of its own fixture's validity (16-REVIEW WR-05) |
| cached tuples -> recomputed day keys | A clock-dependent value entering the cache silently misdates every category with no error and no badge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-15 | Tampering (data integrity) | `_geoJsonCache` HeatRisk entry | mitigate | Scenario 11 advances the pinned clock across a cache hit and asserts the day mapping shifts; its precondition guard asserts no day-keyed field is stored at all |
| T-17-16 | Spoofing (false all-clear) | all-NoData / gap handling | mitigate | Scenarios 6-9 pin all four branches of D-04 and D-05 separately, with a cross-branch independence matrix proving no branch was collapsed |
| T-17-23 | Repudiation (misattributed staleness) | `_staleAsOf` | mitigate | Scenario 10 asserts a data-age trip sets the badge without contributing an age figure, with a fetch-failure control proving `_staleAsOf` is populatable |
| T-17-06 | Denial of Service | once-only log guards | mitigate | Scenario 7 asserts the all-NoData log fires exactly once across two consecutive runs |
| T-17-20 | Repudiation (false confidence) | the six new scenarios | mitigate | Individual mutation proof per Phase 15 D-10, plus the 6x6 independence matrix and per-scenario firing-capable precondition guards |
| T-17-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero new packages |
</threat_model>

<verification>
- Full probe suite green at 17-06 baseline + 6.
- Six verbatim RED mutation outputs recorded.
- The 6x6 mutation independence matrix recorded.
- Every control arm demonstrated non-vacuous.
</verification>

<success_criteria>
- `heatrisk-partial-nodata-is-silent`, `heatrisk-all-nodata-sets-stale`, `heatrisk-tail-gap-is-silent`,
  `heatrisk-day1-gap-sets-stale`, `heatrisk-stale-item-sets-badge-not-staleAsOf` and
  `heatrisk-day-offset-recomputed-on-cache-hit` all exist and pass.
- Each is individually mutation-proven RED with a diagnosable message.
- No mutation reds more than its own scenario.
</success_criteria>

<output>
Create `.planning/phases/17-nws-wpc-heatrisk-parallelized-fetching/17-07-SUMMARY.md` when done.
Record: the six scenario names with their exact mutations, six verbatim RED outputs, the 6x6
independence matrix, every control-arm result, and before/after suite pass counts.
</output>
