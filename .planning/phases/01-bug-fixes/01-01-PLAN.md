---
phase: 01-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - node_helper.js
autonomous: true
requirements:
  - BUG-01
  - BUG-02
  - BUG-03

must_haves:
  truths:
    - "SIGN indicators (Tornado, Hail, Wind) appear on the display when SPC issues a significant risk for Days 1-2"
    - "Day 8 shows Day 8 risk (not Day 7 risk) when extended mode is enabled"
    - "The Day 4-8 aggregate indicator (day48Risk) activates when any day in that range carries a risk"
  artifacts:
    - path: "node_helper.js"
      provides: "Fixed getSpcOutlook() with correct SIGN detection and Day 8/day48Risk return values"
      contains:
        - "label => label"
        - "day8Risk"
        - "day8ProbRisk"
        - "day8Sign"
        - "day48Risk = true"
  key_links:
    - from: "extractPolygons() toValue callback"
      to: "sigComparator"
      via: "evaluatePolygons()"
      pattern: "label => label"
    - from: "day8 return object"
      to: "day8Risk/day8ProbRisk/day8Sign variables"
      via: "object property assignment"
      pattern: "day8Risk"
    - from: "day48Risk assignment"
      to: "days 4-8 probRisk variables"
      via: "OR condition"
      pattern: "day48Risk = true"
---

<objective>
Fix three logic bugs in `getSpcOutlook()` inside `node_helper.js`:

1. BUG-01: Replace all 12 double-arrow `label => label => label` toValue callbacks with the correct identity function `label => label`, enabling SIGN detection for Tornado/Hail/Wind on Days 1-8.
2. BUG-02: Fix the Day 8 return object at lines 521-526 — replace the four `day7` variable references with their `day8` equivalents so Day 8 shows Day 8 data.
3. BUG-03: Fix line 458 — replace the broken 5x-`day4ProbRisk` OR condition and wrong `day4ProbRisk = true` assignment with the correct 5-day OR (`day4ProbRisk || day5ProbRisk || day6ProbRisk || day7ProbRisk || day8ProbRisk`) setting `day48Risk = true`.

Purpose: These are silent data failures — the module runs without crashing but produces wrong output. Fixing them is the prerequisite for Phase 2 (CIG tier support) which builds on corrected SIGN detection.

Output: `node_helper.js` with 16 targeted line edits. No new files. No architecture changes.
</objective>

<execution_context>
@/home/kcreasey/.claude/get-shit-done/workflows/execute-plan.md
@/home/kcreasey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-bug-fixes/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix BUG-01 — Replace all 12 double-arrow SIGN toValue callbacks</name>
  <files>node_helper.js</files>
  <action>
In `node_helper.js`, find every call to `this.extractPolygons()` that uses the double-arrow pattern `label => label => label` as the second argument (the `toValue` callback). Replace each occurrence with the single-arrow identity function `label => label`.

There are exactly 12 occurrences at these lines (current line numbers — verify before editing):
- Line 272: Day 1 Tor SIGN
- Line 283: Day 1 Hail SIGN
- Line 294: Day 1 Wind SIGN
- Line 317: Day 2 Tor SIGN
- Line 327: Day 2 Hail SIGN (orphaned call outside guard — fix in place, do NOT remove)
- Line 329: Day 2 Hail SIGN (inside `if(day2HailRisk > 0)` guard)
- Line 339: Day 2 Wind SIGN
- Line 361: Day 3 SIGN
- Line 408: Day 4 SIGN
- Line 419: Day 5 SIGN
- Line 430: Day 6 SIGN
- Line 441: Day 7 SIGN
- Line 452: Day 8 SIGN

The change at each site is identical — replace only the toValue argument:

BEFORE:
```
this.extractPolygons(geojson, label => label => label, (label,val) => label === "SIGN");
```

AFTER:
```
this.extractPolygons(geojson, label => label, (label,val) => label === "SIGN");
```

Do not change the third argument `(label,val) => label === "SIGN"` — it is correct.
Do not change any extractPolygons calls that use a different toValue pattern (e.g., `label => riskToValue[label] || 0` or `label => label === "" ? 0 : parseFloat(label)`).
Do not refactor, extract into a helper, or consolidate these calls — that is Phase 5 (QUAL-01) scope.
  </action>
  <verify>
    <automated>node --check /home/kcreasey/OneDrive/Projects/weather/MMM-SPCOutlook/node_helper.js && echo "SYNTAX OK"</automated>
  </verify>
  <done>All 12 extractPolygons SIGN calls use `label => label` as the toValue callback. No instance of `label => label => label` remains in the file. Syntax check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Fix BUG-02 and BUG-03 — Day 8 return object and day48Risk condition</name>
  <files>node_helper.js</files>
  <action>
Make two targeted fixes in `node_helper.js`. Both are within 70 lines of each other near the bottom of `getSpcOutlook()`.

**Fix A — BUG-03 (line 458):**

BEFORE (two errors: condition checks day4ProbRisk 5 times, assigns wrong variable):
```javascript
if(day4ProbRisk > 0 || day4ProbRisk > 0 || day4ProbRisk > 0 || day4ProbRisk > 0 || day4ProbRisk > 0) day4ProbRisk = true;
```

AFTER (correct: OR all five days, assign day48Risk):
```javascript
if(day4ProbRisk > 0 || day5ProbRisk > 0 || day6ProbRisk > 0 || day7ProbRisk > 0 || day8ProbRisk > 0) day48Risk = true;
```

Note: `day48Risk = false;` on the preceding line (457) is correct — leave it untouched.
Note: `day4ProbRisk` is used in the Day 4 return object below — do NOT add `day4ProbRisk = true` anywhere.

**Fix B — BUG-02 (lines 522-525):**

BEFORE (four `day7` references in the `day8` object block):
```javascript
day8: {
  "risk": day7Risk,
  "probRisk": day7ProbRisk,
  "sign": day7Sign,
  "color": riskToColor[day7Risk],
  "text": valueToFullRisk[day8Risk],
}
```

AFTER (all four `day7` references replaced with `day8`):
```javascript
day8: {
  "risk": day8Risk,
  "probRisk": day8ProbRisk,
  "sign": day8Sign,
  "color": riskToColor[day8Risk],
  "text": valueToFullRisk[day8Risk],
}
```

The `text` field already correctly references `day8Risk` — leave it unchanged.
  </action>
  <verify>
    <automated>node --check /home/kcreasey/OneDrive/Projects/weather/MMM-SPCOutlook/node_helper.js && echo "SYNTAX OK"</automated>
  </verify>
  <done>
Line 458: condition ORs day4ProbRisk through day8ProbRisk and assigns `day48Risk = true`.
Lines 522-525: day8 object uses day8Risk, day8ProbRisk, day8Sign, riskToColor[day8Risk].
No `day7` variables remain in the day8 return object block.
Syntax check passes.
  </done>
</task>

</tasks>

<verification>
After both tasks:

1. Confirm no `label => label => label` pattern remains: search the file for `label => label => label` — result must be empty.
2. Confirm the day8 return block: search for `day8:` block — all four property values must reference `day8*` variables.
3. Confirm day48Risk fix: search for line with `day48Risk = true` — it must exist and condition must reference day5ProbRisk through day8ProbRisk.
4. Syntax check: `node --check node_helper.js` must exit 0.
</verification>

<success_criteria>
- `node --check node_helper.js` exits 0 with no errors
- Zero instances of `label => label => label` in the file
- `day8` return object: risk, probRisk, sign, color all reference `day8*` variables
- `day48Risk = true` is set when any of days 4-8 has probRisk > 0
- `day4ProbRisk` retains its numeric value (not corrupted to `true`)
</success_criteria>

<output>
After completion, create `.planning/phases/01-bug-fixes/01-01-SUMMARY.md`
</output>
